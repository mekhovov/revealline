# v0.60.4 acceptance evidence curation

This delivery-only record preserves the root-accepted Recovery scope. `acceptance.json` is the exact original `root-acceptance.json`, renamed without byte changes. `native/root-observations.json` and every pinned screenshot/AX/DOM original are unchanged. Native input limits and the unresolved in-app native-select keyboard step remain explicit.

The finite original set includes the reviewed public-audit helpers and their predecessor/adaptation records, actual deployment/intake/authority originals, complete inventory/result/attempt rows, unchanged-after-audit authorities, public native observations, Archive19 admission expectations and source merge originals. The one HTTP 503 plus successful retry is retained. The first caller's absolute nested paths were correctly rejected; the original request/stderr and corrected relative request/stdout remain, with `intake-path-correction.json`. That caller preparation issue is not a product failure.

`README.preparation.md`, pending request templates and older handoff files keep their historical wording; `acceptance.json` and this report record the later scoped decision. No failed or pending original is rewritten into success. Candidate checkpoint receipts/logs under `planning/` support only the next-work counts for unreleased Motion and Team previews; they are not v0.60.4 source or public acceptance.

`retained-files.json` records exact original-to-destination hashes. No source TAR, game distribution, complete build, raw public payload bodies, dependency tree or unrelated cache folder was copied. The small original receipt ZIP is retained as an original, not repacked. All report/evidence bytes are kept under a 12 MiB limit. The only existing-file edit is the current opening paragraph of the delivery README; selectors, versions, runtime and historical reports are unchanged.

Full device, keyboard/controller, BFcache, export-byte, offline/migration and whole P03/P05/P18 acceptance remain open.
