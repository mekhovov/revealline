# Public v0.57.0 browser qualification — bounded results, acceptance blocked

Actual public route: https://mekhovov.github.io/revealline/releases/v0.57.0/site/game/ . Root independently verified deployment6458234240 / run34965929587 / controller74f289510617603c28768bb3f6bfd8e9f7a72cdd. This browser read frozen source b7db0134d4ede3452dc90b5d3f7ffb1491a0579b, version **v0.57.0**, channel **release-v0.57.0**, worker build8b7ab11efc3be749f519b50f4d4299dc2a203a61d4fd970bd3a62278bda7402b. The real inventory is608 files /55,383,385bytes. `00-public-identity.json` and `24-offline-verified.json` retain these browser observations. This does not accept P01 or a later patch release.

A confirmed P01 usability defect remains in this published version: active offline status appends the full optional-world list, expanding portrait content and pushing Stop/count below the initial viewport. Parent independently reviewed `01-preparing-portrait.png`; `04-progress-landscape.png` shows the controls after rotation/scroll. Parent owns a concise-status follow-up in another immutable release. The green native meter is separately deferred P05 styling. No tracked source was changed here.

## Actual checks

- The initial native boot/loading card is visible at390×844 (`00-title-portrait.png`). The actual ready title is `10-actual-ready-title-portrait.png`. Settings → Game data → Prepare produced measured real progress0/608 at12:03:51.623Z, then native focus/Enter on Stop returned focus to Check progress at12:03:52.379Z. Enter rejoined the same shared installation at23/608. `01`–`06` retain raw states, installing/active controller information and terminal result. The ready observation at12:04:53.706Z is62.083seconds after the initial0-file observation; this is an observed interval, not an exact internal install duration. No dependency gate, API monkeypatch or fabricated ready/cache state was used.
- Prepare completed all608 files, and an online Verify action also ran. Offline full Verify later independently completed608/608 with no missing/corrupt files and the correct build/scope (`24-offline-verifying.json`, `24-offline-verified.json`). No unregister, cache deletion, skipWaiting or shared-install cancellation was invoked.
- Ordinary Deploy prepared Pressure Lines and opened Missions; explicit mission Deploy started Orchard Crossing. An initial straight cut was caught and lost one life (`14`, retained ordinary gameplay). A subsequent native Down→Right→Up cut reached4.3% /1000, then Escape paused and saved (`15`). The save contains the exact still presentation pin and SHA53f1206a11a8791892f5c844c0641529acbc2c09c8d558676d0d801d72113850. Raw save strings are retained; no save was seeded or edited.
- The online browser PID26868/CDP64742 was closed and both were verified gone. A genuinely new browser PID37129/CDP64856 used the same retained profile. Its **actual** CDP-reported argv includes the loopback refusal proxy64855, `<-loopback>` bypass override and QUIC disabled. The task proxy has no upstream networking implementation. Fresh `.invalid` canaries produced actual GET502 and CONNECT502 events, not merely DNS errors. Public-root fresh uncached fetches before gameplay and after Team were rejected with contemporaneous public-host CONNECT502 events (`20`, `30`, `33`, proxy log).
- The new-process product navigation succeeded with208 actual `fromServiceWorker:true` responses, including its Document. Both `fromServiceWorker` and `fromDiskCache` are recorded true for many responses; only the actual browser attribution is claimed. No HTTP cache clear, service-worker bypass or page network emulation was used. The cold save bytes exactly match the online paused boundary (`15` vs `20`). Continue visibly verified the saved flight then remained paused at4.3% /1000 in two observations (`21`, `22`). Explicit Resume and another native cut reached11.5% /2700, then paused; exact picture pins remained equal (`23`). Paused screenshots show the overlay, which hides the underlying HUD; the raw DOM/save receipts establish coverage and score.
- Through ordinary Missions → mode link, Team loaded in the refused process. **Relay Yard** is the real arena in `26`/`27`, despite their historical `first` filename. Its initial clock0:00 and anchor objective identify the run. Both players left safe ground using D/Left and both Support actions (Q/Enter) entered recharge. Two paused observations match; explicit Resume advances the clock/charges. **First Connection** is proven only by `32-first-actual-*`: a verified visible setup boundary, explicit Start, clock0:00, both Support ready and objective “Reveal65% together”, then both players moving/Support recharge, stable pause and explicit Resume. This is bounded play, not a full win, rescue or controller qualification.
- Baseline `25-team-entry-baseline.json` is after Solo navigation/pagehide; endpoint `32-first-actual-resumed-paused.json` is before leaving Team. Suspended/library/packs raw values are byte-identical. In this profile the latter two sampled localStorage keys are absent; their null equality is not an IndexedDB audit. The suspended save and exact pins are substantive. No broader no-write claim is made.
- The refused browser exited before the proxy was stopped; both ports were verified closed. A third new browser PID44552/CDP65284 used the same profile, actual `--no-proxy-server` and no refusal flags. A new public-root uncached path returned an ordinary HTTP404 after624ms (`35-restored-network-probe.json`), establishing online HTTP restoration. The same save bytes remained. The early `35-restored-online-state.json` caught boot loading; the subsequent `35-restored-online-title.png` shows ready Continue. Final browser/CLI session are closed and the profile remains intact. Machine proxy settings were never changed.

## Retained failures and limits

1. Two initial about:blank CLI setup attempts did not honor the requested `--enable-automation` argument. Strict identity collection refused them; they never established qualification. Root approved direct owned Chrome launch with explicit argv and exact-CDP attachment. `agent-browser close` on an attached direct browser disconnected the CLI but left the actual PID alive (`16`); exact-profile-verified `Browser.close` then exited it (`17`). The retained direct launcher/close helper document the correction.
2. Pointer actions sometimes reported Done without activation, including scrolled Settings Close and Team Start. Native focus+Enter was verified against resulting UI. `09-title-ready-portrait.png` is actually still Settings, not ready title; `10` is the corrected title. `11-deploy-preparation.png` caught Missions after preparation rather than the intermediate busy state. `05-progress-scrolled-portrait.*` caught terminal ready while attempting to capture busy rectangles: zero Stop/count rectangles there are hidden terminal elements, not active-layout measurements or a proven focus regression. The actual active portrait failure is the screenshot/parent visual finding; no active portrait DOM rectangle was retained.
3. Team setup Enter-on-select attempts did not reliably change the arena and could start the existing choice. Later changing the select while an old run remained visible changed its title without rebuilding that run. `29`/`30` therefore do **not** prove First Connection, and `31-first-actual-start.json` still shows setup/old HUD after a nonactivating pointer click. Corrected proof is `32` only. This is retained as a possible P03 keyboard-routing follow-up: selected label must not be confused with active run identity. Exact observed sequence was focus arena select, ArrowUp/Enter (and later Space/Home/Enter), with Relay Yard still selected; subsequent browser select operation changed the value. No broad input redesign or physical-input claim follows from this sample.
4. The read-only protocol observer explicitly disconnected at12:09:38.681Z, after successful cold boot and before saved continuation/Team. The reason record is `collector-stopped: browser-connection-closed`; the cause was not independently diagnosed. `34-refused-direct-close.json` rechecked actual proxy/profile argv through the same browser endpoint immediately after Team; `34-refused-direct-exit-check.json` then verified the originally launched PID37129 and CDP64856 were gone. Thus end-of-interval endpoint/argv identity is retained despite the event-observer gap. Thus its208 worker responses support cold boot only. Team Document worker attribution and a continuous DOMStorage-event audit are **unavailable**. An initial consolidation assertion caught this gap; `consolidation-first-failure.txt` retains it. Zero recorded Team events was initially reported to parent, then explicitly retracted. Endpoint raw-byte equality remains valid, but cannot exclude write-and-revert behavior. Proxy refusal logging and actual process argv cover the longer measured HTTP(S) interval; these do not establish physical disconnection or all-protocol network isolation.
5. Optional post-restoration IndexedDB enumeration failed before evaluation due to a top-level-await syntax error (`36-storage-scope.json` empty; companion error note). It was not repeated and no IndexedDB no-write/original-byte database audit is claimed. The runtime's real offline verification and saved picture pins are retained separately.
6. Input was desktop browser automation of ordinary controls and native keyboard events, with a browser select operation for the corrected arena choice. Viewports390×844 and844×390 are bounded layout samples, not physical touch/gamepad/console testing. No completion of every Team objective, full optional-download backup, quota/corruption browser injection or cross-browser claim. Existing long Team layouts/map-art issues remain separate P03/P08A scope.

`review.json` contains27 successful bounded receipt checks and explicitly sets releaseAccepted=false. `summarize-evidence.py` derives them from immutable originals; its passing result is not a substitute for the confirmed usability failure or missing observer interval. `evidence-manifest.json` explicitly pins selected files. Browser profiles and browser caches are excluded from delivery; no tokens or unrelated user media are selected.
