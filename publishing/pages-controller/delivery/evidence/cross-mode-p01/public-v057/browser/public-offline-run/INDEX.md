# Public browser receipt index

Read `qualification.md` for the scoped result and every retained correction. P01 acceptance is blocked by active offline portrait status displacing Stop/count; root owns the next immutable patch. `review.json` records27 bounded checks and releaseAccepted=false.

- Identity/loading/progress/Stop/rejoin: `00`–`10`; `00-title` is boot, `09-title` is still Settings, actual ready title is `10`.
- Actual Solo gameplay/pins/save: `12`–`15`; failed first cut `14` is preserved.
- Whole-process exit, refusal proxy, cold worker boot and canaries: `16`–`20`, direct launch/exit files, `refused-cdp.jsonl`, refusal log.
- Paused Continue, explicit Resume/cut and full offline608-file verification: `21`–`24`.
- Relay Yard actual run: `25`–`27` (historical `first` filename is misleading; actual anchor objective proves Relay Yard).
- Selected-label/Enter attempts: `28`–`31`, retained and excluded as First Connection proof.
- Corrected actual First Connection and Team raw-save endpoint comparison: `32`; post-Team refusal probe `33`.
- Refused process/proxy cleanup and safe online restart/cleanup: `34`–`36`. Optional inspector syntax failure is preserved. All browser processes are closed; dedicated profile is retained and excluded from delivery.

Protocol attribution is limited to the208 actual worker responses during cold boot; the observer disconnected before Team. Raw-key equality is supported, continuous no-write audit is unavailable. End-of-Team exact browser endpoint/actual proxy argv is retained in `34-refused-direct-close.json`. Native keyboard/browser-select desktop samples are not physical-input or physical-disconnection proof.

`evidence-selection.json` is the explicit nonrecursive delivery list; `evidence-manifest.json` pins its exact files/bytes/SHA, including small setup/observer/proxy authorities. No profile/cache sweep or release acceptance claim.
