# v0.57.1 public offline correction

The offline progress visibility correction is accepted as an independently shipped P01 feature. [Play v0.57.1](https://mekhovov.github.io/revealline/releases/v0.57.1/site/game/). P01 remains open: the Library operation area and craft preparation inside Flight setup need corrections, and the remaining distinct loading hosts still require their bounded native checks. P02 has not started.

| Authority | Actual value |
| --- | --- |
| Game source | `4c85277ac7393eeeabab035387d4d4ae8734aba1` |
| Game source tree | `d3549d0efd15529f71f4cff9a940bdda5e84f3a6` |
| Publishing revision | `9ef75091762cfb9c84b02475ef79913a4476c651` |
| Publishing tree | `15963dede1cdcd977486d68fdc69714db9c02892` |
| Production run / deployment | `34976023839` / `6460112203` |
| Worker build | `2cb26c9e82fad6384b497699af2f1333bc0556bb1dc8beaadb3a08428f6aa62f` |
| Offline inventory | 608 files, 55,383,737 bytes |
| Full public inventory | 2,398 files, 636,567,742 bytes |

The exact-source six gates passed, including 4,187 tests, followed by production readiness/reproduction, ordinary build, artifact inspection and immutable release publication. These earlier authorities remain in the release qualification record; this directory records the subsequent deployed checks. The sole main-push publisher completed successfully. The full HTTP audit verified every decoded body, size, hash and content type, including the three hidden files and all 62 historical bridges. It had no skipped files, failures or retries; main and the deployment were unchanged afterward. See the [complete HTTP review](http/public-audit/completion.json).

The [native browser report](browser/run/qualification.md) records 59 scoped evidence checks. Actual installation showed the count and 44/47-pixel Stop control together in Standard portrait/landscape and Large/Plain/Reduced portrait/landscape. Stop detached observation; Check progress rejoined the same worker. Cached verification, desktop layout and native Chrome 200% zoom also passed their recorded scopes. Root independently rehashed both source evidence manifests and visually inspected the Large portrait, Large landscape and 200% terminal screenshots.

A real online flight was paused at 6.3% coverage, then restored paused in a new Chrome process with HTTP(S) access refused through a verified proxy. Explicit Resume and a native cut reached 14.3%, preserving its artwork pins. Both actual Team arenas loaded through the worker, accepted each player's movement and Support, and respected explicit pause/resume. Four sampled Solo localStorage keys remained byte-identical across the observed Team interval. This is not a Team victory, rescue-completion, IndexedDB no-write or physical-device claim.

The online collector's message-size failure is retained. Layout/install evidence preceded it; no continuity claim spans the gap. The later refused-network observer remained healthy. Failed selectors, a caught cut and an ineffective initial zoom shortcut are recorded separately from corrected checks. All owned browsers and the refusal proxy were closed; the test profile remains local. The separate Library trial's unexpected hidden page is not attributed to this run.

[copies.json](copies.json) pins 376 copied originals, totaling 11,641,634 bytes. It maps each durable path to its original cache path, size and hash. Original reports retain their original relative paths and timestamped scope; they are not rewritten into later acceptance claims. Browser profiles, payload archives, unrelated managed-policy snapshots and unfinished native families are excluded.
