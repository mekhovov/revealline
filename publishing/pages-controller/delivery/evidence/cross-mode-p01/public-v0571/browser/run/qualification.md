# v0.57.1 public core native browser qualification

Closed on 2026-09-15 at13:54UTC. The bounded core journey has59 passing evidence checks and no failed core check. This is not P01 or release acceptance: the known Library placement follow-up, other native families and independent full-body HTTP audit are separate.

The actual canonical release was `https://mekhovov.github.io/revealline/releases/v0.57.1/site/game/`, source `4c85277ac7393eeeabab035387d4d4ae8734aba1`, worker build `2cb26c9e82fad6384b497699af2f1333bc0556bb1dc8beaadb3a08428f6aa62f`, scope `/revealline/releases/v0.57.1/site/`, save channel `release-v0.57.1`. The actual terminal verifies608files/55,383,737bytes with no missing or corrupt files. `verified-public-identity.json` binds the parent’s independently fetched frozen-original comparison; full public-body audit is not inferred from this browser sample.

## Observed core journey

- Fresh online Chrome PID90207/CDP51302 had no initial worker/cache. Ordinary title boot and ready title are distinct (`003`, `007`). Settings→Game data→native Prepare began actual installation. Standard390×844 and844×390, then Plain+Large+Reduced844×390 and390×844 all show busy measured counts, concise label, meter and44/47px Stop within the viewport (`013`–`028`). Reduced Effects has no signal animation. Ordinary focus/rotation/tab actions determine scroll position; no post-action DOM/CSS injection or synthetic status was used.
- At172files, native Stop detached observation and moved focus to Check progress (`029`–`033`). Enter rejoined the same installation at173files (`034`–`035`). The worker continued to608 and activated. The separate full11-world optional note is readable after normal dialog scrolling (`055`/`056`); it no longer expands the operation label. A subsequent native Verify returned the exact terminal (`050`/`051`).
- Ordinary title Deploy visibly prepared the featured chapter, then Missions→Base game→First Signal→explicit Deploy started play. The first straight cut was caught, losing one life (`083`). One native retry cut reached6.3%/990/2lives and paused (`091`/`092`). This is real gameplay, with the exact still pin `c1aedf89bc3433dc1cb60998fa1e2563480a590c38586332f444c4c12c772fce` in the saved flight. No game state, save, picture choice or cache was seeded.
- The online process and its CDP endpoint were verified gone. A genuinely new Chrome PID816/CDP51531 reused the same profile through the task-only refusal proxy51530. Actual browser argv verifies that proxy, the `<-loopback>` bypass override and disabled QUIC. Actual HTTP and HTTPS canaries reached proxy GET502 and CONNECT502. Fresh out-of-worker-scope public-root fetches before Solo and after Team were rejected with contemporaneous public-host CONNECT502 records. This is observed HTTP(S) refusal, not a physical-disconnection or all-protocol claim.
- Cold navigation succeeded with373 total worker-attributed responses across the refused phase, including both Solo and Team Documents. Chrome also reports disk-cache attribution for many such responses; both facts are retained. The saved flight content matches the online pause, with only the normal `savedAt` update on browser close (`091` vs `098`). Native Continue restored6.3%/990/2lives and stayed paused in separated observations (`105`, `108`). Explicit Resume and another native cut reached14.3%/2240/2lives (`115`–`118`), with the exact presentation pins unchanged. Native offline Verify checked all608files (`128`/`129`).
- Through the ordinary Missions mode link, Team loaded from the worker. Relay Yard is proven by initial0:00 and the two-anchor objective (`143`). Both players exposed lines and both Support charges entered recharge (`149`); pause remained stable and explicit Resume advanced the clock. A verified visible setup boundary preceded selecting First Connection and explicit Start: fresh0:00, both Support ready and the65% objective (`161`–`166`). Both players then moved, used Support, paused and explicitly resumed (`172`–`179`). This samples play, not victories, rescue completion or every arena mechanic.
- Solo-channel baseline is after Solo pagehide/Team entry,13:50:37.190; endpoint is before Team leave,13:51:18.880. Four sampled keys totaling7,288bytes are identical, including a substantive6,492byte suspended flight and674byte library; the packs localStorage key is null. That41.69second interval has21 healthy scoped observer heartbeats, no recorded localStorage events and no observer anomalies (`team-storage-interval-review.json`). This is not an IndexedDB no-write audit.
- The refused browser exited before its proxy stopped. A third process PID7246/CDP51927 reused the retained profile with actual `--no-proxy-server`; an uncached public-root request returned ordinary404 after279.5ms, establishing online HTTP restoration. All four sampled Solo-channel values remained identical (`181` vs `216`). The final ready title is `217`. Browser processes, CDP endpoints and proxy were verified closed; the profile remains intact.

## Desktop and native zoom

Desktop1280×800 is retained (`187`, `192`). A Meta+Equal trial did not change zoom (`193`–`195`). Native Chrome Appearance→Page zoom selected200% (`198`, hashed unchanged relevant excerpt of`199`). On the actual product, DPR2 and640×400 CSS viewport with root CSS zoom1 demonstrate browser zoom (`201`), not CSS scaling. Busy geometry `208` has label/count/Stop within the viewport; screenshot`209` was captured after native verification completed, so it proves terminal visibility only. Native Chrome Page zoom was reset to100%, verified DPR1/1280×800 (`213`, `215`).

All these actions used the named core session and the exact restored process/page. `zoom-target-reconciliation.json` pins commands and matching owned-target navigation events. There was no `chrome://settings/help` command and no global OS input. Another owner’s unexpected About Chrome target is not diagnosed or attributed by this evidence. Unrelated inherited managed-policy text in three Chrome settings snapshots is retained cache-only and excluded from the distributable selection; exact zoom excerpts retain original SHA pins.

## Retained failures and limitations

The original online collector stopped at13:46:28.759 with `Max decompressed message size exceeded`. The actual worker progress, four layouts, Stop/rejoin and initial ready/Verify all precede the gap. Online cut/save receipts are endpoint evidence without continuous event coverage. The failed boundary checkpoint is retained, not recast as passing. The refused and restored phases use a new cache-only transport copy with compression disabled,32MiB frame bound, the existing8MiB oversized-message skip and5MiB evidence-log cap. Original collector bytes remain unchanged. The refused observer’s maximum heartbeat gap was2.222seconds and it ended only on intentional page close. No continuity claim crosses the online gap.

Other retained attempts: `032` screenshot typo failed before writing;`033` corrected the path. `036` wrong note-container selector failed;`037` succeeded. Untargeted scroll`042` did not move the dialog; targeted normal scroll`054` did. Missing Close selector`057` failed and pointer`060` reported Done without closing; verified focus+Enter`062`/`063` closed Settings. `058` did not bypass the still-open modal. Initial caught cut`083` is gameplay, not a loading defect. HTTPS canary`095` intentionally failed navigation. Filename labels do not override the actual DOM/time records; notably`209` is terminal, not busy.

Input consists of desktop native keyboard/browser-select actions and bounded viewport emulation, not physical touch/gamepad/console qualification. No network/storage/game APIs were replaced. No source, tracked files, builds, releases or Git state were changed. Two-second disk samples remained above1,561,305,088bytes free, comfortably above the mandatory512MiB reserve. New native families remain unlaunched pending separate authorization.
