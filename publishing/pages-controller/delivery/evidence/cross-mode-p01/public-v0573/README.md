# P01 public v0.57.3 — required recovery correction

**P01 is not accepted.** The immutable release passed its source gates and public-byte audit, but the final browser journey found a required recovery-flow defect. Returning from Export game data hides Export and Reload and loses focus while the same error still tells the player to reload. A new v0.57.4 patch addresses this; v0.57.3 bytes and tags remain unchanged.

Game source: `7945c6d07714d41cee9b6f848059b8dc18aed86c`. Publishing revision: `016b14af6b42d27de1f194c1ac5d48f7a0b3eaa9`. The [immutable release](https://github.com/mekhovov/revealline/releases/tag/v0.57.3) retains all nine original assets. The public audit verified all 2,451 files / 637,028,197 bytes; its one 503 response followed by a successful retry remains recorded.

`evidence.zip` preserves 552 verified members, including all 470 originals in the three final browser families, failed attempts, closures, original measurements, selected hosted/public records and parent reviews. The adjacent `manifest.json` maps original paths to archive members and digests; `record.json` records the byte-preserving assembly. `review.json` is the final decision for this release. Templates inside the archive retain their original preparation-time status and do not supersede that decision.

- **P1 — authoring and loading UI:** scoped checks passed with limits. Same-file selection, Undo, replay JSON, media preparation and focus were observed. A recorder timeout, unobserved trusted file-cancel event and actual download, recording-duration overrun, and partial textarea border overlap remain disclosed. Text and actions were readable; media playback does not establish physical audibility.
- **P2 — history, replay and offline:** native persisted Back preserved the saved flight and pinned artwork; Continue stayed paused until explicit Resume. Two replay-download completion events used the same path; only the final file's bytes survived and verified against the deterministic replay. Cold refused-network loading and all 609 offline files / 55,449,837 bytes verified, followed by successful network restoration. The recovery return defect blocks P01. The initial socket closure, proxy preflight error and separately reviewed continuation remain preserved.
- **P3 — transfer and original recovery:** authentic older-save Copy and Undo preserved their original channels. Original Verify, reset/reverify and the actual PNG download passed. Fast cancellation and complete layout of below-viewport actions were not observed; storage comparisons establish endpoint equality.

The existing [source qualification evidence](https://github.com/mekhovov/revealline/releases/download/v0.57.3/source-qualification-evidence.zip) is referenced without duplication: 23,954,493 bytes, SHA-256 `6260b4099024b7eee3fa5323f9998c3c706a0a9efb8577895bd15ddad5ba3f8e`. Browser profiles and runtime payloads are excluded. Source tests, browser interaction, modeled input and physical-device checks remain distinct evidence categories; this record makes no physical-device claim.
