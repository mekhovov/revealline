# v0.82.1 frozen desktop-browser acceptance

The original 590,546,744-byte distribution ZIP (`eb98abe0…`) was downloaded from draft release393811220, fully rechecked against the frozen manifest, and served unchanged at a dedicated localhost origin. These observations precede public Pages acceptance.

- Two-tab writer contention, ordinary First Signal win, preserved won results while preparing Next, and Relay Orchard start without a second Start passed.
- The native session-original download contains the exact frozen46,655-byte PNG. Additive Workshop restore preserved the prior original, and a second native export retained both originals byte-for-byte. A separate actual8,025-byte game-data JSON was downloaded; importing that JSON is outside this scoped flow.
- Offline preparation verified896 files. The local server was stopped, the isolated browser closed and reopened, and browser networking disabled before navigation. The cached v0.82.1 title opened and normal keyboard play won First Signal (52.2%,8,160points,3lives).

`browser-acceptance.json` pins every retained observation/download and documents incorrect-selector, offscreen-click and early-reload automation attempts. No game state injection or source modification was used. This is desktop Chromium functional coverage, not physical-device, whole-campaign or soundtrack listening approval. Actual public bytes and affected public play remain separate gates.
