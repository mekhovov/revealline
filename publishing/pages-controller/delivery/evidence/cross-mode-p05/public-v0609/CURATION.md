# Evidence curation

This delivery-only packet retains exact local originals from `.cache/p05-motion-public-audit-628e95da`, including pending and failed attempts. The cache preparation README is retained as `preparation-README.original.md`; the new `README.md` and `planning/progress-and-next.md` provide current prose. `root-acceptance.json` and `public-browser/observations.json` are exact coordinator originals. No screenshot bytes were exported or invented.

The retained helper paths, commands and absolute source-cache paths document where checks actually ran. They are not portable execution instructions and do not create new acceptance when copied. Public response bodies were streamed and hashed without saving payloads. The small original receipt ZIP, both extracted members, binding files, source metadata, original API responses, all request/result rows and before/after authorities remain included.

The root reviewed each intake, HTTP binding and refresh request before the corresponding operation. The HTTP audit ran exactly once; the row reconciler and post-audit observer/authority refresh each ran once. The first pre-deployment observer remains explicitly pending. The first Node self-check failed because its preparation-provenance record was absent; the record was supplied, the unchanged self-check passed, and both executions remain.

`retained-files.json` pins every file in this new directory except itself and records its copied source where applicable. It does not replace historical evidence or claim full phase completion. Current delivery prose is the only existing-file update; top-level source/version/configuration and all predecessor delivery records remain unchanged.
