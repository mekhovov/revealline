# Evidence curation

This evidence-only delivery records the accepted v0.61.1 scope. It changes no game version, selector, release asset or runtime file and requires no Pages redeployment.

`originals.zip` retains 216 finite files from the public-audit cache. `originals-index.json` records each member's original path, byte length and SHA-256. The archive includes the complete HTTP inventory, attempts/results, metadata and API originals, actual small receipt ZIP, source qualification, reviewed and unreviewed bindings, helper copies/diffs, execution records, native observations and root acceptance. Python bytecode is excluded. No game payload, browser profile, database or invented screenshot file is included.

The nearby acceptance, native observations, HTTP report, row review, live-authority result and source qualification are exact copies for convenient review. Their originals also remain in the archive. Compact wording and historical pending states inside those original records are deliberately unchanged; the new README explains the current scope in readable prose.

The planning checkpoint separately retains small, original package/CI records for queued v0.61.2–v0.61.6 candidates. It does not import their game payloads, declare their public acceptance or rewrite the v0.61.0 report. The initial v0.61.6 coordinator handoff and the subsequently inspected terminal run records have separate observation boundaries.

This package was created from controller `c7770eda1dbdf6d241c11496d80faa28dbcb9b2b` on an isolated branch. All prior delivery records remain unchanged. Verification checks every ZIP member's CRC, length, hash and source bytes, the direct copies, planning pins, relative links and the exact changed-path set. Source tests are not rerun for this documentation-only change.
