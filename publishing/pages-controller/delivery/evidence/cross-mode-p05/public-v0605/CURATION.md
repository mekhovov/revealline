# Evidence curation for scoped v0.60.5 Motion acceptance

This candidate retains 197 selected actual audit/browser originals as exact copies. `acceptance.json` is the unchanged root decision formerly named `root-acceptance.json`; every decision pin resolves to its original body here. Root's browser observations are attribution and scope evidence, not observations performed by this curation task.

The copied set includes the complete 2,790-row inventory, every response and every attempt, the independent row review, actual before/after hosted and authority originals, the nine prepared intake inputs, original small hosted receipt ZIP, actual source qualification, native text/screenshots and owned APNG fixture. Applicable audit scripts, bounded checks and their original failures/output are retained as evidence; they were not rerun. The predecessor helper tree, Python bytecode, duplicate source payloads and unrelated cache content are excluded.

`release-transfer/originals.zip` contains a finite 71-member selection (403,276 original bytes), plus its identical `selected-originals.json` index. Each member's original path, size and SHA-256 is recorded in `release-transfer/originals-index.json`. All six failed transfer result records, the final guarded success, final response diagnostics, source zero-POST/distribution one-POST receipts, publication originals and the release-event dispatch-skipped gate remain distinguishable. Full prior intakes remain preserved at the listed original locations; this ZIP does not claim to contain each prior intake's complete contents. The successful transfer does not establish why the network result changed.

No source TAR, runtime distribution ZIP, Pages artifact payload or production asset tree was copied. Small evidence ZIPs are receipt archives, not game payloads. Retained source/API originals can contain their original absolute cache paths; the curation map records the destination without falsifying those original bytes. No predecessor acceptance is reused as new v0.60.5 acceptance.

Authored files here are README, this curation note, the progress/phase report, the retained research recommendation and the inventory below. The root decision and source/browser/audit records remain unchanged. `retained-files.json` hashes every other file in this scoped folder and identifies copied versus authored/curated bytes; it excludes itself to avoid self-reference.

The delivery also updates only the opening current-status paragraph in the top-level delivery summary. Proposed execution/priority document edits are retained separately in cache for reconciliation after Settings lands; those two source documents remain byte-identical to the publisher base. Historical paragraphs and the full phase/content accounting remain preserved. Frozen source, version files, selector, game runtime and release assets are unchanged. No commit, staging, publishing or additional acceptance action is performed by curation.
