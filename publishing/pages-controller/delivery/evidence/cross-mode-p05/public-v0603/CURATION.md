# Public v0.60.3 evidence retention

This packet copies bounded original audit and browser records without rewriting them. `retained-files.json` maps original cache paths to retained paths, byte lengths and SHA-256 hashes. The preparation README becomes `README.preparation.md`; root's final scoped `root-acceptance.json` becomes `acceptance.json`, with identical bytes. Generated delivery, plan, curation and pin-review documents do not replace those originals.

The nine binding inputs, reviewed and unreviewed requests/bindings, full inventory/results/attempts, all three before observations, after observation, small original receipt ZIP, API failure-aware records and subsequent live-authority replies are retained. The initial HTTP503 and successful retry remain intact. Browser records describe actual keyboard routes and explicit limitations. Original source qualification metadata remains exact; its source TAR, distribution ZIP and qualification evidence ZIP remain external immutable release attachments. No game/media HTTP payload, browser profile, credentials or large release archive is copied.

Cache Python bytecode, temporary branch-preparation records and unaccepted planning preimages are omitted. Original helpers, their predecessor bodies, adaptation evidence, retained predecessor catalog and expected archive references are retained for provenance. Absolute source paths remain historical provenance, not portable commands. Never rerun old network requests without reviewing current identities. Archive03's existing admission packet is linked rather than duplicated.

The generated current progress report supersedes stale delivery summaries without altering historical source reports. Source-document summary corrections belong in the next qualified source change. Root's acceptance closes only the named supporting-tool correction; P03/P05/P18 and the Recovery candidate remain open. No physical-input, offline, zoom or full accessibility claims are introduced.

This PR contains delivery paths only. Its main push does not redeploy Pages; its ordinary publisher PR preview still runs. `curation-review.json` pins copied originals, generated documents and the changed delivery summary separately. The old v0.51 evidence index remains historical and is not repurposed.
