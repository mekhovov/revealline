# Archive 36 — retain v0.69.0 alongside v0.68.2

The append preserves 1,506 files / 653,831,399 bytes. The hosted extractor,
independent artifact reread and full public HTTP hash audit passed. Every prior
release path remains byte-identical; only the landing index changed among the
729 previously verified paths. The archive stays below its unchanged 800 MB cap.

The new archived edition passed scoped keyboard title, Start, Pause and Main menu
checks. Another tab owned saving, so that run is explicitly session-only. The
previous v0.68.2 evidence remains retained. See the native records for this append.

Retained v0.69.0 still has the picture-install conflict, cancellation feedback and
large-text short-landscape pause issue. v0.69.1 addresses the first two separately;
v0.69.2 remains a correction candidate for the last. Retention is not full phase,
physical hardware, audible-media or offline acceptance.
