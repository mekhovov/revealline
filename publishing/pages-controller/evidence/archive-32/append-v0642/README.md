# Archive32 — preserve HUD v0.64.2 alongside Replay v0.64.1

Root accepted the append at `2ff124892905eda499e7b1fb29729f092a253ecb` after the complete HTTP audit verified **1,421 files / 627,136,217 bytes** with zero failures or retries. All 709 prior release files and two support files remain byte-identical; only the archive landing index changed, and709 HUD files were added.

The exact [root acceptance](root-acceptance.json), [HTTP report](http-report.json), [independent reconciliation](independent-review.json) and [native record](native-observation.json) remain separate. The81-member original hosted receipt is retained byte-for-byte in [originals.zip](originals.zip), with hashes in [originals-index.json](originals-index.json). Fresh main/run/deployment/status originals are under `final-authorities/`; exact audit-run metadata is under `audit-authorities/`. Earlier Archive32 records remain unchanged under `initial-v0641/`.

Native checks cover keyboard pause/details/back/resume and loss on archived HUD, retained Replay win/picture/results, and archive-to-main navigation. The new HUD run was session-only. Physical devices, audio, offline, zoom and broader phases remain open. This retention admission does not accept the new Workshop main deployment.
