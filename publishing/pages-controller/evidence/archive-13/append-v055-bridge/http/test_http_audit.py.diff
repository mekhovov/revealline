--- prior1302/test_http_audit.py
+++ bridge1303/test_http_audit.py
@@ -47,8 +47,8 @@
 
     def test_pinned_inventory_validates_without_network(self):
         raw, rows = audit.pinned_inventory()
-        self.assertEqual(len(rows), 1302)
-        self.assertEqual(sum(item['bytes'] for item in rows), 624421715)
+        self.assertEqual(len(rows), 1303)
+        self.assertEqual(sum(item['bytes'] for item in rows), 624422286)
         self.assertEqual(audit.digest(raw), audit.INVENTORY_SHA)
 
     def test_full_body_hash_size_mime_and_final_url(self):
