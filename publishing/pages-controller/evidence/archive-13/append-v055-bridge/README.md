# Archive13 v054/v055 bridge successor admission

This directory retains original small evidence for archive controller `58b21ebeff3c2d8e8ca37d5b7764f8203b8824ed`, tree `8c1ef505de484c1175a7d8b2dff83fdd093ee699`, hosted run `34947667008` and deployment `6454910731`. Its source commit is `18f70dba10b6c7e24d3074e05931ecb32b4dcdae`. The two frozen games and their original ZIPs/metadata are unchanged.

The fresh full HTTP audit passed all 1,303 files / 624,422,286 decoded bytes, including the 571-byte `releases/index.html` bridge. All 1,303 requests passed on their first attempt with exact final URLs, MIME, length and SHA. The report, complete inventory, raw observations, actual hosted/deployment metadata and source tooling remain byte-exact under `http/`. `copy-verification.json` pins every copied member and records independent local reconciliation; no public body was downloaded again or retained here. The original audit location is `/Users/oleksandr.mekhovov/work/my_projects/go_test/.cache/round47/archive13-bridge-public/`; copied records retain their original absolute paths and times.

Root followed the actual title Release explorer links using Tab and Enter for both v054 and v055 after this bridge deployment. The canonical main catalog was visible. The v055 flight, 51.5% capture, explicit pause/resume and same-flight reload observations precede this bridge-only deployment; the frozen game bytes remain unchanged. The earlier 404 is preserved in `native/observation.json`. `browser-admission.json` states this scoped acceptance and its limits.

This successor does not claim a newly disconnected archived session. Historical v054 offline evidence remains bound to archive controller03986/deployment6453754021. P00 v055 offline evidence remains bound to main controllerda4459/deployment6454057759 and its main-origin scope. Their exact existing metadata is referenced separately, without copying or rewriting it. Neither historical record establishes a new archive055 offline run, physical-device test or disconnected cold start.

Only native text metadata is copied here. Root's six screenshot bodies remain at their original retained cache paths, with the original receipt's SHA/size declarations recorded in `native-retention.json`. This metadata preparation did not decode, independently hash or copy those images. The original receipt's relative image names resolve against its retained source directory.

`admission.json` is the exact four-field candidate admission object for the controller owner to insert into `publication.json`. It does not modify that selector. Catalog, allocations, README and prior evidence elsewhere remain unchanged. The prior 1,302-file byte pass and route failure remain historical; they are not used as this successor's fresh full audit.
