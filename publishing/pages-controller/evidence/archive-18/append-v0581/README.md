# Archive18 successor evidence — browser admission pending

The actual merged archive infrastructure is eaaf9844f7294be17bd97672de3c5b483e7c1620, tree868c0fbacb90b90b5082728393906e8cb8068416. Run35070855861 passed the original extractor's four checks, archive's twenty checks, two exact original ZIP extractions and independent artifact reread. Deployment6476315337 serves both v0.58.0 and v0.58.1. All original raw hosted JSON/log/ZIP receipt bytes are retained here.

The full streamed public audit passed 1,327 files /625,402,709 bytes in1,327 attempts with no failures, retries or skips. The independent reviewer reconciled every actual result and attempt, original lengths/hashes/MIMEs/encodings/finalURLs, the exact hosted inventory, and fresh archive main/run/deployment/status/Pages. Both original annotated tags and all nine asset descriptors per original release remained unchanged. `after-http-review.json` records this scoped pass. A second full-body audit was not run.

`http-session-completion.json` is the actual root-retained completion. `public-run-01.json` derives its exitCode0 and pins its original bytes. It contains only the final emitted output chunk, not a reconstructed complete stdout/stderr stream. Initial unchanged offline auditor tests were subsequently reported17PASS by root; this note does not fabricate missing raw local logs. The hosted4/20 test logs are original retained bytes.

All content in `evidence-manifest.json` is copied byte-for-byte from the stated source paths. Root authority aliases `http-report.json` and `expected-inventory.json` are exact copies of their original run/artifact members. Earlier `evidence/archive-18/` admission/evidence stays immutable. This addition is not yet a new accepted browser admission: `browser-admission.pending.json` deliberately has statusPENDING and no invented observations.

Root is performing actual canonical browser checks separately. Add their original evidence and a reviewed scoped browser-admission.json only when available; include both versions, exact new source/deployment and truthful per-version observations. Keep offline, listening, physical-input, P03 and broader game acceptance separate. The selector cutover to v0.59.0 requires the independent new game release/public gate and current publisher metadata.
