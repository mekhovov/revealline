# Archive19 v0.60.4 append — scoped retention acceptance

Root accepted archive merge `7e771ad4925031ed8a1f6e4294567f43c1457934`, tree `4335f2971b5754c53d090e2dca6f85219bde5246`, run `35244771283`, deployment `6507017787` / success status `18483946637`. Complete hosted extraction and public HTTP checks passed **1,377 files / 626,197,712 bytes**, with **zero retries, failures or skipped files**. All 690 previous public rows remain byte-identical, including 687 canonical v0.60.3 rows and the root/Explorer entries. Fresh before/after authority bodies match exactly. Original source/tag/ZIP/qualification identities are unchanged; no game was rebuilt or retagged.

Actual online UI verification initially hit a v0.60.4 dynamic module fetch failure. The original AX and empty console are retained, and its cause is unknown. One explicit keyboard Reload recovered, followed by Continue, Pause and Main menu. Semantic Explorer links then reached v0.60.3, whose existing Relay Orchard continuation retained 52.2% and 8,660 points and paused explicitly. All nine original native records, including both screenshots and the failure, are retained in `originals.zip`; the root decision binds them. Keyboard actions and semantic link clicks are distinct. This does not establish fresh profiles, cross-origin migration, new victories, offline, physical touch/controllers, full keyboard-only navigation, export/import, or completion of P03/P05/P18.

`browser-admission.json` derives its reviewed status from the exact copied root decision. The previous initial-v0603 evidence stays unchanged; superseded HTTP/inventory/browser records become reference pins so the successor has exactly one authority of each kind. Other 18 archives/admissions and all 75 catalog entries remain unchanged. The current selector stays v0.60.4; v0.60.5's metadata, final source qualification, immutable assets and public gate are a separate dependency.

The bounded originals ZIP contains exact API receipts, the small hosted receipt ZIP and members, complete audit results/attempts, native records, reviewed helpers and tiny fixture outputs, and the20 committed archive source blobs. It contains no game payload, expanded distribution or source TAR. Every member's original path, size and hash are in `originals-index.json`; root and row-review pins resolve inside the ZIP. Historical preparation/status wording is retained unchanged and does not override the final scoped decision. The previous corrected Release explorer bridge is byte-identical in committed source.
