# Archive14 append v0.59.0 — hosted and public preservation

The combined immutable archive is deployed and all 1,342 files / 625,582,144 bytes passed a complete bounded canonical HTTP audit without failure, retry or omission. PR2/actual merged infrastructure, deployment and hosted source identities are in completion.json. Original ZIPs were extracted only on the hosted runner; frozen games and historical rows were never rewritten. All659 prior v0.57.0 versioned rows remain exact.

Native browser preservation and final main-controller admission remain root-owned and pending. This lane opened no browser tabs and makes no new offline, input-device or P03 acceptance claim. The root/default selector was not changed.

Every original hosted receipt, helper adaptation, actual HTTP observation and fresh post-audit authority is retained here. evidence-manifest.json pins each copied body. The first auditor start failed before requests because its task-owned output parent was absent; the unchanged auditor ran after that parent was created. The first HTTPS push was refused for OAuth workflow scope; the existing project SSH transport was used normally without changing permissions.

Controller integration: keep all old archive14 evidence files/hashes; change its old http/inventory/browser evidence kinds to reference. Append new authoritative inventory/report/browser and a compact reference set, respecting the controller64-pin cap (existing45). Add v0.59.0 to archive14 allocations. A new root-written browser admission must bind this exact merged commit/deployment and cover both versions. No controller change was made here.
