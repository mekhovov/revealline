--- prior/tools/test_prepare.py
+++ current/tools/test_prepare.py
@@ -231,18 +231,18 @@
 
 
 class CommittedMetadataTests(unittest.TestCase):
-    def test_exact_v0571_metadata_and_source_qualification(self):
+    def test_exact_v0572_metadata_and_source_qualification(self):
         lock, inventory = locked_inputs(ROOT)
-        self.assertEqual((lock['expectedFiles'], lock['expectedBytes']), (662, 312555435))
+        self.assertEqual((lock['expectedFiles'], lock['expectedBytes']), (663, 312625191))
         self.assertEqual(len(lock['releases']), 1)
         release = lock['releases'][0]
-        self.assertEqual(release['version'], 'v0.57.1')
-        self.assertEqual(release['tagObject'], '8b2c1106606ab42d3246b1bc836c4e6b1ad1cb3f')
-        self.assertEqual(release['sourceRevision'], '4c85277ac7393eeeabab035387d4d4ae8734aba1')
-        self.assertEqual(release['sourceQualification'], {'sha256': '3f191263ad827c30ecd797d7dcf1186d03551fae9a8500611b91a5d72ce512bd', 'bytes': 52290})
+        self.assertEqual(release['version'], 'v0.57.2')
+        self.assertEqual(release['tagObject'], '67a6fc9c6d0dce662949cb06f55a7228f22967bf')
+        self.assertEqual(release['sourceRevision'], 'cc9f8ffcc3be246f53ca0df7f068476d8401d040')
+        self.assertEqual(release['sourceQualification'], {'sha256': '899192dd56c7baa3824d28fef5d53d9221b90014aa36269649e1eb08d9bbf5fb', 'bytes': 64747})
         rows = [row for row in inventory['files'] if row['path'].endswith('/source-qualification.json')]
         self.assertEqual(len(rows), 1)
-        self.assertEqual(rows[0]['bytes'], 52290)
+        self.assertEqual(rows[0]['bytes'], 64747)
 
     def test_changed_or_wrong_identity_qualification_refuses(self):
         import shutil
@@ -250,7 +250,7 @@
             root = Path(temp)
             shutil.copytree(ROOT / 'metadata', root / 'metadata')
             original = json.loads((ROOT / 'source-lock.json').read_bytes())['releases'][0]
-            path = root / 'metadata/v0.57.1/source-qualification.json'
+            path = root / 'metadata/v0.57.2/source-qualification.json'
             data = path.read_bytes()
             for change in ('bytes', 'source', 'gates'):
                 with self.subTest(change=change):
