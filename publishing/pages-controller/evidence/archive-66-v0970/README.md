# Archive66 public preservation proof

This evidence preserves published Reveal Line v0.97.0 at https://mekhovov.github.io/revealline-archive-66/. It does not promote a new game release.

The successful complete audit streamed all 1,121 pinned public bodies (594,897,841 bytes) with exact URLs, MIME, byte counts and SHA-256, zero failed attempts, retries or skips. Its directory is http/http-20260924T054015Z-ac1d3a74. Independent row reconciliation and fresh final GitHub authorities confirm the same successful deployed source after the audit.

An earlier full audit was interrupted by ENOSPC when flushing metadata after 1,054 complete passing rows. The exact partial attempt line, all original logs and authority snapshots are retained in http/http-20260924T053643Z-a94c6227. interrupted-audit.json explicitly rejects partial acceptance. The allocator responsible for transient capacity pressure is not established. The repeat used the unchanged auditor while other tasks held large RAM writes; its observed minimum free capacity exceeded 70 MB.

The helper differs from reviewed Archive65 only in exact archive identities and expected finite count. Its 26,755,072-byte worst-case metadata bound remains below the unchanged 28 MiB write cap and 32 MiB free-space guard. All 28 offline tests pass, and independent read-only review approved the exact delta. Payload bodies are never persisted.

The original HTTP artifacts and authorities are immutable observations. Native interaction is separate: an exact native observation and a combined reconciliation may be appended without reinterpreting byte verification as persistence, offline, physical-device or complete gameplay qualification. This branch makes no main publication/admission/selector mutation.
