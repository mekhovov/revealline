# Archive29 — accepted v0.61.24 append retention

**Root accepted scoped retention on 19 September 2026.** The root seal combines the completed byte audit, unchanged deployment/source authorities, independent evidence review and recorded keyboard play. Main Pages publication is a separate gate.

Actual archive main `9abddbdf00b8b47d93df63d2d465588d06fb0988`, tree `bf8b5d0784bcd466669465b40c7c73a855625c2b`, deployment `6536099795`, status `18550986076`, and [workflow 35415286512](https://github.com/mekhovov/revealline-archive-29/actions/runs/35415286512) deployed the original v0.61.23 and v0.61.24 release ZIPs without rebuilding either game. The hosted receipt and immutable source/asset chains are verified independently from public HTTP.

All **1,401 files / 626,907,986 bytes** passed in **1,401 attempts**, with no failed rows, retries or skips. Of 702 prior paths, **701 remain byte-identical**: 699 v623 release rows plus `.nojekyll` and `releases/index.html`. Only root `index.html` changes; 699 v624 rows /313,457,732 bytes are appended and no paths removed. Both original tags and all nine release asset descriptors remained stable before/after the public audit.

The [native original](native-observation.json) records actual keyboard entry, movement, Pause and explicit Resume for both editions. v624's 600×400 Pause retained its compact action layout and a legal 52.2% picture win (three lives,8,160 points,0:06). v623's legal win, initially misinterpreted post-win Escape, reentry/briefing steps and later actual Pause/Resume are retained without relabelling the first Escape as a pause. The archive root's visible link returned to the current release explorer.

The [originals ZIP](originals.zip) and [hash index](originals-index.json) retain exact metadata, original receipt ZIP, all public rows and attempts, authority snapshots, helpers, and the root's pre-execution held-checkout path correction. No audit ran with the incorrect path. Original preparation/readiness and hosted-completion records retain their original pending/no-public-acceptance statuses; the later root acceptance is recorded separately.

No physical touch/controller, actual zoom, audio, offline, focus-loss or cross-version save isolation is qualified here. Screenshots were inspected inline only. The source game's wider native UX, content and hardware phases remain open. The main selector stays v0.61.24; this retention acceptance does not select or qualify v0.62.0.
