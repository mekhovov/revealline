# Archive30 — v0.62.1 append retention admission

Root accepted this append on 2026-09-19 from complete public-byte reconciliation, scoped native keyboard observation, and a final actual archive-authority refresh. Main `950e30e6c891345ac5b68e2e796b54c0e64828f5` / tree `fe6cba995c640068d2f9f21805bd815c46831163` deployed through run `35431113584`, deployment `6538726400`, success status `18557058965`. The original v0.62.0 and v0.62.1 release ZIPs were used; no historical game rebuild occurred.

The read-only hosted audit ran at utility `4dd68d07c5c984e78336b21a694a4e245d0934e9` / tree `e4f67d5d786f0882c9d481a69781418e9ae18319`, run `35432038925`, job `105868262875`. Its exact 684,381-byte original artifact `10580988860` has SHA-256 `13f2f45017600a2c202610a80385b5f614d7a8ae92fd081ed0df9becc476b656`. Independent local reconciliation verifies all 1,408 paths / 627,086,405 bytes, 1,408 attempts, zero failed attempts and zero retries. All 702 prior v0.62.0 release rows and two root-support rows are identical; root `index.html` alone changes (+79 bytes). The 703 new v0.62.1 rows total 313,522,906 bytes.

Root's 1280x720 keyboard session covered v0.62.1 Continue into Relay Orchard, continuous steering, Pause/explicit Resume and a 52.2% cut (8,660 score, relay 1/1). Both version titles, archive index return and current-explorer return were observed. This was explicitly session-only because another tab owned saving. It does not accept persistence, migration, physical controller/touch, audio, offline, Team win/Next or broader phases. Inline screenshots were inspected without exported screenshot hashes.

`originals.zip` and its hash index preserve exact hosted metadata, helpers, result rows and every attempt, original receipt ZIPs, before/after and final archive authority originals, root acceptance and native observations. Private local transport stderr is excluded; its digest remains in the retained attempt receipt. The complete original hosted artifact contains only empty stderr members; nested content was checked for raw signed URLs. Reports retain their original HTTP-only acceptance boundaries.

The earlier `initial-v0620/` files, including failed local audit and native launch/recovery observations, remain unchanged. Publication demotes their authority kinds to references and points current archive authority to this append. Other archives are unchanged. This patch leaves the current selector, catalog, source qualification and testing routes unchanged for root's separate v0.63 integration.
