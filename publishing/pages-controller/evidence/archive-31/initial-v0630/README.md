# Archive31 initial v0.63.0 admission

This evidence admits the archive at `f476c311e0050b47d7408db51d57239fba7bece6`, tree `d144cc112088882211bb1b5e04df298f4f4a78be`, deployment run `35434694566`, deployment `6539360227`, successful status `18558487091`.

The original HTTP report covers all 711 files and 313,557,913 bytes with zero failed attempts or retries. The unchanged root acceptance combines that byte audit with scoped desktop keyboard archive routing and session-only play. Its limits remain: no new persisted save, physical controller/touch, listening, offline, Team victory/Next, or whole-phase acceptance. The native note was recorded before the HTTP audit; its then-pending label is preserved as history, and the later root acceptance records completion.

The originals ZIP includes the byte-identical hosted result artifact, individually addressable HTTP rows/attempts, before/after/final authority records, utility run/artifact originals, and root acceptance/native records. The index pins every ZIP member. Private outer transport diagnostics are excluded. Empty stderr members inside the unchanged original hosted artifact are preserved. Selected contents and nested ZIPs were checked for signed URLs/tokens and nonempty stderr. No game payload is copied.

`browser-admission.json` is the derived publisher-format record; it references the original evidence rather than inventing another browser run. Main v0.63.0 remains selected. All 30 prior allocations, admissions, evidence, tags and frozen release bytes remain unchanged.
