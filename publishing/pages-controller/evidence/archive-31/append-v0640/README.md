# Archive31: append v0.64.0

Accepted infrastructure `2ff1b312872857cf04b485f9c987a26c0432f356` (tree `8c3ca7ed1accb04afcb8a92063bb412e05ecc833`) retains v0.63.0 and adds the exact v0.64.0 freeze. Pages run `35441173594` deployed as `6540533321`; the fresh success status is `18561156073`.

The independent hosted HTTP audit passed every **1,420 files / 627,122,610 bytes** in 1,420 attempts, without failures, retries or skipped rows. All 708 prior release rows and both root support rows remain identical. The existing index changed by 79 bytes; 709 new rows were added. This remains below the archive's 800,000,000-byte budget.

`originals.zip` is the unmodified 687,706-byte hosted audit artifact, including every HTTP row/attempt and before/after authority. `originals-index.json` records its exact members. The separately copied acceptance, native observation, independent review, current authorities and audit reports also retain their original bytes. Historical initial-v0630 evidence remains pinned as reference material.

The scoped native observation covers keyboard entry, result/retry, Pause/Help/explicit Resume and the historical entry. It does not close Team earned win/Next, held rescue, physical devices, audio/offline, persistence or P03/P05/P08/P18. The browser-admission wrapper binds that scope to the accepted deployment; HTTP hashes alone are not gameplay evidence.
