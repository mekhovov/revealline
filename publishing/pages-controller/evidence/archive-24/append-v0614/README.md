# Archive24 — retain v0.61.3 and v0.61.4

Root accepted this append on 18 September 2026 at 11:06 UTC. The full public audit matched **1,394 files / 626,715,708 bytes**, with zero public failures or retries. All **697 prior non-index rows** remain unchanged. The archive index adds v0.61.4; its cohort adds 696 rows. The previous initial-v0613 evidence remains immutable.

Archive commit `2ead35c57856ef8948058a1df21788383a6385ed`, tree `1669010d35488be79a3f60df7ff1be64db846c40`, run `35336603609` attempt 2 and deployment `6522823756` bind the actual receipts and public audit. Attempt 1 failed when GitHub returned HTTP 500 before the first v0.61.3 ZIP extraction. Its original log is retained. Root retried the same source once; attempt 2 extracted and verified both originals, then deployed successfully. Both immutable tag refspecs are explicit in the workflow.

Root exercised both retained versions in the actual desktop browser. v0.61.3 passed keyboard Start, Pause, explicit Resume and a legitimate First Signal win at **52.2%, 8,160 points, 0:08, three lives and three stars**, then Release explorer returned to the main catalog. v0.61.4 passed Missions → First Signal → Deploy, the same score and result, View picture → Results → Main menu, and Settings/About Escape and Return navigation. Some v0.61.4 actions used targeted keyboard locator presses; this does not establish complete Tab-order coverage. A locator role mismatch was corrected and retained as an automation observation, not a gameplay defect.

v0.61.4 displayed a session-only saving warning because another game tab owned saving. GitHub Pages project paths share an origin. This retention acceptance does not establish durable saves, migration, physical controller/touch support, audible listening, offline recovery, export/import, performance or complete phases. Screenshots were inspected inline; no exported screenshots are claimed.

Enemy Workshop is shipped in both frozen manifests. The earlier exclusion/source-only claim is incorrect; current documentation corrects it while preserving historical records. The existing public v0.61.4 initial-dialog focus gap remains outside this archive acceptance and awaits its separate correction release. Original game and media bytes are unchanged.

The evidence bundle keeps root acceptance, native observations, every HTTP result and attempt, before/after authorities, the bounded four-member receipt intake, the first hosted failure, exact retry records and local helper checks. Native observations and the full byte audit remain separate evidence.
