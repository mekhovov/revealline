# Standalone video poster workshop

This source-only preview makes the [owned video acquisition API](../../game/video-poster.mjs) reviewable before story/schema integration. It opens no profile, media database, pack library, game or audio engine. It never saves an assignment or awards a picture. The existing still workshop and released game remain unchanged; this new directory is not yet in the build allowlist.

From this worktree, run the repository server and open the explicit authoring URL:

```sh
node scripts/game-cli.mjs serve --port 8930
```

Open `http://127.0.0.1:8930/authoring/video-poster/`. File URLs and failed module imports show local-server guidance with disabled source controls. Do not substitute a browser automation surface or inject application state to make a check pass.

Choose an owned MP4/WebM, inspect its dimensions/duration/hash, and select a time using the numeric field or slider. **Capture poster** is explicit. The preview displays requested seek, the reported frame timestamp (when available), approximate playhead, PNG dimensions/bytes/SHA and source SHA. **Download exact PNG** is a visible native link; asynchronous capture does not click it. The prepared Blob URL stays available for retry until source replacement, successful recapture, Clear, page exit or disposal.

Capture failure/cancellation retains a previous poster from the same source. Changing source clears old preview/download authority immediately; a late old decoder cannot adopt into the new source. Native file-dialog blur only neutralizes controller input. Hidden/page-exit cancellation prevents late publication; BFCache return requires choosing a source again. No video `play()` is called. A silent fixture cannot establish audible-track behavior.

Keyboard Tab/arrows/Enter use the shared game navigation; text/number/file controls retain native browser editing. The controller can edit the time slider and activate Capture/Download; its ordinary neutral gates prevent held input reclaiming focus after native input. File selection and text entry still require browser/OS keyboard or touch support; this page does not provide a virtual keyboard or claim controller-only OS file picking. Escape/East cancels pending work or uses the visible Back route. Phone and physical-controller comfort need actual testing.

## Owned diagnostic MP4

```sh
node authoring/video-poster/generate-fixture.mjs --out .cache/owned-video-fixture-1
```

The generator requires a new directory under this worktree’s ordinary `.cache` tree, rejects existing symlink ancestors and refuses to overwrite an output directory. It never installs dependencies or downloads source media. These path checks do not claim protection against a hostile concurrent directory-renaming process. When an existing ffmpeg is available, it creates a six-second 640 × 360 / 10 fps H.264 clip with color/test patterns and a burned frame number. On macOS without ffmpeg, it writes an owned Swift source into the cache output and uses the existing AVFoundation SDK to generate six colored cards, a moving white bar and explicit frame/time labels. Both routes omit audio. Generation logs, encoder/tool version, exact command, source-script SHA and resulting MP4 bytes/SHA are recorded. Encoder output can differ across systems; generated bytes have their own recorded identity.

Actual local fixture generation used **Swift 6.3.3 / AVAssetWriter**, because ffmpeg was unavailable. The result in `.cache/video-poster/owned-fixture-1/` is **75,767 bytes**, SHA-256 `5abb074b8a740a3305b1d8c6b84466e46eb51d853e2faacad592182999b2c1ea`. Native AVFoundation inspection reports **6.0 seconds, 640 × 360, one video track and zero audio tracks**. A separately extracted native frame at requested/observed 2.0 seconds shows a blue card, “FRAME 20 / 60”, “TIME 2.0 SECONDS” and the moving bar. Its PNG was inspected visually. This native fixture check does not qualify the browser adapter or promise the browser will choose the same frame.

## Verified source-browser handoff

The actual 1280 × 720 desktop source browser at `http://127.0.0.1:8930/` completed native file selection, capture, PNG download and a manual still-picture handoff on **runtime commit `3178d69e53af0c54b20faadeae3f0ebc20e01315`**. The main checkout retains `.cache/round43/video-poster-browser/receipt.json` (4,716 bytes), SHA-256 `09f8aad04f9e3f9d289d0f7730a3d5340d1bcb22e65ed56c4053da84c0b2d9b4`, with nine artifacts and six exact runtime input hashes. The later generator-only guard commit `bbc5fe171f1696484a54160b8cd5d4deabc8f870` changes neither the observed runtime nor the owned MP4.

| Actual action                                                     | Observed result                                                                                                                                                                   |
| ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Choose the owned silent H.264 MP4                                 | 6 seconds, 640 × 360, 75,767 bytes; source hash matches the preserved original.                                                                                                   |
| Request 2.0 seconds and download through the native link          | Observed frame 2.0 seconds; exact 42,937-byte PNG, SHA-256 `4dd29af01b42684788af11915c1501d25cb4bcd900b679b85e845f9502c0714b`. The downloaded image shows FRAME 20 / TIME 2.0.    |
| Request 2.05 seconds                                              | Observed frame 2.0, approximate playhead 2.049999; the same exact PNG. The requested decimal is not frame evidence.                                                               |
| Request the valid endpoint 6 seconds                              | Observed final frame 5.9, playhead 6; valid 30,954-byte PNG.                                                                                                                      |
| Request 7 seconds                                                 | Visible out-of-range refusal, without clamping or replacing the prior 6-second request’s poster.                                                                                  |
| Manually upload the downloaded PNG in the existing still workshop | The exact 42,937-byte original was validated, source declarations completed and an explicit assignment saved to First Signal / FPV. Assignment itself granted no gameplay reward. |
| Start the assigned ordinary game and cut Down                     | Real win: 52.2%, score 8,160, three lives and displayed 0:03. View picture displayed the captured FRAME 20 / TIME 2.0 artwork. No error/warning logs were observed.               |

This qualifies one supported desktop H.264 source and an exact manual PNG handoff. It does not establish audible-track behavior, WebM or a codec matrix, physical phone/controller support, frozen/public delivery, stored video, original-video backup, automatic video assignment or story playback. The workshop keeps the video only in its owned in-memory source; the existing still workflow adopts the downloaded PNG as a separate still with `story: null`.

## Repeat or extend the check

Use the normal in-app browser skill/client and native file chooser. Preserve the actual MP4, each downloaded PNG and requested/observed/playhead evidence; never inject application state or treat a modeled link click as a completed download. [Copyable authoring requests](../prompts/video-poster.md) cover the verified path and pending boundaries.

1. Reproduce metadata, 2.0/2.05/end/out-of-range capture and native download with an owned diagnostic source. Inspect exact bytes, full aspect and frame labels. For a still handoff, explicitly upload that PNG to the edition-matched still workshop, record its provenance and Save assignment; start a normal flight to qualify the reward. Do not re-encode the downloaded PNG or store the MP4 through `.rlmedia`.
2. Remaining browser checks include initial time zero, repeated-link download, cancellation during real decode, changed-source/close/BFCache behavior, corrupt/unsupported/oversized input and explicit no-frame-callback fallback. Code tests cover these boundaries; the completed browser receipt does not claim them all.
3. Test an owned clip with an audible track while confirming the adapter never starts playback, then other intended codecs and variable-frame-rate sources. A missing paused-frame callback must time out visibly; do not start hidden playback to avoid it.
4. Test actual narrow layouts, large text/zoom, focus and a physical controller. Keep browser/OS file-picker and text-entry limitations explicit. Desktop keyboard success is not device certification.

Retained pre-guard checks passed **133/133 affected tests on Node 22** and **51/51 acquisition/workshop cases on Node 20** in `.cache/video-poster/workshop-check-1/verification.json` (SHA-256 `af822386bc4df2106f33b73840bc8cbde5932df439e45de1c450f09b8e603f03`). The later generator guard adds one regression to the now-15-case [workshop test file](../../game/test/video-poster-workshop.test.mjs); its two selected generator tests pass separately on Node 22 and Node 20, with lint/format/syntax checks and independent review in `.cache/video-poster/generator-guard-check-1/verification.json`. The [primitive tests](../../game/test/video-poster.test.mjs) and host tests model DOM/media capabilities; the actual browser receipt above is separate. No whole-suite, immutable build or public-release gate has run for this branch. The [integration contract](../../docs/video-poster-acquisition.md) keeps still identity, exact original/PNG inventories, shared media budgets and optional stories separate.
