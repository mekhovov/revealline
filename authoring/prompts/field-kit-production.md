# Field Kit release asset requests

Use these requests with the selected slot's generated Studio brief attached. They are prose workflows, not new CLI commands. The studio's **New variation**, **Edit this asset** and **Replace collection** actions supply exact current geometry, usage, palette and file requirements.

## Revise one current drone

Open the current release collection in Asset Studio and select `player.scout.detailed`. Copy its Edit prompt. Create a north-facing original variation with the same approved silhouette, actual pivot and all four motor anchors. Retain crisp transparent pixel clusters and separate procedural propellers. Keep the uploaded original, prepare the exact 64×64 PNG, then compare current/draft at native size and in the real board preview. Record dimensions, binary alpha, occupied bounds, anchor checks and readability at 20/24/32 CSS pixels. Export the immutable `.rltheme` only after review; the upload does not publish it.

## Complete a reveal composition

Choose the next missing composition from `authoring/design-atlas/reveal-production-plan.json`. Use its distinctive subject and exact owner/frame entries, plus the current effective art contract. Generate one original scene per subject with the built-in image tool. Preserve the original unchanged, full prompt, actual dimensions and SHA-256. Prepare separately declared crops/frames through `scripts/prepare-field-kit-reveals.mjs`; retain the production palette, integer cluster scale and exact export dimensions. Review gallery and partially revealed board contexts. Add a new presentation revision; never rewrite an earned or saved original.

## Adopt a reviewed collection into a release

Import the complete `.rltheme` into a local studio workspace. Check every selected member and all affected screens, including Standard/Large text, focus/disabled/error states and return navigation. Compile into a fresh review directory with `node scripts/compile-presentation.mjs --bundle candidate.rltheme --out .cache/new-field-kit-review`. Validate byte-preserving transfer, current/history previews and runtime bindings. Adopt only the reviewed artifact files into the next version, update the related skills/prompts and run the exact-source release workflow. Keep failed evidence and immutable previous releases.

## Extend a game screen

Update the named game screen using the existing Field Kit component and navigation contracts. Keep the existing dialog, input, pause, save and recovery handlers. Use a translation key for each new label; use Exo 2 interface text, Plex numbers and Handjet only for large display accents. Resolve icons, frames and artwork through registered slots. Review default/focus/pressed/selected/disabled/error states, Standard/Large layouts and the actual return-focus path. Update the per-screen asset evidence and reproduce the immutable production ledger before the release gates.

For release/archive catalogs, change the controller's `release-catalog.mjs` and shared catalog stylesheet. Preserve canonical Play, ZIP and manifest URLs. Copy only the verified presentation graph and notices into `catalog-ui/`; never rewrite a frozen game's HTML, worker, manifest or artwork. Verify total Pages bytes and the historical-controller fallback.

## Replace a readable font

Select the actual font role in Asset Studio and copy its Edit brief. Prepare a licensed real WOFF2 candidate with retained source and license notices. Verify the shipped bytes contain English and Ukrainian, including Ґґ Єє Іі Її and both apostrophes ʼ ’. Keep role weights/axes and test Continue flight / Продовжити політ, Mission complete / Місію завершено, І l 1, О O 0, 01:24 and 85%. Compare Standard/Large text and 200% native browser zoom. Numeric roles need stable equal-width digits. Record the actual decoder, cmap, layout and byte-budget results; do not render interface copy into an image.

## Replace an audio cue or loop

Select the relevant audio slot and copy its medium-specific Studio brief. For a cue, create a distinct original electronic contour with a clean envelope: mono 48 kHz, 16-bit PCM WAV, at most one second, no leading silence or clipping. For music, create a restrained seamless 4–16-bar instrumental loop, retain lossless WAV source and prepare OGG or MP3 within the selected slot budget. Preserve any approved custom production brief, event meaning and timing. Report actual codec, channels, sample rate, duration, peak, bytes and rights. Audition explicitly with existing music and test Stop, mute and volume. Mark file inspection, native playback observation and listening judgment separately. Export the reviewed immutable collection; a browser upload does not publish it.

## Refine an existing reveal without rewriting it

Use the current prepared scene as an edit reference and preserve its approved landmarks, composition and subject. Record the full effective edit prompt and returned original in `reveal-source-revisions.json`, with the exact previous source identity and next sequential revision. Keep all previous originals and prepared exports. Run the deterministic reveal preparation, inspect the new derivative at native size, 240 CSS pixels and the exact owner's partially revealed board, then append the presentation successor. Signal 06 revision 2 is the worked example: calmer sky clusters replace stippling while the station, path and valley remain recognizable. New attempts use the successor; saved flights and earned pictures retain their original pins.

## Revise victory presentation

Refine the current artwork-led result panel using the completed attempt's loaded picture, existing score/medal text and registered Field Kit frame. Keep missing managed pictures unavailable and explicit legacy originals intact. Preserve Next, View picture, Retry, appearance and menu actions, the award/save order, and focus when returning from the full viewer. Inspect a natural win on desktop, 390×844 portrait and 844×390 landscape with Large text; at 200% zoom, keep independently scrollable details/actions and 44 px targets. Record actual visible bounds and a failed case before a correction. Do not manufacture awards or claim physical-device verification from browser emulation.

## Revise a procedural recipe

Recipe-only rotor, trail and effect slots request a reviewable change to registered source code or supported theme tokens and related body anchors. They do not accept PNG uploads or executable content in `.rltheme` bundles. Recipe metadata stores only a registered ID; new behavior or a new recipe requires implementation and review. Keep prior source/revisions and validate the actual runtime, pause and reduced-motion behavior. All three copy actions retain the current custom production brief.
