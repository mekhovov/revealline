# Live still-picture and recovery prompts

These are copyable prose requests, not new `prompt.py` IDs. The [live host](../../docs/flight-pictures.md), [still workshop](../../docs/still-media-workshop.md), [identity model](../../docs/media-presentation.md) and [shared delivery contract](../../docs/feature-delivery-workflow.md#live-pictures-earned-originals-and-paired-recovery) define implemented APIs. The integrated P5 still source and bounded browser gates pass; frozen/offline/public P5 remain open, and v0.31 remains the verified published milestone. See [current evidence](../../docs/feature-delivery-workflow.md#current-still-integration-evidence). A rendered prompt is not executed asset, browser or recovery evidence.

## 1. Preserve saved A after assigning B

“Use an owned original A for one exact installed chapter/map/theme in the edition-matched still workshop. Preserve A's raw bytes, source declaration and actual dimensions; preview then Save assignment. Start an ordinary flight, make a legal unfinished cut and Pause/save. Retain the v3 session and pin hash. Assign distinct original B as a new immutable revision without modifying A. Restore the saved session and confirm it stages A before replacing the current run; explicitly Resume its saved heading and queued turn. Verify A through capture/loss/recovery and compare the authoritative replay. A fresh Retry may choose B. Test Immediate and Grid + buffer separately. Keep the mask, actors, rules and score unchanged.”

## 2. First-earned A remains A

“Complete a real awarded map with A, then assign B and replay without editing profile state. Check the first-earned receipt still names A, victory uses the attempt's selected image, and Collection thumbnail/full picture use the receipt's exact original. Inspect reduced effects and keyboard/controller Back. Reopening, replaying the gallery, media restore and repeated rendering must not mint another clear, score or receipt. Distinguish an installed later attempt's B from the historically earned A; do not resolve the receipt through today's assignment.”

## 3. Removed pack, retained original

“After earning managed A, export required game data and originals, then remove only the installed pack through its ordinary UI. Open Collection: use the verified retained owner and original A without installing content, inventing a current theme or granting rewards. Show that replay requires the exact pack to be installed. Reinstall that exact pack and verify the same earned image and ordinary replay availability. Compare actual campaign keys and normalized effective levels/rosters; matching names or a Gentle suffix cannot establish ownership. Legacy pack-embedded art keeps its historical fallback rules.”

## 4. Missing file and stale decoding

“Exercise a controlled missing/corrupt original using a test fixture or an isolated recovery origin, preserving the valid source backups. Restoring saved managed A must remain paused, leave the working run/backdrop/slot intact and explain original recovery. Collection must not substitute generated or newly assigned artwork for missing earned A. Restore the real `.rlmedia` original and explicitly retry. Also start a delayed decode, switch context or hide the page, and confirm late completion cannot paint or start a newer flight. Returning focus never resumes. The fresh-attempt authored-art option is explicit and cannot bypass a saved managed pin.”

## 5. Actual native downloads and fresh-origin recovery

“Use two owned stills and an owned MP3 with a mixed playlist. Save a real pinned flight and earn a picture. Through visible native controls, prepare/download Export game data, `.rlmedia` originals and `.rlsound` separately; retain actual files, byte sizes and hashes. Do not count preparation or a modeled anchor default as disk completion. In a separate origin with no test-state injection, restore reviewed originals first, then game data and the soundtrack. Select the intended assignment policy explicitly. Verify saved A, first-earned A, original PNG/MP3 equality and ordered/shuffle transport; re-export the relevant binary inventories. Repeat a cold prepared-offline restore/play/audio journey after stopping the controlled server. Preserve source data and report browser, modeled control, hardware and listening evidence separately. Do not delete the source profile to manufacture a clean result.”

## 6. Shared manager and exact backup ownership

“Review a picture import while another tab changes media or installed content. Use the host's single rich-v3 manager, short locked generation-checked commits and authenticated originals. Preserve audio bytes and music-only intent on failed/cancelled picture work. Capture branded metadata before creating the backup's synchronous identity resolver; use only its prospective included packs and exact retained owners. Normalize effective levels/rosters before comparison. Test quota refusal, source-lock changes and old reader VersionError without deleting data or downgrading storage. Training/practice writes no new pins/awards and preserves its parent's saved A.”

## 7. Future story extension — design only

“Plan a later story that reveals a selected ending/key frame, plays a bounded story from its beginning and returns to that exact earned still. Separate requested seek time from the observed decoded-frame timestamp, poster choice from the story segment, original upload from runtime derivative, and playback from the one-time award. Record `requestVideoFrameCallback` mediaTime where available, honestly label approximate fallback evidence, and preview/save the exact captured PNG bytes; never assign the requested time as proof of a decoded frame. Include stale scrub/cancel, variable-frame-rate input, keyboard/controller/touch Play/Pause/Skip/Replay, static reduced-motion reward and separate audio ownership. Follow the [next-story design and primary references](../../docs/media-library-design.md#next-video-and-story-slice--proposed). Keep this a versioned proposal: current `revealline-media-presentation.v1` requires `story:null`; the still workshop does not import video/GIF, scrub frames or provide a complete story player.”
