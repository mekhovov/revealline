# Verify Studio inspection-position restoration

Select an asset and filter by search, screen, state, kind and readiness. Reload the page, then open Design atlas and return with browser Back. Confirm the selected slot and valid filters return after workspace loading, while document revision and local save generation remain unchanged. Repeat at a narrow portrait width: idle foreground focus should return to the Inspector; on desktop it should return to the selected row.

Exercise removed slots/options, malformed and future history records, unavailable history access, and foreign history fields. Preserve other owners and the current URL without adding history entries. During delayed startup, move focus to Studio guide and verify completion does not steal it. Confirm selection rejected for unsaved pixel/upload work does not overwrite the remembered accepted position.

Keep this record limited to inspection position; do not persist editor drafts, theme choice, animation playback or player data through it. Treat the 160-character search bound as a cosmetic limitation, not permission to truncate the actual input. Report native browser evidence separately from modeled cases and run the complete host tests plus release gates on the composed source.
