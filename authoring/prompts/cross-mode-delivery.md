# Cross-mode delivery prompts

Unjoined picture failure: “Select an installed mission whose approved artwork cannot be prepared before pressing Start. Require a nonempty visible inline error beside its recovery actions, with no Cancel for completed work. Preserve active restore/import/theme owners and ignore stale prewarm results. Return from Export directly to the same message, focus and Reload; inspect the complete message and all button hit targets in portrait/short landscape at Standard and Large text. Distinguish immediate visibility from deliberate scroll reachability; no clipped controls or reduced touch targets.”

Recovery continuation: “Exercise the complete advertised recovery sequence: writer-required picture → Export game data → Library → Back → Reload saved profile. Closing the secondary dialog must restore a visible invoker and keep the settled recovery actions without another Check picture request. Preserve only the exact current picture owner/run/theme; actual pending cancellation, retry, replacement and page retirement still invalidate it. Verify checkpoint, pins, reads and writes, then qualify actual native return focus separately from the modeled test.”

Pending launch cancellation: “Hold actual picture preparation after Start, Retry and confirmed Restart. Deliver Escape through the mounted menu listener and Back through the standard controller router. Both must invoke the existing launch Cancel action, restore Start focus and prevent late success or failure from resuming or changing the saved checkpoint, selected content or art. Preserve modal/course precedence, other preparation owners and terminal recovery. Keep a failing baseline and distinguish modeled routing from native/public input.”

Native evidence: “Pin the installed recorder transport and retain bounded post-open error/close diagnostics. Bind observers to the actual development or release channel; a development server need not provide frozen build metadata. Interactive drivers need a live stdin/PTY. Account for browser-owned profile links without following them, and record data/blob URL hashes instead of embedded payloads. Preserve each actual downloaded file before a repeated same-name download can overwrite it; distinct completion GUIDs do not guarantee distinct retained files. Hash large pack rows instead of returning embedded images in storage observations. Read the actual verifier API (`verifyReplay` returns `match`), and compare saved timestamps separately from checkpoint and picture identity. Keep recorder failures, corrected interpretations and product failures distinct; never rewrite an old failure into a pass.”

Use the [active phase register](../../docs/cross-mode-execution.md). Preserve earlier immutable records, player ownership and historical simulations. Each prompt applies only after its prerequisite phase is accepted; examples do not claim implementation.

## Continue a phase

“After changing navigation, run the existing host regressions through the current visible routes. Open Missions → Prepare before setup selectors and Library → Expansion packs before campaign Play. Keep Title Start, explicit Title Continue and paused generic Load distinct. Preserve unfinished-flight confirmation, exact campaign/art readiness, checkpoint/storage/replay assertions and late-completion ownership. Reproduce failures, distinguish missing sparse-checkout inputs from product failures, and retain original failed logs. Do not remove guards, raise timeouts, skip tests or change gameplay to restore an obsolete hidden-control shortcut.”

“Inspect the current branch, latest main and `docs/cross-mode-execution.md`. Continue the active unaccepted phase. Reconcile related work without touching unrelated hunks. Show exact evidence for its blocking acceptance, fix failures, update this contract and relevant skills, synchronize the next unused version, commit related changes, qualify the exact source, freeze and deploy through the existing Pages controller. Verify all public bytes and ordinary play before marking accepted or beginning the next phase. Report physical devices separately from modeled/browser checks.”

“If a production revision, theme transfer or combined player library fails a JSON capacity check, measure its actual UTF-8 document and wrapper bytes first. Preserve the declared limits and every immutable revision. Count escaped strings, keys and punctuation accurately without executing untrusted getters or toJSON. Before expecting a trusted fixture to be rejected, independently assert that its standalone collections fit and its serialized union exceeds the actual quota; correct an under-limit fixture with valid bounded fields or history, preserving identity, unchanged-input and zero-write checks. Do not change runtime or budgets to restore an obsolete test expectation. Verify exact-limit/one-byte-over inputs and the complete production review, replacement, export and import journey; retain the actual failed source receipt, keep earlier focused results scoped, and qualify the corrected commit afresh.”

## Add or change asynchronous presentation

“Read `docs/presentation-loading.md` and the active A01–A56 inventory. Use the shared operation presenter with the host's existing operation identity, cancellation and storage owner. Announce before the first delayed boundary; show truthful stages and measured counts only. Keep Back/Cancel visible, retain the original audio gesture, and make old completion/finally callbacks unable to alter a newer screen, focus, draft or run. Join existing picture preparation instead of repeating it. Distinguish stopping observation from aborting a durable/shared operation. Add delayed, cached, failure/retry, close/reopen and stale completion checks; await real operation completion or the shared elapsed-time readiness helper instead of counting immediate callbacks. For real installed originals, align initial and later equivalent picture waits through one documented local fixture allowance; audit restoration, recovery and replacement entry points while retaining explicit timeout-boundary overrides. Preserve shared ordinary-host defaults, runtime deadlines and every identity assertion. Then review portrait/landscape and reduced motion. Update inventory, phase evidence and this contract before the release gate.”

“When a native file dialog is cancelled or the same file is selected again, keep the containing editor and its retained state open. Audit file-containing dialog cancel handlers for bubbling input events: handle only events whose target is the actual dialog, preserving genuine Escape/Back, busy cancellation and commit semantics. Capture the invoker before disabling it or awaiting the first read; pass it explicitly without changing other callers' defaults. On actual close during a pending Open, detach that host owner and enable the invoker before restoring focus, while retaining first-Back cancellation without closure. Test retained files/drafts/reviews, focus, current status and late completion with a bubbling non-cancelable input cancel and a separate cancelable dialog close request. Model disabled-control blur and a held panel read; verify late settlement cannot steal a later focus choice or affect a newer owner. Retain the original failure and qualify the corrected browser behavior without calling modeled events physical OS-picker evidence.”

“For offline progress, use the concise status/result summary and keep the complete optional-pack catalogue in expandable details. Preserve the existing full message for older consumers. With the full shipped list, verify Prepare, advancing counts, Stop, rejoin and completion at portrait and short-landscape sizes: status changes must not push the count or Stop action out of view. Retain the original public failure and correct it in a new immutable patch when necessary.”

“For a recorded flight, reach Export replay through Pause → Main menu → Workshop using the original visible button beside Replay Theater. Keep its existing recorder and download owner, compare the exported replay with the exact paused checkpoint, and verify settled Close returns to the Workshop opener without resuming. Retain pending-export locks across close and Retry. Keep the original status and Download/Close controls together in the shared dialog operation rail before the long readonly JSON, with keyboard-scrollable long messages and no forced focus or scroll merely to announce status. Use the existing measured focus-clearance helper so Tab/PageUp and reflow leave the JSON's first visible line below the rail; preserve its independent scroll and deliberate manual scrolling. Check the route with scoped keyboard and modeled controller navigation, then native portrait/short-landscape interaction. Measure status and controls immediately after activation without driver scrolling; keep initial visibility distinct from later scroll reachability. A direct hidden-button click or a modeled download does not qualify visibility, painted feedback or a saved OS file.”

“For Library feedback, retain the original status and Cancel nodes in the dialog's sticky operation area. Keep the existing task, attempt-export and backup owners authoritative. Test same-owner focus, exact sibling restoration, close/reopen, cancelled-but-unsettled backup reads and stale cleanup against a newer section. Activate real actions from deep scrolled Saves and Packs sections at 390×844 and 844×390, including Large/Plain text and reduced motion. Verify status and escape visibility immediately without driver scrolling, full keyboard-readable details and truthful download-request results. Keep fast transient DOM observations distinct from painted browser frames. Preserve the earlier public failure and qualify the correction before P01 acceptance.”

“For Flight setup, verify that real craft preparation appears inside the active Missions modal, including when the player opens the direct briefing. Reuse the original renderer status node and presenter; preserve load-token supersession, exact restoration, current close/reopen ownership and nonblocking cosmetic readiness. Keep title, Back and feedback in one sticky heading; measure both heading and action bar when exposing the current focused selector. Test its full hit targets as well as the message, at Standard and Large text in portrait and landscape. Do not move focus or fight ordinary scrolling. Record real short-lived DOM feedback separately from any captured painted frame, and keep the release's version/source identity in the evidence.”

“For Controller Apply, reproduce native disabled-control blur in the fixture, capture the initiating focus owner before disabling, and relinquish it after later user input or focus changes. Restore the existing Edit action only for a successful current operation, preserving bindings and transaction semantics. Verify abort/disposal cleanup. In the browser record the actual active element/action, `:focus`, and next Tab destination; do not infer failure from an empty element ID.”

“For Playground, keep editor import/generation, replay verification and child readiness in one bounded area using their original status nodes and independent owners. Expose the newest operation first without hiding another pending Cancel. Preserve reading scroll during progress and focused reading during owner switches. Verify real import/Generate/Undo, file and pasted replay, child ready/Back, and persisted page restoration in portrait and short landscape. For Music imported audition, expose status and Finish before awaiting play while preserving the activation turn. Hold play, stop it through the enabled control, and settle the old promise both ways; no late status, URL, audio-intent or storage change may return.”

## Return from browser history

“Reproduce game → Release Explorer → browser Back with both a saved pinned flight and an unfinished new-picture selection. Retire speculative prewarm on pagehide before releasing the profile writer, fence its late result, and preserve session-only semantics. Reuse approved exact retained art without a write; allow normal Continue and explicit Resume to decode the saved original. If new history must be saved, show clear Export game data / Reload saved profile recovery before another download, with no automatic navigation, writer reacquisition or legacy substitution. Verify raw saves, run ID, checkpoint, pinned asset hash and write counts; keep a completed durable append while refusing late pin adoption. Qualify the current host, including an abort-ignoring transport and the pinned display decode, then record actual native navigation separately. Link the retained public failure rather than overwriting it.”

“For original-image recovery, verify an actual image and check that its selected summary and option agree with the successful operation. Derive display text from the existing current verification handle; never mutate inventory availability or claim earned ownership. Reverify/Cancel, failure/retry, reselection and a new review must invalidate the display when they invalidate that handle. File preparation still rechecks bytes and may fail after a prior successful review; preserve its error and existing download ownership.”

## Copy an earlier release

“Review a supported older release with a real suspended flight and no external index, then Copy into a current profile that already has an explicit empty or populated index. Keep source locks, unchanged raw snapshot/fingerprint checks, original picture ownership and the complete-backup Undo path. Only the trusted locked reader’s successful null index may become an explicit empty backup.v2 index; do not reinterpret generic v1 files or failed reads as empty replacements. Prove actual Copy and Undo, unchanged source bytes and retained original media, including failure before adoption. Keep public and source/model results separate.”

## Bind a map across modes

“Resolve this mission using its complete content/revision, edition, collection and artwork revision. Compare fresh Solo and both Versus boards; preserve pinned saved/earned and authenticated installed originals. Verify preview, reveal, result and Retry retain the decoded binding. For Team use a versioned presentation envelope and the shared read-only painter primitives, preserving its 72×36 mechanics. Exercise invalid required art, stale preparation, cancellation and offline. Do not infer missing art from absent JSON image fields or use an unannounced procedural substitute.”

## Qualify shared master audio

Use the P02-A [shared master candidate contract](../../docs/shared-master-audio.md) within the active phase's authorization. These examples add no registered prompt IDs, new playlists or phase/public acceptance claim.

“Inspect every changing-name Mute/Unmute command in Solo, Music Library, Versus, Team and Asset Studio. Use ordinary buttons without `aria-pressed`, following the [W3C APG button pattern](https://www.w3.org/WAI/ARIA/apg/patterns/button/). Verify both labels through real keyboard actions while master remains independent of paused transport, position and gameplay. Check immediate mute-status text before a media event and prevent held Play completion from overwriting a newer Pause or retry result. Record source-host and native observations separately; neither proves audible output or physical assistive-technology acceptance.”

“Start Studio audition or story Play with its actual completion held, then explicitly mute master before settling it. Verify output stays muted, the local fader and transport intent remain unchanged, and late callbacks never unmute. Change master volume while muted, then explicitly unmute without a new seek or selected-track restart. Include storage refusal: output changes immediately and a session-only warning remains visible. Preserve the legacy uninjected caller contract.”

“Open saved and draft Asset Studio audio previews together. Set different local audition volumes and change the shared master. Verify each effective output once, then replace only the saved preview while draft continues. Close during pending metadata/context readiness and settle the old work: no late playback, URL adoption or authority change. Keep native-control attempts subordinate to master policy. Record modeled lifecycle results separately from actual native decoding and audible output.”

“Pause music intentionally, visit another audio-capable page, change master there and return through browser history. Check the shared two-scalar preference, exact track/position where the existing page restores its transport, and the retained music Pause. Exercise visibility interruption and nested practice without resuming gameplay or a hidden page. New-page transport is not transferred by localStorage, and preference adoption alone cannot autoplay. Verify browser permission/retry and physical-device volume behavior separately before claiming public acceptance.”

“Open Team Options and change master volume and mute. Keep its permanent explanation of available sound visible when persistence succeeds, when storage refuses a write, and when a later save succeeds. Storage warnings should clear independently. Verify the actual mounted handlers, paused arena and unrelated player data; then inspect the visible public explanation after the same edits. A shared preference control is not proof that Team has a soundtrack producer.”

## Generate a Team role variation

“Read the selected Asset Studio slot and resolved edition contract. Create original pixel artwork for the exact Team role and states, using the slot's required dimensions, palette, alpha, bounds, pivot and rotor anchors. Keep player number/shape readable independently of colour. Support must remain distinct from Scan; anchors from ordinary pickups; hunters from drifters and border patrols. Preserve runtime propellers and mechanical geometry. Return editable source plus prepared derivatives under the declared filenames, with provenance, byte/dimension/alpha validation and actual-size 20/24/32 CSS-pixel previews. A candidate is not approved until reviewed in Solo, left/right Versus and Team wherever the slot is used.”

## Qualify Team actor readability

“Use the accepted page's prepared sprites without adding loaders or changing Team types, radii, trails, scoring or artwork identity. At actual desktop and phone arena sizes, inspect player 1/2 number-plus-shape, hunter target/charge/recovery, core/anchor and downed/grace cues over dark and revealed art. Keep full cosmetic bodies inside edges through heading and banking; if offset inward, keep the real cutting head luminous and clearly connected. Check complete CSS-sized plates against both heads rather than shrinking text. Preserve pause and Reduced effects, and compare exact run state in source tests. Record clean native dimensions/DPR and original images separately from command bounds, source-sheet detail and inconclusive captures. Retain each failed predecessor; a small-loop reveal does not prove full-win/rescue, all actor sets, bright-art contrast or physical input. Use the [scoped actor checkpoint](../../docs/verification/cross-mode/p08-team-actors/README.md), keeping HUD composition and P08-B surface-motion work separate.”

## Qualify a twelve-mission campaign

“Follow this campaign's P11–P15 row. Author exactly twelve original missions with deliberate progression, one unfamiliar role introduced at a time, no shooters in the first three and no more than two shooter missions. Qualify Gentle/Standard/Hard with encounters on/off, relevant Versus cases and the edition's two existing Team art variants. Keep scenes, actors, menus and audio consistent, saved originals intact, clear Retry/Next/end destinations, and explicit Support/Combat framing. Update inventory, provenance, prompts and guide; give this campaign its own immutable version and public proof.”

## Qualify initial Ready focus

“On the exact candidate source, open a fresh Versus lobby and wait for its real initial preparation. Verify enabled visible Start receives focus once only after boot inertness clears, without starting the round or joining a controller. Use Shift+Tab, explicit Start, Pause and setup traversal. Repeat with the actual bootstrap response delayed and deliberately focus Back before readiness: cleanup may remove that temporary link, but must not steal focus for Start or re-arm after blur, hidden state or pagehide. Test disabled/error/current-generation cases through the real host, recording the actual focus-call ordering rather than relying on a permissive DOM stub. Preserve A1/A2 departure and return ownership. If P08 required-picture preparation is also present, qualify its Cancel/Back availability and explicit Start separately on the combined source. Keep modeled, desktop-native, physical-device and public evidence distinct.”

## Qualify shared mode presentation

“Reuse the existing Solo/Versus/Team anchors with the shared mode presenter. Keep the current mode non-actionable and the visible order equal to keyboard order. In both Couch lobbies, use explicit Start and Pause, open a mode-departure decision and Stay back to the same anchor. Verify Team moves the same nodes into its active pause panel and hides them during play. Preserve fixed routes, return-hint namespaces and checked attempt ownership; presentation must not start, save or discard a run. Check shared token styles against host CSS specificity, then test the actual intended viewport. On composition, separately qualify R5 initial focus, Title departure and P08 pending-picture/Start cancellation. Keep source, modeled host, native desktop, physical device and public evidence distinct.”

## Preserve focus after asynchronous menu actions

Use the [completed-operation focus contract](../../docs/operation-focus.md). Reproduce the actual focused control becoming disabled, including non-bubbling element blur reaching window capture. Preserve true-window blur refusal. Traverse the actual keyboard order to explicit Cancel after the automatic lease retires; that focused action may capture a fresh same-dialog return intent before abort, while detached commits and cleanup focus/background changes still veto it. Prove that completion/error can return only to the original connected control; a newer focus, dialog or foreground choice wins. Retain the real busy/commit owner and test callbacks during cleanup. Check the original native journey and one deliberate pending-focus choice; record routing mistakes honestly. Do not infer focus for a removed/recreated row or count superseded/partial runs as passing qualification.

## Qualify menu appearance without changing a flight

“On the exact P05-B candidate, open a real paused unfinished Solo cut, change Menu colours and each ornament choice, close Options and verify the same checkpoint and explicit Resume. Follow the stored two-field preference into Team and Versus; keep Text style first, controller/native selectors usable, Back on the actual opener and no Solo profile/display/audio writes. Inspect a late Music Library dialog and its replacement/close lifecycle. Use actual compiled borders while checking primary, danger, disabled and focused text in Theme/Plain and Standard/Large. Keep inset ornaments away from labels/outlines and never decorate arena/HUD. Record computed colours/opacity and real viewport/zoom separately from token ratios and finite host tests; an unsaved local choice must stay usable after storage refusal. Do not rewrite authored theme/image/font identities, regenerate art, resume music/gameplay or declare the phase publicly accepted.”

“For an ornament gutter correction, retain the failing screenshots, source and original event labels. Measure actual text/control/focus bounds against both side stripes in Versus and Team, check overflow, then change Rich/Off/Subtle while paused and close Options back to its owner. A source budget or a changed DPR does not prove responsive layout or browser zoom. Record separate action-row/mode-row spacing defects without hiding them behind a successful side-gutter result.”

### Check Collection return after imported growth

“From an empty Collection, open Flight records, import the retained legal 25-result fixture through the actual import UI, then close Library. Verify the same Flight records action is visibly restored after final card publication without another Collection entry or Pause/save. Repeat with held still metadata, refusal, newer Search/Close focus and blur/return. Preserve exact parent visit/model/population, existing pager and picture-return owners, paused checkpoints and original leases. Keep fixture-origin imports, modeled scroll commands and actual viewport evidence distinct; retain failed originals and post-HTTP pins. Do not claim phase, zoom or physical-device acceptance.”

“Before committing the Collection return correction, inspect the complete staged tree against every original composition parent. Retain the four source parents directly, the predecessor 39-file qualification, the separate successor eight-file and Field Kit pairs, and failed/settled/corrected native groups. Reconcile all original bytes losslessly and report the final staged tree for review. Do not describe the inherited full composition as only its last eight changed files or infer phase/public acceptance.”

## Qualify a narrow Team HUD change

“Preserve Team font sizes, arena geometry and input ownership. At 320 and 390 CSS pixels, inspect both full player names, number badges, Ready/Support status and the complete arena in Theme/Plain and Standard/Large. Allocate status text the full player column before shrinking text. Enter Options by keyboard, return to the actual opener and Resume explicitly. Record actual viewport, DPR, HUD height and source pins; retain banded or mismatched captures as inconclusive and use a clean same-browser capture. Check short landscape separately: a rule limited to 600 pixels does not qualify wider layouts, scrolling or Pause placement. Keep actor readability, 200% zoom, physical-device and phase acceptance separate from the [scoped HUD record](../../docs/verification/cross-mode/p05-team-hud/README.md).”

## Qualify retained Results during Next

“Finish a real Versus round, record the completed boards, result text, series score and original, then activate Next while the exact selected picture response fails. Verify Results and keyboard View still show the completed round, then retry with that original delayed. Move focus deliberately to Help before completion: the accepted next round may become Ready, but must not start or reclaim focus. Require a fresh Start. If Help is activated, record its cancellation path separately. Repeat source cases for Cancel, read/decode/final-authority refusal, installed pointer changes, old decoder settlement and reentrant image retirement. Preserve the captured setup, no Solo/media writes and exactly-once cleanup. Bind transport to one exact asset and request sequence, record actual HTTP status and delay, leave HEAD untouched, and retain each failure rather than relabeling an intended action as a pass. Keep this correction separate from full match progression, later source compositions and public/device acceptance.”

For the retained-Results checkpoint, verify concise player failure copy separately from its technical diagnostic. Keep actual native actions and observed outcomes distinct, preserve corrected provisional labels, and retain any capacity-interrupted run without treating an incomplete log as a whole-file pass.

## Qualify an exact gameplay composition

“Name every source parent and inspect the complete staged tree. Preserve the resize owner when adding the optional Next transition-focus gate; leave accepted picture, departure and Collection owners intact. Audit the final host import graph before materializing a finite test cohort, including new inherited imports and explicit platform exclusions. Keep failed raw runs and exact intake recovery, then run every requested complete file on both supported runtimes. Bind native previews to that tested tree and retain source-response pins. Wait for map readiness before Start; record actual Next failure, Cancel and late settlement rather than the intended action label. Distinguish a still-owned shipped Next from installed Ready. Keep Team short-landscape clipping, current-registry rebinding and later progression/device/public gates explicit; a scoped composition checkpoint is not a completed phase.”

## Verify reading prompts across input changes

For Couch: “Open Help in both lobbies and while each mode is paused. Use the existing reader with explicit surfaces, a stable Done reading button and a visible sibling hint. Check native keyboard Enter/Space/Escape, pointer Done, a cancelled press and current controller South/East prompts. The first keyboard arrow must update the prompt in that event; idle controller polling must not revert it. Compare both Versus checkpoints and the Team paused HUD/painter stream without claiming private-state or hardware proof. Close Help, reopen and leave the page; no stale Done click may start, resume or restore old focus. Preserve current P02 audio note and storage-failure tests when integrating. Run complete affected files and source-bound responsive browser checks; publish only through the phase owner after exact-source and public gates.”

For rotation and touch: “Keep an active reader open while rotating portrait to short landscape, in the lobby and inside a real paused panel with Large/Plain text. Measure the whole unit against the viewport and each clipping ancestor's client bounds; Done and hint must remain visible while text scrolls. Preserve actual focus and explicit Resume. Then scroll the native text region before using Done, including the matching pointercancel used by browser pan adoption. Native scrolling must keep its default action, and reading entry/end hints must have one status owner. Record the previous viewport-only clipping and tap-only coverage gaps as failures; repeat on the final source and distinguish physical touch from modeled events.”

“Enter the actual Solo reader with a keyboard, then switch from controller navigation to the first keyboard Enter and from active controller reading to a keyboard arrow. Verify immediate truthful Enter/Space/Escape text, unchanged reading focus and no Start/Resume/save. Switch back through a real remapped controller command and check the current Confirm/Back names. Enter with touch or mouse and use the stable Done reading button. Preserve browser-owned button activation, existing neutral gates and reader-origin validity. Retain any fixture-only failure separately from the reproduced product copy error, then run the complete affected host/adapter files. Record native input/device proof separately; a passing finite DOM harness is not whole-phase acceptance.”

For a separate native supplement: “Bind the preview to the tested checkpoint. Enter Mission brief with the keyboard while paused, inspect both hints, exit using each supported key and verify the actual Read focus. Enter through the pointer, use Done, then repeat pointer entry followed by the first keyboard arrow. Preserve original captures and any accessibility/DOM disagreement without assigning an unproven cause. Compare exact source responses before and after, and state separately which hardware, scrolling, save and integration gates remain untested.”

## Qualify a scoped navigation update

“Compose the current Solo, Versus and Team entry, Help/readers, Back/Resume and departure fixes with the accepted public baseline. Retain failed test attempts and run complete affected files on the actual working source. Exercise keyboard journeys through a real cut, pause, mission reading, earned picture, nested Collection, Library, Team Help/options/Stay-confirm and Versus results/Next. Record source/version, focus, clock, actual input method and viewport. Distinguish real touch/controllers and foreground loss from modeled events or resized desktop windows. If screenshots can only be inspected inline, state that limitation without fabricating exported artifacts. Publish the qualified named feature with its own immutable version, PR and complete public audit while keeping the parent phase's unfinished gates visible.”

## Append reviewed production without rewriting history

“Preserve published production oracles and the measured intermediate source-stage checkpoint. After inspecting only the changed source groups, bind each scoped review to its exact ordered fingerprint. Keep independent image approvals separate from procedural recipes. Generate canonically, retain every historical record and original payload, and rebind Team to the actual generated revision without wildcard fallback. Reproduce from the full current ledger; never flatten an intermediate checkpoint. If testing a historical transition from serialized JSON, restore insertion order only with an exact canonical-content equality assertion. Verify source invalidation, original bytes, repeated export and strict picture bindings; keep review metadata, runtime/browser checks and final public acceptance distinct.”

For story lifecycle: “Focus Pause while an optional story is playing, then exercise hidden-document and unfocused-window transitions separately. Verify the unavailable Pause control loses focus without moving it to Resume in the inactive document. Returning foreground must not resume or reclaim focus. Explicit Resume preserves the video position and restores the active foreground control. Keep real foreground tests distinct from the modeled DOM regression.”

For flight lifecycle: “Create an unfinished cut in Immediate and Grid + buffer modes. Pause, lose focus or hide the document, then deliver a previously queued Resume activation with the picture already cached. Preserve the exact checkpoint, pending turn, focus and stored bytes without polling a controller. Return and explicitly Resume before another frame runs; the saved movement must continue. Also test missed lifecycle notifications through the ordinary frame guard. Retain the failing baseline and distinguish this callback regression from a native tab-switch test.”

For chapter preparation: “Exercise the real embedded-art chapter installation and stored offline selection using exact published content identities. Keep the bounded chapter-specific wait and record elapsed time, selection, picture state and errors. Preserve every final gameplay, image and storage assertion. Diagnose a terminal failure instead of repeatedly extending the deadline. A successful scoped wait does not certify download performance or replace final-source qualification.”

### Chapter selection without losing a flight

“Open More worlds during an unfinished cut. Choose the current chapter and retain the exact run. Choose a different installed or verified original-picture chapter and exercise Stay, Replace, failed saving and changed readback. Hold the checked-save lock and press Stay before verification settles: the exact Choose must become available and regain focus immediately. Then move focus or begin a newer choice and release the old callback; it must not save, adopt, refocus or unlock newer work. Keep the parent choice valid only for its current visit; close or background it during verification and reject late adoption/focus. Record installed catalog changes separately from the active run. Qualify the final source and public route before accepting this delivery.”

### Keyboard remapping through shared navigation

“Open Settings → Controls → Keyboard controls using the real player route. Select Left hand, activate Change Move up and press ArrowUp. The physical arrow must bind and focus must remain on its Change action. Dispatch through document capture and dialog capture; do not call only the local key handler. Reject a duplicate arrow and Return/Space while retaining listening focus, cancel with Escape without closing Settings, and let Tab cancel with native traversal. Then verify normal menu arrows and native select/slider behavior. Delegate only the current capture owner's events; preserve other screens, controller routing, binding validation and Ready/paused checkpoints. Retain the native failure and exact automated baseline/correction records, run complete affected files, and qualify the corrected source afresh before publishing the still-unreleased candidate. These checks do not certify physical controllers, touch or the whole phase.”

### Controller practice frame-exit regression

“From Workshop, open Controller practice with keyboard only. After the real child reports Ready, use Tab and Shift+Tab through its menu boundaries to return to outer setup, then reenter and verify Pause → Options → Back still requires explicit Resume. Preserve modal and reader ownership and native select/range keys. Model current/wrong/replaced frames, session rotation, loading, background focus and pending virtual holds without application-state commands. Retain the actual failing route and scope corrected source/browser evidence separately; do not mark P03 or physical controls complete.”

For repeated Pause focus: “Start an unfinished cut inside Controller practice, pause, visit Settings and return. Tab from Resume to Restart, then deliver another suspension: Restart must keep focus and the checkpoint must stay unchanged. Exit each child boundary into the outer lab; the resulting real blur handler must not pull focus back. Also blur a moving cut before a Pause overlay exists: pause without focusing the inactive child. Reenter and use explicit Resume. Test through the app, preserve profile bytes, and distinguish modeled cross-document events from the served browser journey.”
