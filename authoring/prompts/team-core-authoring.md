# Team relay-core variation prompt

Create original pixel artwork for Reveal Line slot `team.core.shielded` (or explicitly choose `team.core.exposed` / `team.core.secured`). Export a 64×64 transparent PNG with center pivot (0.5, 0.5), crisp nearest-neighbor pixel clusters and every attachment inside the frame. Use the selected collection palette. Preserve the relay objective silhouette; distinguish states through shape or surface detail as well as color. No text, logo, background, blur, baked glow, weapon or implied new mechanic. The runtime draws its own fixed ring, center and status label. This is a stationary Team objective, not a collectible or ordinary enemy.

Return the original source, prepared PNG, exact palette and actual rights/provenance. Verify alpha, dimensions, centered placement and readability at small game sizes. Compare Initial field and Relay core exposed in Studio's Couch Team / Relay Yard preview. Completed field hides objectives behind the revealed picture; inspect secured artwork at Native size and in an appropriate continuing multi-stronghold scenario. Validate and stage the upload, save, export and re-import the actual bundle. Preserve existing asset revisions. Passing validation is not artistic approval or release acceptance.
