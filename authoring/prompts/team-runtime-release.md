# Team collection release verification prompt

Adopt the supplied validated Reveal Line collection through the existing compiler and release workflow. Preserve exact source bytes, immutable asset revisions, historical records, picture ownership and simulation identity. For every selected Team image, confirm its stable ID is a supported renderer contract in `TEAM_RUNTIME_IMAGE_SLOTS`; never broaden loading to arbitrary prefixes.

Load the compiled result through `createPresentationHost` in a real browser. Record each decoded slot, revision, hash and dimensions. Pass that same snapshot to the actual Team painter and inspect applicable anchor, core, Support, slowed-enemy, emitter, rescue and recovery states. A successful Asset Studio preview alone is insufficient. Keep reveal-picture preparation and approved content bindings intact.

Exercise a missing/corrupt replacement and Retry: preserve the accepted snapshot and its owned images until complete replacement succeeds, release partial resources on failure, and release superseded resources on success/Close. Run the source gates and public/offline journeys required by the release plan. Keep QA fixtures and scoped checks distinct from approved production artwork and public acceptance. Report the immutable test URL and exact source only after deployment is verified.
