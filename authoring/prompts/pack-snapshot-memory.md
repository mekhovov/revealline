# Review campaign snapshot memory

> Review Reveal Line's campaign install and recovery paths with real large pack originals. Avoid JSON-encoding an already serialized storage pointer solely for equality. Preserve byte-exact strings, canonical legacy objects, all recovery journal and lock fields, authenticated content identities, ownership boundaries and single-use mutation reviews. Test stale pointer changes including equivalent JSON whitespace, reordered object keys and modified object content, then failed writes, cancellation and a real Download & play journey with another pack installed. Keep existing library and process budgets. Record complete-file results separately from filtered diagnostics, modeled storage, browser play and deployed verification. Do not claim reduced peak memory from a final heap sample or claim all multi-page retention issues are resolved by a single comparison fix.
