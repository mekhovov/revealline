# Maintain backup replacement review

> Review Reveal Line's Library game-data replacement flow using `docs/backup-replacement-review.md`. Preserve the existing transaction, historical backup readers, media ownership, writer guard, recovery journal and pending-award ownership. Keep preflight read-only. Let earned seals invalidate stale Undo snapshots during review. Hold pending seal commits synchronously only after acceptance; cancel them after durable success, resume after verified unchanged/rolled-back failure, and reject writes when recovery, ownership or the backup marker is uncertain. Validate imports and prepare a verified Undo copy before offering Keep current data / Replace game data in the shared operation rail. State Undo unavailability and separate-media scope before a write. Never silently accept, auto-confirm, or use a native browser confirm dialog. Keep controls accessible to keyboard, touch and the existing modal/controller router. Recheck destination identity and earlier-release source fingerprints after the player's decision. Cancelled, closed, stale or page-hidden work must not write or reclaim newer focus. Keep Stop waiting distinct from cancellation once commit begins. Verify import/Undo focus, disabled Undo fallback, real external chapters and saved flights, responsive Large/Plain text and 44-pixel actions. Preserve failed evidence, update relevant tests for the deliberate decision, and do not equate a DOM fixture or local browser pass with public-release or physical-device acceptance.

Include the actual Undo handler when adapting earlier patches. Verify that successful Undo focuses the enabled Export game data action, while failed Undo retains its appropriate action and newer focus decisions remain untouched. Model native beforetoggle events for nested dialogs. Keep strict DOM identity assertions, but report them as booleans to avoid huge failure output. Preserve failed-run evidence and rerun affected complete files after a runtime correction.
