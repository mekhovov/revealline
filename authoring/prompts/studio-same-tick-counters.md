# Consistent counters for one simulation tick

When Studio shows the same progress in a HUD, canvas and description, use the same authoritative snapshot and percentage convention as the real host. Contact rescue displays completed whole percent: floor the existing clamped progress. Do not round an incomplete rescue to 100%, invent elapsed time, or advance the simulation while formatting text. Keep player identity distinct from the rescued target.

Exercise both targets with command-earned fixtures through fractional ticks, the last incomplete tick and ordinary completion. Compare all visible representations in the same render; also retain cancellation and inactive-state contracts. A paused half-complete screenshot cannot expose rounding boundaries. Verify the complete affected test file on supported runtimes, bind recorded reads to exact source, and preserve failed probes separately from qualifying regression evidence.
