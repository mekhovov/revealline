# Couch menu workflow prompts

These four finite prose workflows are separate from the existing 124 CLI prompt templates and add no catalog entries or skills. They describe the Round29 working increment, not release or hardware certification. Read the [Couch controller menu guide](../../docs/couch-controller-navigation.md) and [Runtime Maintainer](../skills/xonix-runtime-maintainer/SKILL.md) before editing. Preserve existing maps, core/protocol identities, saved profiles and frozen proofs.

1. **Two players, one menu.** “Use the actual couch host, input adapter, router, navigation and public duel in an injected-pad DOM test. Give the two stable player slots distinct standard pads. Release, join the menu and hold South across Start; verify one round starts and no held ability, supply, Boost or direction leaks. Test simultaneous joins, a nonowner pressing controls and explicit Release menu controller. Keep the keyboard route usable without a controller join. Report software evidence without claiming physical-controller operation.”

2. **Draft setup and native handoff.** “Open a native couch setup selector through South, browse several choices without resetting the duel, cancel with East/Menu, then apply one choice through the existing handler. Cover replaced option lists, Show setup/Focus boards changes and pointer/keyboard handoff while a controller direction remains held. Use a long existing map or class label; keep shared menu focus out of both pads and canvases. Preserve World as presentation and the existing reset behavior of other setup fields.”

3. **Pause, loss and another round.** “Drive real ordinary and Sentinel duels through Start, flight, Pause, a separate Resume, finish and Next using both steering policies. Cover one craft recovering or lost while the other continues, a draw and first-to-two series reset. Assert one hardware read per active frame and none while hidden/unfocused/disposed; a disconnect, reused slot or history-cache return cannot advance play or activate a stale control. Use existing legal inputs and expected checkpoints; do not regenerate old proofs or add state fields.”

4. **Readable shared controls.** “Inspect the actual couch page with native controls at 320×640, 844×390 and 1280×720 CSS sizes. Check Focus boards, long status/selector text, reachable Start/Resume/next-round actions, visible controller focus and 44px action targets. Record any necessary native scrolling instead of claiming every control fits at once. Verify native keyboard/select behavior and truthful audio activation failures. Keep this browser record separate from injected two-pad tests and physical touch/controller checks; the solo Controller Lab does not establish couch coverage.”
