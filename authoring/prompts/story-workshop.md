# Picture Workshop story requests

Use the implemented [optional workshop contract](../../docs/story-workshop-authoring.md). These are prose examples, not registered executable prompts or completed browser evidence.

**Save one exact poster/story choice.** “Inject the current story adapter from the same explicit v4 manager as still/audio. In the existing Picture Workshop select saved poster A, silently inspect my owned video, choose start/end, then Prepare and separately Save. Verify no write during Prepare and exactly one story generation for original+descriptor+binding. Assign picture B and inspect historical A again; no current assignment may replace its full pin. Keep old attempt/earned formats unchanged.”

**Capture without discarding the movie.** “Request a poster frame at 2.05 seconds. Report the actual encoded PNG hash and requested/observed/playhead facts separately; never equate currentTime with frame accuracy. Save the draft through normal still assignment, then bind the retained original movie to that saved revision. Test invalid time and a late cancelled capture preserving the prior preview. Do not write timing fields into strict still v1 records.”

**Restore deliberately.** “Prepare a native .rlstory download and use the visible Download action. On a compatible destination restore paired .rlmedia poster originals, review .rlstory with default keep-current, then explicitly restore. Preserve destination null. Repeat with explicit incoming bindings, preserving absent destination keys. Test a corrupt second movie, missing poster, stale writer and quota refusal with unchanged prior audio/still/story inventories. Keep requested download versus verified disk file separate.”

**Qualify the eventual host together.** “Wire one current manager across ordinary/practice/course/workshops and one existing controller router, with Back cancelling pending work before Close. Verify file-picker blur, hidden/pagehide, held Confirm, selected history changes and notification failure after successful commit. Qualify browser originals/backups and old-reader VersionError recovery before the versioned saved-attempt/first-earned/Collection integration. No source-only test may stand in for a real win, physical controller, offline playback or public release.”
