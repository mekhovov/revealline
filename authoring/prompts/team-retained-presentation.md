# Maintain Team presentation ownership

Use this prompt when changing Team artwork, theme revisions, or host navigation:

> Extend Reveal Line Team presentation without replacing historical artwork. Resolve each accepted attempt through its exact pack, level, artwork receipt and presentation revision. Prepare a new selection before replacing the previous run or results. Keep Retry on the accepted selection. Use a separate owned preview lease with no live DOM application. Route live painter, DOM assets and menu Auto appearance through the Team display owner; roll back rejected or superseded updates, including later preference refreshes. Retire display ownership before terminal cancellation callbacks can repaint a page snapshot. Preserve current bindings, historical readers, simulation hashes and Solo progress unless explicitly in the feature scope. Test old/current manifests together, invalid and stale imports, cancellation, exact Retry/Next, preview return focus, palette rollback, decoder cleanup and BFCache versus terminal departure. Run the host and ownership cohorts on both supported Node versions, then a real-browser import/Retry/fresh-return journey. Record exact asset hashes and distinguish fixtures from approved production. Do not call local test output a public release; follow the source, production, offline, immutable release and public-play gates.

A theme revision is not ready solely because the registry has no missing slots. Inspect image quality, actual-size readability, all relevant states, both players, and the complete board and controls separately.
