# Adopt a reviewed Team presentation successor

Use the accepted production bundle as the immutable starting point. Preserve every existing document record, original payload, retained runtime and referenced dependency. Generate a new revision with the reviewed equipment images; never edit historical records or substitute a newer picture for a retained attempt.

Bind the current starter arenas and supported historical imports to exact theme, collection, pack, level and picture identities. Keep the explicitly supported retained authority. Reject unrelated revisions, changed picture hashes, unsupported collections and reserved-pack lookalikes. Do not use a revision range or a "latest" fallback. Presentation admission must not alter simulation, scoring, capture, Support or rescue.

Qualify the ordinary committed output, including preparation failure/cancellation, stale completion, Retry picture retention, imported coverage and multi-core maps. In the actual painter frame, verify that the accepted decoded background paints before actors, once, with its original hash; the last drawn image may be an actor. Preserve strong lifecycle assertions when adapting an older test oracle.

Check immutable-history reproduction and bounded bundle export/import on every supported Node line. The version-two transfer format may omit the redundant payload table only under its existing bounded fallback rule; it must reconstruct and verify the same assets. Do not truncate history or raise limits to make a candidate fit.

Review the exact generated inventory, run the required source/build/browser gates, assign the next unused release version, and publish through the existing PR pipeline. Distinguish reviewed equipment images, source-functional recipe review, native play evidence and final public acceptance. A successful local candidate is not a published release.
