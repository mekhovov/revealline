# Implement and qualify retained presentation releases

> Add a new complete Reveal Line visual collection while preserving exact prior presentations. Supply the verified previous compiler-output Map explicitly to `compilePresentation` as `previousOutput`; do not infer a path from an uploaded bundle. Preserve every prior `runtime.<sha256>.json` and its selected original image/font/audio bytes. Keep new current runtime, Studio document and CSS separate. Never normalize old manifest bytes, repoint pins to latest, prune history, rewrite saved simulation identity or award previewed pictures. Reject bad ownership manifests, wrong hashes, absent lazy dependencies, unsafe URLs and capacity excess before replacing the current directory. Confirm byte-identical repeat compilation and controlled check mode; exercise failure with the old directory still usable. Record exact source and collection revisions, test all source/reproduction/build/release gates, then qualify actual saved Retry/Continue and offline restoration on the immutable deployed URL. Distinguish output verification from art approval and from public acceptance.
