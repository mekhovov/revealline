---
name: xonix-runtime-maintainer
description: Maintain, test, build and archive this project's playable Xonix runtime, including staged encounters, chapter appearances, optional mastery, controller layouts and Boost modes, isolated practice and saved-game continuity, using its actual core, content and CLI contracts. Use for game logic, browser integration or playable releases; concept art and the separate motion lab have their own workflows.
---

# Xonix Runtime Maintainer

For production Team artwork, use `scripts/field-kit-team-assets.mjs` and the real
Field Kit producer. Preserve both anchor and all four feedback roles as complete
families. Verify prepared manifests and PNGs against the original generators;
append the exact optional slot contracts through the immutable history writer.
Clone frozen defaults before extending them. Run the real-producer regression in
`game/test/field-kit-team-production.test.mjs`, not just helper tests. Generation
keeps quality at produced; attach scoped visual evidence to a new reviewed revision.
Review adoption uses `scripts/reviewed-team-art.mjs`: bind each role to its exact
PNG and all inspected source/contract/producer hashes. Drift must revoke approval
without mutating the original produced manifests; test swapped role images too.
UI recipe inputs include retained-manifest and all Team slot-registry helpers.
New host imports must enter the dependency fingerprint before scoped review;
exercise each helper change and preserve unaffected recipe identities.
Neither provisional revision39 fixture is a reusable production identity. Follow
`docs/team-production-integration.md` before compiled adoption or a release.

For the shared handheld-control candidate, follow `docs/shared-device-play.md`.
Solo and couch seats must use `attachTouchSteering`; preserve independent fingers,
capture-stop fresh input, held rescue, neutral controller gates and explicit Resume.
Use the shared validated touch preference record without writing another mode's
campaign save. Do not claim Steam Deck hardware acceptance from injected standard
Gamepad samples. Compact HUD work must retain required objectives and complete
arena geometry, selected Large text, safe-area targets and context-owned status.
Test actual viewport dimensions after changing tabs; browser overrides can target
the selected tab rather than the tab being inspected. Prompt: “Exercise two touch
seats in stick/swipe/D-pad, release and interrupt each finger, rotate while paused,
then navigate Start/Settings/Back/Resume using A/B alone. Record source identity,
actual CSS viewport, complete arena bounds, failures and real-device limitations.”

For warning-capable layouts, reserve a stable instruction band from authored
capability before fitting the board. Do not subtract the complete touch-pad height
again when controls already overlay field edges. Check actual text line bounds,
not only card bounds or accessibility text. `open` is also a staged core phase;
only an idle classic card may be hidden by that name. Suppress duplicate encounter
phase notices only through their explicit cue and a present live card; damage,
pickup and failure notices remain independent. Exercise held-controller → new
touch → finger release → fresh-controller handoff in stick/swipe/D-pad modes and
verify interruption plus explicit Resume against the saved replay. Never convert
modeled handoff tests into hardware certification.

Verify warning-capable portrait maps separately from compact Arcade. Long captions
plus a reserved full-height touch pad can collapse the arena even when each control
fits. Test real stage transitions at320×480/390×844, including a nonzero percentage
and wrapped core instruction. Keep current and target percent units explicit,
and expose reveal progress with a labelled range/current value and complete
current/target percent text. Bound short-screen pads without rewriting the chosen
preference; retain44px targets. A layout probe that changes spawn or speed must
record those changes and cannot qualify the original campaign's difficulty or win.

For Team layouts, test control rectangles against the actual drawable board, not
just the viewport. Landscape reserves each visible seat's bounded pad width plus
clearance; a hidden seat must release its gutter. Short portrait may cap the chosen
pad size to132px and omit duplicate action icons while preserving44px named targets,
Large text and required objectives. Exercise both First Connection and Relay Yard,
320×480/390×844/568×320/844×390, regular/Large and all three steering modes. Prompt:
“Measure every visible control/board intersection, then earn a real Solo win using
only controller edges, Retry, win again and Next; verify no held Confirm leaks into
movement.” Actual native layout, modeled controller and physical device evidence
must remain separately labelled.

For shared touch changes, preserve the session-only practice option and legacy
profile fallback until a shared choice exists. Mirror the complete shared choice
on explicit Solo edits, report failed shared writes, and resample a persisted
page return without writing or resuming. Include `device-controls-host.test.mjs`
with the shared-input cohort: a gesture-only pass misses backup/Undo regressions.
Small-screen Versus arenas need explicit grid rows after hiding headings; remove
obsolete seat-specific side columns in the same layout. Test actual authored
Journey flight as well as legacy maps: its Missions/Skip header can override
compact shell styles. Keep the coverage target and critical Team rescue/Support
state visible. Large text may require a taller strip; never shrink it or hide
warnings to claim a single-row HUD. Match Solo's touch target sizes and indicator
geometry; visual preview sizes do not change simulation hitboxes.

For handheld training and short Versus, test real First Flight and authored Journey
routes, not only the ordinary Solo map. A hidden field-condition node can retain
an old high-specificity arena width rule. Inspect complete canvas bounds and the
underlying document. Keep the current lesson instruction visible; lesson selection
and Journey navigation may return on Pause. Large touch pads must fit their own
seat without covering the shared Pause control; preserve their preferred setting.
Test the visible steering surface rather than dispatching input to a hidden D-pad.
Use the actual authored Journey host for untimed-clock checks; Legacy races require
a valid duration. Missing sparse image dependencies must be restored from exact
source blobs before evaluating picture-host failures.

For Team caption clearance, measure the message, both complete touch groups,
Support labels, Pause and authored stronghold objective together. A caption in
normal document flow may still be covered by fixed thumb controls. Bound preferred
sizes without changing stored preferences, and retain 44px direction targets in
the declared minimum viewport. Very short strongholds may omit decorative action
icons while retaining readable Boost/Support labels. Title mode cards must be
checked at Large text in short landscape, including focus reveal for lower menu
actions. New Solo stylesheets use `data-boot-href`, never an early direct `href`;
include actual bytes in `boot-build.test.mjs` reproducibility/offline checks.

For phone title entry, keep Play and mode selection before informational download
copy in the document, while retaining operation status/Cancel ownership. Check the
first viewport at 320×480 and 568×320 with Large text, then Tab to lower actions and
verify focus reveals them. Do not mistake the decorative logo size for chosen
control text size. Test actual opening/authored Journey controller entry as well
as Legacy: discovery-held A must remain neutral, fresh A launches the named map,
then Pause/Resume must retain the same run without leaking Confirm into movement.

For Library selection and saved-flight restoration, use the Solo host's `focusMission` return path only after the owned operation succeeds. Dismiss retained Settings as well as Workshop/Home before focusing the paused mission. Passive Back, Stay, cancellation, failures and stale callbacks retain their existing parent/focus behavior; never replace operation ownership with a global close-all-dialogs shortcut. Cover ready and active-cut selection, suspended/imported restore, unchanged checkpoints and explicit Start/Resume. See [Library launch navigation](../../../docs/library-launch-navigation.md) for the maintenance prompt and the separate modeled/native verification gates. For native saved-flight checks, export a real paused cut, select another mission without starting it, import the downloaded file, and export again before Resume. Compare replay, checkpoint, run identity, presentation pins and continuation exactly; only a refreshed savedAt timestamp may differ. Resume must require an explicit action and use the retained direction. For Grid + buffer, also export an off-center queued turn and prove the queue survives import before explicit Resume consumes it. Record whether the sample was on secured ground or a live cut; one does not establish both. Keep physical-device acceptance separate.

For Team lobby changes, keep the mode/arena choices, preview, objective and Start together, and keep authored threat/Support/stronghold guidance inside the existing Help reading region so it remains available during Pause. Preserve IDs and reading/controller ownership. Large text may scroll vertically; do not shrink it to conceal layout overflow. Resize may reveal only the current foreground lobby/paused action, with ownership rechecked after layout reads; never refocus or resume. See [compact Team lobby](../../../docs/team-lobby-layout.md) for the native and modeled checks and authoring prompt. When restructuring lobby markup, include `coop-lobby-preview.test.mjs` with the navigation/resize cohort. Check common lobby ownership and preview-before-launch reading order through the new grouping while preserving exact image reuse, mystery masking, retry, earned-victory and no-automatic-Start assertions. Do not treat the old immediate-parent shape as player behavior.

For responsive HUD changes, inspect both the active modal and the underlying document. A dialog can fit while the hidden briefing HUD overflows its height-limited arena container. Keep non-running counters centered within viewport safe-area gutters; preserve the running board aspect ratio, chosen text size and focus. Test breakpoint neighbors, title, Settings/Library, paused and running layouts separately. Do not suppress document overflow to hide clipped score or life values. See [short-landscape HUD sizing](../../../docs/short-landscape-hud.md). Prompt: “At 568×320 with Large/Plain text, prove both the Library dialog and document fit, then check 521/680/681 widths, portrait and desktop without resuming during resize.”

For More worlds Play on an unfinished Solo chapter, compare the exact authored campaign key and source-pack ID with the accepted flight before preparing a replacement. Keep the same attempt paused, including its cut, decoded picture and saved continuation; leave the current Play action available with a clear route back to Continue. A different chapter still requires checked Stay/Replace. Finished attempts can still be replayed. Do not broaden this comparison to display names or campaign IDs without revisions. Exercise the actual app with an active cut, compare the authoritative checkpoint and original save bytes, and retain both the public failure and an original-code regression. See [current-chapter preservation](../../../docs/current-chapter-preservation.md). Prompt: “Select the active installed chapter through More worlds. Prove no replacement dialog, no restart, no extra download or save rewrite, and exact focus return. Separately verify different-chapter Stay/Replace and finished-attempt replay.”

For [Studio slot history](../../../docs/asset-studio.md#slot-history-and-restoration), collect exact asset references from retained theme and collection bindings for the selected slot. Never infer history from asset-name prefixes or select the newest same-ID revision. Retain provenance parents as source downloads, but offer Bind only for exact revisions already validated for that slot. Restoration uses `reviseStudioTheme` and the existing staged workspace, preserving saved/draft separation, immutable records, bytes, dirty-editor guards and explicit Save. Follow the guide's upload → restore → Undo/Redo → save/reload maintenance prompt; modeled host evidence does not certify native file dialogs, painted previews or public release behavior.

For Solo flight information, use the accepted host's typed source in `game/ui/flight-information-{source,bridge,host}.mjs`; never infer current danger from an old English caption. Adopt only after a run/runId becomes authoritative, including every verified restore of the same saved ID. Scope each complete event batch before sound callbacks, admit captions before DOM/cue writes, and preserve the original event order, copy and simulation-relative deadline metadata. Capture async run/result warning authority before the await; retain existing operation/dialog/media guards. Deliberate terminal story/replay work can opt into terminal acceptance, but cannot reuse a pre-result sequence. Storage reconciliation and native lifecycle notices remain explicit global system roles, not proof of attempt-owned async admission. Preserve current caption lifetime: `captionUntil` is metadata, not an implemented expiration timer. Keep memory and explicit Pause on BFCache, retire observation on departure, and test through the mounted Solo entry with actual legal events/restore/continuation. See [flight information ownership](../../../docs/flight-information.md). Prompt: ‘Add a compact field status reader using `readFlightInformation(runMessageElement)`. Prove current threat facts, complete-batch notice provenance and preserved unknown/system notices before any visual compaction; retain full Details copy and measure native layouts separately.’

When regenerated presentation history advances the selected theme, audit exact current-theme consumers and their live assertions. Keep Team picture bindings aligned with the new selected revision while retaining exact picture asset IDs, revisions and byte hashes. Do not rewrite historical fixtures or relax strict lease matching.

Keep the Solo Pause layout owned by its existing state-specific rules: one action column in portrait and the established two-column layout in short landscape. Generic mobile overlay grids must exclude Pause without raising specificity for other overlay states. Check full labels, icons, focus borders and at least 44-pixel touch targets with Large/Plain text; do not solve wrapping by reducing readability. Preserve the paused attempt and explicit Resume through rotation. Follow [the narrow Pause qualification contract](../../../docs/solo-pause-responsive-layout.md); source cascade checks are not native layout acceptance.

Current sequencing is [the revised whole-game UX plan](../../../docs/whole-game-ux-plan.md). Settings is a host-local surface with shared Controls, Audio, Appearance & accessibility and Game data categories. Use `attachSettingsPanels` for categories and compose `settingsTabOwnsKey` into the host navigation capture boundary; never add a second preference or controller owner. Enter the real category before editing or asserting control reachability. Preserve input release, paused checkpoints, actual opener return, retained category and explicit Resume. Team Settings is a native dialog, not an Options disclosure; model native Tab/default cancel locally in host fixtures without inventing application return focus. Quick Sound controls operate the existing master authority; their visible labels and pressed state agree across modes.

Team first entry uses First Connection; a separate exact starter-pack arena bookmark restores a returning choice. Capture native Arena intent before the selector is interactive, after visible loading feedback, and consume it once during module attachment. An early deliberate choice wins over saved storage even when it equals the markup default. Missing tracking evidence preserves native setup. Track interaction ownership separately from committed changes: same-value native confirmation may emit no input/change. Opening/cancelling only retains the visible choice for this visit; successful explicit initial Start can remember that accepted built-in arena. Keep native option nodes during boot and avoid assigning an already-correct value. Admitted built-in changes and successful initial Start write only the small dedicated key; imports, artwork completion, Retry, Settings and BFCache never save or replace an arena choice. Denied writes retain a playable visit with live status. Preserve unknown/future record bytes and stale-action guards. Return from an imported pack restores this visit’s last built-in choice. This is not a Team saved flight: checkpoints, difficulty and imported bytes remain page-local. See [Team arena selection](../../../docs/team-arena-selection.md); model evidence does not establish physical input or public acceptance. For DOM ownership assertions compare node identity without recursively comparing entire cyclic trees; keep regression runs bounded in time and memory, and retain diagnostic failures separately from product failures.

Versus starts with the session-only One race format. First to two is an explicit Race setup choice captured by the accepted round recipe, alongside its map, craft, steering, timer and theme. Hidden setup values never rewrite Results or its labels. Name same-mission Rematch and Next round actions with that recipe’s mission; do not imply a campaign successor. Reset series totals only when the confirmed new race commits. Installed continuation uses the same retained-result transaction and uninterrupted activation checks as shipped content, including synchronous focus for an admitted touch activation and revalidation after focus callbacks. Keep authored-original authentication, exact picture-choice confirmation and neutral input guards. See [Versus session formats](../../../docs/versus-session-formats.md); this partial P07 feature does not complete ordered campaign tours or complete-theme retention.

For same-page **Chapters in Couch Versus**, follow [the chapter browser contract](../../../docs/versus-chapter-browser.md). Keep the accepted read-only picture owner separate from the installer that acquires the exact profile writer lease and from the independently staged candidate reader. Capture activation and complete race/recipe/art/series/scope before download; recheck exact installed content, confirm and adopt all new ownership before retiring old media. Cancel or foreground loss must immediately release busy controls even when decode is unresolved; completed installs stay available without granting launch authority. Preserve current boards/results, paused Stay/Replace, terminal cancellation copy and newer callback-owned focus. For a touch click that leaves Play unfocused, capture the attempt before admitting focus to the open catalogue; recheck after that callback. Play is already disabled during work, so do not rely on focusing it. Preserve native decision return and Stay focus, and keep shipped play available if optional catalogue construction encounters denied storage. Test no Solo writes and one-action start through the mounted host. This slice covers embedded optional downloads and checked installed content, not the full unified community/campaign/mission browser or Team installation. Prompt: “Exercise Chapters → Download & play, then held decode → blur/return, Stay, and results replacement with exact picture/checkpoint/score assertions. Prove independent writer ownership and newer focus before qualifying native input and public bytes.”

Menu/audio `pageshow.persisted` reconciliation reads validated current storage and preserves unsaved/session-only local intent. Storage reads, listeners and dialog close callbacks can retire their owner or produce newer intent. Recheck ownership before writes/focus; terminal cleanup retires before closing native surfaces. Test late audio activation after mute, stale restoration, failed saves and actual browser Back separately. A cold Back page load is not BFCache proof. Interface appearance does not pin a complete game theme or change existing map/art identity; exact complete-theme restoration remains a later qualified contract.

When race formats change, cover actual encounter-host routes in both One race and First to two, with both turning policies. Assert the format-specific Race ended/Round ended copy and Rematch/Next round action alongside independent phase cues, legal win ticks and frozen terminal states. Preserve the failing predecessor; do not weaken gameplay assertions to accommodate an obsolete label or count the corrected focused suite as complete source qualification.

Browser history can restore stale select, range and checkbox values before or after `pageshow`, even when the current preference record has not changed. Attach `attachPreferenceRestoration` after the existing authority listeners and repaint from their current snapshots immediately and in a guarded next task. Never treat browser-restored form state as a user edit or persist it. Keep failed-save warnings and newer session intent, cancel deferred work on departure, and retire it with the host. Team restoration uses the pure field renderer; only real preference changes own focus-clearance work. Model both timing boundaries through all three hosts and distinguish that evidence from actual BFCache admission.

Settings tabs have different sequential and spatial navigation contracts. Tab/Shift+Tab include only the selected category, then its visible controls; controller direction still reaches inactive categories for deliberate activation. Test both paths through the mounted host, not just helper listeners. Text-only Settings content needs the existing bounded reading surface so keyboard and controller users can read below-fold copy and return to the same paused attempt. Inspect the actual loaded correction in a fresh local origin when browser caching retains an earlier candidate; a successful file response alone does not prove the running page adopted it.

Finalize a reader's active hint before revealing its unit; longer text can wrap and move the lower focus border outside a short dialog. Recheck the accepted reader, scope, revision, focus and foreground after callbacks, and suppress stale reveals after Back. Observable test waits use a real Node monotonic deadline independent of the simulated gameplay clock. A frozen `performance.now()` must not turn an unmet preparation predicate into an unbounded release gate; preserve the original assertion and correct stale hidden-control routes through their actual category.

For ordinary Solo result continuation, stage the destination media before replacing the completed run or releasing its picture. Retry carries complete picture/story pins, including explicit no-story choices; a difficulty bridge must validate every original authored identity. Keep status and Cancel beside result actions, recheck setup and ownership after awaits or callbacks, and reject stale focus/adoption. Test with legal wins and a held destination decoder after earned thumbnails finish. Skip the actual celebration to reach Retry; retain the old result, picture, checkpoint and records through status, Cancel and failure. Await ready adoption instead of asserting synchronous replacement, and preserve held-Confirm isolation plus fresh Resume after Pause or foreground return. Do not weaken history-return writer policy or turn preparation into another award path. See [Solo result continuation](../../../docs/solo-result-continuation.md); this source contract does not establish other-mode or public acceptance.

When browser input produces unexplained repeated keys or latency, reproduce it on a blank page and retain bounded event counts before changing the game. Pair keydown/keyup and verify platform key-code semantics: the recorded macOS driver failure sent Windows `nativeVirtualKeyCode` values, while the corrected paired CDP route omitted that field. Recheck the ordinary game without probes or runtime overrides and confirm idle input is quiet. Preserve original failures and observation bounds; pointer plus emulated keys do not establish keyboard-only, physical-device or general performance acceptance.

For host navigation regressions, enter the actual active surface before changing selection: Missions → Prepare for setup selectors, and Library → Expansion packs for installed campaign Play. Title Start launches the selected mission; Title Continue resumes deliberately, while ordinary Load prepares a paused flight. Confirm unfinished-flight setup/restart decisions through their visible controls. Await the exact pack/campaign/picture and operation completion instead of an already-ready old picture. Keep checkpoint, replay, storage, identity and cancellation assertions. Native focus refuses inert/hidden/closed-dialog descendants; model that boundary locally when relevant instead of changing production focus recovery to match an impossible fixture state. Verify all registered resize observers retire without fixing their implementation count. Direct handler tests remain useful for promise ownership but do not establish a visible keyboard/controller journey. See the [P03 host-flow corrections](../../../docs/verification/cross-mode/p03/host-flow/README.md).

For phone Missions, use the shared body/control text roles for the menu heading, preserving Standard/Large and Plain/Theme choices. Keep the complete title, all Back/setup/brief/deploy actions and at least 44px targets. Pair sticky footer offsets with local dialog padding; preserve the separate direct-Brief layout. Measure the entire focused card and outline between both sticky bands at 280×568 and 360×568, then the 360/361, 480/481 and 799/800 boundaries. Earlier 280×800 evidence does not establish shorter-phone clearance. Prompt: ‘Fix narrow Missions chrome using semantic text roles; retain all actions, deliberate reading, focus ownership and the original failed measurement. Verify actual chapter, mission, setup and briefing journeys before accepting.’

When composing independently revised presentation histories, seed the exact accepted or explicitly identified published prior ledger and its coherent compiled directory before running the producer. Equal theme IDs/revisions do not authorize combining different records. Append a successor, retain every prior record and payload, and regenerate all compiler-owned output. An independently pinned prior oracle must verify candidate prefixes before reconstruction; prove original-bundle identity, exact additions and repeat retention/export/compile identity. Hydrate optional producer inputs explicitly in sparse checkouts. Published source is not public phase acceptance; keep both source boundaries explicit until acceptance closes.

Read [Delivery priorities](../../../docs/delivery-priorities.md) for current source, release and verification status, and the approved [cross-mode P00–P18 register](../../../docs/cross-mode-execution.md) with its [current prompts](../../prompts/cross-mode-delivery.md) for phase sequencing. Keep historical contracts and evidence below as compatibility context, not current release certificates. Do not advance a cross-mode phase before its exact-source qualification and public acceptance. Team asset integration is a separate P08-A requirement: broad `couch` metadata or matching colours does not prove decoded shared assets, approved map art, responsive controls or Team state coverage. Keep inventory, modeled input, browser interaction and physical-device evidence distinct.

For disabled actions, preserve native disabled semantics and the shared muted/panel/dashed-border treatment after page-specific primary and hover rules. Do not make unavailable Start look enabled or change handlers to solve a styling conflict. Verify computed styles during a real pending operation and after readiness, then activate by keyboard. Example: “Delay the exact original picture response, inspect disabled Start beside Cancel, retry, and verify accent styling returns only when Start is enabled.” Record artificial network timing separately from normal loading performance. See the [scoped Couch check](../../../docs/verification/cross-mode/p05-a/disabled-controls/README.md); broader primary-action hierarchy and physical-device acceptance remain separate.

For loading feedback, follow the [P01 presentation loading contract](../../../docs/presentation-loading.md). Start a scoped operation lease before delayed work, retain the host's cancellation and commit authority, and keep feedback and Back/Cancel visible beside the active controls. Stop waiting never means a shared installation or durable save rolled back. Capture focus ownership before hiding a focused action; preserve component child nodes and copy keys when changing labels. Pending native exports keep their lock across Retry and new-attempt preparation. Extend the A01–A56 inventory for new entry points, qualify delayed/cached/error/cancel/stale completion paths, and distinguish modeled input from browser and physical-device evidence.

For Couch Ready → Start, authenticate the prepared picture without surrendering the original action's focus lease. Admit a direct touch click even when the browser leaves focus on BODY, then recheck the exact run, generation, controller, foreground and main scope after that initial focus. A newer deliberate focus choice permanently revokes this launch, even if focus returns to BODY. Keep ownership through completion UI and input cleanup; recheck immediately before resume and release the lease on every terminal path. Successful authentication may leave the same attempt Ready for a fresh Start. Delay the actual installed-content lock in host tests and verify Tab/pointer focus, blur, Cancel/Retry, cleanup reentry, unchanged picture/checkpoints/profile data, direct touch and the synchronous shipped path. Modeled fixtures do not establish native keyboard, touch or controller acceptance.

Test picture failure before Start joins background preparation. Setup selection alone can expose recovery actions; its explanation must also reach the inline overlay presenter. Publish a terminal fallback only without an active launch, preparation or theme owner, and preserve the original stale/abort fences. Check actual message visibility beside the actions in native portrait and short landscape; visible buttons alone do not prove visible recovery feedback.

Distinguish a settled recovery path from an active preparation. Opening Library from Export game data must retain the same owner's terminal picture error and Reload action, so closing the dialog restores the visible Export invoker. Bind this state to the exact picture owner, run and theme; clear it on actual retry, world/context replacement, restore and page retirement. Do not mask a broken return by adding another Check picture click to the test. Preserve pending cancellation, immutable pins and writer ownership; see [the export-return finding](../../../docs/verification/cross-mode/p01/picture-recovery-return.md).

For pending Start, Retry and confirmed Restart, route keyboard Escape and controller Back through the existing picture-launch Cancel action after modal/course handling. Require the actual pending launch and its cancellation owner; preserve other preparation owners and settled recovery. Test mounted keyboard listeners and modeled controller routing with held decode success/failure, saved-state identity, input neutrality and return focus. Native visibility and physical-device evidence remain separate.

For native file-containing dialogs, handle `cancel` only when its target is the dialog itself. File-picker dismissal and same-file reselection produce a bubbling input event, which must not close the parent, cancel its task or collapse its details. Preserve the selected file, unsaved draft, reviewed backup, original focus and busy lock; retain the existing genuine dialog Escape/Back policy. Dispatch both event shapes in the host fixture, and distinguish that modeled propagation proof from a trusted browser event and physical OS-picker interaction. See the [file-dialog correction](../../../docs/verification/cross-mode/p01/native-file-dialog-cancel.md).

Capture a dialog's invoker before disabling it or awaiting its first source read, then pass that target explicitly while preserving other callers' defaults. If actual closure occurs while the host Open still waits, detach that pending owner and reenable the invoker before the panel restores focus. Keep the existing first-Back cancellation policy; do not treat cancellation alone as closure. Test disabled-control blur, held panel reads and subsequent deliberate focus: late settlement must not refocus the old invoker, overwrite current feedback or unlock a newer operation.

Required game operations need reachable controls in the active shell. Replay export uses its original Workshop button beside Replay Theater, keeping the recorder, export handler and pending-download lock. Place the original status and Download/Close controls together in the shared dialog operation rail before the long readonly JSON; keep lengthy status text keyboard-scrollable and do not force focus or scroll to announce completion. Test Pause → Main menu → Workshop with normal scoped keyboard/controller traversal, exact replay/checkpoint identity and settled return to the same visible opener. Do not reveal the legacy footer, duplicate the action or call a hidden control to claim a usable route. A pending browser download stays owned after closing; do not invent cancellation or force focus onto its disabled opener. Qualify initial status/control visibility at short landscape separately from reachability after scrolling, actual downloads and painted busy frames.

Keep active status labels short enough that changing progress cannot displace its count and Stop action. Put optional-pack catalogues and background explanations in expandable details. Offline UI prefers the concise observation/result `summary`, retaining the full `message` for compatibility and diagnostics. Test the complete shipped optional catalogue at phone portrait and short landscape, including the transition from the activating button to busy status; an empty catalogue cannot expose this reflow problem.

The Library sticky operation area moves original status and Cancel nodes without creating new presenters or transaction owners. Preserve exact sibling restoration, same-owner focus and close/reopen behavior. Cancelled-but-unsettled backup work retains its existing lock; its final cleanup must target only its own status. Check deep-section activation before any driver scrolling, full keyboard-readable details and 44-pixel escape controls at portrait and short landscape. Source fixtures prove ownership; native layout and painted frames require separate evidence.

Measure sticky feedback's actual bottom when exposing the live focused control; also measure a sticky footer's top when it bounds the reading area. Missions keeps heading and feedback in one box and uses `attachFocusClearance` for both boundaries. Replay uses the same helper with its operation rail, measured scroll padding and a small textarea scroll margin; keep the first JSON line clear after Tab/PageUp and text reflow while the textarea retains its own scrolling. Do not follow a remembered opener or react to ordinary scrolling. Keep Close above the feedback reachable, and clamp a tall editor's adjustment so its beginning remains visible. Verify painted focus and hit targets; viewport bounds alone cannot detect one element covering another.

Replay export JSON is a user-readable control: its owning `#replay-json` rule must use the shared control-size token while retaining the mono font, 1.5 leading, readonly data and existing scroll/resize behavior. Distinguish this Solo export field from the standalone Theater import editor. Prompt: “Export a real paused route through Workshop with keyboard and controller; confirm exact replay bytes and download ownership, then compare Standard/Large and Theme/Plain at portrait and short landscape. Inspect the first JSON line below sticky feedback, full focus paint, internal scroll, selection/copy and return to the actual opener. Do not treat modeled host tests or Large mode as browser/200% zoom certification.” See [supporting-screen readability](../../../docs/supporting-screen-readability.md#replay-export-json-reading-size-correction).

For Missions chapter rails, reveal only the currently focused owned card inside its bordered rail/section before measuring the outer sticky heading and footer; preserve the shared helper's default behavior for other dialogs and retire stale callbacks without selection, focus or launch writes. At narrow widths give the complete Large heading and Back separate rows when necessary, keeping 44px actions and direct Brief unchanged; inspect actual loaded-font paint and both sides of the 360px heading and 800px rail breakpoints, since modeled geometry alone cannot prove readable labels. See [Missions navigation](../../../docs/missions-navigation.md) for the candidate scope.

For the still-picture workshop, bind the existing `attachFocusClearance` to the dialog and its actual operations rail, opting into `includeControlLabel` so the field purpose stays visible with its native control. Preserve the default control-only behavior for other dialogs. If the complete wrapper cannot fit between the measured rails, fall back to the focused control so a tall label cannot move it below the viewport; keep ordinary reading scroll available. Keep status, Cancel and Close together through pending, settled and failed work; do not replace their operation owner or remove sticky feedback to expose a field. Test the focused file input after successful preview and reverse traversal using the recorded overlapping rectangles for the entire label/control wrapper, then resize with that same live focus. Ordinary scrolling must remain deliberate, closed panels ignore queued work, and disposal removes the observer/listeners and resets insets. Layout alone must not decode, save or change an assignment. Keep mounted geometry checks separate from native painted focus and physical input; see [the workshop guide](../../../docs/still-media-workshop.md).

Prompt: “After previewing a chosen image, reverse-traverse to the original file picker at 390×844, rotate to short landscape, and verify both its label and control clear the measured status rail without changing focus or media. During a held preview keep Cancel visible and authoritative; close/dispose before a queued layout pass and preserve original bytes. Record actual browser bounds separately from the source regression.”

Report actual computed text settings in each host: the v0.61.8 picker correction has Standard-text browser evidence, but choosing Large in Game did not apply it to the standalone workshop. Keep that preference gap, the separate local-connections Close focus issue, and any game-entry authored-record creation visible in the handoff; do not infer storage neutrality or Large-text acceptance from a Standard-only preview journey.

For Controller Apply, capture focus ownership before disabling the initiating action. Native blur to body does not by itself relinquish ownership; subsequent user input, focus choice, hidden document or window blur does. Restore the existing Edit action only on current successful completion, clean up observers on abort/settlement, and retain transaction and binding identity. Native evidence must identify the active element/action, `:focus` and next Tab destination: unnamed buttons can have an empty ID while correctly focused.

Check the active top-layer owner as well as viewport bounds. Craft feedback uses its original renderer status node inside Missions while selection or direct briefing is open; restore that exact node on actual close/disposal and ignore a queued close from an earlier visit. Keep one presenter and the renderer's existing load-token guards. Cosmetic readiness must not block Deploy or create a second cancellation owner. Verify the sticky slot with real selector changes at portrait/landscape and Large text.

Wait for real asynchronous work through its owned promise or `game/test/helpers/wait-for.mjs` and an exact readiness predicate. Bound elapsed time, not the number of immediate callbacks: Blob/WebCrypto, native module loading and replay timer yields can outlive many event-loop turns. Retain runtime deadlines and all state/pin/cancellation assertions. Short drains are only appropriate for known post-resolution microtasks. Preserve failed runs and reproduce scheduling failures before claiming a fixture correction.

For a JSON capacity failure, compare the actual UTF-8 document and transfer-manifest bytes with the declared limit before changing quotas or history. `boundedJSON` counts escaped primitive/key encodings and actual JSON punctuation; array indexes are not serialized keys. Never stringify an unvalidated container or invoke getters/toJSON to estimate size. Keep the final encoded-size guard and structural limits. Verify exact-limit and one-byte-over cases, then the complete immutable production review/replacement/export/import round trip; do not delete old revisions or raise budgets to make a capacity test pass. For trusted capacity-test fixtures, assert independent actual UTF-8 byte sizes before rejection expectations: each standalone collection must fit and the serialized union must exceed the declared limit. An under-limit fixture must not depend on a conservative traversal estimate to fail. Use valid bounded fields or history to establish the byte premise, preserving identity, unchanged-input and zero-write assertions. The [mastery Library fixture diagnosis](../../../docs/verification/cross-mode/p01/mastery-library-capacity-fixture.md) records this distinction; do not change runtime behavior or quotas to repair a test premise.

For real installed-original fixtures, use one documented local picture-readiness allowance for initial preparation and equivalent later waits. Audit every entry through that fixture, including saved Continue, confirmed Restart and recovery. Preserve explicit test-boundary overrides; do not expand the shared ordinary-host default or runtime deadlines to repair one fixture family.

The active [FPV Field Kit redesign](../../../docs/fpv-redesign-design.md) uses local Handjet display, Exo 2 UI and IBM Plex Mono telemetry through `game/ui/field-kit-{fonts,tokens,components}.css` and `body.field-kit`. Preserve `game-shell`, input ownership, saved identities and opaque Pause. Consult the [phase register](../../../docs/fpv-redesign-execution.md) before claiming studio/runtime adoption. New controls use the semantic tokens and documented states; preserve Standard/Large text and 44px targets. [Current authoring examples](../../prompts/fpv-field-kit.md) replace the old Pixelify guidance for new presentation work.

For native file inputs, use the shared `.field-kit input[type='file']::file-selector-button` recipe and [file-control guide](../../../docs/native-file-controls.md). Pair button text/background tokens explicitly, retain at least 44px targets, inherit the host's actual typography, and preserve focus and filename space with compact padding/gaps. Gate hover/active with `:disabled` as well as aria-disabled so disabled fieldsets retain their appearance. Preserve the real input, chooser, label and change handler; never add a proxy picker to solve contrast. Match evidence to the exact CSS revision: R2's eight Motion font/viewport cells, R3's narrower desktop/Theme-Large/Studio/upload checks and R5's three-viewport Still Media containment/PNG checks are bounded observations, not full-state or release acceptance. Their retained CUA receipts contain transcribed observations, not retained screenshot bytes. Same-file reselection does not prove OS chooser Cancel, which remains unverified in this candidate. Studio's transparent native inputs require visible label/focus and chooser checks. Preserve `minmax(0, 1fr)` label tracks in Still Media so native file inputs and selects shrink inside the dialog without clipping. The CSS study’s settled preview focus-on-BODY finding has no verified baseline. The separately scoped Preview focus correction below now composes; bind its chosen/saved/authored native checks independently of the CSS study. Standalone Video Poster/Still Media global display-preference adoption remains broader P05 work. Do not infer those hosts' Theme/Plain or Standard/Large behavior from Solo settings. The separate Motion delayed-startup receipt verifies real disabled-fieldset ownership and muted/dashed styling before readiness. Verify remaining hover/active, chooser Cancel and other native states; source ratios or fake-DOM assertions do not establish native rendering.

Locate the project from the current workspace or this source skill's project path. Read [development.md](../../../docs/development.md) and the relevant code before editing. The playable application is `game/`; the historical `authoring/motion-lab/` is a separate study. A preview class or catalog reference is not automatically implemented in the game.

For Playground loading, keep editor/replay/child status nodes in one bounded area with independent presenters and epochs. The newest owner reads first; concurrent pending Cancel controls stay reachable. Keep local message scrolling separate from page/focus movement and reconnect one clearance owner on persisted pageshow. Actual child readiness, not iframe load or prepared configuration text, finishes its lease. Imported Music audition must report outside hidden transport details and enable Finish after invoking play in the activation turn, before awaiting it. Verify late resolve/reject after Stop cannot restore status, audio activation, URL ownership or playback intent. These are loading corrections; shared master sound remains separately gated.

Keep original-recovery option and summary text aligned with the existing current verification handle. Redraw on successful verify and existing invalidation; preserve immutable inventory, opaque reader handles, identity checks, links and cancellation. Use past-tense verification text with an explicit file-preparation recheck, so an export failure does not falsely imply a fresh verification or earned ownership.

## History return and picture ownership

On `pagehide`, retire and abort speculative picture prewarm even when no Start observer joined it. Fence both success and failure by the exact prewarm lease. Preserve the permanent profile-writer release, immutable selected pins and decoded images on a history-cache return; existing saved Continue and explicit Resume must work read-only without new persistence or automatic movement. New choices may reuse exact retained release history. When missing history requires storage, refuse before downloading and expose specific export/reload recovery, retaining the commit-time writer check. Do not substitute legacy art, mislabel the condition as a missing original, reacquire the writer, or roll back a commit that already completed. Use the [focused history-picture contract](../../../docs/verification/cross-mode/p01/history-picture-return.md), and distinguish a held validation decode from the later pinned display decode in tests.

## Current native edition

Use [Delivery priorities](../../../docs/delivery-priorities.md) for present status; [detailed delivery history](../../../docs/current-delivery-plan.md) retains earlier evidence. Countercurrent’s recorded four-owner adoption had sixteen external choices, distinct from twelve installed slots at that checkpoint. Preserve prior descriptor hashes, source originals, exact saved/earned ownership and explicit install/choose actions. The [Countercurrent guide](../../../docs/verification/round-47/countercurrent-chapters.md) records scoped proof/test coverage and remaining native/release gates.

The [implementation roadmap](../../../docs/implementation-roadmap.md) retains the earlier approved plan; current ordering and acceptance belong to [Delivery priorities](../../../docs/delivery-priorities.md). Read [continuous steering](../../../docs/continuous-steering.md) for remembered input and explicit Resume. Release and centered sticks do not stop ordinary continuous travel; keep physical input gates separate from saved direction and queued turns. For an authored `rules.stopOnCapture:true` level, a successful cut instead emits `capture.stopped`, clears remembered/physical direction and queued turns, and waits for a fresh direction gesture while enemies continue. Holding the old direction cannot restart it; Resume after that closure stays neutral. Do not apply this stop to ordinary secured-ground travel, omitted/false recipes or historical `direction:null` recordings. Tutorial text and checks must follow the actual lesson recipe rather than promise one closure behavior for every edition.

First Light uses the isolated 72×36 `level.v3/core.v4/replay.v5` branch documented in [wide-engine.md](../../../docs/wide-engine.md). Obtain geometry from the run/level and update every consumer: capture, collision, renderer, editor, theater, gallery and couch. Preserve old 48×36 fixtures. Standard and Gentle route proofs are independent; do not regenerate old oracles to conceal a regression. [First Light](../../../docs/first-light.md) links its actual source, pictures and checks. Later classic versions are deliverable only when their own tests and registered behaviors exist.

For uploaded music use [Audio Director](../xonix-audio-director/SKILL.md). Library/playlist/media identity stays separate from score authority. One session player owns music; packs can supply an authored recipe fallback. Never reset songs on Retry or ordinary Pause. Archive a candidate from its exact committed source before release checks so parallel unfinished work cannot enter a validated version.

For P02-A follow [shared master audio](../../../docs/shared-master-audio.md). Inject one page-owned `createAudioMaster` authority into Soundscape, soundtrack transport, Studio auditions and story playback; master mute/volume change output without changing music Pause, selection, queue, position or gameplay. Lifecycle suspension retains its existing ownership and explicit-return rules. Host gestures own activation; pending Play/resume, metadata and library callbacks cannot unmute. Studio exposes its own reachable master controls while open. Compose native local volume with shared volume once, retain explicit local audition faders, and prevent native volume/play events from rewriting intent. Disposal releases only owned output bindings, URLs and duck leases; one replaced Asset Studio preview cannot stop or unbind the other.

Keep permanent audio capability explanations outside the transient storage-warning status. Team's visual-feedback-only sentence remains visible after mute/volume saves, write refusal and recovery; the shared preference callback may legitimately clear its warning with an empty string. Exercise those actual host changes without starting the arena. A paused audition or story may deliberately retain a local mute while master is unmuted; verify explicit Resume clears only that local pause gate, rather than diagnosing the paused mute flag as leakage. See the [v0.58.0 public review](../../../publishing/pages-controller/delivery/evidence/cross-mode-p02-a/public-v0580/README.md).

Changing-name Mute/Unmute actions are ordinary buttons: omit `aria-pressed` in both markup and updates, and keep their accessible name synchronized with the next action. A true toggle instead needs one stable name and a pressed state that represents its enabled setting. Follow the [W3C APG button pattern](https://www.w3.org/WAI/ARIA/apg/patterns/button/). Render playback/mute text from current transport notifications and current master state, and never let a late gesture result overwrite a newer transport state. Test immediate text before a timeupdate/frame, blocked retry, keyboard activation in Settings and nested Library, both persisted mute values, focus retention and unchanged paused transport/checkpoint; inspect native semantics separately from audible or screen-reader/device acceptance.

Use `game/audio-preferences.mjs` for the separate `revealline.audio-master.v1` localStorage `{ muted, volume }` record, not a profile-schema migration. Hosts retain profile/writer guards and legacy fallback, apply output before persistence, report session-only failures and reject stale profile seeds or mismatched storage events. A remote preference change never calls Play. Use the [bounded audio prompts](../../prompts/cross-mode-delivery.md#qualify-shared-master-audio) to verify races, multi-preview cleanup and navigation recovery. P02-A remains unaccepted until its correction passes exact-source/public acceptance; modeled media/context checks do not prove native decoding, audible output, browser-policy permission, physical-device volume behavior or completion of P02's later content work.

The current Field Kit pause presentation conceals the live board and HUD with its opaque pause surface; do not restore the earlier undimmed-board dock. Preserve the actual Resume, mission/restart and Main menu handlers, responsive action targets and focus-loss protection. Pause must retain the exact saved direction and buffered turns until explicit Resume, subject to an already-neutral capture stop. Check the embedded First Flight reader after Pause → End course as well as ordinary flight.

For the player-facing [Field Guide](../../../docs/enemy-guide.md), use the isolated practice handoff and strict return token. Keep the parent run paused, suppress its controller polling while the child owns focus, preserve prior music intent and never write practice awards. Paint the selected role through flight’s current compiled snapshot and shared painter; acquire the registered original-body lease only for the existing thematic/manual fallback. Retain the separate line-impact diagram. Release any fallback lease on close, topic/theme change, page hide and practice entry; late image completion cannot advance the sampled clock or replace practice status. The guide’s 56 px illustration is a presentation-only frame copy: label it as enlarged and retain the actual center contact cue, sampled pose and collision radius. Display the 192×112 logical canvas at 288×168 CSS pixels on desktop and 192×112 at ≤480px; clamp its width to the container with the 12:7 ratio intact. Do not resize live actors or alter collision, timing or movement.

For the isolated P05-A [shared display candidate](../../../docs/shared-display-preferences.md), use `game/display-preferences.mjs` and its separate `revealline.display.v1` record for the existing `pixel`/`plain`, `standard`/`large` and boolean Reduced effects values. Shared or explicit intent wins over legacy Solo fallback; reads and system changes never rewrite raw storage. Only explicit Solo edits mirror its legacy profile through the existing writer; Couch hosts never write Solo progress. Apply display changes through DOM/painter state, never preparation, simulation reset or Resume. Effective reduction is saved OR system, while controls retain the raw choice and explain a system override. Preserve semantic font/size roles and essential Team cues. Qualify paused checkpoints, cross-page restore, denied storage, real Options navigation and native Large reflow separately; source tests do not certify browser/device readability or complete Team art adoption.

For the scoped [Studio readability candidate](../../../docs/cross-mode-execution.md#p05-supporting-screen-readability), keep one Studio-owned display authority and a read-only motion view. Interface subscribers update existing datasets, values and notices only; never rebuild the inspector, draft, prepared media, sprite history or audio. Resolve host font/palette aliases on the body and mark real host text/actions at creation; selected-file fonts and authored component specimens retain their own identity. Retain raw reduction separately from the system cap, fence retired preview callbacks and reset only a changed effective-motion clock. Studio never writes Solo progress.

Keep the Asset Studio guide in its native header disclosure, before the changing workspace/inspector and outside authoring busy/inert regions. Startup rendering below the reader must not displace its focused summary. Opening and closing help must not navigate, acquire an operation, require settled edits, rebuild previews, or alter draft/prepared bytes, provenance, sprite history or playback. Consume guide-close Escape and its held-key repeats before they reach global operation cancellation, including repeats after focus moves. Release ownership on keyup or a fresh press so a missed keyup cannot suppress a later action. A separate fresh Escape retains the normal editor/operation action, as does a press begun in other authoring controls; retain native summary Enter/Space behavior. Mount Close before exposing it, return only owned foreground focus, and dispose scripted handlers only on terminal departure. Verify held operations and subsequent exact saved bytes through the actual host, and qualify native long-reader focus/reflow separately. Use the [in-page guide maintenance prompt](../../../docs/asset-studio.md#in-page-guide).

Studio's shared font cache is keyed by original byte hash and accumulates compatible requested weight ranges. A failed descriptor update must retain the prior usable registration; Retry cannot duplicate successful loading. Inspect actual selected-role EN/UA specimens and real FontFace matching separately from modeled cache tests. Inventory selection replaces rows synchronously: remember whether the foreground trigger owned focus, then restore its selected replacement on desktop or retain the foreground narrow Inspector handoff. Rejected prepared-upload/dirty-sprite selections preserve the original trigger and work. Do not restore focus from a later Promise, timer or frame. Cold Back selection and disabled mutation-trigger focus remain separate P04 corrections.

Adopt only the reviewed font → Studio F2 → inventory-focus hunks against the current source; do not overwrite newer hosts or import unrelated P04 preview work. Preserve original receipts and failed attempts, do not add overlapping case counts, and qualify the actual composition. Use the [bounded Studio prompts](../../prompts/cross-mode-delivery.md#qualify-studio-readability-and-draft-preservation); native reflow, BFCache, device and public checks remain separate. No Studio candidate pass completes P05.

For [Text size](../../../docs/text-size.md), apply the actual merged preference at startup/save/import/Undo; inspect Standard and Large at 320×568, phone landscape and desktop. Check visible labels and focus after reflow, not only computed font sizes.

For [supporting-screen readability](../../../docs/supporting-screen-readability.md), keep Replay's Interface outside its inert boot region and mount the shared display owner before asynchronous startup. Pass effective reduction/text face to the painter without rebuilding the recording or changing playback intent. A terminal page exit disposes early controls/status owners and rejects late boot. Persisted return resamples current storage and system policy without saving; failed-save explicit intent and its warning win over foreign stored values. A duplicate storage event must not advance preference revision or repaint twice. Qualify all shared consumers plus Studio's prepared-original/dirty-draft restoration, rather than adding independent candidate test totals. Keep genuine BFCache, physical input, current-source qualification and public acceptance distinct.

## Choose the real contract

The separate [read-only profile reader](../../../docs/profile-recovery.md) at `game/profile-recovery.html` discovers exact channels and prepares stored-data diagnostics; it does not override ordinary app keys; the later same-host Settings entry composes its reusable view without importing the standalone bootstrap. Keep `release-v…` and `release-…` separate, preserve the transfer's deterministic newest-32-candidate policy without reading values during discovery, and hold the selected writer/backup locks through stable reads and export revalidation. Open the existing assets DB without a requested version; refuse creation, distinguish absent/null/unsupported values, and never open shared media or execute a pending journal. Preserve raw strings and first-receipt text; invalid components remain explicitly unverified. Prepare asynchronously, then retain a native download link for explicit activation. Cancellation must settle promptly while late native callbacks release their leases without reading. The [native check](../../../docs/verification/round-47/profile-reader-native.md) covers one current-version profile and return to its own game's paused flight; it does not qualify historical replay, original-media backup, migration or offload.

The separate source-only shared recovery snapshot API requires an explicit exact-channel historical campaign/descriptor registry and keeps the raw diagnostic independent. Open only understood existing media DB2/3/4 schemas without a requested version, abort creation, and use readonly transactions. Preserve present/absent/unsupported distinctions, pending journals and reservations; never run their cleanup. Hold the same channel leases through raw fingerprint and shared ledger/generation revalidation, retain immutable Blob handles, and label bodies unverified until a later verifier hashes them. The extracted pure external catalog retains normalized pack/descriptor/original metadata closure; stored picture owners never become playable registry entries. This source boundary adds no app controls, backup components, paused restore, migration or physical removal.

The selected-original API in [profile recovery](../../../docs/profile-recovery.md) adds only `reviewOriginals`, `verifyOriginal` and explicit `exportOriginalComponent`. Keep the original full snapshot strict; only the separate availability snapshot may diagnose unrelated missing/length-mismatched bodies. Retain complete trusted catalog, stored owner/reference, metadata and marker checks. Run the existing still decoder and byte hash against a fresh selected immutable Blob, compare every captured asset field, and revalidate before and after under the same exact channel leases. Export re-verifies fresh bytes; same-key/same-size replacement cannot inherit an old verification. Copied/stale handles and per-call overrides refuse. Produce an exact image or a separately bounded identity report, never partial `.rlmedia`, a full backup, an earned receipt or restore authority. Keep no-upgrade readonly storage, cancellation/late-result handling and caps unchanged. The standalone screen now owns selection invalidation, explicit native download links and independent URL cleanup. Its pinned historical registry supports only the explicitly listed v039/v040/v041/v042 aliases no newer than the built page. Preserve complete source campaigns/classes/themes and descriptor cohorts; never substitute current content or register stored owners. Registry failure leaves raw diagnostics available, and unbuilt/dev pages gain no originals authority. Include the complete finite recovery graph and registry in packaging before native qualification. No selected-file check grants adoption, migration or deletion authority.

The [optional Picture Workshop story capability](../../../docs/story-workshop-authoring.md) consumes the authored binding model through injected `storyStore`, `storyInspection`, `onStorySaved`, `makeStoryId` and `requestStoryDownload`. Follow the [workshop requests](../../prompts/story-workshop.md). Keep the same task/cancel guard and controller router; wire `panel.back()` only when the coordinated host adopts the capability. No injection means no story controls/store access or default database change. New source+descriptor+full-poster binding commits in one CAS after separate Prepare/Save; notification rejection after commit must say saved. Preserve exact video across ordinary captured-PNG draft/save, explicit null and immutable history, native Prepare→Download activation, and default keep-current `.rlstory` restore. Test stale selection/leases/URL allocation and held Confirm; retain all original budgets. Authoring a choice does not extend old sessions/earned receipts or qualify a real win, browser codec, physical controller, offline playback or delivery.

- `game/core/index.mjs` exports the simulation API and `validateLevel`. Keep DOM, Phaser rendering and authoring-file concerns outside that core.
- `game/content/campaign.json` contains explicit `xonix-level.v1` levels inside `xonix-campaign.v1`. Use the actual validator; do not substitute the older game-pack, media or collection schema.
- `game/content/classes.json` configures registered ability primitives. Preserve class revision and the core's `loadoutHash` in result identity; use `campaign.classRecipes` when validating custom campaign recipes. Never derive that identity from a body or reimplement the core hash in the UI.
- `game/content/themes.json` and declared presentation bindings own appearance. A body image, rotor count, material or theme does not silently change movement, collision, enemy behavior or class statistics.
- `game/build-config.json` owns the static build allowlist. Include required modules, assets and licenses when adding dependencies. Preserve relative imports so source, `dist/` and a nested saved version can all load.

Read the exported functions and current data before selecting names or fields. New primitives, enemy types, save migrations or targeting domains require implemented behavior and tests; arbitrary JSON strings cannot create them. Preserve authored `immediate` and `grid-center` turning, and test both for changes affecting movement, input buffering, dashes or contact order. Cosmetic replacement must leave those mechanics unchanged.

For the finite staged encounter, read [Sentinel Relay](../../../docs/sentinel-relay.md) and `game/core/versions.mjs`. Preserve the exact legacy level-v1/core-v2/replay-v3 branch; level-v2/core-v3/replay-v4 owns the new encounter state/checkpoint section. Never add empty encounter defaults or new checksum fields to old runs. Resolve the selected campaign's actual version pair rather than globally changing `RULESET`. Pack-v3 requires empty `masteries`, scenario-v3 requires null `masteryDefinition`; this is not a new optional-goal predicate.

Use `game/core/encounter.mjs` for phase, fresh-trail counting and isolation authority; the renderer only reads them. Preserve contact-before-closure, repeated openings, safe/no-cut recovery exclusion for isolation, sole field seed and centered stationary actor. A short ordinary closure keeps its captured cells; automatic isolated release must not emit a fake `cut.closed`. Run `sentinel-playthrough.test.mjs`, relevant `encounter-*.test.mjs` and `compatibility-v0100.test.mjs`, preserving old proof files. The [ten finite authoring prompts](../../library/sentinel-relay/prompts.json) and source recording helper require legal input traces, not state assignments. Portable replay/session/backup and current catalog adoption must share version dispatch; theme effects and music cannot advance simulation.

For equipment policy, use `arcadeActionCapabilities(actualLevel)` from `game/core/arcade-actions.mjs`; labels, themes, unlock progress and cosmetic bodies are not capability authority. Authored direction-only Arcade masks manual ability, pickup and Boost, with bonuses activating on contact. Tactical/manual recipes retain authored equipment; Supply additionally requires a charged-capacity craft and actual supply pads. Keep visible controls, keyboard/controller hints, help and Guide practice instructions consistent with those capabilities. Mission completion unlocks the next challenge/picture; it does not silently grant Scan, Supply or Boost.

For controls, read `docs/controls.md` and `game/key-bindings.mjs`. Keep mappings separate from simulation rules. Preserve Escape pause, browser/editing shortcuts, unconditional key release, cancellation without partial adoption and visible hints. New preferences require bounded validation, additive migration and portable-backup tests. Archived release profile channels must remain isolated from incompatible new schemas.

For the original solo Text style contract (with shared-authority precedence in P05-A above), keep `textFace` strictly `pixel` or `plain`, independent of `textSize`. Only an omitted field migrates to Theme font (the compatible stored `pixel` value); never rewrite raw profiles on read. Apply the actual merged preference through the semantic display/UI/numeric font roles at every preference refresh, including import/Undo. Reusable QA prompt: “Change Theme font/Plain with Large selected during a paused cut in both turn policies. Verify the exact saved slot and input direction, resume explicitly, and check backup round-trip, old-profile migration and denied storage. Inspect real title, Settings, guide, Collection and short mobile layouts for clipping and legible focus.” Keep historical serialized oracles exact after removing only the newly asserted default. Do not claim that solo font adoption also covers the separate couch/authoring pages, full 200% scaling or physical device certification.

When adding a preference, review every old explicit default projection and portable-byte oracle before the full release gate. Historical inputs and checksums stay unchanged: assert the new omitted-only default, remove only that named addition from the owned comparison, and retain exact old-object/byte equality. Reusable QA prompt: “Search migration, archived compatibility, rewards and complete-backup fixtures for explicit preference expectations. Confirm old raw storage stays untouched and round trips retain the new setting.” Keep modeled DOM properties used by the change faithful to the browser; for control-group moves, verify repeated synchronization preserves node identity, focus and ordering without another move.

For sparse test preparation, include the actual bootstrap JSON requests as well as module imports, explicitly named originals and test media. A static import inventory alone is not a complete fixture proof: the bundled pack index can be requested through a runtime constant. Preserve missing-input failures and restore exact Git inputs. A known missing fixture should not consume repeated readiness deadlines. The final unfiltered gate must use the complete Git source; use that gate to verify an otherwise reviewed correction when another heavy sparse rerun would only duplicate it. Never count an interrupted or incomplete setup as passing.

For steering controls, preserve Field Kit's validated `touchControls` mode/side/size/opacity and its current null/default behavior. Keep legacy `screenSteeringHand` strictly Left/Right. Only an older profile with an explicit legacy hand and an omitted touch object maps to its saved D-pad side; an existing touch object or explicit null stays authoritative. Do not write storage merely while parsing. Use one visible Steering hand selector, synchronize the compatible field on an actual hand change, and report session-only persistence failures. Reorder the existing touch-surface, direction and action groups so DOM focus order matches their visible side without CSS reversing them again. Preserve cardinal node identities, input/replay rules, Auto/Always/Off, safe-area constraints and target sizes. Reusable QA prompt: “Check legacy hand-only and current touch profiles, switch sides/modes in paused Settings, preserve the exact checkpoint, then Resume and complete a cut. Verify import/Undo, denied storage and visible keyboard focus. Inspect fullscreen short-landscape warnings and Large text on both sides.” Source/model checks do not qualify physical touch, controllers or device comfort.

For solo controllers, read [controller navigation](../../../docs/controller-navigation.md), [controller practice](../../../docs/controller-practice.md) and the [Round 15 implementation status and remaining plan](../../../docs/round-15-controller-plan.md). Maintain these responsibilities:

- `game/ui/controller-router.mjs` reads one fresh pad snapshot per app update. It owns standard mapping, deliberate neutral-then-join assignment, connection generation, scope gates, button edges and bounded time-based menu repeats. Only the exact `flight` scope emits gameplay. Preserve selected-pad loss detection when another controller remains; a reused index or replacement cannot silently take over.
- `game/controller-bindings.mjs` owns the exact complete `xonix-controllerbindings.v1` document; [its contract](../../../docs/controller-bindings-contract.md) and [router adoption](../../../docs/controller-router-bindings.md) describe the required maps, axes and thresholds. Validate/compile once at adoption, not per frame. A rejected candidate leaves the current configuration and input history intact; an accepted candidate clears history and gates on both contexts' mapped controls, enabled sticks at/below release, and fixed join buttons. Keep current equal 0.35 defaults, digital precedence and vertical analog ties.
- `game/ui/controller-settings.mjs` edits a complete draft, allowing temporary conflicts but rejecting them at Apply. Preserve Cancel, defaults-in-draft, source-change invalidation and focus restoration after controls are enabled. The host adopts synchronously; an asynchronous host must honor the provided signal/isCurrent guard before side effects. Use configured text labels in help and editor prompts. Explicit select/slider editing is not physical capture or automatic calibration.
- `game/ui/controller-navigation.mjs` operates the existing enabled, visible DOM controls within the active scope. Select/slider browsing is a draft; Confirm commits once through the existing handler, Back cancels. A stale scope, node, value, option list or range cancels its draft. Keep protected transaction cancellation, logical gallery focus restoration, keyboard/pointer handoff and visible focus inside the open dialog. Its explicit reader API accepts only visible, named first-party `data-game-reading` regions with valid origins. Handle reading before ordinary focus/activation, clamp vertical scrolling, consume exits/invalidation and preserve native keyboard/pointer handoff. Back/Confirm/Menu end reading without also invoking the parent action.
- `game/ui/input.mjs` accepts the app's cached canonical flight command through `readControllerCommand`. That mode must not poll hardware or own another disconnect observer. Preserve keyboard/touch arbitration, held ability semantics, one-shot pause/Stop, recorded release boundaries and both steering policies. The app consumes Hangar once through the existing switch UI before polling flight input.
- `game/app.mjs` selects the current scope, samples once, routes UI, then polls input. A scope transition clears cached flight commands and requires neutral. Inactive handlers must unconditionally clear commands and navigation, including ready, results and celebration states where `pause(true)` can return early. Blur, hidden pages and native inactivity also clear preview input. Resume remains explicit. History-cache return must retain a usable UI while respecting the released profile-writer lease.

For reading integration, `game/ui/controller-reading.mjs` binds `overlay-reading` and `mission-brief-reading` plus the explicitly registered Help and Collection surfaces, retaining the full authored brief and seal explanation. Keep Collection pictures/search primary and place progress, reader controls and appearance rewards inside its native Achievements and appearances disclosure. Closed contents must stay out of navigation. Collapse ends reading without resume and restores only focus that would remain hidden in the same open Collection; delayed toggles must not steal newer focus. Hide the gallery pager for zero or one page and restore disappearing pager focus to search without disturbing other focus. Keep stable entry/Done controls and mapped local hints outside scrolling text. Register Done through the navigation adapter's optional `beginReading({region,origin,label,exit})` field: its primary pointerdown preserves the reader for normal click-based focus return, never activating on pointerdown; cancellation and other native input still relinquish. The host pauses a running flight before entry; exits never resume. `onReadingChange` must clear input through the narrow `clearInput({preserveNavigation:true})` path, which skips only navigation reset. Ordinary clear/blur/hidden/disconnect/pagehide still cancels reading. Do not clear the preview bridge on each reader transition: the router must observe neutral before accepting another gesture. With keyboard menu navigation enabled, permit focus only between the text region and its exact registered Done control when they are adjacent in native tab order; other destinations still end reading. Preserve native Enter/Space activation on Done and its disabled inactive state. Capture the actual Tab neighbor before disabling a focused Done, then recheck scope/root, foreground and focus after host callbacks; do not bypass a new focus owner. Keep default non-keyboard reader cancellation unchanged. Model real Shift+Tab as separate Shift keydown, Tab keydown/keyup and Shift keyup; a modifier alone must preserve only a valid current keyboard reader while still notifying native input. Retain foreground, scope/root, ownership, prevented-event and other-modifier guards; never weaken them to compensate for an automation focus limitation. Follow the keyboard Done maintainer prompt in [controller navigation](../../../docs/controller-navigation.md). Test real pointerdown→click order on Done, stale region/text removal, all exit actions, native scrolling, paused live cuts and terminal result surfaces. Read [the reader plan](../../../docs/round-22-controller-reading-plan.md) for the bounded scope; other long-form surfaces and controller direction toggles remain separate work.

For implemented [solo Boost modes](../../../docs/controller-boost.md), `game/controller-boost.mjs` owns the strict `hold`/`toggle` enum. Keep `preferences.controllerBoostMode` separate from the exact binding document and omit live latch state from all portable data. `router.setBoostMode` validates before full neutral reset; `cancelToggleBoost` is a Hold no-op and otherwise disarms only Boost. Pass actual running eligibility once per hardware sample. `input.onClear` may cancel that toggle and cached frame flag but must never call `input.clear`, `clearInput` or the router's full clear recursively; inactive polling also invokes it and must not starve menu joining. Preserve the local-only, nonpolling `localBoostActive()` accessor.

After each fixed tick, `game/ui/controller-boost-host.mjs` handles running→respawning by replacing the remaining frame's controller Boost contribution with accepted local input. Never repoll/drain actions, drop the accumulator, rewrite the triggering command or insert a replay release for this narrow cancellation. Cover life loss, shield absorption and live-cut Impact with a legal short recovery ending in the same render frame, both steering policies and mixed keyboard/touch sources. Default Hold remains exact. The separate **Solo controller Boost** select adopts the actual merged preference and truthful saved/session-only result; import/Undo and settings changes clear input. Mode/latched/label changes update the cue without per-frame announcements. Lab Release all changes physical values, not the game toggle; focus loss can separately pause and cancel it.

The [Controller practice lab](../../../game/controller-lab/index.html) uses actual game UI, not a second simulator. Its validated scenario handoff and `controller-preview=1` flag are required; `game/ui/controller-preview.mjs` additionally requires a same-origin iframe with a nonopaque origin. Current input/status formats are `revealline.controller-preview.v2` and `revealline.controller-preview-status.v2`, distinct from settings v1. Both envelopes require a per-load `session` of exactly 32 lowercase hexadecimal characters matching the iframe's `controller-session` query. Reject another load's session before advancing any sequence counter, even when its queued message comes from a reused WindowProxy. Input has exactly four finite axes in −1…1 and up to sixteen dense boolean buttons; the returned virtual device always has four axes/sixteen buttons. Retain strict source/origin checks, monotonic sequences, the 1200 ms lease and owned data. The bridge-produced `readSequence` acknowledgement proves a snapshot was sampled; parent Apply-stick must observe the neutral acknowledgement before sending its draft. Never manufacture that acknowledgement in the parent or extend its two-second pending deadline indefinitely. Do not add game-state, reward, storage or executable commands. Keep bounded status labels as text.

The lab now offers base, Fieldcraft, canonical Sentinel and one local Reading practice choice through normal preparePack/prepareScenario handoffs. The reading example is original local data, not a new installed expansion or an iframe mutation command. Retain all seven class choices, both steering policies and requested-versus-actual viewport reporting. Practice isolation lasts for the whole preview session, including campaign/map selection, completed-campaign navigation and leaving the original scenario. Do not derive it solely from a mutable `scenario` reference or let a completed saved campaign turn practice flight into overview routing. Preserve the non-writing profile lease and suppress progress, gallery, score and unlock awards. The lab may write the explicit `revealline.playground.current` session handoff; it must not install packs or replace the player's library. Existing stored data may still be read/exported through the ordinary UI.

Use `controller.clear()` for UI scope changes; do not call `preview.clear()` for every transition, because the router must observe the real held/released virtual controls. Preview clearing is a lifecycle operation. Parent virtual controls focus the iframe before sending, distinguish pulses from held toggles, clear timers on release/disconnect, and disconnect on hidden/history-cache transitions without treating ordinary parent blur from iframe focus as device loss.

For saved controller layouts, read [preference migration](../../../docs/controller-preference-migration.md). Keep `controllerBindings: null` as runtime defaults; migrate only an omitted preference, reject explicit undefined/malformed configurations before writes, and merge a whole configuration by canonical equality. Import/Undo refresh the router, prompts and editor, release held input and preserve the current replay's normalized command meaning. Preserve session-only preferences and truthful save-failure messaging. New profile exports are forward transfers; frozen strict readers cannot import the new field.

For the Round29 working [shared couch menus](../../../docs/couch-controller-navigation.md), retain two stable automatic flight assignments and one separate deliberate menu owner. `game/couch/couch.mjs` captures one bounded hardware frame, polls `attachCouchInput` first to update slots, then samples the shared router only for menus using the same cached frame filtered to those slots. Keep the input adapter’s inactive per-frame clear separate from host transition clears so menu joining remains possible. Suppress menu dispatch on assignment/loss samples. A pause or menu-activation frame must not also step either board or leak held commands. Preserve fixed couch position labels and held Boost; solo bindings, glyph preferences and Boost Toggle do not configure couch players.

Keep `race-menu-status` distinct from the player-assignment note and visible in Focus boards; update polite hints only when text changes. `race-menu-release` clears menu ownership and previews without moving flight slots. South commits an existing enabled control; East/Menu cancels a draft or focuses the primary action without starting/resuming it. Native input relinquishes focus/editing while preserving native shortcuts. Blur/hidden/pagehide and replacement pads release held input, cancel stale previews and require fresh neutral controls; a still-running opponent keeps the match in flight even when the other craft has lost. Preserve ordinary duel/core and protocol identity, first-to-two accounting, no campaign awards and the future network boundary. Use the [four couch workflows](../../prompts/round-29-couch-menus.md) with the actual host/router/navigation/input and public duel in tests; keep browser, viewport and physical-device evidence separate. Keep source-check results distinct from later browser, release and physical-device evidence.

For the separate Relay Rescue host, preserve authored cooperative Support, held Boost, rescue, capture-stop and stable controller seats; solo Arcade equipment policy does not apply. Use the existing Field Kit font/token/component/compiled-style layers rather than another palette or registry. Keep both touch pads as cardinal crosses with the shared 44px minimum and pressed feedback. Auto follows coarse-pointer capability and the existing input adapter's actual per-seat assignments; explicit Show/Hide overrides remain local to that visit. All flight pads hide during menus. In short landscape, reserve side gutters without shrinking targets or covering the full arena. Bound pause/result scrolling to the viewport rather than the arena height. Keep the Help disclosure, Settings action and quick Sound control together in the active lobby or pause/result scope. Settings opens its separately owned native dialog and returns to its actual opener, with keyboard/controller reading and focus intact; Back from paused root focuses Resume rather than implicitly resuming. Test both seat assignments, overrides, actual pack imports, pause clock stability and explicit Resume. Source/model checks are not native viewport, physical-device or paired-play acceptance; see [Relay Rescue's source interface follow-up](../../../docs/relay-rescue-plan.md#shared-field-kit-interface-follow-up).

Controller toggle steering, couch Boost settings, physical-button capture and haptics remain future work. Solo Boost Toggle is implemented in the main game and its practice previews; Couch retains Hold. Do not imply simulated input proves physical compatibility or trusted browser activation. Native pickers, fullscreen, share/download and audio requirements must be tested on their actual host; keep the keyboard/touch route and truthful failure feedback.

For working [Replay Theater navigation](../../../docs/replay-theater.md), keep its standalone default-pad router separate from recorded input and player preferences. Neutral-then-join never starts playback. Back cancels a select draft or pending load before pausing/focusing the existing return link; leaving requires explicit activation. Preserve native text/file controls, canvas Space/Right, repeated-key guards, focus-loss pause, stale-import cancellation and disposal. The page remains silent and storage-free; do not claim these isolated host tests prove parent-page music continuity or physical-controller support. Reusable prompt: “Exercise both turning policies through keyboard transport, controller join/Confirm/Back, speed draft cancel/commit, a deferred import and pagehide. Reach the exact final replay checkpoint without input injection or awards, and distinguish modeled events from actual browser evidence.”

A source-locked earlier-release Copy explicitly replaces the whole collection. If its trusted external snapshot proves the old index is null, prepare backup.v2 with an explicit empty index so an already-indexed destination can accept the reviewed replacement. Keep raw null in the reread/fingerprint and never write source data. Do not change the callback-omitted legacy path or the ordinary backup.v1 indexed-target guard. Test Copy and Undo against both empty and populated indexes, with a real suspended-only source and unchanged original media. See the [transfer correction](../../../docs/verification/cross-mode/p01/legacy-transfer-index.md).

For portable saves, local leaderboards and installed campaigns, read [library-and-packs.md](../../../docs/library-and-packs.md). New core results include roster and class-route identities; retain both through rewards and replays. Expansion packs use explicit runtime v1/v2 contracts for core-v2 or the finite `xonix-pack.v3` encounter contract for core-v3, with complete image decoding before atomic installation.

For bundled chapter download failures, keep `fetchBundledChapter` from `game/chapter-download.mjs` inside `packLaunchGuard.run` and before pack preparation/adoption. Installed packs must return before fetching, including offline; do not use `navigator.onLine` to reject a cached or installed chapter. Show named online-download/retry guidance in the active title or mission surface, while preserving the current run and installed library. Keep AbortError and stale-ticket cancellation distinct from actionable network/HTTP/JSON-read failures, and never reclassify schema, image-decoding, capacity or storage errors as an offline problem. Choosing again is the retry; do not add automatic retries or pack eviction. Preserve the existing entry pause/autosave separately from the no-write download-failure boundary. Test a real live cut, offline failure, explicit retry, installed selection without fetching, title-visible feedback and a late failure after a newer action. Keep core offline readiness distinct from optional First Light installation and from real browser storage retention; see [the chapter workflow](../../../docs/library-and-packs.md#prepare-install-and-switch-content).

For [Text size](../../../docs/text-size.md), preserve the strict `standard`/`large` preference and omitted-only Standard migration in library v2. Apply the actual merged `library.preferences.textSize` after load/save/backup/Undo/profile adoption, synchronizing the Settings control and presentation without changing a live run or saved replay. Invalid candidates reject before storage or image allocation; session-only settings do not bypass the writer. Keep the setting out of campaign, core and session identities, and preserve every old fixture. Test Large text in real compact and landscape layouts separately from persistence tests; an enum and a passing import do not establish readable or unclipped UI.

For current/saved attempt export, read [the finite export plan](../../../docs/round-28-save-export-plan.md) and its [source](../../../docs/verification/round-28/attempt-export.md) / [browser](../../../docs/verification/round-28/attempt-export-browser.md) evidence. `prepareAttemptExport` owns the original envelope and validates its actual unfinished replay; it never adopts the reconstructed run or creates an award. `createAttemptFilePreparer` selects a started unfinished normal flight before a stored checkpoint. Ready/results can export the stored slot; practice/training cannot. A stopped current recorder must not silently substitute an older save. Use the exact campaign key, including map/roster identity; a missing campaign permits an explicitly named replay-only rescue, while a supplied invalid campaign must fail closed.

Keep the stored branch read-only and its local-slot limit distinct from the larger portable-file budget. Snapshot and recheck raw slot bytes plus backup state under the short existing operation lock, release that lock during replay verification, then call the returned `assertCurrent()` immediately before publishing the copy/download request. Current export preserves ordinary pause/autosave behavior and must compare the final authoritative checkpoint and recording, not only object references. Invalidate on transitions, source replacement and lifecycle exits. Cancel/Escape/controller Back stops only this pending export, retains the prior copy and keeps Library open without resuming. A successful adapter request is not proof of a file on disk. Preserve old formats, fixtures and frozen releases; use [the export workflow prompts](../../prompts/round-28-attempt-exports.md) for bounded changes and checks.

For chapter appearances, use [chapter-rewards.md](../../../docs/chapter-rewards.md) and the [Round 20 prompts](../../prompts/round-20-chapter-rewards.md). Call `appearanceMilestones(progressFor(library, campaign), campaign)` and `unlockedBodies(progress, campaign)` with the actual campaign context in every campaign UI and class-appearance fallback. Count distinct declared map clears, ignore foreign IDs and preserve targets of one and `min(4, distinct map count)`. The one-argument unlock API deliberately retains the legacy four-clear final tier. Pathfinder follows the campaign target; Last light still requires all maps. These cosmetic projections must not change equipment, map/roster identities or mastery qualification.

Use `newAppearanceBodies(before, after, campaign)` only around an eligible live ordinary-clear transition. It is a pure difference, not a notification gate: imports, reloads, Undo, gallery visits and already-complete profile adoption must not compare against an empty profile and fabricate a new reward event. Preserve selected appearances, lifetime non-awarding preview access, couch/Theater boundaries and truthful session-only save failures. `canPlay` keeps an own-cleared mission replayable even without its predecessor; it does not fabricate earlier clears or unlock unrelated uncleared missions. Keep progress-v1, library-v2, replay-v3, session-v1 and mastery-record-v1 unchanged. Run `appearance-milestones.test.mjs`, `chapter-reward-integration.test.mjs` and applicable progress/library/backup/compatibility tests, then record real interface evidence separately. New helper tests alone do not establish browser or physical-device verification.

For returning-player navigation or earlier-release copying, read [continuity-transfer.md](../../../docs/continuity-transfer.md). Preserve an explicit replay/restoration target and keep a restored flight paused. Discovery is unverified; prepare source data under its actual writer and backup locks, without recovering or writing the source. At Copy, reprepare and compare exact source ID plus canonical SHA-256 fingerprint with the reviewed snapshot. Changed content gets a fresh review; matching prepared content goes through the destination's existing journal/Undo path. Missing or corrupt source data must not become an empty successful replacement. Test conflicts, cancellation, source-byte preservation and changed-source reviews using injectable stores, rather than editing a real player's storage.

For illustrated chapters, read [authored-art.md](../../../docs/authored-art.md). Keep source PNGs/prompts distinct from generated embedded pack JSON, preserve original bytes and verify browser decoding plus actual reveal/gallery rendering. An art-only change does not authorize new gameplay behavior or silently mutate an archived release.

For optional equipment seals, retain [the released one-seal baseline](../../../docs/round-17-mastery-increment.md) and read [the bounded Supply Line / Safe Return extension](../../../docs/round-18-equipment-mastery-plan.md), [completed verification and award lifetime](../../../docs/mastery-verification-contract.md), [observer semantics](../../../docs/mastery-observer-contract.md), [saved-flight reconstruction](../../../docs/mastery-session-contract.md) and [record/library migration](../../../docs/mastery-records-contract.md). Preserve these boundaries:

- Preserve definition v1 exactly: `clean-win` plus `resistant-cut-cells`, its original preview shape and Steady Signal digest `mastery-v1-2e6aae3f42f3d3c2`. Definition v2 dispatches to the separate `game/mastery-equipment.mjs` observer with exactly two compositions: Supply Line has `supply-pickups` + `suppressed-region-crossings` + `hangar-switch`; Safe Return has `clean-win` + `live-cut-impact`. Every composition requires a final win. V2 does not make arbitrary predicate combinations valid under either version. Pass the resolved `definition` to both public setup and fact capture helpers so they select the correct version.
- Call `masteryFor(actualCampaignKey, levelId, currentCatalog)` with `campaignKey(installedCampaign)` and the cached prepared catalog. Omitting the catalog is only the legacy pinned-fallback API. The three shipped Homeward registrations remain exact; edited same-ID content does not inherit those fallbacks. Authored supported definitions live beside maps in explicit pack-v2 declarations; omitted v2 map declarations mean none. Read [pack mastery](../../../docs/pack-mastery-contract.md), preserve old hashes, and never mix definitions into normalized levels or v1 pack fields.
- Supply Line credits actual refills at both named pads, each named suppressed region's distinct cells within one successfully closed live cut, and an accepted Heavy carrier switch at `south-hangar`. Different regions may use different cuts. Full-pad presses, safe-ground walks and rejected switches do not count; failed/redeployed cuts lose only pending crossings. Supply Line has no clean-life condition. Safe Return binds a new impact pulse to its pre-use live-trail threshold, the named actor's actual stun and that redeployment's later completed return, then requires a clean win. Unrelated fields/respawns and claims that the pulse was necessary to survive do not count.
- Observe public facts once after every fixed step. Pending open-cut cells are separate from the best successful closed cut; repeated cells, safe-ground visits and separate short cuts cannot be combined into the goal. Resume the observer reconstructed from a saved replay prefix, keeping its original run ID and setup. Never serialize or trust partial counters as award proof.
- `verifyMasteryRun` snapshots and reconstructs the complete attempt against the installed campaign and registered definition. Only its original in-memory result can yield a new record through `verifiedMasteryRecord`; a preview, clone or imported metadata is not that capability. Keep core-v2, replay-v3 summaries/checkpoints, existing campaign and board identities exact. Run the pinned v0.6.0 compatibility fixtures without regenerating their expectations.
- `createMasteryAwards` bounds pending checks and guards them with cancellation epoch plus current library generation before synchronous commit. Submit only eligible normal live wins; practice, Controller Lab and Replay Theater never award. Ordinary picture/score/progress persistence happens independently first. A next-mission check can finish into the same current library, but status text must still match the displayed run ID.
- Cancel pending award jobs before a replacement's Undo snapshot, not only before final adoption. Keep page-exit and pack-change cancellation, explicit session-only save results and the existing profile writer/backup lock. Do not return an asynchronous Promise from the queue's synchronous commit callback.
- Library v2 requires strict `masteries`; valid v1 migrates to empty records, while a v1 mastery field rejects. `withMasteryRecords` is bounded portable metadata union, not award authority. Preserve ordinary records, complete histories, earliest-earned tie breaking, archived definition hashes, total byte limits and explicit capacity failures. A current gallery label requires the registered definition ID/revision/hash, level identity/revision, roster and ruleset as well as the picture's campaign/map identity; other imported metadata stays archived. Backups/transfer/Undo reuse this importer; old readers need not accept new exports.

Keep the digest algorithm/prefix `mastery-v1-` for both definition versions: the canonical version participates in the content hash. Records remain `xonix-mastery-record.v1`, the library remains `xonix-library.v2`, and core-v2/replay-v3/board identities do not change. New definition contents create new hashes; they do not reinterpret historical seals. Frozen v0.7 readers can preserve structurally compatible new records as archived metadata while still rejecting v2 definition documents.

Use [the eight Round 18 equipment prompts](../../prompts/round-18-equipment-goals.md) for the current extension; [Round 17 prompts](../../prompts/round-17-optional-mastery.md) preserve the earlier v0.7 scope and historical future-work wording. Compact flight text, named pause rows and Safe Return's not-started/awaiting-return/returned phases must agree with typed preview facts; the ordinary picture remains independent. Additional predicates remain future work; the implemented Sentinel uses its separate core-v3 encounter contract described above, with no new mastery predicate. Explicit pack-v2 and scenario-v2 goals now use the shared [catalog contract](../../../docs/mastery-catalog-contract.md) and [pack contract](../../../docs/pack-mastery-contract.md). Keep v1 shape/bytes and shipped fallbacks, v2 explicit absence, pre-decode local reference validation, current catalog labels and archived old records. Rebuild registrations only when content is adopted. Cancel asynchronous awards before replacement/Undo snapshots and preflight the base campaign plus installed packs before persistent mutation. Test both ordinary and authored-goal restore, goal replacement/removal/reinstallation, and lifetime non-writing practice. Read [Expansion Author](../xonix-expansion-author/SKILL.md) for the editor/export workflow. Run applicable `game/test/mastery*.test.mjs`, `sessions-mastery.test.mjs`, library/backup/transfer and compatibility tests. Record browser journeys separately from deterministic proofs, physical devices and player enjoyment; do not infer readiness from a badge preview.

## Same-map campaign difficulty

For working [Standard/Gentle campaign play](../../../docs/campaign-difficulty.md), use `game/campaign-difficulty.mjs` for the strict enum and finite `gentle.v1` transformation, `campaign-contexts.mjs` for exact execution ownership, and `difficulty-access.mjs` for shared noncompetitive access. Standard retains its original data/identities; Gentle derives new campaign and level revisions from the exact base key and filtered roster. Keep ordinary core/replay pairs, fixed ticks, objectives, art, equipment and presentation clocks unchanged. Never mutate a live run, infer mode from a prefix, weaken a restore validator or retune `gentle.v1` under an existing retained identity. Reject execution-key collisions between different base/mode owners even when their execution data is equal; keep authored Pack/Level lists unduplicated and cache context/access derivation outside frame refresh.

`preferences.campaignDifficulty` migrates only omission to Standard within library v2; explicit invalid values reject and the actual merged preference wins. Current and next-attempt modes are separate: changing the preference alone must not autosave or replace raw suspended-slot bytes; Resume/Load keep the exact current context, while explicit Retry creates a fresh selected-mode attempt. Preserve neutral input/reader boundaries, replay-only missing-pack export, included-pack backup expansion and Undo. Gentle cannot submit Standard seal observers/awards. Union only declared clears for mission/appearance access; never synthesize Standard statistics. Group exact recognized picture variants without rewriting stored rows, label mode-specific results and preserve selected-mode fresh mission replay. The shared 1,000-score cap can evict either mode; after recorder exhaustion at 30 minutes, play may continue, but a completion beyond 7,200 seconds cannot update the collection. Keep truthful warnings and existing budgets rather than clipping time or promising unlimited retention.

Use the [five difficulty workflows](../../prompts/round-30-gentle-difficulty.md) for real legal-input transform, host, migration, collision, gallery and limit checks. Preserve old Standard fixtures; test both steering modes and core/replay pairs, mixed-mode cap/merge behavior, out-of-order access and raw-slot preservation during live-cut/recovery preference changes. Browser mode/reset copy, input, picture decoding and celebrations need their own observations; software/viewport tests do not establish hardware accessibility or human balance. The guide cites Microsoft XAG 108 as rationale, not full conformance. These prose examples add no skills, CLI IDs or release claims.

## Terminal loss and Retry

For the retained v0.14 [retry explanation](../../../docs/retry-feedback.md), use `game/ui/retry-view.mjs` only when the actual run is lost. A retained `failureCause` also exists during recovery and can survive a win; mission timeout can replace it without `player.failed`. Preserve the seven finite causes, own-data projection and unknown fallback. Do not infer an actor or add failure metadata to core, replay, sessions or portable profiles. Explicit Retry retains `prepare()`→`resume()`, starting one fresh attempt with the selected starting equipment and cleared live inputs, without changing preferences or earlier collection records. Loss and reading/Done/Back must not grant rewards or start another attempt.

Keep both advice and the reset consequence in the named overlay reader. `#retry-consequence` is hidden/emptied on every other overlay; the compact-hidden footer must not be its sole carrier. Replace stale nonterminal territory-retention captions on terminal loss, preserving independent save warnings. Reusable QA prompt: “Reach an actual contact loss and a staged lane loss in both steering modes through normal input. At 320×640 and short landscape, read the cause and reset note, exit with remapped Back without retrying, then activate Try again once. Verify fresh initial equipment and neutral input, no loss award, retained earlier pictures, and no lost explanation on a later win. Preserve existing checkpoint oracles and report browser evidence separately from physical-device or human-comprehension claims.”

## Flight target layout and geometry

For the retained v0.15 [flight controls](../../../docs/flight-controls.md), keep the solo `.play-controls` target and gap variables at the shared 44px/4px baseline, sizing tracks and real buttons together. Preserve the 140px direction cross, flexible action labels, stable DOM/input and mobile group reflow. Short landscape uses a left rail and right board in document flow, with utilities beside ability rows; do not restore a fixed stack that clips or covers play. Board caps reserve space for controls and status, accepting reachable vertical scrolling while preventing horizontal overflow. No new preference, command, schema or physics is needed for this layout.

Reusable QA prompt: “Import a validated 80-character unbroken Scout label without changing its ability or map. In Solo Playground, record Control geometry → Capture geometry before and after the layout change at compact, short-landscape and wide sizes, including actual pointer media and breakpoint neighbors. Inspect all eleven targets, four groups, board intersections and label overflow, then use visible direction/Stop controls and the real controller reader/Retry flow in both steering modes. Preserve frozen checkpoints and collection records. Capture is a bounded read-only snapshot, not proof of occlusion, glyph clipping or hardware comfort; parent focus may pause the preview. Recheck long-label and successful-picture states through actual interaction, rather than matching CSS strings.” Keep the latest frozen version distinct from working-source evidence; do not add this prose example to a CLI template catalog.

## Optional First Flight course

For the implemented v0.16.0 [First Flight course] contract(../../../docs/first-flight.md), use its trusted finite registry and observer, the checked entry helper and the existing solo host. The three lessons are **Close a line**, **Find the empty side** and **Bring the picture home**. Resolve a course request before claiming a profile writer. Keep `courseSession` immutable and non-awarding across lesson changes, imports, mission selection and embedded Exit. A lesson is not an installed campaign; course progress and changed settings stay in memory. Preserve the separate Watch first cut demonstration. That checkpoint compared its working-source browser observations with frozen v0.15.0; qualify each later artifact independently.

Capture owned public facts immediately before and after each fixed step with the host's explicit `runId`. Bind observations to the exact lesson, normalized map, Scout recipe/roster, seed and policy. For the empty-side example, require the exact center-divider and later allowed-row line/rectangle ownership changes, actual retained field enemies, objective and win. `cut.closed.cells` is total captured cells, not trail length. Failed cuts retain only previously earned lesson facts; Retry starts a fresh observer. Alternate legal wins may miss the example and must keep truthful partial status, Retry and Skip. Isolate observation failures, including the 30-minute fact budget: unavailable guidance must not stop ordinary play. Markers never modify collision, capture or recorded inputs.

Course-specific entry freezes without an implicit save, preflights existing suspended-slot bytes, verifies the current real session, requires explicit save success and checks readback under the existing ownership/backup guards before fixed-URL navigation. A failed or cancelled handoff leaves the real run paused and suppresses lifecycle autosaves until explicit resume/new attempt. Earlier ordinary Help autosaves are a separate boundary. Return uses a fresh normal game page with explicit Load/Resume; embedded Exit remains practice. Gate switching/leaving/ended phases against all resume paths and held input. Next opens Ready; Retry explicitly starts a fresh attempt; Done reading never starts either. Use the [eight course prompts](../../prompts/round-26-first-flight.md) for focused route, preservation, controller, geometry and frozen-offline checks; these prose examples do not add CLI IDs. Compare public complete backups with the post-entry baseline, and keep browser/software-input evidence separate from physical-device performance or beginner understanding.

## Make the requested change reviewable

Use `node scripts/game-cli.mjs serve --port 8768` for source inspection. `npm run validate` checks campaign, theme and class data through actual validators plus copied JSON and literal browser resources. Navigation links outside the distribution appear separately as warnings. `npm test` runs the core, tooling and existing lab test files. Run the relevant checks after the edit; complete the full required checks for a release. Do not describe tests as physical-device, visual or production-save validation.

For a candidate map, run `node scripts/game-cli.mjs generate --seed sunflower --out generated/sunflower.json`. It delegates to the same browser-safe `game/generator.mjs` as the playground, writes an explicit validated 48 × 36 level and refuses to replace an existing file. Import it through the playground's level import and inspect real play; structural validity is not a solvability or balance proof. Keep reviewed level JSON and revisions when repeatability matters.

Use `npm run build`, then `node scripts/game-cli.mjs serve --root dist --port 8769` to inspect the distribution. Check the resulting capture, input, local assets and UI at the requested sizes; generated screenshots or contact sheets do not prove playable behavior. Literal-reference checks cannot find all dynamic asset paths. Preserve source images and provenance when adjusting presentation.

For controller changes, run the relevant `game/test/controller-*.test.mjs` suites plus `ui-input.test.mjs` and `gallery-focus.test.mjs`; Boost work includes settings, library and real-core recovery suites. Preserve couch, replay, session, writer and backup regressions when touching those paths. Tests cover actual router/normalized-core logic plus injected DOM/device behavior; their count alone is not a complete session test. Exercise practice through join/start, pause/resume, remap cancel/Apply, Hold/Toggle and mixed-source recovery, hangar, full-brief/result reading, finish/picture and return. Reach the last line of the actual long reading choice at compact dimensions, exit with remapped controls and require a separate gesture before flight resumes. Check both axis pairs, hysteresis boundaries, both-context release, imports/Undo and session-only reload/export. Record actual browser/viewport outcomes separately from physical-controller/OS/native evidence. Use [P0 journey prompts](../../prompts/round-15-controller-journeys.md) for original default controls and [P1 layout prompts](../../prompts/round-16-controller-layouts.md) for current remapping. These prose guides add no CLI catalog entries.

Reusable Boost QA prompt: “Using the actual Controller Lab and visible Settings, select Toggle, remap Boost, verify one press/release stays on, then test Stop, reader entry and a legal Impact recovery in both steering modes. Separate physical-release behavior from focus-loss pausing. Preserve practice isolation and report software/browser evidence without claiming physical hardware tests.” For a code review: “Check strict omitted-only Hold migration, merged-mode adoption and full-backup Undo; retain the triggering tick's Boost command and local keyboard/touch contribution after same-frame recovery. Preserve all frozen replay expectations and add no mode or latch field to sessions, gameplay commands or pack data.”

## Save or distribute only the authorized result

Read [versioning.md](../../../docs/versioning.md) for `release-snapshot --ref REF --version LABEL`. The ref must already exist. The command archives the selected trusted revision and runs its frozen CLI, without switching the checkout; saved labels cannot be overwritten. Use separate server ports for independent origin-scoped saves. A generated archive is local output, not a deployment or off-device backup.

Follow the user's existing authorization for edits and local artifacts; do not add redundant approvals. Git commits/tags, external publication, store uploads and destructive replacement still follow the scope actually requested. If publication is not yet authorized, finish and verify the local artifact so the approval concerns a concrete result. Never invent a remote, app ID, credential or supported platform.

Read [deployment.md](../../../docs/deployment.md) when delivery matters. Browser builds are implemented; only claim PWA/offline or native readiness after testing the corresponding actual files and target. Capacitor, Electron and Steam packaging require their own configured projects and validation. Record the exact commands, version/source revision, observed results and remaining limitations in the handoff.

For the implemented Electron/Capacitor wrappers, use [Native Packager](../xonix-native-packager/SKILL.md) and [native distribution](../../../docs/native-distribution.md). Keep host exports/lifecycle in the shared platform adapter and preserve deterministic replay behavior.

## Deliver the completed feature

For an interrupted frozen-original upload, use the [hosted artifact utility](../../../publishing/utility/README.md) from its independently reviewed automation revision; keep the qualified game commit/tree, version, immutable tag and original artifact fixed. Reread and retain the actual draft inventory and failed-attempt receipts before a separately authorized recovery invocation. All seven small assets must match. Completed source/distribution assets may be retained only after valid API ID/URL, uploaded state and exact size/server digest checks, retaining the actual asset ID in its receipt; starters, mismatches, duplicates and unexpected assets refuse before any POST. The utility must never delete or automatically retry them. After complete inventory/failure review, a separately authorized operator cleanup may remove only a confirmed unfinished draft starter; preserve completed assets. Both held originals still receive full preflight, and only absent originals are posted once. Verify the distinct zero-POST `EXISTING_VERIFIED` and one-POST `UPLOADED_VERIFIED` receipts against actual API originals, then independently review all nine assets before publication. An infrastructure correction does not requalify or rebuild the frozen game.

For implemented changes, follow the shared [feature delivery workflow](../../../docs/feature-delivery-workflow.md): related commit, exact-source verification, immutable playable version, reviewed/merged PR, GitHub Release and verified Pages deployment. Follow the active task’s existing scope and authorization throughout that sequence. Update [Delivery priorities](../../../docs/delivery-priorities.md) with actual evidence; keep planned assets, modeled input checks and physical-device qualification distinct. Design-only work remains a reviewable design artifact.

For registered enemy roles, optional travelling line impacts and theme-specific pickup/defeat feedback, read [enemy catalog](../../../docs/enemy-catalog.md) and [the R3 edition](../../library/fpv-arcade-r3/README.md). The [enemy workflow prompts](../../prompts/enemy-workflows.json) use actual interfaces. Preserve old identities, explicit authoring activation, local artwork provenance and measured input/visual evidence.

For working [enemy pressure](../../../docs/enemy-pressure.md), `classic.enemyPressure` is a strict optional `enemy-pressure.v1` descriptor on level-v4/core-v5, carried by pack/scenario-v5 and replay-v6. It names 1–8 unique existing moving bouncers, each with mode, sensing radius, scan/warning/commit/cooldown ticks and lead ticks; use the documented bounds and real validator. Warning snapshots the observed target while ordinary patrol continues; commitment follows a finite FIELD route at authored speed, then cools down. Never silently turn it into continuous omniscient retargeting, increase speed through presentation, bypass wall clearance or add a new JSON-only primitive. This is an original extension, not a measured XPOSED AI reconstruction.

Preserve exact target/route/deadline state through verified replay and suspended continuation. Global enemy-freeze holds the actor clock; individual stun does not extend its deadlines. Closure, recovery and changed topology cancel the lock before further pressure steering. The full Classic projection owns all new state; absent maps keep their old definition/checkpoint shapes. Retain the v0.29.2 deferred penetration-recovery ordering and all historical proof bytes. New direction-only pressure chapters are `fpv-arcade-r5` (`fpv-pressure-lines`) and `fpv-pressure-frontier`; their three-map identities are independent of old R4/R3. Their explicit Arcade policy rejects manual equipment/Boost commands while retaining contact pickups and fresh-direction capture-stop. Art, theme, controller mapping and warning animation never grant different mechanics.

Reusable pressure prompts: “Author a new pressure edition with one visible hunter, then a committed interceptor, using only documented fields. Demonstrate long-cut pressure and two legal escape routes under both policies; verify Standard and Gentle separately without changing old oracles.” “Suspend during warning and commitment, restore the exact campaign and buffered direction, then compare subsequent replay checkpoints through freeze, closure and recovery. Include a simple first cut followed by long idle; do not patch state to avoid collisions or manufacture wins.” “Inspect the new pressure chapter at real play size with reduced effects, bright/dark revealed art and controller focus. Distinguish target warning, committed motion and cooldown by shape as well as color. Report actual browser, audio and human counterplay evidence separately from deterministic proofs.” These are prose maintenance examples, not new registered CLI prompt IDs or completed playtest claims.

## Preserve native launch and input access

For any playable theme, asset, rule, interface or pack change, follow the shared [native launch, entry and device contract](../../../docs/boot-launch.md#authoring-and-device-contract). Preserve dark first paint and safe failure guidance, the native player journey, authored action availability, independent keyboard/touch/controller navigation, historical run identities and truthful device evidence. Do not reintroduce legacy webpage controls or advertise unavailable actions. Source, browser, listening and physical-device checks remain separate. Public entry must use the complete immutable edition graph; follow the [entry and retirement contract](../../../docs/boot-launch.md#immutable-public-entry--p77). Verify fresh and previously cached browsers separately from public-byte hashes. Preserve old caches, profiles and live games during normal worker retirement; never clear site data or force takeover to make an upgrade pass. Keep actual storage limits distinct from planned media budgets. Verify an ordinary first capture and continued flight in the frozen browser online and with its server stopped; clean startup, restored saves and complete file inventories do not prove the gameplay journey. Preserve simulation exceptions as release blockers even when source tests pass.

For [browser download retention](../../../docs/offline-release.md#keep-downloads-on-this-device), keep `persist()` inside the explicit Settings button action and use `persisted()` for status. Never infer a grant from quota/estimates or call the request on startup. Preserve truthful deny/error/unsupported messages, one pending request, stale-result guards and normal Back/focus without touching game storage, budgets, flight state or Prepare offline. Retention is origin-level browser policy, not a backup or proof that files remain present. Read the [shared contract](../../../docs/feature-delivery-workflow.md#explicit-browser-storage-retention); source tests do not certify native browser decisions or resolve the observed missing-pack cause.

## Preserve first-attempt transfers

Earlier-release discovery must include exact suspended-slot namespaces as well as saved player libraries. A never-written profile may become an empty/default library only when the same source has a fully verified non-null flight; undefined, malformed, locked, recovering or wholly absent sources never become an empty replacement. Keep source locks, copy-time revalidation and exact difficulty expansion. Verify the actual old-release → new-release picker with a first unfinished flight, separately from the complete-backup fallback. See [continuity transfer](../../../docs/continuity-transfer.md).

For optional illustrated chapters, use the [shared chapter contract](../../../docs/feature-delivery-workflow.md#optional-chapter-authoring-contract) and [requests](../../prompts/optional-worlds.md). Keep original pictures, reused geometries, installed-pack storage and separately managed media explicit.

For the [More worlds browser](../../../docs/more-worlds-browser.md), separate the 36 trusted external choices from 12 installed packs and preserve all existing byte budgets. Keep native Theme/Mode selects, deterministic four/two-card pages, explicit Back and 44 px targets. Filtering never cancels, installs, evicts or changes a flight; retain the active or failed card visibly and preserve deliberate focus. Remote list failure must leave loaded/source-owned and current installed choices reachable, with exact owner/readiness checks. Test every page and native-select Confirm/Back, not just hidden DOM nodes or a permissive click stub. No new catalog entries, producers or device qualification follow from increasing a count.

## Keep live pictures and earned originals stable

When a task touches reveal artwork, a saved flight, Collection or media export, follow the shared [live-picture and paired-recovery contract](../../../docs/feature-delivery-workflow.md#live-pictures-earned-originals-and-paired-recovery) and [concrete prompts](../../prompts/media-presentation.md). Preserve saved A after assignment B, first-earned A and exact owner identity. Keep JSON game data, `.rlmedia` originals and `.rlsound` audio distinct; missing saved originals remain paused without a replacement. Shared-v3 source adoption does not certify old readers, browser recovery or a public release. Unrelated art/behavior work need not open or migrate media storage.

For the bounded [P08-A static Versus parity candidate](../../../docs/shared-static-pictures.md), reuse exact fresh Solo assignment/baseline/release resolution through a read-only picture lease; never call the durable default-materialization path from Versus. Keep one accepted decoded original and fit for both boards. Retry retains the attempt and captured choice; setup changes alone establish a new choice. Confirm current metadata before Start, fence stale/cancelled completion, and release only owned resources. During a focused Retry or asynchronous Start confirmation, transfer only that action’s owned focus to visible Cancel; later navigation, foreground loss and stale completion must prevent focus restoration. Expire Start/Next intent on suspend or controller loss even if the page returns before preparation resolves; keep completed picture readiness and require a fresh Start gesture. Keep the [scoped evidence](../../../docs/verification/cross-mode/p08-a/README.md) distinct from full phase or release acceptance. A failed selected image must refuse Start visibly. Preserve the separate optional whole-release snapshot fallback and historical procedural/training behavior, with an explicit notice; do not infer a stricter global release policy. The shared store may perform its established schema initialization, but this lease must not commit media, assignments or Solo progress. Check user-original priority, saved/earned pin preservation, both aspect ratios and actual native fit independently of modeled source tests. Team adoption and new asset production remain separate.

For [Versus board fitting](../../../docs/field-kit-surfaces.md), measure each arena's content box and pass its fitted bitmap width to the shared painter; `object-fit: contain` alone does not make element width authoritative. Keep the canvas out of intrinsic grid sizing, suppress duplicate resize writes, fence stale observer callbacks and refresh cached-page restoration without resuming or replacing the match. Without ResizeObserver, key meaningful wrapping content: both seats' statistics, state and input labels, encounter visibility/copy, and visible equipment labels. Settle both HUDs and the shell-owned labels before one signature-gated refresh and either painter draws. Keep the existing menu update once per frame; do not add focus, Resume or simulation work. Unchanged frames must not read geometry. Use the guide's two-seat touch/text/rotation prompt and retain the explicit narrow-board 64-logical-pixel cap limitation; modeled geometry is not native readability qualification.

When composing that adapter onto current P03/P05 hosts, preserve their fixed routes, display lifetime and ready-focus ownership; finish lobby boot cleanup before awaiting a required picture so Back and Cancel remain reachable. The [P05-A composition evidence](../../../docs/verification/cross-mode/p08-p05-integration/README.md) separates 210-case runtime coverage from the normal and exact-response-delay native journeys. It does not cover Team pictures, all assignments/devices or P05-B. The existing finished-to-Next path can replace prior results before picture readiness; P07 owns that remaining preservation requirement.

## Retain completed Results during Next

For the bounded [P07 retained-Results correction](../../../docs/retained-next-results.md), stage the next duel and read-only original without replacing the accepted completed round. Preserve both checkpoint identities, result text, series wins and keyboard View through read/decode/authority failure and Cancel. Recheck the captured authored setup and selected original before commit. Publish the new duel, image, generation and score reset before retiring the prior lease; cancellation, disposal and reentrant cleanup must release only resources they own. Preserve ordinary Ready selection and synchronous operation-status contracts. Automatic shipped Next requires the original live action through cleanup, menu updates and queued callbacks: later Help/focus, blur, controller loss or another screen permanently veto that Start, even if focus later becomes BODY. Installed Next and background-completed preparation remain Ready for a fresh Start. Keep modeled release/no-write assertions, exact-response fault/delay native journeys and full progression/public/device acceptance separate. Keep player failure copy concise and actionable; technical paths and the original Error belong in a tagged diagnostic. Treat native event labels as actions, and derive verdicts from captured DOM, inspected images and transport evidence; retain provisional labels with their corrections.

## Qualify the current preview without rewriting a frozen edition

Use the [historical Round 43 milestone review](../../../docs/research/round-43-next-milestone-review.md) for exact source/public boundaries and [focused briefing guidance](../../../docs/controller-navigation.md#read-details-without-starting-a-flight). A directly opened brief hides unrelated chapter/setup controls, preserves its original DOM position for normal navigation, and exposes a bounded readable region plus Reader/Back. Verify actual long text and visible controls, not only successful activation. Ready stays Ready; paused direction/time survives reading and Back; only explicit Resume continues. Practice/course must hide Creator links their existing navigation guard refuses. Preserve the real course/practice/policy label and neutral input gate.

For asynchronous media host tests, wait on the real completion predicate with a finite elapsed deadline and a timer yield. Do not infer storage/WebCrypto completion from a fixed number of immediate turns. Keep delayed-real-digest negative controls and preserve failed CI evidence. These test repairs do not change media identity, await barriers or runtime timeouts. Report focused tests, combined exact-source gates, frozen browser, network inventory and actual public play separately; prior source success does not certify the next merged revision.

For the isolated [authored story binding model](../../../docs/authored-story-bindings.md), follow the [exact API examples](../../prompts/authored-story-bindings.md). Keep logical-v1 history and old still/session/earned schemas strict. Only explicit logical-v2 full-picture bindings select a story; absent/v1/null must never search history. Freeze the complete canonical descriptor hash and original hash for a new authored choice, resolve saved pins independently of current bindings, and leave execution/first-earned authority to the later coordinated host version. Preserve one-CAS generation checks, null-only shared metadata accounting, exact v1 binary export, matched v2 transfer dispatch and default keep-current binding restore. This source model does not authorize v4 host adoption, new awards, public delivery or another preview.

## Training shell availability

Apply the [training navigation contract](../../../docs/device-controls.md#training-navigation) when changing course/player navigation. Active First Flight includes its visible Game menu and Settings in keyboard/controller traversal; campaign-only destinations stay hidden. Back to lesson restores focus without resuming, and embedded End course keeps only the terminal reader. Test native Tab/Enter separately from modeled pad navigation and include a released-input frame when changing devices. Do not weaken neutral gates to make an old held input activate a menu.

For the explicitly authorized [story runtime integration](../../../docs/story-runtime-adoption.md), use one shared v4 manager across current game/practice/course audio and Picture Workshop; keep training picture/story stores absent. A fresh attempt owns one atomic still/story metadata snapshot and bounded flight-pictures.v2 pins, session.v4 saves them, and library.v4 records the first-earned story alongside its exact picture in the existing guarded profile write. Old/null receipts never acquire later stories. Record the win before offering Play; Collection validates retained execution ownership, missing movie keeps the poster, and Play/Skip/Replay never grant awards. Cinematic volume must also obey master mute/volume; use only the temporary soundtrack gain lease. Keep JSON/.rlmedia/.rlstory/.rlsound scopes explicit, add an obtainable owned example before claiming delivered story content, and qualify native/packaged/public journeys separately.

When story actions change availability, capture active focus **before** setting `hidden` or `disabled`: native browsers may immediately move focus to the document body. Move an unavailable story action to the next visible enabled action only if focus remains owned or fell to the body. Preserve a player's focus on volume, the enclosing Close action, or another deliberate blur-handler destination. Verify keyboard Play → Pause → Resume → natural end → Replay in an actual earned story, separately from modeled tests that explicitly implement blur-on-hide/disable. Keep poster identity, soundtrack intent and awards unchanged; a focus fix is not full story or controller qualification.

For chapter selection and reopening, follow [selected-chapter continuity](../../../docs/selected-chapter.md). Keep the menu-only bookmark separate from explicit saved-flight restoration, and preserve exact installed campaign identity, accessible-map checks, writer/training/recovery guards and portable schema boundaries.

For live HUD and warning changes, preserve the [stable flight feedback contract](../../../docs/device-controls.md#stable-flight-feedback--source-candidate): warning phases cannot change arena scale at fixed viewport/input settings. Show stationary exposed-line guidance without changing movement or discarding higher-priority loss/pause states. Verify a real warning transition and wall escape at ordinary and compact sizes; a CSS selector check alone is not browser evidence.

For short landscape, derive compact eligibility once from the original authored map, never current phase or remaining enemies. Full/non-training maps keep the HUD and complete warning panel independent of the inner arena width, using the same stable cap through hidden/open/active warnings. With on-screen controls shown, preserve the explicit two-sided panel clearance; Off controls retain their separate width rule. Do not shrink fonts, move advice below the viewport, change collision geometry or make a warning resize the live arena. Verify both hands, Standard/Large, Auto/Always/Off, compact Arcade and a full Tactical warning transition through the native browser; source classification tests alone cannot prove containment.

For the [shown D-pad clearance](../../../docs/controls.md#d-pad-clearance-in-short-landscape), short landscape at ≤600 CSS px reserves the full current 156/192px touch surface, safe side inset and 12px gap, plus the existing 160px opposite action column only when real equipment buttons are visible. Keep the authored device-controls suffix authoritative for arena center/max-width alongside the later compiled vertical/header cap; do not replace that cap, top, aspect ratio or warning behavior. Off/hidden controls, Stick/Swipe, portrait and pause do not match. A hidden classic/open status card still matches the legacy :has selector: include that exact state in the running parent-panel reset so percentage-based inner clearance resolves against the full viewport, not the old narrow panel. Preserve paused layout and the compiled inner header cap. Verify both hidden and visible warning states; an Arcade-only pass does not cover legacy Tactical. If the touch size or action-column width changes, review the reserve contract together. Native QA must record the actual CSS content viewport and check both hands, touch/text sizes, all four button centers and hit ownership, board containment, safe insets and warning transitions; CSS-source arithmetic, modeled input tests and device preset names cannot establish physical-device comfort.

For inline preparation recovery, review the complete reason together with all recovery actions at Standard/Large text in portrait and short landscape. Preserve shared text tokens and 44px controls; recover excess spacing/width before allowing deliberate panel scrolling. A clipped button or obscured hit target fails qualification even when its DOM is visible. Distinguish initial visibility, scroll reachability, Export return focus and direct Reload.

Keep every pause action inside the visible viewport at Large text. Narrow portrait wraps Resume above the two secondary actions; short landscape anchors the pause dock to the viewport safe area, independently of arena width. Recheck both layouts through the actual Mission brief, Back and Main menu journey. Use Always only to inspect on-screen controls; it does not establish physical touch support or controller certification.

Short confirmation dialogs use the opt-in `restart-dialog` class, not a particular element ID, for content height and bounded safe-area scrolling. Preserve full-screen Title, Missions and Collection layouts. Keep confirmation actions stacked with 50px targets and existing safe-action focus, Escape/Back and opener return. Scope the action selector through `.overlay-actions` so the later Field Kit logical `min-block-size` cannot override that minimum; check actual computed size as well as source specificity. The [scoped desktop confirmation check](../../../docs/verification/cross-mode/p05-a/confirmations/README.md) measured Standard/Large replacement and Restart actions at 50px after preserving the original 44px failure. Inspect narrow portrait, short landscape and zoom separately, including scrolling to the final action; desktop source-browser evidence does not certify physical touch/controller comfort or complete P05.

## Diagnose missing retained data and qualify native evidence

Use the [retained-data incident contract](../../../docs/feature-delivery-workflow.md#retained-data-incident-and-native-evidence) and [v0.39 example](../../../docs/verification/round-47/v039-native.md). Preserve failed UI exports, exact earned pins and actual installed/media inventory before proposing an owner change. Compare a successful exact re-download/restore with the prior player library; recovery does not prove a cause or prevent recurrence. Keep browser retention explicitly requested and report the actual granted/denied/unsupported result. Never infer durable storage from a loaded runtime or successful quota estimate.

Record native selections as observed selected options, not requested key names. With Match off, a valid available manual appearance must survive class setup and reload; Match on uses the authored class recommendation without changing gameplay or hitboxes. Preserve locked/unknown fallback and old saved identity. A failed native selection attempt is evidence to keep, not a reason to weaken source expectations.

For large host fixtures, scope any longer finite bootstrap allowance to the affected fixture and keep ordinary defaults, real completion predicates and exact checkpoint/owner assertions. Improve timeout diagnostics without making readiness pass. Preserve full failed CI output and qualify the complete affected file on supported runtimes; a test-only correction can coexist with an unchanged frozen runtime, but its replacement CI/public gates remain separate. A paused snapshot cannot prove saved direction or an unfinished cut, and a stopped-server note should be accompanied by its actual refusal result.

For the implemented Field Kit navigation, retain the five-action title, exact saved destination and `mission-gallery.mjs` pagination/selection around the existing mission buttons. Decoration and earned-picture thumbnails are presentation metadata only. Preserve optional preparation, locked/unavailable reasons, neutral/held input gates and return focus. Scene originals stay in the authoring library; player assets are separately pinned960×540/540×960 derivatives. Test actual chapter selection→preparation→Deploy→pause→Continue on narrow/short displays whenever changing this flow.

For the current Field Kit presentation, read [screen/navigation contract](../../../docs/field-kit-surfaces.md) and [production adoption](../../../docs/field-kit-production.md). Preserve original dialog/input/save handlers when styling screens, use the English copy keys for new labels, keep Ukrainian-capable fonts, and update the compiled collection through its immutable ledger. Local Studio imports reuse identical revisions and never alter player storage.

When changing a render recipe referenced by an immutable production register, preserve the exact previous module bytes in the separate hash-addressed `authoring/production/history/source-index.json` contract. Its code-owned path allowlist covers the character and actor renderers; snapshots are inert `.source` files and are never imported. Keep the original JSON metadata history and historical registers unchanged. Verify both old and new source pins and malformed/missing snapshot rejection before release. When both history indexes declare the same complete path/size/hash identity, verify every declared snapshot; neither .mjs nor .source may hide a missing, changed or symlinked copy.

For the [standalone fallback derivatives](../../../docs/fallback-body-derivatives.md), preserve the appearance/role and original-source identity separately from the runtime output path, bytes, hash and dimensions. An optional derivation records the registered full-frame recipe and both identities; records without it retain their original contract. Keep the thirty compiled defaults in the existing presentation registry, without a second slot registry or duplicate original fetches. Maintain strict source/role/output checks and the unchanged 64 MiB packaged core cap; smaller source files do not prove the final precache budget or actual-scale readability.

For file import/selection tests, capture and join the Promise returned by the actual click handler before checking successful UI readiness. A fulfilled handler can still report refusal, so require the relevant enabled controls and existing ownership assertions. Keep the subsequent picture-decoding wait separate. A bounded bulk-operation allowance belongs to the affected fixture; cancellation should request the actual panel Cancel and report whether the pending action settled. Preserve setup failures and distinguish their dependent symptoms from independently observed restore regressions. The complete CLI suite caps process-isolated file workers at the smaller of four and Node's CPU-derived default, without filtering discovered files or changing runtime deadlines. Never infer a timeout's cause or a speedup from that scheduling policy alone.

## Return from title tools

Craft switching must follow the active run’s roster and authored hangars, with one availability predicate for the visible action, G and controller entry. Keep fresh Starting class setup and Save & pause separate. Never infer hangar availability from Arcade labels or its ability/pickup/boost policy; preserve Tactical switching and core safe-ground/proximity checks.

Route Missions Back, native cancel and controller cancel through the same origin-aware shell handler. Prevent the native default after handling cancel, but retain cancellation notices for transactions that remain open. Do not reopen Home from a generic close listener: Deploy and Back to flight deliberately leave Missions. Test title, flight and brief origins with the saved cut and explicit Resume unchanged. Capture the explicit title Missions, field Missions or Mission brief opener before closing its parent; a repeated open of the same Missions visit from Collection appearance setup must retain that original parent and opener. On Back, validate the complete return target and its parent, use the available parent Missions entry or primary only as fallback, and preserve newer dialogs, deliberate focus, background/disposal and close/pause callback ownership before reopening Home. Distinguish expected native close/showModal autofocus from a newer usable focus choice. Preserve the existing no-argument programmatic field handoff and its flight focus; this correction does not extend campaign-complete entry behavior. Test the actual shell/host plus queued native-close modeling, held controller Back, unchanged paused-cut/checkpoint/storage and intentional Deploy/Back to flight. Keep native and physical-device acceptance separate.

About’s Ways to play must expose Solo campaign, Couch Versus and Couch Team in document order, with ordinary same-build links that remain usable before optional catalogues finish. Keep current, frozen and archive URL shapes covered; catalogue readiness must not steal the selected mode’s focus. Back selects Return without automatic navigation, and online remains explicitly deferred. Use durable summary wording instead of manually maintained mission, class or pack totals. Follow [About mode guidance](../../../docs/controller-navigation.md#about-choose-a-local-mode); verify native 44px actions and responsive text separately from modeled controller routing.

Keep software release history in About’s Build information disclosure, separate from Home’s game destinations and its unchanged version label. Derive the About page’s sibling game URL before applying the shared catalog resolver; frozen About routes include `site/site/about.html`. Keep the history link hidden under the actual button CSS until its correct URL is initialized, before awaiting the catalog. Preserve the current-build fallback and explicit Open build action. Rebase only already-validated relative archived-play results against the fetched/requested catalog URL; retain the strict validator, exact canonical HTTPS URLs and invalid-canonical-to-valid-local fallback. Do not add automatic navigation, save writes or a new modal-return owner. Regression prompt: “Check first/continued Home, source/frozen About, held/failed catalog loads, relative/canonical/malformed records, and module failure with the history link truly unrendered; verify explicit navigation and original game state.” Record modeled URL/host tests, native focus/44-pixel layouts and actual public catalog qualification separately. See [Software build information](../../../docs/field-kit-surfaces.md#software-build-information). Initialize About’s shared controller owner before catalogue awaits with a stable page scope and an explicit same-edition Return link. On a successful controller join, engage navigation to establish visible focus without activation, preserving any meaningful prior focus; verify BODY arrival, held input, release and a fresh Confirm through the actual host. Preserve native keyboard/touch defaults and SUMMARY behavior; use the existing SELECT draft/commit/cancel semantics. Controller Back cancels an edit first, otherwise focuses Return without navigating. Native-editor Escape must retain browser dismissal after any controller edit is cancelled; only non-editor Escape focuses Return. Loading completion must not steal focus, confirm a stale edit or restart disposed input. Keep hints separate from catalogue status and avoid repeating the adjacent live select preview in the page status. Style that preview locally as a readable full row in the header, build picker and pack launcher; About does not load the game stylesheet. Suspend the single frame loop on blur/hidden/cached departure, require neutral input on restoration, and destroy all owners/listeners on terminal departure while blocking late catalogue mutations. For exact HTTPS numbered Reveal Line archive immutable About routes, resolve the JSON catalogue to the canonical main-site releases path; keep this project-specific rule in the site helper, preserve generic/custom-host routing and same-edition Return, and do not weaken the archived-play validator.

Keep standalone Enemy workshop departure a static same-edition `../../game/` link beside the page actions, available before module startup and during failure. Do not route it through catalog Apply, implicit saving, history/referrer inference, or a new return-context owner. Modal Back still closes only the panel; the practice iframe retains its scoped return bridge and input ownership. The startup auto-open lease begins before the first await and belongs only to untouched foreground body/root focus. Existing meaningful focus, later focus/input, blur, hidden or pagehide retires it permanently; remove intent listeners on retirement and all startup listeners on settlement or terminal departure. Prevent late terminal responses from initializing owners; defer cached-page readiness/failure until pageshow without reviving auto-open. Ready status must not open a modal over a newer Return choice or engage navigation to force a default. Preserve ordinary untouched auto-open and explicit Open workshop. The existing controller owner starts only after themes load, so distinguish ready-state modeled controller routing from pre-ready/failure native-link availability. Use the [Enemy workshop return prompt](../../../docs/enemy-catalog.md#returning-to-the-game); native navigation and physical-device checks remain separate.

For untouched Enemy startup auto-open, pass the enabled Open workshop button as the dialog's explicit return target; BODY is not a useful invoker. Keep ordinary callers bound to their actual invoker. Restore only on an actual foreground close that still owns focus, and recheck after native close callbacks: repeated Back, reopened dialogs, newer focus and child practice must not inherit a retired close's authority. Test Escape and modal Back from Presentation, the returned Open action, then forward page traversal to Level playground and Return to game without storage writes. Keep the loading-intent lease and practice bridge unchanged; finite DOM/default-action tests do not establish painted focus, native modal Tab or physical-controller acceptance.

Preserve the actual dialog hierarchy: Main menu stays beneath Settings or Collection; Workshop stays beneath Field Guide, with its menu parent retained. Let the existing modal stack restore that exact opener on Close, Escape or controller Back. An already-open Main menu remains the return screen even while its underlying flight is paused; a direct flight opener returns to that paused flight. Nested practice returns to the guide first. Preserve Settings → Music Studio → Close studio → Settings, transaction cancellation and neutral input gates. On a first Studio read, disabling the initially focused Close button can move browser focus to the document; restore it after the read only if that disabling displaced focus and no later focus choice or dialog exit superseded it. Remember the initial target before the read rather than requiring immediate BODY focus: native blur may arrive later. Exercise both immediate and deferred disabled blur during read success/failure separately from cached reopening; never steal focus after a user choice, close or disposal. Explicit Replay or mission/appearance selection closes the appropriate parent dialogs; simply inspecting Collection or its picture retains its entry screen. Read [Collection return behavior](../../../docs/collection-title-return.md). Regression prompt: “Open title Settings and Music Studio, return twice; then open Field Guide, enter and leave practice, and close the guide. Repeat from a real paused cut. Verify the original screen/focus, exact saved state and explicit Resume.” Inspect both keyboard and modeled controller paths, then native browser focus and compact illustration clipping separately. The browser milestone still needs complete source gates and packaged/native-browser/public qualification; source or preview success grants no gate waiver. Full physical-device comfort and supported-device qualification remain P7, with no certification inferred here.

The Field Guide keeps the canonical theme catalog captured before the active pack narrows the flight theme list. Restore an FPV-only pack before bootstrap, then exercise all four Guide names, palettes and practice handoffs. Do not repair the Guide by changing the live run theme, removing available themes or inventing fallback palettes.

For external chapter tests, compile each transport fixture only from the owners it exercises. Preserve one complete, unfiltered production distribution audit for every owner and all loose/ZIP/manifest bodies, aggregate totals, equipment bytes and the installed-pack capacity refusal. Its nested capacity case requires the parent audit to run; an old standalone name filter is not equivalent. Fixture deduplication changes no production builders or expected bytes. Report measured duration only after the complete suite finishes, rather than treating fewer producer calls as a proven speedup.

For the ordinary **Settings → Stored profile recovery** entry, keep the existing native Settings dialog and title/flight opener underneath recovery. Lazy-load the reader/view/catalog only on explicit entry; do not import the standalone page bootstrap, navigate, add a second input poller or make saving a prerequisite. Preserve an actual unfinished session even when best-effort saving returns false or storage throws. Keep training, pending content/restore/backup work and other tools unavailable to this entry. All close callers share one asynchronous reader/URL cleanup before the native dialog closes; blur cancels pending work and pagehide prevents late publication. Preserve the writer lease and show the current-channel busy refusal truthfully. The existing game export owns current-flight files. Append historical catalogs only from a freshly verified exact annotated tag and pinned source inputs; preserve prior catalog objects, exact alias separation and version filtering. Dev hosts get raw diagnostics only. Source/model checks do not qualify packaged CSP/offline dependencies, native decode/download, responsive layout or public delivery.

For optional Plain text, read [text presentation](../../../docs/text-presentation.md). Stored `pixel` remains the compatible Theme font choice using the current Field Kit display/UI/numeric faces. Plain overrides those semantic roles in the solo shell and renderer; it does not claim coverage of every external authoring or native page.

The artwork-led result canvas uses only the completed attempt's already loaded picture and fit. Preserve explicit legacy originals; a missing managed still pin must not fall back to today's default. Clear the canvas outside victory, retain the existing award/save order, and return focus from View picture to its opener. Check score, medals and every action on desktop, portrait and short landscape with Large text; very short zoomed layouts may scroll their reading/actions independently while retaining 44 px controls. Keep failed and final measurements distinct.

When reviewing the whole production collection, bind every decision to the selected revision and file or recipe-source hash, then append immutable reviewed successors. Reproduce the ledger and verify a further whole-collection replacement/import/export with all older records retained. Current Journey P02 metadata is explicitly bounded to 5 MiB after the measured fpv34 full replacement exceeded 4 MiB by 9,346 bytes; see docs/verification/journey-p02-music-recipe.md. The 2,048-record, 32 MiB transfer, 4 MiB asset, player-storage and Pages limits are unchanged. Older editions keep their original limits. Never infer authority to raise other budgets. Asset readiness, exact-source gates, public bytes/play/offline and physical devices are separate acceptance scopes.

Before freezing a new production release, run `node scripts/check-field-kit-readiness.mjs` after committing and reproducing the collection. It requires reviewed declarations with nonblank evidence for all selected required slots and binds the result to the committed ledger/source identities. Optional historical sources may remain. An older reviewed revision cannot qualify a newly selected unreviewed successor. This mechanical check does not replace native visual review.

Optional indexed chapters must not gate startup of unrelated screens. Arcade R5 is optional in the v0.51 offline core: keep its shipped original unchanged, show an honest unavailable notice when its request fails, and let Couch use base or checked installed maps through the existing reader. Never write solo state from Couch. Asset Studio player/owner previews must not require downloading unrelated optional fixture packs. Validate the generated 64 MiB core cache and installed-chapter offline play; source-file subtraction alone is not a build receipt.

Keep keyboard focus paint inside a fitted landscape viewport. Field Kit adds up to 7px beyond the canvas box; the authored device layout reserves 8px below it via `--fk-flight-bottom`. Verify both Arcade and Tactical after keyboard Resume, since pointer activation can hide the outline. Keep this layout rule out of generated presentation records.

Title operation feedback belongs to the current menu operation. Hide stale non-error success text on fresh Home entry, preserve actionable errors, and keep the actual Deploy destination explicit. In regression fixtures, use the current touch-side control and distinguish safe Resume from an exposed saved cut. Explicit uploaded actor art has `geometry: null`: compiled crop/pivot geometry belongs only to its matching compiled body.

Use [delivery-priorities.md](../../../docs/delivery-priorities.md) for the concise current release/status register. Before freezing, integrate the chosen main cutoff without dropping audio, entry links or existing recovery/focus behavior; preserve the new release metadata explicitly. Re-run gates for the final combined source, rather than relabeling an earlier passing result. [Snapshot staging](../../../docs/snapshot-staging.md) describes the rename-first TAR transfer, the EXDEV fallback and separate same-filesystem disk admission. Keep the source TAR, extracted source, loose site, ZIP, intermediates and reserve in the estimate; only retire proven owned generated copies with retained receipts.

### Source identity at release gates

Run the workflow-pinned `scripts/check-source-identity.mjs` before and after source gates. Retain its exact HEAD/tree, raw byte/mode verification and aggregate digest with the gate logs. A successful checkout alone is not a post-test file-preservation record. Keep the verified source commits when another task rebases a shared PR, and compare merged side-branch features explicitly before adopting the rewritten candidate. Never overwrite another task’s branch or delete its active build to reclaim release space.

For the [guided game/originals backup set](../../../docs/full-backup.md#prepare-game-data-and-originals-together), reuse the four existing serializers and the current shared manager; never acquire or release another current-channel writer. Keep sequential, bounded read-only preparation, same-manager generation fences, full game-snapshot revalidation and current identity checks before native link activation. Cancel must join pending work before allowing replacement; Close/hidden/pagehide revoke only owned prepared URLs. Preserve distinct Prepared and Download requested states, explicit per-file native links, missing-original refusal and detached-story incompleteness. The coverage JSON is a report, not a new archive, import authority or cross-file transaction. Individual session-only exports and current playback/flight intent remain unchanged. Native download/readback, fresh-origin restore and offline playback remain separate evidence.

For an actual downloaded guided set, run `node scripts/check-backup-set.mjs --report /absolute/path/your-downloaded-coverage.json` (optional `--directory` for its four components). Preserve the existing files: this command only checks safe filenames, stable regular-file bytes/SHA-256, bounded component inventories and paired story/poster context. A matching unsigned report is not earned ownership, replay verification, native decoding, restore compatibility or offline proof. Keep partial/detached coverage visible. Positive backup fixtures use the existing valid-CRC owned PNG; the older embedded PNG is a corrupt-input negative, and injected decoders never qualify a native browser.

For guided backup names, generate one UTC-time/random prefix per preparation and reuse it across all five explicit native links and coverage references. Keep names bounded ASCII and leave standalone export names and component schemas unchanged. Retry keeps the same prepared names; a new preparation gets a new prefix. Check the actual chosen report with the strict CLI, never guess browser suffixes or the newest file. The recorded a858 readback uses earlier fixed names; the separate 050a02a readback verifies prefixed downloads for empty-player/default metadata. Later label and responsive-layout changes still need native review.

Keep guided backup commands short and purpose-based; expose full exact filenames in a native **File names** disclosure outside the primary download list. Preserve each link’s `download` attribute, coverage reference, byte size and Prepared/Download requested state. Keep component layout in `game/ui/backup-set.css`, linked from the game entry, without changing unrelated compiled UI recipe inputs. Use existing Field Kit typography/focus tokens and at least 44px targets; allow filenames to wrap on narrow screens. Reset filename detail with the same owned-link cleanup. Route both native dialog cancel and controller Back through the existing Library cancellation seam: pending preparation cancels first; otherwise open File names consumes one Back, collapses and refocuses its summary without releasing URLs. The next Back follows normal dialog closure. Test the actual Library listener’s default prevention and resulting focus/storage/URL state; standalone key handlers do not prove this propagation contract. Native filename disclosure, phone layout and the new purpose labels need current browser evidence; older downloaded-byte receipts do not certify later UI.

For P02-A composition with frozen P01, preserve the [combined source and evidence boundary](../../../docs/verification/cross-mode/p02-a/p01-composition/README.md). Invoke native audition Play in the activation turn after the explicit local master binding reset, expose status/Finish before awaiting it, and keep shared-master intent independent of asynchronous success or failure. Retain file-input cancel target filtering, picture-writer recovery, speculative-work retirement, replay focus cleanup and shared-preference precedence over restored profiles. Exercise master mute/zero/13% with an independent local fader, then Finish/Close/hidden/dispose before delayed resolve or reject. Preserve original evidence bytes, including historical whitespace in raw logs/diffs; scope any exception to those pinned originals, never runtime source. Keep the P02 version/dependency quartet, accepted-main integration and exact final hosted qualification separate from preparatory test passes.

### Accepted source and incoming P03 guidance

The current [execution register](../../../docs/cross-mode-execution.md) accepts P01 in v0.57.4 and the scoped P02-A public sound-control gate in v0.58.1; P03 is integrating and unqualified. Preserve the c930 Team permanent sound explanation independently of transient storage warnings, shared master intent and all accepted recovery behavior while composing incoming navigation. Keep 4,333/4,333 hosted results per family and the v0.58.1 public audit bound to that exact accepted source, not the new integration. Incoming P03/P05/P08/P07 candidate guidance and historical receipts below remain useful contracts, not additional public phase certificates. Apply the canonical story-focus change already present in the held candidate only once; review the separately retained disposal extension against the final audio/focus composition. Derive Team artwork bindings from actual composed registries and qualify the combined source and affected native journeys before a new release.

### P03-A actual-opener candidate

Keep Workshop and its menu beneath Scores & saves and How to play, as for Field Guide. Use the shared modal close/cancel focus restoration for button, Escape and controller Back; returning must not resume a paused flight or alter its checkpoint. Successful explicit Library content selection and saved-flight loading use the existing field handoff to close both retained parents before focusing the chosen or restored flight; never apply that closure to passive Back. Existing Team entry links may declare only code-owned Solo/Versus return contexts. Its visible lobby return link and Back share that destination, with Versus fallback for direct, unknown or ambiguous contexts. Never use an arbitrary URL, referrer or history as return authority. Cross-document return does not promise the old page's menu/focus restoration. Preserve Team's paused Resume focus and existing departure behavior. Treat this as a source candidate until its native/public gates close; the complete P03 entry/lobby redesign remains separate.

For initial Solo title focus, respect the boot visibility guard: defer the shell's first focus until the ready handshake, then select a connected, visible, enabled control. Do not choose a hidden lesson/Deploy placeholder merely because it is first in DOM order. Preserve a deliberate visible focus choice and do not claim focus in a background document. Test actual app/markup with native-like focus refusal before ready, saved Continue, disabled-primary fallback and exact saved bytes after the departing pagehide boundary. Treat the P03-B source fix as native-pending until the fresh returned page is checked.

For the P03-C Solo Missions → Team candidate, keep menu return separate from flight-save authority. Use a bounded one-use same-tab record with exact installed selection and source/version/channel identity, fixed view/focus enums and code-owned destinations. Validate before applying it after ready; never load a flight, install content or autoplay on return. Unknown/expired/duplicate/incompatible contexts fall back to Title. Source-preview null revisions provide no exact frozen-source claim. Unavailable tab storage must still offer an explicit fixed-title fallback.

Before leaving an unfinished Solo flight, hold autosave and reuse the checked flight-retention helper's writer, recovery/journal, replay/checkpoint, backup-lock and actual readback checks. A truthy `persistAttempt()` return is not successful storage. Preserve any saved slot that differs from this tab's last actual readback and recheck after the helper and before Leave. Stay/Escape/controller cancellation keeps the flight paused and restores the Team opener; late work cannot navigate. Save failure requires truthful session-only loss wording and another deliberate Leave, never silent departure or an unsupported older-slot-preserved claim. Keep the hold through pagehide until explicit Resume/new attempt. Test quota, damaged readback, newer-slot interleavings, pending cancellation, unavailable return storage and real receiving-host focus. Keep different-mission replacement and broader mode-entry redesign outside this candidate.

Treat P03-A/B/C/D/E/F/G/H as internal work packages of the approved P03 phase. Scoped desktop keyboard or source tests do not accept P03 as a public/device-complete phase; keep its delivery row implementing through integration and public qualification. P03-D story focus has been explicitly integrated in this P03 branch; its scoped native evidence is not whole-phase acceptance.

For Team's P03-E initial focus, capture untouched-versus-deliberate focus before the classic loader can hide a focused recovery link. Focus the visible enabled current default only after ready and only in a still-untouched foreground document. Reuse shared navigation's one-shot `focusAvailable()` filter rather than `engage()`, which claims ongoing controller ownership. Veto focus after a deliberate choice or foreground loss, clean up boot-only observers on success/error, and never retry on later frames or browser focus returns. Preserve existing error controls and lobby/paused Back/Resume behavior; test actual markup with native-like disabled/hidden focus refusal before claiming native acceptance.

For P03-F, keep native modal Tab containment at the current top-dialog boundary only. Reuse shared visibility/eligibility for native tab stops, including authored tabindex reading regions and native tab order; exclude negative tabindex targets. Let interior Tab, select/text/file activation and Escape retain their existing owners. Wrapping never commits a controller draft, invokes a control or resumes flight. Cover forward/reverse, nested opening order and actual parent-opener restoration, then obtain a fresh native story/Collection pass; keep the predecessor BODY-focus failure and package integration fixture refusals without relabeling them application defects.

For P03-G, guard only the real Solo mission-card, Level, Campaign and Pack change adapters. Keep generic selection/restore/Library adoption contracts intact. Include paused/restored running or recovering attempts; same live selection is a no-op. Share the P03-C checked retention seam, hold autosave before pausing, keep Stay enabled, and recheck saved bytes before a deliberate Replace. Failed saving requires honest loss wording, never a truthy session-return assumption. During a confirmed pack download/install, propagate `preserveCurrentRun` through the existing library replacement and check the captured ticket before target adoption. Preserve the old run/recorder on failure or cancellation, even if pack metadata has committed. Keep the confirmation's focus above the gallery's queued selection focus and return to the actual visible chapter-card or selector opener. Tests must start a real ready flight, retain its queued turn and replay checkpoint, cover a changed saved slot and failed asynchronous installation, and rerun the Team retention cases after factoring. Starting class/Steering live resets and Team setup departure remain explicit later P03 work; authored Tactical Hangar switching stays separate.

For P03-H, route only Starting class and Steering through G's existing confirmation owner. Restore the committed select value before awaiting, keep same-value changes inert, and capture starting class/policy plus scenario, roster and lesson identity. Do not mutate the current run or replay options, and do not treat active Hangar craft as the starting class. Apply preferences only after explicit Prepare fresh attempt. Ordinary flights reuse checked retention; course steering and practice require truthful session-only loss wording with no campaign/session write. First Flight class remains disabled. Preserve G/H/Team exclusion, queued input, capture-stop and neutral Resume. Test real controller draft/Confirm/Back separately from the second confirmation, and retain native and whole-phase qualification limits.

Current First Flight menus do not expose the Immediate/Grid selector: it is inside the Missions deck, whose entry is hidden during the course. Treat H’s in-course steering test as defensive host coverage. A course entered through the supported URL inherits the chosen policy; do not force a hidden selector or claim native in-course policy editing without a separately approved visible route. Ordinary practice retains Menu → Missions → Flight setup.

For the isolated [P03-I Team departure candidate](../../../docs/team-departure.md), guard unfinished Change setup, Retry and both same-tab header exits with one fixed-action Stay/discard owner. Team attempt state is only in the page; never borrow Solo's saved-flight transaction or claim autosave. Preserve normal Team pause releases and fresh-input requirements, capture the validated authored starting level before the core adds live enemy fields, use the actual configuration/seed for Retry, and keep terminal/no-run actions direct. A rendering fault must cancel pending destructive intent without repainting, pause/release input and show a stopped attempt with Resume hidden; distinguish retained old attempts from an explicitly adopted fresh attempt whose painting failed. Route native dialog Escape locally around the window flight listener, retain the shared navigation root/default, and return faults to visible Retry rather than hidden Resume. Invalidate intent on suspension/disposal; late work cannot navigate or restart. Test actual both-player HUD/paint continuity and event propagation without presenting a DOM model as native geometry or physical-device acceptance. Keep the strict co-op core, level/ruleset formats and unrelated Solo work unchanged.

The P03-I desktop record confirms 50px confirmation actions, paused Stay and explicit Retry/setup/header departures, with exact source pins. Its paused-header keyboard attempt was redirected to Resume; keep that gap explicit and add future destination actions inside the active panel rather than widening its root. Native pointer request plus keyboard confirmation is not keyboard access to the header. Retain original failed source/test evidence and annotated intent labels; do not infer phone, physical-controller, offline or complete-P03 acceptance.

For the P03-R1 Workshop Restart correction, reuse the existing native restart decision for every real Restart opener. Keep cancellation paused and restore the actual visible opener; never route Workshop cancellation to the hidden overlay action. Preserve the current checkpoint, queued turn and existing saved bytes while deciding, with no new saved-success claim. Recheck the captured attempt/setup and competing operation ownership before confirmation. Only deliberate Restart may close retained parents and request a fresh run; terminal Retry stays separate. Foreground/pagehide invalidation must not reuse an old confirmation, and queued native close events must not cancel a reopened dialog. Exercise the actual Workshop disclosure/control path, both cancel modalities and currentness, plus delayed/failing picture preparation. Keep predecessor native observations, source tests, successor native and device/public acceptance separate.

### P03 training lesson replacement

Route only the visible First Flight lesson-choice action through the existing fresh-attempt decision. Restore the actual active lesson while waiting; same-option changes are inert. Training is session-only: Stay/Back preserves its exact run and queued turn without any campaign/save write; Prepare adopts the named lesson at Ready and requires a fresh Start. Keep deliberate Skip/Return available outside that decision and unable to bypass its ownership. Do not route generic course creation or ordinary save restoration through this interactive adapter. Use the [P03-K verification and reusable prompt](../../../docs/verification/cross-mode/p03-k/README.md), keeping native keyboard, modeled controller and physical-device claims separate. Compose with other navigation candidates and requalify the final source before phase/release acceptance.

For P03 mode-departure A1, keep Solo and Versus destinations inside Team's active pause/results panel and reuse I's fixed-action discard owner, including stopped-arena behavior and exact v1 Solo return identity. Versus shares one leave screen for its two fixed routes; its host owns pause, and the decision must reread the actual match reference and setup generation before allowing native navigation. Suspension, loss of the menu controller and stale ownership cancel the decision. Stay preserves paused continuations, while ready/terminal routes remain direct. Do not infer a saved Solo flight from a Couch mode change or replace native anchor behavior with arbitrary URLs. Test actual native-default propagation separately from the finite DOM's explicit anchor Enter model. Preserve the separate P08 picture preparation/Start/currentness hunks on integration, and leave Solo outgoing retention and new-origin hints for their own reviewed change.

A1's scoped desktop record now observes both Team in-panel links reached by keyboard with Stay returning to their actual openers, and explicit Versus/Team/Solo departures. Keep that evidence separate from modeled fault, terminal, exact checkpoint and v1 hint cases. Its fresh Versus lobby still had no initial focus and first Tab reached Team; preserve that R5 gap until the separate first-ready correction is qualified. Recorded desktop target rectangles do not certify physical controllers, phones, zoom or responsive layouts.

For P03 A2, route only the visible Missions Team/Versus links through the existing checked Solo departure owner. Capture the actual opener, writer/readback authority, run/recorder/selection/generation and foreground; keep the paused save hold through Stay, failure and suspension. Retire pending decisions before delayed locks or hint callbacks can act after blur/hidden/pagehide. Preserve the byte-exact v1 Team helper. The separate v2 session hint is a bounded one-use, exact-source Solo-Missions → Versus bookmark, never a save, arbitrary route or autoplay command. Couch reads only a validated token and constructs its own fixed URL, capturing the token with its leave decision; it must not adopt a newer token at Confirm or forward a nested hint into Team. Receiving Solo validates and consumes once, then opens the exact Missions selection/focus after ready without adopting or resuming a flight. Preserve truthful second-Leave warnings, current saved bytes and modified native gestures. Compose the distinct R3/title and P08/R5 hunks instead of replacing their host files; qualify the actual combined source and keep modeled, native and public/device evidence separate. See [mode departures](../../../docs/mode-departure.md#a2--solo-missions-to-versus-and-same-tab-return).

A2's desktop record observes a nondefault Missions return and a separate explicit paused saved restore; do not turn its intended cut/Versus event labels into geometry or focus claims that the recorded DOM does not support. Keep stronger checkpoint/queued-turn host tests separate. When composing TitleB, only the named title Continue may change that later explicit restore to run; mode-return receipt/Back remains a presentation-only paused handoff.

For P03-R3, guard only Library's installed Play, challenge Launch and picture Replay flight adapters through the existing Solo replacement owner. Resolve finite descriptors in the app; never expose arbitrary navigation or caller-provided adoption callbacks. Capture the exact Library visit or picture generation and earned record, then recheck it alongside the run/recorder, current selection and saved-slot authority before adopting. Stale work cannot select content, close a newer screen or reclaim its focus. Retire launch-only intent on blur, hidden state and pagehide, including picture Replay; require foreground at request/completion and a fresh request after return. Keep picture generation and import/export lifetimes independent. Same installed/challenge selection is inert; picture Replay is deliberately fresh. Practice retains session-only wording and no campaign/session writes. Keep raw selection, restore/export and P01 operation lifecycle untouched. After deliberate adoption, failure copy must not claim rollback. Test actual retained-parent menu journeys, pending cancellation, newer saved bytes, same selection and fresh Replay seed/difficulty. Descriptor equality does not prove earned-media fidelity: fresh replay still prepares current authored artwork, while exact earned-original playback remains P08-A/P07. Keep source, native and physical/public phase acceptance distinct.

Use the [R3 retained evidence](../../../docs/verification/cross-mode/p03-r3/README.md) to reproduce the real visible controls: enter Expansion packs before installed Play, and press visible Pause Restart plus its explicit confirmation when a compatibility test needs a fresh attempt. Await guarded Library closure instead of assuming synchronous adoption. Preserve difficulty/checkpoint/save oracles. Native earned-picture Replay and challenge observations are narrower than the host failure/queued-turn cases; retain corrected event labels and keep earned-original playback and final integrated/device qualification separate.

For P03 title Start/Continue, follow [the title continuation contract](../../../docs/title-start-continuation.md). Start must launch the named prepared mission; Continue may explicitly resume only the current retained or newly verified saved flight. Keep ordinary Library restores paused. Separate the shell visit/focus owner from the host run/recorder/selection/save/media owner, invalidate old requests on departure or suspension, and keep preparation/Cancel in the active title. After cleanup, reread foreground, current visit and actual focus before returning to the primary; hiding a control can synchronously move focus. Test realistic focused activation separately from synthetic clicks and preserve newer focus or saved bytes. Keep restored-but-stale runs paused, and qualify the combined source before release.

For the [current-P03 Ready focus adaptation](../../../docs/verification/cross-mode/p03-r5-adaptation/README.md), capture untouched foreground focus before the launch helper can hide a chosen recovery control. Deliberate focus, blur, hidden state or any pagehide permanently relinquishes initial focus. Keep the current host's loading barrier and clear its boot inertness before focusing the exact successful initial Ready match's visible enabled Start. Do not activate Start, engage a controller, re-arm after pageshow or replace a newer setup/decision. Test the actual focus invocation while boot flags are clear; a permissive finite DOM must not conceal inert focus refusal. Remove temporary boot observers after settlement. When composing P08, preserve required-picture Cancel/Back availability and requalify that distinct preparation boundary instead of copying its boot order or acceptance onto another source. Keep earlier source/native receipts and later combined/public/device gates explicit.

The minimal R5 native record observes fresh Start and deliberate loading Back retiring the automatic handoff. When that temporary Back is removed, BODY followed by fresh Tab is an honest outcome; do not claim the disappeared control stays focused or re-arm Start to conceal it. Keep raw source-specific observations and exact original receipt bytes. Use the [ready-focus prompt](../../prompts/cross-mode-delivery.md#qualify-initial-ready-focus) for later composition; required-image preparation and physical input remain separately qualified.

For the [common Couch mode presenter](../../../docs/common-couch-lobby.md), pass existing destination anchors into `mountModeChoices`; preserve their IDs, hrefs, listeners and A1/A2/Team discard owners. Current mode is a static `aria-current="page"` span without a keyboard stop or action. Move the same Team row between lobby and active paused panel; never clone links, proxy-click hidden controls or add navigation/Start/state ownership to the presenter. Keep DOM order and actual navigation roots consistent. Shared card styles must outrank host anchor defaults for padding, alignment, current and focus states without bypassing Field Kit tokens. Qualify anchor identity, Tab/Back/Stay, hidden live-play links and computed styles separately from saved-cut/device claims. Preserve R5 initial Ready focus and P08 asynchronous picture/Start ownership during later composition; the retained lobby evidence does not contain those separate candidates.

For P03-M Title mode entry, use the real shared-presenter anchors and the existing checked Solo departure owner, with a retained `solo-title` visit and actual opener. Title Back opens Title via fixed `return=solo`; do not create or consume a Missions hint, proxy a hidden control or auto-start/resume. Retire pending Start/Continue before departure and respect restore cleanup that still owns busy state. Check origin/currentness through save/readback/navigation and post-close focus; preserve newer visits, foreground choices and saved bytes. Generic modal close must remove its stack entry even while inactive, but admit queued opener restoration only from foreground and recheck before focus. Keep these checks distinct from sticky Title lifecycle cancellation. Follow [the Title mode contract](../../../docs/title-mode-entry.md), preserve Missions v1/v2 and separate source/native/device evidence.

### Completed-operation focus ownership

For [Still picture preview actions](../../../docs/operation-focus.md#still-picture-preview-actions), opt in only the actual focused chosen/saved/authored Preview opener. Capture after task admission and before disabling, restore after the owning final sync, and recheck serial/abort/disposal after DOM callbacks. Abort must retire focus listeners immediately even if decoding never settles. Preserve newer input or lifecycle intent, media originals and assignment authority; never focus Save automatically. Model native disabled-button blur to BODY, then qualify actual keyboard focus. Discard and other media workflows retain their own contracts.

For composed navigation qualification, retain exact complete-file scope and failed predecessors. If an unchanged committed fixture was absent, restore that exact Git blob and rerun the affected whole file; report aggregate passing coverage separately from a single complete passing command. Keep physical inputs and canonical staged path/mode/blob identities pinned. Raw Git index cache hashes are diagnostics, not staged-content authority. See [the composed P03 record](../../../docs/verification/cross-mode/p03-composed/README.md); internal commits and partial desktop checks do not establish phase, device or public acceptance.

Follow [the operation focus contract](../../../docs/operation-focus.md) when disabling a focused initiating action. Capture before mutation, keep task/commit authority separate, retire for newer focus/dialog/lifecycle intent, and restore only the original connected/enabled/visible control. A window capture listener sees descendant non-bubbling blur: require the actual Window event target before treating it as backgrounding. The local native model must emit element blur and use one Window identity. Treat automatic completion and explicit Cancel as separate intent: actual focused Cancel may capture a fresh one-shot lease before abort, constrained to the original connected opener in the same dialog. Never resurrect a retired generic lease; cleanup callbacks, backgrounding and root changes still veto restoration. Test actual Tab traversal to Cancel, not only synthetic direct focus. Use only the explicit Remove adapter for a logical installed-row fallback; other recreated controls have no implicit return target. Keep corrected native intent labels, failed/partial test logs and source/native scope separate, as in [the P03 correction evidence](../../../docs/verification/cross-mode/p03-operation-focus/README.md).

For the installed-pack Remove focus adapter, preserve the fixed-target default and opt into logical resolution only for that action. Capture row identity/order and the Library visit; after settlement and refresh, recheck the actual pack model and render/task generations. Prefer the recreated same-version Remove control, then next/previous surviving Play control after deletion, then the fixed Expansion packs section button. Focus never invokes an action. Only a still-live focus lease may resolve, and post-resolver same-root/foreground checks remain mandatory. Preserve durable Stop waiting as detached, and do not equate an error with rollback. Score/Collection paging is separate; delayed picture metadata must not steal newer navigation focus.

For [Score and Collection pagers](../../../docs/operation-focus.md#score-and-collection-paging), retain the actual Previous/Next controls and update their callbacks/page state. Keep enabled focus in place; only an owned unavailable action may fall back to the opposite enabled pager or matching search. An automatic refresh must transfer the existing one-shot lease rather than recapture focus or discard still-valid intent. Carry its retired state, detach stale reads from the transferred owner and recheck exact generation/model after callbacks. Preserve newer focus, dialogs and foreground decisions during delayed metadata. Test native disable/removal, collapse/expansion, reentrant reads and actual Solo record/run preservation; keep fixture-origin Import UI paging distinct from human-earned gameplay and exact-original artwork. The adjacent picture-view close/return owner is separate.

For pager reveal, logical focus is insufficient when content grows above the same persistent control. Opt in only from the pager owner, reveal with nearest scrolling without another focus call when already focused, and retain veto listeners through any actual focus callback. Re-resolve only while the original lease is valid; check exact active target/root/model/foreground before scrolling. Preserve default helper return semantics, reject recursive restoration and clean up even on thrown DOM callbacks. Inspect the resulting viewport as well as activeElement. Seal post-native source/HTTP pins before editing; if that boundary was missed, retain the original hold and state the missing post-check explicitly rather than reconstructing a stable-after claim.

For the explicit Collection picture-Back adapter, keep the original card reference and its stable key/page/query. Admit its independent focus lease at child close only when native restoration still owns that exact connected card, before metadata refresh rebuilds cards. Never adopt newer Search/Close focus or generic BODY as origin. Resolve the keyed enabled successor or existing enabled-card/Search fallback only under the same model/view/population and a live lease; recheck target eligibility and retire after settlement. Preserve Replay handoff, read-only records and original-image releases. Do not merge this owner into pager/import/row owners. Model native opener restoration before queued close and one Window identity; keep legacy synchronous/direct-activation adapters explicitly qualified. See [the gallery-return evidence](../../../docs/verification/cross-mode/p03-gallery-return/README.md); source/host tests do not certify native focus visibility or complete P03 acceptance.

When composing P05 onto current P03, apply the named display/confirmation/style commits incrementally; do not replace newer app or Couch hosts with old candidate files. Preserve Title and Library request identities, mode-return tokens, Team discard/current recipe and common mode rows. Add display disposal to the existing lifecycle: Team pagehide must still suspend and cancel departure, then close display/audio only outside BFCache. Keep one authority/subscription per host. Retain historical receipts unchanged and qualify the actual combined source; Standard/Large alone does not certify text scaling, reflow or physical-device readability.

When adding display controls before existing Couch options, update the screen's explicit initial target to the first visible enabled field while preserving its Back opener and pause owner. Check both ready and paused entry, no opening write or step, exact Back focus and explicit Resume. A finite HUD may update only on the next frame: observe it with a zero-time frame instead of reading stale text or advancing the round. Keep baseline failures, corrected host expectations and source-specific native groups separate in the [P05/P03 integration record](../../../docs/verification/cross-mode/p05-p03-integration/README.md); its desktop preset checks do not certify zoom, reflow or devices.

### Narrow Team HUD layout

For a narrow Team HUD correction, keep semantic font sizes and arena/input geometry unchanged. Give each player a number/name row and full-width status rows within its half of the HUD before reducing text. Inspect actual 320/390-pixel Theme/Plain and Standard/Large combinations, the complete arena, Options/Back and explicit Resume; record viewport, DPR and measured height with exact source pins. The [scoped HUD checkpoint](../../../docs/verification/cross-mode/p05-team-hud/README.md) leaves wider short-landscape scrolling/Pause placement, actor readability, zoom and physical devices open. Banded screenshots are inconclusive capture evidence, and a fresh-tab recovery is not a gameplay fix. Do not turn these observations into P05 completion or a public release claim.

### P05-B menu appearance ownership

Read [menu-appearance.md](../../../docs/menu-appearance.md) before changing menu palettes or ornaments. Keep `revealline.menu-style.v1` separate from the strict display/audio/profile schemas. Menu controls may update only page appearance and the two-field menu record; preserve actual Options order, checked departure, paused checkpoints and explicit Resume. Automatic classifies the already accepted theme without selecting or rewriting any artwork. Use existing semantic colour and border-only frame slices for opaque functional surfaces; preserve original image/font bindings and canvas inputs. Ornaments must remain static, noninteractive and outside text/focus gutters; late player dialogs need page-owned attachment/replacement cleanup, not an arena/HUD observer decoration rule. Retain failed/superseded diagnostics and distinguish token ratios and finite native-default modeling from actual computed pixel, zoom, system/storage-event, physical-device and public acceptance. This P05-B candidate adds no production art or phase-complete claim.

Do not infer a gutter from a panel border: both Couch shells originally had zero inline content padding and native Rich stripes crossed controls. Keep an explicit border-box budget for stripe, inset and focus outline, preserve Off and existing block spacing, and permit content shrink without clipping. Source-rule arithmetic is not rendered-width proof; compare actual text/control/focus bounds to decoration in every affected host. Preserve the failed native source separately from its correction. Row-to-row spacing, scale changes and device readability remain distinct checks even when side ornament clearance passes.

For [Couch visible focus](../../../docs/couch-visible-focus.md), preserve the current screen flow and focus owner. Install final synchronous title, result copy and visibility before the transition focus, then reveal only the action that actually owns focus after its callbacks. Recheck foreground, connected/visible/enabled state and the same root, screen and status before nearest/auto scrolling; a newer choice, screen, suspension or disposal must veto the old request. An already-focused persistent action may still move outside the viewport when surrounding content changes. Keep this local shell policy separate from pager/operation leases and Team ownership. Test focus-callback retirement and unchanged checkpoints, then inspect the actual restored action and focus ring at the recorded short viewport. Scroll-request assertions do not prove geometry, and Standard/Large or viewport override does not certify browser zoom or physical devices. Preserve the failed native originals and exact corrected source scope; do not claim blanket WCAG or phase acceptance.

Separate DOM focus geometry from capture quality: keep distorted overridden-viewport screenshots as limitation evidence, record actual viewport/DPR and encoded image type, and compare normal reset captures without inventing a runtime or capture cause. A visible action in geometry does not certify complete-arena visual layout. Resize-only persistent focus and physical/zoom qualification remain separate from explicit screen-return restoration.

For the [P05 Couch resize candidate](../../../docs/couch-resize-focus.md), a Window resize owns only the actual current focus at that instant. Validate the same connected, enabled, visible screen/root/status and foreground after geometry/style reads before requesting nearest/instant reveal; never select a new primary, call focus or queue restoration. Preserve the existing transition focus/order, actual live/paused/finished checkpoints and terminal listener cleanup. Keep modeled rectangles and callback regressions separate from native viewport/DPR/focus-ring observations; do not classify unexplained capture bands as an arena defect. Retain the scoped native resize result with its actual viewport/DPR, same current action and displayed paused state; authoritative checkpoint equality remains host-test evidence. Final phase, broader layouts, other modes, zoom and device qualification remain separate.

### Team required-picture host ownership

Follow [the Team picture host contract](../../../docs/team-picture-host.md) when wiring the strict two-row authority. Retain one presentation-page lease and keep a candidate core/image separate from the accepted attempt. Refuse changed starter content before picture reads; retain the authored recipe rather than mutable live enemies. Cancel and late preparation settle only to Ready, never automatic Start/Resume. Preserve the existing departure, mode-row and shared preference owners. Fresh pointer/key intent can retire asynchronous focus even without moving activeElement; remove those listeners with their owner. BFCache suspension retains the accepted picture, while terminal cleanup must not repaint after the page's presentation lifetime has ended.

Exercise controls through their actual event registration and use elapsed readiness deadlines for Blob/hash/decode work. Keep HUD continuity separate from exact checkpoint proof, and assert storage read keys if claiming no Solo reads. Retain initial menu-fixture and terminal/focus failures, complete-file counts and held source identities. Native delayed loading must use an explicit exact-byte response fixture with normal HEAD and separate before/after source pins. A missed locator or failed human route is not a successful action or automated-test failure. Partial reveal does not establish full Team completion, joint/rescue behavior, actor parity or physical-device acceptance; preserve those limits in the [host evidence](../../../docs/verification/cross-mode/p08-team-host/README.md).

For [Collection Flight records return](../../../docs/collection-records-return.md), refresh the open parent through its explicit read-only owner instead of clicking its entry control. Capture only the actual restored records opener before population; bind visit, launch, exact model and expected population before publication. Reveal only after final synchronous or asynchronous content settles and the lease remains live. Never capture newer Search/Close or BODY as origin, reopen a closed Collection, or refocus an already focused control. Preserve separate pager/view leases and original releases. Model native restoration before queued close; test import-driven growth, still-metadata success/refusal, newer input and unchanged run/save state. Actual scroll geometry needs its own source-bound browser check.

When checkpointing a sparse integration, name its entire staged composition and every original parent; a narrow working diff does not describe an index already containing earlier merges. Preserve each parent's ancestry, immutable failure history and source-specific test/native scopes. Keep separately run complete-file cohorts and post-run fixture dematerialization explicit. A native preview admitted before a remaining test finishes must retain its pending-gate receipt; record the later test result separately. See the [four-parent source checkpoint](../../../docs/verification/cross-mode/cross-mode-source-composition/README.md).

### Team actor presentation boundaries

For the [Team actor checkpoint](../../../docs/team-actor-presentation.md), map actual strict `drifter`/`hunter` identities explicitly to borrowed prepared slots; source-image names are not behavior types. Preserve Team's authored radius, including 0.01, when a shared cosmetic sampler normalizes values. Paint every body before functional overlays and restore each real contact marker after overlapping art. Derive readable labels and number/shape badges from canvas CSS pixels, not board cells; bound full plates away from both pilot heads without shrinking text. An inward cosmetic body offset must include rotated/banked source, motor and nose bounds, restore before contact drawing, and visibly connect to the unchanged cutting head. Keep the shared default drawing path unchanged. Test plain/theme fonts, compact/detailed frames, pause/reduction and exact run preservation, then inspect actual desktop/phone edges, warning and bright-art cases. Source previews, token ratios and command tests do not establish native readability. Retain failed enum/radius/readability evidence and scoped native successes; no new Team sets, catalog surface-motion, full-win/rescue, hardware or P08 completion claim follows automatically.

### Qualifying a composed host and its finite inputs

For the [gameplay/presentation composition](../../../docs/gameplay-presentation-composition.md), preserve exact incoming bodies and all four source histories; review the small shell/guidance merge separately. Derive finite relative imports and literal URL dependencies from the final test entries and actual Solo/Versus/Team hosts. Old qualified inventories can omit a newly inherited import. Classify directory bases and platform-gated generated artifacts explicitly, hydrate only exact required Git bodies within the capacity guard, and retain the failed whole run before rerunning the complete affected cohort. Keep runtime/index stability and post-test guidance copies visible in the evidence map.

For native response fixtures, use fresh source-bound origins and record actual asset ordinals, status and elapsed delay. Separate map readiness from Start, and batch a timing-sensitive Next/Cancel observation when necessary. A missed Cancel or extra setup request is an operator timing record, not successful cancellation or proof of stale automatic Start. Check the same earned result after late response completion. Shipped Next may start while its gesture still owns adoption; installed Ready has a distinct explicit-Start contract. Use HEAD for postchecks that must not consume fixture ordinals. Preserve actual viewport/DPR and geometry versus inspected screenshots, and keep an observed layout failure open even when other orientations pass. Future P02 composition must rebind reviewed exact Team snapshot/assets and requalify authority; historical revisions must not become wildcards.

For reading prompts, describe the active input method and actual exit handlers: keyboard Enter/Space/Escape, current mapped controller names, and touch or mouse Done reading. Native button activation remains browser-owned. A keyboard scroll can publish before the input-modality callback; refresh only the still-current reading hint after modality changes, without sync, focus, re-entry or a new reading lease. Keep the visible Read origin and existing reader validity rules. Optional providers must preserve legacy defaults in other hosts. Test first-gesture switching and unchanged checkpoint/storage separately from native geometry or hardware; see [reading input prompts](../../../docs/reading-input-prompts.md).

For [Couch Help reading](../../../docs/couch-reading.md), reuse explicit `surfaceDefinitions` with stable Read/Done siblings and a visible hint outside the text region. Preserve default Solo definitions when omitted. Update copy only on fresh native or routed controller input; neutral polls must not overwrite the keyboard prompt. Done stays end-only through pointerdown, click and cancellation; no reading return starts or resumes a run. Test actual listener registration instead of calling an obsolete `onclick` property. Retain exact Team picture mounting/readiness in composed audio fixtures and preserve the newer permanent audio note. Qualify the completed phase and version only after integration with the active accepted release.

For delayed Couch menu polling, distinguish the host's last observed scope from navigation's current scope/root. A newer keyboard or pointer reader may already own the destination when the router catches up. Synchronize actual navigation ownership and discard that stale routed command; do not unconditionally clear the newer reader. Retain genuine scope/root invalidation, disconnect/departure cleanup and neutral-input gates. Exercise Help entry plus native pan followed by controller frames, and failed/cancelled Next followed by a newer reader, using complete host tests and exact current picture preparation.

Keep Team's foreground inactivity separate from core Pause. Suspension clears reader and held menu/flight state even when already paused; background frames must neither poll controllers nor step the simulation, and stale Start/Resume must be rejected. Foreground return only resets lifecycle timing/gating; Back and explicit Resume retain their own ownership. Preserve Team's fresh-direction continuation and P02's permanent capability note, transient storage status, master output and transport intent. Retain failing-then-passing host evidence for blur, hidden state, persisted return and missed blur; actual browser foreground journeys and physical/native-wrapper qualification remain separate. See [Couch Help lifecycle guidance](../../../docs/couch-reading.md#host-foreground-and-delayed-scope-observations).

When opting a reader into resize visibility, reveal only the same current foreground reading unit with immediate nearest scrolling. Revalidate reader generation, actual focus, scope, text and visibility after host callbacks and geometry reads; no sync, focus, re-entry or delayed restoration follows from resizing. Preserve opt-out callers. An oversized-unit model proves repeat suppression only: inspect Done, hint and scrollable text together at the actual short-landscape viewport and Large text size before accepting the correction.

Intersect resize visibility with overflow ancestors' native client bounds; a unit may fit the viewport yet be clipped by a pause panel. For opted-in native reading scroll, retain the actual current region and matching primary pointer without preventing browser defaults; verify pan adoption/cancellation followed by Done, not just taps on the toolbar. Reading hint context includes kind and regionId so ended messages can remain with their original sibling status after ownership clears. Preserve one-argument non-reading consumers and keep physical touch proof separate from modeled pointer events.

For a native reading supplement, keep its exact source checkpoint and original model-only evidence separate. Record the actual input, focused region and paused state before and after each exit or modality change. Retain every original observation and image with byte hashes and before/after source-response pins. If an accessibility snapshot and subsequent DOM disagree, distinguish the intended action label from the observed DOM and leave the cause unresolved until reproduced; do not count it as a passed journey or proven product defect. Keyboard/pointer observations do not qualify physical touch/controllers, scrollable reading, exact save bytes or the whole phase; see the [bounded native reading record](../../../docs/verification/cross-mode/p03-reading-prompts/native/README.md).

### Preserve source-stage production and honest browser evidence

For [P03 production integration](../../../docs/verification/cross-mode/p03/recipe-review.md), preserve intermediate source-stage ledgers as well as published ones. Pin a measured checkpoint's canonical prefixes and original payloads before appending review metadata; reproduce against the complete current ledger. Canonical export sorts binding keys, while the original producer visits slot order: a historical replay may restore insertion order only after asserting exact canonical equality. Never change an oracle, flatten a checkpoint or normalize original evidence to conceal a failing test. Distinguish recipe slots from independently approved raster images when testing source invalidation. Rebind Team only to the actual generated revision with unchanged exact image identities.

If browser tooling cannot export screenshots, record actual observations and response pins, state that visuals were inspected inline, and keep raw-image/public/device acceptance open. Never invent artifact paths or image hashes, and do not turn an unsupported focus query or a hidden-tab operation into proof of real foreground loss. A named feature release can deliver qualified corrections while the parent phase remains implementing; retain the parent's required input and public gates.

Story controls may blur synchronously when hidden or disabled. Capture the prior owner before rendering, but recover focus only while the page is visible and focused. A background Pause must retire the control without focusing Resume in the inactive document. Test focused Pause through both hidden-page and window-blur paths, unchanged playback position, no automatic foreground resume and a later explicit Resume. Preserve the initial failing observation and qualify the corrected source rather than reusing an earlier running CI result.

Guard every Resume entry against the actual hidden/unfocused document, including the already-cached picture path, before changing ownership, audio or run state. A queued activation can arrive after lifecycle suspension. Test both turn modes with an unfinished line and pending grid turn, unchanged checkpoint/focus/storage while inactive, and fresh explicit Resume immediately on return. Do not require a frame to clear an old controller-inactive flag before accepting that fresh gesture. Separately test frame-based suspension when a lifecycle notification is absent. A captured callback is a race regression, not physical background-input evidence.

Real embedded-art chapter tests validate and store multi-megabyte originals. Use the scoped chapter-install wait with its bounded 30-second allowance and elapsed/state diagnostics; ordinary action waits retain their five-second limit. Keep exact selected-content/readiness predicates and all gameplay/storage assertions. Cache expected campaign identities outside polling. A timeout alone does not distinguish slow work from terminal failure, and a larger allowance is not an installation performance guarantee. Retain failed hosted originals and verify the complete affected files before fresh final-source qualification.

Keep filesystem staging budgets separate from the JavaScript heap. Use the repository's established test invocation for embedded-art hosts; imposing the worktree's byte limit on V8 can terminate verification before its assertions. Retain such execution-configuration failures separately from reproduced product regressions.

## More worlds selection ownership

Keep **More chapters** beside Back in the existing sticky Missions heading, outside the collapsed Game mode controls. Preserve selected-chapter focus and the measured heading/footer clearance; scrolling must not hide the chapter-discovery action. Reuse `shell-worlds` and its handler, explicitly hide it in briefing, preserve Training's visibility authority, and qualify keyboard/controller reachability with Game mode closed. Use the existing `navigation.missions` copy key for the concise heading, retaining Standard/Large text sizes. Check narrow portrait and short landscape with Large/Plain text before accepting the layout. Do not add a second installation entry or change launch authority when reorganizing the menu.

For Solo More worlds, use one Download & play / Play action and the [staged launch contract](../../../docs/more-worlds-flight-selection.md). Capture the exact activation and original attempt before any network await. A durable install is distinct from launching: cancellation may keep installed content but must not adopt or start stale work. Prepare run/recorder/picture separately, install stage authority before storage reads, and recheck after every reentrant boundary. Preserve Stay and verified-save warnings for unfinished attempts; confirmed Replace & play starts the accepted ready attempt once. After Stay, publish a guarded ready status explaining that the flight is kept paused and Play remains available; do not retain the closed replacement dialog’s instruction or let late work overwrite a newer status. Consume authority before disposal/close callbacks and recheck the committed run before Resume. Preserve exact pack metadata through difficulty, Retry and Next. Differing artwork under identical historical media identities is a visible conflict, never permission to rewrite earned originals or choose the first pack.

Legacy Choose-only panel adapters, ordinary Library and setup retain selection-only behavior; same-chapter Choose stays inert while explicit Play requests a fresh attempt. Manual exact-pair recovery remains install-only. Retire automatic play on close, newer navigation, filter/page changes and lifecycle changes, while retaining any completed install. Early Stay releases its own parent operation and restores its stable Play button without letting late completion unlock newer work. The pinned card label denotes browsing, not the active campaign.

Prompt: “Reach Missions → More chapters with Game mode closed using keyboard and controller navigation. Keep the chapter action visible in the sticky heading and the selected card clear above Deploy on portrait and short landscape with Large/Plain text. Verify Download & play through the real app and exact published bytes: one-action start, installed no-download, held/failed artwork with old result retained, committed-install cancellation, Stay/Replace saved-slot changes, storage-read and disposal reentry, newer navigation, and duplicate campaign identities with different art. Retain legacy Choose and explicit Resume contracts. Record complete affected tests, native device/input journeys and public release evidence separately.”

## Keyboard capture before shared menu navigation

A document capture listener runs before a dialog capture listener. Let the exact active key-capture owner claim its event before shared navigation moves focus; use the host's current top dialog and live capture state, not a blanket Settings exemption. Yield without preventing the event or changing focus, while retiring stale navigation previews. Keep the existing binding validator, Return/Space rejection, Escape cancellation, native Tab traversal, repeat/modifier/composition rules and controller command handling unchanged.

Reproduce through the actual document → dialog chain, including a preset with unassigned arrows. Verify valid remaps, rejected duplicates, correct listening focus, normal menu/select/slider behavior after cancellation, and unchanged Ready/paused run checkpoints. Component-only dialog tests miss this ordering. Preserve native baseline observations and failed-then-corrected host logs separately; a known regression in an unpublished candidate needs correction and fresh final-source qualification before release. See [keyboard capture ownership](../../../docs/controller-navigation.md#keyboard-capture-ownership).

### Embedded practice keyboard exit

For a Controller practice focus-boundary change, retain the explicit registered same-origin iframe/session owner. Test actual document key propagation at first/last nonmodal Tab boundaries, including Shift+Tab, plus a real modal, active reader, native input and unchanged standalone menus. Parent focus transfer must be synchronous, release held virtual input/pending stick application and leave gameplay paused. Reject stale sessions, loading/replaced frames, background or newer parent focus; do not add a broad Escape override or change historical replay behavior. Repeat the actual served iframe journey without mouse or state fixtures; simulated bridge coverage is not physical-controller acceptance.

Repeated Pause rendering is not a new focus request. Retain the selected action when the Pause overlay is already visible, and only focus a newly opened overlay while its document is visible, foreground and not owned by a dialog. Test the actual app blur handler during the synchronous child-to-parent handoff, not just the transport helper. Cover first blur from a moving cut, repeated suspension after Settings, both paused Tab boundaries, unchanged checkpoint/profile and explicit Resume on return. A background child's attempt to focus Resume can silently reclaim its iframe and undo an otherwise correct parent handoff.

For Replay focus, let a native Jump anchor perform its fragment default before an owned deferred handoff to a usable transport control. Newer focus/input, cancellation or backgrounding retires that handoff. When completion disables focused Play/Step, move to Restart only while that completing control still owns the journey. Preserve unrelated editor focus and recorded checkpoints; qualify native browser defaults separately from the modeled regression.

When primary controls use ID selectors, guard both shared and route-local active/hover styles against native disabled and `aria-disabled="true"`. Otherwise legacy specificity can leave an unavailable action looking primary. Preserve the existing semantic disabled state, focus handoff and recorded progress. Compare native computed colors across empty/loading/ready/completed states; do not add new navigation owners or treat a source-string check as rendered-state evidence.

For cold-module startup tests, await an observable application boundary such as entry into a deliberately held fetch; do not count event-loop turns as elapsed startup time. Assert early preferences while that fetch remains held, propagate import failure, and clear bounded diagnostic timers. On any failure, close the host, release and drain pending imports, retire late owners, then restore globals. Preserve failed hosted originals and run fresh exact-source qualification after correction.

## Team live-shell and focused-action clearance

Follow [Team display clearance](../../../docs/team-display-clearance.md) when changing shared preferences or the Team shell. A viewport-visible action can still be clipped by the Settings dialog scroller. Preserve the current Settings category and dialog owner. Intersect its real inner client bounds with the viewport and reserve the complete focus ring; reveal only the same current usable action with immediate nearest scrolling. Revalidate attempt, menu, display revision, focus, foreground and disposal ownership after geometry/style reads. Never refocus, schedule restoration, replay input or Resume as a side effect of display changes. Preserve unsaved local preference intent when persistence is denied.

During a live Team attempt, hide redundant masthead navigation only while the real gameplay overlay is hidden; restore it for pause/results/lobby. Keep essential exits and tools reachable through the existing Pause owner, keyboard Escape, mapped controller Pause and visible Pause action. Preserve canvas focus after explicit Resume. Solve the shared live-shell constraint before accumulating device breakpoints; do not crop the arena, shrink readable text or 44 px controls, or alter simulation/replay/checkpoint identity to fix layout.

Review Theme/Plain and Standard/Large at actual playing size, including short landscape with hidden and visible pads and portrait with both pads. Verify the entire current selector/ring inside its scroller, not merely its viewport rectangle. Retain exact source/override body pins, prior failures and actual preference readback during cleanup. Responsive browser observations, modeled ownership tests and physical touch/controller/zoom qualification remain separate; a scoped layout pass cannot close the phase or replace final composed-source and public-release gates.

## Controller Practice and Playground reading ownership

Follow [Practice and Playground readability](../../../docs/practice-playground-readability.md) for this supporting-tool integration. Mount the lightweight display entry independently of the application. Controller Practice delegates its existing text-size selector to one display writer; Playground reads shared settings without adding a duplicate settings form. Apply only host text/copy/effective motion attributes. A display update must not neutralize controllers, replace or resize the child, sync an editor, create history, start a preview or write player progress. Only an explicit marked preference action may save the separate shared display record. Keep the live save warning under one owner so later cosmetic copy cannot erase it. Retain the display owner on persisted pagehide; dispose it on terminal departure without claiming that every legacy application listener/timer has also retired.

Last Undo deliberately restores the prior working configuration and resets the unapplied editor draft through the existing sync path. If it disables itself, hand off only the focus it owned to the available selected brush, then the existing editor fallbacks. Keep Undo focused when history remains; respect newer focus and hidden/unfocused pages. The child changes only through explicit Play configuration. Do not add automatic preview, broad autofocus, input replay or a global focus observer to repair this path.

Live viewport readouts describe only the current measurable child document. On replacement, delay or inaccessible/error content, clear stale live arena/control/launch values and say why measurements are unavailable. Empty or nonfinite boxes cannot establish positive minimum sizes or a complete visibility pass. Preserve an explicit geometry capture and timestamp as historical until recaptured; current live updates do not overwrite its bytes. Test the actual HTML entries with genuine edits and held imports, and drain owned work before restoring fixture globals. Compare focus, frame and draft identities rather than fixture aliases. Keep modeled owner guards, browser next-Tab/geometry, physical controls and zoom as separate evidence; use the guide's exact source and partial native rows before advancing the feature.

Browser history form restoration is presentation, not preference intent. A selector can receive an old value without a change event after the shared owner has applied its current state, including after `pageshow`. Reconcile marked controls immediately and in one cancellable next-task pass from the current owner's snapshot; never persist the restored DOM value or replay a captured old snapshot. The existing owner still decides shared refresh and unsaved local precedence. Cancel deferred repair on pagehide/disposal, coalesce repeated pageshow, and preserve failed-save warnings, child identity, focus and controls. Only an explicit selector change may save. Test both event shapes, late restoration, changed shared authority, unsaved intent and a newer explicit choice. Retain the actual native before-failure and corrected return separately; a new child session on return is not proof of BFCache admission. See the [history restoration evidence](../../../docs/verification/cross-mode/p05-practice-playground/history-restoration/README.md).

## Production and Viewport supporting hosts

Use the independent read-only `authoring/shared/host-display-entry.mjs` for [Production](../../production/README.md#shared-reading-preferences) and [Viewport](../../viewport-lab/README.md#reading-preferences-and-startup). Consume the existing shared authority; never couple text/effective motion changes to register fetches, image decoding, draft edits, child navigation or profile writes. Preserve Production filter/detail/image identity and exact Back-to-row focus, including its existing pagehide cancellation.

Viewport uses the shared recoverable launcher, disabled waiting controls and an available direct-open escape. Hand retiring Reload focus to Game only while that action still owns the handoff in a visible, focused document. No late foreground autofocus is allowed. Test newer focus during retirement and all five actual presets with both an empty frame and Couch loaded/Solo pending. Keep the same iframe and require explicit Load to replace it. Run both complete `authoring-host-display.test.mjs` and `viewport-startup-host.test.mjs` on the final composition; actual history/form restoration, font/layout/zoom and physical input need separate evidence. These are local-only authoring routes, not publicly deployed game URLs.

For saved Continue cancellation, create and save a real flight through UI, cancel the visible title action, and check focus plus the retained checkpoint through mode departure/return. Keep missed cancellation windows and preliminary lookup failures explicit. A pinned original restores through IndexedDB and blob decoding; an HTTP-delayed fresh Start is a different boundary. Distinguish actual ordinary cancellation from modeled late decode rejection, busy cleanup and terminal Team results; neither accepts the whole navigation phase.

## Earlier profiles reading ownership

For [Recovery readability](../../../docs/recovery-display-readability.md), use the existing packaged `game/ui/tool-display-entry.mjs` before the Recovery application entry. This page has no marked preference controls and must remain a read-only display consumer. Shared/system changes may update only text/effect attributes; preserve selected original, pending reader signal, prepared Blob/download link, current feedback and focus. Do not create a second controller/router/reader or inspect profiles to apply display settings. Keep the data host's existing pagehide cancellation and persisted-return restart separate from the display owner's retained preference subscription. Native layout, current-action focus, actual downloads, system reduction, zoom and physical input require their own observations; modeled page events do not prove BFCache or release readiness.

For [Recovery startup navigation](../../../docs/recovery-startup-navigation.md), keep Back/Reload navigable before version/catalog readiness with one page-owned router. Retry replaces the read generation and deadline, not controller assignment. Background or stale completion must neither adopt data nor restore focus. Retire focused Reload to Find only synchronously for the same current foreground startup; clear stale navigation focus so later neutral polling cannot revive that handoff. Keep the existing reader's close-before-Back guarantee, explicit discovery and zero-write scope. Test actual held, failed, timed-out and late startup with the real router/navigation entry and distinguish modeled pad edges from physical controller acceptance.

When Recovery Back awaits reader close, capture its originating view and read generation. Permit the deferred host callback only for that same current foreground owner; invalidate the final navigation after pagehide. Preserve close-before-Back and allow a fresh current Back after a cancelled stale request. Test the actual host/view/reader with a held readonly transaction abort, both legitimate closure and history/background supersession. Keep modeled storage events separate from native IndexedDB and BFCache evidence; see the deferred Back correction in [Recovery startup navigation](../../../docs/recovery-startup-navigation.md).

When a Recovery read disables its focused Find, Review or preparation action, pass that exact origin into the current operation. Transfer only its foreground focus synchronously to Cancel; never use body focus alone as ownership evidence. Return only while Cancel still owns focus, the operation remains current and background cancellation has not invalidated that return. Keep Find's result-aware Find/Review destination, other enabled origins and the existing successful download-link destination. Preserve newer focus, unowned/background starts and closed views. Test the real view with the observed disabled-control blur, empty/error/channel outcomes and original preparation; retain native focus and downloaded-byte evidence separately. Recovery is currently an explicit troubleshooting URL, not an advertised Library destination. Do not silently add navigation or fabricate eligible profiles to complete a test. See [ready-action focus continuity](../../../docs/recovery-startup-navigation.md#ready-action-focus-continuity).

Prompt example: “Repair only the current Recovery action's focus through async work: focused origin → Cancel → result-appropriate action or prepared download. Prove newer focus and background cancellation win, preserve all read-only data semantics, and keep corrected native and physical-input acceptance distinct from modeled tests.”

Explicit Back is a transient departure until the browser declares terminal `pagehide`; do not irreversibly destroy Recovery's sole navigation owner before possible cached retention. Suspend sampling and clear held controls while leaving, retain close-before-Back, and restart one fresh read generation with neutral input on persisted return. Terminal departure must destroy the owner and reject stale return/async callbacks. Test a fully completed startup Back and ready Back before the persisted lifecycle, not just cleanup interrupted before navigation. Preserve the failed predecessor and separate actual-host modeled cache events from native history traversal, verified cache admission and physical-controller evidence. See [the history-return correction](../../../docs/recovery-startup-navigation.md#explicit-back-and-cached-history-return).

Prompt example: “After Recovery Back actually completes navigation, simulate a persisted hide/show and prove the same controller can use the fresh page only after neutral input. Repeat a terminal departure and prove it cannot revive. Keep one navigation owner, no automatic Find, unchanged prepared-data permissions and exact original failure receipts.”

## Motion Lab reading and startup ownership

Motion has one independent display owner and an explicit shared Text size writer. Retire the generic surface marker before adding the writer; retain newer copy keys and shipped build configuration. Keep local reduced-motion intent distinct from the effective shared/system cap. Preference updates may repaint labels, but cannot reset recipes, selected art, steering, queued turns or Play/Pause. History-restored form values are not intent: reconcile the marked selector immediately and through one cancellable next task from current authority, preserving unsaved choices and warnings.

When ownership moves, update shared surface/build integration assertions as well as the local host tests. Require the independent entry and its shipped module closure, and reject the retired generic marker so a second body-only listener cannot return. Keep the actual shared-preference behavior tests. A finite host fixture cannot qualify complete build-file discovery/reference validation; run that complete integration file on the real source tree in the hosted gate when the local tree is sparse.

Keep the study locked until validated presets and setup succeed. A disabled parent fieldset preserves each child's domain-disabled state; never blanket-enable Clear, locked equipment or unavailable abilities. Canvas input and Tab participation require their own startup lock. Keep the independent reading control and return navigation available during both module and preset waits/failures. After module attachment, failed preset setup needs its own authored same-route Retry link outside the study lock; show it without focus movement. Keep it hidden while pending/ready and after terminal stale completion, and preserve the existing launcher action for graph failure. When attachment hides Reload, transfer only still-owned foreground focus to the reading control, respecting newer ownership even during retirement. Readiness must not focus the arena or override a pre-start interruption.

Exercise the actual classic launcher, module delay/failure separately from preset delay/failure, current-owner handoff/refusal and the successful lock release. Use a single owned frame, reset its clock on interruption, and abort/drain initialization on terminal departure. Keep recipe/simulation sources and original art unchanged. Run whole affected files on supported runtimes with bounded fixtures; exact-source build/release, native layout/focus, imported animated-background policy and physical input remain separate acceptance.

Example prompt: “Continue Motion Lab from its exact reviewed source. Preserve independent reading, one explicit writer, local effect intent, semantic disabled flags and explicit Play after interruption. Prove actual-launcher delay/failure, focused Reload retirement, actionable network/validation retry without focus theft, preset readiness and history selector restoration without saving browser-restored values. Keep study geometry, algorithms and asset originals byte-identical. Qualify complete scoped tests, then ordinary build and real-browser/native/public journeys in their allocated lanes; retain failures and separate evidence categories. Update this skill and the Motion guide without claiming full P05.”

### Motion label visibility and overflow

Share presentation-only descriptors between canvas captions and the ordinary DOM stage legend. Preserve current note concealment before either output: no hidden/title/ARIA copy may retain expired authored note text. Update on render rather than the throttled general readout. Keep stable rows and skip unchanged text writes; live preferences cannot change focus or the active frame. Fit measured grapheme prefixes plus ellipsis at the existing font minimum, clamp actual ink vertically and horizontally, and omit an impossible caption while retaining its full permitted DOM label. Bound measurement caching and handle invalid scale without invalid canvas metrics. Keep strict ability schemas, coordinates, original assets and simulation byte-identical.

Example prompt: “Check long Motion labels in EN/UA, combining clusters and unbroken strings; prove exact-fit/no-fit and board-edge behavior without font compression. Exercise a real scan with short valid duration so the note expires before the next general status update. Confirm the legend conceals immediately, preserves rows/focus across reading changes, and clears when disabled. Use complete bounded helper/renderer/host tests, then native font/layout checks in the allocated lane. Do not infer animation playback from accepting GIF; consult Canvas image-source rules and keep native fixture evidence distinct.”

### Motion upload preparation ownership

PNG static-preview preparation reads bytes asynchronously before native Image load. Claim the generation before that read and fence each continuation before allocating a URL or mutating pending image/status. Clear, supersession and page departure invalidate pending reads; retain accepted art until replacement onload. Keep PNG container work bounded by 25 MiB / 16,384 chunks, preserve original File bytes, and do not describe encoded bounds as decoder-memory qualification.

Prompt: “Extend Motion upload handling without changing the APNG default identity contract. Run complete parser and real-application host files for byte-read rejection, supersession, Clear, cached/terminal departure and late decode. Preserve native source evidence separately; no fake Image event establishes browser codec conformance.”

### Motion slider names and current values

Keep each native range explicitly associated with a stable visible purpose label. Do not place an unrelated labelable output inside that label. Expose current units through `aria-valuetext` on the range and update it with the visible readout for input and preset/recipe changes; hide only the duplicate visual output from accessibility. Preserve native bounds, steps, keyboard handling, focus and preview intent. Use the actual-host regression and the [Motion slider guide](../../../authoring/motion-lab/README.md#stable-slider-names-and-current-values); native label activation and screen-reader/touch operation require separate checks.

Prompt: “Change each Motion slider and then replace the rotor recipe. Its purpose name must stay stable while current value text follows the actual setting. Preserve paused intent, focus and storage; do not infer assistive-technology qualification from modeled DOM assertions.”

For [Motion focus after rotation](../../../authoring/motion-lab/README.md#keep-the-current-control-visible-after-rotation), keep resize visibility within the existing page-owned display lifecycle. Reveal only the current eligible foreground control against the viewport and active settings clip; include its label/value field when it fits. Cover the retained native case where document scrolling leaves the entire settings panel outside the viewport: do not veto native nearest scrolling merely because that intersection is empty. Keep 8 px vertical scroll margin on the grouped target itself so its child's focus outline clears the scrollport. Retain native failures and earlier candidate pins; native Standard/Large vertical clearance does not certify horizontal clipping or physical rotation. Never assign focus, change a range value, scroll an already visible field, resume the preview or queue future focus on return. Preserve newer focus and hidden, inert, disabled, background and terminal-departure guards. Use the actual-host RED/GREEN and full display/restoration files, with native rotation and focus-ring checks recorded separately from modeled rectangles.

## Backup-owned content feedback

A complete-backup Import or Undo owns a journaled replacement, even when it temporarily shares the content-switch busy flag. Preserve cancellation generations, paused adoption and storage guards; announce a pending pack cancellation only for pack-owned work. A blur during held backup commit and successful adoption must not create a fictional newer-play-choice message on Home. Keep restoration feedback in Saves & loads and do not alter pager or profile ordering to fix this copy. See [backup feedback ownership](../../../docs/operation-focus.md#backup-feedback-ownership).

Regression example: exercise actual Collection → Records → Saves file import, explicit Undo and the real held backup lock plus blur; check both title/content statuses, exact saved-flight preservation and no autoplay. Retain the genuine delayed-chapter cancellation/late-response cases. Keep modeled transaction checks separate from native file chooser, focus and device acceptance.

## Exact presentation revision consumers

When a reviewed producer repair advances the compiled theme revision, inspect explicit runtime consumers as well as registry metadata. Team's closed picture table binds the complete pack, level, theme revision, collection and asset tuple; unchanged image bytes alone do not make a newer snapshot compatible. Revalidate each tuple against the actual compiled snapshot, then advance only its explicitly reviewed revision. Never wildcard, infer the current revision at runtime or edit historical release bindings. Run the complete binding and host tests against the real compiled metadata and originals, retaining the stale-binding failure and both older/newer snapshot refusals. Do not alter simulation, artwork, saved ownership or unrelated producer fingerprints to repair this compatibility boundary.

Prompt: “After a metadata-only theme revision, find exact revision consumers, reproduce their refusal against actual compiled metadata, and reconcile only reviewed identities. Prove both starter pictures still use their original full-frame assets and that unreviewed revisions fail before image reads. Preserve historical failures and distinguish source tests from native/public acceptance.”

## Team lobby picture consumer

Keep a Team lobby artwork preview passive and outside `coop-tools`, which moves into Pause and results. Borrow only the current prepared selection's exact image; never create another decoder, URL, read, core or animation loop. Clear retired pixels synchronously; stale async completions must neither paint nor clear a newer selection. Use the existing picture status and Cancel/Retry owner. Preview-only rendering failure must offer retry from the retained binding without disposing it or blocking valid Start. Keep preview recovery lobby-only, preserve explicit procedural registrations and exact starter rejection, and let the existing lease own resource release. Trace preview and arena canvases separately in tests; retain no arena draw before explicit Start and no Solo data writes. Native layout/pixel/input, offline and release evidence remain distinct. See [Team picture host](../../../docs/team-picture-host.md#lobby-artwork-preview).

## Team completed-picture parity

Follow [Team victory picture](../../../docs/team-victory-picture.md). A won Team arena may display its accepted full-frame picture while recorded coverage remains below 100%. Preserve the existing picture identity/lease checks before the terminal presentation branch, and restore canvas state even when drawing fails. Do not fill simulation cells, retire image ownership, create scores, or replace the accepted image to produce the reward. Running/paused/lost and missing/procedural-image behavior stay intact. Prove the branch with actual authored public-command wins and before-fix masking failures; distinguish finite drawing/lease tests from a real winning browser journey and the still-separate unobstructed result viewer/animation.

### Unobscured Team result viewer

Follow [Team result picture view](../../../docs/team-result-picture-view.md). Open only a won attempt's accepted image in a native dialog; keep Back outside the artwork, preserve full-frame containment and return focus to View picture. Borrow the accepted decoded image without another lease, award or restart. Keep dialog navigation ahead of results and retire held controller edges on both transitions. Test actual authored wins, repeated viewing, draw failure, foreground return, terminal cleanup, Retry and Change setup; model native dialog state without inventing host focus. Pointer activation is not physical-touch certification. Preserve the finished run and all original records.

### Preserve the earned Team picture

A lobby preview is not an earned reward. Keep the exact prepared image identity and full 2:1 mapping, but cover its centre with opaque #000 and expose only a narrow border teaser. State that winning reveals the full picture. Do not create a fake run, claim captured cells or change ownership/progress to render the teaser. Prove ready and retained Change setup stay concealed, with opaque source-over drawing; pair those checks with actual authored-win full-picture tests. Keep the earlier full-lobby candidate and its missing-mask discriminator as history, not product authority. Native pixel/layout evidence remains separate.

When composing the Team teaser onto shared Settings, retain `modelTeamDialogs()` in the finite host fixture and keep preview/arena recordings separate. Test a held original finishing behind the current Settings selector without focus or run adoption. Use each authored route in actual-host victory tests: `coverageClear` for First Connection, `yardOpening` for Relay Yard. The route defaults correspond to Full teamwork; do not substitute another play-style label or count simulated wins as native input qualification.

For [visible Team lobby return](../../../docs/team-victory-picture.md#visible-lobby-return-action), finish retained-teaser layout before handing focus to the current primary. Reveal only an offscreen action with nearest scrolling; preserve already visible placement. Keep the synchronous handoff bound to its lobby generation and Settings visit, with newer focus/input/lifecycle intent permanently vetoing its remaining work. Recheck after focus and layout callbacks and remove temporary listeners on every exit. Reuse actual-host confirmation and earned-win journeys; finite rectangles verify requested scrolling, not native viewport or hit-target results. This does not alter asynchronous picture recovery, start a run, reacquire artwork or award Solo progress.

For [Team picture recovery](../../../docs/team-picture-host.md#picture-recovery-focus-and-player-copy), keep direct pending Cancel and recovery Retry within the existing picture-focus owner. Capture selection identity, run/generation and Settings visit, preserve explicit Cancel's same-selection handoff, and recheck ownership after focus/layout before nearest reveal. Ignore only the exact intended self-focus; nested newer focus or lifecycle intent retires the owner even if focus later returns to BODY. Retain observers until the direct handoff ends and clean them on exceptions. Reveal failure cannot prevent the reader from starting. Keep the existing function-target success navigation unchanged and outside the expanded direct-handoff guarantee. Friendly player copy must retain original technical exceptions only in current-failure local diagnostics; stale/cancelled failure stays silent. Test actual host selection, recovery and delayed Settings completion without altering simulation or picture leases.

### Still Media reload-status ownership

For [Still Media read recovery](../../../docs/still-media-workshop.md#recovering-an-open-workshop-after-a-read-failure), notify the host only after a current panel load has verified the catalog, saved picture originals and optional story history. Recheck cancellation after the final await. Reconcile the failed opening's existing operation-status lease; never begin a fresh success notice that can overwrite newer host messages, a replacement opening or lifecycle retirement. Notification failure is a diagnostic after a verified read, not a failed save. Preserve the current reload draft policy, original bytes, media generation and navigation ownership. Keep the actual host/panel regressions for success, repeated failure, Close/hidden invalidation, newer messages, replacement openings and a throwing notification. Opt this shared load path into the existing `work(..., { opener: reload })` ownership adapter so initial/reopen and deliberate Reload capture before disabling and restore only after current final sync. Reproduce native disabled-control blur to BODY in the actual host/panel tests; an unchanged fake-DOM activeElement is not focus evidence. Keep unrelated programmatic focus, newer input, cancelled/stale loads and enabling reentry authoritative, and retire listeners immediately on abort. Do not add unconditional focus, timers or change disabled controls to a parallel ARIA implementation. An HTTP boot failure occurs before the panel exists and does not qualify this in-dialog path; use ordinary same-origin lock contention for the separate native check, without patching browser globals, stores or handlers. Retain original RED/GREEN receipts and report actual-browser/final-source/public gates separately.

### Still Media shared reading

For [Still Media reading preferences](../../../docs/still-media-workshop.md#shared-reading-preferences), reuse the existing independent `game/ui/tool-display-entry.mjs`. The cosmetic presentation loader does not adopt player reading preferences by itself. Apply the shared body attributes before opening media; do not add a second preference store, settings selector, database open or editor lifecycle owner. Keep selected original bytes, unsaved metadata, preview identity, saved generation and focus unchanged through preference/system-motion updates. Cached return refreshes reading authority without reopening the panel; terminal departure must retire subscriptions and deferred repair. Preserve the separate picker-clearance change when integrating. Run the actual HTML-selected entry with the real host/store, then separately verify computed Standard/Large sizes, Theme/Plain fonts and native portrait/landscape focus visibility. An attribute assertion or an unchanged 16 px control is not a native Large-text pass.

Prompt: “Open Still Media after selecting Large and Plain in the same-origin game. Confirm the shared attributes and actual font metrics before Open local media; preview an owned file, change shared reading settings, and verify the draft, file, preview, focus and stored generation are unchanged. Cover missed updates during cached-page departure and retained callbacks after terminal disposal. Keep original RED/GREEN and native evidence scoped to their exact sources.”

The retained combined preview is exact v0.61.8 plus the reading-entry HTML: Plain/Large/reduced settings applied before media access, the body measured 22 px, and the picker measured 20 px with its label and outline clear in portrait and short landscape. Keep that scoped browser result separate from final v0.61.9/public acceptance, a complete dialog traversal, actual background return and physical devices. Preserve the raw observation and request-log snapshot; inline-only screenshots have no exported image hashes. Do not equate the pre-existing authored picture record with a save performed by this preview.

When a workshop HTML entry gains a script, update the real build fixture's explicit HTML-entry dependencies as well as the shipped allowlist. `still-workshop-build.test.mjs` follows JavaScript imports recursively, but does not discover HTML script references. Include `game/ui/tool-display-entry.mjs` in that fixture graph, then retain the strict loose-file, ZIP, offline and Pages-route assertions. An ordinary build passing does not qualify a fixture whose declared graph is stale; preserve both hosted failures and the corrected Node 20/22 results.

### Bordered modal focus geometry

Follow [bordered scrollport focus clearance](../../../docs/still-media-workshop.md#bordered-scrollport-focus-clearance). Measure usable vertical bounds from the client scrollport, excluding borders and scrollbars, before accounting for sticky regions. Preserve the full focus outline and the label when it fits. Do not solve clipping by shrinking readable text or targets, changing focus ownership, or resetting unsaved data. Keep genuine zero-height viewports inert, retain finite-surface fallback and recheck existing lifecycle guards. Shared-helper changes require Missions/briefing and Replay coverage as well as Still Media.

Prompt: “Preview an owned image without saving. In Large/Plain, Standard/Theme and Large/Theme, Tab to the picker and rotate 390×844 → 844×390 → 390×844. Check the entire outline against the inner scrollport and the label against sticky status. Preserve filename, metadata and verified preview. Reach Close by keyboard, reopen with Return and close with Escape. Retain the original failed screenshots, computed-family facts and the corrected source identity; local preview and passing modeled geometry do not qualify a public release.”

### Installed Versus picture choice and readiness

Use the shared exact-owner release policy for embedded and external installed
maps; read [the installed picture contract](../../../docs/couch-installed-chapters.md).
Explicit still assignments retain their exact presentation revision. Embedded
imports must pass the approved authored-image baseline before release substitution.
External descriptor originals remain mandatory authenticated readiness inputs,
independently of whichever accepted picture both boards display. Never replace
that readiness proof with a valid alternate image, or write assignments/progress
from Couch.

External Retry keeps the captured immutable pin while obtaining fresh readiness
and branded metadata from its own store. A changed assignment cannot select new
pixels; a missing retained revision fails. Start instead validates the generation
of its already accepted binding. Keep staged Next separate from accepted Results,
confirm without publication, publish before explicit retirement, and release only
owned images/URLs once. Shared release-image acquisition checks exact revision,
bytes/hash, header and decoded dimensions; it does not decide ownership or write
media. Preserve optional-presentation notices and required-picture failure.

Run the complete installed external/embedded, existing installed and static
picture/host cohorts. Keep modeled IndexedDB, image decoding and host board inputs
separate from native rendering, physical input, offline, production asset quality
and full P08-A/public acceptance. Update the reproduction prompts and preserve
negative predecessors; no source-only green suite completes the release gate.
For no-mutation assertions, observe completed readwrite transactions as well as
puts; delete-only transactions must be detectable. Put deliberate fixture edits
before the consumption baseline and prove the observer sees those edits.

### Team ordered results

Follow [Team ordered results](../../../docs/team-ordered-results.md). Keep a completed attempt and its exact original while preparing a separate successor. Resolve authored order from the validated accepted pack snapshot and full accepted level; matching IDs or later source changes do not establish a replacement itinerary. Starter-namespace imports still require exact approved picture bindings. Other validated historical imports use the exact approved generic artwork policy and owned picture loading described in [Team import artwork](../../../docs/team-import-artwork.md); this does not add a custom-art envelope. One Next gesture starts only its current, foreground owner. Cancel on alternate action/focus, Settings, picture viewing, Back/Menu, mode departure, suspension and terminal disposal. Reentry during abort, status, paint, cleanup or storage must not overwrite a newer run/dialog or leak either image. Preserve accepted difficulty/teamwork and input assignments; refresh Help and setup for the successor. Only the literal built-in source owner may update built-in arena memory/storage, including when imported levels reuse its IDs. Final-arena position is not proof of completing the whole pack. Countdown and complete imported visual presentation remain separate qualification work. Keep composed test fixtures explicitly distinct from ordinary-source/build/native/public acceptance.

### Team picture preparation messages

Keep Team lobby and Next status text under host control. Reader/decoder diagnostics belong in logging, never in a player live region, including intermediate writes before a promise rejects. Map known preparation stages to bounded player copy; unknown stages remain Preparing. Only the lease's `status === 'ready'` is final readiness: a forwarded reader `stage === 'ready'` still reports preparation and cannot announce Start or Starting together. Let the owning catch publish failure/recovery copy once; lobby offers Retry picture or another arena, while Next retains results and offers Next again.

Log the original exception with a guarded logger, then revalidate operation/run/picture, generation, settings visit, active focus, abort and disposal before changing status or focus. Logging can reenter Cancel or replacement; the newer action must win. For rollback, capture the already-restored generation instead of comparing with the pre-adoption generation. Regressions must record every status write through actual read/decode and first-paint failures, verify retained result/artwork and successful explicit recovery, and cover logger reentry. A settled-message assertion alone misses transient diagnostic announcements.

### Keep Team Pause focus visible on rotation

On viewport resize, reveal only the currently focused actionable control within the visible paused Team overlay. Use its bordered client scrollport intersected with the viewport and the existing eight-pixel focus clearance. Recheck the same attempt, generation, modal visit, foreground and active element after layout/style reads. Do not refocus, queue a future reveal, resume, or change gameplay geometry. Guard reentry and remove the listener on disposal. Qualify Settings → Back → portrait/landscape with unchanged paused time, coverage and reserves; distinguish actual browser geometry from modeled host tests and physical-device acceptance.

For [initial presentation recovery](../../../docs/presentation-recovery.md), use the shared page lease's explicit `retry()` only to recover an unsuccessful initial load. Concurrent calls join one load; an accepted snapshot never refreshes. Team Retry picture must retain its exact arena and operation/focus ownership and must not Start automatically. Retire failed hosts before replacement, fence stale status/error callbacks, preserve pinned painters and independent leases, and distinguish cancelling picture adoption from cancelling a shared load. Reproduce first-load failure and same-page recovery through the actual host; retain native/public qualification separately. The guide includes the current maintenance prompt.

### Shared Team terrain and trails

Follow [Team presentation](../../../docs/team-actor-presentation.md#shared-terrain-and-active-cuts). Borrow `terrain.wall` and its prepared pivot from the accepted page snapshot once, before publishing any actor/look replacement. A malformed advertised image must fail without partial adoption; a missing legacy binding keeps the flat wall. Preserve the host's image lifetime, existing full-cell backing and nearest sampling. Do not reapply source frame offsets or invent wall cells.

Adapt the shared active-trail primitive with a local 1/16 transform, passing unchanged logical safe anchors, visited cell centres and true fractional player heads. Derive line widths from canvas CSS width and motion from active run time; retain both player accents, number/shape badges, contact cues and reduced-effects readability. Cosmetic body offsets must never move a cut. Explicit null snapshots retain historical drawing. Keep simulation, replay, input and HUD changes out of a presentation-only feature.

Prompt: "Qualify Team terrain/trails with real-core simultaneous cuts in both starter arenas at desktop and narrow widths; check prepared image identity/pivots, replacement and failed adoption, null compatibility, exact heads, both player identities, pause/reduced effects and unchanged outcomes. Then review actual artwork in the browser and run the release gates. Report command tests, native review, physical devices and public verification separately; do not label this slice complete P08-A coverage."

### Flight Details and bounded recent context

Follow [Solo flight information](../../../docs/flight-information.md). Use the accepted typed host source, not DOM captions or the R7 fixture reducer. Collect complete nonempty batches and accepted notices at their synchronous source; polling only the latest batch can lose explanations from earlier fixed steps. Keep current `lastWarning` authoritative, preserve full text and caption lifetime metadata, report bounded-history omissions and reset on each accepted generation.

Field details is an explicit Pause-menu action with the existing modal and Read/Done owner. It cannot resume on close, drop a buffered turn, activate a craft ability, open itself for unknown data or steal focus after replacement/backgrounding. Keep its open reading text stable. Preserve every existing field channel until an actual measured compact replacement proves full simultaneous threats/objectives/actions and accessible fallback coverage. Do not copy hide-all prototype CSS or silently set its qualification gates true.

Prompt: “Start an exposed cut in both turning modes, pause, open Field details with keyboard and controller, read to the end, leave the reader and return to Pause. Verify the exact saved checkpoint/queued turn and require explicit Resume. Repeat with a lane warning, mandatory objectives and automatic Arcade bonuses, then a Tactical craft. Resolve a delayed music failure while reading, replace the run, and return from browser history: retain full owned notices without restarting the reader, restoring stale focus or resuming. Record actual viewport/font/input observations separately from modeled DOM tests.”

Flight Details regression prompt: “Start and pause an exposed cut. Enter Field details and Read. Tab to adjacent Done without clearing Read; Shift+Tab back to the text; activate Done and return to Read. Read again, Tab through Done to Back, close and verify the identical paused checkpoint. Reopen after scrolling and verify the heading starts in view. Test Standard/Large at600×400 and portrait/landscape without reducing text size. Keep the native Read/text/Done/Back order, and omit only the zero-required-objective Details row.”

#### Details lifetime through backup completion

Keep the installed Field details presenter owned by the page, not by a backup transaction. Editing the shared declaration must not accidentally rewrite unrelated `backupBusy = false` assignments in finally blocks. Verify actual app-mounted successful import, Undo and failure, then persisted and terminal pagehide. A presentation reference reset can leave click handlers alive while breaking lifecycle cleanup; testing only whether the dialog opens is insufficient.

### Approved artwork for historical Team imports

Follow [Team import artwork](../../../docs/team-import-artwork.md). Read the exact theme revision and null collection from the selected source’s `COOP_HISTORICAL_IMPORT_PICTURE_POLICY`, and require its theme identity to match the actual compiled snapshot. Do not copy a revision number from historical guidance or infer an accepted revision from runtime “latest.” When regeneration advances the selected theme, qualify explicit successor starter bindings and import policy while preserving every picture descriptor and historical record. Preserve the existing closed gameplay schemas. Use the approved `scene.reveal.wide.field-kit` revision 2 image, 1152×576 PNG, 52,720 bytes, SHA-256 `53f1206a11a8791892f5c844c0641529acbc2c09c8d558676d0d801d72113850`; this is a generic scenery association, not approval of imported level design. Keep registered namespaces, especially `relay-rescue-starter`, under exact full-pack/level binding checks with no generic fallback. Preserve typed historical string/numeric level revisions and exact theme/collection/asset identities.

Stage replacement pack and picture together while retaining the previous selection and image. Stop waiting abandons adoption; Retry pack keeps the draft. Fence late reads/decodes, cancellation, focus, reentry and disposal. Recovery never starts play. Retry retains accepted visuals and gameplay recipe; Next prepares a separate successor from the retained authored pack. Only a literal built-in source owner updates the built-in arena preference. Artwork cannot define collision or objectives. Custom artwork uses the separate versioned `.rlteam` presentation envelope, never extra historical fields.

For production I/O and WebCrypto tests, use the existing monotonic `game/test/helpers/wait-for.mjs` predicate helper; a fixed count of immediate callbacks can expire before real work finishes. Observe pending rejections before awaiting readiness, then assert the expected rejection after the deliberate cancellation or replacement. Historical imports now prepare approved artwork rather than procedural previews. A failed replacement reports a separate pack error while preserving the accepted scene, arena and playable Start; update stale fixture expectations without weakening exact identity, ownership or recovery assertions.

Prompt: “Use the Team import artwork guide's exact descriptor and maintenance journey. Qualify coverage and multiple-stronghold imports with string/numeric revisions; changed starter IDs/content; Stop waiting, failed replacement, Retry pack and late decode; then earned Next → conflicting inactive setup drafts → Retry → another win. Check retained image ownership and unchanged simulation/solo storage. Separate modeled tests, native visuals, physical inputs, offline and public evidence. Reusing existing asset bytes does not complete those gates or all of P08-A.”

### Bounded custom Team artwork sources

The separate `.rlteam` reader and in-memory staging adapter are described in [the custom Team artwork contract](../../../docs/team-custom-artwork-envelope.md). They preserve historical pack/level formats and original file bytes. Use `createCoopPresentationImport` to keep one accepted source and one candidate, and commit only after the host verifies exact registered content identity, theme compatibility and the new drawable picture. Uploaded provenance never grants production review. The Team host connects these modules to its existing pack picker; identify `.rlteam` or its registered MIME, validate the complete container, and label accepted content Local artwork. Historical JSON keeps its own reader.

Cancellation retires adoption but the importer stays busy while its bounded image decoder settles. Do not queue more body readers or pass an early-rejecting cancellation signal into qualification decoding. Fence every callback, release acquired image handles once, and preserve accepted content after failed replacement. Original exports and image reads require the exact live owner, not a copied receipt. Keep string/numeric level revisions and historical level IDs unchanged.

Prompt: “Extend the Team artwork adapter through the actual host. Carry one immutable source receipt through staged import, Retry and Next; preserve old results on failure/cancel; enforce exact reserved namespace/theme compatibility. Exercise malformed containers, changed bytes, reentrant callbacks, late decode, original export and disposal. Run native import/play/capture/Retry/Next and offline reimport separately from modeled codec tests before release.”

### Atomic custom artwork adoption in Team

Keep the selected pack, opaque source and prepared picture in one host transaction. Preserve the previous tuple until tentative rendering and exact compatibility checks pass. Guard Start during adoption; capture proposed pack/source references before callbacks, because cancellation clears the draft. Commit the source and relinquish the import operation without intervening callbacks. After commit, never roll back to a retired source; fence UI/focus completion against newer imports, Reset and Start. Reset retires the exact previously accepted owner, not a tentative candidate. Deferred picture/import work must recheck the latest selection.

Retain the owner through Retry, Next, Settings and lobby return. Cancellation during native validation keeps the importer busy until it settles; disable new intake and describe that wait. Retry uses a fresh owner. Release orphan tentative drawables after cancelled adoption, and release all source/drawable ownership on terminal page disposal. Keep complete original byte export at the source API; do not claim a player export control exists.

Prompt: “Import an original `.rlteam` bundle through the actual Team picker, win its first arena, select Next, Retry, and return through Settings/lobby. Reimport identical bytes deliberately. Replace accepted local A with B; cancel, fail decoding, reset to starters during tentative setup, and start a newer import during completion. Confirm exact picture retention, no stale focus/adoption, usable prior content and every temporary image released once. Record modeled File/Blob and simulation tests separately from actual browser decode, touch/controller, memory, offline reimport and deployed verification.”

### Narrow Solo active-flight counter clearance

Follow [the scoped HUD layout contract](../../../docs/device-controls.md#narrow-solo-flight-counters--p05-hud-correction). The 521–680px, at-most-600px-high running-landscape correction uses two stat columns and a text-token-derived stable header reserve. Preserve selected fonts, complete numeric values, target/caption ownership, 44px actions, conditional controls and full-board aspect fitting. Keep training, portrait and Couch outside this rule; no event-dependent geometry or hide-all prototype substitution. A larger header reduces the playing arena and needs actual play-scale review.

Retain exact original baseline and native candidate observations. Check actual DOM text/control/board rectangles with Standard/Large, Theme/Plain, shown/hidden D-pad and the deliberate floating-stick overlay; preserve both steering hands and explicit Pause/Resume. A zero-score candidate preview does not certify long or fractional scores, and a resized desktop is not a physical device. Do not add tests that merely repeat CSS declarations or treat fake rectangles as native layout evidence. Run applicable existing contracts when their behavior changes, then qualify the exact final integrated source and affected public journeys. Use the [maintenance prompt](../../prompts/cross-mode-delivery.md#qualify-narrow-solo-active-flight-counters); keep P03/P05 and broader zoom/input/play-comfort gates open.

### Embedded practice belongs to its launching guide

Follow [embedded practice navigation](../../../docs/practice-owner-navigation.md). A registered Field Guide or enemy Workshop practice frame must reuse its validated, token-bound Return action for the logo and offer one explicit owner exit instead of campaign Home. Its Missions header opens the current lesson brief only. Preserve reward isolation, exact parent window/origin/session validation, neutralized inputs and once-only disposal; do not invent a generic parent-navigation message. Opt in only when the actual validated return action exists, preserving standalone Playground, Controller practice and First Flight contracts. Qualify keyboard/controller/visible-action return from ready and paused cuts, unchanged saves and lesson checkpoints, stale repeated activation, and ordinary-menu regressions before native review.

### Team arena discovery and retained attempts

Follow [Team discovery](../../../docs/team-discovery.md) when changing the Team catalogue. Cards expose the two approved starter arenas and the exact local pack opened for this visit. Solo/Versus formats are incompatible; do not advertise them as playable or installed Team campaigns. Browsing and previewing do not award pictures or mutate Solo saves. A Play action prepares the target core and exact artwork before replacing the current attempt; paused unfinished play requires Stay/Replace. Keep old results, setup, picture and imported artwork ownership usable on cancellation, foreground loss, decoder failure and stale completion. Switching to starters must preserve the opened local pack for switching back without another file picker. New imports and explicit reset retain their existing ownership rules. Do not turn discovery metadata into simulation fields or persistent Team saves.

Prompt: “Exercise Team arenas from setup, pause and results using keyboard, touch and an assigned controller. Cancel delayed preparation before decoding finishes; choose Stay, retry and Replace; lose foreground; switch local pack → starter → local pack; win, Next and Retry with the original artwork. Verify one accepted start, no stale status/focus/award, readable loading and 44px controls at portrait and short landscape with Large/Plain text. Report finite host tests separately from native browser, physical devices, offline and deployed release evidence. Team downloading, campaign hierarchy and complete P06 acceptance remain separate until reviewed compatible content and transport are implemented.”

### Chapters busy-focus ownership

When a focused Play, Download & play, Install or Refresh action becomes disabled, move its keyboard/controller focus to the visible Cancel operation action in the same foreground dialog. Preserve the exact opener for cancellation. Do not claim focus if the player moved it, opened another dialog, hid or blurred the document, or if a newer operation replaced this one. Keep late completion and cancellation generation guards. Retain native focus-loss observations; verify visible busy Cancel focus and exact opener restoration in both Solo and Versus with keyboard, then qualify physical controls separately.

### Native modal Escape remains owned by its dialog

Flight key adapters must yield Escape from an open native dialog before calling `preventDefault()` or pausing. The dialog's cancellable, non-bubbling `cancel` event owns the keyboard close decision; busy operations may refuse closure. Preserve modifier/default-prevented events, gameplay Escape outside dialogs and the existing controller router's guarded Back operation. Return focus through the owning dialog's established lifecycle, without auto-resuming play. Do not synthesize an Escape event for controller Back or add per-dialog propagation patches when the shared flight adapter is the cause. See [MDN cancel behavior](https://developer.mozilla.org/en-US/docs/Web/API/HTMLDialogElement/cancel_event) and [WAI modal dialog navigation](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/).

Prompt: “Compose the real couch input, menu navigation and controller router. From a settled Chapters dialog, press Escape and verify native cancel is available, the dialog closes and focus returns to its opener. During unsettled work, verify its cancel guard retains the dialog and actionable focus. Repeat with controller Back, a held Back, modifiers and an already-handled key. Verify gameplay Escape still pauses both players outside dialogs. Run complete affected adapter tests on Node 20 and 22; record actual native keyboard/touch/controller observations separately from modeled cancellation.”

### Keep shared panel focus through host activation

When a shared chapter panel transfers focus from disabled Play to its visible Cancel action, the Couch launch-capture hook must preserve that exact connected, enabled control inside the current busy dialog. The touch fallback may focus the dialog only when this owned Cancel or the original opener does not already hold focus. Keep foreground, attempt, source and post-focus lease checks; do not preserve arbitrary controls or bypass a newer Back choice. Solo capture does not focus a dialog and needs no matching workaround.

Prompt: “Exercise the real panel plus Couch host activation rather than a presentation-only stub. Hold chapter transport and then installed-picture decoding. Confirm Cancel keeps focus immediately after Play and throughout each wait; cancel from paused and finished races, preserve both exact boards/results, and return to the Play opener. Repeat touch Play from Back, moved/nested/background focus, Stay/Replace and actual controller Back. Run the complete host regression and Solo/Couch shared-panel file on Node 20 and 22. Record native focus separately; a late accessibility snapshot alone cannot establish the initial transfer.”

A nested Stay can return browser focus to Cancel. Include only that same owned Cancel in the established `onCancelled` return set, preserving all operation-generation, launch-epoch and foreground checks. An old Stay or late completion must not release newer busy controls or reclaim their Cancel focus.

## Team artwork discovery previews (P06 successor)

- Team cards use bounded artwork teasers and an explicit enlarged, concealed Preview within the same gallery dialog. The full picture is the win reward; ordinary discovery must not expose its centre. Preview is not an earned picture, completion, bookmark, installation or simulation start. Keep Play a single deliberate action and preserve the old attempt/result until its existing preparation gate succeeds.
- Resolve the exact row pack, level, theme and authenticated local artwork through a dedicated preview lease. Never borrow or dispose the accepted attempt's picture or the opened pack's opaque source. Copy pixels into 288×144 teasers or one 1152×576 expanded teaser, mask the centre with proportional 24/96-pixel borders, then release each temporary decoded image. Keep one actual preparation permit until the underlying work settles, across cancellation and reopened visits.
- Back from expanded Preview restores the exact Preview opener; the next Back closes the gallery. Cancellation, foreground loss, disposal and newer visits fence late reads and draws. Keep Retry focused while preparing, expose progress without fabricated percentages, and retain newer focus choices. Recheck visit ownership after abort callbacks; an old close cannot clear a new dialog.
- Qualify scheduler/resource lifetimes and real-host authenticated artwork separately. Preserve legal win→Preview→Back→Next/Retry outcomes and record attempted storage mutations. Native layout, physical controllers, public bytes and offline play remain separate release evidence.
- Prompt example: “Extend Reveal Line’s Team gallery preview from its reviewed release. Preserve exact content/artwork identity, optional concealed player Preview, unchanged full creator/earned views, one actual preparation permit, copied-pixel limits, cancellation ownership, focus and accepted originals. Exercise read/decode/paint failure, Retry, abort reentry, local `.rlteam` identity and legal earned-result continuation. Update the guide and prompts, then qualify the exact integrated source and deployed journey.”

### Team preview viewport allocation

Follow the expanded-preview responsive layout in [Team discovery](../../../docs/team-discovery.md). Fit the whole 2:1 artwork into the space remaining after Back and readable text. Landscape uses a bounded viewport; portrait uses natural picture height to avoid excessive empty letterboxing. Keep gallery-only copy out of the picture view, retain visible failure/recovery messages and accessible successful status announcements, and preserve exact artwork and focus ownership.

Prompt: “Review the concealed Team picture preview at 844×390, 600×400, 390×844 and desktop with Large/Plain text and meaningful 200% zoom. Verify the complete preview frame, opaque centre, caption and at-least-44px Back action together; rotate while keeping the exact picture and focus. Fail decoding, Retry, return to the precise card and close to the prior opener. Capture settled native screenshots and geometry; do not call CSS declarations or fabricated rectangles visual proof. Keep original artwork, simulation and release evidence unchanged.”

### Team guidance follows actual arena content

Use [Team briefing guidance](../../../docs/team-briefing.md) for setup, launch and paused Help copy. Derive Hunters, Drifters and relay emitters from the exact selected level; derive launch and paused Help tips from the accepted run. Coverage goals can include relays and still need their warnings and instructions. Keep gameplay policies, content identities and simulation unchanged. Do not describe absent attacks or imply a slowed actor is harmless.

Prompt: “Import an enemy-free territory arena, a Drifter-only arena, an emitter-only arena and a multiple-core arena. Check setup, accepted Start, paused Help, Retry and Next for truthful threat/Support/goal text under joint and independent cuts. Enter Help reading, return through Escape and confirm visible opener focus without resuming. Switch back to starters and verify restored guidance. Keep old attempts and artwork on cancelled preparation, and inspect expanded briefings at Large/Plain text and narrow viewports. Run actual-host and public/offline checks separately from pure-copy cases.”

### Journey and optional chapter discovery composition

Journey stays opt-in. Resolve a selected mission through its exact authored pack before projecting difficulty; identical simulation keys do not establish identical artwork ownership. Preserve both world-download and result-preparation cancellation owners. A newer route, Cancel, hidden page or return to menus must not allow an old ticket to adopt a flight, clear newer controls or award progress. Registered practice-return containment precedes optional Journey Missions dispatch.

Prompt: “Compose Journey direct Next with optional chapter discovery. Test a cold campaign boundary, unavailable download followed by Retry, cancellation before and after picture decoding, a newer mission choice, exact pack/artwork identity and Practice briefing return. Decode the authentic embedded picture header in finite Image doubles, modeling both width/height and naturalWidth/naturalHeight; never weaken production image validation to satisfy a fixture. Run complete affected host and practice files on Node 20 and 22, preserve initial failures, then qualify the actual merged/versioned source and public player journeys separately.”

Keep Solo Settings Close and tabs outside the active panel scroll region. Restore only a visible, enabled opener; if responsive layout hides the toolbar opener, use the host-provided Game menu fallback without resuming flight. Preserve newer focus and nested-dialog ownership, including reentrant fallback callbacks. Replace a stale resumed caption on Pause while retaining interruption warnings. Follow the [responsive Settings contract](../../../docs/controller-navigation.md#responsive-settings-and-accurate-pause-status); verify short landscape, portrait, Plain/Large text, saved-cut checkpoints and exact public-source behavior separately.

## Journey action policy and historical route checks

New Solo/Versus Journey projects pin `journey-arcade-v2`. The shared compiler emits
`classic.arcadeActions` so the fixed-tick simulation, imported replay commands and
visible controls agree: bonuses activate on contact, while manual ability, Supply
and Boost commands are unavailable. Do not fix this by hiding buttons alone.
Imported `journey-v1` projects keep their exact previous levels and identities.
Team uses its separate registered rules; do not infer Solo capabilities from a label.

Prompt: “Create a Journey Arcade map through the shared compiler. Check both turning
modes and all existing classes against attempted manual commands; collect the four
authored bonuses through movement and verify the replay. Preserve v1 route fixtures,
then run `node scripts/qualify-journey-arcade-routes.mjs` to compare all sixty historical
opening outcomes. Changing policy requires new simulation/campaign identities, never
rewriting old checkpoints. Restore a saved candidate after changing the next-attempt
preset; resolve it through candidate ownership, not an installed Legacy pack lookup.”

### Explicit Journey race timing and old-edition recovery

Untimed authored Versus uses `xonix-duel.untimed.v1` with explicit zero seconds and
a null match deadline. Never silently reinterpret legacy timed-v1 or infer protocol
from a missing duration. Hide the legacy timer option for these authored races;
summary, HUD and Help must agree. Both-ended ranking and first-clear adjudication
remain deterministic. Unavailable old candidate saves must keep exact original
bytes and offer export/original-edition guidance, never a false pack-install fix.

Prompt: “Run an idle authored race beyond 90 seconds without awarding a clear,
then verify first clear, simultaneous clears, Pause/Resume and both-ended ranking.
Reject mismatched protocol packets; keep legacy timed replay outcomes. Load an old
v1 saved flight and verify unchanged bytes plus truthful recovery copy. Preserve the
old reference crosswalk and prove its identities before renewing new-policy pins.”

### Responsive recovery dialogs

Use the shared focus-clearance owner for scrollable player dialogs. Supply a sticky heading only when the dialog actually has one, dispose listeners with the owning screen, and retain the selected action through reflow without activating it. Verify portrait to short-landscape resizing with keyboard focus on Cancel/Stay/Play, including pending and completed media operations. A passing layout-model test does not replace observing the final integrated browser view. Preserve original failed observations and sparse-input failures separately from product regressions.

### Independent presentation observer ownership

When adding a resize observer to a menu, audit lifecycle fixtures that select the
last observer by creation order. Identify the board observer through its actual
arena targets before disconnection, require unique ownership, and retain cached-
page disconnect, stale-callback, no-background-simulation and zero-RAF assertions.
Also verify terminal disposal of independent menu observers and that queued focus
reflow cannot scroll or activate a hidden page. Restore missing sparse inputs from
the exact selected Git revision; keep setup failures distinct from product failures.

### Replay artwork status

When changing Replay Theater artwork feedback, keep `#asset-status` mounted as a polite, atomic live region, including while empty. Collapse empty spacing without removing it from the accessibility tree. Check failed-image and successful-theme updates without changing focus, transport intent or recorded checkpoints. Distinguish inspected browser semantics from an actual screen-reader listening check. See `docs/replay-theater.md` for the verification prompt.

### Consumed story-restore controls

When a one-use reviewed action becomes disabled, keep its initiating focus owner
through every adapter wrapper and restore only to a logical enabled successor
after the parent operation re-enables controls. Reuse `captureOperationFocus`;
never blindly focus from an async completion. Cover cancellation, notification
failure, explicit focus movement and native disabled-button blur. Example:
"Restore an owned story backup with keyboard-only input, verify Review chosen
story backup receives focus after success, and prove late completion cannot
reclaim focus after Back or another action."

### Pending replacement focus

When a confirmation disables its focused button while awaiting a picture or chapter, move focus to the still-enabled Stay/Cancel action before disabling the opener. Do so only when that opener still owns focus in a visible, focused document. Preserve any newer user focus. Model native disabled-button blur in host regressions; retaining focus on a disabled fake button is not browser evidence. Keep delayed pending-state observation separate from a cancellation input that arrives after completion.

Example: “Verify Replace & play with a bounded local download delay. Keep Stay focused during preparation, retain the current flight on cancellation, and prove background/hidden or moved-focus cases do not reclaim focus. Preserve failed observations and distinguish local preview from deployed acceptance.”

### Compact Pause acceptance

Verify focused Pause actions at568×320,844×390 and390×844 with Plain/Large text. Record actual viewport and document dimensions after resizing settles; an immediate scaled screenshot alone is not layout evidence. Tab through visible actions and verify the complete focus outline after internal scroll. Reset viewport overrides afterward. Keep keyboard, mouse inspection, screen-reader listening and physical device results distinct. A mounted empty polite status is not proof that a missing-picture announcement was heard. Preserve the original public receipt and scope when updating the execution register.

For Asset Studio navigation, preserve the fixed same-edition Workshop return and
bounded Journey route in `game/ui/asset-studio-return.mjs`. The UI hint opens a
menu only; do not broaden historical mode-return readers or turn it into save or
selection adoption. Restore after boot only while the existing host owns focus,
and keep pause callbacks from covering newer dialogs/focus. Return to game remains
separate. Follow the native and modeled maintenance prompt in
[Studio return navigation](../../../docs/asset-studio.md#returning-to-workshop),
including unchanged checkpoints/save bytes and explicit Back to the Workshop
opener. This Solo tool route does not certify Couch departure or restore behavior.

For the shared standalone Workshop return, use the finite tool map in
`game/ui/workshop-return.mjs` and the maintenance journey in
[Standalone Workshop navigation](../../../docs/field-kit-surfaces.md#standalone-workshop-return-navigation).
Keep the existing practice token/session protocols, Replay explicit departure and
Video Poster cancel-first Back unchanged. No route hint may adopt saves or implicit
mission selection. Include early loading, all exact tool openers and native nested
Escape in the next release's evidence; partial Studio coverage is not nine-tool acceptance.

For Playground keyboard exits, use the named same-origin frame's nonmodal native
Tab boundary and the current host's input suspension. Do not repurpose controller
session or enemy draft-return transport. Test exit/re-entry in both directions,
explicit Resume, frame retirement, modal containment and in-frame mode links;
see `docs/field-kit-surfaces.md` for the maintenance prompt and evidence limits.

### Content Studio preview and return

Content Studio is a descendant of Playground, not a second registry entry for its
Workshop opener. Use the fixed same-edition descendant links and bounded Journey
hint; never forward draft IDs, practice tokens or caller return URLs. Initialize
the destination before enabling the link. Add embedded keyboard exits only through
the finite named same-origin frame owners. Test both Tab boundaries, paused re-entry,
Return to draft focus and early Close without losing the draft. A slow boot warning
must keep observing the owned exact preview URL; close, replacement and pagehide
retire the observer so late readiness cannot reclaim focus. When integrating an
independent fix, preserve the receiving branch's campaign/editor content and test
the actual composed source. Owner-branch evidence alone does not qualify it.

For explicit authored Solo/Versus entry, follow [Authored Journey mode entry](../../../docs/authored-mode-entry.md). Preserve the finite opening/authored library route and receiving mode’s independent cursor, not a converted run or transferred award. Use checked Solo retention in its exact authored save slot, the existing Versus/Team Stay-discard decisions, and explicit Continue/Resume. Do not send authored selections through Legacy mode-return tokens. Label Team as separate arenas; its bounded journey-return hint changes only fixed navigation. Keep the small classic loading-state Back initializer consistent with ready-host destinations, including labels; never let a delayed mode import silently return to Legacy. Prompt: “Verify ready and unfinished mode entry, held/failed save cancellation, exact picture/checkpoint restoration, unchanged Legacy bytes, both Team origins and ambiguous hints; report modeled, native and public evidence separately.”

For Journey title entry, separate the first-mission launch fallback from proof of prior Solo progress. Fresh opening/authored libraries offer Start; only a running flight, accepted saved flight or known Solo cursor offers Continue. Preserve unknown stored identities and independent Versus progress. See [Journey primary entry](../../../docs/journey-primary-entry.md). Maintenance prompt: "Test visible Start and Continue controls for fresh, selected, completed, unknown-cursor and denied-storage profiles; activate the displayed control, check its named destination and confirm no progress is granted merely by rendering the title. Preserve saved-flight and memory-flight behavior."

For the host-provided Journey chooser, retain Home as the parent while browsing; only explicit mission selection closes it before staged preparation. Back returns to the original Missions control, with a truthful menu/game label and explicit Resume. Follow [Journey menu returns](../../../docs/journey-menu-return.md). Maintenance prompt: "Exercise Home and paused-field Missions, native cancel and button Back, modeled controller Back, nested progress backup, retained search, and mission selection. Assert exact paused checkpoint and independent progress; do not replace native search Escape-clearing semantics or infer physical-device acceptance from modeled input."

For Journey Continue captions, use the same retained-cursor/successor resolver as the actual title action. Saved-flight titles retain priority; unfinished in-memory flights retain their own mission name. See [Journey continuation captions](../../../docs/journey-continue-caption.md). Maintenance prompt: "Seed selected and completed Journey cursors, assert the visible Continue destination, activate Continue and verify that exact mission starts. Include a campaign boundary and paused/saved-flight precedence. Do not change progression or award behavior to make a caption test pass."

### Workshop recovery before modules are available

A standalone read-only tool must expose a static, same-build Game home exit even if every script fails to load. Keep contextual Workshop returns gated until their validated route is installed; the independent entry can upgrade the static Game home href with a validated Journey hint without activating it. Do not hide or replace the focused static exit on readiness. In navigation host tests with nested dialogs, model native `beforetoggle` and initial autofocus so the real modal owner receives opening order; do not relax production focus ownership to compensate for a missing DOM boundary.

Verification prompt: “Serve Replay Theater with scripts deliberately unavailable. Use only Tab and Enter to reach its same-build Game home. Restore normal modules and verify both contextual Workshop and Game home URLs retain the validated Journey hint. Separately run Home → Workshop → Back → Missions → Flight setup → Back with native opening events modeled, asserting exact checkpoint and saved bytes. Distinguish native keyboard recovery from modeled controller and physical-device acceptance.”

For every registered standalone Workshop tool, keep the static same-edition game exit available before any JavaScript runs. The contextual Workshop return can stay gated until its independent route module validates hints. Never gate both exits on that module or weaken focus assertions to hide the gap. Include actual markup checks for all registered tools and a failed-script native keyboard journey. Miniature build fixtures must copy newly HTML-selected entry modules and styles plus their exact transitive imports; keep production reference validation strict. Requalify the final source after correcting full-shard failures, and preserve each original failed family separately from passing focused cohorts.

### Complete visual-theme identity boundary

Use `game/presentation/visual-theme-catalogue.mjs` only for declared compatibility and exact compiler/theme/collection identities. Supply accepted authored owners and canonical level hashes before difficulty transforms; keep numeric and legacy string revisions exact. A compatible declaration is not art approval or byte validation. Compare loader-verified declarations against authoritative required slots, preserve historical catalogue entries, and keep unsupported content distinct from unavailable revisions. Do not expose Settings choices or change retained picture/story pins until the complete two-collection benchmark and staged-host integration pass. Follow the prompt in `docs/visual-theme-catalogue.md`.

For two-seat narrow portrait layouts, fit preferred touch sizes to each seat's
usable width instead of stacking both pads over the board. Verify at least 44px
actual D-pad targets, preserve stored size and independent pointers, and keep
Solo/couch gesture vocabulary identical. Catalogue-bound arrows must not render
their text fallback twice. Inspect long captions between Large landscape pads;
a fitting canvas alone does not establish unobscured action feedback. Extend
controller checks through Missions/Deploy/Pause/Resume without pointer clicks,
while keeping modeled and physical Steam Deck evidence separate.

### Team actor-state authoring

Use `game/presentation/team-actor-slots.mjs` for the optional 32 Team body contracts. Preparation appends immutable source-stage bodies using exact existing image bytes, pivots and rotor anchors; it does not approve artwork or write player data. Separate global slot registration from bindings in the selected theme. Preserve numbered shape markers, runtime warnings and original simulation timing. Reject partial/conflicting imported contracts, preserve selections across undo/reset, and decode every declared Team state before atomically adopting a painter snapshot. A state preview must say when the selected state or compact/detailed treatment is inactive. If any Team override is selected for publication, require the full reviewed family in the readiness gate. Follow [Team actor-state authoring](../../../docs/team-actor-state-authoring.md) and its maintenance prompt. Do not claim this body-authoring slice completes procedural objectives, five-edition artwork or physical-device qualification.

### Team semantic cue contrast

Keep Team cue colours in `coop-cue-palette.mjs`, derived from the accepted presentation. Preserve null-snapshot legacy drawing. Text uses opaque paper backing with unrounded 4.5:1 contrast; neutral fallback does not authorize a replacement theme or picture. Functional paths over artwork/concealment require opaque contrasting inner/outer strokes, with widths derived from CSS cell size. Paint the actual-radius player head centre after its halo so outlines cannot erase it on phones. Keep existing shared three-layer trails. Check Support, recovery, slowed, spawn, core and warning paths over black, white and gray; compare exact run state and restore canvas alpha/width. See [Team cue palette](../../../docs/team-cue-palette.md) for the reproduction prompt and evidence limits.

### Team relay-anchor authoring

Treat `team.anchor.available` and `team.anchor.captured` as a complete optional contract pair. Explicit Studio preparation adds source-stage recipes without changing historical selections; selected pairs require review evidence before production. Imports accept only exact known contracts. Prepare all declared images atomically even in First Connection, which has no anchors. Cosmetic artwork gets a 24 CSS-pixel minimum and pivot-aware arena containment; preserve the original capture border. Draw all anchor art before functional labels and reserve its rectangles during label placement. Verify actual uploaded pixels remain visible on phones, not merely `drawImage` calls. See [Team anchor authoring](../../../docs/team-anchor-authoring.md) and its native, import/Undo and release requirements.

For [Team Studio state inspection](../../../docs/team-studio-states.md), keep scene-menu availability sourced from `teamPreviewScenarios`, the same immutable tick table used by the public-command fixture. Support, slowed enemies and Hunter recovery apply to both starter arenas; emitter warning and travelling spark apply to Relay Yard. Derive the spark from a legal warned cut and ordinary release commands, never by adding a projectile or altering the authored level. Preserve held-state, bounded motion-loop, reset and disposal behavior. Use the [Team state prompt](../../prompts/team-studio-states.md). Keep procedural state preview, editable asset-slot coverage, native visual evidence and full publication acceptance distinct.

For Studio preview controls, preserve a wrapping flex basis when host text styles reset `min-inline-size`. A physical `min-width` can be overridden by that logical property; verify actual portrait geometry rather than relying on the declaration. Keep the Geometry overlay label and all selectors at least 44 pixels high, and qualify Large/Plain text with retained keyboard focus.

For [Team feedback authoring](../../../docs/team-feedback-authoring.md), preserve exact four-slot contracts and source-stage recipe defaults. Support is a pulse, Slowed belongs to an affected enemy, Rescue belongs to the active helper, and Recovery belongs to the revived player, never the hunter. Derive badges from authoritative active time and keep existing functional cues. Prepare declared images atomically; reserve actor/core/anchor bounds and suppress cosmetic badges when nearby space is unavailable. Stronghold positions are nested under `core`, unlike players/enemies. Qualify actual started simulations with valid direction/boost/support commands and assert time advances. Follow the [feedback prompt](../../prompts/team-feedback.md), distinguish earned scenes from synthetic layout stress, and keep missing recovery causes explicit.

For [Team threat overlays](../../../docs/team-threat-authoring.md), preserve the separate exact three-slot family and historical feedback contracts. Derive warning/shield/impact state from the simulation only. Sparks use exact finite sub-cell coordinates; pair legacy cell fallbacks only when both axes are absent, with valid cell/owner/player/trail checks. Clip edge art without relocating it. Paint shields beneath core bodies and warning/spark accents above decorative bodies but below functional cues and labels; require actual visible uploaded pixels rather than draw-call counts. Test multi-core ownership, cleared impacts and a started warning-to-spark trace. Use the [threat prompt](../../prompts/team-threat-overlays.md); registration/readiness/native/public evidence remain distinct.

For the Team reserve-recovery Studio specimen, retain the command-earned Relay Yard tick484 state (Standard / seed17), two reserves and two revived craft at authored spawns. It is not a contact rescue or independent reserve revival. Hold exact state when paused/reduced, advance real released-input ticks to expire grace, and reset only the private specimen copy. Do not replay rewards/audio from retained last-tick events. Any future live celebration must consume `run.events` after every simulation step, since multi-step frames overwrite earlier events. First Connection must not offer a fabricated equivalent.

### Team event receipts and artwork

Keep Team `cut.joint` and `team.recovery` presentation in the host-owned `coop-event-feedback.mjs` latch. Ingest after **every** completed step, not once per rendered frame. Never add receipts to simulation/checkpoint data or infer them from grace, counters or individual revival. Retry/new-run identity resets the latch; its 1.25-second lifetime uses simulation time. Captions own live status: newer messages hide obsolete artwork and expiry leaves the text available. Reduced effects disables the decorative entrance, not event meaning.

`team.event.joint-capture` and `team.event.team-recovery` are a separate exact two-slot family (`team.event.v1`), both centered 32×32 transparent status icons. Prepare/decode all declared revisions atomically with the accepted snapshot. Keep icons outside playable cells, cosmetic and silent; do not imply medals or new reserves. Field context uses actual per-step events from both Joint capture scenes and Relay Yard Team reserve recovery. Source recipes and complete bindings are not art approval. Follow `docs/team-event-authoring.md` and `authoring/prompts/team-event-artwork.md`; qualify real live-host events, stale assets, short landscape, history and byte-preserving round trips.

### Authentic paused crawling specimens

Team crawling is derived from two same-attempt, same-status observations with genuine displacement. Studio's Relay Yard crawling scenes use actual tick494→495 samples earned by public commands. `fixture.prime(observer)` is one-shot at construction/reset; observe a temporary predecessor, run one recorded core step, observe the result, and only then adopt it. Preserve the selected clock, trace cursor and existing scene on failure. Never fake velocity or make the live renderer trust an authoring state flag. Prime before advancing a new scene and after its bounded loop resets; do not send predecessor events to the presentation-event latch. Qualify both players, compact/detailed, Paused/Reduced, release-to-downed, reset and independent real-core replay.

## Original Team relay-anchor production

`authoring/library/team-anchor-field-kit-v1` contains the original 24px available/captured radio pair, editable integer-pixel source, exact manifest and variation prompts. Run `node scripts/produce-team-anchor-art.mjs --check` and `node --test scripts/test-team-anchor-art.mjs`. Source bytes are captured alongside the imported drawing; another project-root source cannot be credited for cached pixels. Conflicting retained outputs fail before known missing outputs are written; create a new art revision instead of overwriting an accepted family. Keep both slot bindings in one coordinated collection. Production metadata remains **produced** until imported-map/live-size review and release adoption are evidenced. Studio save/export does not publish these assets or change player saves. Follow the family README for the real upload, collection, import/Undo and runtime adoption steps.

### Imported Team objective clarity

Derive required relays from `run.level.goal.cores`, never from stronghold count or order. Keep optional relay status explicit without changing simulation objectives. Before painting minimum-size anchor artwork, reject decoration that overlaps another planned anchor image or an authoritative anchor/core target; keep the original border and label/check. Test mixed image/recipe neighbors, dense border maps, narrow boards and captured/available states with font-aware measurements. Record native visual evidence separately from modeled painter checks. This fallback does not approve an incomplete production collection.

### Original Team feedback production

The four `team.effect.*` roles have editable original 32px art in `authoring/library/team-feedback-field-kit-v1`. Reproduce with `node scripts/produce-team-feedback-art.mjs --check`; keep source identity, retained files and produced/reviewed boundaries explicit. Use the manifest's role-specific prompts. Radio pulse may hit nothing, hourglass needs the SLOWED context, paired links denote held helper rescue, and the tapered craft badge denotes personal revival grace. Keep runtime captions/progress and do not reuse these icons for Scan, joint capture, shared-reserve rewards or hunter recovery. Inspect earned scenes, inactive states, reduced effects and collection byte preservation before release adoption.

## Explicit production slot migration

Use the exact code-owned optional contracts through `retainProductionHistory(desired, prior, { appendSlots })` when adopting a new production family. Preserve the complete prior slot prefix and immutable asset/theme/collection ledger; validate the final successor against the original prior document. Do not make old slots optional or relax historical theme/picture/import identity checks. Require bound new roles, one successor, idempotent reproduction and unchanged old selection resolution. See [production slot migrations](../../../docs/production-slot-migrations.md) for the contract, prompt and required tests. Runtime adoption, reviewed quality, historical receipt compatibility and public/offline gates remain separate.

### Retained Team presentation ownership

Use `createOwnedCoopPresentation` for live Team attempts and independent previews. Exact archived manifest hashes and immutable Team identity associations authorize older artwork; imports cannot supply runtime URLs. Retry confirms the accepted owner, while Next/catalogue prepare and first-paint a candidate before replacing the previous attempt/results. Route painter, DOM asset variables and menu Auto appearance through `createCoopPresentationDisplay`; failed/reentrant updates must restore all surfaces, including future Auto refreshes. Retire display ownership before terminal cancellation callbacks because the page may already have released its borrowed snapshot. Keep BFCache suspension separate. Follow [host ownership](../../../docs/team-owned-presentation-host.md) and the [maintenance prompt](../../prompts/team-retained-presentation.md); qualify late decoder cleanup, rollback, preview return focus and real old/current asset bindings. A provisional two-revision fixture does not authorize changing production defaults or skipping offline/public release gates.

### Reproducible output and immutable runtime archives

Prepare fresh compiler output with `preparePresentationOutput` before both production write/check. Preserve the generator's ownership metadata format, and retain historical manifests only after formatting current output. Keep `.prettierignore` protection for `game/presentation/compiled/runtime.*.json`: ordinary repository formatting must never rewrite hash-pinned originals. Validate archives through ownership, exact hashes and complete dependencies instead. Test current-output byte reproduction, multiple retained generations, formatter-policy changes and the real CLI/lock path in disposable fixtures. Follow [output preparation](../../../docs/presentation-output-preparation.md); no helper/fixture result approves a new theme, recipe quality, offline journey or public release.
