---
name: visual-theme-identity
description: Preserve accepted content ownership when extending full visual theme compatibility.
---

Reuse accepted campaign/Team validators, canonical JSON and the existing SHA-256 helper. Require code-owned edition associations and exact authored level membership. Never derive ownership from a name, cosmetic selection or mutable simulation run. Keep save and progression authorities unchanged.

Standard/Gentle retain the original base campaign key and original level hash. Team binds the full accepted pack and selected level. Journey requires its own compiler/policy authority, never Legacy reinterpretation. See `docs/visual-theme-identities.md` for boundaries and outstanding integration.

Example prompt: “Implement an exact visual-theme adapter for the accepted content owner using its validators and existing execution/simulation identity. Prove real-fixture identity, changed-content detection, cross-mode rejection, immutable ownership and cancellation. Preserve historical readers. Separate module evidence from art review, gameplay and deployment. Do not expose selection before complete benchmark coverage.”

Journey implementation: use `createJourneyVisualThemeIdentityAdapter` with the accepted project and explicit mode. Preserve existing source-pack membership, policy, base campaign key and Standard manifest simulation identity. Include exact project source hash and never relabel Journey as Legacy or raw imported Team. Test a real picture-metadata edit separately from a policy/physics edit: visuals may change while simulation identity remains identical. The context must change appropriately without inventing save/progression authority. See `docs/journey-visual-theme-identities.md`.
