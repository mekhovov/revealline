# Collection and Guide follow-up native review

Source `ab3c14f` at `http://127.0.0.1:58215/game/`, after the combined `485903` baseline. This review qualifies the observed source behavior; no new immutable edition or deployed release is claimed.

## Collection and Guide

The keyboard journey reached Collection from the title. It initially showed search, the empty picture message, an unopened Achievements and appearances summary, Flight records and Back. The reading toolbar and chapter controls were hidden; empty Previous/Next pagination was absent.

Tab reached search and then the summary. Space opened it. Tab reached Chapter progress and Read achievements; Enter started reading and focused the reading region. ArrowDown/PageDown and Escape returned to the Read button. Shift-Tab reached the summary again; Space collapsed it while retaining focus on that summary. Escape returned to the original Collection title button. No flight started or resumed during this journey.

The keyboard journey then reached Workshop and Field guide. The larger illustration visibly rendered the selected tank. Changing the native Learn about selector to Territory eroder produced a distinct auger body, role badge and different Spot/Risk/Try explanation. Escape returned through the Workshop opener to the original title selection. This verifies the illustrated examples and navigation; static screenshots do not certify every animation frame or theme.

## Real flight, loss and retry

Continue verified/restored the earlier 31.1%, 7,300-point saved flight, paused. Further Standard play lost a life with visible Craft lost / −1 life and recovery text; captured territory remained. A subsequent completed cut reached 50.0% and 11,720 points.

A later horizontal cut reached a blocking wall. The screenshot shows the distinct CHASE cue, exposed-line/choose-a-turn status and the warning that a travelling spark must be outrun. The flight subsequently lost its remaining lives and ended at 50.0% / 11,720 points. The loss screen explained the failed cut and offered Try again. Pressing Enter on Try again started a fresh attempt with three lives, zero coverage and zero score; Escape paused it. The prior picture/achievement library was not artificially populated.

This was a failed human-interface test attempt, not a verified win, speedrun, balance verdict or exhaustive impact-timing test. Source-level simulation tests remain separate. A successful real victory and populated Collection journey on the combined edition remain open.

## Preview startup issue

After cherry-picking the polish, Git's narrow worktree sparse patterns omitted unchanged runtime files, including `game/boot.mjs`. The HTML showed its static Preparing screen. The files were restored from the exact committed source, and this isolated worktree's sparse includes now retain all explicit runtime/build-include leaves across future checkouts. The source diff stayed clean; no fallback, release artifact or gameplay code was changed to conceal the missing-file issue. Reloading then completed the native journeys above.

## Evidence and limits

`evidence.json` binds six unedited native JPEG captures. Plain/Large typography was used in this follow-up. The earlier Theme font and small-layout checks remain separate observations. No physical controller, phone, full custom-media transfer, offline recovery, complete campaign or new public release is certified here.
