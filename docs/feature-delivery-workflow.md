# Feature delivery and public verification

This project’s current authorization is to commit each completed feature or phase, freeze a new independently playable version, create and merge its pull request, deploy it to GitHub Pages, and verify that deployment. No repeated permission request is needed for these scoped actions. This does not authorize unrelated external messages or native-store purchases/submissions.

## One completion path

1. Implement the player-visible result and update the relevant roadmap step, source documentation and AI authoring guidance. Keep future phase code out of the candidate by staging explicit related paths. Preserve supplied references and unrelated working changes.
2. Run the appropriate regressions and actual browser journeys. For navigation, reproduce terminal win/loss, picture and nested menus through keyboard and modeled controller inputs, then inspect touch layouts. Run ordinary first-capture and continued-flight routes in the actual frozen browser online and with its server stopped; startup, restoration and inventory success are separate results. A simulation exception blocks delivery even after all source gates pass. Modeled pads and desktop resizing are not physical-device certification.
3. Commit the logical changes. Extract that exact committed revision with `git archive` into an isolated verification directory; run tests, lint, format, native-format, content validation and motion-lab syntax there. Record commands, source SHA, logs, failures and actual support limits.
4. Use the tested CLI’s `release-snapshot --ref FULL_SHA --version vX.Y.Z`. Never overwrite a release/tag. Create the annotated tag on that source SHA. Verify the manifest, ZIP and source hashes, old release/tag preservation and frozen browser play.
5. Push the feature branch and the exact new tag. Create a PR with the problem/result and validation evidence. Wait for PR checks; review the concrete diff, resolve failures, and merge with history preserved. A successful local test is not a merged PR.
6. Publish the tag’s original `distribution.zip`, checksum and `release.json` as GitHub Release assets. Historical releases use `--latest=false`; only the current delivered milestone is marked latest. Never regenerate an asset under the same version.
7. The main-branch Pages workflow verifies source, builds the exact package-version tag, and deploys only after verification. Public mutable-root HTML must lead into the complete immutable versioned graph. That graph's manifest and `game/build-info.json` must match the frozen source, even when the main merge commit is later than the source tag. Generated root aliases and root-worker retirement are explicit delivery overrides recorded in `current-entry-routing.json`; compare them against their prepared output, not the frozen HTML/worker bytes they intentionally replace. An archive transition must deploy and verify its canonical archive first, before enabling main-site forwarding; follow the storage contract below.
8. Verify the completed Actions run and compare the full prepared public inventory. Test the public game in fresh and previously cached browsers; inspect actual document/module URLs, saved-flight continuation and the new offline scope. Network-byte equality alone is insufficient: a worker may still return older cached modules. For an archive transition, hash every canonical payload and check old entry forwarding, saved-data continuity and worker migration separately. Record version/source identity, public URL, PR, deployment run, failed checks and remaining issues. Only then mark the feature delivered.

## Current release boundary

All project skills use the current [production priorities](production-plan.md) and [UI/storage research](research/round-47-native-ui-followup.md). Exact build allowlists must advance with intentional runtime asset adoption: include the explicitly shipped files in packaging tests while continuing to exclude other source originals and authoring proofs. Keep a failed inventory check and qualify its successor normally; never change frozen bytes to make the old expectation pass.

The large-library follow-up is explicit offload and exact restoration of downloaded chapters, with earned records and saves preserved. This is not implemented by the storage-retention button. Do not describe current external packs or still originals as removable, raise budgets silently, or claim that all production art and audio can be resident together. Add offload prompts and interface examples only when their versioned implementation exists.

**Current delivered milestone — v0.38.0.** [Play v0.38](https://mekhovov.github.io/revealline/releases/v0.38.0/site/game/). All six source gates / 3, 302 tests, independent frozen/native checks, main deployment, all 1, 726 public files / 883, 150, 912 bytes and the complete 226, 341, 360-byte public ZIP stream pass. [Release record](https://github.com/mekhovov/revealline/releases/tag/v0.38.0) · [Native scope](verification/round-47/v038-native.md). Full hardware and human quality testing remain open.

**Frozen v0.39 — scoped native recovery passed; CI/public pending.** Exact `23b886bbdbea` retains six gates / 3, 337 tests, independent artifacts and local Pages checks. The Collection/backup incident involved valid earned ownership with missing durable pack/media data. Exact chapter re-download restored the unchanged player library and original. Offline frozen Split Ring won 62.1%/13, 820/three lives/Gold/29s, preserving the 12.33s best; a separate 30s Grid-center Couch timed win left solo progress unchanged. Storage eviction is plausible but unproven; do not weaken validators or label re-download a runtime fix. [Native evidence](verification/round-47/v039-native.md) preserves unsuccessful attempts and exact limitations. PR #15's two bootstrap-deadline failures have a passing test-only correction `316a50fac9ae`, merged as `4d6154e8a0c3`; main deployment and public delivery remain pending. Keep the frozen tag and assets immutable.

**Integrated next UI source.** `ea59eb24` merges More worlds, stable flight/Pause and seven FPV role presentations, alongside explicit retention. Scoped native paging/filter/portrait and stable-arena checks exist; integrated manual Light appearance survives Heavy class/reload and Match-on selects Heavy. The actual browser retention request was denied and reported honestly. These source previews are not frozen/offline/public qualification. The observed 17 choices, twelve external catalog entries, 36 external-choice ceiling and twelve installed slots are different counts. Countercurrent's twelve source originals and Moving Edges' three Scout-qualified source layouts remain unadopted. The [production plan](production-plan.md) and [roadmap](implementation-roadmap.md) preserve the complete targets and zero new quality approvals.

### Historical release-boundary observations

The observations below retain the state of their named milestone; the current delivery and hold above supersede their pending-status wording.

For a local game preview using the external chapter store, use a supported numeric version such as `0.37.0` or serve the ordinary source-development channel. The generic build tool also accepts descriptive labels, but those labels are not necessarily supported saved-profile channels. Check the actual title, installed chapter and saved continuation before calling any build playable. Keep the `0.37.0-preview` launch refusal and its numeric successor as separate evidence; do not loosen profile ownership checks or overwrite that failed build.

**Historical delivered milestone — v0.37.0.** [Play v0.37](https://mekhovov.github.io/revealline/releases/v0.37.0/site/game/). Frozen c8 source passes 3,275 tests/all six gates; main `4bb84ee`, run `34794013075` and deployment `6428943064` pass. All 1,679 public files / 818,303,462 bytes match, and the actual Courtyard Quiet saved-cut/win/Collection journey passes. Old 033/034 URLs forward; v034 retains its checkpoint, while v033 has a changed profile and no suspended attempt, so unchanged-save continuity is not claimed. Release `388082096` was published at 02:17:53 UTC on 2026-09-14. Its complete unauthenticated 226,293,379-byte public ZIP matches frozen SHA-256 `96593b558a77c71a1574b5f6c9265ab15135efe933791155f426c3d0c5a038ef` in one successful HTTP 200 download. The [final delivery receipt](https://github.com/mekhovov/revealline/releases/download/v0.37.0/v037-delivery-receipt.json) closes delivery. The next gate is the Couch successor; source-only Fracture/audio candidates and hardware/listening quality remain separately qualified. [Exact evidence and remaining gates](verification/round-47/v037-delivery.md).

**Historical source sequence — v0.38 then v0.39.** Frozen `b0a7ded` passes all six gates / 3,302 tests, independent artifact integrity and the local 1,726-file / 883,150,912-byte Pages inventory. [Draft PR #14](https://github.com/mekhovov/revealline/pull/14) is open; native/public qualification remains ongoing. This v0.39 source preserves that Couch implementation and integrates four Fracture owners with twelve unchanged originals over three geometries. Its full source gates, measured build, native chapter journeys and public delivery remain pending; licensed albums remain separate. A source binding or route proof adds no human-quality approval.

**Previous fully closed milestone — v0.36.0.** [Play the release](https://mekhovov.github.io/revealline/releases/v0.36.0/site/game/). Five external chapter entries, original-picture ownership and explicit Install/Choose are delivered. Frozen source passes all six gates and 3,185 tests; main passes 3,188. The complete public site and 203,106,282-byte public ZIP match. [Final receipt](https://github.com/mekhovov/revealline/releases/download/v0.36.0/v036-delivery-receipt-v2.json) retains the interrupted first download and successful range resume. Its successor’s completed delivery and remaining product gates are recorded above.

**Previous verified milestone — v0.35.0.** [Play the release](https://mekhovov.github.io/revealline/releases/v0.35.0/site/game/). Optional victory stories now follow a real completed picture and replay from Collection, returning to the exact earned poster. Training Settings is keyboard-accessible; story captions and controls are larger. All six exact-source gates and **3,008 tests** pass. [PR #11](https://github.com/mekhovov/revealline/pull/11), [main CI/Pages](https://github.com/mekhovov/revealline/actions/runs/34772146072), **all 1,765 public files / 780,633,089 bytes**, actual public and cold-browser journeys, and the complete **161,587,494-byte public ZIP** are verified. [Final delivery receipt](https://github.com/mekhovov/revealline/releases/download/v0.35.0/v035-delivery-receipt.json) has SHA-256 `96d87ca4be0a155844650dc1ae23368b132d700998671e8c8915da0b64b6ecce`. [Evidence and limits](verification/round-45/v035-delivery.md) distinguish the one-story milestone from complete P5/P6, native export-to-disk and physical-device/human qualification.

**Previous verified milestone — v0.32.0.** Frozen source `b37f5fc3ecbeedc06fa1fd13229a9b1864cb05af` passed all six gates and **2,529 tests**, including **48 legal optional-world win/saved-continuation proofs**. [PR #8](https://github.com/mekhovov/revealline/pull/8) merged as `71ade5e659c214893d530f65826cfce7d330b193`; [main Pages run 34750435816](https://github.com/mekhovov/revealline/actions/runs/34750435816) and deployment `6420413260` succeeded. The independent public audit matched **all 2,364 files / 880,747,058 bytes**, with zero failures. Actual public keyboard install → separate Choose → capture → exact paused reload passed; a legal win reached **70.3% / 16,480 points / three lives / 8.65 seconds / Gold**, with the full original reward and Collection visible. [Play v0.32](https://mekhovov.github.io/revealline/releases/v0.32.0/site/game/) · [Final delivery receipt](https://github.com/mekhovov/revealline/releases/download/v0.32.0/v032-delivery-receipt.json), SHA-256 `eaec84668198d1b59c22016114a9961f1d3cf3c367341b6ada18c4e7b281acfe`. This delivers four optional three-map variants: **12 original pictures over three reused R5 layouts**, with unchanged music/rules and separate progress. It does not complete the 29-map/116-picture production target or adopt live uploaded-picture storage.

**v0.30.0 remains independently playable.** Its stronger warned pursuit, role silhouettes, trails and direction-only chapters passed 2,451 tests and all 1,881 public files. [Receipt](https://github.com/mekhovov/revealline/releases/download/v0.30.0/v030-delivery-receipt.json). Preserve its suspended-only picker defect as historical evidence; v0.31 corrects it. The earlier mutable-root startup/cache defect and correction remain documented in [v0.29](verification/round-38/v029-release.md) and the [v0.29.2 receipt](https://github.com/mekhovov/revealline/releases/download/v0.29.2/v0292-delivery-receipt.json). Frozen versions are never repaired in place.

**Previous verified milestone — v0.31.0.** Source `70c67e172ceb39947b442948add0d745b19f9a29`, [PR #7](https://github.com/mekhovov/revealline/pull/7), main Pages `34747037720`, six gates and 2,506 tests passed. All 2,117 public files / 688,163,158 bytes matched. Actual public testing covered suspended-only first-flight discovery, exact saved restoration, ordinary capture and continued simulation, actual uploaded MP3/mixed playback and an exact 418,029-byte native soundtrack export. Frozen cold offline startup, flight and audio also passed. [Final receipt](https://github.com/mekhovov/revealline/releases/download/v0.31.0/v031-delivery-receipt.json). Earlier v0.32 GitHub server HTTP 500/502 attempts remain recorded. Delivery-only `-diff` attributes preceded the successful PR request, without establishing causation. Frozen releases and their historical evidence remain unchanged.

**Previous verified milestone — v0.33.0**, frozen from `e29f2ac9207b047c07e6b72f94fa24cea9b00e1c`. All six exact-source gates and **2,707 tests**, independent artifact reconstruction and the **238-file / 45,409,025-byte core offline inventory** passed. [PR #9](https://github.com/mekhovov/revealline/pull/9) delivered through test-only wait correction `62cb6b5`; main `ee2793e411b274c259e040dc2e4454cb3fa12ba0`, [run 34755819337](https://github.com/mekhovov/revealline/actions/runs/34755819337) and deployment `6421412022` passed without changing frozen runtime. All **2,246 public files / 892,646,633 bytes** matched, including nine hidden files, with zero failures, retries or skips. Actual public native backup transfer, original upload, legal win, settled Collection reload, shared MP3 preservation/mixed playback and old v0.27/v0.28 archive forwarding passed. Frozen A/B retention, missing-original recovery and stopped-server cold playback have separate evidence. The release was published/latest at **2026-09-13 12:38:05 UTC**; the complete **148,237,966-byte public ZIP** matched frozen SHA-256 `965f26322b144a4a8e1e6ef01b584239ed647f14f313253807552f581d49813c` after one retained transport timeout. [Play v0.33](https://mekhovov.github.io/revealline/releases/v0.33.0/site/game/) · [Final delivery receipt](https://github.com/mekhovov/revealline/releases/download/v0.33.0/v033-delivery-receipt.json), SHA-256 `efd1611d37786ef90f349bf85e52ecef2166e0d6bb7116b6d3b22ec1cf46ede4`. This delivers live stills and first-earned retention, not video stories or complete P5. The frozen Retry preparation caption can remain stale while flight runs; its successor correction is separate. Preserve failed candidates and prior pending snapshots. Physical-device and human-balance qualification remain open. [Exact evidence and next-preview boundaries](research/round-43-next-milestone-review.md).

The next preview is separate from delivered v0.35: external chapter installation and paired game-data backup, nine new themed rewards, three new Sentinel Circuit layouts and the Older chapters menu grouping. Story playback is delivered for the bounded Dawn Signal example; full story/content production remains open. Keep source qualification, native integration and public delivery separate, and count distinct geometry, theme variants, pictures, winning routes and finished stories independently.

## Current still-integration evidence

The exact development gate report is `.cache/development-qualification/verification-2aa93f737575/source-gates.json`, SHA-256 `b3b454e785f7924966b71ad231408e07845f1ab8f7ae2af61bd2d51b5022a36d`. It records the isolated Git archive, all six passing commands, unchanged source, and preserved prior releases/tags. Later documentation commits are outside that exact-source run.

The actual source-browser receipt is `.cache/round43/picture-browser/receipt.json`, SHA-256 `6b710ccfe6aa149341f10ad50c1fd7b3518c379f77c43e939b739f1cf33c6256`; all 38 listed artifacts match their byte/hash pins. It records native controls at source ports 8920/8921 in a 1280×720 desktop browser: A assignment → saved A → assignment B → restored A/legal win → first-earned Collection A retained after a better B win; native `.rlmedia` download/fresh-origin review/restore/byte-identical re-export; paired JSON recovery/Undo; and shared-v3 audio with a 418,029-byte native `.rlsound` re-export. Coded silence proves MP3 transport, not finished music quality. This receipt does not certify a frozen build, cold offline paired recovery, public P5 deployment, removed-pack/missing-original browser recovery or physical devices.

A separate source-browser supplement, `.cache/round43/picture-browser/missing-original-recovery.json`, SHA-256 `cd981b12c5e88dc4b6b0e64acb0628c92c6fcd0f5286926c9784442c4de359e3`, adds five pinned artifacts. At source origin 8922, native game-data import retains a saved flight without its original; explicit Load refuses with `.rlmedia` guidance, then native originals review/restore makes Continue recover **0% / zero points / two lives / 0:28, paused**. This closes that bounded missing-original source-browser journey; it does not amend the original 38-artifact receipt or certify offline/frozen/public recovery.

## Explicit browser storage retention

The working [Settings retention control](offline-release.md#keep-downloads-on-this-device) requests `navigator.storage.persist()` only from the visible **Keep downloads on this device** action. Opening Settings reads `persisted()`; quota estimates, successful installs and offline verification never imply a grant. Preserve distinct granted, denied, unsupported and failed statuses, one pending request, late-result invalidation and ordinary keyboard/controller Back without resuming flight. Do not trigger a startup permission prompt, change databases/receipts/budgets or describe retention as a backup or missing-data repair. A browser decision applies to its origin, while explicit offline preparation and portable backups remain separate. Qualify actual browser behavior separately from source tests; the observed missing-pack cause remains unproven.

### Retained-data incident and native evidence

When Collection and complete backup disagree with current installed content, retain the visible failure and inspect actual UI exports/workshop inventory before changing identity rules. Compare the first-earned pin, exact source descriptor and pre/post recovery library sections. An intact local player library does not prove that managed originals or installed packs remain stored. Preserve a refused-export textarea as the old value, not a successful backup. Re-download/restore only the exact missing owner through normal actions; never manufacture empty metadata, reassign an earned picture or relax the validator to make it open. Record the actual recovery separately from the unproven cause and future retention policy.

For stopped-server evidence, retain the command/result that demonstrates connection refusal. A termination note alone is an operator observation, not that network result. Paused DOM proves visible numbers/status; it cannot establish exact direction or an unfinished trail from a filename. Keep failed input attempts, a later retry win, prior best time and a timed Couch result separate. The [v0.39 native record](verification/round-47/v039-native.md) applies these distinctions.

## Optional chapter authoring contract

The working [More worlds browser](more-worlds-browser.md) separates up to 36 trusted external choices from the unchanged 12-installed-pack limit. Preserve native filtering/paging, visible active/failure cards, exact offline installed access and independent flight state. Raising choice capacity does not add content or change optional-body caching, producer membership, ownership or media budgets.

All project skills use this shared contract when the requested change creates or publishes a chapter. Read [optional worlds](optional-worlds.md), the [source compiler and proof guide](../authoring/library/four-worlds-chapters/README.md) and [reusable requests](../authoring/prompts/optional-worlds.md) for that work; unrelated sprite or music tasks need not load the complete chapter workflow.

Preserve original image bytes and provenance. Separate the picture count from unique geometry, enemy behavior and finished soundtrack counts. New campaign/map identities need actual legal route and saved-continuation verification across advertised modes; never copy a predecessor's pass verdict into a new identity. Generated imagery still needs inspection under partial reveal and at play scale.

Optional packs use the explicit versioned distribution registration, full formatted-byte and normalized-content hashes, and distinct Install/Choose actions. Matching gameplay or IDs does not establish matching artwork. Keep their bodies in the full manifest/ZIP and outside automatic core caching. Keep installed 48 MiB/12-pack, per-pack 24 MiB, core 64 MiB and separately managed-media limits distinct; none authorizes silent eviction or increasing a different budget. Test cancellation, failed download/decode/storage, unrelated saved-flight preservation and cold offline play after installation.

Use the tested source revision's optional-capable build adapter. Historical configs without this opt-in preserve their exact distribution. Root aliases, archive allocations, old releases and tags retain the contracts below. A `.json` pack embeds its authored pictures; separate P5 `.rlmedia` originals and `.rlsound` audio have their own explicit transfer contracts. Published v0.33 implements pinned live stills and first-earned Collection retention through the contract below. Keep exact-source/frozen checks, named browser journeys and each successor’s public qualification separate. None of these formats alone is a complete multimedia backup.

## Live pictures, earned originals and paired recovery

For still assignment, rendering, saves, Collection or media transfer, read [flight pictures](flight-pictures.md), [the still workshop](still-media-workshop.md), [the binary bundle](media-bundle.md) and [applied prompts](../authoring/prompts/media-presentation.md). These still-only interfaces are published from v0.33 onward; the current release boundary above remains authoritative.

- `createFlightPictures` records the attempt’s registered theme choices once. The first tick waits for the selected original to be authenticated and decoded. A saved A remains A after the admin assigns B; fresh Retry can select B. Theme changes stage that attempt's exact theme pin. The painter receives one owned `{image,fit}` backdrop without resetting actor rigs or changing masking, physics, scores or replay authority.
- `xonix-session.v3` carries presentation pins beside the verified continuation. Old v1/v2 sessions remain legacy. Explicit Resume preserves direction and the buffered turn; cancellation, backgrounding, stale decode and newer selections cannot start a run. A missing saved original leaves restoration paused and preserves the working run/image. Only a fresh attempt offers an explicit authored-art fallback. Practice/training create no new pins, receipts or awards; a training handoff preserves the parent pins.
- Completion records its first-earned pin in the existing profile transaction. Collection resolves that receipt, not the latest assignment. Retained authenticated owner metadata allows the original image to remain viewable after a pack is removed; replay requires its exact installed gameplay context. Neither viewing nor restoring a receipt grants another reward. Missing/corrupt originals show recovery guidance instead of a substitute image.
- The host shares one explicit rich-v3 manager between still and soundtrack adapters. Metadata and selected-image reads remain bounded; image loads never decode an album. Catalog factories normalize effective levels/rosters and validate Standard/Gentle against exact authored owners. Same names, ID suffixes and unrelated installed packs do not establish backup ownership. `pictureMedia({signal})` returns `{store,metadata}`; backup code captures the trusted synchronous identity factory after that awaited read.

| Transfer                         | What it owns                                                                                                              | What it excludes                                                                 |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| Export game data / verified JSON | Profiles, scores, Collection receipts, included packs and saved-flight pins/continuation according to its existing schema | Separately uploaded still and audio originals                                    |
| `.rlmedia`                       | Referenced still/generic original bytes, immutable presentation revisions and retained owner metadata                     | Audio-only originals, player progress, saved flights and installed pack payloads |
| `.rlsound`                       | Original MP3 bytes, soundtrack records, playlists and assignments                                                         | Still originals and player/game data                                             |

Qualify recovery as a set of files: explicitly prepare and download each required inventory, retain its hashes, restore reviewed originals first, then game data, and restore the soundtrack separately. Check the resulting saved A, earned A, exact original bytes and uninterrupted ownership in a fresh origin; then separately verify server-stopped operation. A successful preparation/status message or native Save dialog is not proof of disk completion. Keep one bounded prepared URL with a visible keyboard/touch/controller action, invalidate stale generations and preserve native-adapter cancellation. File picking/text entry still relies on browser/OS controls; no controller virtual keyboard is implemented.

The v3 database is shared across editions on one origin. Upgrading may require older tabs to close; older v1/v2 readers can subsequently refuse the newer database. Do not certify old-edition uploaded audio after that migration, delete caches/profile/media to force success, or modify a frozen release. Keep the 256 MiB committed-plus-staging ledger, 64 MiB per-source manager limit and the separate still/file/pack/core limits. A logical budget is not available-device storage. Video/GIF stories, pruning and a single all-in-one backup remain outside this still integration.

## Pages storage

The [archive hosting design](archive-hosting-design.md) and [explicit shard plan](../scripts/pages-archives.json) preserve all independently playable versions. The default Pages command retains the old layout; supplying `--archive-plan` activates the reviewed allocation. Main output is capped at 950 MB and each archive at 800 MB. Do not raise these limits or silently remove versions. New allocations use a new bounded shard; existing canonical version locations remain fixed.

The archive contains exact frozen runtime files, manifests, original workers and ZIP checksums. ZIP downloads remain original GitHub Release assets in the source repository. The local ZIP/source TAR and every tag stay unchanged. All current and historical versioned runtime files remain exact. For the current edition only, mutable-root HTML and the mutable-root worker use the explicit immutable-entry delivery overrides described above; root non-HTML files remain available. For relocated versions only, standing **HTML entry URLs** serve query/fragment-preserving forwarding pages and the old **worker delivery URL** serves a scoped retirement worker. Their original bytes remain at the canonical archive. Other old asset URLs move to that canonical prefix; do not claim old-prefix HTTP byte equality. Original release metadata, manifest and ZIP checksum remain available at the old paths, and the main version index links directly to canonical play.

Main and archive Pages uploads must use `actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9` (v5.0.0) with its supported `include-hidden-files: true` input for the strictly constructed distribution. Include `.nojekyll` and historical `.xonix-build.json` in the exact public inventory; do not skip them to obtain a passing audit. The first attempted input fix used v4, which ignored the unsupported input; the workflow succeeded but the second public audit found hidden-file HTTP 404 failures. Preserve those failures and rerun the full audit after the corrected v5 deployment. Validate the finite hidden-file allowlist and reject repository internals, unexpected hidden directories, links and special files before upload. Every audit gets a new output directory.

The retirement worker uses normal activation without taking over or navigating an active game, deleting caches or changing profile/IDB data. Same-account HTTPS project paths preserve the origin and existing version-keyed saves, while changed offline scopes require fresh canonical preparation. Verify both fresh visitors and previously cached old pages; an already-running old worker can intercept its old HTML before forwarding. The canonical index link is the direct route around that scope. Retaining a historical edition also retains its documented defects.

Deploy archive infrastructure from its reviewed full source SHA, then audit every public canonical body against the prepared artifact, including runtime Content-Type and original metadata. Inspect secondary entry points, query/fragment forwarding, saved attempts, writer contention and offline migration in the browser. Only after those pass may the main workflow enable forwarding. A repository, uploaded artifact, successful build or still-running workflow is not that gate. Archive-01's corrected public audit now passes **3,888/3,888 exact files**; local old-scope migration and an actual saved-profile public-canonical journey also passed. Preserve the earlier hidden-metadata failures as historical evidence. The v0.28 main archive-routing cutover subsequently passed its workflow and public inventory. The later v0.29.2 final receipt separately closes P7.7.

Archive-02 canonical delivery is also verified: **418 files / 136,528,268 bytes**, zero failures/retries/skips and all three hidden metadata files. The public canonical browser checked v0.27/v0.28 saved progress, writer contention and secondary entries. A separate loopback rehearsal passed normal worker retirement, query/fragment forwarding, preserved save/Collection and a fresh cold tab after stopping its server. Those tests preserve the older runtime unchanged. The v0.33 main allocation and its full public inventory pass; **actual old-main v0.27 couch/v0.28 solo URLs now forward with query/fragment preserved**. v0.28 Continue restored 1.4% / 340 points / two lives / 2:06 paused under another-writer session-only protection; no new writer or win is claimed. Evidence: `.cache/round43/archive02/delivery/canonical-receipt.json` (`4d1ec10ca3262d28573e1a3befa4214d06c2abe11a631228bb6535f82419f802`), `browser/canonical-receipt.json` (`eddd92edc36a0103f6be9387e0808194eb5782d087dbe9f5f91ef6750072d663`) and `browser/migration-receipt.json` (`c889f0bad10d06f7eea39e7d99d9b0211559a93a627ecb4801ee00a2a240e9d5`), all under the same archive-02 evidence directory.

Next work follows the [active production plan](production-plan.md): **exact-original chapter externalization and one complete versioned-story journey → deeper Tactical content and P6 broader maps, art and finished audio**. Keep the human chapter quality check active alongside these stages; P7 physical input, performance and full public-product qualification remain mandatory before P8 native distribution. P9 online races remain later.

For the current root correction, retire only the mutable-root registration through normal activation. Its finite HTML routes forward to the complete same-origin versioned graph; `/releases/` is excluded. No cache/profile deletion, `skipWaiting`, `clients.claim` or live-tab navigation is allowed. Existing root clients may need all root tabs closed before normal migration; offer the explicit immutable permalink and fresh offline preparation. Keep prepared root override hashes separate from immutable artifact hashes.

## Reusable agent requests

For device-aware controls, follow the [current touch presentation review](research/round-47-collection-and-production.md#touch-presentation-follow-up). Show only available authored actions; keep Arcade pickups automatic. Verify input-device changes preserve pause and clear stale physical input, action icons match their effects, and touch controls respect safe areas without covering the complete arena. A simulated adapter or resized window does not qualify a physical controller or phone.

- “Finish the terminal menu feature; reproduce win/loss using the actual host; verify every visible action without a mouse; preserve saved runs; follow the delivery path and report the public source SHA.”
- “Create a new enemy appearance for a registered role, inspect its direction and damage cues on phone and desktop, compare simulation checkpoints, and publish the validated cosmetic feature without altering old campaigns.”
- “Prepare an MP3 playlist update with source credits and an exact binary backup. Keep the session song playing across menus and ordinary pause. Verify actual uploaded audio separately from synthetic fixtures.”
- “Add a new authored behavior edition. Preserve old proof files, bind the new rule explicitly, prove ordinary wins and failure/recovery, and document what remains inferred from reference footage.”
- “Replace the large pause message with a compact dock. Keep explicit Resume, exact live-cut continuation and accessible Main menu; prove that opening and closing a menu never resumes the flight.”
- “Add a Large-text preference through existing profile APIs. Preserve saved flights and failed-write recovery, then inspect compact portrait and landscape reflow with keyboard/controller focus.”
- “Teach a registered enemy or line impact through Field Guide practice. Use actual legal inputs, grant no campaign awards, restore the original paused flight and listening intent, and reject stale child returns.”
- “Move an explicit historical version group into its bounded archive. Preserve canonical bytes and tags, deploy and hash the archive first, then verify old HTML/worker migration before changing main routing.”
- “Qualify immutable entry from a previously cached root and a fresh browser. Inspect the complete versioned graph, preserve saved flights and old caches, exercise normal retirement/offline preparation, and retain any failure even when all public network bytes match.”

All project skill entry points link here. Skill text supports the user’s chosen scope; it does not turn a design-only request into a release or bypass a failed check.

## Next source boundaries for all project skills

The v0.35 public release is closed. Its training Settings header and focused briefing corrections are delivered, while the broader P1.14/device matrix remains incomplete. The next source groups old First Light editions behind Older chapters and removes stale running-state wording from the picture-ready notice. Every visible player action needs a usable input route; unavailable isolated-session destinations stay hidden.

The shared DB4 story host and per-attempt/first-earned pins are delivered in v0.35 with actual win/Collection/cold playback. Preserve that ownership across game, course, practice and workshops. Removing video retains its exact poster. JSON game-data export is not a complete original-media backup.

The five illustrated packs exceed the existing installed JSON budget. The next explicit install descriptor should externalize unchanged original bytes, publish media before the pack pointer through a recoverable journal and qualify old saved/earned identity before migration. Never raise a cap, discard an older picture or count a proposed compact descriptor as an installed chapter. The [current feedback review](feedback-and-next-steps.md) records exact capacities and the 13-song/60-effect audio candidate limits.

## External chapter and original-art production contract

This section applies to every linked project skill when the task changes optional chapters, their artwork or their transfer. Read [exact-original external chapters](external-chapter-originals.md), [the source Route Worlds compiler](../authoring/library/route-worlds/README.md) and [the current research decisions](research/round-46-campaign-input-and-reward.md). Source qualification, actual host integration and public delivery are different checkpoints.

- Keep the compact gameplay descriptor and exact original-media payload separate. Match the trusted registered descriptor, full campaign/map/theme identities, original hashes and dimensions; a self-declared uploaded descriptor cannot register new behavior. Preserve original PNG bytes, prompts and provenance. Do not silently crop, resample or raise storage caps to make a collection fit.
- Install must preserve the active flight. Choose is a separate explicit action. During review and decode, show that work is in progress; stale success text must not imply readiness. A failed pair, cancelled operation, stale writer or full storage must leave the working library and its original bytes intact. Primary chapter cards show mode, reward count, a short player-facing pitch and Download/Choose actions. Put file recovery and technical backup details behind an accessible Restore from files disclosure. Closed fields must be absent from keyboard/controller traversal; expanding and collapsing must preserve focus and selected files. Keep cancellation reachable during downloading. When downloading completes, return focus to the ready Choose action, unless the player deliberately moved elsewhere; refusal or cancellation needs an eligible recovery action. Test the actual asynchronous native journey; source focus assertions alone are insufficient.
- A paired original-media file carries pictures and owner history, not installed gameplay or progress. A game-data backup must explicitly understand external chapter references and verify their required originals before adoption. Keep the installation and transfer journals distinct, recover through the proper owner, and preserve old backup formats. Source transaction models do not establish native download-to-disk or browser restoration.
- Reused route geometry with new themes counts as a theme variant. Route Worlds has nine new original pictures over three existing layouts. Its 114 contexts are 84 wins and 30 failure controls, not 114 wins. New geometry needs new ordinary inputs, complete replay verification and a serialized unfinished prefix under its actual identity.
- Keep scenery independent of active hazards and collision. Explain class effects and counterplay in the player briefing; manual Tactical equipment does not add manual actions to direction-only Arcade. The new Sentinel Circuit source work is three missions with a two-schedule final boss, not an unsupported three-phase primitive.
- Native verification includes install while a real cut is paused, separate Choose, new-chapter win, exact saved continuation, Collection after reload, and paired recovery. Then verify frozen/cold and public artifacts. Never erase profile/media or mutate historical releases to make those checks pass.

Example agent requests:

1. “Create an original Ukraine Atlas Route Worlds variation using the current compiler. Keep the exact source PNGs and existing physical recipes, produce the compact pack and paired originals in a new output directory, verify both steering modes and saved picture pins, and report nine pictures separately from three reused layouts.”
2. “Adopt one registered external chapter in the game. Preserve a paused cut during native Install, require a separate Choose, complete a real win and reload Collection. Verify storage failure and paired backup recovery before publication.”
3. “Add a new three-mission Tactical chapter using registered Scout information, carrier control and fiber resistance. Author distinct geometry and an alternative route, retain failed tuning attempts, prove actual wins and missed-window recovery, and do not invent a third boss schedule.”
4. “Generate a new original reward for one exact map/theme. Retain the complete prompt and unmodified tool result, inspect the picture at arena scale, create a new presentation revision, and prove that saved and first-earned older pictures remain unchanged.”

5. “Simplify a chapter card without changing its immutable descriptor. Keep Download and Choose primary; verify closed recovery fields are skipped, Enter opens them, cancellation retains a valid focus, and successful asynchronous download reaches Choose using only keyboard input.”

## Production register intake

All thirteen project skills already follow this shared workflow. For new asset production or replacement, first use the [production register](../authoring/production/README.md) and [concrete intake requests](../authoring/prompts/production-intake.md). Run `node authoring/production/cli.mjs report authoring/production/register.json`, inspect the exact slot with `inspect ... --slot SLOT_ID`, then use `validate ... --files` to distinguish declared metadata from available original bytes. The standalone source view is a read-only aid; it cannot approve, save or adopt work.

When changing catalog metadata already pinned by a production register, follow [metadata revision preservation](../authoring/production/README.md#preserve-metadata-revisions). Retain its exact prior JSON bytes in the bounded content-addressed history before advancing the current catalog. Keep old register pins, assessments, runtime owners and original media unchanged. Verify both the historical register and the new catalog; formatting an old snapshot changes its identity. This source history does not recover browser data or migrate a saved game.

Preserve generated originals, actual tool output, effective prompts, reference roles and versioned provenance. Reuse the existing raster media importer and finite production adapters. Append new work and binding history to a separate candidate; validate with `--previous` and the expected current binding revision before an exclusive CLI proposal. Never overwrite an original pathname with different historical bytes, silently retarget a saved/earned owner, or infer a new campaign owner from art. Expanded theme ownership uses an immutable `variantOf` layout record in the same planning slot. Runtime/pack adoption remains a separately reviewed change.

Keep the 29 map slots, 116 picture cells, seven player classes × four themes × two treatments, twelve stories, forty curated reserves and twenty-four tracks distinct. Shared rigs, reused layouts, derivatives, synth recipes and candidate music must not inflate finished counts. Human assessments name the actual reviewer, date, subject revision and evidence; unresolved failures and missing work stay visible. Viewing or generating an image never automatically fills quality, listening, device, browser or release checks.

The separate `register-sentinel-themes.json` candidate appends nine Ukraine Atlas, 1994 Forever and Spend Network pictures to the unchanged baseline. Validate it with `--previous authoring/production/register.json --files`; use the source view’s explicit snapshot links to compare 27 and 36 produced picture works. Its finite `sentinel-world` adapter verifies the original, descriptor, provenance and exact mission/theme owner without executing the chapter compiler. Three reused layouts remain three layouts. The snapshot selector is read-only and cannot publish a chapter or approve an asset. Example: “Inspect the Sentinel theme candidate, preview one exact original, list its outstanding checks and prepare a separate runtime adoption; preserve the baseline and all saved/earned owner identities.”

## Closed media connections and missing originals

All thirteen project AI agents and their skills follow the [media connection recovery contract](verification/round-47/media-connection-recovery.md). Invalidate a closed cached handle only if it is still the owned connection; preserve the current failure and allow the next deliberate operation to reconnect. Never automatically replay an ambiguous mutation, delete or downgrade a database to recover, or let an old close event invalidate a replacement connection. Explicit manager close remains terminal. Test both transaction-creation paths, cancellation and saved-pin recovery.

Investigate missing pack rows or originals separately. Retain saved/earned identities and the original failure evidence; use explicit matching-original recovery without substituting artwork or inferring a storage-loss cause. Keep modeled regression checks, unpatched native recovery, patched browser checks and complete release gates distinct. An interrupted source run with partial passing cases is not qualification.

## Couch shell and accepted input

All thirteen project agents and skills use the [installed Couch contract](couch-installed-chapters.md), [current native evidence](verification/round-47/v037-delivery.md#later-source-work) and [concrete Couch requests](../authoring/prompts/couch-native-chapters.md). The [earlier shell checkpoint](verification/round-47/couch-native-shell.md) retains its historical source boundary and mandatory native source-binding supplement. Reuse the existing shell, navigation, input and match adapters. Preserve two independent direction latches, capture/recovery stops, explicit Resume, neutral gates and series results; menu Confirm must never become flight input. Scope navigation to the current screen, restore the actual opener, retain one confirmed new-match action and keep completed-board review read-only.

Couch `196a9588` starts with shipped **Pressure Lines · Arcade** and its exact wide originals. **First Signal · Tactical** stays an explicit choice; authored capabilities determine available actions. `6028b72d` adds installed chapter selection through `createCouchInstalledChapters`. Its `refresh`, `select` and `confirm` operations borrow the existing chapter host, one shared DB4 manager and one accepted image binding for both painters. Each player still has independent simulation state. Use the exact solo channel: `dev` for source, or `release-${buildInfo.version}` for a built edition. Preserve the raw version spelling; a different URL path does not create a profile namespace.

Install and recover in solo **More worlds**, then explicitly **Refresh installed chapters** in Couch. Keep the selected owner, authenticate the pointer/index/journals and chapter originals, fully decode the selected original, and recheck its snapshot and media generation before Start. Start stays disabled on stale or missing data. The adapter requests no writer lease, pack installation, saved-flight, Collection, backup or media mutation; initial schema initialization is a separate read-path detail. External originals never fall back to generic artwork. Existing embedded originals use their validated path; only older authored maps without a background keep their procedural presentation.

Serialize and join cancelled preparation before starting its successor, including legacy decoders using the executing operation's signal. Back cancels pending lobby Retry/Start checks as well as setup work. Retry checks the same selection without starting it; Refresh does not select a different chapter or resume solo. Return focus only while the current operation still owns it. Keep a paused race's accepted image until replacement or disposal; terminal exit releases images/URLs, while persisted pagehide pauses and retains the owned binding. Preserve the player's actual solo saved cut and first-earned identities on return.

Auto/Always/Off controls belong to each player. Observe accepted adapter requests, not connected devices or raw held samples; notification is not proof that the core executed an action. Fresh input from one player must not change the other's presentation. Clear physical input before hiding controls while retaining movement intent. Keep full arenas visible, controls outside them and 44 px targets; allocate space for actual encounter rows. Hide unavailable Arcade manual actions and label Tactical actions from the actual class. The `6ea648b`/`513caf1` responsive layout gives visible pads a side column on stacked viewports at least 720 px wide and at most 3:2 in aspect ratio.

Native observations cover an embedded Immediate 2:0 match, external Grid capture and one 30-second round, then return to the exact paused solo cut and Resume without a new direction. The tablet viewport at 931 × 967 shows two separate 44 px pads and pictures about 670 × 335 px. A brief Player 2 pad click continued to 51.5% / 12,060 / three lives and stopped after capture while Player 1 stayed at zero; this is pointer evidence, not physical touch. These are bounded browser observations, not all-chapter, physical-device, compact comfort, offline Couch or release qualification. Source tests retain the failed supersession and main-lobby Back cases; later fixes and native results have separate evidence. Do not repeat old claims that installed originals are unsupported, or promote this source-only successor into frozen v0.37.

## Viewport previews and actual devices

All linked project skills can use the [Viewport lab](../authoring/viewport-lab/README.md) for Solo and Couch. Reuse the existing source server. Load deliberately; changing a size must preserve the running iframe and paused match, while selecting a different game must wait for Load. Inspect full intrinsic dimensions and scroll to every edge. The tool is separate from the player shell and is not automatically included in runtime or offline bundles.

Use 390×844, 844×390, 768×1024, 1280×720 and 1280×800 as layout examples, not a device certification list. Inspect both pads, encounter rows, focus, long text and the whole arena. Keep 44-pixel touch targets; do not shrink controls to hide an overflow. A contained but tiny arena still needs a play-comfort review. Record physical touch, connected controllers, safe areas, device pixel ratio and audio interruption separately.

The v0.38 candidate places short-landscape Couch pads at the two outer edges, preserving the whole arenas between them. Portrait stays stacked, with a separate side column on wider stacked layouts. All project skills must preserve this device-dependent placement and distinguish an outer Viewport Lab scroll from a clipped game layout. The [compact source observations](verification/round-47/couch-compact-layout.md) retain both the earlier layout and the corrected live cut; they do not replace physical-device or frozen-release checks.

The initial browser review preserves a paused Couch match through all five dimensions and checks both Always pads at portrait/landscape sizes. That initial keyboard/pointer record does not qualify physical two-player phone play. Installed original-picture chapters and the later tablet side-pad layout now have separate bounded evidence above; the five-size lab record does not inherit those results. The source evidence is `.cache/round47/viewport-native/receipt.json`; the tool was committed at `0119e19`.

[Apple’s game-control guidance](https://developer.apple.com/design/human-interface-guidelines/game-controls) supports showing only relevant touch actions and reducing overlap with gameplay. [Steam’s compatibility requirements](https://partner.steamgames.com/doc/steamhardware/compat) require controller access and readable presentation; active-input prompts and text entry need their own checks. The web-game adapter uses accepted input to select presentation, without changing simulation rules.

Example request: “Open the viewport lab, preserve a paused match while checking all five sizes, inspect both players’ pads and encounter rows, then test the same flows on available real devices. Report layout findings separately from physical-device and human-comfort results.”

## Remember menu selection independently from saved flight

All thirteen project skills follow the [selected-chapter contract](selected-chapter.md) when changing chapters, themes, player navigation or saved continuation. Remember only deliberate accepted choices under the existing writer/recovery/training guards. Startup may prepare the exact installed chapter but cannot install, unlock or resume it. Keep local menu preferences outside the portable library/session/media schemas; Continue flight restores its own exact owner and does not rewrite the menu bookmark. Check missing/replaced content, locked maps, unchanged saved checkpoints and return from Couch separately from actual device qualification. Reuse the documented prompts and focused checks.

## Fixed source previews during parallel work

A preview serving files directly from an active sparse worktree can lose its images after a merge reapplies checkout rules. For a reviewed combined preview, serve a small immutable copy of the exact changed Git blobs over the declared frozen base, and hash every override. Preserve route aliases and actual runtime version/profile channel. Do not infer an asset or game defect from a missing checkout file, or relabel a source overlay as a frozen/offline release. Keep failed native observations, the old binding and the replacement binding. A later source change requires a new explicit binding; do not silently serve changed bytes under the old source label.

## Cancelled chapter-launch feedback

All project skills must preserve the launch-generation and storage guards when improving download feedback. If focus loss cancels an active launch, replace its progress message with an explicit cancellation notice; idle focus loss must not overwrite an existing result. A late response cannot adopt the chapter, resume play or publish success. Join the actual pending handler in regression tests and check the unchanged flight, selection and durable writes. Do not infer that an unrecorded browser blur caused a particular native failure simply because source code can reproduce its symptoms.
