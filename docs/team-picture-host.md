# Team picture host integration candidate

This internal P08 work package composes the reviewed Team painter, lease and two-row binding table onto P05-B/static checkpoint `063ff5d650b6e071d2b59e89fa6e9fb5058b3f08`. It is not whole-phase acceptance or a release. Later P03/P02 compositions and final accepted presentation authority remain separate gates.

The compiled runtime snapshot is SHA-256 `94e24b96644c5c1c2e8d4e014f4d80fe7812359827863d75f00ab132c684982f`: fpv revision 19 with a null collection. The existing First Connection and Relay Yard recipes use only the two approved Orchard/Foundry derivatives described in [Team picture bindings](team-picture-bindings.md). No mission, artwork, producer, registry or Team schema is added. A newer accepted theme revision requires an explicit binding review and qualification; the match is not broadened automatically.

The host mounts one shared presentation-page lease. Starter picture preparation verifies the complete authored pack, selected level, snapshot and original bytes before enabling Start. The existing Relay Yard default remains. Preparation can be cancelled from the current lobby/pause navigation root; delayed completion settles only to Ready. Keyboard/controller defaults and deliberate focus are owned by the current action. Foreground loss retires pending work and completion focus.

A new core is validated before adoption. The accepted image and authored recipe remain attached to the current attempt through pause and same-attempt Retry. Retry rechecks the exact accepted snapshot/asset identity and reuses its image; refusal leaves the old paused attempt and image available. An explicit Change setup discards the attempt but can retain its already prepared picture when the selected authored pack/arena is unchanged. Required starter artwork never falls back after failure. Separately named imported Team packs retain the explicitly labelled legacy procedural path; this is not expanded picture parity for arbitrary imports.

Team departure confirmation, fixed mode destinations, neutral explicit Resume, one-shot initial focus and shared audio/display/menu lifetimes remain in their existing owners. BFCache suspension pauses without releasing the accepted picture; terminal cleanup releases candidate and accepted image resources and the page lease. A failure after adopting a fresh core is reported as a stopped new attempt, not a rollback.

New actual-host tests use real Team markup, entry modules, core, controller navigation and painter with the existing finite DOM/frame model. They inject a host through the shared presentation-page API and use the actual compiled metadata and approved PNG bytes. Image decoding and layout remain modeled. Source checks and 12 complete related test files are qualified on both supported runtimes, covering 273 cases each. Node 22 retains 254 cases in eleven unchanged passing files from the initial 272/273 run, plus the corrected complete 19-case picture-host file; the original menu-event fixture failure remains retained. Node 20 passed one complete 273/273 run. The fresh-input focus and terminal-pagehide product regressions retain separate failing/passing characterizations. All 332 finite game/authoring inputs stayed unchanged within each run. The earlier 195-case unwired binding checkpoint remains separate evidence.

The [host evidence](verification/cross-mode/p08-team-host/README.md) retains two closed native recordings on the same held production. The normal origin has nine events and seven inspected JPEGs: actual First Connection play reaches 4.6% captured ground, retains its picture through Retry and paused Stay, and returns to ready setup only after explicit discard. An attempted unboosted opening failed; this is not a native full win, joint-cut completion or successful rescue. The delayed origin has nine events and six inspected JPEGs. Only the exact Foundry PNG GET waits six seconds; its bytes and HEAD response are unchanged. A fresh required-picture request is cancelled, remains cancelled after the delayed response, then explicitly retried. Ready preserves a later Help focus, and only explicit Start enters Relay Yard. The first attempted Cancel found no matching target after readiness and performed no action; that observation is retained.

Fifteen HTTP bodies and local files match their exact authority before and after each native run. These desktop observations do not complete the Team encounter matrix, actor parity, imported-pack picture coverage, offline/public checks, responsive layouts or physical-device qualification. Later P03/P02 composition and final accepted presentation authority remain open. No phase or release is accepted by this checkpoint.
