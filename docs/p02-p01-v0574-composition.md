# P02 candidate on published P01 v0.57.4

This candidate composes P02 `cbc09f7640ad554c4c34d8699f2e81458e5b8faf` with the immutable v0.57.4 source `856850ce8d4597c1fb6e8bb28ddb8a63db337a8c` (tree `4cd7f24680af174ea35773d28e07e54e5f39e01f`). The annotated release tag resolves to that source. P01 public acceptance is still pending; publication alone does not satisfy it. If acceptance names a different source, recheck this composition before qualification. No P03 or later-phase implementation is added here.

Both branches independently authored `fpv@23`. P01 selects seven recovery-layout screen recipes at revision 12; P02 selects eight shared-master audio recipes at revision 7. The candidate retains the exact published P01 history and adds a new `fpv@24`, whose parent is P01 `fpv@23`. It does not rewrite either historical theme23 record or adopt an auto-merged runtime that mislabels combined content as theme23.

The actual producer CLI starts with P01's coherent ledger and compiled directory. Its 175 source/data inputs match the reviewed model, including optional reveals and all referenced originals. The generated ledger is 6,829,381 bytes, SHA-256 `c9e893cb6970596463e06b30f93f15ecc9ca52ec311c018de7ff70a8ff042cc7`. It retains all 293 slots, 1,074 asset records, 24 themes, one collection and 127 original payloads; eight audio7 records and theme24 append. The compiler regenerates its whole output; only manifest, runtime and studio JSON differ from the coherent P01 output. The compiled-directory swap and ledger rename are separate writer steps, each checked before proceeding.

The compact [published-prior oracle](../game/test/fixtures/production-p01-v0574.json) comes independently from the exact v0.57.4 ledger. It pins canonical ordered record groups and the complete sorted payload digest. The history regression must verify these before reconstructing a prior from candidate prefixes, then reproduce the original published bundle hash. It checks immutable-history refusal, exact additions, screen12 retention and second retention/export/compile identity. No fixture silently derives its expected prior from newly generated output.

The actual CLI reproduction check and a second write both succeed, with all 132 ledger/output bodies unchanged. An earlier shell invocation was refused before the producer started because the shared disk was full; writes paused until the reserve recovered. That launcher failure remains distinct from producer execution. The independently reviewed history regression passed. The frozen Node 22 run recorded 315/316: the only failure was a missing dynamic pack inventory input in the complete presentation-system file. Its exact inventory would exceed this worktree's 100 MiB limit, so that whole file is explicitly deferred to hosted qualification. The other 22 complete files passed; the unchanged Node 20 run passed their 294/294 cases. All 797 physical inputs remained identical during execution. These counts are overlapping scopes, not additive.

Version 0.58.0 remains consistent across package, lock and build configuration after a fresh remote check found no such tag. This allocates a candidate version; it does not publish it. A later native review found inappropriate toggle semantics on changing-name Mute/Unmute commands. The bounded successor removes `aria-pressed` consistently in Solo, the Music Library, Versus, Team and Asset Studio; the original source tests and native observations remain attached to their earlier hold. Its five complete affected files pass 79/79 on both Nodes with all 797 inputs unchanged. The original twelve-event/seven-JPEG native review verifies 13% persistence and actual Library Pause at 16.8 seconds, but retains the mute semantics finding and an initially mistaken Settings Pause label. A subsequent shared status renderer refreshes immediately from current master and transport state; stale awaited Play results cannot replace a newer Pause label, and blocked text survives master edits. Its two complete host files pass 20/20 on both Nodes with all 797 inputs unchanged. The [retained composition evidence](verification/cross-mode/p02-a/published-v0574-composition/README.md) includes the separately bound eight-capture/seven-JPEG semantic review: ordinary Mute/Unmute commands and actual Library Pause at 23.4 seconds pass. The initially uncaptured cold-start delay remains unexplained and requires final build/public rechecking. The deferred inventory file, final committed-source qualification, reproduction/readiness, frozen artifacts, public verification and P01 acceptance remain required. Before those final gates, recheck and compose current main publication history as a separately reviewed step; source `856850ce` alone is not the final PR merge target. Earlier 4,291-test and scoped browser receipts keep their original source boundaries. The producer's reviewed-slot count is a declaration, not listening, device or phase acceptance.
