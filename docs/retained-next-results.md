# Retaining completed Results while preparing Next

A completed Versus round now remains the accepted round while Next prepares and confirms a separate candidate. Both run identities, their checkpoints, result text, series wins and shared original remain available. A failed original read, decode or final authority check releases the candidate only. Cancel keeps Results and returns its owned action to Next. View still opens the completed boards.

A preparation error tells the player that Results are kept and Next can be retried. Internal asset paths and the original Error remain in a tagged console diagnostic, not in the player message.

The two read-only picture owners expose an opt-in staged lease. Confirmation does not publish or release the old image. Synchronous commit adopts the new picture references and returns an idempotent old-lease retirement callback. The host publishes the new duel, picture, generation and any match-score reset before calling that callback. Owner disposal can also complete an interrupted retirement. Existing Ready selection, Retry and confirmation contracts remain in place.

Each candidate receives a monotonic in-memory identity separate from the accepted generation. Failure and Cancel can retry the same captured setup and picture choice. Explicit new setup receives a different identity and prevents a late candidate from replacing it. The host reads the authored recipe, never the simulated level state.

Shipped Next starts after successful adoption only while its original action still owns foreground, scope and focus. Its observers remain active through old-image cleanup, Ready layout, focus restoration and queued callbacks. A newer choice that moves to Help and then BODY cannot revive the earlier intent. Blur can allow successful preparation to reach Ready, but requires a fresh Start. Installed Next retains its existing separate Start activation.

This is a bounded P07 correction. It changes no save/progression writer, replay format, core rules, artwork registry or persistent schema. The exact 90e8 baseline and four source overlays remain separate from later P03/P05/Team candidates until explicit composition. Native and dual-runtime evidence must be stated separately; no physical-device, offline/public or phase acceptance follows from these tests.

## Source qualification

The transaction implementation at held06 passed 246/246 tests on Node 22.22.2 and Node 20.19.5 across twelve complete affected files. All 536 participating inputs (78,700,235 bytes) were identical before and after each run. The scope covers actual host identity/checkpoint/result/series-score preservation, candidate and prior-image release, final installed authority refusal, Cancel, background readiness, newer focus, and the existing navigation/display/audio contracts. The finite DOM models and decoded-image objects do not establish native browser behavior or physical input.

Earlier failures remain retained: the original failed-Next diagnostic, transition focus and button-default modeling, a newer Help intent that previously started the accepted next round, an unfocused synthetic retry, and the first complete run's missing exact R5 fixture plus synchronous-status regression. They are not additive coverage. The final source restores the original synchronous installed verifying notification after publishing its promise, so reentrant operations can still join it.

The final held07 message successor passed both complete affected host files, 46/46 on each supported Node runtime. Its 536 inputs (78,704,523 bytes) stayed exact. These files overlap the twelve-file qualification and their counts are not additive. A preceding attempt was interrupted during shared disk exhaustion: its raw log has 30 reported passes without a complete summary, and Node 20 did not start. The successful successor ran at concurrency 1 after the shared test slot was released; no product failure or specific cause of the capacity change is inferred.

Root completed five independent held06 native recorders, with 28 observations and 23 inspected JPEG files. A real First Signal clear and successful Next, HTTP 503 retention, Help cancellation, New-match cancellation, direct Cancel, View and explicit retry were observed. A separate attempt missed the six-second Cancel window and is inconclusive. Two mostly blank screenshots remain visually inconclusive despite populated DOM. Ten source HTTP bodies matched before and after each recorder. Only the exact First Signal PNG used GET 1 normally, GET 2 as HTTP 503 and GET 3 after six seconds; each origin had a fresh counter and append-only log. No application state, clock or results were injected. A separate held07 native check on fresh origin 55031 passed the friendly message and console-diagnostic scope in two keyboard observations with two inspected JPEGs. Results, the tally, Next focus and View remained intact; its ten source HTTP bodies matched before and after. The preceding 54515 setup refresh was an operator diagnostic only.

Native event labels describe actions, not anticipated success. Verdicts follow the captured DOM, inspected image and transport evidence. Provisional labels and their explicit corrections remain retained; the source and native scopes are detailed in the [checkpoint evidence](verification/cross-mode/p07-next-results/README.md).

Later composition must preserve the resize shell's current-action visibility logic. The optional shell `focusTransition` argument only suppresses automatic transition focus on the first accepted Next update; its default remains true. Original action ownership still controls any asynchronous restoration or Start. This candidate does not implement the remaining one-race tours, named campaign successors, Team order or complete P07 progression contract.
