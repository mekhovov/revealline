# Unified mission library: native source evidence

These browser observations qualify bounded source-native behavior, not an
immutable Release B artifact, GitHub Pages, physical devices or human balance.
The source server used genuine reviewed optional original-media bytes and the
development storage channel. No synthetic release markers or image decoder
replacements were injected into these native checks.

| Receipt | Result and scope |
| --- | --- |
| [70f return failure](b-return-scroll-native-70f-failure.json) | Retained failure: correct filters/card but490→0 scroll left the focused card outside the viewport. |
| [6e return repeat](b-return-scroll-native-6e-pass.json) | Corrected490→490 return, fully visible focused card, exact opener focus and retained paused Solo flight. |
| [6e Custom raster](b-custom-raster-native-6e.json) | Native file import, exact Custom identity and actual Solo/Versus captures reveal the uploaded reviewed raster. |

The passing observations bind runtime source
`6eebeb9e039001f16972f5c92c237e03793f8313`. Subsequent checkpoint `eee77bf70`
changes only test-fixture startup/cleanup and documentation. Receipt-local paths
identify the original observation environment; they are not distribution inputs.
Inline screenshots were visually inspected but not exported as these receipts.

Custom raster evidence uses qualification data, not a new shipped mission or
artwork. It does not prove full-image earned-original export/reimport byte
preservation, a win, or a complete playthrough. Release-channel absent-pair
admission, final frozen/public flows and byte integrity remain separate gates.

The coherent local6e test cohort completed315 tests:314 passed and one failed
while waiting for host startup to reach a deliberately held metadata request,
before its visibility action. The isolated case passed in3.873s. Test-only
startup/cleanup correction passes the three relevant cases in11.422s, with13
unrelated name-filter skips. The failed combined result is retained as such;
there is no315-pass claim or full-suite rerun. Any temporary full-test waiver
must be explicitly recorded by the release publisher, not inferred from these
native observations. Build, exact-source identity, immutable asset integrity,
deployment and public acceptance are not waived.
