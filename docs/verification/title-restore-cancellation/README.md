# Title restore cancellation correction

v0.69.0 public byte audit passed all 3,789 deployed files (698,351,359 bytes). Keyboard victory, picture, results and Next worked. Native continuation testing exposed stale field preparation feedback after immediately cancelling title Continue. Explicit Load still restored the same second mission at 0% / 3 lives / 0 score / 3:43, paused. Preserve the original observation; this is not full browser acceptance.

The title restore abort signal now settles the field status it owns immediately, before a held image decoder finishes. The existing operation token prevents old completion from replacing newer feedback. No saved bytes, simulation, scoring or automatic-resume rules change.

The original full title-entry file with stronger status assertions failed the cancellation case (42/44 passing, including its failed parent). After the runtime correction, both complete title-entry and title-operation-status files passed 47/47 on Node 20.19.5, zero failures, cancelled, skipped or todo (34,050.285334 ms). They cover saved cuts, cancellation, changed save, Missions/blur, terminal retry and focus ownership. These focused checks do not replace final hosted source qualification or public correction testing.

The public short-landscape Main menu focus border remains slightly clipped under Large/Plain preferences and is tracked separately for P03/P05. Physical device/controller, audio listening and offline acceptance remain open. Native screenshots were inspected inline; no exported-image hash is claimed.
