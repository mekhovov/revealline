# Round 25 source browser checks

Observed on 12 September 2026 against isolated source port 8830. The working version is v0.15.0. Tests used the real solo page, Playground imports and Controller Lab. The workspace server on 8767 and its preserved player releases were not changed.

## Baseline and corrections

The new, visible Playground **Control geometry → Capture geometry** report measures the child page through the application's existing same-origin preview. Browser automation reads the displayed report; it does not access private iframe state or inject a terminal result. Captures are dated snapshots, with actual CSS viewport, display scale and pointer media recorded separately.

Before the solo CSS edit, [1280×720](before-desktop.json) measured five direction/Stop buttons at 34×28 and three utility buttons at 34×35. The browser reported fine pointer and hover, with no coarse pointer. No targets overlapped in that measured case. [844×390](before-landscape.json) already had 44px targets, but its fixed 337px stack occupied y=45…382. Genuine coarse input was not established; the previously predicted wide/coarse overlap remains a source-derived hypothesis, not a reproduced hardware failure.

The [first 15-size matrix](initial-layout-matrix.json) confirmed the shared 44px targets and non-overlap after the first CSS pass. It also showed ordinary controls below the viewport at several otherwise roomy sizes. The revised height allowances bring standard controls into view at those sizes while retaining the existing short-desktop scrolling allowance. Short landscape now uses a rail in normal page flow, with utilities beside ability rows; it no longer pins a tall stack to the viewport.

The initial [maximum-label matrix](long-label-ordinary.json) found horizontal page overflow despite unclipped flight buttons. Opening the same practice through **Open full preview** allowed direct inspection of the visible page: the unbroken 80-character class label overflowed `#hangar-state`. It now wraps. A separate key hint had been allowed to shrink; its flex sizing now reserves the hint's own space. Both corrections are presentation-only.

## Final measurements

All final rows below report **eleven visible flight targets, each at least 44×44 CSS pixels, zero target intersections, zero target/arena intersections, no horizontal page overflow and no button scroll/client overflow indication**. Offscreen controls are still visible layout boxes and may require normal vertical scrolling. Border-box and scroll/client measurements do not establish glyph-level readability or absence of every possible overlay.

| Configuration and evidence                                               | Actual CSS sizes                                                                    | Observation                                                                                                                                                     |
| ------------------------------------------------------------------------ | ----------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [Ordinary](ordinary-viewports.json)                                      | 320×640, 390×844, 680×720, 681×390, 844×390, 844×501, 1280×720, 1100×901, 1440×1000 | All flight targets initially inside the viewport except the accepted 844×501 short-window case.                                                                 |
| [Standard Sentinel](sentinel-viewports.json)                             | 320×640, 390×844, 681×390, 844×390, 844×501, 1280×720                               | All eleven initially fit at 390×844, both short-landscape widths and 1280×720. At 320×640 six fit; at 844×501 none fit initially, requiring vertical scrolling. |
| [80-character ordinary label, corrected](long-label-ordinary-fixed.json) | 320×640, 390×844, 681×390, 844×390, 1280×720                                        | Label wrapping can make the action column taller; all controls remain in document flow. At 390×844 all eleven fit initially.                                    |
| [80-character Sentinel label](long-label-sentinel.json)                  | 320×640, 844×390, 844×501, 1280×720                                                 | Taller content scrolls vertically; it does not enlarge the page horizontally or put controls over the board.                                                    |

The 80-character fixtures change only Scout's authored label, through the actual validated scenario importer; an 81-character label is rejected. The source maps, ability recipes other than that label, and old route proofs stay unchanged. The cache fixture generator separately compared initial and 120 subsequent checkpoints for each case in both steering modes. Those bounded simulations are not complete human playtests.

In the separate full preview, used **Settings → Keyboard controls → Change Use ability**, then pressed Backspace on the focused capture button. The visible setting and action hint changed to Backspace. [Actual hint evidence](long-keyhint.json) shows client and scroll widths both 49px, with the full 80-character label wrapping beside it and the hangar text contained. [Confirmed screenshot](long-keyhint-confirmed.jpg). The earlier `long-label-keyhint.jpg` is an initial E-hint observation, not proof of Backspace adoption.

## Actual game and controller journeys

- In fresh solo play, used the enlarged Down and Stop controls, then completed First Signal in **both Immediate and Grid + buffer**: 52.2% coverage, three lives, 8,160 points and 0:03 on each HUD. The full-picture celebration remained available.
- Installed Sentinel through the library and imported the unchanged, public-API replay-backed Immediate prefix from Round 21. It restored paused at 26.1%, one of two relays and CORE OPEN. Read/Done kept it paused; Resume and visible Left completed the remaining live cut. The result showed 100%, three lives, 16,640 points, gold and 0:15, then the full picture and chapter appearance notice. This checks the final live input after a verified saved prefix, not a fresh full manual route. [Reward](staged-reward.jpg), [settled result](staged-result.jpg).
- In Controller Lab at actual 844×390, joined with a separate initial gesture, started, toggled Boost on, cleared it with native Stop and paused through Menu / start. Read details and controller Back returned focus to Read details while keeping the flight paused.
- Resized that Lab to actual 320×640, explicitly resumed and made three real Down/Up self-contact routes. The final lost attempt showed the cause and complete practice reset consequence. Held controller Down reached **End of details**; Back kept the attempt lost. [Reader screenshot](compact-controller-reader.jpg).
- A single pulse navigated to Retry. Held Confirm started one fresh attempt; while the same button stayed held, the clock advanced from 0:01 to 0:02 with three lives, zero coverage/score, Scout Ready and Boost off. It did not repeatedly restart or activate an ability.
- A live Sentinel preview at 844×390 showed the new rail beside the board, with standard actions visible and phase/status content reachable through the page. [Landscape screenshot](landscape-live.jpg). Capturing or editing parent controls can invoke the existing focus-loss pause; the diagnostic itself never sends game input.

## Scope and remaining device evidence

Solo, Playground and Controller Lab error/warning logs were empty after their described journeys. The separate full preview used for the long-label and key-hint check recorded [two host-script errors](long-preview-host-log.json) at `2026-09-12T14:01:34.353Z`: `Electron sandboxed_renderer.bundle.js script failed to run` and a `TypeError` reporting that null is not iterable in `Object.invokeSync`, with `node:electron/js2c/sandbox_bundle` stack locations. The game DOM, Settings and key-hint journey completed, and those recorded stacks identify no game source file. Their cause and impact are not established by this pass; the full preview is not described as having a clean log.

Ordinary semantic pointer clicks and simulated controller gestures were exercised. Coordinate-level edge clicks were not established through the selected browser surface; an unavailable coordinate interface made no input. No physical touch, pointer-cancel gesture on a real device, native application, browser text-zoom matrix, hardware controller, audio listening, public-host deployment or retention outcome is certified here. These limits are distinct from the measured responsive layouts and the existing automated input/cancellation checks. Final gates, packaged-browser checks and frozen/offline verification are separate records.
