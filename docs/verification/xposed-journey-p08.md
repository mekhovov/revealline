# P08 Livewire Foundry — source preparation, not release acceptance

Provisional version0.76.0, stacked on P07 source96dd5cdd. Earlier phase release,
human and production-review gates remain open; this does not bypass promotion.

The immutable `journey-actors-v5` catalogue adds lane emitters through the existing
stationary `lane-boss` engine. Prior role objects and compiled missions remain
identical. Cadence is explicit: 1.5seconds warning,0.7seconds active,6/5.5/5seconds
between warnings for measured/standard/brisk. Difficulty presets do not shorten
these windows. The first warning occurs at2actorseconds, freeze holds that clock,
the chosen row/column stays locked for the attack, and lane width remains1.2cells.
Emitters retain field regions. Enclosure does not disable them. A lane threatens
exposed craft/trail; reclaimed ground shelters the craft from lanes, not other roles.

Studio uses explicit axis/cadence controls, validates all preset/mode variants and
retains copy-on-write/undo semantics. Team rejects the unqualified role instead of
substituting Solo. Studio and chooser diagrams have a distinct twin-post marker;
descriptor-backed geometry text states axis/timing rather than inventing movement
speed. Foundation ready cards teach locked lanes and preserve selective-impact
warnings; historical non-foundation briefs remain unchanged.

Initial focused cohort:35/35PASS on Node20.19.5 and22.22.2, zero skipped/cancelled:
content-livewire-catalog, content-actors, content-phase-catalog, phase-briefing,
mission-brief. Covers unchanged historical identities, all preset/tier descriptors,
Solo/Versus equality, field anchors, Team rejection, immutable invalid edits,
Studio controls, shared map markers and actual idle warning/active/rest public
replay. These are clock/compiler/UI-model checks, not completed mission playtests.

Remaining: original greyboxes, full clear/mastery/seed/timing evidence, runtime
functional actor/attack readability, original assets, native/device/human tests,
Team mission, independent recipe review, full-source CI, immutable release and
public Pages verification. No P08 content is yet added to the player Journey.

## Greybox and functional presentation increment

Seven original candidates now use two three-mission learning arcs and an optional
Remix. The first arc introduces locked lanes; the second combines known terrain,
frontier and erosion rules. Bands8–9 inherit the Phaseworks handoff. Three assigned
references have original, non-final proposals and verified original hashes in
`../research/livewire-reference-crosswalk.md`. No screenshot-only mechanic was
inferred. Studio inspection still requires explicit Apply before local adoption.

All42 mission/preset/control first returns close without life loss, retain stable
coverage denominators and do not win the mission immediately. Each has five seconds
of idle decision space. The first lesson closes at330ticks after the warning at241,
giving89ticks of visible warning before closure. All initial field regions have
real retaining anchors; patrols and dormant roamers never act as fill seeds.

Runtime Classic emitters now share the twin-post cue. Explicit supplied bodies
still draw beneath its outline; collision radius and replay state are unchanged.
Attack lanes have static dashed warning edges and solid active edges in two inks,
with no flashing dependency. Bounds use actual board extents. Freeze preserves
the marked geometry. Paused Details describes field retention and live-trail danger.
This intentionally updates presentation in the new source, not any frozen edition.
Changed presentation recipes need new source/review evidence before release.

Initial presentation test harness omitted the required `started` host flag; that
test failed and was corrected to use the real snapshot contract. No production
validation was loosened. The actor/classic/renderer presentation cohort60/60PASS
Node20; combined10-file framework/greybox/presentation cohort104/104PASS on both
Node20.19.5 and22.22.2. Scoped ESLint and git diff checks also pass.
Native screenshot/interaction, full-source CI and independent visual review remain.

## Full clear and scoped native evidence

All42 mission/preset/steering combinations now have complete, legal, no-life-loss
Solo routes with exact simulation identity/checkpoint and exported public replay
verification. The same inputs finish42 independent equal-board untimed races.
Five explicit seeds replay every route,210 full runs without life loss. Fixed
authored placements make these seed-stability checks, not210 different layouts.
All clears need multiple closures; no objective remains after coverage. Optional
mastery and delayed-start routes are separate open gates. The bounded development
search has omniscient state access; only fresh legal-input replays count as evidence.
Optimized clears are about19–56seconds, not human90–150second pacing validation.

Exact35327b62 served read-only from Git on localhost8792, tab34. Native Studio
inspection compiled seven candidates without applying; explicit Apply created the
local draft. Emitter selection showed cadence/axis and no movement heading. A
horizontal→vertical change applied with all-mode/preset validation; Undo restored
horizontal, saved checkpoint3. No published content or repository map was edited.

Practice loaded the original ready card and fresh Down closed its first cut:
1.2% displayed,270score,3lives. Paused at13seconds, resumed without fresh direction,
and later paused at50 and61seconds with the same score/coverage/lives. This verifies
one native return and scoped stop-on-capture, not a full native clear. Details
reported the real horizontal warning with1.1seconds left, stationary field anchor,
trail danger and enclosure not disabling it; Back preserved Pause. A native51second
screenshot showed the actual dashed lane, shared twin-post emitter, craft locator
and reclaimed connector. The earlier P07 background-only capture issue did not
recur in this new tab; no forced visibility or viewport override was used.

Preview closed normally and focus returned to Play. Remaining six native layouts,
active-solid/muted/reduced/compact visual matrix, optional mastery, timing variety,
art, Team, whole-source and release/human gates remain. None of these observations
is an independently approved production-recipe review.
