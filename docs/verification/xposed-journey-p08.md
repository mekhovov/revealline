# P08 Livewire Foundry — source preparation, not release acceptance

Provisional version0.76.0, stacked on P07 source96dd5cdd. Earlier phase release,
human and production-review gates remain open; this does not bypass promotion.

The immutable `journey-actors-v5` catalogue adds lane emitters through the existing
stationary `lane-boss` engine. Prior role objects and compiled missions remain
identical. Cadence is explicit: 1.5seconds warning,0.7seconds active,6/5.5/5seconds
between warnings for measured/standard/brisk. Difficulty presets do not shorten
these windows. The first warning occurs at2actorseconds, freeze holds that clock,
the chosen row/column stays locked for the attack, and lane width remains1.2cells.
Emitters retain field regions. Enclosure does not disable them. A lane threatens
exposed craft/trail; reclaimed ground shelters the craft from lanes, not other roles.

Studio uses explicit axis/cadence controls, validates all preset/mode variants and
retains copy-on-write/undo semantics. Team rejects the unqualified role instead of
substituting Solo. Studio and chooser diagrams have a distinct twin-post marker;
descriptor-backed geometry text states axis/timing rather than inventing movement
speed. Foundation ready cards teach locked lanes and preserve selective-impact
warnings; historical non-foundation briefs remain unchanged.

Initial focused cohort:35/35PASS on Node20.19.5 and22.22.2, zero skipped/cancelled:
content-livewire-catalog, content-actors, content-phase-catalog, phase-briefing,
mission-brief. Covers unchanged historical identities, all preset/tier descriptors,
Solo/Versus equality, field anchors, Team rejection, immutable invalid edits,
Studio controls, shared map markers and actual idle warning/active/rest public
replay. These are clock/compiler/UI-model checks, not completed mission playtests.

Remaining: original greyboxes, full clear/mastery/seed/timing evidence, runtime
functional actor/attack readability, original assets, native/device/human tests,
Team mission, independent recipe review, full-source CI, immutable release and
public Pages verification. No P08 content is yet added to the player Journey.
