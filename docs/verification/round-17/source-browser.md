# Round 17 source browser checks

The owned source server was `http://127.0.0.1:8805/`. The existing user server on 8767 and all frozen release profiles were kept separate. These are browser viewport and ordinary UI checks; no physical controller, iPhone, screen reader or native executable was tested in this pass.

## Equipment goal and saves

Homeward Skies was installed through Library & saves → Expansion packs, with its original images decoded by the browser. A fresh Copper Orchard Fiber attempt used Tap steering, Boost and the ordinary direction buttons. Its first vertical cut closed with 2.2% coverage, three lives and the displayed 8/8 resistant-cell threshold. This was a fresh partial attempt, not a claimed full manual clear. Pausing saved it; reloading and selecting Load saved flight restored that same closed-cut credit.

The existing Round 14 legal near-finish session fixtures were then imported through Load a saved JSON file. Each recorded prefix is reconstructed and verified by the real engine; the final closure was made through the normal Resume and Right controls. Both Immediate and Grid + buffer restored the live-cut readout (8/8 already secured, 20 pending), finished at 74.1% with three lives, and produced “Steady Signal seal earned and saved.” The two setup records retain their original fixture run IDs and distinct turning modes. They share the unchanged Homeward campaign, map, recipe and roster identities.

A separate Interceptor near-finish session was generated from the immutable v0.6.0 route through `createRun`, fixed-tick input recording and `suspendSession`; no state or award was fabricated. Its source is `homeward-skies/homeward-01/interceptor/immediate`, stopped at tick 3724 of 3748. After normal UI restoration and Right input, it also won at 74.1% but displayed the optional unqualified result. Next uncleared mission opened River Switchyard and hid the unrelated equipment goal. The library contained one picture, three local-score setups and exactly two Fiber seals.

The collection card displayed Steady Signal. The picture view retained the original Copper Orchard art, celebration/replay/back actions and recorded setup details. The initial [picture screenshot](screenshots/copper-orchard-seal.jpg) was taken before the later friendly-class-label refinement; it shows `fiber` rather than `Fiber relay`. That screenshot is observation of an intermediate UI, not a claim that all final labels are represented.

## Old backup and Undo

The historical `.cache/round-14/earned-browser-backup.json` was imported through the public file chooser. Its v1 player library migrated to v2 with eight pictures, ten scores and zero mastery records. No prior clear was inferred to satisfy a new equipment goal. Undo complete backup import restored the previous one-picture, three-score, two-seal profile byte for byte, including its preferences. The related Node regressions also cover cancellation before the asynchronous Undo snapshot, generation replacement and bounded failures.

## Practice and layout

The authoring playground loaded Homeward Skies and selected Fiber. One early attempt to change class before the pack had finished decoding correctly rejected that stale import; retrying after the decoded status succeeded. Play configuration opened the actual Copper Orchard preview with the explicit **Practice goal** label. A normal boosted downward cut closed at 2.2% with 8/8 goal credit; Pause retained that preview. Reloading solo and exporting through its UI produced an exactly unchanged 4,102-byte profile. This check exercised a practice partial cut, not an entire practice victory; terminal eligibility is additionally exercised by the queue tests.

The registered practice preview requires the exact installed map and roster. Changed map content with the same IDs does not inherit the built-in goal. The source UI uses pinned content registration; structural imports with mismatched goal metadata remain archived records rather than current named seals.

At 320×640, 844×390 and 1440×900 CSS pixels, the document had no horizontal overflow and Start remained inside the arena at 44, 44 and 45 pixels high respectively. At 320 pixels, optional text uses the overlay message's existing vertical scrolling while launch actions stay fixed and reachable. It is also reviewable in Mission brief. The 390×844 playground reported every action visible at a minimum 44×44. These measurements do not emulate phone hardware or establish full accessibility conformance.

The first screenshot revealed low-contrast gold text on the light page. Goal and gallery text now use dark ochre on paper, with a separate pale-gold color on the dark overlay. Important status text remains readable without music or animation. Sampled source and playground warning/error logs were empty.

Structured evidence: [viewport measurements](goal-viewports.json) and [public export comparisons](profile-checks.json). Large imported/exported QA files remain local under `.cache/round-17/`; the JSON records their hashes and provenance. Frozen artifact and offline verification are separate release gates.

The later [two-steering screenshot](screenshots/two-steering-seals.jpg) shows the friendly Fiber relay label and both retained modes. Late-result DOM tests also verify that an already-open collection and picture update their seal text without replacing focused cards, reopening dialogs or redrawing artwork. Detailed setup lists are independently scrollable so they cannot push the main picture indefinitely downward.
