# v0.70 Workshop loading-exit correction

Both complete hosted families for source `4bb553d0` failed: **7,302/7,313 tests passed and11 failed per family**, with no skipped/cancelled source tests. Preflight, production reproduction/readiness and ordinary build passed; freeze was skipped. Complete original jobs and logs are retained. This source cannot qualify a release.

Six failures exposed static exit anchors that stayed inert until a navigation module ran: one Replay markup case and five Motion focus cases. All nine registered tools now retain an available same-edition game exit before JavaScript starts. Their contextual Workshop links stay gated until validated. No automatic navigation, save, gameplay or collision behavior changed.

One title-flow fixture omitted native beforetoggle/autofocus behavior needed for nested dialogs. Its bounded model now supplies those events and strengthens exact checkpoint/storage assertions; production focus rules stay unchanged. Four miniature-build failures lacked the newly HTML-selected return module and CSS. The fixtures now copy both and their existing transitive graph, retaining strict production reference validation.

The root's complete two-file Motion/build cohort passes55/55 on Node20.19.5 (11.107seconds); its complete five-file Replay/title/Workshop cohort passes58/58 (42.134seconds). Lint and source formatting pass. Earlier setup/observer failures remain beside corrected results. The owner's separate Node20/22 cohorts and exact Replay keyboard observations are retained without treating them as whole-source qualification.

Root native Motion testing deliberately returned503 for all five selected scripts. Tab twice reached Return to game; Enter reached the same-origin game boot route. Shared refused scripts also prevented the game from starting, which is explicitly not a playable-game result.247 distinct successful response paths were reconciled against exact Git or pinned HTML overrides. The temporary browser tab and server were closed. Root native HTML precedes formatter-only anchor wrapping; public verification must target final immutable bytes.

Source remains an unpublished0.70.0 candidate. No phase completion, physical-controller/touch, audible-listening, offline or full-browser qualification is implied. A separate deployment coordinator now owns integration sequencing; no source merge, tag or Pages mutation was performed during this correction.
