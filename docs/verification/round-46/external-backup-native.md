# External chapter backup: native source acceptance

The source host at `db6fc77ace41ddb2ce8150b432744963e102c5a3` passes the native desktop browser backup journey on 2026-09-13. This is integrated successor source, not a change to public v0.35.

A genuine unfinished Orchard Crossing cut was paused at 0% coverage, zero points, three lives and 2:58 remaining. Exporting through the visible save JSON form produced a v2 game-data file with its exact external descriptor, pack, picture pin and saved replay. Explicitly importing the earlier empty baseline replaced metadata without deleting originals. Undo restored the same library, pack, descriptor, run ID, picture pins and complete replay/checkpoint (`3e2328cab466a865`). The export timestamp changed as expected; no game tick or content identity changed. Explicit Load suspended attempt returned the same visible cut paused.

On a fresh origin, importing that JSON first correctly refused because the exact original presentation was unavailable. The unchanged original `.rlmedia` bundle was then selected, reviewed and restored through Picture Workshop: three originals, 8,024,867 bytes, three immutable revisions and one exact owner. Returning to the game and importing the same JSON succeeded. Explicit Load restored the visible cut and original art. Resume without another direction secured 51.5%, 12,060 points and three lives, then stopped the craft at the completed cut.

The evidence retains two early test observations: a copy taken before asynchronous export preparation finished still held the baseline, and a screenshot before explicit Load showed the baseline with the restored-flight action. Later ready-form data and explicitly loaded screenshots are the acceptance evidence. No hidden game state, clock, storage or input was injected.

Receipt: `.cache/round46/external-backup-native/receipt.json`, SHA-256 `a9af96114afaf6e83d1df75350414551d91534d41ad522e0965d715b15655adf`. It pins source files, exact pair and native snapshots, images and logs. Source and old host qualification remain separate evidence sets.

This verifies JSON plus separate original-picture recovery, not a single-file complete game/media archive. Native save-to-disk completion, physical controller/phone testing, story/MP3 transfer, frozen/offline/public qualification and a full native level win are outside this bounded test. The previously separate external-host win and automated route proofs retain their own evidence.
