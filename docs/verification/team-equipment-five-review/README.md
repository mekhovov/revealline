# Five Team equipment images: scoped review

This review applies only to the two anchor and three core PNGs listed in [review.json](review.json). It permits their next immutable image records to use `reviewed` when every reviewed renderer/art input and each original PNG hash still matches. The other 37 Team roles remain source recipes without approval. No production output is generated or published by this source packet.

The independent original-pixel review found different available/captured mast-and-coupler silhouettes and shielded/exposed/secured cabinet states, with exact transparent bounds. The coordinator then viewed all five originals at 1× on light/dark and at enlarged grayscale, and actual Studio Team initial/exposed/secured states with reduced effects. See [native observations](native-studio.json), [pixel measurements](pixel-analysis.json), and [original identities](originals.json). Studio's secured two-relay specimen uses a neutral backdrop and does not establish canonical Relay Yard full-picture readability.

The gate binds [29 ordered inputs](inputs.json): original construction, palette, encoder, and the existing complete Team renderer dependency list. It also requires each exact PNG hash. Changed input or bytes returns `produced`; unknown roles receive no image approval. The dedicated gate does not add a Team recipe-group review. Later adoption must retain produced ancestors, all original blobs, canonical history, and retained runtime dependencies through the existing immutable-history path.

No live full-gameplay, full-arena reveal, physical input/device, offline or public acceptance is claimed. Evidence observations are scoped, attributed and source-bound; metadata counts are not an art review. The complete equipment-review, equipment-art and Team-history files pass nine cases on each Node 20 and Node 22, including changed-input rejection, exact original hashes, immutable produced ancestors and repeated export/import. Three selected existing production-approval assertions also pass on both runtimes. The unchanged complete-collection two-successor capacity case fails: its compact-evidence version-one transfer manifest is 5,247,076 bytes, exceeding the unchanged 5,242,880-byte limit. Its document alone is 5,235,068 bytes; the redundant payload table causes the overflow. Keep adoption blocked until a separately reviewed transfer-format correction passes that original capacity case. No limits, prompts, rights, records or assertions were relaxed.
