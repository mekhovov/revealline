# Workshop return and embedded Playground navigation candidate

The fixed nine-tool routes return to the matching Workshop opener within the same edition. Single bounded Journey context is preserved; no return hint starts a mission or adopts a save. Embedded Playground controls allow native keyboard exit after pausing/releasing input; Controller practice retains its own protocol. A nearby return action avoids traversing the entire editor.

The sealed31-file packet is retained with its original pins, modeled checks and native limitations. Root independently reproduced82 navigation tests on Node20.19.5 in229.32seconds. The first120second run ended after58 passes; its incomplete log is retained, not counted as a pass. No assertions were dropped in the complete retry.

Integration into the music candidate preserves shared music roots in both Couch modes and the current recovery/Pause guidance. Most postimages are exact packet bytes; host files also retain the newer runtime. This integration requires its own tests, full source gates, immutable release and public journey verification. Content Studio is a separate follow-up; physical device, controller, offline and full-phase acceptance remain open.

Primary guidance: [W3C keyboard exits](https://www.w3.org/WAI/WCAG22/Understanding/no-keyboard-trap.html) and [visible focus](https://www.w3.org/WAI/WCAG22/Understanding/focus-not-obscured-minimum.html). The target is a fully visible focused action, while native modals retain explicit exit behavior. This is not an accessibility conformance certification.
