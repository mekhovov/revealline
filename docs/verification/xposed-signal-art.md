# Signal Gardens original artwork — candidate evidence

Prepared on the P04 source branch so the qualified P03 `9ee58177` stays frozen.
This does not imply accepted-baseline integration or a P03/P04 release.

## Originals and authority

Seven Solo/Versus compositions and a distinct Shared detour Team composition
now have modern-pixel-art candidate revisions. Three earlier illustrated variants
are preserved separately rather than overwritten or relabelled as pixel work.
All were generated with the built-in image tool; original tool outputs remain in
the generated-image directory. Project copies, exact PNG dimensions, byte lengths,
SHA-256 and descriptions are pinned in `game/content-design/signal-art.mjs`.
The full prompts are `docs/research/signal-original-art-prompts.json`.

Each image is1774×887 (2:1), original artwork rather than copied Xposed material.
The primary subjects are seed vessel, ceramic chimney, paired conservatories,
irrigation wheel, hanging planters, rain pavilion, seed lanterns, and Team seed
bench. Their slate-blue ceramic, aged copper, silver-green plants and distant
radio observatory establish a common material palette with separate discoveries.
Full-image inspection found no UI text, logos or copied map geometry. The hanging
garden remains more finely textured than the strongest pixel silhouettes; visual
style consistency and small-board readability are still open review items.

`createSignalCandidates({ artwork: true })` explicitly selects the seven pixel
revisions. `illustratedArtwork: true` selects only the three available alternate
compositions; missing alternates remain greyboxes. Choosing both fails closed.
Default calls preserve the historical greybox and palette behavior. The compiler
continues to mark artwork as candidate, never human-qualified or award-eligible.
No map geometry, actor speeds, capture policy, score or earned coverage changes.

`createTeamSignalCandidates({ artwork: true })` selects its separate original.
The Team manifest exposes the exact background, but the existing Team test-pack
export intentionally remains geometry/rules only. This is not evidence that the
picture is displayed in the Team gameplay host; that integration is still needed.

The shared packaging registry now contains28 originals totaling73389498bytes.
Optional online-only treatment does not exclude originals from loose files or ZIP
archives. The core offline cache guard and Pages guard have not been relaxed.

Seven optional-artwork packaging tests pass7/7 in12.134seconds on Node20.19.5,
including authentication of every registered original from this selected source.
Missing/duplicate/changed pins and symlinked sources remain rejected. An earlier
combined run passed35/36 but its real-source packaging check lacked tracked pack
metadata in the sparse checkout; exact HEAD content was restored and the complete
packaging file rerun. No byte/hash validation or packaging limit was weakened.
Changed-source lint and `git diff --check` pass.

The final four-file art/packaging/Team cohort passes36/36, zero failures,
cancellations or skips,43.868seconds on Node20.19.5. It checks all84 Signal route
checkpoints under both artwork selections,84 Border checkpoints, all exact asset
pins, three-preset Team simulation/export parity and the existing Shared detour
route/alternative tests. This is not the full final-source hosted gate.

## Native preview observation

The exact-Git local preview served P04`fe45d384` plus three explicitly hash-checked
pixel PNG overrides at port8783. Studio source inspection and explicit Apply
selected those pins before the real Solo Practice iframe launched. A first launch
failed because this test server disallowed an existing public motion-lab module;
adding that exact public dependency path repaired the harness, not game logic.

Soft crossing: native keyboard Down connected the outer boundary to the interior
foundation, yielding0.6%,150score,three lives and a message that five slow-field
cells were neutralized. A second straight connection yielded1.3%,310score, with
occupied regions still retained on both sides. Real image fragments appeared on
the permanent foundations and earned line, with enemy and slow-field overlays.
This is scoped line-only/partial-reveal evidence, not a full mission clear.

Craft detail is less distinct against some pale narrow fragments; the locator
helps, but this is a contrast concern, not a pass. Later unattended perimeter
contact exhausted Practice lives, exposing its truthful retry screen at1.3%.
It is not a human difficulty/enjoyment test and not Journey automatic recovery.

Remaining: broad captures/full-picture victory for every composition, mobile and
paired-board contrast, muted/reduced-effect inspection, whole-set style review,
Team presentation adoption, authored continuation integration, human observations,
exact-source CI, reviewed immutable release and Pages acceptance.
