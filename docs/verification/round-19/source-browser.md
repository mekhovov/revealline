# Round 19 source browser checks

Observed 12 September 2026 using the Codex in-app browser at the owned source origin `http://127.0.0.1:8811`. Work progressed to source version 0.9.0 during this pass; the final workshop preview displayed `v0.9.0 / DEV`. The user's port 8767 and preserved releases were kept separate. Final static/source checks have their own [input inventory](source-inputs.json).

## Authoring and practice

The ordinary Playground Load homeward skies control decoded the original illustrated expansion. River Switchyard and Light carrier were selected. Copy campaign goal created a v2 Supply Line scenario with its full editable JSON. Play configuration showed the typed practice checklist. Start, Supply and Pause performed a real pickup: West pad became collected, ammunition became 1/1, and the other conditions remained incomplete.

No optional goal removed the preview goal without silently restoring the shipped fallback. Undo restored the declaration. Applying JSON with a nonexistent pad rejected with a missing-reference message and retained the previous accepted goal. Erase at column 0 / row 18 also rejected because it would remove referenced objects; the prior map remained present. Microtile presentation and Grid + buffer steering retained the definition in the next practice launch.

Export map as expansion reported that the goal now belongs to the new `homeward-02` campaign and has a separate definition identity. The browser reported a download request; this check does not claim an externally saved file. Show current pack exposed the accepted v2 scenario, retained separately in the QA cache. Pure export/import tests verify the resulting one-map pack's structure and retargeting.

The solo player profile was exported before and after these practice actions. Both copies were exactly **433 bytes**, SHA-256 `f9ffa8ab7671f2efbacbc0eb5e07ad93f74a7af9021dbfd7f28da115ae809e43`, with no pictures, scores or seals. [Profile evidence](profile-checks.json) retains these counts and digests.

Independent review found that selecting another mission could re-enable in-memory rewards in an ordinary preview. After the lifetime-practice fix, a fresh preview selected **First Signal** using its mission button, then Start and tap-steered Down. It finished a complete normal cut at **52.2%, 8,160 points and three seconds**, explicitly reporting **Practice complete**. The campaign remained 00/12 and Collection gained no First Signal picture; only the already-existing solo River Switchyard picture was visible. No crafted completion state or suspended fixture supplied this practice clear. Preview navigation now uses the existing practice containment guard; downloads and the parent's links remain available.

## Installed definitions and historical rewards

A valid standalone pack that reused the exact permanent base campaign with a conflicting goal was rejected by the real installation UI. Its error identified the base campaign, candidate pack, map and none-versus-goal conflict. It left the installed list empty.

An opt-in v2 copy of Homeward, retaining original maps, recipes and image bytes, installed through the file picker. The existing Round 14 Grid + buffer Supply route was imported through Saves & loads. Its earlier input prefix supplied the route history, not a manually flown full attempt. Resume and Move up finished at **71.1%, three lives, 11,910 points and gold**, and the pack-authored Supply Line seal was saved.

Replacing that same pack ID with revision 2 of the definition changed its name to Supply Line Plus and its west threshold to three cells. The unchanged picture remained, while its previous seal appeared archived. Loading the same recorded prefix recomputed the new three-cell requirement; finishing earned a second, distinct seal. An empty v2 declaration list removed the live goal and archived earned metadata. Reinstalling the original definition restored Supply Line's current label and left the revised record archived. No prior award was inferred from an ordinary picture.

After reload, the exported profile contained **one picture, one score and two seals**, 3,123 bytes, SHA-256 `00851bf096e9ffe2f898b04d87b81cd1ff42e848e1fc6fd492ebde4358061778`. The same run identity correctly retained one ordinary scoreboard record while the two definition hashes distinguished its equipment records. Gallery card text deduplicates identical archived labels; the underlying records remain separate.

## Workshop and layout

The indexed Equipment Workshop loaded through the same authoring control. Its three selectable maps showed Ukraine Atlas, 1994 Forever and Spend Network themes. Clear Ledger with Impact craft opened through the actual Open full preview link in a separate practice tab, with the Safe Reconcile checklist and ordinary opportunity objective.

[Measured CSS viewports](authoring-viewports.json) were **320×640**, **844×390** and **1440×900**. Document width matched each viewport. The visible Start action measured approximately 44, 44 and 44.65 CSS pixels high. The browser had DPR 1.2, so capability dimensions were scaled to obtain the recorded CSS sizes; an initial 266×533 diagnostic is retained honestly. [The phone screenshot](screenshots/workshop-phone.jpg) records the actual view. Its reading region scrolls; the still does not claim to display every checklist row simultaneously.

Sampled source/preview warning and error logs were empty. One export click initially targeted a button inside a closed details panel; opening Complete pack JSON exposed it and the ordinary action succeeded. No content change was needed. System Python lacked PyYAML for the skill validator; the existing project validation environment then validated all four updated skills successfully. These setup diagnostics are separate from game failures.

These observations do not certify physical touch/controller hardware, native hosts, arbitrary authored-goal solvability or player enjoyment. [Independent authoring review](authoring-review.md), [pack/replay/backup integration](pack-integration.md) and [the full source gates](source-gates.md) record their separate scopes.
