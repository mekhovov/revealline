# Twelve-mission Team pressure player

2026-09-22. Explicit test edition, not P14 acceptance or default replacement.

## Integration gap closed

The pressure catalogue and144 feasible direct-input cases existed, but
`?journey=team-originals` still selected its historical difficulty catalogue.
`?journey=team-pressure-originals-1` now exposes the exact tested twelve-mission
pressure source through the real Team player, with original pictures and the
existing Next/Skip/chooser flow. No extra pack-selection stage is introduced.

The source factory first applies pressure-v2 to the unchanged greybox donor,
then attaches the twelve immutable original picture/presentation records. This
ordering preserves all36 donor runtime levels, mission revisions and simulation
identities. Pictured campaign execution identities intentionally differ from
greybox execution identities. The separate project ID and profile key keep old
originals, greyboxes, checkpoints and progress untouched. Shared preferences
remain intentional. This is a bookmarked mission start, not an in-flight save.

Gentle/Standard/Expert use moving-enemy multipliers1/1.4/1.75, with4/2/1 shared
reserves respectively. Player handling and warning durations do not change.
These twelve missions do not gain lane-attack schedules merely by selecting
pressure-v2. The player labels the edition and exact preset factors instead of
implying difficulty changes only lives. New timed/spatial studies remain
separate; this edition does not silently blend their geometry or evidence.

Studio keeps one Team Journey library card with an edition selector, deliberate
Inspect and separate Apply. Its new bundled player link explicitly excludes
draft edits and opens a separate tab. Native inspection kept `my-journey`
applied; explicit Apply created `team-pressure-originals-review`. The preview
listed the exact5/3/2 shared lives and1/1.4/1.75 speed factors. Original source
options remain available. Shared lives include the current attempt; the HUD
shows the remaining4/2/1 reserves, so these are not competing difficulty rules.

## Qualification

- All12 IDs ×3 presets retain exact pressure-donor runtime levels. Twelve
  original assets and their presentation bindings are unchanged.
- All144 historical seat/joint public-input cases replay exactly through the
  new factory. Each fixture must contain all four unique switch combinations.
  Historical delayed failures and optional-mastery misses are not overwritten.
- Actual keyboard-host Standard clears all twelve missions in sequence, across
  five campaigns and eleven deliberate Next actions. Pictures are authenticated
  against the registered original hashes and retired only after a successful
  handover. A failed first Next retains the source picture/run, then retries.
- The host's live seed17/initial1tick results are pinned separately from the
  historical direct engine's seed1/delay0 checkpoints. No neutral braking,
  engine injection, artificial win or input-policy changes are used.
- Expert selection does not auto-start. Skip requires two activations, awards
  no clear and stays reversible in the twelve-card chooser. Choosing Twin
  Depots from another campaign persists the new bookmark; reload still requires
  Start. Historical originals load their independent Shared Lookout bookmark.
  Pressure and historical writes leave each other's profile untouched.
- Unknown/duplicate entry parameters do not enroll the pressure source. The
  shared host's existing original/bonus suites cover late preparation rejection,
  picture failure, authority and older player compatibility.

Host ticks below count driven120Hz fixed frames, not an exposed engine hash.

| Standard mission    | Actual host clear tick | Displayed coverage |
| ------------------- | ---------------------- | ------------------ |
| Twin landings       | 3200                   | 66.8%              |
| Stepping exchange   | 3186                   | 81.7%              |
| Divided workshop    | 2513                   | 83.7%              |
| Switchback partners | 2866                   | 74.8%              |
| Shared detour       | 4037                   | 82.9%              |
| Crossed gardens     | 4361                   | 73.8%              |
| Split orchards      | 2357                   | 87.7%              |
| Weaver crossing     | 3927                   | 77.3%              |
| Shared lookout      | 2901                   | 81.6%              |
| Twin depots         | 2194                   | 77.8%              |
| Changing courtyard  | 3216                   | 80.8%              |
| Last rendezvous     | 3095                   | 89.8%              |

Native localhost8846 observation additionally checked labelled boot, deliberate
Expert selection, both outward first returns (1.5% with one reserve), two-action
Skip directly into Stepping Exchange, and reload to its Expert bookmarked Start.
These are bounded keyboard observations, not full native-route, physical
controller, timing-performance or two-human enjoyment qualification. Agent tabs
are temporary; the user's8778 tab was not touched.

## Remaining

Final seven-file regression cohort: **73/73 on Node20.19.5 and22.22.2**:
`team-pressure-originals`, `team-pressure-originals-host`,
`studio-candidate-library`, `team-art`, `team-originals-host`,
`team-journey-progress`, and `team-timed-originals-host` under `game/test`.
Separately, the source/library/artwork cohort plus snapshot/boot guards passes
**66/66 on Node20**, including all13 build guards. Independent review found no
production blocker and independently replayed the source matrix on both Node
versions. Changed JavaScript passes ESLint; changed files pass formatting and
`git diff --check`. Sparse historical files use the existing read-only fallback
at`daaef1facfe573cf13a7da2132ea8fd57aded898`; present worktree files always win.

Several optimized routes remain shorter than the target45–150seconds. Ordinary
feasibility does not close the17 base optional-mastery misses, prove meaningful
cooperation or make enemy pressure monotonically difficult. Shared Lookout/Twin
Depots spatial successors, the three timed missions, controller/accessibility
and human balance must receive their own integration/acceptance gates. This
delivery makes the existing pressure programme testable in continuous play;
it is not a claim that the twelve maps are now final.

Public marker still reports v0.80.1 at`b810521a53af7be145acb8dedce0a01a747339cf`.
The single release owner must integrate the reviewed source into the versioned
immutable release and verify Pages. No competing publication is made here.
