# Journey continuous-delivery checkpoint

September 22, 2026. This checkpoint wraps the ready Journey lane through
`0addde9c12dd364eb3e8384b8abb8dc23ca9be47` into accepted main
`1222454049ea411b29c8822f77283f2109cafbd3`. Proposed release: **v0.82.0**,
leaving the already allocated v0.80.3 handheld and v0.81.0 soundtrack work intact.
It is a labelled test build, not completion of P00–P15 or human balance approval.

## Source scope

The incoming history contains 188 commits after the common ancestor. It includes
versioned relay/directional/encounter contracts, original campaign and Team
candidates, explicit pressure presets, timed relocating bonuses, Ukrainian
ornament and FPV-workshop studies, Studio authoring and candidate inspection,
continuous Journey editions, spatial successors, and their verification records.
The exact merged diff, rather than the historical commit count, is the release
scope. Older editions and recorded progress remain separate.

The merge preserves main's current touch controls, compact HUD, P-to-pause,
terminal-state announcements, soundtrack catalogue and guarded Pages pipeline.
Eight conflicting files require explicit integration. In particular:

- Team capture/recovery messages keep the terminal-announcement guard.
- The new closure explanation lives in the existing Help panel, without restoring
  an obligatory briefing or duplicate element IDs.
- Returning between modes preserves a finite, known Journey edition, including
  the native escape link before large modules load or after loading fails.
- Build-time spatial snapshot integrity and hosted soundtrack inclusion both run.
- Actor tests retain the released presentation revision rather than downgrading it.

## Delivery gates

1. Resolve and independently review the integrated diff; run focused regression
   checks and source validation, lint and formatting.
2. Push the source branch and open a reviewed PR. Run every required hosted CI
   family on its exact head. Repair failures; do not bypass them.
3. Merge only the qualified head, then freeze its exact versioned source and
   distribution through the existing hosted qualification workflow.
4. The single release owner publishes the immutable release and reviewed Pages
   selector. Keep current and previous playable builds and the 950 MiB guard.
5. Verify successful deployment, the public version/source marker, asset binding,
   and playable entry. Only then resume new plan implementation.

Every subsequent completed increment follows the same PR → tests → versioned
release → Pages → public verification sequence before starting another increment.
A local commit or a source PR merge is not a deployment. A successful technical
release is not evidence of human enjoyment, accessibility or balanced difficulty.

## Remaining plan

The complete phase-by-phase and cross-cutting checklist is
[Journey completion and remaining items](journey-completion-2026-09-22.md).
All sixteen phases retain acceptance work. Human/device sessions cannot be
replaced with scripted clears. Soundtrack generation remains user-paused.

## Current evidence

Conflict resolution passed bounded independent review. The reviewer separately
passed 36 bootstrap/music/touch checks on Node 20.19.5 and 22.22.2. Local focused
authored-mode, terminal Team, recovery-cause and actor-presentation tests pass;
lint, game/native formatting, motion syntax and the production ledger check pass.
The first focused run exposed old URL-order and old safe-ground caption
assertions; these now verify the preserved route and revised banked-line language.

Full hosted qualification remains mandatory. No release or Pages success is
claimed for v0.82.0 here. The last checked public marker is v0.80.1; v0.80.2 has
an immutable GitHub release but its Pages promotion remains in the release owner's
pipeline and does not include this checkpoint.

PR [#225](https://github.com/mekhovov/revealline/pull/225) now contains the source
checkpoint. Its first run correctly rejected the stale composed presentation
ledger. [The repair review](journey-delivery-effects-review.md) records complete
input fingerprints, immutable fpv58 metadata, exact Team artwork binding,
134 independent presentation checks and 69 binding/history checks per Node,
plus a final native Team launch. The original failed run stays visible; full CI
must pass again on the repaired head before merge or release.
