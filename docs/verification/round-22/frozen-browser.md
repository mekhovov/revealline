# Frozen v0.12.0 browser and offline verification

Checked 12 September 2026. The saved release is [v0.12.0](http://127.0.0.1:8767/releases/v0.12.0/site/game/), from annotated tag `v0.12.0`, commit `640f3e3570323394a22922261e8928634db7bf3b`. The source gates and working candidate checks are separate records; [independent integrity verification](integrity-notes.md) ties the tested inputs to this exact archived source and reproducible distribution.

For the offline browser test, served `releases/v0.12.0/site` at isolated port 8823 using the CLI extracted from the frozen source archive. The workspace server on 8767 remained untouched.

1. Opened the frozen solo page and used Settings → Prepare offline play. The UI verified all **123 files / 20,505,581 bytes**, with no missing or corrupt entries. [UI inventory record](frozen-offline.json) reports build `c48ab2455a432323d7a96fb969b3c9319ea167ceda6bba8800b8ef81b286c03f`, matching the independent artifact check.
2. Stopped the entire isolated 8823 server, then navigated afresh to the game. It reopened through the prepared browser cache. No offline/network interception or app state injection was used.
3. Selected Grid + buffer and Tap steering. Read and exited the ready details. Started First Signal, moved Down into a live cut, entered the full brief, waited while paused, exited through Done, then explicitly resumed and continued Down. The run won at **52.2%, three lives, 8,160 points and 0:03**. The local score list later retained its more precise 3.99-second result.
4. Reloaded while the server remained stopped. Collection retained one gold First Signal picture and its first-clear appearances; Local scores retained exactly one grid-center / scout / seed 1 entry at 8,160. The next campaign mission was ready. [Reloaded collection](screenshots/frozen-offline-collection.jpg).
5. Opened Controller Lab for the first time on this origin while the server was still stopped. Its local pack metadata, added reading fixture and iframe runtime all loaded. Selected The patient route at an actual reported 320×640, joined the virtual controller, entered the full brief and held Down for seven seconds. Reached `End of details`; native Done returned to Read mission brief while scope remained ready. The reader did not launch or award a mission.
6. The frozen game and offline Lab error/warning logs were empty after these operations. The 8823 server remained stopped afterward. This tests one browser's retained offline cache, not the browser's future storage-eviction policy.

On the persistent workspace origin, opened a separate v0.12.0 player page, installed all six bundled expansions through Library and selected Sentinel Relay. It remains ready at 0%, three lives, 0:00 and zero score. No QA scores, unlocked pictures or test saves were imported into that player page. Earlier release pages, data and immutable artifacts remain available.

The [source browser report](source-browser.md) covers remapped exits, native handoff, five reported frame sizes and the layout/focus corrections. The [packaged candidate report](candidate-browser.md) covers production-policy page loading and actual clears in both steering modes. These records do not certify physical controllers, actual phones, native-store binaries, a public deployment, audio listening, network multiplayer or player enjoyment/retention.
