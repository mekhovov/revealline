# Complete opening reveal candidates

All ten Prologue/Horizon/optional Remix greyboxes now have a distinct original
1774 × 887 PNG, each below 4 MiB, in
`game/content-design/assets/horizon-r1/`. First return was previously committed;
the four preserved local pictures and five final integrations complete the set.
Exact prompts, generated source filenames, dimensions, byte sizes and SHA-256
pins are retained in that folder's `PROMPTS.md` and `horizon-art.mjs`.

Built-in image generation produced the pictures; no Xposed artwork was copied.
The two latest compositions are Long way home's evening inlet/boat shed/village
and Islands in concert's pavilion/archipelago. Stepping stones, Two bays and
Courtyard return use the previously generated originals, copied without edits
after disk headroom recovered above the 512 MiB binary reserve. Originals remain
in Codex storage. No user data was removed to make room.

Initial visual inspection finds continuous, distinct coastal compositions with
no HUD, text, enemy sprites or collision grid. Natural paths, architectural
edges, water reflections and floor ornament are decorative, not safe-return
surfaces. A pictured island is never evidence of a foundation cell. Partial
reveal may expose very different contrast/detail across the scenes; native
whole-set contrast, small-screen, reduced-effects and human acceptance remain
pending. Every asset remains explicitly `review: candidate`.

The asset regression requires one pinned picture per mission, ten unique hashes,
exact dimensions/length/digest, bounded verified loading, and unchanged simulation
identities for all ten missions with artwork enabled or disabled. Asking for the
art collection no longer silently falls back when a pin is missing: the shared
compiler rejects an unresolved reference.

The complete content/foundation/Horizon cohort passes 100/100 with zero failures,
skips or cancellations, including the existing 60 Solo route/replay combinations,
60 paired-board routes and actual Versus host foundation fixtures. Full lint,
format and validation gates are rerun for this slice.

Native Studio reloaded its saved checkpoint 3, inspected and explicitly applied
the complete collection, and saved checkpoint 4 without replacing old history.
Courtyard return's real Solo Practice loaded the pinned picture; a fresh Right
input crossed the inner court and closed at 5.1% earned reveal, 1,140 points and
three lives. Pause/Resume retained the stopped craft; the displayed picture
fragment matched the courtyard, with field and its two keepers distinct. Closing
preview returned to the saved draft. The actual observed parent viewport was
1280 × 720 (iframe about 1203 × 720), not the requested 1024 × 768 override; the
override was reset. Screenshot observations were inline, not saved receipts.
The small gray player over the pale revealed architecture needs a targeted
contrast review; this observation is not whole-set or small-screen acceptance.

This completes candidate image generation/integration, **not P01 completion,
published content, or gameplay/visual acceptance**. The actual Journey host still
requires verified artwork adoption alongside its isolated preset/progress policy.
