--- donor/collect-authority.py
+++ collect-authority.py
@@ -5,7 +5,7 @@
 assert sys.argv[3].isdigit()
 home=Path(__file__).resolve().parent
 revision=sys.argv[2]
-head='904cd973b9ede30f21dd6b378562831def194350'
+head='fe1169291ed726a5c4cd0bc59df4e08583de1156'
 runid=int(sys.argv[3])
 out=home/'inputs'/revision
 assert not out.exists() and shutil.disk_usage(home).free>272*1024**2
@@ -25,7 +25,7 @@
 assert run['path']=='.github/workflows/publish-frozen-pages.yml'
 current=api('main','git/ref/heads/main');assert current['object']['sha']==revision
 commit=api('controller','git/commits/'+revision)
-assert commit['tree']['sha']=='12403f15d1e6d796f774548211595b10e48b0835'
+assert commit['tree']['sha']=='118d12df9f0eb488cc9362c62e6697c859a8f5bb'
 arts=api('artifacts','actions/runs/'+str(runid)+'/artifacts?per_page=100')
 assert arts['total_count']==len(arts['artifacts'])
 a=[a for a in arts['artifacts'] if a['name']=='frozen-pages-receipts'];assert len(a)==1
@@ -46,14 +46,14 @@
  members.append({'path':info.filename,'bytes':len(b),'sha256':hashlib.sha256(b).hexdigest()})
 put('members.json',(json.dumps(members,indent=2)+'\n').encode())
 receipt=put('receipt.json',z.read('artifact-receipt.json'))
-assert receipt['controllerCommit']==revision and receipt['currentVersion']=='v0.69.2'
+assert receipt['controllerCommit']==revision and receipt['currentVersion']=='v0.69.3'
 deps=api('deployments','deployments?sha='+revision+'&environment=github-pages&per_page=100')
 assert len(deps)==1,(len(deps),'ambiguous deployment')
 d=deps[0];dep=api('deployment','deployments/'+str(d['id']))
 statuses=api('statuses','deployments/'+str(d['id'])+'/statuses?per_page=100')
 assert statuses[0]['state']=='success'
 env={k:v for k,v in os.environ.items() if not k.startswith('GIT_')};env.update(GIT_NO_LAZY_FETCH='1',GIT_OPTIONAL_LOCKS='0')
-paths={'catalog':'catalog.json','configuration':'publication.json','manifest':'metadata/v0.69.2/manifest.json','record':'metadata/v0.69.2/release.json','qualification':'evidence/current-v0692/source-qualification.json'}
+paths={'catalog':'catalog.json','configuration':'publication.json','manifest':'metadata/v0.69.3/manifest.json','record':'metadata/v0.69.3/release.json','qualification':'evidence/current-v0693/source-qualification.json'}
 for key,p in paths.items():
  r=subprocess.run(['git','--no-lazy-fetch','show',head+':publishing/pages-controller/'+p],env=env,capture_output=True,timeout=20,check=True)
  put(key+'.json',r.stdout)
@@ -68,6 +68,6 @@
 js="import{createHash}from'node:crypto';let t='';for await(const c of process.stdin)t+=c;console.log(createHash('sha256').update(JSON.stringify(JSON.parse(t),null,2)+String.fromCharCode(10)).digest('hex'))"
 p=subprocess.run(['/Users/oleksandr.mekhovov/.local/share/mise/installs/node/22.22.2/bin/node','--input-type=module','-e',js],input=json.dumps(receipt['files']).encode(),capture_output=True,check=True)
 assert inventorysha==p.stdout.decode().strip()
-binding={'format':'revealline-final-main-public-binding.v1','reviewed':True,'base':'https://mekhovov.github.io/revealline/','currentVersion':'v0.69.2','gameSourceRevision':'e1b58ad673f30bddda15ff66b76c03d149e155e1','qualifiedSourceTree':'7a3f98eadca83439d307318661f05d0bad277b43','controllerCommit':revision,'controllerTree':commit['tree']['sha'],'deploymentId':d['id'],'runId':runid,'inventorySha256':inventorysha,'pins':pins}
+binding={'format':'revealline-final-main-public-binding.v1','reviewed':True,'base':'https://mekhovov.github.io/revealline/','currentVersion':'v0.69.3','gameSourceRevision':'406637f36c039f991e83d241cb49d4568a871109','qualifiedSourceTree':'5d3604419aadc2d023c6814bc965310f645c2973','controllerCommit':revision,'controllerTree':commit['tree']['sha'],'deploymentId':d['id'],'runId':runid,'inventorySha256':inventorysha,'pins':pins}
 raw=(json.dumps(binding,indent=2)+'\n').encode();put('binding.json',raw)
 print(json.dumps({'binding':str(out/'binding.json'),'sha256':hashlib.sha256(raw).hexdigest(),'files':len(receipt['files']),'bytes':receipt['totalBytes'],'deployment':d['id']}))
--- donor/audit-main.mjs
+++ audit-main.mjs
@@ -8,10 +8,10 @@
 import { summarizeCatalogCohorts } from './catalog-cohorts.mjs';
 
 export const HOME = path.dirname(fileURLToPath(import.meta.url));
-export const SOURCE = 'e1b58ad673f30bddda15ff66b76c03d149e155e1';
-export const TREE = '7a3f98eadca83439d307318661f05d0bad277b43';
-export const VERSION = 'v0.69.2';
-export const RETAINED_CATALOG_SHA = '48d974934b9b3e4071b13de418ebf81e25d774a3bd701d515d8e6007c818f5db';
+export const SOURCE = '406637f36c039f991e83d241cb49d4568a871109';
+export const TREE = '5d3604419aadc2d023c6814bc965310f645c2973';
+export const VERSION = 'v0.69.3';
+export const RETAINED_CATALOG_SHA = '240ef680e52cf5eb0da9da0e4eda4f218ad5acc45460509e1ecc0f0fefda6ce6';
 export const sha = (raw) => createHash('sha256').update(raw).digest('hex');
 const encode = (value) => JSON.stringify(value, null, 2) + '\n';
 const require = (ok, message) => { if (!ok) throw new Error(message); };
@@ -83,7 +83,7 @@
   "archive-34": "dde1648bbc5b0b6cdce8778e2964e57a204d5d12b47ae1c814043b428766e7ac",
   "archive-35": "320084f4a120dd87878d97215266370d2c18a0b3ebac6f65f08c77a830da2448",
   "archive-36": "d96a5e0e691493a2cfa2cdf9d82aeef308e6cf4ff1f6a858c490a4647b5bd726",
-  "archive-37": "ad4c8b808bda61d6c37c7715ce6228bd88a7e1e0299af7df83834268765800bd"
+  "archive-37": "794392c3726e2e49abe73a904f43ec426dd056714b676b189cc23aa98e23a9de"
 };
 const canonicalAdmission = (v) => Array.isArray(v) ? '[' + v.map(canonicalAdmission).join(',') + ']' :
   v && typeof v === 'object' ? '{' + Object.keys(v).sort().map(k => JSON.stringify(k) + ':' + canonicalAdmission(v[k])).join(',') + '}' : JSON.stringify(v);
@@ -113,7 +113,7 @@
     require(sha(Buffer.from(canonicalAdmission(a))) === RETAINED_ADMISSION_SHA256[a.id],
       'Exact reviewed archive authority changed: ' + a.id);
   }
-  return { total: 37, retainedUnchanged: 36, updated: [], added: ['archive-37'], externalBodiesReaudited: false };
+  return { total: 37, retainedUnchanged: 36, updated: ['archive-37'], added: [], externalBodiesReaudited: false };
 }
 
 function rowEquals(actual, expected) {
@@ -132,7 +132,7 @@
     recordSha = binding.pins.record.sha256, qualificationSha = binding.pins.qualification.sha256;
   require(sha(raws.catalog) === catalogSha && catalog.format === 'revealline-frozen-catalog.v1' &&
     catalog.sourceRepository === 'mekhovov/revealline' && Array.isArray(catalog.releases) &&
-    catalog.releases.length === 111 && retainedCatalog.releases.length === 110 &&
+    catalog.releases.length === 112 && retainedCatalog.releases.length === 111 &&
     configuration.format === 'revealline-pages-controller.v1' && configuration.catalogSha256 === catalogSha &&
     configuration.currentVersion === VERSION && configuration.deploymentEnabled === true &&
     configuration.retainedReleasesPerMajor === 'all' && configuration.testingRoutes &&
