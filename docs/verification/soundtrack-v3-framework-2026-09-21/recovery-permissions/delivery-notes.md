# Recovery storage permission repair

Independent release review reproduced a mismatch at source `2248f7f35ac5ad75b8bdff5aaa66ca45417180be`: recovery export included redistributable audio when offline storage was denied or unverified, but the importer correctly refused those bytes. The shared recovery plan now includes bytes only when both permissions are allowed. Otherwise it retains exact recording identities as references. The Music backup notice explains both permission dimensions and lists the affected songs. Standalone MP3 export keeps its separate redistribution check.

The six new direct, additive-share and panel cases failed before the fix and pass afterward. They cover denied and unknown storage permission, same-hash aliases, no audio payload or export read, preserved playlist order and selection, successful import/preparation, and repeated recovery. The complete affected files pass 109/109. Separate source tests pass 6/6, independent rights/share/panel review passes 117/117, and complete backup API/host tests pass 29/29. These counts overlap.

The ten-input audio fingerprint now includes the model, rights, bundle, share, source and Music panel helpers. Each added helper is covered by review-invalidation and missing-source tests. Unreviewed source-stage 42 precedes reviewed successor 43; all revision-41 and revision-42 metadata prefixes and all 127 original payload bodies remain exact. UI, screen, motion and effects recipes are unchanged. The three Team pins advance only to exact revision 43; both pictures, geometry, five actor descriptors and strict refusal behavior are preserved. The complete Team cohort passes 235/235.

Local checks pass: validation, lint, both formatting checks, motion syntax, generator 6/6, production reproducibility, the complete production-history file 12/12, and immutable production sources 70/70. The independent review predates the production-history test's approved-digest update; `post-declaration-checks.json` proves that only that expected digest changed, retains the original review unchanged, and binds the complete subsequent test run. Two initial generator attempts failed because sparse checkout omitted tracked authoring code/provenance; both original logs are retained alongside exact hydration receipts and the successful complete run. No assertion was relaxed for a missing fixture.

The obsolete `2248f7f` CI run `35551612984` is retained separately. Preflight, build and shards 1, 2 and 4 passed. Shard 3 was cancelled after this independently reproduced source defect required a successor. Its exact partial log contains no TAP failure and records a completed long nested test 22 seconds before cancellation; it does not establish a stall or a complete shard pass. The post-cancel source-identity check passed. These results cannot qualify the corrected source. Fresh preflight, all four shards and build are required on the new commit. Large builds remain hosted.

PR #209 remains draft with no new version allocated. Owner sequencing, the final source cutoff, frozen-build and offline qualification remain prerequisites to merge and Pages publication. This repair approves **zero recordings** and does not complete the 36-composition milestone, native-device checks or musical/cultural review. All work remains in the isolated soundtrack worktree.
