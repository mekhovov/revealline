# Countercurrent optional chapters

Countercurrent adds three authored wide missions in each of four original-picture editions: FPV Front, Ukraine Atlas, 1994 Forever and Spend Network. The chapter browser offers sixteen exact external choices, using the existing theme/mode filters and small pages. Installation remains explicit, with room for at most twelve installed packs; adding choices never evicts another chapter or replaces a saved flight.

The shared mission order is Offset Docks, Sandbar Braid and Crossing Watch. Offset Docks asks the player to choose a return around opposing hooks, Sandbar Braid combines staggered bars with a head-intercept threat, and Crossing Watch adds two watched lanes. These are Arcade missions with ordinary steering, contact pickups and stopping after each completed capture. Standard and Gentle difficulties and Immediate and Grid turns retain the accepted authored recipes.

## Source and exact originals

The accepted authoring commit is `7f1f77dfefd557491b0e18591b803f07db6c9aac`, combining geometry `3428` and original-art `bd2b053`. Four independent chapter owners bind the same three physical layouts to twelve different originals. The source stills total 31,692,501 bytes; each is preserved at 1774 × 887. The eight authored download bodies total 31,768,658 bytes. Their exact source descriptors and payload hashes enter the runtime registry and catalog without changing the previous twelve owners or twenty-four bodies.

The original [builder](../../../authoring/library/countercurrent-chapters/build.mjs) remains the only source of these paired bodies. `readExternalDistributionEntries` includes them only through the ordinary explicit external-chapter build option. The optional authoring tree does not become an automatic offline prerequisite.

The external-choice ceiling remains 36. The installed index and pack limit remain twelve; the generic pack limit remains 24 MiB, the complete pack library including its index remains 48 MiB, and the shared managed media budget remains 256 MiB. External descriptors retain their 16 KiB limit, external pack JSON retains its stricter 1 MiB limit, and every declared original keeps its exact owner and hash. Collection and backup recovery continue to require the original chapter/media identity.

## Qualification boundaries

The retained authoring proof is Scout-only for executed owner adaptations: 54 contexts per edition, 216 records, 545,664 simulated ticks and 848 saved boundaries, comprising 800 nonempty continuations and 48 endpoints. The seven selectable class recipes remain structurally preserved. This adoption does not re-run or broaden that proof to every class or claim a native browser win.

The focused integration checks use real producer bytes, the real core, application, installed reader and stores. Browser DOM, image dimensions, IndexedDB scheduling and transport are finite test fixtures. The host follows an already accepted Standard/Immediate/Scout route through an actual first-mission win for each owner, checks the capture sequence and stop behavior, and exports/reloads an exact unfinished second-mission cut. A fresh profile must refuse backup recovery without originals, then accept it after explicit matching downloads and Continue. Separate Couch coverage checks one new owner across all three originals, shared board artwork, independent motion, pause and no solo writes.

The browser test reaches all sixteen exact source choices even when the remote catalog is unavailable. A deliberately interrupted Countercurrent download must keep its own card visible across filtering and allow retry on that card, without automatic selection or play. Prior owner tests retain their exact identity hashes; only the expected appended counts and visible recovery controls change.

Focused qualification completed on 2026-09-14. The Node22 scope combines thirteen retained passing records from the first run, forty-one from the affected-file follow-up, and the single corrected Sentinel capacity case. The initial 13/20 and follow-up 41/42 results remain recorded: the first exposed an exact catalog-description mismatch and one missing sparse audio fixture; the second exposed an old capacity fixture that still counted twelve choices and attempted to install the whole expanded catalog. The narrow corrections preserve the original ownership, twelve-installed, 48 MiB and no-eviction assertions. The corrected Sentinel case passed 1/1 in 67.79 seconds, including producer setup; this is a composed qualification, not a claimed single 55/55 run.

The final five-file Node20 run passed 28/28 records, with zero failures, skips or cancellations, in 556.39 seconds. It covers the new distribution and host, installed Couch reader, chapter panel and choice-capacity cases. All four owner wins, exact next-mission saved cut, missing-original refusal and explicit fresh-profile recovery passed. Separate production-register checks passed 10/10 on each runtime, including 68 source-file identities; their initial sparse-history fixture failure is retained too. Actual command logs, source pins and the final handoff are under `.cache/runtime-adoption/` and `.cache/production-countercurrent/` in this worktree.

Native install, full-picture/Collection, saved-run and offline acceptance remain root-owned follow-up work. No version, release, frozen artifact or public catalog has been changed by this source adoption.

## Production register and future work

The additive revision-four [production candidate](../../../authoring/production/register-countercurrent.json) derives 60 produced picture works, 56 unbound picture targets, 15 proposed map families and 14 unassigned map slots. Earlier records and IDs remain unchanged; no new quality or implementation approval is inferred. The metadata-history reader preserves prior source pins across the accepted appearance-catalog revision rather than rewriting old registers.

For the next authoring request, use the existing [runtime maintainer workflow](../../../authoring/skills/xonix-runtime-maintainer/SKILL.md), preserve previous descriptors and source originals, append finite explicit producers, and prove the actual install/choose/earned/saved ownership path before native review. Continue to separate authoring proof, source adoption, native acceptance and public delivery. Moving Edges geometry and originals remain independent, unadopted work.
