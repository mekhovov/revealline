# Default Journey candidate verification

Status: implementation candidate; **not public acceptance**. Tested development
checkout based on `e77889637` with scoped uncommitted default-entry changes.
Final PR/merged source and publication receipts must qualify their exact SHA.

## Corrections retained

- Initial historical host fixtures opened bare URLs. They now explicitly choose
  Legacy; genuine default tests pass an empty query. Old destination expectations
  failed and were updated while retaining lifecycle/save/award assertions.
- Team fixed-mode fallbacks and early boot escape could accidentally enroll the
  new default after Legacy practice/pack/token entry. They now preserve resolved
  Legacy intent, validated authored-return priority and valid token restoration.
- Workshop links from an explicitly launched pack lost catalogue intent. Outgoing
  links now pin the resolved route, including historical `journey=1`; bare old
  Workshop returns remain Legacy. All nine tools retain their return contracts.
- New catalogue links use existing departure safeguards. Team's switch was moved
  into the existing tools area so keyboard/controller Pause navigation owns it.
- Default UI copy avoids authoring labels, while historical review URLs retain
  their original scope warnings. Online-picture limitations remain explicit.
- One development full Couch shell run hit an imposed 512 MiB heap ceiling.
  Disjoint named partitions cover all 22 shell cases; no assertion was removed.
  Hosted full source gates remain required.

## Focused automated evidence

Node20.19.5 and Node22.22.2 are local runtimes, distinct from hosted runtime pins.

- Shared resolver/bootstrap: 25/25 Node20 checks, including ambiguous parameters,
  both catalogue intents and pre-module escape/ready-host priority.
- Solo default entry, Studio and nine Workshop return cases: 42/42 Node20;
  historical navigation cohort: 128/128; targeted modal cases: 3/3 (other named
  cases skipped in that targeted run).
- Versus default/departure/shell: 51 unique Node20 checks; current-copy default
  entry rerun: 9/9. Shell evidence uses complementary 13+9 partitions.
- Team initial shared host cohort: 135/135 on Node20 and Node22; final fallback
  correction: 89/89 on each runtime, including a valid unconsumed Solo return.
- Selected final recovery/transition cases: three Versus preparation failure/
  cancellation cases and one revised Team-map/ending case passed Node20. These
  targeted runs intentionally skipped other cases; injected failures were
  expected and recovery assertions passed.
- Current queryless v5 endings:4/4 Node20. Solo and Versus optional/core endings
  retain results and original pictures; deliberate chooser/Rematch records no
  duplicate award. Older route/optional and v5 cross-campaign cohort15/15 passed.
- Six explicit Legacy fixture corrections:56/56 Node20 across complete files;
  only launch URL lines changed, all behavior/assertions retained.
- Authored cross-mode host regression: 16/16 Node20, including real original PNG
  dimensions/hashes and return from paused attempts.

- Ordinary development build:1,055 files /590,195,343 manifest bytes. Every file
  length/SHA256 and ZIP checksum re-read without mismatch. ZIP SHA256
  `07c74a0d3eda6b35639ac496e8aff53666f586a8dc7ab626a5f070ec3d98dba3`.
  SourceRevision is null for this mutable preview; this is not a release receipt.
- Field Kit reproduction:293 slots,131 files,4,007,816 asset bytes;194 reviewed
  required slots,99 source slots,zero missing in that bounded inventory.
  Production tests71/71 Node20 passed.
- Full local lint, formatting, native formatting, validation and motion-lab syntax
  passed. Full npm test remains an exact-source hosted gate.

Later source edits require applicable retests; this list alone does not qualify
the final immutable release.

## Development browser observations

Codex in-app Chromium at isolated localhost port49383; existing shared preference
records in this test origin. Ordinary UI actions only; no hidden state injection.

- Queryless Solo title: New Journey /91 missions. Missions contains91 cards.
  Keyboard first mission clear:34.3%,8160points,13seconds,three lives. Next shows
  cancellable original-picture preparation while retaining results, then starts
  the next mission directly. Pause shows Continue for Choose your share.
- Paused Solo → Legacy switch shows verification status and Stay. Stay cancels
  departure and restores focus to the Legacy missions opener.
- Queryless Team: Twin landings first,12 missions/five campaigns plus retained
  starter arenas in discovery. Play from its card starts directly. Ordinary
  keyboard play reached52.7%; the initially timed route did not finish. A fresh
  cut then completed63.6% in1:20 with two reserves. Next kept the result while
  checking artwork and started Stepping exchange directly at0%/66%.
- Team explicit Show both players + Direction pad: at390×844 board370×185, all
  direction targets52×52; at844×390 board482×241, targets52×52. Entire boards and
  both pads fit; no horizontal document overflow. Settings return preserves
  Pause and Resume remains explicit.
- Queryless Versus:91 missions, first First return, proper original preparation.
  Visible Legacy library opens the retained race with explicit Legacy mode links.
  Solo Legacy entry opens First Signal; Missions and16-chapter More worlds
  discovery remain available alongside a visible New Journey return.
  Keyboard Find missions reveals91 cards; Escape restores its exact opener.

These observations used a mutable source preview, not the eventual frozen build.
Browser pointer activation/viewport testing is not physical touch/controller
certification. Full latest-route546-case playthroughs, human balance, comprehensive
offline/lifecycle and listening remain open. Screenshots were not exported; do
not invent image hashes or include them as artifact evidence.
