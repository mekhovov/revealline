# Round 24 packaged candidate

Checked 12 September 2026 at isolated port 8828, serving `.cache/round-24/candidate`. The working-tree build is **v0.14.0-rc1**, with 130 manifest assets and distribution SHA256 `8fdc55de98ca4c2f4034942b5b51dba902d01a3244b2fd886b9ec54ba2d1d24b`. Its `sourceRevision:null` distinguishes it from the separately tagged frozen artifact.

The generated landing page, solo game, Playground, couch race, Replay Theater and Controller Lab all loaded under the build server's production CSP. Error/warning logs were empty for all six after these checks. Theater verified its default Copper Crossing example at 1,305 exact ticks and enabled playback; no additional complete Theater playback is claimed.

In the packaged solo game, real Tap-steered First Signal runs in both Immediate and Grid + buffer completed at **52.2%, three lives, 8,160 points and 0:03** each. The full-picture celebration remained available. The new loss-only consequence did not appear in these successful results.

Imported the complete, validated staged Grid loss practice scenario through Playground's visible Complete pack JSON controls. At an actual reported **320 × 640**, started, moved Right briefly, then stopped with a live line. The active boss lane ended the one-life attempt around 0:02. Its persistent reason, tip and practice consequence were correct; the terminal caption no longer promised retained attempt territory. The consequence could be brought into the existing scroll region: [packaged boss loss](screenshots/candidate-boss-consequence.jpg).

Read details and Done left the attempt ended. A separate Retry immediately started a fresh one-life, zero-coverage/score attempt, with two uncaptured objectives and the encounter reset to SHIELD RELAY. These imports were authored practice scenarios, not terminal-state imports, and they awarded no campaign progress.

The [source browser record](source-browser.md) separately covers ordinary losses in both steering modes, five measured viewports, controller reading/held Retry, a successful campaign picture followed by a loss and retry, retained rewards, and both presentation corrections. Physical hardware, native applications, public deployment and human enjoyment remain outside these browser observations.
