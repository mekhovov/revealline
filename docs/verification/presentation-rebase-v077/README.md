# Held Team integration on accepted v0.77.0

The original branch at `e69f4ecabbe8a92525853cc5f148f824487ecbaf` remains intact.
The new candidate `0a54fc850fa3b21634855ed95b245c47dc22c423` includes accepted
main `6f851d1bce4cb7d5f257546ab6ffd3c68231f08a` as an ancestor. Whole-tree comparison
proves that game runtime, assets, scripts and tests retain the original bytes.
The three version files change only version values to 0.77.0; accepted publication
records and one additive handheld skill paragraph are the only other differences.
Thus no source qualification or public acceptance is inferred from rebasing alone.

The first Node22.22.2 scoped run passed 91 checks and failed to load the viewport
suite because its tracked sparse fixture was absent. Restoring the unchanged
`authoring/viewport-lab` files made all 12 viewport checks pass. Node20.19.5 then
passed all 103 checks together. Both runtimes therefore passed the same 103 scoped
cases. Raw first failure and rerun logs are retained. The cohort covers touch
steering, terminal controller navigation, viewport startup, Team presentation and
required-look preparation/host behavior. It does not cover every game feature.
The production generator's `--check` also reproduced revision 41, 140 files and
4,009,248 asset bytes. No generated asset changed during this check.

`public-intake.json` records root's independent comparison of the controller's
v0.77.0 HTTP observations with the small downloaded deployment receipt. Every one
of 4162 results matched its expected path, byte length, hash and 200 first-attempt
status. This is not another independent full network audit; the controller remains
the owner of the retained network/browser observations. The intake’s then-pending
browser note is historical: `controller-browser-acceptance.json` retains the later
accepted controller receipt, hash-checked against its delivery message.

`build-input-inventory.json` inventories missing tracked inputs for an ordinary
build: 78 files /122,693,557 bytes across the declared distribution roots. It is not
a build pass or a storage guarantee. Large hydration/build remains coordinated
with the music release. No game version or PR is allocated to this candidate.
The separately committed landscape enlargement remains outside this cutoff.
Physical iPhone, Steam Deck, simultaneous fingers, full native journeys, listening
and final public qualification remain open.
