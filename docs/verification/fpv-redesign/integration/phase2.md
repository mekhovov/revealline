# Phase 2 controller integration

This branch merges frozen P2 source `0b0d2d793d61187f2544e82f3b0fd96b251e93e1` (v0.46.0) onto the accepted P1/main controller integration `a3b852f0ae526bee25dc843481c0337be663893f`. It preserves both existing actor-art build entries and the new presentation-system documentation. The added qualification workflow records this integration commit independently and runs all six source gates, plus production reproduction when that producer exists.

The controller commit is not the frozen release identity. Existing tags, release manifests and frozen bodies remain unchanged. Hosted qualification is pending until the exact integration push completes.
