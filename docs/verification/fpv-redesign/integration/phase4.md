# Phase 4 controller integration

This branch merges corrected P4 source `028ead24cfb31963d2b9bb733667c22993139ff4` (v0.48.0) onto P3 controller integration `d78cee446b97a340d2f5b7aaa3e955059d16a380`. It retains main’s actor/touch behavior and both inert historical-source formats. The five-action title and image-led mission preparation use the corrected native navigation fixtures.

The Field Guide now lives inside Workshop. To preserve main’s exact visible-opener return rule, Workshop and the title remain beneath the Guide: Back returns through Guide → Workshop → title, then the paused flight. Host assertions check each dialog and focused opener without changing saved-state or held-input oracles.

This controller source is distinct from frozen release identity. Tags, manifests and released bodies remain unchanged. Exact-source hosted qualification runs all six gates and production reproduction when available; its result is recorded separately after push.
