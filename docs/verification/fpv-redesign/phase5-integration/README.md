# Phase 5 combined-source compatibility

The independent v0.49 release-source qualification is pinned to `8af0d47d73e8633c771823fe024a3456441d62c1`. This merge concerns the ordered main integration and does not replace that source or its later immutable artifacts.

The integration retains the Phase 4 optional-chapter compatibility contract and current release workflow. The existing P5 compiled presentation, audio lifecycle, actor-body selection and motion/geometry arguments are preserved. Production ledger records, source/metadata history formats, campaign identities and original picture bodies remain unchanged.

The private v2 missing-snapshot fixture now excludes its matching duplicate entry from the separate source-history index inside that temporary fixture only. The runtime verifier correctly accepts an independently preserved exact source copy; production indexes and originals must remain intact. The complete source gates use the reviewed four-way deterministic sharder, with explicit exact commit/tree and file-discovery evidence. An absent P7 review-declaration command is recorded as not applicable to this earlier phase, not fabricated as a passing visual review.

The earlier full integration failures and local attempts are retained in task verification records. Exact committed PR qualification, including the real generated offline core, remains required before merging.

Local checks passed all five Studio cases and, in a separate final run, all nine production-history cases. The first history attempt failed because the deliberately sparse checkout omitted pinned metadata; 32 exact tracked metadata files totaling 202,783 bytes were restored without changing their contents, and the failed output remains retained. Scoped lint/format checks passed; the Studio browser module also passed an explicit browser-global ESLint configuration and syntax check. These focused results do not replace the full PR gates.
