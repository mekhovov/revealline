# Phase 4 combined-source compatibility

The original `v0.48.0` release remains pinned to `028ead24cfb31963d2b9bb733667c22993139ff4`. This note concerns its later integration with current main and earlier phases, not a replacement for that frozen release.

The earlier integration `615085f80e1b732556478624f07f3a56066c0104` failed its actual distribution test because the combined actor library exceeded the existing 64 MiB offline core. The integration applies the already qualified final Field Kit policy: Arcade R5 remains shipped and explicitly installable, but is optional for a fresh core cache. Couch retains base and checked installed maps after an unavailable optional request; Studio generic and exact-owner preview dependencies retain their actual identity and truthful failure states. Budgets, campaign identities, originals and solo saves are unchanged.

Two inherited modal fixtures are also corrected to enter the visible Workshop before Field Guide and return through both owners. The exact picture-ready waits for three featured-original cases use the existing bounded 180-second bulk-original allowance with diagnostics; ordinary input waits and application timeouts are unchanged.

The staged merge includes the current main release workflow and preserves the prior integration's actor-body geometry/motion handling. Runtime changes are confined to optional chapter compatibility and its tests; they are recorded separately from test-fixture corrections. Exact committed-source PR gates remain required before main merge. Earlier failed logs and focused results are retained in the task's integration verification records.

The complete modeled modal-navigation file passed all 11 cases on Node 20.19.5 (224.76 seconds). The adapted Studio loader also rejects an explicitly supplied incomplete owner at the real preview entry point before any fetch or canvas creation; an unavailable exact owner cannot become a generic board. These local checks supplement the committed-source CI and do not claim a physical-device or frozen-release migration test.

All seven focused optional-chapter checks passed: five Studio loader/entrypoint cases, one actual Couch base-map fallback, and one installed R5 original recovery after its shipped URL returns 404. Sixteen unrelated Couch cases were explicitly filtered. The installed-original case completed in 193.87 seconds. Lint, formatting, syntax and diff checks passed for the scoped change; generated full-source byte-budget qualification remains a separate CI gate.
