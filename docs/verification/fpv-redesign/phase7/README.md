# Phase 7 source qualification

These are working-source visual checks, not frozen-release receipts. The 9042 captures include the redesign, P6 surfaces and P7 preview/layout fixes. The 9045 Studio serves that same source with only authenticated external owner pack metadata from the immutable v0.44 distribution; it does not install packs or substitute another board. The 9043 controller captures concern accepted integration `604f5995` before Field Kit integration. The 9044 release catalog uses three explicit fixture versions, not a claim that those releases are deployed.

Normal standalone browser controls were used after the subagent's required in-app browser binding proved unavailable. CSS viewport and DPR measurements accompany the screenshots. Physical touch and gamepad hardware remain unverified. The completed review decisions were subsequently adopted into immutable production revision 13; the original observations and failures below remain historical evidence.

`asset-visual-decisions.json` consolidates explicit visual approvals for **133 unchanged or newly rechecked slots**: 30 sprites, 45 semantic glyphs, two title illustrations and 56 picture-owner bindings across 44 files. Every decision names its file hash and observed evidence. Shared glyphs without a live instance are approved as reusable library components; shared picture files distinguish individually inspected owners from metadata-verified variants. These decisions authorize asset readiness review, not a claim that all gameplay states or physical devices were tested. The root agent reviews the remaining recipes, generic picture defaults and fonts.

## Natural victory and Collection

Through the normal interface, chose Base game → First Signal → Deploy, resumed and pressed ArrowDown once to follow the introductory border-to-border route. The mission completed at **52.2%, 8,160 points, 42.67 seconds, Silver**. No progress or game state was injected. `first-signal-result.png` records victory; `first-signal-earned-viewer.png` shows the full current replacement; `collection-earned-first-signal.png` contains the earned thumbnail. The Collection viewer was opened and visually inspected on desktop and at 390 × 844 with Large text (`collection-full-viewer-*`); the picture and all actions fit. Earlier paused screenshots retain their literal observed state.

A fresh post-observer Continue → Resume → Pause → Restart → confirm check also completed normally. Preparing was observed at page t35.818s and Picture ready at 43.672s; Start then set running/ready with score 0 and three lives. This does not diagnose the previous whole-browser Blob stall. That capture remains in P5, recorded before its evidence freeze.

## Reveal review

All **44 exported scenes** were visually inspected in their 240 CSS pixel gallery specimens on the real read-only review page, measured 1440 × 900 / DPR 1. `reveals-gallery-row-01` through `11` cover every export. All 44 were then inspected in Studio: exact owner concealed BoardPainter at left and the selected replacement viewer at right. This covers one owner per export: 32 local contexts on 9042 and 12 external contexts on 9045. The 56 total bound owners include 12 additional variants whose metadata integrity was audited separately; this is not a claim of 56 independent visual journeys.

`reveal-context-review.json` records each prepared file hash, owner, screenshot, measured preview rectangle and limits. Native 44-file findings and 56-owner integrity are independently recorded in `reveal-native-review.json`. The initial missing-external-metadata failure on 9042 is retained; the authenticated metadata host subsequently allowed all external contexts to complete. The host's source pin receipt is `.cache/fpv-redesign/source-context-metadata.json`.

The Studio correction retains the selected replacement pixels while using the registered owner level, theme, seed, fit and sampling. Local metadata needs no request. External metadata loads only its exact pinned lightweight pack with bounded reads, hash/identity checks and cancellation. An unavailable owner produces an explicit error instead of another mission's board. Four new tests cover all 107 local owner records, authenticated external lookup, rejection and cancellation. The three focused files pass **8 tests** (`source-preview-tests.txt`); ESLint and Prettier pass for the changed source.

**Signal 06 needs art refinement before acceptance.** Fine upper-sky speckles remain visible at 240 pixels and native size, conflicting with the brief's broad clusters and no tiny speckles. `signal-06-context.html` reproduces an isolated held BoardPainter sample: the exact owner run advances down 420, left 140 and up 420 core ticks to 28.3% revealed. No profile, save, unlock or installation is used. `signal-06-partial-and-240.png` shows the result. Capture edges and the visible actors remain distinguishable in this held fixture; it does not establish all moving-actor contrast conditions. Snow, bright courtyards, blossom clusters and rail lines remain active-play contrast watchpoints.

**Resolved in Signal 06 revision 2.** The successor `scene-signal-06-768x576-v2.png`, SHA-256 `2444795bb19516949a98519f0e23f9cdf64b454f01a0bb297521d17b5af376da`, was inspected directly at native size, in the same 28.3% held run with a 240 pixel specimen, and in exact-owner Studio. The calm sky removes the grain while preserving the dish station, tree frame, path and valley depth. The successor is visually approved; v1 evidence above remains as the reason for revision. `signal-06-v2-partial-and-240.*` and `studio-signal-06-v2.*` record the follow-up. The reproduction HTML uses the current prepared manifest, so rerunning it now renders v2. The four `studio-player.*.rotors` pairs are additional held BoardPainter cosmetic-loop samples, not a complete motion certification.

## Native 200% zoom and responsive layout

Chrome's visible Appearance → Page zoom control was changed from 100% to 200%, then restored through the same control. The game measured **640 × 316 CSS pixels, DPR 2, CSS zoom 1, visualViewport.scale 1**; restoration measured 1280 × 633 / DPR 1. No hidden browser preference or CDP state was used. This is separate from the previously labelled CSS zoom stress check.

Title actions remain accessible by vertical scrolling. Settings tabs and Standard/Large controls remain readable without horizontal clipping. The first actual-flight check exposed Fullscreen overlapping Time and the mission hint crossing the header. The P7 stylesheet correction assigns Fullscreen its own 44 pixel position, widens telemetry, keeps the target on one line and reserves a separate hint row above the board. The complete arena remains proportional. Longer hints may ellipsize while running and remain available in the paused view.

`native-zoom-200-flight-fixed.png` shows Standard after the main layout correction; `native-zoom-200-flight-large-final.png` and its measurements show the final Large result. In the latter, menu, Fullscreen and Pause are each 44 × 44; HUD ends at y93.7; hint ends at y127; the complete 2:1 board begins at y128 and ends at y312.5 within the 316 pixel viewport. `native-zoom-200-pause-large-final.png` shows all pause actions fitting. Earlier `flight.png` and `flight-large-fixed.png` retain the overlap and intermediate stages, not final acceptance images. Native zoom was restored to 100%; final restoration receipts are included.

The 9044 fixture catalog was also inspected at 390 × 844 / DPR 1 in Standard and Large. Content stays within the viewport and action links remain readable; the Large actions measure 64 pixels high. No fixture Play/ZIP/Manifest link was treated as a deployment receipt.

## Controller merge source

The separate 9043 browser measured 844 × 390 / DPR 1. Stick right, swipe left and D-pad left fit, with Scan/Boost on the opposite side. D-pad targets are 52 × 52; Scan/Boost are 160 × 52; Pause and Fullscreen are 44 × 44. The steering-hand selector retained a visible native Tab focus ring and reported a successful save. Reload preserved D-pad / left / Always. The 44 × 40 menu target is an existing baseline limitation retained for this integration gate, not introduced by Field Kit; the 9042 redesigned menu measured 245 × 50 in Large picture state. This is layout/settings evidence, not proof of physical gesture behavior.

`screenshots.json` records local captures' physical raster dimensions, bytes and SHA-256. Production revision 13 now has 194 reviewed required slots and 99 optional historical source slots, with no missing or merely produced required slots. The separate declaration gate verifies that recorded state; each decision retains its actual observation scope.

## Saved-flight resume copy

The actual running-flight capture exposed a stale restored-session instruction. The app now replaces only that specific paused instruction with “Flight resumed.” after Resume succeeds. No simulation or storage operation was changed. Both steering-mode host cases failed with the old instruction before the fix, then the complete continuous/classic host set passed all 9 tests. The assertions cover paused copy, running copy, unchanged saved bytes and the existing checkpoint/continued-movement behavior. `stale-resume-copy-before.txt` and `stale-resume-copy-after.txt` preserve the regression evidence. The normal browser Continue → Resume path also shows paused/ready before and running/ready with the corrected message after (`resume-copy-paused.*`, `resume-copy-running.*`).

## Artwork-led result and native controls

The victory panel now draws the already loaded picture from the completed attempt. It does not look up a newer default or modify a save/award. Explicit legacy originals retain their existing fit; a missing managed still stays unavailable. Six result-picture cases and the existing flight/continuation tests cover that boundary. A normal First Signal win at 52.2%, 8,160 points and Gold supplied the native layout review. View picture → Results restores focus to View picture.

The accepted desktop capture is `artwork-led-results-desktop-final.png`: a 445.7×300 picture shares the reading region with score and medals, and all actions fit. `artwork-result-large-portrait-final.png` and `artwork-result-large-short-landscape-final.png` show fresh first wins with all five actions at 390×844 and 844×390. The compact image, heading, score and medals are initially visible. Portrait actions are 47 or 74 pixels high; short-landscape actions occupy their own right column. Earlier captures with hidden details and a collapsed 33-pixel reading region remain as failed evidence.

At 640×316 with Large text, details and actions scroll independently. The accepted repeat-win traversal (`artwork-result-large-640x316-keyboard-final.json`) verifies all four currently available result actions scroll fully into view at 74 pixels, with no clipped labels. Choose appearance was present and checked on the fresh first-win portrait/landscape cases. Read details → End → Escape and View picture → Results retain the expected focus. This is a measured browser viewport, distinct from the separately recorded native 200% zoom flight check and from physical-device testing.

The corrected Studio button specimens show the actual primary, danger, icon, tab and chip roles. `component-native-observations.json` records the primary default/focus/pressed/selected/disabled/error states and related variants; `component-native-tabs.json` records ArrowRight selection, roving focus and the matching labelled panel. These are bounded native library specimens. Actual game settings, dialogs, galleries and controls have their own screen evidence; a specimen does not certify every event in a running game.

## Revision capacity and generation briefs

The complete-collection regression first failed with “Too many assets” when it retained the production history, appended review successors and replaced all 194 required slots. The authoring record cap is now 2,048, with the existing 4 MiB manifest, 32 MiB bundle and 4 MiB per-file ceilings unchanged. A successful exact import/export preserves every prior record and original file. The final ledger's capacity check is repeated after review adoption; an older Studio may reject a future collection that exceeds its original record cap.

Font and audio prompts now request files in their own medium and preserve a custom current production brief. Fonts require actual WOFF2, rights, glyphs, metrics and bilingual layout; short cues require measured WAV output, music requires a bounded encoded loop, and playback retains explicit activation/Stop/mute. The audio asset decisions concern the existing functional original recipe and observed audition controls. No listening-panel or physical-speaker judgment is implied.

## Integrated preview parity

The source was advanced to the reviewed P6 integration `055599d47365a4eb8a919de2ae7d651ce4c080cc`, retaining the main enemy-body/skin and touch-control changes. The unpublished P7 draft was preserved as a portable original, imported through the existing conflict namespace adapter, then reproduced from integrated inputs. `integration-draft-adoption.json` records source identities, exact prior-record retention and a byte-preserving round trip. `asset-visual-decisions-integrated.json` rebinds the earlier 133 visual decisions only after comparing their full file, geometry, recipe, description and source/creator/license/prompt identity. It does not invent new playthroughs.

A fresh integrated First Signal attempt naturally reached 52.2%, 8,160 points and Gold in 0:07 (`integrated-natural-victory.*`). All five desktop actions measure 44 pixels high; the image is 445.7×300. The full viewer returns focus correctly. Field Guide → Close restores `shell-guide` in Workshop; Workshop → Back restores `shell-workshop` on the title (`integrated-workshop-guide-return.json`).

Two preview gaps were corrected. Studio Context now uses the actual runtime `imagePresentation` adapter for custom pivots and rotor anchors, including distinct enemy slot IDs. An asymmetric pivot and mixed rotor blade counts are exercised through real BoardPainter at wide and narrow widths. Field Guide now uses the compiled FPV enemy sprite and geometry instead of separately loading legacy FPV artwork. Its seven native selections are recorded in `integrated-guide-roles.json`; `integrated-field-guide.png` retains the old artwork observation and `integrated-field-guide-compiled.png` records the corrected preview. The guide regression also checks unchanged contact radius, no redundant legacy download, other-theme isolation and fallback when a release slot is absent.

## Final asset readiness

`production-review-adoption.json` binds all 194 explicit decisions to production revision 13. It preserves every prior asset and theme record, retains the existing three font approvals, and appends 191 immutable review successors. The exported 6,534,494-byte bundle has SHA-256 `9bf459cbbde311413887feae897d1905f9cab4645c8a3a5444441eec4e79ce99`; its 932 asset records include historical revisions, not 932 required assets. Reproduction yields 131 runtime files totaling 4,007,816 bytes. Exact bundle round trip and historical identity checks passed.

After reproduction, `node scripts/check-field-kit-readiness.mjs` requires every selected required slot to be declared reviewed with nonblank model-valid evidence. It preserves optional historical sources, reports exact unresolved IDs, and binds the result to HEAD's production ledger SHA-256 and source commit/tree. The gate verifies recorded declarations; it does not independently prove visual quality.

Canonical public v0.44, v0.45 and v0.46 journeys are recorded separately in `public-v044`, `public-v045` and `public-v046`. All three completed normal play, saved continuation, earned-picture viewing, offline verification and cold offline play. Older frozen releases initially reached their existing 60-second preparation UI timeout while the worker continued installation; a normal retry completed verification. Those attempts remain in evidence. The v0.45/v0.46 checks also demonstrate separate release profiles and workers, not automatic migration. Frozen bytes were never changed to repair a current-source issue.

### Actual offline build budget

The initial combined-source build failed the existing 64 MiB guard. Declaring the unchanged Arcade R5 pack optional through the existing indexed-pack policy produced a passing working build: **568 core files / 65,923,013 bytes**, below **67,108,864 bytes**, with **1,185,851 bytes** remaining. All 614 distribution entries (322,899,778 bytes) remain present, including the exact 11,708,176-byte R5 original. The generated note identifies the optional packs and their online installation requirement. `offline-budget-build.json`, the full cache/manifest and before/after logs bind this result. This build had `sourceRevision: null`; the later frozen-source build remains the release authority. Couch startup and Studio preview recovery are separately checked for the newly optional dependency; no gameplay or cache limit was changed.

### Optional chapter recovery

The new optional R5 policy is accompanied by tested recovery in Couch and Studio. Couch keeps the online featured default; a failed optional request shows a notice and permits base or checked installed maps without writing solo progress. Forty-four compatibility tests and two focused failure/installed-original tests passed. `optional-r5-couch-native.json` and captures show the real source lobby, unavailable notice, base round startup and Pause using an HTTP503 fixture; this is separate from the later published worker/offline journey.

Studio player previews now read presets independently; an exact owner with complete metadata fetches no mission pack. Generic contexts retain online fixture order and use the same code-owned metadata when optional R5/R1 downloads are unavailable. All seven enemy types retain the same level/theme/checkpoint; absent exact owners fail clearly. Thirteen focused tests passed. `integrated-r13-studio-context.*` records the reviewed release loaded through the normal Studio action and its selected detailed body preview.

### Duplicate immutable-source fixture

The e903 candidate's PR preflight found that the private v2 missing-snapshot test still had an independent valid copy in its source-history index. The verifier correctly resolved that retained copy. The corrected test removes only this duplicate from its private fixture before checking missing, changed and symlinked v2 snapshots. Both production indexes and originals remain unchanged. All nine focused cases passed; the failed hosted preflight remains in the qualification archive. This correction requires qualification of the new exact commit.
