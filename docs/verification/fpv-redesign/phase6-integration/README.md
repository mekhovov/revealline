# Phase 6 combined-source compatibility

The independent v0.50 release-source qualification is pinned to `d52b634a231ce0907b83ea681e1e1aaf02387485`. This ordered main integration preserves that separate release authority and the four 0.50.0 version fields. Existing frozen editions and their artifacts remain immutable.

The integration retains the preceding optional-chapter contract, source-history fixture isolation and exact-source qualification workflow. Phase 6's categorized Settings, Couch presentation and audio, shared supporting-page hosts and existing input/save ownership remain intact. R5 is shipped and explicitly installable outside the fresh offline core. Couch retains base or checked installed maps when the optional request fails; Studio preserves exact-owner requirements. Production assets, immutable history and campaign identities are unchanged.

The Creator and storage-retention host fixtures now open the visible Game Data category through normal keyboard navigation. They assert that hidden categories exclude their controls, preserve the paused checkpoint and restore category/focus ownership. This corrects earlier fixtures that attempted to operate controls hidden by the implemented category layout; it does not relax the interface behavior.

Earlier integration failures remain retained in task evidence. Focused local checks and independent source comparisons are recorded separately from full committed-source PR qualification. The latter, including the generated offline core, remains required before merge. Final public release delivery uses the main-only publisher and separately verified immutable artifacts.

All four focused Creator/retention cases passed, with zero skipped cases. Scoped lint, formatting, syntax and diff checks passed. The independent review compared 277 exact Git blob/mode identities, including every compiled production file, the ledger, Settings handlers and actor-motion integration. These source comparisons establish preservation within the stated scope; they do not replace native browser or physical-device checks.
