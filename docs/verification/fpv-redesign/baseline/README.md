# Reviewed source baseline

Captured from the exact `fe9961e` Git blobs, served from an isolated copy at `http://127.0.0.1:9041/game/`. The source page reports DEV because it is not a frozen release; its package baseline is v0.43.0. Runtime source was not modified for capture.

Desktop captures are 1280×800; portrait captures are 390×844. JPEG screenshot bytes are retained unchanged. The first portrait title image was taken before viewport settling; it was replaced with a verified 390×844 capture before this evidence was committed. The mission Back route returned to briefing, so that image is correctly named briefing-phone.

The title has ten primary links before a save and additional continuation/restart actions afterwards. Its phone menu extends below the visible viewport. The mission browser is text-heavy; settings combines audio, display, input and storage in one long form. Flight and Pause record the existing HUD and opaque gameplay concealment. These are observations, not changed mechanics.

This is desktop-browser layout evidence. No physical touch, controller, device performance, localization translation or offline claim is made by these captures. File hashes are in inventory.json.
