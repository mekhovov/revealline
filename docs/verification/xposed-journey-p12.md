# P12 Apex Aurora — isolated candidate evidence

Status: local implementation in progress, not a released or human-qualified phase.
Source follows the local P11 technical checkpoint `c74d89ac`; it is not an
accepted release baseline. The release coordinator retains publication ownership.

## Candidate scope

Four band-twelve core missions and one optional Remix combine established rules.
No runtime schema, actor tier, player movement, timing recipe or mandatory mechanic
is added. All use the shared MapDesignV3 / MissionDesignV4 compiler and pinned
level.v8 successor; ordinary missions have an explicit null encounter. Home signal
uses the same three-shield Sentinel recipe introduced in P11.

- Crossing complete: terrain enclosure versus a direct marked crossing.
- Final broadcast: protected T returns, erosion and selective trail impacts.
- Returning light: arrow efficiency versus an awakened reclaimed-ground threat.
- Home signal: wing connections and a shield-linked permanent release dock.
- Dawn circuit: optional durable outer loop versus erodible inner spokes.

Each has authored counterplay, capture consequences, an optional mastery goal and
proposed—not measured—duration. Contact bonuses occupy optional detours; the first
returns need none. All 30 preset/steering full routes now also collect no bonus.
The core sequence ends at Home signal without automatically assigning its Remix.
Studio import remains Inspect → explicit Apply into a local draft; no public
Journey enrollment, award or publication is performed.

The reference crosswalk records direct inspection of all three assigned finale
stills, exact hashes, original adaptations and non-final dispositions. An initial
676-cell unoccupied Remix chamber was rejected and its field-retaining actor
placement corrected before route qualification. Current diagnostics contain no
unintended auto-fill or blocked-relay warning.

## Initial verification

The candidate/preview-readiness cohort passes **16/16 on Node 20.19.5 and
22.22.2**. This includes all 30 preset/steering first returns without a collected
bonus, five seconds of initial decision space without life loss, constant earned
coverage denominator and public replay. All initial field components have an
actual retaining enemy. Ordinary and encounter previews match CLI manifests on
all three presets; the core terminal does not automatically continue into Remix.
Outputs: `.cache/p12-candidates-node20-r1.tap` and
`.cache/p12-candidates-node22-r1.tap`.

## Full-route and recovery evidence

`apex-clear-routes.json` now pins 30 prepared Solo no-loss clears and 30 equal,
independent paired-board races across all presets and both steering options.
All routes collect no contact bonus, keep the denominator unchanged, open every
gate before victory and actually use a connector where one exists. Non-encounter
routes reject reaching quota while required objectives remain. Home signal checks
the explicit shield → transition → exposed → release sequence instead. Public
replays reproduce every final checkpoint. Route tests pass 13/13 on both Node
20.19.5 and 22.22.2; with cue/goal-observer checks the Node22 cohort is 28/28.

An initial legal Home signal clear never used its release dock and was rejected.
The search now checks idle victories as strictly as cut-triggered victories and
can choose a legal walk over an opened connector without forcing another cut.
The accepted route replaces that counterexample; raw search wins are not evidence.

Five additional deterministic seeds per combination provide 150 further no-loss,
bonus-independent replay samples. Session tests restore 32 snapshots across live
trails, captures, opened connectors, emitter warnings and Sentinel stages, then
finish with identical checkpoints and public replay. Changed level rules are
rejected; a fresh attempt resets objectives, gates and encounter phases. These
checks pass on both Node20 and Node22 (8/8 seed/lifecycle cases on Node22).

Optional goals have a read-only test observer and five negative-boundary tests:
landing visits must be connected both when visited and at completion, every
lethal cell must be reclaimed, and a connector visit must follow opening and
precede victory. Fabricated unit arrangements are not route evidence. Full
mastery and delayed-start qualification remain in progress. Optimized clear
durations are feasibility evidence, not human pacing or enjoyment measurements.

## Scoped native inspection

At exact source `b5b8f803`, local read-only port8800 Studio Inspect left the existing
draft unchanged until Apply; applying produced four core missions plus an optional
Remix in two packs/campaigns. Returning light's exact Solo preview accepted
Start → Right, closed for 1.2% / 250 points with three lives, and stopped the craft.
The reclaimed arrow strip disappeared while adjacent field arrows remained. The
craft remained alive on the return platform through the later lane warnings.
Home signal's ready screen correctly showed “3 shield relays + core”, all-three
capture instructions, the eight-cell release and the isolation alternative.
Closing preview returned to the same draft with Play focused.

This inspection exposed an ordinary emitter warning incorrectly using the themed
name “Sentinel”. A shared read-only caption now names it “Lane emitter” for
foundation-era editions, preserving historical theme labels. Unit tests cover all
four authored rulesets, unknown/wrong actor events and checkpoint immutability.
The native source above predates this correction; native recheck remains due.
No full native clear, controller/touch or independent human qualification is claimed.

## Publication and artwork capacity

All candidates use temporary inherited presentation with no bound original assets.
The active v0.76 source/release remains frozen. Release capacity evidence received
from the offline owner: the real optional Journey catalog contains 42 originals,
107,594,416 bytes. PR206 aligns its count with the shared 512-asset limit, while
the existing aggregate original-byte metadata ceiling remains 128 MiB, leaving
26,623,312 bytes. This is not a core-cache or managed-media budget.

Before adding future campaign art, qualify the aggregate original-byte budget and
the actual generated offline marker on the accepted integrated source. Do not
silently disable offline preparation, raise a limit in the frozen release, or
claim a hypothetical compressed size as shipped evidence. Artwork generation,
bulk builds and broad hydration remain on hold under release coordination.

## Remaining gates

All-preset mastery routes; delayed starts; pacing review; original visual/audio identity;
native/accessibility and human evidence; distinct Team mission qualification;
phase PR/review/version/immutable release/Pages verification. No whole-phase
completion is asserted by first-return or compiler tests.
