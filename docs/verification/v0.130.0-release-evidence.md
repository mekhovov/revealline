# v0.130.0 release evidence boundary

This documentation-only pull request supplies the fast-release pre-merge validation record for the
already merged and frozen v0.130.0 source at commit
`0e678f42579c957eb66de0d4bb842e56e324842e` (tree
`7359433b6db660031cb5ceea6992d0f49cf3512b`). Its parent is that exact source. The document is
excluded from the frozen artifact and is the sole approved PR-to-frozen difference.

The runtime correction makes controller Confirm an explicit single activation transaction, covers
Steam Input compatibility activations through release grace, and groups South/West Confirm aliases
behind one neutral-to-pressed edge. The automatic offline inventory contains 1,078 files and
64,015,743 bytes; Homeward Skies remains hosted, manifest-bound and explicitly installable without
being part of the automatic service-worker precache.

Automated suites remain waived by the repository policy and are not claimed as passing. Mandatory
validation, lint, formatting, native formatting, motion syntax, production reproduction/readiness,
source identity, frozen artifact inspection, immutable tag/assets, Pages publication and public
acceptance remain separate required evidence. Physical Steam Deck verification remains the final
hardware-only gate.
