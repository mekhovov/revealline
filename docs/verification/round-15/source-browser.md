# Round 15 source browser observations

These observations were made on 12 September 2026 against the changing source at isolated `127.0.0.1:8799`, using the app browser and actual controls. They are development observations, not evidence that a later frozen distribution contains identical files. No browser storage mutation, fake browser Gamepad API override or direct simulation-state edit was used. The new Controller practice page supplies its documented virtual-pad messages to the actual game/router.

## Solo baseline and controller journeys

- Ordinary keyboard/touch UI completed First Signal at 52.2%, 8,160 points and three lives. Opening its Collection card and returning focused the replacement card inside the reopened Collection dialog.
- Virtual Connect then South joined without starting. A separate released South press started a Scout run with its ability still Ready. Menu paused with Resume focused; a subsequent Menu resumed.
- North opened the real hangar. D-pad selected Next craft. South began an edit; Down previewed Light carrier while the native select remained Scout. Back cancelled it. Repeating the edit and confirming committed Light carrier exactly once; its description updated. D-pad and South activated Switch & continue. Flight resumed as Light carrier with zero of one charges and no automatic field. A held Down completed the immediate-turn practice at 50.0%, 7,820 points, three lives, about 24 seconds.
- A separately loaded Grid + buffer Scout practice completed at 52.2%, 8,160 points, three lives, about 13 seconds using a held virtual Down. The release gate stayed visible after the held movement reached the result. After Release all, D-pad moved to View picture, South opened the whole picture and East returned to results without starting another attempt. The result remained explicitly practice-only.
- The practice result's Try it yourself action returned to a fresh ready screen while retaining PRACTICE / NO CAMPAIGN REWARDS. Another South started that practice. Disconnecting during held movement immediately paused it. Reconnect alone kept it paused; a deliberate South join focused Resume and still did not resume.

The immediate and grid attempts were full ordinary-input flights. Their different class, timings and routes mean the scores above are not a steering-mode equivalence comparison; normalized-input equivalence is tested separately through the core. No physical pad was connected.

## Corrections from this pass

The joined controller originally showed its ring only after a navigation gesture; joining and entering a new menu now visibly engage a valid focus without activating it. A selection preview originally lived inside the label and expanded the select's accessible name; it is now a sibling after the enclosing label. Browser snapshots showed Next craft remained its name while the separate status described the pending choice. Five additional navigation regressions cover these and host-restored focus behavior.

Read-only review identified two app branches to correct: ready/result screens needed unconditional release on blur/hidden/native suspension even when `pause()` returns early, and a completed campaign selected in practice needed its overview flag suppressed so its new flight receives flight input. The current source contains both corrections. Module tests do not execute these app branches; completed-campaign and final-version browser checks are recorded below when exercised.

The source tab's isolated profile was deliberately replaced through the native save-file picker with the previously verified 11,398,487-byte complete backup from Round 14 (SHA-256 `e4dc9786a842930975d48060dad8550d6a4c0090d8a98114e85b80e253b5da57`). The UI reported eight pictures, three campaign histories and four packs, providing completed Homeward content for the next regression. User profiles on port 8767 were not changed by this QA import.

The initial [grid-turn picture screenshot](screenshots/grid-controller-picture.jpg) shows the real practice iframe and full picture. It is a browser screenshot, not new artwork. Error/warning logs sampled after these journeys were empty. Device performance, physical controller mapping, Bluetooth, native wrappers and subjective enjoyment remain separate checks.

## Final source and candidate checks

After the fixes, the existing fully completed Homeward campaign was selected from the practice iframe’s normal Campaign selector. It opened Home Beacon in ready scope, then a deliberate controller join and separate Confirm entered **flight**, with Pause and Hangar usable. All three Homeward cards retained their existing gold badges. This exercises the completed-campaign overview correction; it is not another full Homeward completion.

The lab now contains ordinary navigation clicks inside practice, while downloads and same-document fragments retain their behavior. Clicking the game brand inside the iframe kept the current paused practice and showed the parent-header exit instruction. A named-frame boot invariant separately rejects a query-stripped solo page before writer acquisition. The isolated HTML regression fixture used a real named iframe with an unflagged game URL to exercise that path; it contains no script or profile mutation.

At logical iframe sizes **390×844** and **844×390**, the hangar editor retained the focused select and a separate pending-choice status. After cancelling at landscape size, Down brought **Switch & continue** into view with its ring. Screenshots: [phone editor](screenshots/phone-hangar-preview.jpg), [landscape editor](screenshots/landscape-hangar-preview.jpg), [landscape action](screenshots/landscape-hangar-action.jpg). The parent lab measured 1280 CSS pixels wide with 1280 scroll width; its displayed phone frame measured 390×844. The browser automation DOM facade does not expose nested document geometry, so these iframe checks use rendered screenshots and accessibility snapshots rather than invented inner scroll-width measurements.

The eight-picture fixture’s Collection opened through the visible UI. D-pad navigated from Close through Search to Home Beacon. South opened the original picture; East returned to the same Home Beacon card with its controller focus restored. The collection remained eight entries. This actual journey covers one-page gallery return; page-two preservation is covered by DOM tests rather than claimed as a browser observation.

The first packaged candidate (`.cache/round-15/candidate`, version v0.5.0, no source revision marker) contains 107 manifest assets. Its production-policy solo UI completed the ordinary first mission with Tap steering and visible Down input. Sampled error/warning logs were empty. At outer browser viewports 320×640 and 844×390, document scroll widths matched the viewports, and Start remained 44 CSS pixels high and fully visible. Bounds were respectively `(27,328.80,266,44)` and `(247.67,283.24,307.33,44)`. The [DOM measurement record](candidate-viewports.json) retains these bounds. The browser capture returned incorrectly scaled/cropped screenshots while the viewport override was active; those unusable captures were discarded instead of presenting them as visual verification. The viewport override was reset.

The [source gates](source-gates.md) passed **700 main tests and 70 Motion Lab tests** against the unchanged 227-file source inventory. Microsoft’s [input guidance](https://learn.microsoft.com/en-us/xbox/accessibility/xbox-accessibility-guidelines/107) and MDN’s [Gamepad API guide](https://developer.mozilla.org/en-US/docs/Web/API/Gamepad_API/Using_the_Gamepad_API) were rechecked during this pass: whole-menu digital input, straightforward individual button actions, configurable controls and explicit browser/device verification remain the direction. These references do not certify this game’s accessibility or hardware compatibility.
