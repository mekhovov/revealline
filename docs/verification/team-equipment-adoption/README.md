# Reviewed Team equipment and exact artwork admission

This change adopts five reviewed anchor/core images and the generated presentation revision60. Current Team attempts use exact60 artwork bindings; held revision59 attempts retain their approved picture identities. Both starter arenas and valid historical coverage/multi-core imports remain supported. Arbitrary revisions, wrong collections, changed pictures and reserved-pack lookalikes fail before picture reads.

The entire canonical58 and prior59 history, all132 original payloads, raw54 runtime and its dependencies remain intact. The generated bundle is `9c8bc1302fe9f31259c0de5e8652d314b5cc1e76eb480481551326974d52b263`. This changes nine generated paths against the source-only Team candidate: five images, three manifests and the bundle. All137 generated outputs reproduce exactly. Five equipment images are reviewed;37 Team recipes remain source-stage pending their separate functional review.

The earlier [five-image review](../team-equipment-five-review/README.md) records a real version-one transfer capacity failure. Its adoption prerequisite is now resolved by the separately reviewed bounded transfer change: the original complete two-successor fixture passes without deleting history or increasing limits. The adopted bundle itself still fits version one; larger valid documents may use the version-two format.

Five complete retention/history/equipment files qualify25 checks per Node20/22. Five complete admission/actor/host files qualify89 checks per Node20/22. Node20 combines fresh corrected complete files with audited unchanged-read passing results; Node22 reruns each full cohort. All34 static checks pass. Generation binds470 exact inputs with no mismatch. See the [qualification record](qualification.json) for source and receipt identities.

The previous59 preview also received ordinary keyboard testing in the real Team host: both arenas, each player's short capture, Retry, paused Help, explicit Resume, arena replacement and Support cooldown. All285 requests returned200 with no source-byte mismatch or console warnings. This is scoped candidate evidence, not a full victory/rescue, final60 native-byte, physical-device, offline or public qualification.

The separate Studio rescue-display fix is outside this PR. Its consumer tests and integration remain independent. Final integration must assign a fresh game version, run the six source gates plus build/production/browser checks, freeze immutable outputs, deploy through the PR pipeline and verify public play before acceptance. No simulation, scoring, capture or rescue rules change here.
