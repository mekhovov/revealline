# Shared music and Journey composition

Candidate only; no phase or public acceptance.

Combines f009474a Journey integration and a3cb15e3 shared Couch music, whose
actual common source is aadd855e. Five conflict sections in the Versus host
preserve both owners: candidate map/picture/preset selection and exact music
campaign context; music first, then catalog/backup/chooser scopes and Back.

Candidate Versus records now use baseCampaignKey for presentation-only music
assignment, matching Solo across all thirty mission/preset contexts without
changing their execution identities.

Node20.19.5 passed32 tests in the actual music host, candidate Versus host and
authored context suites. The earlier33-test composition cohort predates the
new keyboard regression; do not add their overlapping counts. A duplicated
showModal/close pair introduced by automatic helper merging shadowed native
modal-stack behavior. It was removed; full host tests now exercise Escape and
opener restoration. A subsequent test exposed its own omitted frame boundary
after Settings Back: advancing the actual host frame makes visible controls
current before the next deliberate command. Production dialog behavior was
not weakened. Initial failures remain in the composition cache logs.

The modal behavior follows the [W3C dialog pattern](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/):
focus enters the active dialog, Escape closes it and focus returns to the opener.
This composition still needs the final P01 correction merge, native combined
journeys, complete final-source gates, version allocation and public verification.
The earlier packet's native MP3 transport evidence is not a listening or offline
qualification of this composed source.

## Final P01 corrections composed

The automatic merge of041f1a71 preserves explicit untimed authored Versus, saved
old-edition guidance and the original reference crosswalk. The composed
Node22.22.2 host/core suite passed37 reported tests. The separate restored
context/keyboard cohort passed2 tests (one overlaps the full host cohort).
Sparse checkout removed supporting motion/fixture files during merge; startup
refusals remain retained and exact index/original inputs were restored before
the passing runs. The extra context test was restored and run explicitly.
Reviewed materialized paths are now explicit sparse entries so a later merge
keeps the test graph. No production validation was weakened. Native combined
hosts and complete release gates remain; this is not public acceptance.
