# Integrated Journey menu return and Continue caption

Runtime source `41d0b908e4c7c5f1cfbbd24994f2e96381332b28`, tree `e84b3f14fd60076bd0c11b7c2b080ca31c37f71c`.

The exact composition passed **42/42 tests on Node20.19.5**, zero failed/skipped/cancelled, across five complete files. It combines authored-mode entry, first Start, Missions return and successor caption. Prior Studio and shared music evidence remains separately scoped.

Native browser verification passed title Missions → Back with exact focus, a legal first win → truthful successor Continue, checked Solo → Versus → Solo retention, and nested Progress backup → Missions → paused flight without changing its0:12/0%/3lives/00000 state. The run used keyboard except the recorded pointer opening of paused Missions. The original HTTP response log was reconciled against every served immutable Git blob. See `native-observations.json` for precise observations and limits.

The initial lint invocation found the unrelated root configuration because the sparse checkout omitted its own config. Preserve `lint.log.gz`; restoring only the exact committed ESLint/Prettier config and selecting the absolute local config produced the passing `lint-r2.log.gz` and `format-r2.log.gz`. No production change was made to hide that setup failure.

The source loader uses a bounded32MiB Git output buffer for the existing11.7MiB fixture. Observer code is retained as `.txt`, logs/diff as lossless gzip, with original and retained hashes in `pins.json`; these are evidence, not additional production tests. The local server and temporary tab were closed after verification.

Full final-source gates, production reproduction/readiness, ordinary build, immutable freeze, public deployment and affected public journeys remain mandatory. Physical controllers/touch, listening, actual offline recovery and human challenge/enjoyment remain separate outstanding gates. This does not complete a parent programme phase.
