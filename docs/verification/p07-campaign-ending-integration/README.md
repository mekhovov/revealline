# Campaign completion integration evidence

Held source integration on `fac78c03`; no release or full-phase acceptance.

The earned Legacy final result now names campaign completion and opens Browse campaigns as a child. Back preserves the result and focus. Existing Journey routing remains separate, including Legacy attempts restored inside a Journey-enabled shell.

All 142 unique scoped regression cases passed on each of Node 20.19.5 and 22.22.2 across unchanged-source runs. The first seven-file cohort passed 133 and failed nine because the sparse checkout omitted tracked artwork. The affected twelve-case file then passed eleven, with a second missing picture. Restoring that exact Git dependency yielded twelve passes. Original failed runs are retained alongside corrected runs; no single clean 142-test run is claimed.

Native browser verification used legal keyboard cuts through an installed two-map fixture, final Browse/Back, picture view, Retry, and 568×320 Large/Plain. A restored Legacy attempt also completed inside the Journey shell and returned from its Legacy picker with result/focus intact. The fixture establishes navigation only. Physical devices, real controllers, production challenge, offline and public qualification remain separate. See the observations and hashed receipts.
