# Loss difficulty integration evidence

Held source on `76586bd1`; no full-phase or public-release acceptance.

The loss result offers Standard/Gentle through the existing preference authority. Changing the preference preserves the accepted failed attempt and picture; explicit Retry owns replacement. A later selection cancels pending Retry. Practice, course, scenarios and owned automatic-reset Journey attempts retain their existing rules. Short-landscape failure details and commands have separate scroll regions.

Node 20.19.5 passed all 62 cases in five relevant files. Its initial command used a nonexistent filename and aborted before tests; this log is retained. Node 22.22.2 ran 52 cases across the four existing file arguments, then passed all ten actual difficulty-host cases separately. No assertions were weakened. ESLint, Prettier and diff whitespace checks passed.

The integrated native browser check used an installed owned fixture, legal self-contact loss, 568×320 Large/Plain, keyboard difficulty selection, explicit Retry and Pause. Changing Standard to Gentle retained the loss result; Retry started at zero with five lives. The reading region remained positive (125.625px), Retry measured47px high and the selector45.5px. Actions scroll when focused; the initial view does not display all details simultaneously. No console warning/error appeared.

This proves a bounded navigation journey, not hardware certification, production balance, whole-source qualification, exact native storage bytes or deployment. Shared touch/device release remains a separate frozen candidate.
