# Frozen v0.8.0 browser and offline checks

Verified 12 September 2026 in the Codex in-app browser against immutable source `8410d814dbcc7b3aef53e11c6f94d384d01cce05` and `releases/v0.8.0/site`. The owned release server used port 8810 and CLI security headers; its separate origin kept QA progress away from the player profile.

Settings → Prepare offline play reported **113 files verified**. The independently checked inventory contains **20,086,392 bytes**, build ID `176cbe39c36029e758c2016ade1e8eed770fccc62266a8282f4d09688abefe92`. Homeward Skies was installed using the ordinary expansion control.

The normal file picker imported the Round 14 legal-input River Switchyard / Grid + buffer suspended flight. Its recorded route starts as Light carrier and switches to Heavy carrier. This fixture supplies the earlier input prefix; the check does not claim a complete manually flown attempt. The pause checklist reconstructed both supply pickups, both banked signal crossings and the accepted hangar switch, with the final mission completion pending. Tap steering and Save & pause were used before stopping the server.

A separate curl probe returned connection error 7. The server remained stopped throughout the following checks:

1. The game reloaded from its cache and offered Load saved flight. Loading reconstructed the same Supply Line checklist from the recorded input prefix.
2. Resume and the ordinary Move up control closed the final cut. The result was **71.1% revealed, three lives, 11,910 points and gold**. Replay verification reported “Supply Line seal earned and saved.”
3. Collection opened the completed River Switchyard picture with **Supply Line · Light carrier → Heavy carrier · Grid + buffer · seed 1**. The [offline picture screenshot](screenshots/frozen-offline-seal.jpg) records it.
4. A second reload retained the reward. Export player library produced a **2,388-byte** `xonix-library.v2` file with exactly one picture, one score and one mastery record. The [export digest and identities](frozen-offline.json) record this persistence check without embedding the profile.

Sampled game warning and error logs were empty. The owned QA tab was closed, and its server remains stopped. The user's port 8767 server was kept running; a separate v0.8 player tab opens Homeward Skies at Copper Orchard with Fiber relay, without imported QA progress.

[Source browser checks](source-browser.md) cover both equipment goals, ordinary completion without a seal, exact practice isolation and responsive reading controls. The [packaged candidate](candidate-browser.md) complements their steering coverage. [985 unique source tests and required static gates passed](source-gates.md). [Independent archive verification](integrity-notes.md) reproduced all 120 loose files and confirmed all eleven older releases and tags unchanged.

These are browser, deterministic simulation and archive checks. Physical controllers, native hardware, store signing, public hosting and human judgments of enjoyment remain separate work. Pack-authored goals and further planned content are not claimed complete by v0.8.0.
