# Chapter reward source browser checks

Observed 12 September 2026 in the Codex in-app browser at the owned source origin `http://127.0.0.1:8814/game/`, displaying v0.10.0 / DEV. The [gate inventory](source-inputs.json) records the final tested source; all 266 inputs also matched after the candidate build.

## Existing progress and reversible adoption

The player installed Equipment Workshop through the ordinary expansion control. Saves & loads imported the actual three-picture, three-score, three-seal library exported from the v0.9 candidate browser. Selecting Workshop showed its complete chapter overview, both final appearances enabled, and ordinary available reward rows. No new-unlock result or celebration was emitted. Pathfinder correctly described three different missions. Undo restored the prior empty player library while keeping the installed pack.

## Live milestones and repeat play

Each live attempt imported an unchanged Round 19 legal suspended fixture through the file picker, resumed it, and supplied the final direction with Tap steering. Those fixtures contain recorded normal inputs ending 24 ticks before victory. They supply the earlier route history; these checks do not claim manually flying each full route.

1. **Neon Switchboard / Immediate:** Resume then Up finished at 71.1%, three lives, 11,910 points and gold. Its first new clear named the five first-tier appearances. Despite Garden of Threads still being uncleared, the earned Neon Switchboard mission button was enabled. Choose appearance focused `body-select` while retaining the won state. Selecting Skyline FPV kept 11,910 points and the active Heavy carrier ability, and disabled automatic appearance matching.
2. **Garden of Threads / Grid + buffer:** Resume then Right finished at 74.1%, three lives, 12,590 points and gold. No new tier message appeared. The flight deck and Collection showed **Chapter explorer: 2 / 3**; its row named Night signal FPV and Delta interceptor and said to finish one more mission. [The default-size screenshot](screenshots/chapter-progress.jpg) records this actual presentation.
3. **Clear Ledger / Grid + buffer:** Reduced effects was enabled before resuming. Right completed at 73.1%, three lives, 11,740 points and gold. The result named exactly Night signal FPV and Delta interceptor; both options became enabled. Keyboard Enter on Choose appearance focused the selector. Explicitly choosing each final body retained the won state, 11,740 points and Impact craft ability. The later [result screenshot](screenshots/final-appearance-reward.jpg) records the same reward after the placement correction below.
4. **Clear Ledger / Immediate repeat:** A different recorded attempt and steering policy completed the same mission. Its new ordinary score and equipment record were saved, but the distinct map count remained three. The reward text was empty and its Choose appearance action hidden; no duplicate tier was announced.

The three-clear profile exported **three pictures, three scores and three seals**, 5,840 bytes, SHA-256 `0a3ebc3dc4948b9a208b2460fac9659798d89cdc7f42089128dd404825f3f10b`. The repeat profile held **three pictures, four scores and four seals**, 7,065 bytes, SHA-256 `e7c5b758cb66604ccc468dfb59171c25b00cde4ff1a703bdd8d614e225b66416`. Both retain three distinct Workshop clears. [Profile checks](profile-checks.json) preserve paths, counts and digests. Source warning/error samples were empty.

## Compact controls and verification limits

Final DOM measurements at actual CSS sizes **320×640**, **844×390** and **1280×720**, DPR 1, showed no horizontal overflow in the document, Collection dialog or either reward row. The Collection action measured **44 / 45 / 45 CSS pixels** high. The next-mission, picture and appearance result actions measured **44 / 44 / 45**. Keyboard focus reached the new action and remained visible through scrolling. [Reward rows](reward-viewports.json) and [result controls](result-viewports.json) retain the measurements. Temporary viewport overrides were reset.

An initial compact Collection action measured 42 pixels; a narrow minimum-height fix raised it to 44 before the final gates. The browser's screenshots under the viewport override were scaled inconsistently with the measured CSS area; those diagnostic captures remain in `.cache/round-20/screenshots-diagnostics/`. Those early captures are not presented as faithful phone screenshots. The initial linked Collection image uses the normal 1280×720 browser surface. The final placement pass below measured settled viewport dimensions before capturing.

The focused progression, integration, continuation, controller-navigation and practice-navigation run passed 82/82 tests before final gates; it overlaps the unique [1,128-test root total](source-gates.md). Generic controller navigation is tested, but this pass directly exercised keyboard and pointer UI, not a physical or browser-simulated controller journey through the new reward action. Existing campaign-local eligibility, optional seals and ordinary score records were checked separately. No claim of physical touch comfort, native compatibility, audio listening, player enjoyment or changed simulation timing follows from selecting an appearance.

## Final placement correction

Reviewing the result screenshot after the first passing gates found that the reward paragraph could be clipped inside the scrollable mission-reading region. Only HTML and CSS changed: the same static paragraph now sits directly under the picture/status row, with bounded wrapping. No award, persistence or simulation code changed. The [initial gate reports](source-gates-initial.md) and original screenshot remain preserved; the final gates identify the updated two files.

The normal UI again imported the two-clear library generated from published ordinary routes and restored the unchanged Grid + buffer final-cut fixture. Resume and Right earned the final tier. The corrected paragraph was visibly below the board, and View picture left it visible with the exposed artwork. An initial rapid viewport loop still reported 1280×720 for every requested size; [that diagnostic](reward-placement.json) is not counted as compact verification. Allowing the override to settle and accounting for the browser’s 1.2 scale then yielded actual **320×640, 844×390 and 1280×720** layouts. [Final measurements](reward-placement-final.json) show no document or reward-text horizontal overflow, no ancestor mission-reading region, and action heights approximately 44, 44 and 44.65 CSS pixels. [The compact capture](screenshots/reward-phone-final.jpg) records the visible wrapped reward; browser screenshot scaling does not establish a physical device. Temporary overrides were reset.
