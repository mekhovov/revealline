# Packaged chapter rewards — initial candidate

Observed 12 September 2026 in the Codex in-app browser at `http://127.0.0.1:8815/game/`. The packaged working-tree build displayed **v0.10.0-rc1**, contained 118 manifest assets and had ZIP SHA-256 `4b7b41e0fd7e2c8198c283e6733d85c9f62c5705365a953bc0966106acec29dc`. Its source revision was null; the immutable artifact is verified separately after commit. All 266 [initial gate inputs](source-inputs-initial.json) still matched after building. This candidate predates the final HTML/CSS change that moved the reward notice below the picture; later candidate evidence is recorded separately.

The owned server returned HTTP 200 and the production CSP, MIME, referrer and permissions headers. The player installed Equipment Workshop through its normal button. A two-clear library was imported through Saves & loads: its generator ran the first two published ordinary Workshop proof routes through public simulation and completion APIs, asserted their original expected summaries and validated the exported library. It contained two earned pictures and scores, with no inferred equipment seals.

Selecting Workshop showed **Chapter explorer: 2 / 3** and disabled Night signal FPV. The unchanged Round 19 Immediate Clear Ledger suspended fixture then restored through the file picker. Tap steering, Resume and Right supplied the live final cut. At **73.1%, three lives, 11,740 points and gold**, the result named Night signal FPV and Delta interceptor and enabled both options. Keyboard Enter on Choose appearance focused the existing selector; Night signal FPV could be selected.

After reload and reselecting Workshop, all chapter appearances remained available with no new reward notification. The exported profile contained **three pictures, three scores and one seal**, 4,418 bytes, SHA-256 `08ef9be1628e133b576747f968e53fec92657389b052712ff1a50783535fcde5`. The one seal belongs to the qualifying final live attempt; ordinary prior clears supplied no equipment record. [Profile evidence](profile-checks.json) retains this export's path and counts. Sampled warning/error logs were empty.

This is a prepared-library-plus-recorded-prefix-and-live-input check, not a claim of manually flying all three full routes. The [source browser report](source-browser.md) separately covers live first/final tiers, repeat suppression, out-of-order replayability, explicit cosmetic changes, reduced effects, Undo and CSS measurements. Physical devices/controllers, native hosts and enjoyment remain separate verification needs.

The packaged Couch page also booted with both boards ready, its normal map/class selectors and the installed Workshop maps. Warning/error samples were empty. This smoke check exercises the campaign-aware starter-body lookup; it does not repeat a full two-player match or certify physical controllers.
