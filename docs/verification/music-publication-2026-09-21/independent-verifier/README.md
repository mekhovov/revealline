# Independent local preview verification

The final preview payload passed a complete local SHA-256 verification: **70 MP3s, 354,986,122 audio bytes and 355,267,348 public bytes**. The nested preview repository was clean at commit `833bacbe2fa6a426053085fbd9c408d0a2cac21c`. The final deployment manifest SHA-256 is `5c4f28b184258e4980ab04ad698bab8b200134d73b717f06568a2768aa1a420f`.

[review.json](review.json) pins the verifier, workflow, final metadata and player bytes and records the exact local command. [independent-final-verify.json](independent-final-verify.json) is this reviewer's fresh command output. [licensed-preview-final-verify.json](licensed-preview-final-verify.json) and [licensed-preview-builder-tests.log](licensed-preview-builder-tests.log) are unchanged copies of the requested original temporary receipts. The latter reports 38/38 tests; this reviewer inspected it rather than rerunning that cohort.

Before the final player error-clear update, this same verifier passed one positive metadata case, 15 metadata refusals and four filesystem refusals. Full real-payload staging also passed, excluded private files, retained exact hashes, used verified hardlinks and refused an existing destination. Its own temporary stage was removed. Those results are retained as transcript-derived observations, not invented original log files; their prior manifest and exact scope remain in the review JSON. Staging was not repeated after the player update, while every final public file was independently rehashed. Syntax, scoped ESLint and Prettier passed for the templates.

This is local integrity evidence. It does not certify listening quality, game admission, Content ID, public deployment, Ukrainian cultural authenticity or physical-device behavior. UA-FPV recordings are excluded. The reviewer authored the verifier/workflow and separately read the preview builder; this role is disclosed rather than presented as independent authorship of those templates.
