# Scoped presentation review after Team integration

Review source: `aced2706`, with no simulation or gameplay-rule change in this adoption. This is a compatible presentation successor, not full phase or device acceptance.

## Screens

Inputs SHA-256 `d7b27aa169ea52615d451e8a05207a2d972fab3895cf6c0f538e9e51bb46b3a0`. Compared with accepted source17bd1965, only67 appended lines in `game/ui/field-kit-surfaces.css` change the screen group. Their scope is Lost overlays in landscape at at most500 CSS pixels high. Details and actions receive separate positive scrollports; reading controls occupy their own row; recovery feedback gets a later row; direction of focus is retained. Pause/Ready/Won selectors are unchanged.

The exact CSS hash `a1e83e6bc757fbe5c524b73831b389ea8b02e7645e0b4b1cc3bfa74601ce3a1d` matches the retained native evidence in `../p07-loss-difficulty-integration/`. Its legal loss at568×320 Large/Plain measured125.625px positive reading height,47px Retry and45.5px difficulty selector. Keyboard change and explicit Retry preserved the result until replacement, with clean console. This is the scoped layout evidence; it does not re-certify every screen or establish physical touch/controller behavior.

## Effects

Inputs SHA-256 `2614da4d54ada1b63ed78a9361bdfcf2355457f58577ea205ab6427d14a67f79`. The actual diff introduces required-look preparation and renderer adoption in `game/ui/render.mjs`, plus its explicit `game/ui/look-preparation.mjs` dependency. Prior lane/marker/classic/event-feedback inputs remain unchanged.

Review checked bounded decode deadlines, cancellation during load/decode, owned versus borrowed images, one-owner adoption, stale token/snapshot rejection, compact and detailed compiled readiness, and explicit-override priority. The required path waits before run replacement; the historical cosmetic fallback API stays distinct. No collision, score, core or simulation changes accompany this effect recipe.

Retained integration evidence at `../p08-look-readiness-integration/` establishes92 distinct scoped cases per Node20/22 with sparse-checkout reruns separately recorded; legal capture→Next, queued-turn Pause→Resume, held/error/cancelled images and real painter checks are covered. All377 served hashes from the retained native keyboard journey match the integrated renderer. This declaration is a bounded readiness/ownership review, not a new visual-art approval or all-character, all-theme, offline, hardware or playability acceptance.

## Immutable adoption

The real producer first recorded source-stage fpv40 with183 reviewed and116 source slots. Its full history/payload oracle is `game/test/fixtures/production-team-followup-source-fpv40.json`; source bundle SHA-256 `e86eb350dcd9968e2a226e969d1b9b70d76de5305f68aeaf8b93bcbcccb93949`. The following scoped review must append revisions while retaining all133 original media payloads, every earlier record and both Team picture identities. Exact reproduction and full-source qualification remain mandatory. UI review already changed to the scoped Team loader in this branch; tests must check that current independently recorded digest, while historical fixture hashes continue to verify old approval records.
