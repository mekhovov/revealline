# Reviewed presentation successor — held integration

The producer appended measured source-stage fpv40, then scoped-reviewed fpv41. It preserves all1317 prior asset records,40 prior theme records, the collection,299 slot contracts and133 original payloads byte for byte. The old selected runtime39 and intermediate runtime40 remain hash-addressable. The new runtime SHA-256 is `e56e5d5182513ffb4fe41a2ca2a821c856cdc40c63a6f700effd5399367bc7da`; full ledger SHA-256 `dd8d17b2227fef4d2e6b63a3f73253be1dbf4f870a4f74325fff24f7ce2371e3`.

Both source fingerprints have fresh bounded review in `source-review.md`. Current Team bindings name exact fpv41; both approved picture assets and the separately retained fpv38 policy remain unchanged. The source-stage oracle reconstructs the exact measured40 bundle from later immutable records, including every payload hash. Historical earlier oracles remain unchanged.

Validation: production `--check` reproduces all140 files and4,009,248 original asset bytes. Declaration readiness identifies200 reviewed required slots and99 optional source slots; this is not inferred visual certification. Node20.19.5 and22.22.2 each pass13/13 production-history checks and98/98 adopted Team/host/Studio checks. The initial98-case runs passed97, exposing an outdated expected compiled revision38; the corrected test now pins exact41, never a latest-match fallback, and still verifies the complete authored pack and exact picture hashes. Raw first/final logs are retained separately. Related lint, formatting and whitespace checks pass.

The preceding Studio log copies now trim trailing whitespace only. `../p04-studio-transfer-integration/tap-whitespace-normalization.json` retains the original and normalized hashes; original test output remains in the root evidence cache.

This is a held production integration, without a new game version or public claim. Fresh native review of the final compiled outputs, full source gates, ordinary build, merge of accepted device release, final version/PR/freeze/deployment and actual device/controller qualification remain separate.


Final scoped browser check on committed `05d2d201` is now retained in `native-evidence.json`. All 425 responses match exact integration. Relay Yard Start/Pause/Settings/explicit Resume passed; direction/action targets remained visible at 568×320 and 390×844. The small two-player landscape board is 254×127 because controls consume side space; this is readable-control evidence, not proof of maximum arena use. Solo actual self-contact loss at 568×320 Plain/Large allowed a wholly visible 45.5px difficulty selector, then explicit Retry to five lives. No warning/error appeared. This does not cover all Team reveal/rescue states, physical devices, zoom, offline or public delivery.

Source validation, lint, both formatting checks and Motion syntax pass after exact sparse-source hydration; earlier missing-file failures remain recorded. The ordinary build stopped before output on absent tracked optional originals (`authoring/library/ukraine-route-art/originals`); it is not accepted. Local free space fell to about 423MiB during concurrent release work, so root did not hydrate optional collections or repeat the large build. Full test shards and final integrated ordinary build still belong to the post-v0.77.0 qualification gate.
