# Test-DOM lookup integration

Separate future proposal, not part of v0.70 source4bb553d0. Applied on caption correction878788af. Three helper/test/doc postimages exactly match the sealed packet; its maintenance section is appended while retaining the receiving branch's newer guidance.

Root's complete seven-file composition passed **136/136 on Node20.19.5**, zero failed/skipped/cancelled,123.225seconds: six selector-boundary tests,114 unchanged input/presentation/menu tests and all16 optional-chapter launch/recovery tests. Changed helper/test ESLint and Prettier passed. The initial isolated runner could not resolve the existing installed Prettier dependency; that failed startup is retained, and the corrected runner links the already-pinned local toolchain without changing source or assertions.

The sealed owner's distinct Node20/22 semantic/114-test checks and unchanged ten-clear scenario remain in packet receipts. They are not root's current-source full qualification. Every original file is retained losslessly with hashes; executable observer files use `.txt` to avoid discovery as shipped source.

The improvement stops ordinary one-result test-DOM lookup at the first matching descendant. It preserves tree order, mutation, supported selector syntax and deliberate fixture overrides. Browser runtime/rendering/gameplay are unchanged. Synthetic timings and differently loaded host runs do not establish browser or whole-gate performance. The next final versioned source still requires all release gates and appropriate player verification.
