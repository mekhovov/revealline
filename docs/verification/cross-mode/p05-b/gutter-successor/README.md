# P05-B gutter source successor

The original desktop review found Rich ornament stripes crossing Versus and Team
content. Only menu CSS changed in production: enabled ornaments reserve a 28px
border-box gutter, with shrink constraints and no clipping. Off retains the original
layout; Team lobby block spacing remains intact. The budget includes the stripe,
inset and existing focus outline, but arithmetic does not prove rendered geometry.

[The map](evidence.json) retains every source/test predecessor and raw check from
this correction, plus the separately bound corrected preview. [Held02](source-held-02.json)
changed CSS and two test files. Both complete affected files passed 22/22 on each
runtime. The final [held03](source-held-03.json) changes only a test name to make its
source-rule scope explicit; the complete appearance file passed 12/12 on both
runtimes. Earlier 205/205 results remain historical and these counts are not added.

The proper before-fix single-case failure checks missing source gutter rules.
The separately labelled unmatched-filter setup run is not regression evidence.
All raw copies are exact or lossless gzip; none are whitespace-normalized.

Root independently reviewed the CSS successor and Dewey reviewed the CSS, inherited
shell/focus styles and complete changed tests. Neither reran tests or claimed native
geometry. The corrected native record is retained separately after its owner seals it.
P05 layout, zoom, system/storage and physical-device acceptance remain open.
