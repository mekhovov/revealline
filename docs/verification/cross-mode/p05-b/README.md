# P05-B menu appearance — local candidate evidence

This source candidate extends exact P05-A
`3f934b7508645d2cad03466276d20e8d038f9d76` / tree
`5be2690d05921fbe99fdc4e306de79fa6dd5be5c`. It adds the
[menu appearance contract](../../../menu-appearance.md): one separate strict menu
record, menu-only Ukrainian colours and static Off/Subtle/Rich ornaments. It
changes no version, compiled artwork/registry, scoring, simulation or public
selector. Later P03 operation-focus and P08 composition are absent.

The original [source evidence map](evidence.json) binds 25 exact or losslessly compressed originals:
92,647 retained bytes from 262,385 original bytes. Compressed files use gzip with
zero mtime; decompression must reproduce the original SHA-256. No whitespace was
normalized in that source package. Its runtime predecessor is pinned
by [held01](source-held-01.json), with tracked hunks in
[source-held-01.diff.gz](source-held-01.diff.gz). New source/test files remain normal
repository files and their complete hashes are in the record. The separate
[gutter successor](gutter-successor/README.md) pins the current source, while
[failed native placement](native-held01/README.md) and
[corrected native placement](native-gutters/README.md) retain original screenshots
and losslessly compressed event metadata. No bulk artwork is copied.

Two raw diff carriers are lossless gzip because Git's staged whitespace check flags
their blank context lines. [The carrier record](carrier-update.json) retains the
initial diagnostic and exact original hashes; no raw evidence was normalized and
this is not a runtime whitespace exception.

## Executed source checks

At held01, eight **complete files** passed **205/205** on each runtime, with no skips or
cancellations: Node 22.22.2 in 73.967 seconds and Node 20.19.5 in 82.613 seconds.
These are wall times from the runner; each raw test summary also retains its own
duration. All 291 finite physical input pins, totaling 15,727,778 bytes, matched
before and after. The [execution receipt](focused-execution.json) records the exact
argv, and [verification.json](verification.json) binds raw logs and static results.
Lint, format and tracked whitespace checks passed. This is not the full release
qualification or a build.

The complete files are menu-style preferences, menu appearance, menu-style host,
shared display host, Couch shell, Couch navigation, Team host and Title entry host.
The three new files contribute 42 cases. They exercise strict storage identity and
failure/reentrancy; immutable authored canvas inputs and late-dialog ownership;
actual paused Solo cuts and continued replay; Team painter/checkpoint preservation;
keyboard/controller selector access; cross-page storage; and terminal cleanup.
Browser defaults/Canvas remain finite modeled boundaries, including Solo's
unprevented interior Tab. Existing navigation/audio/display owners stay in use.

Dewey independently closed the host/markup/appearance/CSS source and complete
appearance/host test review. Root closed the strict adapter source and all 21 unit
cases, retained in [root-peer.json](root-peer.json). Neither peer reran tests or
claimed native acceptance. The original verification receipt predates the separate
root peer; that historical attribution is preserved.

The production gutter correction subsequently passed both complete affected files,
**22/22** per runtime (Node 22: 6.320 seconds; Node 20: 7.426 seconds, raw summary
durations). A test-name-only clarification then passed the complete appearance file,
**12/12** per runtime (0.315 and 0.055 seconds). These are overlapping checks, not
an added total or a repeat of all eight files. Current
[held03](gutter-successor/source-held-03.json) has the same eleven production pins
as corrected held02; only the appearance test name changed. The post-run comparison
retains 288 unchanged original inputs and identifies the three source/test changes;
it is not a new before/after qualification of the complete import graph.

## Retained diagnostics and native review

The initial 29/30 result failed host import at absent finite authoring code. The
next host 1/10 result lacked the unchanged presets JSON and includes dependent
cross-page failures. Their exact base inputs were restored with small hydration
receipts. The next 8/10 host result exposed missing native interior Tab behavior in
the finite Solo DOM; the test now models only an unprevented browser default, and
the whole file passed 10/10. Appearance's earlier 8/8 precedes the late-dialog
observer successor's 11/11. These overlapping runs are history, not extra passes.

A fresh [immutable preview binding](preview/binding.json) fixes eleven production
overlays over the exact base; [HTTP source checks](preview/http-source-check.json)
verified all eleven bodies. This reuses only a bounded loopback Git HTTP server,
with literal configuration changes retained in [helper-reuse.json](preview/helper-reuse.json).
No old browser helper, origin, profile or native closure is reused.

The held01 desktop run retains **12 events and eight PNGs**. Keyboard menu choices
persisted between modes; a late Music Library received one noninteractive ornament,
and Close returned to its Settings opener. It also exposed a real placement failure:
Rich stripes crossed Versus and Team labels because those shells had no inline
gutter. An earlier provisional colour/hover inference is corrected in the original
events: an immediate inherited 100ms transition was not a persistent contrast
failure. Two Settings screenshot filenames contain identical bytes and are not
independent visual states. The Music Library keeps existing teal primary text;
comprehensive palette parity is not claimed.

The corrected independent origin retains **six events, five PNGs and two geometry
JSONs**. Root inspected all five images. Rich ornament and focus bounds clear the
sampled Versus controls by at least 6.44px and Team lobby controls by 5.33px; neither
sampled surface has horizontal overflow. Team Pause controls clear decoration;
paused Off/Subtle changes retain focus, and closing Options restores its summary
while the round remains paused. All eleven served source bodies and fourteen local
source/test pins were rechecked after native review. Both runs record 1309×1348;
their differing DPR values are retained without inferring a controlled zoom test.

Four settled opaque computed DOM pairs yield 13.07:1 (Title Start), 14.07:1
(ordinary Settings tab), 7.66:1 (selected tab) and 15.42:1 (select), derived in
[sampled-contrast.json](native-gutters/sampled-contrast.json). These samples do not
certify every state, image edge, transition, text face or size.

Remaining P05 work includes the Team pause action row touching the mode row with
overlapping outlines (text stays legible), narrow/short layout, actual browser zoom,
real system-motion/storage-event/denied-save checks and physical input. Large is a
preset, not 200% zoom. Complete danger/disabled/hover, Theme/Plain and Standard/Large
combinations remain broader acceptance work. P05-A's prior desktop evidence does
not close these B gates. This candidate neither completes P05 nor establishes
public, offline, release or device acceptance.
