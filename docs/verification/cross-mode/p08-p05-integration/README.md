# Static P08 on P05-A: scoped composition evidence

This is an internal candidate based on P05-A `3f934b7508645d2cad03466276d20e8d038f9d76` (tree `5be2690d05921fbe99fdc4e306de79fa6dd5be5c`), including P03 `3acce19`. Only the approved `f047a46..1da77ee` static-picture delta is composed. There is no new version, phase acceptance or public qualification. The [composition guide](../../../p08-static-p05-composition.md) describes the four production paths and remaining requirements.

The resolver and page forwarding are byte-identical to `1da77ee`. Current fixed mode routes, v2 return context, common mode rows, checked departures, display/audio lifetime and Text style Options focus remain. The intentional boot merge calls existing cleanup before the required-picture wait, keeping Cancel and Back reachable; R5 still offers initial Start focus only after exact readiness with untouched foreground ownership. Four new held-picture cases cover untouched focus, a deliberate mode link, blur/return and decode failure.

| Execution | Complete-file result | Scope |
| --- | --- | --- |
| Node 22.22.2 first run | Eleven files / 198 checks passed; installed-chapter module did not load | Original raw log retains the missing exact sparse-fixture refusal. |
| Node 22.22.2 installed-file rerun | 12/12 | Exact baseline Git inputs restored; no runtime or assertion change. |
| Node 20.19.5 final run | Twelve files / 210 checks passed | Entire scope together, zero failure/cancel/skip. |

Node 22 therefore has passing coverage for the same 210 checks across twelve complete files, rather than a new twelve-file final command. The source and existing input bytes stayed unchanged during every run. Whole-file scope includes static resolver/host, page forwarding, shipped/installed chapters, current shell/navigation/initial focus, Couch input, shared controller navigation, display host and master-audio host. Changed JavaScript lint, formatting and source whitespace checks pass. There is no combined phase total.

[Evidence mapping](evidence.json) pins all originals and retained exact or lossless gzip copies, including the initial refusal, actual commands, before/after input pins, conflict text and finite fixture intake. The first hold's guidance body is copied before the result-only documentation update. Historical P08 evidence in the adjacent `p08-a` directory remains byte-identical to the approved incoming commit and retains its earlier source scope. It is not a browser result for this composition.

The installed tests require their existing exact embedded pack fixtures and six selected original images. Twenty-four finite Git inputs (55,258,200 bytes) were restored after the import refusal; no build, network intake, media regeneration or archive extraction occurred. The worktree remained below 100 MiB with more than the required 256 MiB reserve. These test originals are not copied into this evidence package.

Root subsequently completed two scoped desktop keyboard journeys against the same four held production files. [Native mapping](native-evidence.json) retains the original events, screenshots, preview helpers, bindings and checks without changing their bytes. Both origins served exact Git `3f934b7` plus four immutable overlays held at `e92141bf`; fifteen source/metadata responses per origin matched before and after the journeys. No application state or clock was injected.

| Native origin | Retained observations | Scope |
| --- | --- | --- |
| Normal `64092` | 12 events and 7 PNGs | Solo First Signal won at 50%, 7,820 points and showed its full hut/sunset original. Matching Versus won at 52.2%, 8,160 points; View both boards showed the same original, Player 2's unfinished line and complete 4:3 arenas with side pillars. Plain/Large/Reduced preserved the finished boards. Solo Collection retained the original 7,820-point Gold record at 16.14 seconds. |
| Delayed `65050` | 9 events and 5 PNGs | Only the exact original `c1aedf89` PNG GET waited six seconds; bytes stayed unchanged. Setup Back cancelled preparation; Retry focused Cancel, explicit Cancel restored Retry, and another Retry preserved deliberate Team-link focus through Ready. Fresh Start and explicit Resume/Pause worked. Root inspected both short gold cuts and original borders in `live-resumed-cuts.png`, with 0% and three lives each. |

Normal event 2 and event 7 prematurely said “paused”; events 3 and 8 correct them to completed wins. The corresponding screenshot filenames are preserved and do not establish paused-cut behavior. Event 5 returned through the header Game menu, not a Results Main menu button. Event 10 briefly toggled Boost and restored Off before Reduced Effects. Event 12 reports device-pixel ratio about 1.8 versus earlier 2 at the same 1309×1348 viewport; root made no zoom command, the cause is unverified, and this is not a 200% scaling claim. Delayed event 9's DOM follows the separately captured live screenshot and is paused; these sequential reads are not an atomic checkpoint.

These native results cover one shipped map/theme identity, not every assignment, failure, viewport or device. DOM, Image, storage and controller tests remain finite models. Physical controllers/touch, phone/Safari, narrow/short layout, contrast, assignment A→B, native failed-image/blur cases, offline/public delivery and full integration remain open. The existing finished → Next path creates a new ready match before its picture finishes, so failure can replace prior results; preserving those results belongs to P07. The scoped guarantees here concern same-attempt Retry, captured artwork, interrupted Start and existing running/paused continuation. Team picture modules/host, P05-B menu style and later P03 operation-focus correction remain separate integrations.
