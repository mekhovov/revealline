# Static pictures on the current display and navigation candidate

This internal P08-A composition starts at P05-A `3f934b7508645d2cad03466276d20e8d038f9d76`, which includes P03 `3acce1941116d18f9e86dcfa26dea463433cd3b2`. It applies only the approved static-picture delta `f047a46..1da77ee`. Team presentation modules and host binding, P05-B menu decoration, and the later P03 operation-focus correction are separate. No version, phase acceptance or public release is established here.

The four production paths are `game/couch/couch-static-pictures.mjs`, `game/couch/couch.mjs`, `game/couch/index.html` and `game/presentation/page.mjs`. The exact static resolver and page forwarding come from the earlier candidate. Current Solo, Team, common mode rows, fixed departure routes and v2 return context, shared display/audio lifetimes, and the Options Text style initial target remain in place. Historical P08 evidence is retained unchanged; it is not new evidence for this composition.

Boot ownership has one intentional merge: once handlers and the lobby are installed, the existing idempotent boot cleanup runs before awaiting the required picture. Cancel and mode links are reachable while Start is disabled. The R5 one-shot focus decision still waits for that exact initial match and generation to become ready, and still respects deliberate focus, foreground loss, unavailable controls and recovery focus. It never starts a round or claims controller ownership.

The retained picture protocol checks exact map/theme/media identity, shares one accepted image/fit across both boards, retains the captured choice and attempt on Retry, and confirms authority before Start. A cancelled, failed or stale operation cannot start the round. Suspend invalidates pending Start/Next intent; completing the picture after return still requires a fresh Start. Explicit setup preparation creates a new ready attempt as before. The reader may perform its existing store initialization; it does not commit media, assignments or progress.

Focused verification will run the complete static resolver/host, page forwarding, shipped/installed chapter, current shell/navigation/initial-focus, input/controller and display host files on Node 22 and 20. New composition cases exercise required-picture boot focus and cancellation against current mode rows. Finite DOM, Image, storage and controller models do not establish native decoding, layout, physical controllers, phone/Safari, artwork fidelity across every identity, or public acceptance. Native and full integration gates remain separate.
