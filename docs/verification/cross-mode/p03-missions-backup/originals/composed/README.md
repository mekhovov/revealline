# Missions return and backup feedback: composed source candidate

The isolated worktree `.cache/worktrees/p03-backup-feedback` composes the reviewed Missions return correction with the backup-feedback correction on exact source `965f2049cb273b93d0258ee43cd806c62d479421`. This packet does not stage, commit, version or publish the candidate. Version `0.60.8` is proposed by the parent and remains unallocated in source.

Missions Back, Escape and controller Back return to the actual title, field or brief opener. Returning through Collection appearance selection keeps the original Home parent. Newer dialog, focus, background and disposal ownership take priority, including direct Brief callback boundaries. Intentional Deploy and Back to flight preserve their existing behavior.

Complete-backup Import and Undo keep their own restoration feedback instead of announcing a fictional cancelled pack launch. The app change is the reviewed `!backupBusy` announcement guard; transaction invalidation, journaled storage, paused adoption and actual pack cancellation remain unchanged.

## Exact composition

`composed-pins.json` records nine source paths and exact Git preimages. Both runtime bodies are unchanged from their reviewed proposals: app `8eded908…`, shell `f36217b0…`. The four non-overlapping backup paths are unchanged. The shared runtime-maintainer skill contains the single Missions line replacement and preserves the backup append byte for byte. `composed.patch` includes the new backup test as well as all tracked hunks.

The sparse checkout materialized only seven explicit paths, 132,950 original bytes; `hydration.json` identifies their exact Git blobs. No source archive, asset duplication, dependency installation or build was needed. The index remains empty. Existing source version files remain unchanged.

## Verification and evidence boundaries

The composed checks run seven complete files sequentially on Node 20.19.5 and Node 22.22.2 with a 1,024 MiB V8 cap. These actual host tests combine app and shell changes in the same source tree. The files are backup-content-feedback-host, text-size-host, chapter-download-host, backup-storage, field-kit-flow, modal-navigation and practice-brief-host. Each run records its command, complete output and before/after source input hashes. Both runs passed 102/102 cases, with no failures, skips or cancellations. All 324 source inputs (32,325,539 bytes) remained unchanged; the same manifest SHA is recorded for both runs. See `verification.json`.

`static/receipt.json` retains syntax, changed runtime/test formatting and whitespace checks. The unchanged historical Markdown formatting conventions are preserved in the existing skill and prompt, rather than reformatted wholesale. Original failing and intermediate Missions/backup test attempts remain in their original candidate packets; this composition does not relabel those results.

`native-references.json` pins the parent's finalized native report and its exact original index. The predecessor shell `80de` provides the backup Import/Undo and nested appearance return evidence. The successor `f362` provides Home Missions return and paused brief return; explicit Continue reached an actual First Signal completion, followed by explicit Retry and Pause. The final request review reports 312 and 309 distinct exact source/override bodies, respectively. This packet only rechecks those existing originals and runtime correspondence; it does not perform another browser run.

Physical controller/touch, public deployment, offline, all menu journeys and completion of P03/P05/P18 remain separate. The tied-timestamp pager ordering finding is outside this correction. The parent owns final review, ancestry integration, version allocation, commit, hosted gates and release/public verification.
