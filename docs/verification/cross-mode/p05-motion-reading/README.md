# P05 Motion v0.60.5 — integrated correction candidate

This unreleased correction is integrated on exact `d77ea4f5c2aeccf2ba3bdf483a3f25531f8095b6` (Recovery). It is not a new release, final-source qualification or full P05 acceptance. The original candidates and native evidence were produced on a13 plus the three exact Motion patches. Their failed attempts remain unchanged; their candidate-time “pending” wording is historical, not a new regression or current acceptance claim.

The correction gives Motion one shared-reading owner and explicit Text size writer, preserves a study through preference changes, locks startup controls without locking reading/return actions, and keeps Reload handoff with its foreground owner. A single animation loop and lifecycle fences preserve explicit Play after interruption. Bounded labels share permitted content with the DOM legend. PNG preview preparation preserves original upload bytes and keeps APNG's separate default image; stale work cannot replace current image/status. Five required helper build entries are added; accepted Recovery runtime and guidance are preserved. A separate authored Retry loading study link is available after failed preset download or validation, outside the disabled study. It does not focus itself or revive the disposed preview. Module-load failures retain the original launcher Reload; ready and pending studies keep the new link hidden.

## Retained evidence and what it establishes

- `originals/p05-motion-successor-a13ab970/`: 145/145 across 13 whole files per Node family, plus original omitted-fixture and formatting attempts.
- `originals/p05-motion-reading-followup-a13ab970/`: 160/160 across 14 whole files per family; original expiry-test timing failure is retained.
- `originals/p05-motion-apng-followup-a13ab970/`: 181/181 across 15 whole files per family; original invalid-reserved-bit test typo and its 180/181 first result are retained. These cohorts overlap; do not sum them as distinct checks. They are not final d77-composed qualification.
- `originals/p05-motion-native-a13ab970/`: original blue APNG default failure and its browser record. The separate valid wrapped Turn policy label review resolves only a locator interpretation, not an application bug.
- `originals/p05-motion-apng-native-a13ab970/`: root's 768×900 native retest, red static/APNG defaults at Cover 70%, Standard/Large redraw, explicit moving craft/paused state, invalid PNG retention, GIF/WebP fixture observations and Clear. Effective shared/system reduced effects remained on. Its independent audit matches all 151 recorded responses / 2,698,221 bytes, including all ten runtime overlays, with no mismatch. Root cleanup records tab closure, viewport reset and owned-server exit.

Native observations belong to root. Modeled events and tiny fixture uploads do not establish actual pending native read/decode races, BFCache admission, full-effects behavior, continuous every-frame capture, physical inputs, cross-browser conformance, decoder memory or whole-phase completion. Source authoring and publicly exposed Motion routes must be verified against the eventual frozen build; neither unrelated authoring exclusions nor this local proof establish deployment.

`evidence-copy-manifest.json` binds each retained original to its intended repository path. No captured dependency tree, source artwork body, complete build or unrelated Recovery/publication payload belongs in this record. Preserve originals losslessly; do not replace predecessor failures with successor successes.

## Fresh composed-source verification

The original d77 integration passed 181/181 across 15 complete files on each Node runtime before the retry finding. The actual-launcher failure discriminator then passed 32/33 and failed on the missing action. The first complete retry attempt passed 180/183: the changed status sentence conflicted with an existing expectation, and two new tests assumed a slow-launch Reload existed without triggering its slow timer. The final correction preserves the original sentence and exercises that real slow-launch boundary. Original logs and pins remain in the owned integration cache; their compact identities are preserved in the summary below.

The **v0.60.5 retry successor passes 183/183 across all 15 complete affected files on each supported runtime**, sequentially with 1,024 MiB old-space and concurrency one. All 208 input pins were unchanged within each run. The two input manifests are byte-identical, so only one original is copied. These are overlapping modeled cohorts, not additive test totals or hosted qualification.

- [Node 20 output](integration-d77/node20-output.log) and [receipt](integration-d77/node20-receipt.json).
- [Node 22 output](integration-d77/node22-output.log) and [receipt](integration-d77/node22-receipt.json).
- [Shared exact input manifest](integration-d77/inputs.json).
- [Scoped summary and original-attempt identities](integration-d77/summary.json).

The six records are exact copies of their cache originals. Guide/status/skill text is updated after the test snapshot; no runtime or test body changes follow it. Version 0.60.5 is synchronized in package, lock and build configuration. No source artwork, simulation, geometry, collection identity or gameplay recipe changes are included.

## Remaining gates

The accepted public predecessor is v0.60.4 within its Recovery scope. At the preparatory cutoff this Motion candidate was unstaged and uncommitted; the delivery-only acceptance ancestry from merged PR #92 (`a53d646153fc9dfb2d8f152fdc9dbde607782777`) is integrated with all 150 candidate file bodies preserved. Review final hunks and exact evidence pins, qualify the final source/production/build gates and hosted families, then publish an immutable release and verify actual deployed bytes/player journeys. The scoped local retry check now passes: injected ability-preset HTTP 503 kept the study locked and reading/return available; native Tab/ShiftTab reached Retry and Return, and native Return retried the same route to HTTP 200, enabled the study, hid retry/error and preserved Standard text. Original Large text was restored afterward. The first Return timed out during a browser disconnection; its failed-state readback and later successful retry remain distinct. Text-size selection used semantic interaction. This check used ten exact runtime overlays on an a13 Git scaffold, not a frozen/public v0.60.5 build; it does not qualify physical controller/touch, full effects, BFCache or new upload behavior. The root original is `.cache/p05-motion-retry-native-a13ab970/native/root-observations.json`, SHA-256 `2d1bcd4e670e01d26b75b827fe2aa009156c03bc074a0abb9a17572cb3eac8bf`; its 301-request record and original screenshots remain in that cache for release qualification. The six retained integration records keep their earlier dated native/ancestry/acceptance wording. Other native/physical boundaries above remain open.

P03 and P05 remain implementing. Other presentation, Team full-picture, sound/content, campaign and hardware gates retain their approved priorities and scope.

## First hosted qualification and integration-test correction

Committed source `40fe4a31f89efc6686b841f4f0de7fdc17874c72` reached both complete hosted families. Each recorded **5,581 passes and one failure out of 5,582 tests**, with no skipped/cancelled/TODO tests. Source/production checks and the ordinary PR build passed; the manual freeze was correctly skipped. The failed Workshop build integration check still expected Motion's retired generic text-size marker. Restoring that marker would add a second, body-only preference listener.

The correction keeps all existing build/reference assertions, requires the independent display entry and new runtime modules, and requires the dedicated Motion selector while rejecting the generic marker. Production runtime bytes remain unchanged. [Diagnosis and original failed jobs](hosted-40fe-failure/diagnosis.json), [complete-family summary](hosted-40fe-failure/terminal-summary.json) and [independent hunk review](hosted-40fe-failure/correction-review.json) retain the failed source boundary. The short player roadmap is refreshed from its stale v0.56/P02 candidate summary.

The corrected complete integration file depends on the real build-input closure. Local syntax and source review are not a complete-file pass. Fresh exact-source hosted qualification and all later immutable/public gates remain required; v0.60.4 stays the accepted public release. No failed release has been published.
