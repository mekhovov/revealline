# Supporting-screen readability evidence

Target: unpublished **v0.60.0**, main cutoff `10df6751b0f31d3f8ed68ee0132c830b6eefbec8`. The public baseline remains v0.59.1. This record does not close P05 or P03.

## Retained originals

- [Historical evidence index](history/INDEX.md) identifies the three Studio layers, their native observations, Replay candidate and compatibility review. Its ZIP preserves 241 original files, including failed attempts. The member manifest and receipt pin every retained byte. Captured dependency trees are excluded.
- [Composed-check archive descriptor](composed-checks.json) pins the new 19-file core and shared-host results, added Studio return regression and static composition review. The ZIP contains its own member manifest and exact original logs/receipts.

The composed cohort passed 151 checks in 19 complete files, plus nine reported results in the unchanged shared-host file (eight leaves and one parent), on both Node20.19.5 and22.22.2. That is 159 leaf behaviors per runtime. The 795 captured inputs stayed unchanged. Core files used100MiB; only the existing shared-host file used160MiB. Explicit source formatting passed. These checks precede subsequent native Replay corrections and are not final-source qualification.

The added actual-Studio case verifies prepared original bytes through stage/save/restore, then retains selection/focus, dirty sprite data and denied-save intent through modeled persisted returns. The static review found Studio textarea whitespace and recorded its correction separately. Historical and current test counts overlap and must not be added.

## Final correction checks

[Final corrections descriptor](final-corrections.json) pins original successor patches, failed and passing test attempts, predecessor and corrected browser records, and final root checks. The eight distinct affected files pass 57 checks per runtime: seven files reported 44, and the navigation file separately reported 13. The initial argv omission is recorded rather than hidden. Both runtime input sets stayed unchanged. Formatting passes for all 26 changed code/config paths. These checks still precede exact-source full qualification.

## Remaining release checks

Native Replay identified Jump to playback and completion focus defects plus a readout-size selector conflict. Their five-path successor is now integrated. Preserve the original observations, final source tests and corrected native observations as separate snapshots. Qualify the final combined source, actual Studio/Replay journeys, production reproduction/readiness and ordinary build before freezing. The release must then pass reviewed source/publication PRs, immutable assets, complete deployed-byte verification and affected public journeys.

Physical touch/controller results, audible listening, genuine browser cache/lifecycle observations and disconnected operation must retain their own evidence boundaries. A dispatched event, viewport resize or source test does not certify them. The execution register tracks the remaining broader phase work.
