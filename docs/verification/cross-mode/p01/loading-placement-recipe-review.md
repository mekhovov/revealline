# Loading-placement recipe review

This review approves the seven existing `screen.*.background` component recipes for screen-input SHA-256 `47874ed7ba035680e0f5abac3fa5a69c3e7774ecdbddcf769f490a9f09fdffc6`. The fingerprint concatenates, in order, `field-kit-flow.css`, `field-kit-surfaces.css`, `field-kit-compiled.css` and `site/release-catalog.css`, using the production compiler's existing contract.

Only `field-kit-flow.css` changes within those inputs. Its new rules apply to the Missions heading and `shell-craft-feedback`: a shared heading grid, a full-width status row, wrapping, existing panel/border tokens and empty-row collapse. The other three inputs are byte-identical to the accepted source. Hangar, results, collection, settings, couch and studio backdrop declarations and their effective selectors are unchanged. This carries their existing functional approval into new revisions; it does not claim new artwork or new cross-mode qualification.

The [craft correction review](craft-operation-visibility.md) records the changed Missions behavior, both intermediate overlap failures, the measured-clearance correction and native checks. Standard and Large/Plain/Reduced layouts at 390×844 and 844×390 leave the focused Pack selector's center and four interior targets unobstructed. The status uses its original presenter inside the active modal; Back remains reachable. Direct briefing reuses that same node and hides the empty ready row. Browser timer delays and transient-versus-painted observations remain explicit evidence limits.

The focused clearance, shell, renderer, briefing and modal-navigation batch passes **68/68**. The helper changes scroll position only; the renderer's loading identity and the simulation are unchanged. [Library](library-operation-visibility.md) and [Controller Apply](controller-apply-focus.md) have separate state/focus evidence because they do not change these backdrop recipe inputs.

The official producer appends reviewed successors. It must retain every historical asset/theme/collection record and original blob, including the source-stage records created during the failed layout trials. Reproduction, complete required-slot readiness and exact-source release checks remain mandatory. This scoped recipe approval does not accept P01, corrected public behavior, physical devices, or the later Team map-parity work.

The reviewed append creates production revision 22: seven asset successors and one theme successor. All 1,060 earlier asset records, 22 theme records, the collection, 293 slot contracts and 127 original blobs (4,007,816 bytes) remain identical. Each of the seven successors changes only quality evidence, revision and provenance parent; runtime recipe content is identical to the native-tested revision 21. The resulting inventory has 194 reviewed and 99 optional source-stage slots, with zero missing slots. These counts describe the current registry, not completion of the future Team asset families.
