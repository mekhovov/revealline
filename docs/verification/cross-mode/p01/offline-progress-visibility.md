# Offline progress visibility correction

The public v0.57.0 portrait check found a P01 usability defect after the source and public-byte gates passed. Every offline progress message appended the full eleven-pack optional catalogue. The long label expanded the status row, pushing its measured count and Stop waiting button below the initial 390×844 viewport. Native scrolling still reached Stop, which correctly detached and returned focus to Check progress; this was a visibility defect, not a cancelled or failed worker installation.

The original [portrait preparation](offline-visibility/01-preparing-portrait.png), [stopped state](offline-visibility/02-stopped-portrait.png) and [landscape progress](offline-visibility/04-progress-landscape.png) are preserved with their [copy hashes and scope](offline-visibility/copies.json). v0.57.0 and its public byte audit remain immutable. The first browser launch attempts also remain separate automation setup failures; they did not reach the game.

The v0.57.1 correction adds a concise `summary` wherever the offline observer previously appended the optional note. The complete `message` remains unchanged for existing consumers. The panel prefers the summary, retains the older message fallback and keeps full result JSON in verification details. A separate plain-text optional-pack paragraph inside those details is available before, during and after preparation. No worker protocol, timer, cancellation, storage, gameplay or shared component recipe changes are involved.

The focused [correction receipt](offline-visibility/offline-concise-correction.json) records 44 passing tests, with no failures, cancellations, skips or TODOs. The regression uses all eleven packaged optional names, a real offline host and Node MessageChannel across progress and terminal states. Existing verbose-message compatibility, stale-observer, focus, cancellation and retry assertions remain. Node fixtures are distinct from native service-worker and browser layout evidence.

The corrected [ordinary-build browser check](offline-visibility/local-browser/qualification.md) passed 38 bounded assertions in isolated desktop Chrome 153 with viewport emulation. At 390×844 and 844×390, both Standard text and Large/Plain text with Reduced effects kept the concise status, measured count and Stop visible together after the native action. Stop measured 44 or 47 pixels high. Before each action, normal scrolling positioned its button; no driver scroll occurred between the before-action and after-action samples. Large-text cases had ordinary browser scroll anchoring of 22 and 5 pixels without losing those controls. These are viewport checks, not physical-device results.

The generated v0.57.1 preview has `sourceRevision: null` and build ID `39f9ba278274ad326e6d85751711e3b50e682c66389796bb4499608999f0bb1a`. Its real worker prepared 608 core files totaling 55,383,689 bytes. The server withheld the original 3,500-byte `authoring/asset-studio/operation.mjs` response for 82.83 seconds, then delivered its unchanged bytes. Native Stop returned focus to Check progress; rejoining observed the same 607/608 installation. Releasing the body completed verification. Cached Verify subsequently showed measured 504/608 progress and completed 608/608 without another server request. The held original's SHA-256 also matched its native CacheStorage body.

The full eleven-pack note was readable inside verification details [before](offline-visibility/local-browser/04-note-before-readable-portrait.png), [during](offline-visibility/local-browser/11-note-during-standard-landscape.png) and [after](offline-visibility/local-browser/20-note-after-large-portrait.png) preparation. Full result JSON keeps its verbose message. State JSON and screenshots are separate sequential samples, so advancing counts can differ; no values were spliced into screenshots. The early offscreen note-click attempt remains retained and excluded from successful note-readability claims. The copied close helper's first Node 20 invocation lacked a global WebSocket and failed before connecting; its retained recovery used `--experimental-websocket`, verified the exact owned browser arguments and closed only that browser. Both owned browser and server PIDs and ports are closed, with the isolated profile preserved and the public profile untouched.

The [copy manifest](offline-visibility/local-browser/copies.json) records all 80 selected originals, totaling 2,547,155 bytes, rehashed before and after copying with identical bytes. The original selection manifest, controlling server/browser helpers, exact small source/artifact authorities, failed attempts, screenshots and result records are retained; browser profile and CacheStorage directories are excluded. The [detailed review](offline-visibility/local-browser/review.json) records the measurements and limits. No further runtime or CSS change was needed after this browser review.

Corrected ordinary-build layout verification is complete. Exact committed-source gates, the new immutable v0.57.1 frozen artifact and public patch verification remain required before P01 acceptance. The public v0.57.0 bytes and their earlier evidence remain unchanged. The native meter colour and the wider Team/navigation redesign retain their separate P05/P03/P08-A scope.
