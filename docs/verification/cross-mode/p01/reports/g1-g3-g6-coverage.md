# P01 G1/G3/G6 coverage

Working source in `codex/cross-mode-p01`, 2026-09-15. Inventory names below follow `publishing/pages-controller/delivery/evidence/cross-mode-p00/async-baseline.md`. This is implementation and focused verification evidence, not release or physical-device acceptance. G0 owns the Solo adapters and shared status; G4 owns Library/backup commit callers. The isolated browser observations below include the two defects found and corrected during review.

| Inventory | Implemented functions / ownership | Decisive focused tests | Remaining gaps / boundaries |
| --- | --- | --- | --- |
| A03 compiled presentation | `presentation/host.mjs` `load({onStatus})`, `readPicture({onStatus})`: real manifest/read/verify/decode phases, current-controller status only. G0 `app.mjs` `presentationFeedback`, `presentationReady` observes cosmetic loading without gating a flight. | `presentation-host.test.mjs`: current source verification, stale snapshots, owned resource release, observer failure isolation; `release-pictures.test.mjs`: required originals retain exact identity/bytes. | Root qualifies Solo visibility. Cosmetic readiness is distinct from required picture acceptance. `readAudio()` belongs to G5; unchanged ownership is retained. |
| A04 shared presentation page | `presentation/page.mjs` `mountPresentationPage({onStatus})`: synchronous initial status, per-page lease fanout, ready only after applying accepted snapshot, cached lease readiness, close/disposal fences. | `presentation-page.test.mjs`: shared deferred load, closed/reopened lease, no duplicate load, no stale status or painter updates. | Couch and Replay now opt in through their own `presentationFeedback` operation. Their release artwork remains nonblocking and errors preserve the current look. The controlled browser runtime read proves Couch stays ready with Start enabled; other callers remain their owners’ responsibility. |
| A05 craft/body look | `ui/render.mjs` `BoardPainter.setLook`, optional `onAssetStatus`: real requested image work, existing load token, ready/error after settlement, procedural look also terminates the prior loading signal. G0 `craftFeedback` is nonblocking. | `presentation-renderer.test.mjs`: held actual image completion, newer image wins, obsolete image cannot publish, failure warning retained, procedural neutral-marker supersedes a pending image without leaving busy. | Root qualifies Solo; G6 awaited Replay look has its own decoding phase. Versus painter looks remain cosmetic and nonblocking; no new image owner or per-board content preparation. |
| A14 bundled pack install | G0 `app.mjs` `ensureBundledPack`: downloading, verifying/preparing artwork, saving, terminal selection via existing `packLaunchGuard` and `replacePackLibrary`. | `chapter-download-host.test.mjs`, `title-operation-status-host.test.mjs`, `optional-chapters-host.test.mjs`: delayed actual pack request, preserved current selection, title leave/reopen and stale owner behavior. | Root owns this adapter and Solo browser evidence. Download progress is indeterminate because the existing read boundary provides no byte denominator. |
| A15 optional catalog | `ui/optional-chapters-panel.mjs` `run`, `reloadCatalog`, `inspectSource`, `inspectInstalled`: scoped presenter, actual inspected chapter counts, pinned active card and existing controls/Back, partial failure, truthful cancellation. | `optional-chapter-descriptions.test.mjs`, `external-chapters-panel.test.mjs`, `external-chapter-card-focus.test.mjs`, `worlds-browser.test.mjs`: real inspection boundaries, partial errors, cached refresh, focus, cancel/close and no late reopen. | Root owns actual More worlds browser journeys. Completed installs remain after cancellation; no invented rollback. |
| A16 optional install/download/choose | Panel forwards optional `onStatus`; G0 `installOptionalChapter`, `installSourceChapter`, source `choose`, optional `choose`: read/download, verify, durable save, recheck and fresh-picture phases. | `optional-chapters-host.test.mjs`, `chapter-download-host.test.mjs`, `release-pictures.test.mjs`: source/current-owner guards, actual save/readiness, stale completion and preserved current flight. | Root adapter qualification and G4 recovery interactions are separate. Existing download caps/authentication/pins retained. |
| A17 shared pack commit | Existing G0 `replacePackLibrary` and commit coordinator remain authoritative. Caller stages identify save; panel cancellation says completed installs remain. | `content-launch.test.mjs` commit/reconciliation tests plus `optional-chapters-host.test.mjs`; G4 Library/backup tests own removal/import/cancel outcomes. | No new lower-level callback or competing writer introduced. G0/G4 must verify all non-optional callers expose an appropriate save/reconciliation stage; this slice does not independently claim that complete coverage. |
| A18 release picture materialization | `presentation/release-pictures.mjs` `prepareSelection`, `assignFreshChapter`, `materialize`: optional real read/download/verify/save callbacks; report finishing save before commit and verify after; observer failure cannot change durability. `ui/flight-pictures.mjs` `ensure` forwards phases through actual decode. | `release-pictures.test.mjs`: held actual commit, cancellation after commit preserves history but does not adopt pins, retry reuses stored original; CAS conflict, corrupt bytes, stale fresh-install proof; `flight-pictures.test.mjs`, `flight-pictures-host.test.mjs` and `picture-ready-status.test.mjs`: exact prewarm join, retry rejected promise, hidden late completion and explicit Resume. | Root owns A08/A09/A16 foreground adapters and browser evidence. No new fallback or assignment policy. |
| A19 offline | `offline.mjs` `prepareOffline`, `checkOffline`, observation/report helpers; service-worker `prepare`/cached verification and versioned opt-in progress/terminal subscriptions. Root `ui/offline-panel.mjs` owns screen observation and Stop waiting / Check progress. | `offline.test.mjs`: >60s successful install, separate report timeout, old terminal-only compatibility, same installation/two clients, stale build/request/worker, detach with background completion, quota/integrity failure, waiting/cached paths, bounded subscriber cleanup. `offline-panel.test.mjs`: immediate feedback, close/reopen, stale finally, read-only Check. | Real public/frozen service-worker qualification remains root release work. No unregister/cache deletion/shared cancellation/skipWaiting. Concurrency4,64MiB and2000file caps and ready-marker-last retained. |
| A20 storage retention | `ui/storage-retention.mjs` existing `refresh` and explicit request retained unchanged; offline readiness is not storage-persistence readiness. | `storage-retention.test.mjs`: actual method receiver/gesture ordering, persisted/denied/unsupported/error distinctions, stale status vs newer grant, coalesced requests, close/reopen/dispose fences; `platform.test.mjs` native/web distinction. | Browser permission grant itself depends on browser policy; not a physical-device claim. Root qualifies Settings visibility. |
| A42 Replay | `replay-theater/app.mjs` boot, `load`, `cancelLoad`, `fetchExample`: immediate static/host boot, reading, actual verified tick counts, separate artwork decoding, request epoch and cancellation preserve prior replay. Existing import status/Cancel now live in a visible row above playback, sticky only while busy. | `replay-theater-navigation.test.mjs`: delayed file read/Back, actual tick progress, held artwork decode cancellation, successful retry paused, blur/disposal, native editing/controller neutral gates; `replay-theater-wide.test.mjs`: bitmap adoption, stale/failure preservation and exact checkpoints. | Isolated browser review below covers portrait/landscape and delayed example work. File codec/timing is modeled only where stated. |
| A44 Couch startup | `couch/couch.mjs` startup JSON, featured preparation, installed refresh: real boot stages, static signal and outside-inert Back link; `toolState` starts loading and settles after initial prepare. All three G6 routes use shared `ui/direct-tool-launch.js` and call `attached()` before host async work, so pre-module failures offer same-page Reload. | `couch-markup.test.mjs`: static signal and reachable exit; `couch-shell.test.mjs`: actual held campaign fetch remains loading until real setup, ready tick0; installed host fallback cases retain shipped content when optional source unavailable. | Isolated browser review below covers both sizes and delayed real resource. No physical controller claims. |
| A45 Versus preparation | `couch/couch.mjs` `prepare`, Start confirm, retry, installed refresh, `cancelContent`: shared preparation target visible in main/setup, onStatus forwarding through existing run/controller/generation, Back cancels and late completion cannot enable Start. | `couch-installed-chapters.test.mjs`: real deferred image decode in actual Couch host, visible signal outside hidden/inert ancestors, reachable Back, exact shared original, retry focus, explicit Start, separate motion; `couch-navigation.test.mjs` / `couch-shell.test.mjs`: v056 navigation/input/resume retained. | Browser installed-original selection needs an installed library fixture; not substituted by a shipped-map cosmetic delay. If not exercised in this run, remains explicitly unit/host verified. |
| A46 installed reader | `couch-installed-chapters.mjs` `operation`, `refresh`, `select`, `confirm`: optional phase observer, same serialized/cancelled authority work, real decoder status and current generation fence. | `couch-installed-chapters.test.mjs`:10 tests including authenticated originals, changed metadata/pointer, cancelled decode joined before replacement, missing journal/blob refusal, Fracture/Countercurrent exact owners, no Solo writes, shared boards. | This is a dependency of A44/A45, not another UI or media owner. Browser catalog setup gap is the A45 gap. |
| A47 Team | `couch/relay-rescue.mjs` boot and file change: immediate read/check phases, Stop waiting, Back while pending, closed-picker and request/run/disposal fences, retained selected pack and deliberate focus; static route signal and `toolState`; `cancelImport` clears the native file value so reopening permits the same file again. | `coop-host.test.mjs`: immediate held File.text, stopped/closed/reopened/Back reads cannot adopt, latest request wins, failure/size cap, focus retained, v056 touch/Help/Resume controls. | Isolated browser review below uses a real JSON file with a controlled read wait. Team artwork binding remains P08-A, so no invented pending-art stage. |

## Focused evidence already completed

- G1: `node --test game/test/offline.test.mjs game/test/platform.test.mjs` —44/44; `/tmp/p01-offline-final-tests.log`.
- G3:78 focused preparation/panel tests; additional actual Solo picture/optional host set22/22 (`/tmp/p01-g3-host-tests.log`); procedural-look correction12/12 (`/tmp/p01-g3-empty-look.log`).
- G6:68/68 host regressions (`/tmp/p01-g6-host-final.log`), all10 installed-reader tests pass in `/tmp/p01-g6-final-tests.log`. That earlier aggregate was75/76 due a new static Team test using the wrong boot ID; corrected test is included in final68. Earlier parser fixture failure was corrected using real textContent semantics and parsed text-node append; original failed logs retained.
- Couch readiness addition:35/35 relevant regressions (`/tmp/p01-g6-readiness-tests.log`) plus one actual deferred-fetch gate (`/tmp/p01-g6-readiness-gate.log`).
- G0 read-only review tests:22/22 boot/shared-status/offline-panel (`/tmp/p01-g0-review-tests.log`). Root corrected soundtrack initial-read generation race; no remaining concrete G0 finding after that and the renderer terminal fix.
- Browser correction regressions: `node --test game/test/replay-theater-navigation.test.mjs game/test/replay-theater-wide.test.mjs game/test/couch-markup.test.mjs game/test/coop-host.test.mjs game/test/couch-shell.test.mjs` —45/45 (`/tmp/p01-g6-browser-fix-tests.log`); then Team same-file correction `node --test game/test/coop-host.test.mjs` —15/15 (`/tmp/p01-g6-same-file-tests.log`). Shared launcher owner reports2/2 helper tests.
- Final scoped ESLint/Prettier and `git diff --check` passed after all browser corrections. No full-suite/build, release, branch/index/version or commit mutation by this owner.

## Isolated browser review

Real Chromium through `agent-browser`0.27.0, isolated sessions `p01-g6-browser` then `p01-g6-review2`, ordinary direct routes on own source server `http://127.0.0.1:63210`. Viewports390×844 and844×390 were explicitly set. Server/control/request log and screenshots are in `browser-g6/`; root server60743 and its media gates were not used. The gate delays real HTTP responses; Team additionally uses a finite `File.prototype.text` wait before delegating to the real method on an uploaded real JSON File. This is automated browser/keyboard evidence, not physical phone, chooser, controller, storage permission, or deployed-service-worker acceptance.

| Host / operation | Observed outcome | Evidence under `browser-g6/` |
| --- | --- | --- |
| Couch initial featured chapter, actual `fpv-arcade-r5.json` held44.388s | Loading text and outside-inert Back visible at both sizes, Back44px, no horizontal overflow. Tab/Enter activated Back. Reopening after release produced ready lobby without starting. A later explicit Start advanced the round to1:19; Escape paused it with explicit Resume required. | `couch-held-portrait.png`, `couch-held-landscape.png/.json`, `couch-ready-paused-landscape.png/.json` |
| Couch cosmetic presentation, actual compiled `runtime.json` held141.572s | Both sizes showed “Loading release artwork and fonts…” at top of ready lobby, Start enabled. Release ended status and hid it; no gameplay gate was added. | `couch-cosmetic-held-portrait.png/.json`, `couch-cosmetic-held-landscape.png/.json`, `couch-cosmetic-released.json` |
| Couch entry module HTTP503, shared direct loader | Both sizes exposed truthful startup error with Reload/Back44px, game inert, `toolState=error`, no horizontal overflow. Native Reload after removing failure reached real ready lobby, including cosmetic-pending readiness above. | `couch-module-failure-portrait.png`, `couch-module-failure-landscape.png`, `couch-module-failure.json` |
| Team actual JSON upload with controlled File.text read | With the native details picker opened and visible, both sizes showed reading plus Stop waiting44px and kept selected `relay-yard`. Stop waiting, Escape, and closing picker detached old work. No late selection or automatic play. | `team-held-portrait-corrected.png/.json`, `team-held-landscape-corrected.png/.json` |
| Team same-file close/reopen retry | Browser exposed stale file value: after close, selecting the same file did not emit change. Fixed `cancelImport` to clear value, while onchange captures a replacement File first. Actual retest observed detached/value empty/reads1; reopen and same file became ready/value empty/reads2, selected `browser-coverage`, playingfalse. | `team-close-file-cleared.json`, `team-retry-ready.json`, `team-same-file-retry-ready.png`; regression `coop-host.test.mjs` “Back and closing the Team pack picker detach a read before reopening” |
| Replay real first example fetch held62.322s after correction | Browser first exposed reading/Cancel below initial viewport. Moved existing scoped status/Cancel above main and made row sticky only during import. Both corrected viewports show real reading and Cancel44px in initial view; no horizontal overflow. Escape cancels without adoption. | Before: `replay-held-portrait-initial.png`. After: `replay-held-portrait-fixed.png/.json`, `replay-held-landscape-fixed.png/.json` |
| Replay second example fetch held10.192s while prior replay loaded | Busy status/Cancel remained visible at scroll position beside import controls. Escape preserved Copper Crossing at tick0/1305, paused, focused Play. A subsequent ordinary route example selection loaded Switchyard Circuit at tick0/1210, paused. Same-document cancelled decode/retry is additionally covered by actual host test. | `replay-prior-held-landscape.png/.json`, `replay-cancelled-prior.json`, `replay-retry-ready.json`; `replay-theater-navigation.test.mjs` real verification/cancelled artwork test |

Browser limits and discarded attempts are explicit. Installed-original library selection (A45/A46) was not seeded in this browser run; the10 real reader/host tests remain its decisive evidence. An early CLI click did not scroll the Team picker into view, and an upload therefore occurred in closed details; `team-held-portrait.png/.json` is an invalid setup, superseded by `*-corrected` files. `team-held-landscape-overscroll.png` and `team-held-landscape.json` record a deliberately overscrolled control, also superseded. Initial `couch-cosmetic-held.png/.json` targeted unused `manifest.json`, not actual `runtime.json`, so it proves no delay and is superseded. `replay-held-portrait-initial.json` was collected after the initial gate released; only its PNG proves that initial held view. Several CLI clicks outside the viewport were corrected with ordinary scrolling, and a hanging CLI wait was abandoned by closing that isolated session; those are not product outcomes. Final measurements and the final explicit Couch start/pause replace earlier incorrectly assumed click success. No remaining concrete G6 browser failure was observed in the accepted paths above. Both isolated G6 sessions were closed and own server63210 stopped after review; control gate is empty. An unrelated Studio input-isolation investigation occurred afterward: this review had no global CDP or keyboard loop, and its earlier explicit CDP port63216 was distinct from Studio49464.
