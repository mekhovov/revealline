# P01 G0 coverage and open qualification

Implementation source in codex/cross-mode-p01, integrated a395462. No release/version/public acceptance claimed here.

| Row | Implementation | Evidence / pending |
| --- | --- | --- |
| A01–A02 | Dependency-independent static game boot; deferred classic loader, monitored stylesheet loading, phase labels for missions/equipment, saved/installed state; mounted/Phaser/ready handshake preserved. | Boot + boot-build 17 tests passed; real held boot-script and stylesheet Chromium first paint/status/failure-target samples. Static first paint 80ms is local fixture evidence only. Main module and stylesheet failure/late disposal are modeled tests. |
| A06 | Immediate title Preparing label + visible owned status; download/verification/save/selection stages, home-visit generation fence. | chapter-download-host final5/5, title lifecycle tests included in29/29 G0 batch. Actual download hold, navigate Settings and late success preserves current modal. Corrected first below-viewport failure retained. Large/Plain/Reduced Effects at390x844 and844x390; Tab escapes disabled Deploy. |
| A07–A08 | One picture owner/prewarm promise; foreground observes existing work; status/cancel in flight overlay. | flight-pictures-host tests include one decode only, failed prewarm successful retry, pinned A/B, historical v2, paused checkpoint, background return, unavailable originals. 29/29 G0 picture/music/title aggregate, all source guards retained. Native delayed picture and saved-flight cancellation/retry checks are recorded below. |
| A09 | Restore status before replay validation, same restore AbortController, stage callback from pinned-art prepare, explicit paused adoption. | Existing actual host restore/pin/storage tests pass in14-case earlier set and29-case current set. Stale AbortError does not overwrite current warning/focus. Native delayed saved-flight sample below preserves saved JSON and requires explicit Resume. |
| A10–A11 | Theme-specific status/cancel plus candidate owner; stale finally cannot replace newer selector. Cosmetic craft/presentation status uses optional existing callbacks and never blocks a playable run. | flight-pictures and renderer focused tests; procedural/no-image supersession correction12/12. Native phone compiled-load sample is in the G6 record; actual Solo foreground samples are below. |
| A12 | Retry uses same prepared-art join/status contract; next current flow retains existing semantics. | picture-ready-status tests actual legal win/retry under both steering policies; failure/background checkpoints pass. No P07 progression redesign claimed. |
| A13 | Existing First Flight retention/Cancel path retained. | Relevant launch/entry/view tests to run in final source gates; source review preserves context/export/storage ownership. No extra artificial operation. |
| A36 app | Soundtrack summary consumes preparation, explicit initial read, generation comparison does not strand loading. Audio activation stays before await. | soundtrack-host included29/29; G5 detailed playback tests. Actual Music Studio controlled real MP3 Blob read, cancel and real retry to draft; original Blob method restored. No saved music mutation. |
| A42 Solo export | Replay dialog owns requested-download status, duplicate lock, Close detaches observation; late run/page changes cannot overwrite current warning. | practice-retry2/2 passes with exported replay validation; platform export tests in G1 44/44. Delayed native download adapter is not a physical iOS Save test. |
| Shared presenter | Lease-based DOM only, native measured progress, polite changing phase text, reduced-motion CSS. | operation-status4/4, offline-panel4/4, boot/status/offline independent22/22. Actual reduced effects yields animationName none. |

Current logs: g0-picture-music-tests.log29/29; g0-chapter-final-tests.log5/5; g0-chapter-replay-tests.log initially6/7 (missing original-image decoder fixture in new successful-Deploy test), corrected focused test1/1 and full chapter5/5; practice replay2/2 were already successful. All initial failures kept.

CSS zoom alone is not native browser zoom; the exploratory CSS200 image is retained but not acceptance evidence. Reflow model uses640x400 CSS pixels at DPR2 for1280x800 device pixels; label it modeled, not physical/native zoom. Music status/Cancel fit that model with Large/Plain.

Remaining: final cross-inventory review, related source/docs final diff, committed-source six gates+build, immutable release/public bytes/play/offline. Version0.57.0 is allocated. P02 remains blocked on P01 acceptance.

## Later root browser checks and corrections

- Native original-image decode completion was held at its real boundary. Flight status and Cancel fit844x390 and390x844. The first exploratory hold exceeded the existing decode deadline: visible error, paused attempt and successful ordinary Retry were observed; the `flight-cancel-before/after-decode` filenames describe the attempted action but their JSON correctly records a timeout/error, not a cancellation pass.
- A fresh saved-flight restoration was held and cancelled within the deadline. Saved JSON was byte-identical after cancellation and late completion; retry restored paused and explicitly required Resume.
- Native hiding dropped focus before the original Cancel handler checked it. Root now captures ownership before hiding; a real keyboard Enter cancellation ends focused on Start. Unit reproduction models native focus loss and passes1/1 (10 unrelated cases skipped). `saved-flight-keyboard-cancel-focus-corrected.json` is the actual browser retest.
- Music Studio uses a controlled real Blob.arrayBuffer completion with unchanged fixture bytes; status and Cancel fit portrait, landscape and modeled200% reflow. Cancel retained all5 original tracks, then a normal retry added the sixth only to the unsaved test draft. APIs restored after each gate. Earlier attempted mouse automation did not scroll to the offscreen Import action; that initial observation is not counted as an import/cancel success. Actual delayed import used native keyboard activation after focus.
- Catalog + First Flight entry/launch focused checks passed19/19. Explicit operation-status.css is included in catalog packaging for the auxiliary page host.
- Integration lint,format,format:native,validate,Motion syntax passed on the working tree. Full preliminary npm test running; exact committed source gates remain required. Production reproducibility initially detected changed source fingerprints; root appends metadata through the existing immutable generator, then verifies retained history and readiness. No pixels are replaced by this operation.

## Final independent corrections

- Collection retains score/difficulty metadata while a separate per-picture state reports loading or unavailable. Earlier integration failures3/3 were corrected; Collection/gallery57/57 pass in `collection-state-final.log`.
- Offline Stop waiting and completion capture focus ownership before hiding the button. `offline-focus-retest.log`5/5 includes native-hide behavior and background focus refusal. Actual DOM keyboard Enter retest returns to Check progress; late detached completion leaves status/focus unchanged (`browser/offline-keyboard-stop-focus.json`, `offline-late-detached.json`). That browser sample controls the worker-observer boundary; it does not claim a real worker install.
- Deploy changes only its label span, preserving the original copy hook and decorative aria-hidden arrow across success/failure/return. Chapter/title8/8 pass in `title-component-retest.log`.
- Preparing a new attempt preserves a pending replay-download lock. `replay-lock-retest.log`3/3 uses the real web export promise boundary with Close and Restart before settlement; late completion does not reopen the old panel or overwrite current status. Native Save-sheet duration is not claimed.
- Project runtime/release skills and phase prompts link the actual loading/ownership contract.

## Trusted emulated-touch follow-up

A fresh Chromium session used explicit CDP Input.dispatchTouchEvent with touch emulation enabled at each action. Five native touch activations and zero key events completed Deploy → visible actual held pack request → rotate to844×390 → Settings → old completion retained Settings → Close → cached Deploy → Missions Back. See `browser/touch-deploy-portrait.json`, `touch-leave-loading.json`, `touch-late-completion.json`, `touch-cached-deploy.json`, `touch-missions-back.json` and associated PNGs. Cached Deploy opened Missions without a minimum delay. This is emulated trusted touch in a real desktop browser, not a physical phone. The CLI resets touch emulation when reattaching; intermediate CLI measurements explicitly record touchPoints0, while each dispatched action receipt records5 and trusted touch events.

This sample found Settings Close was36×44. The shell now enforces44×44 for dialog-close controls. A fresh corrected run used CDP metrics/touch/screenshot directly, retaining5 touch points throughout each action/capture, and zero keyboard events. `browser/touch-fixed-{settings,close}-{portrait,landscape}.json/.png` records Settings and Close at390×844 and844×390; Close is44×44 at both, returns to Home, and original navigation is retained. Both owned sessions were closed and the server gate reset. These bounds do not certify every input/platform or P03's full menu navigation.
