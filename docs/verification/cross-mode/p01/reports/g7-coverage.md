# P01 G7 implementation and focused evidence

Working-tree implementation; no release acceptance or physical-device claim. Follow-up actual browser evidence is in [Studio/G7 qualification](studio-g7-browser-qualification.md); that report supersedes the applicable earlier browser-pending notes below for Studio, Motion, Playground startup/child readiness and Controller Lab startup/child readiness. Other gaps remain explicit. The table distinguishes executed controlled-boundary tests from browser work still required. Shared CSS is linked by every owned direct HTML route. The new classic `game/ui/direct-tool-launch.js` supervises dependency attachment only; hosts keep data, input, cancellation and commit ownership. Its 15-second notification says work is still loading and offers same-page Reload. It never re-executes a module in the same page or turns slow work into a false error.

| Inventory | Implemented behavior | Executed focused evidence | Browser gaps / N/A |
| --- | --- | --- | --- |
| A43 — Playground replay | Reading label before file.text; separate replay presenter and Cancel; real measured ticks only with positive total; older reads/results fenced; previous verification retained on cancel. Solo export remains G0. | `playground-import-host.test.mjs`: delayed file read, cancel, rejected late bytes, previous result retained, then real shipped Fieldcraft recording verifies its full simulation. | Browser file chooser, large recording cancel, native/keyboard controls and physical controller remain root qualification. No invented percentage. |
| A48 — Playground | Static tool loading signal; header Back links available during initial inert editor; file/generation/artwork validation phases; explicit preparation cancel; existing editRevision/import epoch retained; separate example-list status; download leases prevent late completion overwriting newer editor status. Preview load waits for Solo bootState or Couch toolState. | Actual entry tests cover deferred Classic original decode, stale/newer import, failed parse, explicit cancel retaining map/preview URL, generator replacement/Undo semantics, teaching imports. Child-document fixture proves iframe load remains busy, ready marker settles, old ready marker is ignored before next load, failed child shows recovery. Viewport pure tests pass. | Real iframe loading and failed module/network behavior, default/cached success, portrait/landscape/200%/Large/reduced-motion require browser review. No gameplay or authoring schema changes. |
| A49 — Controller Lab | Base and optional-practice load phases; scenario validation; child-loading phase; iframe load alone leaves virtual pad disabled; only authenticated same-origin, source and session status marks child ready. Existing neutral/sequence/input policy stays intact. | `controller-lab.test.mjs` executes actual entry and real scenario/pack preparation. New readiness case rejects wrong source before accepting current child. Existing status bounds, old-session rejection, neutral handshake, all physical indices, storage failure, course isolation and bfcache tests pass. | Physical pads and real iframe/network startup are untested here. Load practice remains retry path while waiting; no automatic reconnection of held controls. |
| A50 — controller settings | Existing small draft operation uses shared presenter; host guard/baseline/signature/AbortController logic preserved; scoped failure state; completion focus only within current focused tool. | `controller-settings.test.mjs` passes 17 cases, including delayed Apply, refresh/abort, changed binding signature, host failure, destroy and session-only persistence result. | Native focus with hidden settings/modal and real controller mappings need browser/device qualification. Existing host decides adoption; no new settings transaction or global lock. |
| A51 — enemy catalog | Immediate practice/import/export/save phases; one operation owner; cancellable read/preparation Back; stale import cannot publish or unlock newer work; non-abortable authoring save Stop waiting retains lock and reconciles actual completion. Workshop checks operation before session handoff; avoids calling child prepared state loaded. Static Open disabled until host attaches. | `enemy-catalog-panel.test.mjs`: delayed old/new file reads with cancellation/failure retain exact draft; delayed save Stop waiting stays locked then reports saved; existing enabled/skin/apply/import byte-budget/controller reading/back cases pass. | Actual workshop theme fetch and child-practice screen handoff need browser review. No scored-map/reward/profile writes introduced. |
| A52 — motion study | Static/initial three-file startup status and terminal retry copy; asset image-loading presenter; local background decode presenter; Clear available during decode; pending URL/token cleanup on replacement, clear and pagehide; bfcache keeps accepted image URL. Compact original formatting retained. | `direct-tools-loading.test.mjs` executes actual entry, withholds all three dependency requests, verifies busy first, then failure with Reload copy and visible error. Node syntax check passes. | Full success/recipe setup, local-image decode success/failure/cancel/pagehide and accepted-image preservation still need real-browser exercise. Existing model/animation/steering logic unchanged. |
| A53 — production register | Existing reader/controllers/generations reused with shared scoped register/source-preview state. Real 20-second deadlines stop activity even if an injected read ignores abort; stale late source is disposed, valid register retained. | `scripts/test-production-panel.mjs`: deferred preview status, Back and filter cancel/resource cleanup/focus; failed/mismatched PNG exact-byte checks; stale reload retains register; new forced deadline + late source never adopts. | Browser native image and direct-route module failure rendering, layout sizes and file links need qualification. Viewing remains metadata/source inspection, never production approval. |
| A54 — design atlas | Separate font/reference/clipboard presenters; initial labels before awaits; optional absent private references finish as expected unavailable source state; font failure keeps fallback text; late clipboard failure cannot change a newer selection. | Actual atlas entry test delays fonts and local HEAD; clipboard also delayed independently. Missing font/reference result is scoped; late copy failure retains focus outside Copy and does not select text. | Real font success/glyph rendering, successful clipboard permission, local screenshot decode and published/no-local-reference mode require browser qualification. Missing private files are not runtime errors. |
| A56 — reserve catalog | Static manifest signal; separate selected-original loading state; existing imageRequest fence gates success/failure; retry by reselection; only selected original requested, pinned source links unchanged. | Actual module test delays real 40-entry manifest fetch, changes selected image, delivers old onload, then current failure and successful reselection. Old result never settles new state. | Native decoded pixels, very large image loading, responsive source catalog and direct-route failure rendering require browser qualification. Source-only reference; no runtime art installation. |

## Validation record

- `.cache/cross-mode/p01/g7-qualified-tests.log`: 75/75 tests pass across direct-tool launcher, actual Studio host follow-up, Controller Lab, controller settings, enemy panel, Playground, ancillary direct tools and production panel.
- `.cache/cross-mode/p01/g7-replay-retest.log`: 8/8 Playground host cases pass after adding delayed replay/read/cancel plus real-recording verification.
- `.cache/cross-mode/p01/g7-build-viewport-check.log`: 4/4 static Studio dependency-allowlist and viewport tests pass. This did not build an artifact.
- Scoped ESLint/Prettier and `git diff --check` pass; `node --check authoring/motion-lab/app.js` passes. Motion study keeps its existing distinct compact formatting.
- Initial logs `g7-first/second/third/fourth/final-tests.log` and direct test retests preserve corrections: obsolete fake-DOM text/attributes/options semantics, iframe-load-only fixture expectations, combined-selector test limitations and a test selector matching tbody itself. Final assertions model current browser DOM semantics. No full npm suite or build run by this owner.

## Integration

- New `game/ui/direct-tool-launch.js` was explicitly reserved by root. Owned HTML loads it with data-module/data-status; entry modules call `RevealLineToolLaunch.attached()` after dependency evaluation. Test uses real dynamic import in VM with a missing static dependency, and real delayed top-level-await module proving late attachment wins over captured slow timer and no repeated module execution occurs.
- Couch owner added `document.documentElement.dataset.toolState` loading/ready/error; Playground observes it after iframe load at its existing measurement interval. Solo uses existing bootState.
- Root still owns global app/HTML/CSS release/build integration. Static Studio allowlist confirms the new loader/CSS are included. No storage/bundle/core changes, source acceptance, physical device qualification, commit or version change by this owner.

## Browser follow-up

Actual isolated Chromium checks and corrected layout/contrast findings: [qualification.md](studio-g7-browser-qualification.md). Four runtime files changed (StudioCSS, PlaygroundCSS, ControllerHTML/CSS), with47/47focused checks passing. Module/dependency503/Reload, nativePNG/save/export/import, preview/pixel cancellation, Motionlocalimage, and realchildreadiness evidence are qualified only to the extent recorded there. No source-only browser/device claim.


## Remaining direct-route native follow-up

A51/A53/A54/A56 now have isolated real-browser initial-module/data/failure/Reload and return-link samples at390×844 and844×390, including native Production/Reserve PNGs and all3Atlas font roles. See [remaining direct-route qualification](g7-remaining-browser-qualification.md) for exact screenshot/measurement names, source hashes, corrected6HTML/CSSfiles and24/24focused checks. This supplement supersedes the initial table's pending-browser wording only for its recorded cases. Missing private Atlas originals prevent successful native reference decode; actual absent-source fallback passes. Existing Atlas decorative portrait overflow is explicitly outside the P01 status correction.
