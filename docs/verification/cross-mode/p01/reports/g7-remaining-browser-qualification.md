# P01 remaining direct-route browser qualification

2026-09-15. Current source on isolated HTTP server `127.0.0.1:56357`, fresh named Chromium session `p01-g7-remaining`. Both 390×844 and 844×390 used. No root browser/server touched; no `wait --fn`, global key dispatch, persistent profile, production review, Git mutation, build, or version change. Session closed and server PID30390 stopped after checks.

## Method and scope

`server.py` served current source with no-store and CSP. `control.json` held selected real HTTP GETs, then released or returned503. `requests.jsonl` records actual holds (0.7–134seconds). Modules, browser font loads and selected original PNGs used native browser implementations. No Image, fetch, decode, font, storage or clock API was replaced. Measurements come from `measure.js`; JSON records viewport, document width, status text/state/rectangle/style, links and controls. Screenshot and JSON pairs use the names below. None of these checks is a physical-device/controller qualification.

## Confirmed defects corrected

- Enemy workshop: existing Level playground link was22px, then an inherited button attempt measured39px. Final native anchor is inline-flex/min-height44px; destination unchanged. Scoped generated Reload sizing and short-landscape paragraph spacing keep recovery visible while the independent release-artwork banner is present.
- Production register: existing Authoring library return link was22px. It is now44px. Scoped Reload sizing and short-landscape header spacing keep recovery visible alongside the independent artwork banner.
- Atlas: initial module/font status was y4205portrait/y3451landscape, including actual module-failure status. Existing font-status is now first normal-flow feedback before the header, with all header links at least44px. Destinations and runtime owners unchanged.
- Reserve catalogue: no visible return destination existed. Header now has44px Return to game. Native selected-image loading/error originally appeared below the blank original at y978portrait/y1082landscape, after the visible catalogue status had already finished. The existing image-status node now appears beside catalogue status before the chooser. Image selection, request fencing and pinned sources unchanged.

Six HTML/CSS files changed; exact names and SHA256 are in `source-hashes.json`. No module/JS/store/shared-style change in this follow-up.

## Actual cases and final geometry

| Inventory | Real delayed/failure/recovery case | Final viewport evidence |
| --- | --- | --- |
| A51 Enemy workshop | Entry GET withheld; themes GET withheld10.956s then503; native Reload reached seven registered roles. Separate module503→generated Reload→ready also exercised. Escape closed the automatic panel; native focus+Enter on Level playground navigated to `/game/playground/`. | `enemy-module-held-final-{portrait,landscape}.png/json`; `enemy-module-failure-final-{portrait,landscape}.png/json`. Portrait final error y323/h163, Back y261/h44, Reload y504/h44. Landscape with artwork banner: error y239/h81, Back y187/h44, Reload y328/h44. `enemy-data-failure.json`, `enemy-ready.json`, `enemy-ready-fixed.json`, `enemy-back-fixed.txt`. |
| A53 Production register | Entry withheld; register GET withheld8.348s then503; native Reload register succeeded revision1/116matching slots. Module503→generated Reload→ready also exercised. Preview original used real Orchard Crossing PNG; held GET then Clear removed the preview, release did not restore it; retry decoded1774×887/2999096bytes and added no assessment. Native Authoring library link navigated existing `/authoring/` directory page. | `production-module-held-fixed-{portrait,landscape}.png/json`, `production-data-held-portrait.png/json`, `production-data-failure.json`, `production-ready.json`. Final module error: `production-module-failure-final-{portrait,landscape}.png/json`; portrait error y320/h136, Back y16/h44, Reload y472/h44; landscape with artwork banner error y245/h81, Back y16/h44, Reload y342/h44 (bottom386). `production-image-held.png`, `production-image-cancel.json`, `production-image-ready.json`. |
| A54 Atlas | Actual atlas.mjs503 produced terminal error and44px generated Reload. Native Reload then withheld all three WOFF2 requests; document.fonts reported all three loading. One real font503 produced one error/two loaded and visible fallback warning. Reload with released requests reported all three loaded. Native brand+Enter navigated `/game/`. | `atlas-module-held-fixed-{portrait,landscape}.png/json`, `atlas-module-failed-fixed-{portrait,landscape}.json`, landscape failure screenshot. Module status y0/h144portrait or h72landscape; Reload44px and header Back44px fully in view. `atlas-font-held-{portrait,landscape}.png/json`: y0/h48 at both. `atlas-font-held-native.json`, `atlas-font-failure-native.json`, `atlas-font-failure-{portrait,landscape}.json`, `atlas-ready-native.json`, `atlas-back.txt`. |
| A56 Reserve catalogue | Catalog entry withheld133.64s while static status remained truthful. Manifest withheld then503→native Reload→40entries. Initial original request withheld, failed503, then reload decoded complete native1774×887PNG. Only selected original was requested; native Return to game+Enter navigated `/game/`. | `reserve-module-held-{portrait,landscape}.png/json` (before return-link correction); final `reserve-data-held-{portrait,landscape}.png/json`; `reserve-data-failure.json`; `reserve-image-held-fixed-{portrait,landscape}.png/json`; `reserve-image-failure-{portrait,landscape}.json` and landscape screenshot; `reserve-ready.json`. Catalogue status y264 at both; native image status y328/h24portrait, y304/h24landscape (error h48/bottom352). Back44px at y16portrait/y24landscape. |

Final source originals were read-only and still match their declared SHA256: Orchard Crossing2999096bytes, `20f3a1c03a074d3aae981c989cb063a092d577cb0c0318ea4a1fd1df1d0653fc`; Sunflower Signal2943263bytes, `dc20bbb10005b385590da02e8401572089a78022239e8ffc41a365ca322af776`. These native dimensions are browser observations; this does not approve production art or typography. No file downloads were invoked in this bounded follow-up.

## Focused validation

After final source edits: `node --test game/test/direct-tools-loading.test.mjs game/test/direct-tool-launch.test.mjs game/test/enemy-catalog-panel.test.mjs scripts/test-production-panel.mjs` —24/24pass,0fail,0skip (`focused-tests.log`). Prettier applied to all six owned files. No full suite/build run. Existing tests cover actual entry/font/reference/selection fencing plus preview bytes, abort/deadline and stale-resource disposal; browser results above separately qualify native integration.

## Explicit remaining limits

- Atlas private source screenshots do not exist in this checkout. Actual local HEAD failures produced the expected absent-source explanation (`atlas-*-native.json`). Successful native decoding of those absent reference originals is not exercised; source tests cover deferred/missing reference branches. No invented substitute files were added.
- Lower-priority existing Atlas portrait layout overflow remains:450px decorative hero starts at x20, document width470 on390viewport; lower screen-study art also exceeds its panel. Top status and header remain exactly390px wide and usable. Evidence `atlas-unrelated-overflow.json`. This is outside the P01 status/recovery correction and is not a clean whole-page responsive-layout claim. Production snapshot/documentation links also retain legacy small text hit areas; the relevant return/Reload controls are44px.
- Enemy child practice, all register records, all40reserve originals, successful clipboard permission and published/no-private-reference Atlas mode were not resampled here. Earlier implementation/host/browser receipts retain their own scopes. No new full acceptance or public-release claim is made.
