# Studio / direct-tool browser qualification — P01

Executed 2026-09-15 against the shared current source served read-only at `http://127.0.0.1:49462`, with an isolated request-gate server (`server.py`, `control.json`, `requests.jsonl`). No original-worktree, version, Git index, release or player-store changes were made by this browser task. `p01-studio-qualified-clean` was the accepted browser session; `p01-studio-qualification` was closed after the automation interference described below. Chromium desktop viewport emulation, 390×844 and 844×390; this is not a physical phone or controller result. The browser user agent is preserved in `studio-dependency-recovery.json`.

## Runtime corrections from browser findings

- `authoring/asset-studio/studio.css`: the global message and Cancel/Stop waiting strip stays visible while working farther down the editor and retains the terminal result. The top Workshop link now has a 44 px target. A busy-only sticky trial was broadened to terminal states too, since an immediate validation failure must remain visible.
- `game/playground/playground.css`: changed the old cream status backgrounds to Field Kit panel/text colors. The shared presenter's cream text had insufficient contrast on the old background. Initial module status now occupies normal document flow instead of covering the header.
- `game/controller-lab/index.html`, `style.css`: moved the existing status before the long introduction/setup, made it remain visible for later loading, and made header links at least 44 px. The original portrait status began at y811 and was below the landscape fold.

These are the only runtime edits in this follow-up. They add no store, navigation, editor or gameplay behavior.

## Executed browser cases

| Case | Actual evidence and result |
| --- | --- |
| Studio pre-module and dependency failure | Withheld `studio.mjs` over HTTP; five static mutations disabled and signal visible. Final status bounds: portrait y0/h66; landscape y0/h49, page width exactly390/844. `studio-module-final-portrait/landscape.png` plus matching JSON. Actual HTTP503 of imported `game/presentation/studio-session.mjs` produced terminal failure, `toolState=error`, disabled mutations and native Reload. `studio-dependency-error-portrait/landscape.png`, `studio-dependency-recovery.json`. Native keyboard activation of Reload after restoring dependency reached Saved studio workspace loaded (`studio-final-recovered.json`). Entry-module503 was also exercised earlier. |
| Studio PNG read/decode/candidate cancellation | Generated a real24×24 project fixture PNG,134bytes (`original.png`). Native FileReader/readAsDataURL was withheld, Cancel invoked abort, stage stayed disabled. Native createImageBitmap was then withheld, cancelled, and a new upload reached native canvas encode; releasing the old decode closed its bitmap without unlocking the new operation. `studio-stale-decode-new-encode.json`: closed1, encodes1, replacement inert=true, stage disabled, exact Encoding crop message. The initial read/race case preceded the excluded repeated-save interference; it is independent of ordinary-save evidence. Delay wrappers passed unchanged Blobs to the original browser APIs and were removed by closing that context. |
| Studio clean native upload/stage/save | Fresh session, real file input upload and native image decode/canvas crop. Entered explicit fixture provenance, staged, then separately invoked Save once by keyboard. `studio-clean-native-save.json`: generation1, documentr19 (compiledr18 base), only `revealline-presentation-studio-v1`, empty local/session storage, source134bytes preserved with SHA256 `0ae5d58c1169056d086892937112a4605d56ca11b6d11e701d1e7796b18a6af7`. Two single Enter events correspond to Stage and Save. |
| Studio export/import | Native keyboard Export created the actual6,669,364-byte download `downloads/revealline-fpv-r19.rltheme`. Node re-import validated128assets and exact original bytes (`studio-export-verification.json`); native browser file-input re-import also fully validated/decode-tested the bundle and atomically staged it (`studio-native-import.json`). Reset returned to the saved workspace. Download proof refers to this isolated Chromium download file, not OS share/save completion elsewhere. |
| Studio non-abortable save | Delayed the first crypto.subtle.digest invocation, retaining the real digest implementation/bytes, after host commit ownership began. Stop waiting left mutation regions inert and message truthful; `studio-commit-stop-waiting.json/png`. Released original digest; native verification/IndexedDB save completed, terminal success reconciled. Later Reload found generation2. No claim that the underlying save was cancelled. |
| Saved/draft previews | Both native bitmap decodes withheld, separate statuses and44pxStop waiting buttons visible. At portrait current y399.5/h44 and draft y722.5/h44; landscape each can be brought into view independently (single-column comparison). `studio-previews-held-portrait/landscape.png/json`. Both cancellation actions were invoked by native keyboard; releasing native decoders closed2latebitmaps and left0painted canvases with both stopped-waiting labels (`studio-previews-cancelled.json`). Retry performed a real native decode before the next test. These are raster previews, not every scene/audio/font surface. |
| Pixel edit/cancel | Opened blank raster, used native Space to draw a pixel, delayed the original HTMLCanvasElement.toBlob for Prepare edited sprite. Sticky message and44pxCancel visible at both sizes: portrait y0 status, Cancely49; landscape statusy0, Cancely2.5. `studio-sprite-encode-held-portrait/landscape.png/json`. Escape then released original encoder. The editor stayed open; staged candidate stayed disabled; focused-canvas bytes (including the same focus cursor overlay) remained identical. Save correctly refused unprepared pixel edits (`studio-sprite-cancel.json`). Initial canvas comparison used differing focus states; corrected comparison uses identical focused states, not altered pixels. |
| Native invalid PNG/retry | Real invalid file failed header validation, with terminal message still visible above the deep inspector; `studio-upload-error-portrait.png/json`. Subsequent real PNG prepared successfully. The second prepared candidate was discarded; saved bytes remained intact. |
| Motion direct startup/image/Back | Withheld app.js; initial status visible at both sizes (`motion-module-held-*.png/json`). Released it: real three JSON study files and required setup reached Motion study ready. Delayed only the native image src assignment of a blob URL; Clear revoked URL and kept cancelled message when old work was released (`motion-background-held.png`, `motion-background-cancel.json`). Restored native image/URL APIs; re-upload reached original.png24×24 via native decode. `motion-ready.json`. Native Return to game activation navigated to `/game/`. |
| Playground direct startup/child/Back | Corrected initial status visible portrait y12/h53 with no overflow (`playground-module-fixed-portrait/landscape.png`, portraitJSON). Withheld real child `/game/app.mjs`, while allowing child document load. Host remained Child document loaded. Waiting for game readiness; `playground-child-held-portrait/landscape.png`, landscapeJSON. Released app; actual child boot ready settled the host (`playground-child-ready.json`). Native Back to campaign navigated to `/game/`. Wrong earlier gate `/game/bootstrap.mjs` never matched a request; that preliminary run is not delayed-child evidence. The accepted gate is `/game/app.mjs`, recorded in requests.jsonl. |
| Controller Lab direct startup/child/Back | Corrected initial status fully visible: portrait y189/h51, landscape y129/h51 (`controller-module-fixed-*.png/json`). Withheld real child app before correction: iframe document loaded, child `bootState=loading`, host waiting, Connect disabled (`controller-child-held-landscape.json/png`). Released app; exact live session handshake reached Practice controls are ready and Connect enabled (`controller-child-ready.json`). Same readiness result after final-layout reload, and native header Back navigated to `/game/`. No physical pad connected. |
| Reduced motion | Browser CSS media emulation enabled prefers-reduced-motion:reduce. Signal computed animationName values are all none; final Studio recovery also retained reduced preference. `reduced-motion.json`, `studio-final-recovered.json`. This is CSS emulation, not a physical OS setting test. |

Raw bounds are consolidated in `measurements.json`; individual capture JSON includes scrolling, inert/disabled state and relevant rects. Some earlier DOMStringMap values serialize as `{}` through the CLI; readiness is proved by explicit state reads and host status/control transitions, and later measure-tool.js spreads the dataset into plain data. Actual keyboard activation was used for action/navigation proof. The CLI pointer click did not consistently trigger the target in this environment, so no pointer/touch-device claim is made.

## Excluded automation run and corrected harness issues

After one early Save command followed by `agent-browser wait --fn 'document.querySelector("#cancel-studio-operation").hidden'`, the initial browser received thousands of unexpected trusted KeyW/Unidentified keydowns and repeated trusted detail0Save clicks. Its isolated generation rose139. `automation-input-interference.json` and `save.cpuprofile` retain diagnostics. The cause is unresolved; the timing correlation is not proof that wait caused it, and it is not attributed to product behavior. All peers confirmed independent session targets. The context was closed and the ordinary save was repeated in a new context using only bounded read-only polling; generation1 and bounded input events passed. That contaminated save is excluded from acceptance. Any `.crdownload` in downloads is excluded; only the named verified `.rltheme` is download evidence.

An early native bitmap wrapper failed to preserve Window binding and threw Illegal invocation. It was corrected to nativeBitmap.apply(window,args) before accepted decode race evidence. A repeated top-level `const style` declaration during live CSS refresh was replaced with a scoped function; final captures use refreshed source CSS. These were test instrumentation errors, not application errors. All clean-context native wrappers were restored (`studio-clean-final.json`); later reload removed all instrumentation. No browser instrumentation is shipped.

## Focused checks and remaining qualification

`focused-tests.log`:47/47 pass across actual Studio host/loading/build allowlist, direct launcher, ancillary direct-tool startup, Playground host and Controller Lab. `eslint.log` and `diff-check.log` pass; Prettier checked the four changed runtime files. No full suite/build/version/commit work by this owner.

Still not qualified by this browser task: Studio every scene/font/audio audition/clipboard permission/quota/competing-tab CAS; large recordings and Playground import/generation native-file scenarios (focused host tests cover races); enemy workshop, controller Settings modal, production register, design atlas and reserve catalog direct-route native rendering/clipboard; Large text/200%zoom on all those tools; physical gamepads/phones, native OS share dialogs or production art acceptance. Motion decoded one local PNG and its actual startup assets, not every recipe. These remain explicit gaps for root's total inventory, not silently passed source inspection.

Cleanup: both owned browser sessions were closed; owned request-gate server49462 stopped. Other agent sessions/servers and the original worktree were untouched.
