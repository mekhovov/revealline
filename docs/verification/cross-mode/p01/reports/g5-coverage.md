# P01 G5 coverage — source and focused tests

G5 implementation ready for integration. This is not browser, physical-device, full-phase or release acceptance. Detailed design/corrections/file ownership: [g5-delivery.md](g5-delivery.md).

| Inventory | Exact implementation | Decisive focused checks | Remaining evidence / not applicable |
| --- | --- | --- | --- |
| A35 | game/ui/story-dialog.mjs open/readMedia/acquire owner; game/ui/victory-story.mjs render/preparing/starting/dispose | story-dialog.test: initial held read visible before acquire, exact poster retained, error terminal, close during delayed acquire and reopened owner; victory-story-ui.test: preparation/play signal, muted native preparation, reduced motion, timeout, stale video/frame epochs | Actual browser story decode/play/Close/Skip with available retained story; physical codec/device is separate. No new story or award behavior. |
| A36 | game/ui/soundtrack-player.mjs startAt/play/snapshot preparation; existing ui/published-audio.mjs read/decode fallback reviewed unchanged | soundtrack-player.test: silent held preparation, current-gesture cached start before enable settles, Pause fencing, overlap cached-play observer; published-audio.test and soundtrack-gain-leases.test retain no-late-cue and mixer contracts | Root owns initializeSoundtrack/activateAudio and summary observation. Background cue decode does not need a blocker or per-cue announcement: original event uses immediate procedural fallback and never replays late. |
| A37 | game/ui/soundtrack-panel.mjs task/reload/commitDraft/importMP3Files/export/audition/control status; sticky operation bar in soundtrack-panel.css | soundtrack-panel.test: first read immediate/cached/delayed/error/blur/focus/dispose, real MP3 and atomic store/bundle bytes, cancellation vs durable result, timer announcements isolated, delayed audition close/reopen | Root browser: Music Studio first read + controls, mp3 preparation, audition, phone/short landscape/zoom. OS audio activation is not proved by modeled tests. |
| A38 | authoring/still-media/index.html/launch.js pre-module signal; ui/still-media-host.mjs open/readBase/panel.open/prepareAudio/cancelOpen/dispose | still-media-host.test: held catalogue read immediately busy, Close usable, late failed read cannot replace closed state; existing actual v4/audio export/legacy refusal/router ownership cases | Root browser: delayed route/module/open, visible independent media storage action, explicit backup link. Loading itself does not open storage. |
| A39 | game/ui/still-media-panel.mjs work/load/upload/savedPreview/commit/prepareDownload/reviewBundle/restoreBundle/cancelWork; sticky status/Cancel/Close in still-media-panel.css | still-media-panel.test and still-media-bundle-ui.test exercise real retained bytes, atomic saves, codec failure, catalog race, cancellation, originals export/import and explicit download | Root browser: initial load and held preview/cancel with header signal and reachable controls; gallery redesign/P04 excluded. |
| A40 | game/ui/still-story-panel.mjs guarded/load/inspectVideo/capture/prepare/save/clear/prepareDownload/reviewBundle/restoreBundle | still-story-panel.test + still-story-lifecycle.test retain parent task/selection generation, original/capture/staged binding/commit/cancel/reload and stale original cleanup | Parent picture-workshop operation reports history read; child reports optional-story error locally. Root browser story editing sample pending. No new movie/schema. |
| A41 | authoring/video-poster/index.html/launch.js pre-module signal; game/ui/video-poster-workshop.mjs begin/inspect/capture/stopTask/cancel/dispose | video-poster-workshop.test: cancelled delayed inspect visibly terminates, stale older inspect cannot replace newer notice/source, previous PNG preserved, exact download and controller navigation cases | Root browser native file/codec wait/seek/cancel, responsive status. Source preview remains local and does not modify game/media storage. |

## Final logs

- `.cache/cross-mode/p01/g5-qualified-tests.log`: 172 tests / 172 pass / zero failures, cancelled, skipped, TODOs; 12 focused existing files; 9,007.955417 ms. Includes new tests and assertions listed above.
- `.cache/cross-mode/p01/g5-route-visibility-tests.log`: 26 tests / 26 pass / zero failures, cancelled, skipped, TODOs; 444.335375 ms. After the qualified run, root's actual 390×844 held-module browser review found the Still Workshop notice below the viewport. The notice now immediately follows its h1, followed by a 44-pixel-minimum Back link; the Video Poster notice likewise follows its h1. Two focused source-order assertions cover this correction. Root retains the initial screenshot at `browser/still-media-held-module.png` and owns live viewport revalidation.
- Scoped ESLint: exit 0, zero warnings (14 changed MJS production/test files).
- Prettier: 20 changed production, route, style and test files passed. `git diff --check` passed.
- Retained intermediate logs: `g5-first-tests.log` (136/137, obsolete direct-P status lookup corrected), `g5-second-tests.log` (168/168), `g5-final-tests.log` (170/170), `g5-visible-status-tests.log` (intermediate node-before-attachment layout defect; actual node reference fixed), and final qualified log. Failures were corrected and not relabelled as passes.

No full suite, ordinary build, commits/version bump, network publication, public HTTP audit, live browser run or physical-device testing performed in this G5 implementation slice. Root retains those gates. No skipped required source task in A35–A41; remaining app A36 integration and actual browser acceptance are explicitly owned above.

## A04 auxiliary document supplement

Root additionally reserved `game/presentation/page-entry.mjs`. It now observes the existing shared `mountPresentationPage` through one document-scoped auxiliary owner. A nonblocking status directly after h1 reports cosmetic collection/font loading, hides after the accepted snapshot is applied, and retains the exact existing release-art warning on failure. Other local tool notices, focus, controls, storage, authored previews, playback, and artwork fallback rules are untouched. Explicit `[data-presentation-status]` or `#presentation-preparation-status` owners suppress the auxiliary notice. Repeated entry calls share the existing observer; ordinary page leases retain their independent lifecycle. BFCache keeps observation; nonpersisted pagehide disposes the notice and lease and fences late callbacks. Shared operation CSS is linked only when its resolved URL is absent.

`game/test/presentation-page-entry.test.mjs` adds five actual shared-host fixtures for immediate loading, errors, duplicate mounts/CSS, cached readiness, explicit owner coexistence, focus and local-work preservation, BFCache, and late completion after disposal. Combined with existing page/host tests, `g5-a04-entry-qualified-tests.log` passes 23/23, 1,026.963834 ms, zero failures/cancelled/skipped/TODOs. Scoped ESLint, Prettier and diff checks pass. The initial `g5-a04-entry-tests.log` (22/23) remains: the minimal DOM fixture does not recognize numeric h1 tag selectors, so the dedicated fixture now supplies that standard query without changing the shared helper or runtime behavior.

Root integration hook: generated release catalogue UI also imports this entry. Its recursive copier discovers the new MJS import automatically, but `scripts/release-catalog.mjs` must explicitly copy `game/ui/operation-status.css` because it does not scan dynamic URL expressions. Root was notified and owns the script/fixture change. Live auxiliary-page delay/failure and responsive review remain root evidence.

## Subsequent bounded real browser follow-up

The root-assigned G4/G5/A04 sample is recorded in `browser-g45/review.json` and its hashed evidence manifest (60 files before this coverage update; browser profile excluded). A separate source server and named Chrome session used existing fixture files, actual native video/image decoding, IndexedDB and explicit controls. Library file-read cancellation/retry, native optional-story picture drafting, auxiliary cosmetic hold/failure, and native poster encoding cancellation/late completion/retry passed within the report's explicit scope. No picture assignment or story was saved, and no public release or physical-device result is claimed.

This browser sample found one further production defect: while scrolled to Capture, Video Poster's busy notice was above the viewport. `authoring/video-poster/workshop.css` now keeps that existing status sticky below its existing Back navigation. Repeated actual held native PNG encoding proves the notice remains visible at 1280×800, 390×844, and 844×390; the short landscape screenshot includes the focused Cancel and notice together. Original failed visibility evidence remains. Final relevant source fixtures pass 31/31 in `browser-g45/final-focused-tests.log` (747.804542 ms); CSS formatting and scoped diff checks pass. The two native API wait wrappers were restored, request gates cleared, and the isolated browser/server closed.

Initial relative file-path automation error and two unactivated pointer attempts are retained as automation limitations. Focused Enter activated the actual controls. This sample does not establish touch-only, controller-only, or keyboard-only navigation; complete backup export/restore and saved-story playback remain outside its claimed evidence.
