# Pending launch Back and visible picture recovery

P01 remains open. These corrections join the unpublished v0.57.4 candidate; package, lockfile and build already reserve that unused version. Historical v0.57.3 bytes and prior candidate observations stay unchanged.

An ordinary Retry on archived v0.57.3 remained running after an early Escape. The native observation did not establish the precise pending state at the key event. Source inspection and held-picture host tests reproduced the cause: flight input is inactive until launch, while the ready menu's Back path merely focuses Start. The visible Cancel button already invalidates the launch correctly.

The correction handles Back after modal/course ownership and only while the current picture launch owns preparation. It invokes the original Cancel action and restores Start focus. It does not reinterpret speculative prewarm, saved restoration, theme preparation or settled recovery. Six host cases exercise Start, Retry and confirmed Restart through mounted keyboard listeners and modeled standard-controller input, with late success/rejection, unchanged checkpoint, selected content, saved bytes and originals. Ordinary ready-state Back and terminal recovery retain their behavior.

The isolated baseline failed the pending cancellation assertions. An intermediate Retry fixture also incorrectly required exactly one held decoder; the retained diagnostic observed two valid decodes during the win/Retry transition. The final fixture waits for at least one held decode plus the current busy launch without changing the shared five-second readiness allowance. Its complete corrected flight file passes 19/19. The earlier six-file run retained 157 passing unaffected cases and two fixture failures; those receipts are not relabelled as one uninterrupted passing batch. A download's existing one-minute URL cleanup timer explains an earlier apparently idle history process. All original logs and corrections are retained in [source evidence](pending-picture-back/source/review.json).

Independent native review of candidate `114a5eb` then found that an unjoined background-picture failure exposed Export/Reload but left the inline reason hidden. The existing `warmPicture` failure path only populated the general warning underneath the overlay. A new setup-selection regression, without pressing Start, first fails on that hidden inline status. The correction publishes its terminal message through the existing preparation presenter only when no launch, preparation or theme operation owns it. The fixed targeted case passes, including Export return, unchanged storage and no extra reads/writes. Existing stale-history coverage now also checks the inline message. This does not change artwork resolution, writer ownership or gameplay.

The final integrated flight/history host run passes 25/25 on Node20, with focused lint passing. Its source pins and original output are in the evidence bundle. Exact committed-source qualification and native message/control geometry are recorded separately as they finish. Public/offline verification remains required. Neither modeled controller routing nor browser viewport emulation qualifies a physical device, and no later phase is accepted here.
