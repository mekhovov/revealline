# Recovery verification feedback correction

Public v0.57.2 successfully verified and downloaded the selected Dawn original, but its option and summary still called the bytes unverified. The [independent public observations](legacy-transfer-index/independent-recovery/review.json) and [unchanged originals](legacy-transfer-index/independent-recovery/evidence.zip) retain that contradiction and successful native download. The separate [earlier-release Copy failure](legacy-transfer-index/public-failure/review.json) remains preserved independently.

The selected option and summary now describe the existing verified review handle. Reverification, selection changes and fresh original review clear the label immediately; cancelled or failed verification cannot restore it. The wording is past tense: file preparation always rechecks bytes, so a later failed preparation keeps its accurate error and the existing other-component link. Reader handles, stored descriptors, original bytes, locks, component URL ownership and focus remain unchanged.

The enhanced view fixture failed four assertions against the previous runtime. After correction, four related fixtures passed 58 tests with no failures, cancellation, skips or todo; scoped lint, formatting and diff checks passed. [Source receipt](recovery-verification-copy/source-receipt.json), [exact original logs](recovery-verification-copy/source-originals.zip) and [member/hash mapping](recovery-verification-copy/source-originals-record.json) distinguish these modeled checks from native evidence. The unchanged receipt retains its original loose `.log` paths; the archive durably preserves those ignored files under the explicit mapped member names. They cover opaque handle use, exact option/summary selection, successful preparation, delayed reverification, cancellation, verification failure, retry and fresh-review invalidation.

The corrected native/public recovery sample remains part of the next patch qualification. This source correction does not accept P01, earlier-release Copy or the wider recovery matrix.
