# Picture recovery after Export

P01 remains open. This v0.57.4 correction addresses a required recovery-flow defect observed on immutable v0.57.3, source `7945c6d07714d41cee9b6f848059b8dc18aed86c`. Its [completed public review](../../../../publishing/pages-controller/delivery/evidence/cross-mode-p01/public-v0573/README.md) preserves all three browser families, including the failure. This correction does not change that release or claim the new source has completed qualification.

The public portrait journey visited Release Explorer, returned through a genuine persisted browser-history restore, continued the retained Orchard Crossing flight, then selected an enabled Base game / First Signal picture whose exact FPV owner was absent from retained media. The game correctly refused to create new picture history without its writer and displayed Export game data and Reload saved profile. Export opened Library's Saves page. Closing Library then removed both recovery actions and lost the invoker focus, although the same recovery message still told the player to export and reload. The saved run, checkpoint, picture pins and original-store endpoints remained intact.

Library opening calls the ordinary pause handler. That handler previously cleared all picture preparation and recovery, including an already completed writer-required error. The existing host test hid the defect by clicking Check picture again after closing Library.

The correction retains terminal recovery only when its exact picture owner, run and theme are still current and no preparation or theme operation remains active. Incidental pause can preserve that state while a secondary dialog opens. The visible Export invoker then remains available to the existing dialog focus restoration, and Reload can be selected directly on return. Actual retry, world selection, context replacement, saved-flight restoration and page retirement still clear the previous state. Pending work retains its cancellation and late-completion protections.

The host regression now follows Export → Library → close → original invoker → Reload without another Check picture. It verifies unchanged simulation checkpoint, raw saved data, original writes, picture-read count and writer acquisition. A separate boundary case checks explicit retry, world change and page retirement; the existing pending-decode and background-return cases remain applicable.

The first four-file focused run passed 39 of 40 tests. The new boundary test used the wrong presenter state name (`preparing` instead of the existing `busy`); the actual recovery visibility assertion had passed. Its original failure remains evidence. The test now uses the presenter's real contract without changing runtime status semantics; all five history-host checks pass on the corrected run. The [source review and original TAP logs](picture-recovery-return/source/review.json) keep those runs separate. Native return focus, exact-source gates, immutable publication and public verification remain required before acceptance.

The v0.57.3 public browser originals retain separate recorder limitations: the first run stopped at an unexplained socket close; the corrected transport used a pinned installed WebSocket implementation. Two native replay downloads completed to the same browser path, leaving one final byte-verified file. The first file's historical bytes were not independently retained. Neither a recorder failure nor an overwritten evidence filename is reclassified as a product failure or an unobserved product pass.

The [local native attempts](picture-recovery-return/native/review.json) preserve 248 originals from five closed attempts. The first four stopped at recorder prerequisites or limits. The fifth verified the actual changed DEV source, an ordinary flight, a trusted persisted Back navigation, and paused Continue at 390×844. It could not exercise the recovery correction: both enabled FPV originals were already retained, and selecting Ukraine correctly used its separate artwork path. Raw saved data and original-store endpoints stayed equal after return; an earlier menu save changed only the departure timestamp. The required direct Export → close → Reload browser journey remains pending. This evidence does not qualify public play, offline behavior, landscape, or physical devices.

A subsequent local native run on source `114a5eb42083009db6865d50bcbb53a8e7592b53` completed the controls-and-focus portion of the recovery journey. It installed the optional illustrated FPV pack without choosing it, verified that its exact current picture owner was absent, returned through trusted persisted Back, and chose the already installed mission. Export → Library → Escape restored Export focus with Reload still visible at 390×844 and 844×390. Direct product Reload created a fresh document while preserving raw saved data and every observed media-store endpoint. No second Check picture was used. The [additional native evidence](picture-recovery-return/native-followup/review.json) preserves this passing scope and the earlier offscreen-target refusal. Native Enter produced no observed activation during installation, so a verified visible pointer action completed that step; this is not a keyboard-only claim. Short-landscape labels outside the recovery controls remain a P03/P05 layout finding. Final-source and public qualification remain required.

Independent inspection of the same native controls found a separate limitation: `flight-preparation-status` was empty, hidden and 0×0 in both recovery captures. The message was present only in the general warning beneath the overlay. Choosing installed content had failed during background preparation before Start joined it, bypassing the launch presenter. The controls/focus result above remains valid; it does not prove a visible recovery reason. The [pending launch and unjoined-prewarm correction](pending-picture-back.md) addresses this finding before final qualification.
