# Recovery-panel recipe review

This review approves the seven existing `screen.*.background` recipes for screen-input SHA-256 `dd485d897456b7eeea1e435cf44975ec785d8502177de655a57f5bfdbfb054f0`. The producer concatenates `field-kit-flow.css`, `field-kit-surfaces.css`, `field-kit-compiled.css` and `site/release-catalog.css` in that order. Only the surface file changes from the [previous scoped approval](loading-placement-recipe-review.md).

The 26-line correction applies to a game overlay with its original flight-preparation status visible. It restores the shared control-text token, prevents message shrinkage, gives short landscape more width and less empty spacing, and allows panel scrolling at constrained sizes. It preserves every action, the reading pane, 44-pixel minimum targets, input handlers, operation ownership and storage behavior. Other screen backdrop rules and all original artwork are unchanged. The source review found no concrete blocker.

Native Chromium source `0b368297` provides the [original evidence and scoped results](recovery-layout/review.json). R5 verifies the complete message and recovery actions at Standard text in 390×844 portrait and 844×390 landscape. Native Export → Library → Escape restores Export focus and the same terminal reason. R6 sets Large through the real Settings control and verifies both viewports without manual scrolling; landscape recovery buttons remain entirely visible through y=361. Direct product Reload creates a new document with the saved Orchard flight offered as Continue. It does not claim a resumed flight after Reload.

Raw player-storage endpoints and observed media rows/blob digests remain identical across each run's post-Back recovery sequence, including Large selection and Reload in R6. The ordinary earlier departure changed only its recorded save timestamp. The earlier clipped-layout failure remains in [the pending-picture review](pending-picture-back.md). R5's existing landscape Settings button extends 1.3 pixels above the viewport and was refused by the full-rectangle pointer guard before input; this is an open P05 navigation finding. R6 uses the visible portrait Main menu → Settings route. Its unsuccessful End/arrow selection attempts and the successful, separately recorded native letter-key selection remain original evidence.

These checks approve the scoped CSS recipe inputs. They do not qualify physical devices, all menus, the future Team presentation, the public release or P01 as a whole. The final source still requires the six release gates, production reproduction/readiness, frozen artifacts and deployed play/offline verification. The official producer must append successors while preserving every earlier record and original byte; the retention comparison is recorded below after adoption.

The [retention comparison](recovery-layout/retention-review.json) passes for production revision 23. All 1,067 earlier asset records, 23 theme records, one collection, 293 slot contracts and 127 original blobs (4,007,816 bytes) remain identical. Exactly seven screen metadata successors and one theme successor are appended. All 286 other resolved assets/bindings, the URLs, token values, collection and actual recipe content are unchanged. The producer reproduces its output. The precommit readiness refusal is retained in [production evidence](recovery-layout/production-manifest.json); readiness deliberately requires the final committed ledger and must pass again with the other exact-source gates.
