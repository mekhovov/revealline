# P01 installed-original fixture readiness

The external-artwork host fixture now uses one local 30-second picture-readiness allowance for initial preparation and its existing later picture waits. All thirteen calls through its `page()` wrapper receive that allowance; explicit fixture options can still override it. This replaces the earlier single-subtest override. The shared ordinary-host five-second default, runtime deadlines, inventory limits and every state/artwork assertion remain unchanged.

On source `c5bf0bd0f4ae82aac370adde0bff5d12d9e63501`, [manual shard 3](https://github.com/mekhovov/revealline/actions/runs/34956641161/job/104341804856) failed in the published-original Continue/Restart fixture. Its initial picture wait still inherited five seconds and expired with the exact external pack selected, picture state pending and no host errors. The child took 6,860.524822 ms in total; that is not a measurement of the internal picture wait. Teardown cancelled pending work, so its eventual result in that CI attempt is unknown.

That manual run remains failed: 4,183/4,185 passed, with one causal assertion and its failed parent. Its freeze was skipped and the artifact count is zero. The same source passed 4,185/4,185 locally and in the ordinary PR workflow. Those passes do not override the manual failure.

A process-only preloader reproduced the failure by holding one already-computed original-image digest for six seconds on the second, restored test document. It checked the exact saved campaign, pending picture state, original byte length and SHA256 before the hold. The original digest bytes, image model, clocks, readiness predicates and runtime deadlines were unchanged. The first install/play/pause child passed; the restored child failed at the five-second initial wait with no host errors. After the local allowance correction, the identical controlled condition passed all existing Continue, exact-pin, paused restoration, Resume, confirmed Restart, tick-zero and error assertions. This demonstrates the fixture boundary under the controlled condition; it does not reproduce the precise hosted scheduler.

The [correction receipt](reports/external-original-readiness.json) retains both controlled outcomes, the preloader and event hashes, complete unfiltered file results, the thirteen-site audit and original hosted evidence pins. The full external-artwork file passed 20/20 without the preloader or a name filter, with no failures, skips or cancellations. The [broader fixture audit](reports/installed-original-fixture-audit.json) distinguishes real installed-original bootstrap work from empty, embedded, mocked or refused paths; passing cases are not labelled as observed failures.

That audit covers 53 caller files. It also identifies the retained `fpv-arcade-r5` selection reload, whose initial picture preparation inherited five seconds while later waits allowed thirty. This separate case passed both hosted runs. Its reload now uses its existing local picture allowance, preserving the first tiny-campaign host's default and every selection/save/checkpoint assertion; its [full-file retest passed 3/3](reports/selection-bookmark-readiness.json), with no failures or skips. It exercises real embedded PNGs and baseline picture hashing, with modeled decoding; it does not establish downloaded release-art or native-browser coverage. Larger multi-edition original fixtures already declare their own 180-second inventory allowance and remain unchanged.

New exact-source qualification and public acceptance remain required. No tag, frozen bytes or historical release is changed by this correction.
