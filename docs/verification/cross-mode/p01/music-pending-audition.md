# Music pending audition feedback

An imported track could remain pending in `audio.play()` while its busy status had a hidden ancestor and **Finish audition** stayed disabled. The status now sits immediately above Audition/Finish, outside the conditional playback controls. Playback is still invoked in the activating event turn; the panel refreshes controls before awaiting that promise. Existing audio intent, token retirement, original URL disposal, close/error handling, storage behavior and native file-dialog cancellation remain intact.

The two new held-play cases reproduced the hidden-ancestor failure before the correction. The final focused soundtrack-panel run passed **38/38**, with zero failures, cancellations, skips or todo, in **334.647958 ms**. The cases check a visible ancestor chain and enabled, focusable Finish while playback is held without media events; explicit Finish revokes the original URL and restores previous music intent once. Late resolve or reject cannot repaint status, persist activation, resume again or change storage. Targeted lint and formatting both exited 0. These are modeled DOM/media checks with real panel handlers and original MP3/store fixtures, not a native geometry or painted-busy pass.

[Source receipt](music-pending-audition/source-receipt.json) pins the exact runtime and test files. [Original evidence](music-pending-audition/source-originals.zip) contains the exact original before/focused/lint/format stdout and their original tool-result JSON strings: 8 members, 47,614 original bytes, 11,182 archive bytes, SHA-256 `fcc42f126160e9d3b31ba0d9e29983f73b7162d88b313f53a9b47bdd709efe4b`. The [member record](music-pending-audition/source-originals-record.json) hashes every member. These outputs were extracted unchanged from the original task records without rerunning commands; no ignored loose log is required. The initial targeted run has two expected failures and 36 explicit skips; only assertion diagnostic formatting changed before the final fixture.

The final [integration review](music-pending-audition/integration-review.json) found no concrete blocker in the current cancel/Still/replay/history/Playground/Music/transfer/recovery changes at its listed source pins. It is a read-only source review, excludes the separately reviewed hosted utility and does not establish native or deployed correctness. Shared release gates, native confirmation, public/offline evidence and P01 acceptance remain separately owned; this note claims no committed version or phase completion.
