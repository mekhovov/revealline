# Corrected local offline feedback

PASS: 38 bounded assertions on the generated ordinary v0.57.1 preview. This is local desktop Chrome153 with viewport emulation, sourceRevision:null, build ID `39f9ba278274ad326e6d85751711e3b50e682c66389796bb4499608999f0bb1a`. It is not frozen/public or physical-device acceptance.

| Case | Viewport | Status bottom | Count bottom | Stop bottom / height |
| --- | --- | ---: | ---: | ---: |
| Standard portrait | 390×844 | 599.72 | 599.72 | 708.72 / 44 |
| Standard landscape | 844×390 | 103.80 | 103.80 | 174.59 / 44 |
| Large/Plain/Reduced landscape | 844×390 | 111.88 | 98.83 | 184.38 / 47 |
| Large/Plain/Reduced portrait | 390×844 | 597.06 | 597.06 | 712.06 / 47 |

Before each action, ordinary scrolling positioned its visible button. No driver scrolling occurred after that click before the matching state and screenshot. Standard scrollTop stayed unchanged; Large/Plain cases had native browser scroll anchoring of22px and5px. The concise status/count and44px or47px Stop remained visible together. Root independently reviewed06,10 and15 screenshots.

A real service worker installed the generated608-file/55,383,689-byte core. The owned loopback server held original `authoring/asset-studio/operation.mjs` for82.83seconds without replacing bytes. Native Stop focused Check progress, rejoin reported the same607/608 installation, and releasing the original completed verification. The original3500-byte body was independently hashed in native CacheStorage and matched the artifact. Cached Verify then showed real504/608 progress and completed608/608 with no new server request.

The actual11 generated optional pack names are retained in the separate paragraph inside Offline verification details. Full note readability is captured before (`04`), during (`11`) and after (`20`) the operation. Raw result JSON retains the verbose message.

Measurements and screenshots are separate sequential samples, so moving counts can differ; their original bytes remain separate. Early `01` was an unsuccessful offscreen click and is excluded from note-readability claims; `03`/`04` are the successful visible interaction. Initial `00` is the unscrolled Game data tab, not the button-visible baseline.

Controls used native agent-browser actions through exact owned CDP, not injected app statuses or cache/save data. Browser/profile/session and server were all distinct from the public offline profile. Native browser and server PIDs/ports are closed; profile preserved. The first copied close-helper invocation failed before CDP because Node20 needs `--experimental-websocket`; the successful bounded retry recorded actual browser arguments and closed only the owned browser.

No further runtime or CSS correction was needed. Source/artifact authorities are copied byte-for-byte under their named authority directories, with provenance in `authority-copies.json`. `review.json` records all bounds and limitations; `assertions.json` lists38 checks. Profile data is excluded from the evidence index.
