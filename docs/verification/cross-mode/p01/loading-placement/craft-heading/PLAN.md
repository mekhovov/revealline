# Authorized craft-only heading-row retest

Root GO after59focusedtests PASS. Candidate0.57.2/r21 only; no ordinary/frozen/public qualification. Refresh/check all14 source pins before server; fresh own profile/session p01-craft-heading-row with exact Page.bringToFront, unique target and visible+focused guards before/after every action; stop on unexpected page. Prior Library closure is reused, not replayed.

Use real Missions→Flight setup→actual enabled character choices at390×844 and844×390 Standard, then Large/Plain/Reduced relevant cells. Passive actual preparation records label, shared heading, first enabled selector and Back geometry/hit-tests. No artificial delay/error or app-state fixture. Check cached return with real selection; normal completion clears status. One direct brief entry after ordinary Deploy/pause if feasible, preserving same original status node/Back. A short busy interval may have DOM/geometry evidence but no busy screenshot; preserve that limit.

Stop any required placement/focus defect before expanding the matrix. Error/stale/cancel failure branches remain focused-source tests, never invented native results. Finish exact source/process closure and selected original manifest; preserve preceding failed craft and successful Library originals.
