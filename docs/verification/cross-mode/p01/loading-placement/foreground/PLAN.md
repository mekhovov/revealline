# Final R3 foreground follow-up — prepared, not launched

Await root GO after p00 releases sole foreground ownership. Use this folder only; prior r2/r3 originals remain unchanged. The final source library-panel SHA is `9fc7ae8e3bd699cabb17681e4d1fd5203f08fd3beb53db609640635fb1b8b3b5`; serve-current-source refuses any changed pin. No build or release claim. This one short trial closes foreground uncertainty, rather than repeating the clean landscape matrix.

1. Run existing `serve-current-source.mjs --go` only after GO; it creates exclusive run and source/server receipt. Run `launch-owned-chrome.py --go`: a new lightweight profile, distinct session `p01-library-r3-foreground`,4MiB HTTP cache,60MiB profile cap,512MiB projected reserve plus16MiB evidence, eight-minute limit. Source CLI supplies all bodies unchanged. No full offline preparation.
2. Read launch receipt; use `control.py connect EXACT_WS`, then `control.py open EXACT_SERVER/game/`. These are the only bootstrap exemptions. Run Node22 `foreground.mjs bind initial`: records the exact unique game target and actual browser profile/endpoint, attaches its session, calls **Page.bringToFront**, verifies visible+hasFocus. Unexpected extra page targets fail; no first-target selection or fallback.
3. From now each wrapper action performs read-only foreground checks before and after. A hidden/blurred page, changed URL/target/PID/endpoint, or unexpected page causes an immediate stop and retained diagnostic. `foreground.mjs bring LABEL` is an explicit action for root-authorized initial/recovery use only, never automatic healing after a failed guard. No global OS keyboard or Chrome settings/zoom operations in this follow-up.
4. Attach passive observe.mjs; it records exact-target visibilitychange/focus/blur along with real status phases. Use actual Settings UI to select Large/Plain/Reduced,390×844, then Saves & recovery via fresh snapshot+focus+Enter. Check open Library before acting. Focus original Export and trigger one native game-data export. Retain actual download bytes and phase visibility; fast work may evade busy screenshot/Cancel input.
5. Focus original `library-operation-message`, record visible+hasFocus and actual region scroll metrics. Press one native PageDown, then take ordinary read-only samples. Wait with a bounded wall-clock observation if needed; do not await unbounded animation frames. Prove bottom reached (`scrollTop + clientHeight >= scrollHeight - 1`) and complete text visible in screenshot. If it cannot scroll while confirmed foreground, stop/report a required defect before more cases.
6. Native Tab or existing Arrow navigation exits details. Focus Close through normal keyboard navigation and confirm visible/hit-test clear; Escape must close Library, restore Settings trigger focus, restore original save-status parent and hide rail. Open Saves & recovery natively again and verify no stale rail. One ordinary close/reopen only.
7. Record guards and screenshots at each relevant step; reuse earlier r3 clean landscape/focus/resize/wheel proof by exact references. No new zoom/Cancel consumption claim; these were not part of this deliberately small foreground closure. Close exact owned browser, attached daemon and source server; verify PIDs/ports; rehash source and explicit evidence. Preserve any failed guard, partial result or late unexpected tab.

Preparation validation: Python AST and Node syntax only; no browser/server/network requests, test suite or source mutation. Guard behavior is not claimed runtime-tested until actual GO.

## Root-authorized craft extension at GO

Current source is candidate0.57.2/r20; source pins include package/lock/build-config and original ledger/compiled runtime/studio/manifest/theme. No frozen/public claim. After Library closure, use title Missions and actual Flight setup; inspect the same craft-preparation-status in its sticky slot. Select actual enabled character/body choices in Standard390×844 and844×390, then relevant Large/Plain/Reduced. Record naturally observable loading/error/cached phases with passive status observations; no fake delays or forced errors. Especially hit-test first enabled setup selector after native Flight setup scrollIntoView, plus Back. One normal direct brief entry if feasible. Stop on a required visibility/focus defect and close with originals. No generated build or source changes.
