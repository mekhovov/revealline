# Bounded transfer/recovery family — availability and reuse

Prepared only. No browser/profile/database was copied or opened. Existing native-extension/PLAN.md sections2–3 remain the journey authority. A fresh family profile protects original public/core proof. No raw localStorage evidence is converted into an importable fixture.

**Material prerequisite:** source CLI is not a release: game/app.mjs sets profileTransfer only after real build-info.json, and the direct recovery page requires build metadata/catalogues. Root confirms the next transfer family will use actual public v0.57.0→v0.57.1 for existing recovery behavior. Corrected Library placement will be repeated on a genuine candidate build in the next release cycle. No overlay or fabricated release markers are needed. Await family GO before launch.

| Bounded case | Actual actions and required evidence | Reuse/limits |
| --- | --- | --- |
| A25 old→current transfer | In a new family, open retained Archive14 v0.57.0 on mekhovov.github.io, verify identity, make one real cut/pause/save. Record exact source channel/pins/raw values read-only. Leave old page to release writer. Open actual built target on the same origin; Library Saves→profile-transfer summary→transfer-source→Review→explicit Copy. Measure rail transfer-status + transfer-cancel, summary/source identity and focus. Source stays byte-exact; target Continue restores paused, no implicit Resume. | One pending Cancel/re-review only if native duration permits; no artificial delay. Current authentic backups do not contain an attempt, so real old play remains necessary. Old installed pack/art not required for this representative same-host owner. |
| A22–24 / A25 native import+Undo | Export copied target natively and pin completed file. Change one preference via UI. Upload the unchanged export through save-file. Record read/verify/commit, original save-status and generic Cancel ownership. Undo must restore pre-import target raw values/preference and leave old channel unchanged. | Existing799-byte v0.57.0 export is real but empty/session:null; useful simple import provenance, not transfer-flight proof. Prefer freshly exported target for meaningful Undo. Do not compare only semantic preferences while claiming full raw bytes. |
| A25 refusal/retry | Upload shipped Dawn manifest.json unchanged: valid JSON but wrong backup format. Require visible error and unchanged profile. Retry with fresh native target export. | Existing shipped manifest2840bytes; no hand-built malformed state. Exhaustive schema/CAS/quota/stale races remain focused-test evidence. |
| A26 recovery dialog | Settings Game data→Stored profile recovery→Find→exact old/current channel→Review→Export snapshot/native Download if supported. Record loading/catalog/read status, Back/Cancel geometry, close/reopen and read-only raw source/target comparison. | Actual version/catalogue support determines result. Raw diagnostics are not original-image verification; no blanket IndexedDB no-write claim from localStorage equality. |
| A27 direct recovery | Built target/game/profile-recovery.html→Find/Review→review shared originals. If actual originals exist, choose and verify/export one; Back returns same edition. | Without restored/earned originals, record empty/unsupported result only. Substantive original identity/download proof waits for earned-media family's completed native .rlmedia/.rlstory + game backup. |
| Deferred earned recovery | Native restore .rlmedia then .rlstory, followed by corresponding earned game backup; verify actual recovered Collection/story pin. | Offline agent confirms earned family not launched and no finalized native saved-attempt/earned exports exist yet. This is a genuine prerequisite, not a pass or a synthetic fixture opportunity. |

Use390×844 and844×390 for the new shared status/cancel placements, plus Large/Plain/Reduced on the dense representative review; use clean landscape rail proof to avoid repeating all formats. Keep native duration/cancel limits explicit. Existing exact tests cover profile-transfer.test, profile-transfer-suspended.test, profile-recovery-settings-host.test, profile-recovery-dialog.test, profile-recovery-view.test, profile-recovery-snapshot.test, profile-recovery-catalogs.test, backup-storage.test and attempt-export-panel.test. They establish lifecycle/validation boundaries, not native visibility passes. No tests run during preparation.

Authentic existing old offline profile contains real saved-flight evidence, but is original proof and will not be touched. Its read-only JSON receipts are not native import files. A fresh old-version cut avoids copying a browser database or mutating that profile. Expected profiles/downloads are small; retain512MiB reserve and close family before earned/native foreground work starts elsewhere.
