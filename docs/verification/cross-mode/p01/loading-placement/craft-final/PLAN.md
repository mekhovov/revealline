# Craft focus-clearance retest — prepared only

Await root focused host/CF1 completion and GO; all other browsers closed before sole ownership. Fresh own source server/profile/session p01-craft-focus-clearance. Rehash every pin at GO, especially pending controller-settings change. Candidate0.57.2/r21 is not an ordinary/frozen/public build. No Library replay: reuse final guarded portrait and earlier clean landscape proof.

Repeat only prior four cells:390×844 and844×390 Standard, then Large/Plain/Reduced; actual Flight setup and enabled body selection. Record label/Back/first-selector full interior hit tests against live heading AND footer. Return once to a previously selected body, no unverified HTTP-cache claim. If no required defect, use one ordinary Deploy/pause/direct brief entry and Back to inspect original status node placement. No synthetic delay/error or app-state injection. Manual wheel must remain manual if bounded corroboration is needed; no constant scroll watcher or forced focus in test.

Observer emits immediate MutationObserver geometry as status-mutation-immediate, then a correlated post-three-frame-turn sample after3requestAnimationFrame callbacks and a task boundary. These are separate categories. The latter lets the product ResizeObserver→requestAnimationFrame adjustment run; it is not a guarantee of physical screen/compositor presentation. Record the then-current status: if real work already completed, do not relabel a ready frame busy. A1.5s watchdog records inability to observe frames instead of leaving an unbounded Promise; no app wait/clock/data is changed. Every action remains exact-target visible+focused guarded before/after. Stop on hidden/extra target or persistent required obstruction after the product layout opportunity.

Native screenshots remain independently timed; do not manufacture a visible busy screenshot by freezing work. Source tests cover bounded geometry/error/stale semantics separately. Close exact owned processes, rehash sources and preserve every original before final report. Do not launch until GO.
