# Playground operation feedback and bounded authoring closure

P01 remains open. These are working DEV source and native-browser checks, not a frozen or public release claim.

Native Generate at 390 × 844 originally left its completed status entirely above the viewport (top −1211.703125, bottom −1002.703125), while Generate remained visible. The first correction passed that placement check, then native replay verification exposed its separate result below the 844 × 390 viewport: the file control was at 324.8125–368.8125, but the successful result was at 527.59375–554.59375. Both failures and the first run's resource stop remain in the archive; no busy-state pass is inferred from a terminal sample.

The original editor, replay and child-preview status nodes now share one sticky rail with a single 24dvh reading region. The newest owner is ordered first; another pending owner's message and Cancel remain available. Only an actual owner switch resets local message scroll, preserving the offset of a focused reader. Focus/reflow uses measured minimum clearance without assigning focus or observing ordinary scrolling. Page hide disposes clearance; persisted page show reconnects one owner. The existing presenters, import/replay epochs, verification, child readiness markers, history, Undo and stored bytes remain unchanged. Configuration-prepared text cannot hide the actual child-readiness message.

The final three-file focused run passed **23/23**, zero failures, cancellations, skips or todo, in **620.416041 ms**. It covers held decoding and real verifier yields, cancellation/stale completion, concurrent Cancel availability, original node identities, exact draft/result/map preservation, Generate/Undo, current-message ordering, focused reading, child-load versus ready/error, and persisted page lifecycle. ESLint, formatting and diff checks passed. Geometry and lifecycle fixtures are modeled evidence, not native observations.

| Inventory                     | Native disposition                                                                                                                                                                                                                                                                                                              |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A34 Studio preview/prompt     | First run: actual recipe Audition/Stop and generated-prompt clipboard success. Retained separately; no repeated PNG editing matrix.                                                                                                                                                                                             |
| A37 Music Studio              | Final metrics run: real 4,170-byte silent MP3 import; painted audition message and enabled 44px Finish at 390 × 844; native Finish stops the actual audio element. Tab reaches the next visible playlist; Escape restores the Music opener.                                                                                     |
| A43 Playground replay         | Final source run: genuine 1,305-tick recording verifies; terminal feedback is visible in portrait and short landscape. Pasted-JSON and held cancellation paths remain focused source checks.                                                                                                                                    |
| A48 Playground editor/preview | Generate terminal feedback passes at 390 × 844 and 844 × 390; native Undo restores exact imported level bytes. Header Play changes child revision 1→2, and the actual child reports ready. Native Back reaches campaign and history Back reloads Playground; persisted BFCache reconnection remains modeled, not a native pass. |
| A51 Enemy practice            | Final metrics run at 390 × 844: actual child ready, native Return reopens the same workshop draft, and Escape restores Open workshop.                                                                                                                                                                                           |
| A54 Atlas clipboard           | Final metrics run at 844 × 390: opened brief, native Copy success, visible Copy/result with retained focus, and native Back.                                                                                                                                                                                                    |

The third run's Music/Enemy/Atlas screenshots were **500 × 701** despite their filenames. Their functional observations and geometry reads remain separate; they do not establish the requested capture sizes. The fourth run corrects this evidence gap with Runtime viewport and CDP layout metrics before/after every input, same-target capture, and exact PNG-header checks. It contains three 390 × 844 Music/Enemy frames and one 844 × 390 Atlas frame. Earlier wrong-selector/closed-details guard refusals, two transient cache-file-walk refusals, and the short MP3 ending before an earlier Finish attempt are retained. BODY focus immediately after Finish disables itself is recorded; subsequent Tab and Close remain usable. No physical audibility/device, synthetic delay, application-state injection or public acceptance is claimed.

All owned browser/server/observer processes closed and source/fixture pins matched after each final run. The [evidence record](playground-operation-feedback/evidence-record.json) binds [all four native attempts, controlling helpers/source copies and focused logs](playground-operation-feedback/native-authoring-evidence.zip): **456 originals / 4,271,186 bytes**, archived as **2,025,384 bytes**, SHA-256 `db237246a4996308b3c91fc431fdd76f5eef30a1756ab6cc89dc1a43c5735915`. Every ZIP member CRC/hash and source reread passed. Chrome profiles and unrelated sessions are excluded.
