# P03-F: keep Tab inside the active modal

This internal P03 correction wraps only the first/last native Tab boundary of the current visible modal. Interior Tab, native input/file behavior and Escape keep their existing owners. Authored reading regions and tab order are included; hidden, disabled, inert and negative-tabindex targets are excluded. Wrapping does not play media, commit a draft or resume a flight.

The six complete focused suites pass **179/179 on Node 20.19.5 and Node 22.22.2**, with zero failures, cancellations or skips. They include actual story presentation controls, a real Solo nested Collection/picture host, both boundary directions, native input behavior, eligibility and E's Team focus regression. ESLint, Prettier and Git whitespace checks passed. [Source verification](source-verification.json) retains commands/scopes and the raw evidence index; its native-pending status predates the successor observations below.

Root's [13 native desktop keyboard observations](native-events.json) verify forward and reverse wrapping in Collection, picture and story dialogs. Story Close Shift+Tab reaches Cinematic volume; volume Tab reaches Close, without BODY focus. Interior Tab reaches Play and volume without activation or gain changes (0.4). Explicit Enter plays and focuses Pause; the natural eight-second end focuses Replay. Escape returns to the earned-story action, then the earned Collection card, then Title Collection. The recorded reward remains 8,160 points / Gold / 12.18. [Native source binding](native-verification.json) records exact held runtime/test pins and the matching HTTP controller module.

The preview overlays F on `9e8c9cf`, which integrates P03-D and E. The predecessor's forward/reverse BODY-focus failure remains unchanged in [story-tab-boundary.json](story-tab-boundary.json). The [retention index](retention-index.json) also preserves the initial F negative baseline, the missing mock Window method in its first test run, and root's D+E sparse-file/fixture refusals followed by its 135/135 Node 20 result. These are separate runs and their counts must not be summed as unique tests.

This is scoped source and desktop keyboard evidence, not P03 phase or public-release acceptance. Physical controllers, touch, phones, Safari, zoom and comprehensive assistive-technology checks remain open. Different-mission unfinished-flight replacement, broader mode entry and final combined-source/public qualification are separate work. The public baseline remains v0.57.1.
