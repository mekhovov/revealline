# Enemy Workshop Return — readiness correction R2

This is a five-path source candidate against `ccd04f9cccc321513483ba63f30e27d800b76a40` (the isolated Team v0.61.2 commit). It is not committed, versioned, built, published, or accepted as a release. The release coordinator owns composition with current main, subsequent version assignment, complete source/production/build gates, native verification and publication.

The original `p03-enemy-workshop-return-628e95da` packet remains unchanged. Its static same-edition Return anchor is preserved byte-for-byte. R2 keeps successful delayed readiness from opening a modal over a newer Return choice. A task-local initial-opening lease is acquired before the first await only for untouched foreground body/root focus. Existing meaningful focus, subsequent focus/input, blur, hidden or pagehide retires automatic opening. Normal untouched startup still opens the panel; later explicit Open retains its ordinary behavior. Navigation is never engaged merely by readiness.

A terminal departure prevents late fetch/body decoding from constructing host owners, scheduling frames or reporting stale errors. A cached-page departure waits for pageshow before completing readiness/failure and cannot revive auto-open. Intent listeners retire immediately; lifecycle listeners are removed on settlement or terminal departure. The existing modal, draft, practice bridge, Apply, storage, failure copy and explicit reload/Return semantics remain intact. The live guide now describes the already-allowlisted route instead of an upcoming v0.27 task, and the maintainer paragraph and maintenance prompt cover the implemented contract.

`candidate.patch` and `candidate-pins.json` are the final 46-test source. `qualified-node20` and `qualified-node22` are the authoritative final whole-file runs: 46/46 pass in each, no failures/skips/cancellations, one file worker, a 120-second ceiling and 1 MiB output ceiling. Each hashes all 110 local inputs before and after; both use the same 1,301,599 bytes. `quality.json` records scoped lint/format, the exact five-path change set and retained shared-skill bytes. `lifecycle-peer-review.json` records the independent read-only lifecycle review.

Earlier receipts remain distinct: the original runtime fails eight targeted cases in the 15-case `red-node20` host run. `green-node20`, `final-node20` and `final-node22` cover the initial 41-case correction; `opening-only.patch` preserves that candidate. `lifecycle-node20` and `lifecycle-node22` cover the subsequent 45-case lifecycle correction, preserved by `lifecycle-45.patch`. The final 46-case version adds the independent pending response.json terminal-departure boundary. The first formatter invocation from the repository root skipped ignored cache files; the later invocation inside source with an explicit empty ignore file formatted the actual candidate before final qualification.

These are modeled host/input/lifecycle checks, not native browser, physical controller, touch, assistive-technology, offline or public qualification. Earlier native evidence belongs to the original static-link candidate, not this new asynchronous behavior. Pending native checks should hold successful themes while Return is focused, restore cached pages, and confirm ordinary initial and explicit panel opening. Exact committed-source gates and public same-edition return remain mandatory. No root or Team worktree, Git index, remote branch, release, production asset, gameplay rule or player record was changed by this task.

The independent `parent-production-compatibility.json` verifies every existing candidate preimage against Team composition `bb5e6e7787bf81232368b96845d716a808c21309`, with the new test still absent. None of the five candidate paths participates in the five maintained producer fingerprint groups; FPV compiled revision 28 and production ledger remain unchanged. No art/recipe revision bump is required by this scoped change; complete production and release qualification still applies.
