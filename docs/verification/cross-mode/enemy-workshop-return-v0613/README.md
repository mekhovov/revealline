# Enemy Workshop Return and safe startup — v0.61.3 evidence

This feature keeps **Return to game** available before loading finishes and after failure. Successful delayed readiness preserves a newer page action or focus. An untouched foreground visit still opens the editor automatically; **Open workshop** remains explicit after the automatic opening retires. Terminal departure prevents late initialization and feedback. Cached-page departure waits for restoration without reopening the editor automatically.

The five reviewed source paths are adopted on Team integration `bb5e6e7787bf81232368b96845d716a808c21309`. Package, lockfile and build configuration are synchronized to **0.61.3**. The change preserves the existing draft, practice bridge, controller ownership, theme artwork, simulation and player records. Startup/failure Return remains a native keyboard/pointer link; the controller owner becomes available after successful startup. This slice does not close whole-phase controller coverage.

The [versioned source qualification](adoption/source-qualification/qualification.json) records **46/46 passing checks on Node 20 and Node 22**, using four complete affected files with no skips or cancellations. All **114 ordinary source files / 1,343,348 bytes** remained unchanged through testing and scoped lint/format/reverse-patch checks. The five feature bodies match the independently reviewed R2 candidate exactly. These are modeled source checks before the commit; full exact committed-source qualification, build, artifact and browser/public gates remain required.

The [production boundary](adoption/production-boundary.json) verifies that the five maintained producer fingerprints, FPV revision 28, compiled asset tree and production ledger remain unchanged. This means no artwork/recipe revision update is needed for these paths; it does not replace production reproduction/readiness or approve new visual behavior.

## Preserved evidence

[The adoption manifest](manifest.json) maps every literal copied source to its repository destination. The [intake manifest](intake/manifest.json) and [intake explanation](intake/README.md) preserve the original snapshot and relocation boundaries. Their historical absolute paths and pending statements are intentionally unchanged.

- `originals/p03-enemy-workshop-return-r2-ccd04f9c` contains the final candidate, independent lifecycle review, full input pins and every retained failed/intermediate/final run. The original runtime failed eight targeted host cases. The 41-case opening-only and 45-case lifecycle stages remain separate from the final 46-case source.
- `originals/p03-enemy-workshop-return-628e95da` preserves the preceding static Return-link candidate and its guidance/tests.
- `originals/p03-enemy-workshop-return-browser` preserves its earlier native observations. Those use exact `628e95da` plus the static HTML link; they do not qualify R2 loading/focus/lifecycle behavior or v0.61.3.

## Remaining release checks

Run all six source gates on the exact committed source, production reproduction/readiness, the ordinary build and frozen artifact checks. In a real browser, hold theme readiness with Return focused, verify normal and explicit opening, exercise failure and cached-page/terminal departure, then check same-edition Return and the practice bridge. Keep keyboard, touch, modeled controller, physical controller and assistive-technology evidence distinct. Verify deployed version/source identity, bytes and affected public journeys before accepting this feature; previous releases remain immutable.
