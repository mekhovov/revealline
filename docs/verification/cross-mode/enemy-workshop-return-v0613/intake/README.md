# Enemy Workshop v0.61.3 — immutable evidence intake

This is an evidence snapshot for scoped local adoption on Team source `bb5e6e7787bf81232368b96845d716a808c21309` (tree `d8b19e99b762b1c4ac974118abc59935f6ca8e3a`). **v0.61.3 is proposed; this intake is not a source commit, complete qualification, publication or acceptance.** The coordinator owns source composition, version changes, exact-source gates and release/public verification. No source, index, production asset or remote state was changed by this intake.

`manifest.json` maps each literal source file to its preserved destination, byte length, SHA-256 and permission mode. Files were inventoried before copying, read back afterward, and the complete source inventories/hashes were checked again. No symlink or special file is included. Every file in the three named finite source packets is retained; no in-progress v0.61.3 adoption directory was traversed or copied.

## Evidence boundaries

- `originals/p03-enemy-workshop-return-r2-ccd04f9c/` contains the final five-path candidate against `ccd04f9cccc321513483ba63f30e27d800b76a40`, its source/fixture snapshots, failed/intermediate attempts, final tests and independent review. The final patch is **35,267 bytes**, SHA-256 `d078acbb8f0cc98d30c293f9305ca45fefd2bfb314f561caf0e9fda63b74510e`. `parent-production-compatibility.json` separately verifies current Team `bb5e6e77` preimages and lack of maintained producer-fingerprint overlap; it does not qualify a newly versioned source or new UI behavior.
- R2's authoritative `qualified-node20` and `qualified-node22` receipts each report **46/46** passing tests across four complete files, with 110 unchanged local inputs. These are scoped modeled host/input/lifecycle checks. They do not replace the six complete committed-source gates, production/build/artifact checks, current native behavior or public acceptance. Earlier 31-, 41- and 45-case cohorts overlap and must not be summed; red failures, the initial ignored-cache formatting attempt and intermediate patches remain unchanged.
- `originals/p03-enemy-workshop-return-628e95da/` is the earlier static same-edition Return-link candidate. Its original 31-case source qualification is retained at that source boundary.
- `originals/p03-enemy-workshop-return-browser/` is the original native packet, present and included within budget. It observes exact `628e95da` plus one HTML override, not R2's asynchronous startup/lifecycle correction. Native delayed/failure exits, modal draft return and ordinary same-origin links retain their recorded limits. The receipt's immutable journal prefix and later live journal are separate originals; their names and bytes are preserved. CUA locator actions and emulated viewports do not establish full keyboard-only entry, physical controls, touch-only use, assistive technology, offline, public-release or new lifecycle acceptance. Inline visual observations are not retained screenshot files.

Absolute paths, localhost addresses, original version labels and helper-script references inside copied files remain literal provenance. Copying them into a later repository does not make those old paths executable or relabel their source. References outside these named packets have not been recursively imported. Use the manifest's source-to-destination mapping to find retained originals.

## Adoption

The coordinator may copy this intake into `docs/verification/cross-mode/enemy-workshop-return-v0613` after verifying the manifest. Add a separate current source/version/adoption receipt; do not rewrite these originals. Related source hunks must preserve the exact existing Return anchor, ordinary untouched auto-open, explicit Open, modal/practice input ownership and player data. Verify late success/failure/body decode after departure, pending cached restoration and focus-neutral readiness against the actual adopted source, then complete its independent release/public gates. No earlier source or native pass qualifies the final v0.61.3 commit automatically.
