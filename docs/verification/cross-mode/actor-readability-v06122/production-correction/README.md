# v0.61.22 production correction — original evidence

Source `307f46382a580b877a99123b3a7df7d9bc53dd40` failed both the [PR preflight](https://github.com/mekhovov/revealline/actions/runs/35357160057) and [manual qualification](https://github.com/mekhovov/revealline/actions/runs/35357331143) because the production ledger had not recorded the changed motion input. The original logs, run/job metadata and dispatch receipts remain unchanged in the [ZIP](originals.zip), with every member pinned by the [index](originals-index.json).

The supported producer appends fpv29 as the unreviewed source stage, then fpv30 with the exact scoped actor declaration. Previous revisions and all 127 original payloads remain intact. The two Team consumers require fpv30 without changing the accepted pack, level or artwork identities. The [review declaration](../../../../actor-size-recipe-review.md) states the actual actor and native limits; the [earlier implementation record](../README.md) retains its original native images.

Local reproduction passes on Node 20.19.5 and 22.22.2. Working-ledger readiness declares all 194 required slots reviewed; it is not visual acceptance. Each initial four-file cohort has 57 passing cases and one failure because an assertion still named the previous motion fingerprint. Only that exact-current-authority assertion changed; the selected rerun passes one case on each runtime (Node 20 reports ten deliberately unmatched skips). Thus 58 distinct cases pass across retained cohorts, **not a clean 58-case command or a complete source suite**. The earlier stale-Team-binding failure, source-stage readiness refusal and final intentional committed-ledger refusal are preserved too.

The compact bundle includes the original eight-path candidate pins, patch, check receipts and independent review. Its native entries are existing review/manifest references only; no new native journey or image is claimed. The guide's later evidence-link paragraph and these three evidence files are packaging additions, not part of those earlier candidate pins.

**Required next:** commit the exact correction, pass strict committed-ledger readiness and qualify that new source through all hosted gates. Full CI, native public verification, release and whole P08/P18 acceptance remain open. Narrow Large HUD overflow remains a separate correction.
