# Couch shell visible-focus checkpoint

Internal P03 checkpoint on combined P05-B/P08 static `063ff5d650b6e071d2b59e89fa6e9fb5058b3f08`. It is not a phase, version, release or public acceptance. The [local contract](../../../couch-visible-focus.md) preserves screen/Start/picture ownership and simulation while revealing the current shell action after final synchronous content publication.

[Final held02](automated/source-held-02.json) binds shell `a6f66819d255cf6fd0bb3bc3e22a52991ef1435095fc1d7cbb72af9153dce893` and complete host test `d02d788b864a1b430228415c04b469ff93f0d878f6233835c27b496ec8ebe768`. Root reviewed both the original guard and the final content-order delta before their executions. The prior held01 bodies and diff remain retained.

| Complete shell/navigation pair | Node 22.22.2     | Node 20.19.5     |
| ------------------------------ | ---------------- | ---------------- |
| Final held02                   | 44/44 · 36.129 s | 44/44 · 51.043 s |
| Earlier held01                 | 44/44 · 50.361 s | 44/44 · 88.343 s |

These overlapping counts are not additive. The [final receipt](order-successor/verification.json) and [predecessor receipt](automated/verification.json) record exact commands, raw log hashes and passing lint/format/diff checks. Final before/after pins match all 524 admitted physical input bodies (11,200,732 bytes); that finite list is not claimed as the complete runtime dependency graph. Documentation-only final appends follow execution. Seven new host cases plus the strengthened actual finished-draw case assert focus-callback retirement, same-action reveal, final result rows/copy before reveal and unchanged checkpoints. Scroll requests are not rendered-geometry proof.

The [original native review](native-predecessor/review.json) retains five samples: at 844×390 board return focused View both boards at y666.55, height 49.30, scrollY0, outside the viewport. Its event 05 was actual **Shift+Tab**, despite the preserved generic Tab label; that diagnostic workaround was not acceptance. The [held01 review](native-root/review.json) retains seven samples/six screenshots: board returns passed at both viewport shapes, but first Results event 03 still put Next below the viewport. Its intended pass label is explicitly corrected. Resize-only event 01 also left an already-focused lobby Start outside the new viewport; this remains a separate P05 follow-up.

The [final held02 native review](native-order-root/review-held-02.json) records ten samples and ten screenshots. Root observed and visually read them all. Keyboard First Signal produced 52.2%/8160 points/three lives and 1:0; Next was visible at actual 938×433. Explicit Next led to a second actual win 50%/7820/three lives and match 2:0: Play another match was at y323.97, height 50, inside actual 844×390. Board return preserved results and placed View both boards at y324.07, height 50, in landscape and y779.44, height 48.78, inside actual 390×844 portrait. This supports the scoped keyboard/focus-order result, not equivalent simulation timing across separate runs.

**Full responsive visual acceptance is incomplete.** All ten overridden-viewport screenshots show black bands and repeated edge strips. Both screenshot entrypoints reproduced the issue. Requested 844×390 yielded actual 938×433 at reported DPR 0.9; requested 760×351 yielded actual 844×390, and requested 351×760 yielded actual 390×844. The record reports one Meta+0 diagnostic with no observed effect; no browser-zoom certification follows. Root reset the override and inspected coherent [normal Results](native-capture-diagnostic/normal-results.jpg) and [normal boards](native-capture-diagnostic/normal-boards.jpg), whose screenshot dimensions are 1923×1356 with reported normal DPR 1.8. Cause remains unresolved; no speculative source fix is included.

Historical `.png` names contain JPEG/JFIF payloads. Names and original bytes are preserved; [image metadata](automated/native-image-metadata.json) and the [final reconciliation](automated/native-final-reconciliation.json) record encoded type/dimensions without claiming image inspection by the packaging agent. Full-arena labels in event intents are not acceptance of distorted frames.

[Final preview binding](preview-order/binding.json), [initial HTTP check](preview-order/http-source-check.json) and [post-native pins](automated/native-held02-post-http.json) preserve exact Git063 routes plus only the shell overlay. The old 53085 origin continued serving immutable held01 bytes after the physical successor; 54539 served held02. Final post-diagnostic HTTP bodies also match. No app-state, clock or response-timing injection was used. The helper’s 64 MiB Git-body LRU is not a total RSS bound.

[evidence.json](evidence.json) maps every exact or losslessly compressed body. Gzip retains original logs, diffs and native text without whitespace normalization. Team, pager/operation-focus and later phase compositions remain separate. Physical input, audible behavior, browser zoom, complete responsive visuals, public/offline qualification and P03/P05/P08 completion are not claimed.
