# Recovery reading and navigation — v0.60.4 source candidate

**Not a released phase.** This correction makes Earlier profiles readable with the game's shared settings and preserves keyboard/controller navigation ownership through startup and asynchronous checks. P03/P05 remain implementing. Recovery is an explicit troubleshooting URL at `game/profile-recovery.html`; there is no newly advertised Library entry, automatic discovery, write/restore operation or gameplay change.

The candidate composes four reviewed patches plus the separately reviewed explicit Back history-return correction from exact source `a13ab970222498d7c5fa7f62f9fc04fe436979d5` onto main `63796aab5bb692578fef77f43fb9fa61050a372b`. Main's intervening changes affect publication records only. Final source integration also includes acceptance PR #89 at `65a6efcb3bfabf8458702cca452c8120fa756471`; its changes are delivery records only. The [composition record](integration/composition.json) lists all 11 unique source paths; version metadata is separately synchronized to 0.60.4. Motion and the Team full-picture candidate are excluded.

## Verification retained

- The preceding six complete affected files pass **89/89 on Node 20.19.5 and 89/89 on Node 22.22.2**, sequentially at 1024 MiB. Each pins 95 unchanged inputs / 1,152,610 bytes. See [Node20](cohort/20.19.5/receipt.json), [Node22](cohort/22.22.2/receipt.json) and [executed-body equality](composition/action-focus/final-input-review.json). Those original inputs remain unchanged. The subsequent history-return host and test supersede two bodies; see its final cohort below. Version/documentation additions are outside both precursor fixtures.
- Original failing discriminators remain under `regressions/`: missing early display adoption; unavailable startup controller navigation; stale deferred Back; and disabled Find/Review focus loss. [The final view discriminator](regressions/action-focus/receipt.json) reports 26 pass / 7 fail with the preceding runtime. It models the browser-observed disabled-control blur using the actual view. Earlier cohort totals are historical scope, not additive unique-test counts.
- [Preceding native observations](native/preceding/browser/root-observations.json) cover direct ready Back, real build-info 503, the actual ten-second timeout of a thirty-second server delay, Reload → Find, shared text preferences and Plain/Large responsive layout. Original Find/Review focus failures remain alongside corrected observations.
- [Corrected native observations](native/corrected/browser/root-observations.json) cover actual Find → Review, Review retaining focus, raw export → download link, next Tab → Back, Escape → title/Start, and keyboard same-origin text changes preserving the exact prepared Blob URL, channel and feedback. The genuine profile had no completed levels, pictures, scores or saved flight; this is not historical-original recovery or downloaded-byte acceptance.
- [Preceding byte review](native/preceding/request-byte-review.json) confirms 1,767 successful responses across 322 distinct bodies, with one deliberate 503 and two delayed events separately retained. [Corrected byte review](native/corrected/request-byte-review.json) confirms 705/705 successful responses across 319 bodies. Every successful body matches exact a13 source or a reviewed runtime override. These are local observed-request audits, not complete public inventories.

## Explicit Back history-return successor

Independent review found that completed explicit Back destroyed the page's router before the browser could cache the departed document. The prior stale-Back case covered cleanup superseded before navigation and missed this boundary. The new transient-departure correction keeps one cleared navigation owner until terminal pagehide and restarts a fresh generation after persisted return. [The predecessor whole-file discriminator](history-return/runs/before-fix-20.19.5/receipt.json) reports 14 pass / 2 fail; [Node20](history-return/runs/candidate-20.19.5/receipt.json) and [Node22](history-return/runs/candidate-22.22.2/receipt.json) now pass **92/92** across the same six complete files, with 95 unchanged inputs / 1,155,888 bytes per runtime. [Independent review](history-return/independent-review/review.json) confirms the ownership correction and input pins. Modeled persisted lifecycle events remain separate from browser cache admission.

The [actual history journey](native/history-return/browser/ready-back-history.json) passes ready Back → game navigation → browser Back → enabled Find → genuine profile/Review → Escape → fully ready title/Start. The first game page was still loading when browser Back occurred. [635/635 successful requests across 320 bodies](native/history-return/request-byte-review.json) match exact source or reviewed overrides. The server and owned tab are closed. This verifies ordinary history usability, without instrumented persisted-event or BFCache-admission evidence.

## Evidence limits and next gates

The native reads settled too quickly to inspect Cancel timing; current-owner cancellation and stale close ownership retain modeled coverage. There is no actual downloaded-byte comparison, eligible historical-original verification, saved-flight restore, physical controller/touch, offline/BFCache or 200% zoom acceptance. Native preparation used the actual frozen v0.60.3 build-info original plus explicitly pinned candidate overrides; it was not a deployed v0.60.4 release. The one retained PNG has the recorded compositor/logical-viewport limitation. The preceding two owned servers and the additional history server stopped, temporary tabs closed and viewport reset.

Source-cache absolute paths in original records are historical. `retained-files.json` maps **105 exact originals / 1,804,046 bytes** to their source paths and SHA-256 values. No thin-fixture bodies, giant distributions, source archives or duplicate browser body caches are included. Runtime overlays can be reconciled from the named exact source and retained patches; server records are observation artifacts, not portable deployment instructions. The initial sparse-checkout path and unsupported option errors are setup-only records; corrected exact path admission and all four patch checks then passed.

Final intended-source six gates, both full hosted test families, production reproduction/readiness, ordinary build and immutable release/Pages verification remain mandatory. The preceding v0.60.3 delivery is accepted within its separate supporting-tool/public-regression scope; it does not qualify this successor. No precursor or local browser result qualifies a later commit by itself.
