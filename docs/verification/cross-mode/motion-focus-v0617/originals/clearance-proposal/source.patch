--- a/authoring/motion-lab/styles.css
+++ b/authoring/motion-lab/styles.css
@@ -420,6 +420,10 @@
 .controls {
   border-left: 1px solid var(--line);
   padding-left: 25px;
+}
+/* The grouped resize target must leave room for the native control focus ring. */
+.controls :is(.range-field, .field, .segmented label) {
+  scroll-margin-block: 8px;
 }
 .control-section + .control-section {
   margin-top: 20px;
