# Media controls and Preview focus evidence

This v0.61.1 candidate composes two reviewed changes onto `fc2c7af6b22c703cc3bb2c081579878b7510f667`: native file-button contrast and narrow-screen Still Media sizing; and operation-owned focus return for chosen, saved and authored picture previews. No collision, scoring, original media, saved assignment or video behavior is changed. The shared UI recipe requires a new append-compatible production review; that review is recorded separately. This packet is candidate evidence, not release or full P03/P05 acceptance.

The three complete media-panel/bundle/story test files pass **49 distinct cases on each of Node 20.19.5 and Node 22.22.2**. This includes native disabled-button blur to BODY, success/failure, supersession, immediate abort cleanup, lifecycle changes and reentrant callbacks. Seven new regressions fail against the original panel module. The preceding missing-fixture and missing-asset failures remain in `candidate-originals.zip`; they are not passing tests. Counts across precursor and composition runs overlap and must not be added.

Root's combined browser run served exact fc2 Git bodies with the three pinned runtime/CSS overrides. A genuine native chooser selected an owned 243,034-byte PNG. Its 1280 × 633 preview retained the initiating action at 390 × 844; the dialog's client and scroll widths were both 342px. At 844 × 390, authored preview returned focus to its visible, center-hit-tested 44px action and the dialog widths were both 796px. Clearing the description with keyboard selection/deletion produced the required-description error while retaining Preview focus. Tab advanced normally to authored Preview; Close returned to the host opener. Connections were closed and the viewport override reset. No assignment was saved. These were mixed UI/keyboard actions, not a complete keyboard-only or physical-input qualification.

Saved-revision preview has separate predecessor-source browser evidence and was not repeated on the composition's fresh local origin. The original CSS-only studies, portrait overflow observation and later loading-disabled Motion observation retain their exact source/override identities. OS chooser Cancel, exhaustive states, forced colours, meaningful zoom, additional browser engines, physical input and assistive technology remain open. Standalone Still Media/Video Poster global text preference adoption remains broader P05 work.

`candidate-originals.zip` retains the exact precursor packets, test output, failed attempts, source preimages, proposals and handoffs. `browser-originals.zip` retains bounded server bindings/request receipts and observer transcriptions, including the composed run; no screenshot bytes or raw transcript export are claimed. `composition-originals.zip` retains both complete composed test runs and the independent read-only review that identified the production fingerprint requirement. Later source/docs changes do not rewrite these originals.

`production-originals.zip` retains the producer review, exact before/after fingerprints, append/payload preservation checks and two complete 58-case production/Team test runs (one per supported Node version). The UI recipe advances to FPV revision 28 with 24 UI successors; all 1,117 prior asset records, 28 prior themes, 127 original payloads and compiled artwork remain exact. The two Team presentation bindings advance to that revision without changing their picture, level or geometry identity. These tests are separate from the 49-case media-panel cohort.

`manifest.json` pins every ZIP and member by original path, size and SHA-256. All member hashes and CRCs were reread. Extract an archive only into an empty directory. Historical absolute paths are provenance, not required extraction locations. No browser profiles, user saves, dependency trees or unrelated artwork are bundled.

Final integrated-source gates, qualified immutable assets, publication, public-byte verification and affected public journeys remain required. Candidate scope cannot close full navigation, presentation, content or browser qualification phases.
