# Score and Collection pager focus — internal candidate

This package starts at `87f0f49c84e2f92bceb24b0a317d5e6c79e64ac6`, tree `53fdf6789c28a26e141ee190d5d12395bfc1a980`. That source combines the row correction with the newer composed navigation guidance. Production changes are limited to Library pager ownership and an optional reveal action in the existing operation-focus helper. Its default callers, picture-view close/return, saves, content formats and gameplay keep their existing behavior. See the [pager contract](../../../operation-focus.md#score-and-collection-paging).

Previous/Next nodes survive ordinary page changes. An unavailable focused action hands off to the opposite enabled pager, then the matching search field. Collection collapse detaches its pager and reuses the same controls on expansion. Automatic metadata refresh transfers the original focus lease and its retired state; explicit focused activation alone can renew intent. Late callbacks must still match the current render, model, dialog and foreground. The pager opts into nearest scrolling after settlement, including an already-focused persistent control when content grows above it. Focus callbacks retain the existing veto listeners; the target and resolver are checked again before reveal. Other operation callers do not opt in.

## Source and test scope

The source hold pins the Library module, operation-focus helper and three affected test files. The new 35-case file contains two actual Solo entry cases, 28 exact-markup Library presenter cases and five focused helper cases. The initial 24-case scope preceded the native visibility finding. Profiles originate in legal core completions; the optional still-media fixture uses the established receipt/store helpers. Native disable/removal, focus events and keyboard defaults are finite models. The tests cover stable interior focus, both endpoints, collapse/re-expansion, delayed success/error, live-lease transfer, retired transfer, foreground/dialog/model changes, reentrant lookup/disable and explicit new intent. The full Solo cases retain actual run/checkpoint and exact storage records while paging.

The existing gallery test changes only its native tag/dialog ancestry. All 41 test declarations and assertions remain unchanged. The older Main menu fixture now follows retained Library → Workshop → Title Back and explicitly starts through the named Title action, keeping its original paused-cut/storage/checkpoint assertions.

The initial two-file Node 22 run was **58/59**: all 41 existing gallery cases and 17 other new cases passed; automatic supersession dropped a still-valid handoff. The revised same-lease ticket corrected that source defect. The initial nine-file run was **175/177**, with only two old Main menu assertions expecting parents to close. A bounded read-only module loader substituted the exact original `87f0f49` Library source at its normal URL and reproduced both failures; physical runtime files and the index were unchanged. The corrected whole Main menu file then passed **2/2**. All originals remain retained; these diagnostic counts are not summed into final qualification.

The predecessor nine-file commands passed **177/177 on each runtime**: Node 22.22.2 in 75.723 seconds and Node 20.19.5 in 115.712 seconds. The complete list is Library pager host, gallery focus, Collection context host, Main menu Collection host, gallery difficulty, difficulty host, Library row host, operation-focus host and modal navigation. No skipped or cancelled cases are hidden. That predecessor receipt binds exact argv, runtimes, runner, canonical staged identities and all 292 finite physical inputs (27,093,791 B), unchanged before/after and on its final recheck. The native visibility failure below prevented acceptance of that source. Two targeted reveal cases then failed **0/2** on the unchanged predecessor; the reveal successor passed all **11/11** new targeted cases. The final nine-file successor commands passed **188/188 on each runtime**: Node 22.22.2 in 124.529 seconds and Node 20.19.5 in 147.395 seconds, with no failures, cancellations or skipped cases. All 292 participating inputs (27,100,025 B), source holds and canonical staged identities remain unchanged. The initial targeted failure did not separately snapshot the complete intermediate test file before later additions; its old production and raw failure log remain retained as a diagnostic, not complete before-source qualification. No full build or whole-release CI ran here.

## Native fixture and preview

The first normal preview used exact `87f0f49` Git blobs plus the single held Library module. The corrected preview uses a fresh origin and two held overlays, Library and operation-focus. Its bounded Git server retains a 64 MiB cache/member limit and four reader slots; six direct HTTP bodies match the held source or original Git. It adds no browser/game state, timing or response-body fixture.

The separate portable Library document contains 25 score boards and 25 pictures generated by legal core commands, each a 518-tick down movement ending in a real simulated win. The generator, exact JSON and detailed provenance are retained for actual Library textarea/Import use. Those records are fixture-origin; the synthetic campaign is not installed in the native catalogue, so archived Collection entries do not certify original artwork. No storage or DOM injection is authorized by this document. The predecessor native record contains 16 events and six PNGs, all visually reviewed by the parent reviewer. Logical page transitions passed, but Collection's return to a full first page left its focused Next below the viewport (event 13). This remains a visible-focus failure, not native acceptance. Event 15's intended Back label is corrected by event 16: the first Escape cleared Search; the second closed Collection.

All five held predecessor files were copied before the reveal edit. The earlier six HTTP pins remain retained, but no post-native stable six-file HTTP check was captured before editing; no such check is claimed. A later diagnostic correctly gets HTTP 409 for the old changed Library overlay while its Git-backed helper remains the original. The corrected origin has a separate binding and six verified HTTP inputs; no recorder is retargeted. Its closed native record contains **13 events and seven PNGs**, all visually inspected by the parent reviewer. Setup used actual accessibility clicks plus keyboard text/Import; paging and Search used keyboard. Records and Collection advanced through pages 1 → 2 → 3 → 2 → 1 with visible logical focus. On Collection expansion, Previous/Next measured y = 1288.234375 and height = 44 within a 1348-pixel viewport. Filtering retained Search; the first Escape cleared the native Search field and the second returned to Title Collection. All six post-native HTTP/local inputs matched the held source before any further production edit. The observed desktop scope is 1309 × 1348.

Delayed metadata/error, retired focus, adversarial model changes and reentrant callbacks remain host-test evidence. This internal candidate does not accept P03, a public release, physical controllers, touch, phone/short layouts, actual browser zoom, offline operation or later P05/P08 composition. The separate picture-view close/return correction must retain this pager owner during integration.

The [evidence map](evidence.json) describes every original in the [lossless archive](evidence.zip), including complete logs, input manifests, source holds, failed predecessors, preview bindings, the valid Import fixture and both native records. An unused first successor preview was closed before browser use to correct a metadata sentence from one to two overlays; its original helper and binding remain retained as an admission diagnostic. Historical records are not rewritten to match later source.
