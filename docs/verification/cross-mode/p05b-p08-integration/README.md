# P05-B/P08 static composition — scoped internal evidence

The [composition contract](../../../p05b-p08-static-composition.md) joins exact
P05-B `fcc4032` and P08 static `1817377` over common P05-A `3f934b7`. Original
non-overlapping source/test/evidence retains its original bytes.
The sole manual conflict kept both the menu presentation observer and
`staticPictures` declaration. The independent Team pause-slot spacing hunk is
separate from the already accepted B side gutters.

The [source hold](source-tests/source-held-01.json) binds fourteen production files,
seventeen complete test files, the shared host fixture and merged maintainer guide.
Root and Parfit independently closed the narrow source review. The
[independent proof](source-tests/independent-source-evidence.json.gz) reconciles all
98 non-overlapping B and 111 non-overlapping P08 paths against their original Git
objects, including historical evidence. The initial composed tree is
`455ba9a573273d26811e41b9b1e910ec0619f310`; later documentation is not part of that
runtime hold. No original worktree, version, registry, producer or selector changed.

## Actual automated scope

| Runtime command                               | Result                            | Wall time | Meaning                                                           |
| --------------------------------------------- | --------------------------------- | --------- | ----------------------------------------------------------------- |
| Node 22.22.2, initial seventeen-file command  | 358 pass, one file-import failure | 93.229s   | Sixteen complete files passed; the installed file did not import. |
| Node 22.22.2, complete installed-chapter file | 12/12 pass                        | 166.143s  | Exact script dependencies restored; no source or test edit.       |
| Node 20.19.5, complete seventeen-file command | 370/370 pass                      | 197.312s  | Entire selected union in one command.                             |

These counts are not added across runtimes or overlapping history. All selected
files have passing Node 22 coverage across separate commands; there is no claimed
passing whole-seventeen-file Node 22 rerun. The original 24 fixtures (55,258,200 B)
were present and verified. The prior physical-input snapshot omitted four imported
scripts, totaling 76,256 B, including `scripts/game-cli.mjs`; their exact Git bytes
were restored, without a CLI rewrite. The original failure and merge conflict stay
retained. No skipped or cancelled cases are hidden.

[Verification](source-tests/verification.json) binds exact argv and raw logs.
Initial 532 inputs matched before/after; final 536 inputs, 78,653,307 B, also matched,
with the original 532 unchanged. This finite materialized inventory includes
preserved documentation and does not claim complete dynamic import/read tracing or
node_modules coverage. Format, Couch lint and staged whitespace checks passed at
source hold. Full release CI remains separate.

## Closed normal native record

The normal origin uses exact Git `fcc4032` plus fourteen immutable production
overlays, five differing from that base. [Its binding](normal-preview/binding.json)
and [HTTP checks](normal-preview/http-source-check.json) match all fourteen source
bodies. No old browser record or profile is claimed as new evidence.

Root closed [twelve events, eight PNGs and three gap samples](normal-native/review.json)
and inspected all eight images. At 1309×1348/DPR2, the Team pause-row gap measured
16 CSS px with Rich, Off and Subtle while Plain/Large was selected. Options editing,
explicit Resume/Pause and checked Team → Versus → Solo keyboard departure retained
the existing owners; Solo displayed shared Plain/Large/Ukrainian/Subtle. Normal
Versus picture preparation completed before explicit Start/Pause; Options Back
returned to its opener. Initial stored Team choices were observed, not assumed
empty. Reports containing closed descendants do not prove those descendants were
visible.

## Closed delayed native record

The second origin uses the same fourteen production pins. Its
[binding](delayed-preview/binding.json) and [helper adaptation](delayed-preview/helper-reuse.diff.gz)
limit the six-second delay to GET requests for the original First Signal PNG
`c1aedf89…772fce`, 40,810 B; HEAD is immediate. The original bytes, application
state and game clock are unchanged. The initial default Orchard Crossing did not
use that picture, so its quick completion is not delayed-picture proof.

Root closed [fifteen events and ten PNGs](delayed-native/review.json), inspecting
the final six selected images. Events 8–11 show actual First Signal selection,
Back from Setup cancelling preparation with Retry available and Setup focused,
explicit Retry → Cancel → Retry focus, and a later Retry preserving deliberate
Setup focus through Ready without auto-starting. Explicit Start and separate-seat
S/Down commands then produced a Sunflower win: 52.2%, 8,160 points, three lives,
1:0. View both boards displayed the full original winner picture and losing
unfinished gold cut. Return preserved the result totals and restored the View both
boards opener.

Raw intent labels are retained. Events 1 and 4–6 missed their intended timing;
events 2 and 7 correct them. Event 13 had already finished before Escape, so it is
not a Pause transition. Event 15's note says Next, but actual focus is `race-review`,
the View opener. The prior review is retained alongside the corrected final one.
DPR temporarily read 1.8 during events 4–7 without a controlled action; this is not
a browser-zoom check. No finished → Next action was tested; preserving prior results
through a failed next-picture acquisition remains P07 work.

[Post-native HTTP reconciliation](native-post-http.json) rechecks all 33 physical
source-hold pins and fourteen responses from each origin. The delayed original
picture again matched its Git bytes, with a measured 6.004s GET. This establishes
the retained source responses, not a complete browser import graph.

The [source/normal map](evidence-source-normal.json) and
[delayed map](evidence-delayed.json) retain 62 exact or lossless gzip pairs.
Original diff/conflict context is compressed, never whitespace-normalized; no
original artwork fixtures or browser profile is copied. The packager reconciled
metadata and bytes; all browser actions and visual inspection are root's evidence.

No normal-run movement/win, audio, offline/public, actual zoom or physical-device
claim is made. Team still uses its geometric arena. Later P03/P02/P01/Team-binding
composition and broader P05/P08 acceptance remain open; this is not a phase release.
Broad theme/state contrast, narrow or short layouts, real system/storage-failure
behavior, physical input and exact final-source release qualification remain open.
