# Bordered-dialog focus correction — v0.61.23 candidate

The v0.61.9 picture picker clipped approximately 1.77 CSS pixels of its keyboard focus outline after Large/Plain returned from landscape to portrait. The selected image, unsaved metadata, preview and actual focus were retained. The full public file audit passed; the complete focus journey did not.

`attachFocusClearance` now measures the inside scrollport using `clientTop` and `clientHeight`, excluding the border. It retains the 8px gap, sticky regions, label-fit fallback, existing scheduling and lifecycle guards. Minimal non-native surfaces without client metrics retain the prior geometry fallback; a real zero-height scrollport does not scroll. Font sizes, target heights, outline styling, media, saves and gameplay are unchanged.

The candidate patch was authored and independently reviewed against exact source `6db978db40287151489803e915cb4ecb9ea232ee`. Both changed original files are identical at the composed parent `17e0a455f76c3f4a72f2e596397a0e73463335a5`. Historical RED evidence has 13 passing and three failing cases; the corrected file passes all 16 on Node20 and Node22. The retained actual browser preview covers nine focused picker states across Large/Plain, Standard/Theme and Large/Theme, with keyboard Close/Return/Escape. The Large portrait-return outline clears the actual clip by approximately 1.23 CSS pixels. These are isolated candidate results, not a public correction or exact composed-source certification.

[Original evidence index](originals-index.json) pins every member of [the evidence archive](originals.zip), including the failed public screenshot, measured border geometry, corrected native originals, computed font-family addendum and independent source review. Originals are preserved byte-for-byte. Computed-family adoption is not a claim that a specific fallback supplied every glyph.

Composed v0.61.23 checks now pass all 157 tests in nine complete Focus/Missions/Replay/Still files on each of Node 20.19.5 and Node 22.22.2, with no skipped tests. The independent review found no new defect. The archive includes both raw test outputs and the separate 619-based shared-consumer browser observations. Horizontal mission-card focus and small Replay JSON text remain separate open findings. These tests do not establish a composed-source browser or public result.

Remaining gates: all six source gates, production reproduction/readiness, ordinary build, source PR, immutable release, Pages deployment and actual public rotation retest. Missions/briefing and Replay also use the shared helper. Physical devices, screen readers, zoom and BFCache remain separate. No parent phase closes in this candidate.
