# Playground native-defect correction — bounded runtime result

Source `170508f11dd41204b7e24917a823f21701c15961` plus qualified parent patch `e674b1ba1234f98a1d957345a7d52bf3caf5930c9df8bf7b6488b3842e00f021`, then this two-path successor. At admission, merged main `a185c94c1d7b981f677f6356eafd84a4f68babba` had the same exact tree. The [source reconciliation](../plan-continuation-0e9437e0/current-source-reconciliation-a185.md) preserves this boundary.

**Both complete eleven-file cohorts pass: 127 leaf tests plus one parent group per runtime** on Node20.19.5 and22.22.2. The128 reported TAP results are not128 independent behaviors or256 distinct tests. The parent production with the same strengthened host test produces five behavioral assertion failures and18 passes on each runtime. All901 final inputs remain unchanged. Final formatting and two syntax checks pass.

Final patch SHA256: `c79fd47750a57a7b3aa27dd211ddb5ec9f4f34c8408169a277d7157877cc5d87`. [Candidate/source pins](candidate-pins.json), [run receipts and logs](results.md), [test review](review/genuine-edits-review.md) and [independent evidence review](review/runtime-evidence-static.md) retain exact boundaries. No browser, build, source/index/version or remote mutation ran in this lane. All owned runtime processes are terminal.

## What changed and was checked

Only the Playground application and its existing actual-host test file differ from the qualified parent. Last Undo restores its prior map/settings and hands off only the retiring action's own focus to an available editor control. More history, newer focus and background state retain their own focus authority. Empty/unavailable/nonfinite measurements cannot claim valid minima or complete visibility. Preview replacement clears stale live measurements; explicitly captured geometry remains separate. The running preview changes only through explicit Play.

Owner review strengthened the proposed tests to perform genuine Scout→Bomber→Fiber edits. The first local run exposed a mistaken assertion reading scenario settings from the level-only JSON textarea; its seven failures and exact test source are preserved. The corrected tests verify class restoration through the actual Undo→sync path, while level JSON remains responsible for draft/reset assertions. Production code did not change during that correction. See [first iteration](review/first-run-correction.md).

The final candidate passes all five defect discriminators. Preservation controls also pass on the old parent, as expected; they are not misreported as new baseline failures. The actual-host fixture uses real app/HTML with modeled DOM, focus retirement, layout, frame delivery and scheduling. These results establish model behavior, not real next-Tab order, target geometry, physical controllers, BFCache or deployed play.

## Remaining gates

Use the [current acceptance status](acceptance-status.md). Complete the [nine-row native correction matrix](../p05-playground-native-fixes-170508f1/native-correction-matrix.md) on these exact bytes and retain the wider parent reading/input matrix. Reconcile the qualified parent and successor into the owner's reviewed source with the [maintainer prompt](../p05-playground-native-fixes-170508f1/guide-proposal.md). Then perform the ordinary six release gates, build/artifact checks, related-hunk staging, synchronized unused version, immutable release, Pages deployment and public verification. No feature/phase/public acceptance is claimed now.

The granted lane used a1024MiB V8 old-space ceiling, one Node process group at a time and a180-second test timeout; formatter/syntax commands have60-second timeouts. The heap ceiling is not an RSS or global-memory measurement. No timeout, skip or cancellation occurred. APFS clones preserve original qualified/static packet files; no shared hard links were used.
