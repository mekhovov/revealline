# Practice/Playground candidate evidence

This is an additive selection of original candidate, native and fresh composed-model records. It is **not a release or full-phase acceptance**. The [guide](../../../practice-playground-readability.md) explains user-visible behavior and the open gates.

- `parents/`: exact parent/successor patch, pins and bounded runtime summary, including the retained failed fixture attempt and old-parent discriminators. These retain source `170508f1` and their own patch identities.
- `native-c79/`: original browser README, request log, served bindings, reconciliation, cleanup and receipt manifest. All rows retain `170508f1 + e674 + c79`; U3 remains native-limited, U4/G4/R1 remain partial. No physical-device, zoom or complete-phase assertion is added.
- `composed-822f/`: fresh exact `822f3290 + e674 + c79` composition, twelve preimages, 901 input pins and two sequential eleven-file cohorts. Each reports 127 leaf cases plus one parent group. Input manifests and raw logs are retained; fixture bodies are not duplicated.
- `selected-originals.json`: exact source-to-destination mappings, byte lengths and SHA-256 pins. The integration owner must copy those exact bytes and validate them before committing this directory.

Original records are preserved unchanged, including their historical relative links and cache absolute paths. Those paths describe their original environment and are not claimed to be portable fixture locations. Binary fixture payloads, Git archives, browser screenshot exports and a runnable server are not included in this selection. The native server source and its final fault settings document the original fixture only; do not start it against a different source and inherit its acceptance.

The collection adds no release version or accepted-current claim. Final integrated qualification and public verification belong to the release owner. Prior evidence from other supporting screens or Team cannot fill these open rows.

## Later history-restoration correction

[History restoration evidence](history-restoration/README.md) adds original failed and corrected native observations plus the bounded successor patch, preimage/candidate pins and focused test logs. It binds `822f3290 + e674 + c79 + eb0`, adding one new regression file to the original twelve-path scope. The current selector is reconciled from the preference owner after history form restoration, without saving a stale DOM value or affecting the child.

The corrected actual Back routes pass within their stated scope; the initial mismatch remains a failure. The final three-file model cohort passes 61/61 separately on Node 20 and 22. Earlier 38-test runs and the expected failing parent discriminator remain unchanged. New child sessions on browser return explicitly prevent a BFCache claim. A later bound observation adds Theme/Large width checks, genuine draft/Undo and actual imported-pack Play progress. These scoped passes do not close the entire older U3/U4/G4/R1 matrix, qualify hardware, or establish final source/public release acceptance. The original selection manifest remains unchanged; the history subfolder has its own exact source-to-destination manifest.
