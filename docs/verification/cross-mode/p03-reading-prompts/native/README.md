# P03 reading prompts — native supplement

This separate record observes checkpoint `6727d83e0fefc5d54b1ef6f1d61a19c272b595bd`, tree `786b2cf56a2af8508c4b9c5da8f9ed2693a62a77`. The normal local preview serves exact `1f3b9995` plus the three qualified reading overrides. All twelve recorded HTTP responses match before and after observation and match the checkpoint's Git bodies. The earlier [model-only evidence](../README.md) remains unchanged; its 78/78 results on each Node runtime are not repeated or relabeled here.

The root used actual keyboard and pointer input in the in-app browser at 1454×1348 CSS pixels. Reading records measured DPR 2; the initial diagnostic measured approximately 1.8. The root personally inspected the three retained original JPGs. This is one desktop Mission brief journey, not a viewport or hardware matrix.

| Observation                                   | Result                                                                                                                                                 |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Keyboard Start → Pause → Mission brief → Read | The real reading region receives focus while the game stays paused. Both hints name Enter, Space and Escape; the short brief says all text is visible. |
| Escape, Enter and a separate Space exit       | Read receives focus again without resuming the flight.                                                                                                 |
| Pointer Read → Done reading                   | The hints describe Done reading; the real Done button restores Read focus while paused.                                                                |
| Pointer entry → first Down key                | Both hints immediately switch to keyboard wording without leaving the reading region.                                                                  |

The initial route diagnostic remains ambiguous. An early accessibility snapshot reported Versus while the following retained DOM record showed Solo/Start; an attempted Solo locator had no match. Record 01's label states the intended diagnosis, and its actual DOM is authoritative. Subsequent paired observations were coherent. No product, browser-tool or external-input cause is established. This record does not turn that ambiguity into either a product navigation defect or a passed Versus journey.

[The evidence map](evidence.json) binds all 26 exact original members of [the lossless archive](evidence.zip): five observation records, three JPGs, observation bindings/events/closure/root review, immutable preview configuration and helper history, twelve before/after response descriptors, and checkpoint/source qualification references. Preview preparation and staged-state wording describe their original times; the separate commit receipt records the subsequent immutable source checkpoint. The live server log is not part of this observation package. All decoded members were compared with their retained originals. This documentation addition changes no runtime source and reruns no gameplay tests.

The tab ended in the paused briefing with Read focused. No physical controller/touch, native remapped-controller switch, scrollable reader, full Help journey, save-byte/checkpoint assertion, responsive/zoom matrix, current P02 composition, whole-source suite, public release or P03 completion is claimed.
