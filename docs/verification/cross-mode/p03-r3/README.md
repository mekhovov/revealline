# P03-R3 Library launch evidence

This internal candidate extends exact `4397b367b3f1fa17bf4a0989c90febaca9de9710`. Installed campaign Play, dated challenge Launch and earned-picture Replay flight use the existing Solo replacement decision. An unfinished ordinary flight is paused and checked through the existing save/readback and newer-writer guards; practice is explicitly session-only. Stay keeps the actual parent menus and opener. Confirmed Replace prepares a ready target, closes only its originating menu stack and does not Resume. Same installed/challenge selection is inert; Replay deliberately requests a fresh attempt.

The app resolves finite descriptors against current authored content and the exact earned record. Library visits, picture generations and launch epochs fence delayed work. Blur, hidden state and pagehide retire launch intent, so returning requires a fresh request. This does not cancel image/media preparation or alter P01 import/export operation ownership. Raw selection and saved-flight restore remain separate. A failure after deliberate adoption does not claim rollback.

The [source receipt](source-tests/verification.json) retains three distinct test boundaries:

| Source boundary                          | Complete files                                                                                                              | Node 22.22.2 | Node 20.19.5 |
| ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------ | ------------ |
| Original R3 before foreground retirement | Library launch, mission replacement, starting setup, Restart, course choice, attempt export, backup-set host, gallery focus | 157/157      | 157/157      |
| Final foreground successor               | Library launch, attempt export, backup-set host, gallery focus                                                              | 87/87        | 87/87        |
| Final test-only compatibility adaptation | Difficulty host                                                                                                             | 10/10        | 10/10        |

Each execution checks the same 272 finite materialized inputs before and after. The final two pairs differ only in the explicitly adapted difficulty test; their runtime bytes are identical. These overlapping counts are not summed or presented as a full-suite/phase total. The app and HTML also stayed unchanged during the foreground successor. Final syntax, lint, formatting and whitespace checks passed. Root read the app/Library change; Parfit independently read the final foreground panel and found no blocker.

The new real-host cases cover all three launch adapters, actual Workshop and nested Collection parents, exact Stay focus and paused checkpoint, queued turns, current/changed saved slots, cancellation and foreground loss, stale/closed/reopened origins, same selection, fresh Replay seed/difficulty, practice no-save behavior, and honest post-adoption failure. Actual attempt export, saved-slot load and file import restore the original paused cut. These finite DOM tests do not establish native event sequencing or media fidelity.

Initial failures remain in the [copy mapping](copied-evidence.json). New test fixtures initially had invalid briefs/timestamps, an incorrect completed-overview starting state, missing canvas modeling and a mistaken serialized-library envelope read. The first foreground run had four real blur/hidden source failures; two pagehide cases additionally needed hidden-document modeling. The retained correction runs distinguish those causes. The original difficulty file passed 2/10: its hidden Workshop Restart shortcut failed identically in the exact4397 projection. The corrected test presses visible Pause Restart and explicitly confirms. Its first 9/10 successor then exposed a hidden Expansion packs shortcut; opening that real section and awaiting guarded installed/challenge closure preserves every existing difficulty/checkpoint/save expectation. Final difficulty passes 10/10 on both runtimes. No production change was inferred from these test-model failures.

The [root native receipt](native/native-root-verification.json) records **22 desktop keyboard observations and three PNG screenshots** over the same Git4397 base with the two pinned runtime overlays. The [preview binding](preview/binding.json) and [six HTTP byte checks](preview/source-checks.json) identify the served source. All other assets come from actual Git blobs; the helper has a bounded memory cache and no injected game state, timing fixture, alternate artwork or simulated response.

Root won First Signal and earned its Collection picture, then started and paused Relay Orchard. Picture Replay opened the checked warning above both Collection parents. Stay returned to Replay; a later enabled Replace closed the picture/Collection stack and selected ready First Signal with Start focused. Root then started and paused that flight and used Main menu → Workshop → Scores & saves → Challenges. Pressure run Stay returned to the launcher with its three parents retained; fresh confirmed Replace closed them and selected the ready dated expert route with 75% target and Relay requirement visible.

The raw event labels record intent and are preserved unchanged. Event01 was loading; event04 showed the earned reward, event05 opened Missions, and event12 still took Stay while confirmation was unavailable. Event14 was the accepted replacement. Later paused native attempts showed 0:00: exact nonempty-cut checkpoints, saved bytes and queued turns are host-test evidence. Root owns the browser observations and screenshot inspection; the source author only copied and verified the retained bytes.

Native installed-pack Play, background/stale-slot faults, physical controller/touch, phone/zoom, public/offline and final integrated-source qualification remain open. Picture viewing retains its earned original; mission Replay still prepares current authored artwork. Exact earned-original Replay fidelity remains a P08-A/P07 acceptance item. This package does not include A1/A2/TitleB/P05/P08 composition, a version change, a new accepted phase or complete P03 acceptance.

The mapping preserves 71 originals, including all failed logs, source holds, executed runners, source/test snapshots and native records. Logs/diffs use deterministic gzip and are compared after decompression; other files remain byte-exact. Historical cache-relative receipt paths resolve through that mapping. Source receipts pin the pre-native documentation snapshot; later evidence prose is a separate reviewed documentation change and does not rewrite those receipts.
