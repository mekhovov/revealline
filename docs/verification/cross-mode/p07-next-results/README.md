# Retained Results during Next preparation

This bounded P07 checkpoint is based on `90e8bc8102cc71a992a57aa1f41d7cdcd6659b3e`. A completed Versus round remains accepted while a separate duel and selected original are prepared and confirmed. Failure and Cancel preserve both completed boards, Results, series wins and keyboard View. Successful adoption publishes the new duel and image before releasing the previous original.

The source changes four Couch modules and three affected test files. It does not change core rules, authored content, the compiled artwork registry, save/progression writers, replay formats or persistent schemas. The [contract](../../../retained-next-results.md) describes candidate ownership and later composition with the resize shell. The [evidence map](evidence.json) identifies each exact original in the [lossless archive](evidence.zip); retention of an attempt does not imply that it passed.

## Executed source checks

The transaction and focus implementation at held06 passed twelve complete files on both supported runtimes:

| Runtime      | Result                                       | Elapsed        |
| ------------ | -------------------------------------------- | -------------- |
| Node 22.22.2 | 246/246, no failures, cancellations or skips | 578.51 seconds |
| Node 20.19.5 | 246/246, no failures, cancellations or skips | 473.80 seconds |

All 536 participating inputs (78,700,235 bytes) matched before and after both executions. The receipt binds actual runtime binaries, the base/tree, canonical Git index, input authority, runner and raw logs. Three guidance files were subsequently updated; their exact tested 90e8 bodies are retained separately. All seven held06 runtime/test bodies were also preserved before the final message change.

Held07 changes only the Next failure message and its console diagnostic. The existing static host case now proves that an asset path and hash stay out of player text while the original Error remains available to diagnostics. Two installed-host assertions retain their exact decode and changed-snapshot causes in the diagnostic instead of the player message. All result, image-release, View, currentness and no-write assertions remain in place.

The two complete affected host files then passed 46/46 on each runtime at concurrency 1: Node 22 in 358.30 seconds and Node 20 in 404.80 seconds. Their 536 inputs (78,704,523 bytes) remained exact. These files overlap the twelve-file run; the counts are not additive. The three guidance files were finalized after this boundary, and their exact held07 test bodies are retained too. Runtime/test pins stayed unchanged. Source peers closed both the transaction and the message successor, and ESLint, formatting and whitespace checks passed.

Tests use real duel, host and authority code with finite browser/image boundaries. They observe run identities and checkpoints, result and series-score text, prior/candidate images, release counts, installed pointer bytes and unchanged writes. They cover read/decode/final-confirm refusal, captured-setup retry, Cancel and late settlement, background Ready behavior, new setup, deliberate Help focus and queued callbacks, and old-image cleanup observing the newly published duel. The local View button default follows actual handlers and requires unprevented, unmodified, enabled and focused activation. It models a browser boundary, not native behavior.

## Retained diagnostics

The original single-test failure showed Next replacing the completed round with Ready when decoding failed. The first targeted implementation passed 16/18: transition focus still moved after background return, and the local View fixture lacked native button activation. The focused shell option and finite fixture default produced 18/18. A later 2/3 characterization exposed Next intent surviving deliberate Help focus; the live owner now remains valid through cleanup, restoration and queued callbacks.

A 27/28 targeted successor retained one fixture issue: a synthetic Retry click left focus on View. Focusing the actual initiating control corrected that case without weakening assertions. The first complete Node 22 run then passed 230/239; Node 20 did not start. Eight failures traced to an omitted exact R5 pack or its fallback selection, and one exposed changed timing of the installed owner's initial status. The pack was restored directly from the base Git blob. The initial notification is synchronous again after publishing its promise, preserving reentrant joining. Its original targeted test and the twelve-file pair passed.

The first friendly-message rerun was interrupted while shared disk capacity fell below the reserve. Its raw Node 22 log contains 30 reported passes and no complete TAP summary; Node 20 never started. The runner then failed its reserve check before persisting a child exit code. A later check found all 536 held inputs and the canonical index unchanged. This attempt is neither a passing complete-file result nor a diagnosed product failure. The subsequent pair used fresh logs and concurrency 1 after the other agent released the test slot. No specific cause of the shared capacity change is claimed.

## Native scope

Root recorded five independent held06 origins: 28 observations and 23 JPEG files. All five had ten matching source HTTP bodies before and after their closed recorders. Root inspected all 23 images. Two mostly blank captures remain visually inconclusive despite populated DOM; adjacent images and recorded state support only the narrower findings below.

| Origin | Actual observed outcome                                                                                                                                                                                                                                                                    |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 64817  | First Signal cleared at 52.2%, 8,160 points and 1:0. Next started a fresh round with three lives and the same tally. A later attempt lost a life; it was not a second clear.                                                                                                               |
| 64818  | HTTP 503 retained Results and View. Help cancelled a delayed retry and retained Read controls focus. The earned image remained viewable, and a fresh Next worked. This is Help cancellation; the host-only Ready-with-Help case is separate.                                               |
| 64819  | New match cancelled the delayed retry; Keep this match restored Results. This was not direct Cancel. Screenshot05 is mostly blank and is not a visual pass.                                                                                                                                |
| 50955  | Cancel had focus, but the next action missed the six-second window. A new round was already running. This is inconclusive for Cancel and is not a product failure.                                                                                                                         |
| 51279  | Direct Cancel was observed and activated within the delay. Results, Next focus and the tally remained after 6.5 seconds; View showed the earned original and a fresh Next worked. Screenshot04 is mostly blank; images03/05/06 support the retained result, original and subsequent round. |

Each fault origin used the exact First Signal original with independent counters: GET1 normal, GET2 HTTP 503, GET3 delayed six seconds, later GETs normal; HEAD never advanced the sequence. Aborted GET3 requests and later successful GET4 responses are retained in append-only logs. The fixture changed transport only, with no application, clock, result or storage injection. The actual viewport was 1454 × 1348 CSS pixels at DPR 1.7999999523; no cause of that DPR is inferred.

The held07 friendly-message successor was checked on fresh origin 55031: two keyboard observations and two root-inspected JPEGs. After an actual First Signal clear, GET 2 returned HTTP 503 retained Results, the tally, Next focus and View. Player text matched the concise message exactly with no path/hash, while the original technical Error appeared in console.warn. Ten source HTTP bodies matched before and after. This is a message check, not a repeated transaction matrix. A preceding 54515 setup attempt activated Refresh instead of Done and consumed GET2 before any round; its original DOM, note and transport log are retained as an operator diagnostic, not a product failure or Next acceptance result.

Native event labels describe attempted actions, not anticipated success. Verdicts follow the captured DOM, inspected image and transport evidence. All provisional labels and their explicit corrections remain unchanged in the retained originals. The predecessor normal preview was never used for a native recorder; its prior source responses and four overlays remain preserved, and its binding was not retargeted.

Full match progression, one-race tours, Team order, later P03/P05/Team composition, audible output, browser offline/public release and physical-device acceptance remain separate. No version or phase-complete claim is made.
