# P07 Retry contract correction evidence

This is a local candidate record against source `c4851d140b8c7c8ebaebfede998b4d9e60f18807` (tree `4fd8806a081f1a947d32d8c8a8acf910a6d2c6d4`). It corrects three regression-test files to exercise the new result-retaining asynchronous Retry/Next contract and updates two guidance files. No game runtime, decoder deadline, storage, media reuse or saving policy changed. Final source qualification and public acceptance remain pending; this does not complete P07.

The four complete affected host files pass **38 distinct cases on each of Node 20.19.5 and 22.22.2**, with no failures, skips or cancellations. They cover real wins/losses, retained run/art/checkpoint while preparing, Cancel and stale completion, friendly failure, direct start when ready, both turning modes, and held-Confirm/foreground-return safety. The guide and maintainer-skill prose changed after those runs; the original run input manifests remain unchanged.

`test-evidence.zip` retains all six local attempts, complete output, receipts, input manifests, hydration failures, interim fixture failures, preimages and prior candidate snapshots. `ci-failures.zip` preserves the four original hosted logs and their descriptors: seven distinct preceding CI failures, not seven new game defects or fourteen distinct failures. Repeated manual/PR occurrences are historical observations of the same obsolete assertions.

`ordinary-browser.zip` contains exactly the 129 files (297,515 bytes) listed in the unchanged root browser observation. On source c485 with no runtime overlays, the observer completed a legal win → native story playback beginning → Retry → second win → Next. The result stayed visible during preparation; the new attempt started directly. Retry was observed by 0.87 seconds and Next by 1.93 seconds. This used pointer UI and paired emulated keys in Chrome at 1280 × 633, DPR 1. The initial failed wait and recovery are retained. It was not a keyboard-only or physical-device qualification.

Read the browser observation together with `reports/browser-peer-review.json` and the append-only `reports/browser-timing-correction.json`. The original story timing summary is imprecise: Play was observed by **14.094 seconds** after the opening wrapper began; exact readiness is unknown. Playback beginning is supported, uninterrupted completion of the eight-second story is not. The original observation was not rewritten.

`isolated-input.zip` and its observation preserve the earlier blank-page reproduction of a repeated-input storm with one input-driver method and quiet behavior with explicit paired CDP keys. It does not establish a game performance fix or a general Chromium diagnosis. The later ordinary journey is separate evidence. No browser profile, saved-user-data directory, media payloads or source dependency tree is included.

`candidate-patch.zip` contains the unchanged `candidate.patch`; `candidate-pins.json` pins the exact reviewed five-path proposal. The patch is archived so its context-only whitespace is retained as evidence without being treated as source whitespace. The plaintext original remains in its cache packet and commit `951e8f8b`. `reports/` contains unchanged original reports; their historical absolute paths are provenance, not required extraction locations. `manifest.json` maps each retained file and every ZIP member to its original repository-relative path, byte count and SHA-256. ZIP names are relative, CRCs and every member hash were checked. Extract only the selected archive into an empty directory when inspecting originals.

Full final-source checks, public verification, offline recovery, assistive technology and physical touch/controller/keyboard acceptance remain separate gates. This browser pass did not repeat fault injection or independently establish stored original bytes and duplicate-award behavior.
