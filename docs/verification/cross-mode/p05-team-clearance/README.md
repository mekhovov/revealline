# Team clearance correction evidence

These exact originals support the [Team display contract](../../../team-display-clearance.md). The current v0.60.1 source preserves the reviewed runtime9792a081 and CSS541f5998 byte identities. `originals.json` records each copy from its retained candidate packet. Historical cache paths inside records identify their original producer and remain unchanged.

- `native-final/`: actual F1–F4 browser observations, 702 requests / 251 distinct exact response bodies, two candidate overrides and original source identity.
- `native-before/`: original clipped-selector, portrait-control and intermediate desktop failures. These are not rewritten as passes.
- `composed-local/`: fresh 0.60.1 Node20/22 complete 164-case runs, syntax/format results, 161 input manifests, retained setup failures and a separate documentation successor. No fixture bodies or compile caches are embedded.
- `prior-review/`: prior bounded runtime cohorts, source chain, static action reachability and CSS formatting. Their pending-native wording retains its earlier cutoff; final native observations are separate.

The earlier complete 164-case Node20/22 cohorts bind their own source inputs. The final candidate browser checks do not certify physical controllers/touch, 200% zoom, terminal win/rescue or the whole phase. Fresh integrated-source checks and public release acceptance are separate gates. No screenshot artifact is claimed: visuals were inspected inline, with child geometry and exact response identities retained.

The original six-path proposal/baseline and failed attempts remain in `.cache/p05-team-final-delivery-170508f1/`; duplicate source copies are intentionally not embedded here. The current files and Git source identities are authoritative for the new release. Frozen v0.60.0 and its accepted delivery are unchanged.

The two original cohort `stdout` files each contain two trailing-space lines in the test name for an intentionally whitespace-only stored value. Git's whitespace check reports those four original TAP lines; they are retained verbatim to preserve the evidence hashes. The reviewed runtime, test, guide and other changed paths pass the check. This exception concerns original output bytes, not source formatting.
