# P03-H: deliberate starting setup

Starting class and Steering reuse G's Stay/Prepare confirmation for unfinished ordinary Solo attempts. The current select value, run and replay identity stay intact until a separate deliberate Prepare. Same values do nothing; ready/terminal selection remains direct. G/H/Team share one owner. Ordinary flights reuse checked saving and changed-slot reconfirmation; practice/lesson guards are explicitly session-only. The Tactical Hangar remains its existing in-flight action.

Eight complete files passed **132/132 on Node 22.22.2 and 132/132 on Node 20.19.5**, with no failures, cancellations or skips. The 26 new actual-host cases cover running/paused setup, original replay/save restoration, same-value/invalid inputs, pending Stay/Escape/controller/pagehide, storage failures and newer slots, controller draft/held Confirm, practice/lesson isolation, a real Tactical switch, secured capture-stop, respawning and terminal behavior. Seven existing concerns are covered by the other complete files: mission replacement, mode-return unit/host, checked retention, mission picker, difficulty and continuous input. [Verification](verification.json) and [retention](retention.json) pin the source, exact native originals and losslessly compressed logs. Lint, formatting and Git whitespace checks passed; these host models do not prove browser decoding, hardware or native storage failure.

Root's [19 native records](native-events.json) cover an actual paused eleven-second Scout/Immediate flight: changed class and Steering each offered Stay and returned to the exact selector; deliberate Light carrier (`bomber`) and Grid preparation each produced a ready 0:00 attempt without starting. Explicit Load saved restored the original eleven-second Scout attempt paused. Actual Learn by playing carried `grid-center` in the course URL; Return to the game → Continue restored the original Scout/Immediate flight paused again. The early Tab while Prepare was disabled actually selected Stay; its original action label and correction are retained, not counted as a failed or successful Prepare.

The [native guard](native-verification.json) binds served app/HTML to the held source and separately identifies the reviewed P05 compact stylesheet overlay (`de78f169…`). That stylesheet is not included in H. [Class confirmation](class-confirmation.jpg) and [course return](course-return.jpg) are root's original screenshots; the packaging agent verified bytes without performing another browser review.

Course UI currently exposes no visible Immediate/Grid control. `game/index.html` places Steering in the flight deck; `game/ui/game-shell.mjs` moves that deck under Missions and hides course menu entries. Settings Controls configures bindings, touch and Boost, not turn policy. Therefore H's in-course steering case is defensive host coverage only. Ordinary practice retains Menu → Missions → Flight setup; its host test is not a new native practice journey. Do not force hidden controls to claim acceptance.

Initial failed logs remain: the first run used the wrong profile envelope and assumed a queued Grid turn had already applied after two ticks; the correction uses the existing library reader and exact replay-reconstructed fixed-tick comparison. The second run assumed class history started empty; the actual authored switch appends one entry to existing history. No production change was required for these fixture corrections. The final complete pair is separate from those failures.

P03-H is an internal work package, not a completed public phase. Native scope is desktop keyboard only; queued Grid/capture-stop/controller drafts/recovery/storage failures remain host evidence. Physical controller/touch, phone/Safari, combined release and public acceptance remain open. Lesson Skip/Retry/selection, Library scenario launches and Team Change setup are distinct departure journeys. No version, producer, core, replay schema, saved format, content or music change is included.
