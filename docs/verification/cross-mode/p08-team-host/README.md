# Team picture host — internal checkpoint

This checkpoint composes the reviewed Team painter, lease and two-row binding checkpoint `0c5ee5fc866eecec6e924c1030b0078bce0f6531` onto exact P05-B/static source `063ff5d650b6e071d2b59e89fa6e9fb5058b3f08`, tree `8538fc8d919434e42d6390f08e73c8db3d82caf1`. The [host contract](../../../team-picture-host.md) describes the candidate/accepted-picture boundary. Later P03/P02 work and completed actor parity are absent. This is an internal source and observed-journey checkpoint, not P08 acceptance or a public release.

## Exact source and authority

Held06 is SHA-256 `b41b49a59382a44bb960f18c5c69ccd8278c5b5d79039434441fcd15a3e7bbdd`. Its fourteen game source/test/helper pins remain exact through both native runs; its guide pin is historical and was subsequently updated with qualification and native scope. The source holds and original diffs are retained without relabeling them as final documentation. Seven production overlays are the Team host, HTML, CSS, image decoder, binding table, presentation lease and painter. The existing 72 Team host declarations and assertions survive helper extraction; three other host fixtures await the actual pre-mounted presentation and dispatch through the real preference event owner.

The compiled runtime snapshot is `94e24b96644c5c1c2e8d4e014f4d80fe7812359827863d75f00ab132c684982f`, with fpv revision 19 and null collection. Only the complete canonical starter pack and the two exact authored arena identities may use the approved Orchard/Foundry tuples. The images are 1152 × 576 PNGs: Orchard `53f1206a…`, 52,720 bytes; Foundry `d76f309d…`, 38,090 bytes. These are reused approved derivatives, not new art or mission counts. Same-ID content changes are refused before reads/decode. A newer accepted theme revision requires a separate exact authority review; this checkpoint does not silently broaden its match. Original binding/painter evidence remains byte-exact to the incoming checkpoint and retains its earlier source-only scope.

## Executed tests and retained diagnostics

Twelve complete files cover 273 cases on each supported Node runtime: picture host, Team host, picture bindings, presentation lease, painter, presentation page, controller navigation, Couch input, Team input policy, shared display host, menu appearance host and Couch master audio. The Node 22.22.2 initial whole run was 272/273; the one new preference case called an obsolete direct `onchange` fixture path while the actual control uses an event listener. Corrected real bubbling-change activation then passed the complete 19-case picture-host file. Eleven unchanged complete files contribute 254 retained passes; these scopes are not added to the initial run as independent totals. Node 20.19.5 passed all twelve files in one 273/273 run. The receipts pin 332 finite game/authoring inputs before and after each run, with no participating drift. Documentation was updated after this boundary. The earlier unwired nine-file 195-case checkpoint is not additional host coverage.

Two narrow product characterizations precede that qualification: fresh pointer/key intent which did not move focus originally allowed late Ready focus (0/2 before, 2/2 after), and terminal pagehide originally rendered after presentation retirement (0/1 before, 1/1 after). The terminal correction performs cleanup without repaint while preserving the BFCache suspension path. Raw failures remain in the archive. Finite DOM, image decoder and frame behavior are modeled; real metadata and approved image bytes are used for admission. Tests do not certify native layout, hardware, sound, or a whole-release build. No new tests were run merely to package evidence.

## Native observations

Both origins serve exact Git063ff5d bodies plus the same seven immutable held06 overlays, with a 64 MiB Git-blob cache and four request slots. The normal origin is port54062. The explicitly labelled timing origin is port54591; only GET of the exact Foundry PNG waits six seconds before headers/body, including ranges. HEAD remains normal. No state, image, schema or application-clock substitution is involved. Fifteen HTTP/local/authority bodies match before and after each closed run. Delayed GET measured 6.208 seconds before and 6.098 seconds after; post-run HEAD took 0.050 seconds. These values validate the fixture, not real-network performance.

The normal recording contains nine events and seven JPEGs, all visually inspected by the root reviewer. It observes explicit Ready → Start, First Connection's separate picture, confirmed Retry, and a real Gentle flight. An attempted unboosted timed opening failed: a Hunter downed Skyline and a later attempt consumed a reserve. Short loops with Support then reached 4.6% capture with both craft on safe ground. Pause and Change setup → Stay retained the visible capture and picture; explicit discard returned to ready setup without starting. These observations are not a native full win, joint-cut completion or successful rescue. The retained source-route note describes an existing boosted simulation route; the failed unboosted native attempt is not relabeled as its reproduction or as an automated-test failure.

The delayed recording contains nine events and six JPEGs, also visually inspected by root. Its first targeted Cancel press found no matching target because the picture was already Ready; no action occurred. A fresh First Connection → Relay Yard selection then exposed real pending loading. Keyboard Cancel left Retry available after the delayed response. Explicit Retry followed by Tab to Help retained Help when Ready settled. Explicit keyboard Start entered Relay Yard with its distinct original visible at the border, then Pause retained the arena and image. Arena preparation used the observed combobox selection API; Cancel and subsequent actions used keyboard activation. A label locator mismatch and the initial missed action remain recorded.

The normal viewport was 1923 × 1356 at DPR1.8; the delayed review records 1924 × 1356. Screenshots and DOM/text observations are sequential, not an atomic checkpoint or a hash of every rendered image pixel. Exact original identity comes from the independent source/body pins. No native corruption, offline/custom-pack admission, terminal-dispose or hardware tests are claimed. The full Team completion/encounter matrix, rescue and joint-cut proof, actor parity, responsive layouts, controller/touch devices, accepted-source composition and public/offline release gates remain open.

The [evidence map](evidence.json) identifies each original in the [lossless archive](evidence.zip): complete test logs and manifests, historical holds/diffs, preview configuration and timing checks, both original native recordings and their visual-review receipts. Earlier preparation and verification files retain their then-pending status; the present narrative adds the later native boundary without rewriting those originals.
