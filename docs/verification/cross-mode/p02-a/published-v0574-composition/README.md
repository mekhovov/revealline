# P02-A composition on published P01 v0.57.4

This is an internal candidate record, not a qualified release or phase completion. The uncommitted merge combines P02 `cbc09f7640ad554c4c34d8699f2e81458e5b8faf` with published P01 `856850ce8d4597c1fb6e8bb28ddb8a63db337a8c`. Version 0.58.0 was unused when checked. P01 public acceptance and a separately reviewed merge of current main publication history remain prerequisites for final source, build, frozen-artifact and public gates. See the [composition guide](../../../../p02-p01-v0574-composition.md).

The producer starts from P01's coherent ledger and all 131 compiled bodies. It retains P01's immutable `fpv@23` history and appends eight audio7 records and `fpv@24`, parent23, preserving screen12 and all 127 original payloads. Actual Node 22 CLI write, reproduction check and repeat write pass; all 132 ledger/output bodies remain unchanged on repetition. The fixed compact oracle comes independently from the exact published bundle. The history regression verifies accepted record prefixes, every actual payload, the reconstructed original bundle hash, exact additions, immutable-history refusal and repeat export/compile identity. This does not replace final CLI/build/public qualification.

## Execution boundaries

| Source boundary | Execution                                   | Scope                                                                                                                                                                                      |
| --------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Original held01 | Node 22 initial 315/316                     | One missing dynamic inventory input in the complete presentation-system file; all other 22 complete files pass.                                                                            |
| Same held01     | Node 20 remaining 22: 294/294               | The whole presentation-system file is deferred to hosted qualification. Its 17 absent exact pack bodies exceed this worktree's 100 MiB budget; no symlink or synthetic substitute is used. |
| ARIA held02     | Five complete files: 79/79 on each Node     | Changing-name commands have no `aria-pressed`; labels, keyboard actions, master state, focus and transport/checkpoint oracles remain.                                                      |
| Status held04   | Two complete host files: 20/20 on each Node | Immediate master text before a media event, later notifications, held Settings Play followed by Library Pause, and blocked Play followed by master edits and explicit retry.               |

These cohorts overlap and must not be summed. All 797 physical inputs remain exact within each recorded runtime boundary; later docs/guidance and narrow successors have separate pins. The initial raw inventory failure and capacity-refused shell launch are retained. The latter occurred before producer execution. One inherited raw P01 log whitespace line remains byte-exact, with its warning recorded.

Root reviewed the original composition, the independent history oracle, the ARIA changes and the status renderer. Lagrange's independent callback audit found the text refresh seam. The renderer reads current master state and current transport notifications; awaited gesture results cannot overwrite a newer player's label. It makes no playback or storage calls. The legacy completion also rechecks whether a current player has taken ownership.

## Native observations

Root inspected the original 54754 origin's 12 events and seven JPEGs. Actual Library Pause kept Quiet Orchard at 16.8 seconds through mute/unmute; 13% persisted through reload, Versus, Team and Asset Studio. Event 05 initially mistook Settings Play for Pause; its correction and the later actual Library Pause remain explicit. Root observed contradictory changing-name toggle semantics and a transient stale muted-status phrase, so this predecessor cannot qualify the correction. All 146 HTTP/Git body checks before and after agree.

Root then inspected the corrected 57536 origin's eight captures and seven JPEGs at a 1454 × 1348 viewport and DPR approximately 1.8. Keyboard master commands have coherent names without `aria-pressed`. Actual Library Pause followed by Unmute kept Signal Afterglow paused at 23.4 seconds. Versus Options used keyboard, Team Options used a pointer, and Asset Studio opened by URL; all showed the shared 13% setting. Studio showed FPV revision 24 and 293 slots. These are bounded semantic/preference observations. The source tests, rather than native capture timing, establish the immediate-before-media-event and held-completion races. All 147 HTTP/Git body checks before and after agree, including all nine modified modules/markup and all 132 generated bodies.

Capture 01's provisional loading-stall label is corrected by its actual ready Title snapshot. Earlier uncaptured DOM reads stayed on “Loading flight systems” for roughly four minutes. Six checked server resources responded normally, with no pending HTTP connection or traceback; the cause remains unresolved. This is neither a reproduced source defect nor accepted cold-start performance. Final build/public qualification must recheck cold start.

Both previews serve immutable staged Git blobs. Their HEAD/tree identify the `cbc09f` baseline, not a final candidate commit. Adding this evidence changes documentation only; all reviewed runtime/test and generated-body pins remain unchanged. No new MP3, audible output, PCM, hardware, screen-reader/device, complete mode-navigation or whole-phase acceptance is inferred. Earlier audio proofs retain their original source boundaries. The actionable Settings transport label remains a separate P02-B experience item.

## Retained evidence

The [evidence map](evidence.json) and [lossless archive](evidence.zip) retain 137 unique members, 6,390,660 decoded bytes, in a 2,851,548-byte ZIP (`aa37f43a41f81127afeb95245c02636e93ccc5201b16b7c6029ff89c160ab4ef`). Every member matches its current original byte count and SHA-256, and ZIP CRC checks pass. Source roots distinguish this worktree from root-owned native files. Images were visually inspected by root; packaging only rehashed them.

Runtime files and generated assets remain in the Git change itself. The accepted prior is identified by immutable published Git bytes and the independent oracle instead of a duplicate ledger body. Raw failures, initial and final source holds, complete logs, input manifests, preview bindings, root reviews and corrected native observations remain separate. The current-main merge and final committed-source qualification are still required.
