# P02 final composition — source-bound browser follow-up

The reviewed candidate is `86e381aed02a94e0882d3a0d6f29ebea9b7b386d`, tree
`729b8e4f2de2ab1ba7f43d716b26c92db31ef8ac`. This package records its local
history merge and Root's final native master-control observations. P01 public
acceptance still awaits the real owner receipt. This is neither a new accepted
baseline nor final P02 hosted, frozen or public qualification.

## Source and history

The merge joins the reviewed P01/P02 composition `c1bb897` with main history
`016b14a`. Relative to `c1bb897`, all 95 changed paths are under
`publishing/pages-controller/`, whose complete tree equals the incoming history.
Every non-controller body and mode remains unchanged. All six merge conflicts
retain their exact previously composed bodies, including the master/audition
panel, version files and execution register. The candidate remains 0.58.0.

The [original merge review](retained/p02-p01-main-merge/review.json) keeps its
then-uncommitted wording and the inherited raw whitespace diagnostic. The
[commit receipt](retained/p02-p01-main-merge/commit-receipt.json) binds its actual
merge parents and tree. The [new metadata review](retained/p02-final-composition/review.json)
independently checks the controller-only difference, reviewed combined bodies
and both sets of HTTP source pins. No controller archive bodies, test run or
publication operation were added by this review.

## Final browser observations

Root recorded [12 events](retained/final-native/browser/events.json) and two
original PNGs at the exact Git preview on port 60258. The preview binding has
**no overlays**, uses normal delivery and records no active artificial delay.
Twelve actual source bodies match this commit before and after the observations.
The source list includes Solo, master/persistence, music/story presentation,
Versus, Team and Asset Studio modules; inclusion in that list alone does not
establish a new browser journey through every listed module.

Observed actions were:

- In Solo Settings, keyboard changes master from 80% to exactly 13% using the
  aligned one-percent steps. Explicit Unmute and Play start the selected
  playlist; Mute preserves its playing transport state.
- The Music Library Studio inherits muted 13%. Explicit Unmute retains the
  existing song, and Close returns to the exact Settings library opener.
- Direct navigation to Versus shows inherited 13%; its actual control saves
  mute. Direct navigation to Team shows muted 13% and the truthful visual-only
  feedback notice. Team changes master to 80% while still muted.
- A fresh Solo page inherits muted 80% and starts with paused transport.

The [muted Studio](retained/final-native/browser/muted-studio.png) and
[Team shared master](retained/final-native/browser/team-shared-master.png)
images are original bytes. Root owns their inspection; this reviewer hashes and
packages the records without new browser actions. Event 1 also retains an early
Title BODY-focus observation requiring Tab traversal, outside this P02 slice.
Cross-mode checks used direct page navigation; they are not P03 route acceptance.

These observations do not add a new MP3 upload, decoding/listening assessment,
PCM measurement or physical speaker/controller/device proof. Earlier actual
MP3/story observations and the [digital mixer probe](../output-probe/README.md)
retain their own exact source and evidence boundaries.

## Qualification and preservation

No tests were rerun for this controller-only history merge or package. The
[previous composition](../p01-composition/README.md) retains 178/178 focused
checks on each supported Node runtime and 40 offline utility checks. The
`ac644a9` full 4,291-test/six-job record remains historical; none of these counts
is relabelled as a full qualification run of `86e381` or a future documentation
successor. P01 acceptance must precede final intended P02 source gates,
production/readiness, build/freeze, immutable release and public verification.
P02-B music/content expansion and P03/P05/P08 preparations remain separate.

[The evidence map](evidence.json) retains 19 bounded originals: exact JSON/PNG
bytes and lossless gzip for helper text, logs and diffs. Every copy and decoded
body was checked against its original SHA-256. The merge review's references to
larger local evidence remain references; the 6.66 MB staged diff and controller
archive bodies are not duplicated here. No original was edited. The top-level
register, PR62 and accepted-baseline records are unchanged by this package.
