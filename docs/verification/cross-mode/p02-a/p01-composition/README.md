# P02-A composition with frozen P01

This is a local v0.58.0 candidate, not an accepted phase or release. It applies P01 source `b9179e391e7ef11435c12f16693fb03ff3352f2e` and its `7945c6d07714d41cee9b6f848059b8dc18aed86c` documentation successor after cutoff `d0288c502620bfa7a4a0861fbdbce1a60be183c5`, onto P02 `ac644a99eebb2fef049458f6abf69fa626f81983`. The earlier cutoff's non-controller evidence-staging skill paragraph is retained. The original P02 worktree and PR62 are unchanged.

The reviewed pre-packaging index tree is `7c84746509c51c6546528292004d10b9cf386373`. Its 117 changed paths comprise 110 exact incoming files and seven combined source/test/guidance files. The three full version files remain byte-identical to P02: package, lock top/root and build config all retain 0.58.0, with unchanged dependencies. The complete Pages-controller subtree remains unchanged; this composition imports no selector or archive-admission update.

Five conflicts were resolved explicitly:

- Studio retains the local master-binding reset, calls Play in the activation turn, exposes pending status and Finish, then awaits playback. Injected shared authority continues to suppress the legacy enable callback.
- The execution register retains P02's approved preparation/order and P01's published history without claiming P01 acceptance.
- Package, lock and build-config keep P02's existing versions and dependency bytes.

The host merge retains P01's typed picture-writer recovery, retired speculative work, replay operation rail/focus cleanup and trusted absent external-index replacement alongside P02's shared preference precedence, 1% controls and output lifetimes. All ten utility files and their workflow dependency are exact frozen P01 bytes. The [shared master contract](../../../../shared-master-audio.md) remains authoritative for output versus transport behavior.

| Executed boundary                                                     | Result                                  |
| --------------------------------------------------------------------- | --------------------------------------- |
| Twelve complete focused files, Node 22.22.2                           | 178/178; 78.534 s runner, 78.579 s wall |
| Same twelve files, Node 20.19.5                                       | 178/178; 84.330 s runner, 84.627 s wall |
| Existing offline utility suite, Python 3.14.4                         | 40/40; 16.741 s runner, 16.885 s wall   |
| Combined app/Studio/test lint and combined source/guidance formatting | Pass                                    |

The exact twelve argv paths are retained in [the execution record](retained/focused-execution-02.json): ten audio files plus history-picture and profile-original recovery. Both runs use test concurrency one. Eight added resolve/reject combinations change master mute, zero and 13% while retaining a separate local audition fader, then Finish, Close, hide or dispose before completion. They check visible Finish, silent retired output, unchanged master intent, one-time transport restoration, URL cleanup and unchanged saved generation. P01's file-picker cancel versus dialog Escape case remains in the complete Studio suite.

All 400 tracked physical execution inputs were unchanged before/after. This is a finite input record, not a complete dynamic import-graph claim. The eight new cases initially existed beyond the staged automatic P01 test merge; the reviewed and executed physical test is now staged exactly. The [original boundary record](retained/source-index-boundary.json) preserves that distinction. Packaging changes only guidance/evidence; the two updated, previously tested guidance originals are retained losslessly.

The first Node22 attempt passed 158/178: twenty host boots failed on absent sparse catalog input. Four exact indexed catalog/scenario files totaling 10,810 bytes restored the finite checkout; no runtime or test correction occurred between attempts. The failed raw log and both passing raw logs remain separate. The original [pre-packaging completion](retained/completion.json) deliberately retains its then-uncommitted status and tree.

The complete staged whitespace check reports only three original incoming evidence files: `offline-portability.diff`, `workflow.diff` and `recovery-verification-copy/before-fix.log` under the P01 verification directory. Each remains byte-identical to frozen `7945c6d`; the [evidence map](evidence.json) pins their exact paths and hashes. Excluding only those originals, all remaining staged paths pass. This is preservation of historical evidence, not a runtime-source waiver. No original bytes were trimmed.

The map pins 29 exact or lossless gzip copies, including raw failures, commands, input manifests and source disposition. Receipt paths retain their execution-time layout; the map connects them to this package. Gzip files decompress to their recorded original bytes. No archive was expanded or media generated for this packaging.

Public P01 acceptance remains required. Root will integrate accepted main history, review any conflicts and qualify the exact final P02 source through hosted gates, production reproduction/readiness, build/freeze and later public verification. The focused tests and mocked utility requests do not establish live transfer, audible/native media, device, responsive, offline or public acceptance. Earlier P02 browser evidence remains pinned to its own source. No phase, version release or publication action follows from this package.
