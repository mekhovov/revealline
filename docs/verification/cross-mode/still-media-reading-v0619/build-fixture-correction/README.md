# Still Media v0.61.9: workshop fixture dependency correction

The first hosted source `d2c3fbf947f82c35fcb2e3adda8ee6b9df162878` added a reading-preferences script to Still Media HTML. Its miniature build fixture copied an explicit list of HTML-selected entries and recursively followed their JavaScript imports, but did not include the new script. Strict distribution validation correctly refused four cases because `game/ui/tool-display-entry.mjs` was missing from the fixture.

The correction adds that exact entry to `fieldKitFiles`; the existing copier follows its transitive imports. No runtime or build implementation changes, assertions, byte comparisons, URL expectations or validation rules are weakened. The runtime-maintainer skill records the dependency-list rule. The unpublished v0.61.9 allocation remains unchanged.

The original PR family `35317591343` finished with **5,884 / 5,888 passing tests and four failures**, all the described miniature-build cases. Preflight, ordinary build and the other three shards passed. Original manual run `35317589810` also recorded the four failures in shard 1; at the retained snapshot its final shard remained active. This packet does not invent complete manual totals or a frozen release. Its terminal originals are a separate follow-up record.

Both Node 20.19.5 and Node 22.22.2 pass the **four affected build cases and 101 reading/host cases**. Lint, formatting and diff checks pass. The full-release allowlist case was deliberately outside this bounded local run because it requires large optional pack bodies; it already passed in the original hosted shard. Node 20 reports that unmatched case as one skipped test, while Node 22 omits it. Neither is counted as executed coverage. The four retained cases verify original loose and ZIP bytes, same-edition Pages routing, and prepared-cache reads without a network.

Earlier local sparse-input failures are retained: missing multiline script imports, missing optional-pack inputs in the broader first local attempt, and an explicit playground file read. The exact tracked dependencies were hydrated without changing the fixture's success predicates; optional pack payloads were not downloaded or invented. These preparation failures are separate from the real hosted dependency-list failure. The peer reviewed the two source hunks and actual local receipts without rerunning tests or using a browser.

`originals.zip` preserves the failed hosted job, complete original PR logs, actual dispatch/source records, retained manual snapshot, local failed and passing command outputs, hydration manifests, source diff and peer review. Every member was reopened and rehashed against `originals-index.json`. Final successor source qualification, native/public checks and the wider P05/P18 gates remain pending; no earlier success qualifies a new commit.
