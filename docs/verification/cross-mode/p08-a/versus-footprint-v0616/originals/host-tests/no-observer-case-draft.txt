Proposed actual-host case (no worktree mutation while parent Node20 cohort runs):

- Extend local renderingBoundary option with resizeObserver:false, exposing undefined ResizeObserver so the existing helper installs no observer.
- Extend only the local fixture options with campaign and onMount. Select actual sentinel-relay.json campaign level, preserving fetched registries/presets/runtime.
- onMount models each arena clientHeight from its REAL racer-encounter-{seat}.dataset.phase: delay height300; warning height225/234. clientWidth600 both. Getter has a setter so the existing finite notify/bootstrap fixture remains compatible; no synthetic resize or observer notification during phase transition.
- Initial real host Start reaches delay phase and paints width400 for both4:3boards.
- Advance ordinary zero-input host frames to actualtick241 (historical encounter delay->warning); no direct mutation of run/encounter/DOMcue. Actual encounterView updates phase/title/instruction.
- Same changed frame must pass300/312 as displayCSSWidth, and actual BoardPainter body span remains>=16. The old fallback key omits cue text/state and retains400/400, producing RED.
- Both run refs/artwork refs retained; unchanged checkpoint through later zero-delta paints. Count arena geometry getters to ensure unchanged cue/pause/dimensions do not force reads on every frame.

This is a finite modeled layout boundary, not browser layout/raster proof. Existing four GREEN cases and source receipts remain untouched.
