# Team presentation — final local source hold

Base/HEAD `1da77ee586d0fdd3ac73883235a822b959e85b37`, isolated branch `codex/p08a-team-presentation`. No staging or commit. Current source authority: source-held-04.json; current execution: execution-cleanup-final.json and static-cleanup-final.json. Earlier README/verification/101 and105 logs remain historical, non-additive evidence.

Four files only: Team painter, injected read-only picture lease, and their two new complete test files. The existing Team host, strict game formats, simulation, registry/producer and art are unchanged. The picture reader has no default fetch, external URL, Solo storage or production binding. The painter keeps all old callers through explicit procedural fallback, shared snapshot/font/motion and semantic overlays; prepared images retain complete1152×576 nearest drawing and opaque black concealment. No Team actor art is adopted.

Final full focused boundary: seven complete files, 107/107 on Node22.22.2 (reported1.552165625s; process1.845814542s) and Node20.19.5 (reported1.536151125s; process1.823797042s). Zero failure/skip/cancel. Lint, format and diff checks pass. Four owned file hashes match before/after both processes. This is not full CI or a complete transitive import-graph identity proof.

Independent painter source review is closed (Dewey), and root reviewed both main flows. Parfit completed the full lease/test source review and final incremental closure at exact9f5be908/d50bd82a after two callback ownership fixes; no blocker remains in this scoped source review. The first was snapshot callback invalidation during the final identity check, now guarded after all injected reads and with an initial selection epoch. The second was old-image cleanup disposing a newly adopted image before return, now guarded after cleanup without calling another external reader. Ready callbacks run before adoption; cleanup invalidation afterward refuses return but does not pretend to roll back an already-released prior image. A cleanup that only throws does not undo the valid new adoption. Six added lifecycle cases retain prior101/105 success as predecessor scope.

Initial96/98 failures were new fixture omissions (neutral commands lacked boost/support; fake presentation host lacked apply), retained unchanged in initial-node22.log. No game failure, weakened deadline or suppressed test is claimed.

The future host must confirm the full current request before a fresh explicit Start; painter level ID/revision is only defensive and cannot replace full lease/hash plus attempt ownership. The image decoder is injected and owns its image/URL release; tests use a valid-CRC in-memory synthetic PNG and modeled decoded objects. Native decode, actual-size mask/actor/font/contrast/layout, keyboard/controller/touch in an integrated host, offline and release acceptance remain open. This is an initial P08-A preparation, not phase completion.
