# Team presentation preparation — local source hold

Base `1da77ee586d0fdd3ac73883235a822b959e85b37`; new isolated branch `codex/p08a-team-presentation`. Four owned source/test paths only. The completed static-picture worktree is unchanged. Team-I host/markup, strict level/pack formats, simulation, registry/producer, version, release and artwork remain unchanged.

The painter accepts the existing page snapshot interface, preserving font roles and explicit legacy procedural rendering. An injected image uses its complete 1152×576 frame, nearest sampling and opaque black hidden-cell concealment, with existing Team state cues above it. Its level ID/revision check is defensive; the lease verifies full canonical pack/level content and the future host must own current attempt/Start intent. No new detailed actor atlas or state animation is claimed.

`createCoopPresentation({bindings, getSnapshot, readPicture, decodeImage})` has no network or storage defaults. Binding rows are owner-supplied, validated metadata, not a signed publication or upload format. They pin pack and level IDs/revisions/canonical SHA-256, theme revision/collection and exact asset slot/revision/SHA/bytes/MIME/1152×576 full frame. A null picture is the explicit procedural policy; absent required bindings/images fail instead of substituting it. No production binding or source image is included.

`select({pack,levelId,themeId,attemptId,signal,onStatus})` captures validated request data and the exact snapshot before asynchronous work. Retry with the same attempt ID cannot adopt a changed pack or snapshot. Returned blobs must be native, bounded, byte/hash/header exact. The injected decoder returns `{image,release}` and owns native decoding/URLs; this module checks decoded dimensions and releases only its own candidates. Late/stale/cancel/dispose settlements cannot replace a newer accepted lease. Replacement retains the previous accepted image until the next validates. Ready display callbacks run and currentness is checked before the accepted swap/release, so callback cancellation/selection preserves the previous picture. Readers and status callbacks never authorize starting a game.

`confirm(request)` checks the full captured request and current snapshot without reselection. `current()` may retain the previous accepted picture while a new candidate prepares/fails; host integration must keep Start unavailable until confirm succeeds for the current operation. A returned binding is borrowed by the painter; only the owning lease disposes its decoded resource. Host wiring, imported-art envelope and registry allowlists remain later review boundaries.

Validation: seven complete files, 101/101 on Node22.22.2 (1.865467917s reported test duration; 2.188223417s observed process wall) and Node20.19.5 (1.008784667s reported; 1.051933083s observed wall). Four held authored source/test pins remained unchanged before/after each process; this is not a complete transitive source-identity proof or full CI. Lint/format/diff checks passed from the captured tool commands. `execution.json` pins both exact raw logs and argv. The earlier 96/98 log is retained: two new fixtures omitted required neutral boost/support fields and fake host.apply; neither is a game failure. They were corrected without weakening assertions or deadlines.

These tests include real Team core/host modules plus finite modeled Canvas/decode readers. Synthetic valid-CRC PNG bytes exist only in test memory. No native browser image decode, audible/media test, actual-size visual/contrast/layout, physical controller/touch, offline or public acceptance was performed. P08-A remains incomplete. Independent source review and root review precede any commit; this worktree is not staged or committed by this preparation.
