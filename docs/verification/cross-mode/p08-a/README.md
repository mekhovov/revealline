# P08-A static Solo / Versus picture parity candidate

This is local candidate evidence for the bounded behavior in [shared-static-pictures.md](../../../shared-static-pictures.md). It covers the existing fifteen static Versus maps and their 51 map/theme identities. It does not declare the whole P08 phase complete, approve new artwork, or qualify a packaged/public release. Team presentation and new content remain separate.

The source base is `f047a46a9a1bb9982600bd7e2682d765a75cb9cc`. The preview used four pinned runtime overlays: the Versus host and entry, the new static-picture reader, and the shared presentation-page lease. All other preview responses came from that exact base's Git objects. The native observations below preceded the later pending-focus correction. Their host SHA is `88f41d1d51ee9f30da6e868b7b971fc1cf84be009df83ce786d3a7ce84e37fc0`; they must not be relabelled as a native retest of the successor.

## Actual native observations before the focus correction

Root observed the following in the local browser on 2026-09-15, from 16:58:52Z to 17:03:16Z. [All 23 events](native-before-focus/events.json), [HTTP/source and screenshot pins](native-before-focus/native-root-verification.json), and [two exact PNG response checks](native-before-focus/native-root-picture-bytes.json) are retained without transformation.

- Both Orchard Crossing boards displayed the same wide artwork after actual cuts (1.5%, 340 points). Pause and explicit Resume retained the cuts. See [Orchard](native-before-focus/orchard-cut.jpg).
- An actual First Signal Versus win reached 52.2% and 8,160 points. Its earned full picture matched the picture shown after an actual Solo win at 50.0% and 7,820 points. The different scores and coverage came from separate playthroughs; this is picture parity, not equal simulation or timing evidence. See [Versus](native-before-focus/first-signal-earned-versus.jpg) and [Solo](native-before-focus/first-signal-earned-solo.jpg).
- Explicit Next first prepared the picture, then resumed fresh boards while retaining the 1:0 series score. The raw event record preserves two corrected intent labels: two Return presses had reopened the viewer, and an initial Next snapshot was still preparing. See [fresh next round](native-before-focus/first-signal-next-round.jpg).
- After reload, keyboard Enter on Cancel stopped real picture preparation, focused Retry and left Start disabled. Cancellation remained after other artwork/fonts settled. Explicit Retry subsequently became ready, focused Start, and required a separate Start action; both Orchard boards then showed the matching picture with a fresh 0:0 series. See [cancelled](native-before-focus/cancelled-picture.jpg) and [retried](native-before-focus/retried-orchard.jpg).

The historical pending-Retry record combines an accessibility snapshot showing Start disabled with a DOM-focus read showing Start active. Those reads occurred in separate awaits and may straddle readiness; they do not establish a native disabled-focus bug. No trap or automatic Start was demonstrated. Independent source review identified a concrete gap: Retry hid/disabled its focused button without transferring focus to the newly visible Cancel. The successor adds an operation-owned handoff for Retry and asynchronous Start confirmation. Deliberate focus movement, foreground loss, scope changes and stale operations relinquish completion focus. The scoped successor retest is recorded separately below.

The two originals are unchanged reviewed source art: First Signal SHA `c1aedf89bc3433dc1cb60998fa1e2563480a590c38586332f444c4c12c772fce` (40,810 bytes, 768×576) and Orchard Crossing SHA `53f1206a11a8791892f5c844c0641529acbc2c09c8d558676d0d801d72113850` (52,720 bytes, 1152×576). [Exact owners and existing review provenance](native-before-focus/native-picture-identities.json) distinguish identity and prior asset review from this gameplay observation.

## Automated evidence

Before the focus correction, nine complete focused files passed 109/109 on Node 22.22.2 (164.783 seconds wall time) and Node 20.19.5 (161.661 seconds). [The receipt](automated/complete-results.json) records exact commands, raw log hashes and unchanged direct input pins. These counts include the reader's unit cases; they must not be added again. They are not full CI or a complete import-graph identity proof.

The prior [108/109 diagnostic](automated/final-results.json) is retained: an installed-picture fixture awaited its intentionally pending decode before inspecting loading state. The fixture now observes actual decoder entry, cancels, then joins the operation, preserving its assertions and deadlines. Earlier sparse fixture hydration and development diagnostics remain in the owned cache; none establishes native failure or passing evidence.

For the focus successor, five held-decoder journeys reproduced the absent pending handoff on the old source. The corrected five journeys and the strengthened Start → Cancel → Retry late-old-confirmation case passed together on Node 22. [Baseline](automated/pending-focus-baseline-node22.log) and [targeted result](automated/pending-focus-targeted-node22.log) remain separate from the required fresh whole-file pair.

The intermediate focus successor passed **114/114 on both Node 22.22.2 (168.495 seconds) and Node 20.19.5 (165.460 seconds)** across the same nine complete files, without filtering. [Fresh whole-file receipt](automated/focus-complete-results.json) records unchanged direct input pins and both raw log hashes. [Static checks](automated/focus-static-results.json) passed lint, formatting and whitespace checks. Its predecessor hold is [recorded separately](automated/held-source-focus.json); the later Start-intent fix is qualified by its own whole-file runs below.

A later source review found a separate Start-intent lifecycle gap: a ready run could still begin when an old confirmation or Next preparation resolved after blur and return. Two actual-host regressions reproduce `running` where `ready` was required ([retained baseline](automated/start-intent-baseline-state-node22.log)); all eight targeted cases passed after correction ([targeted result](automated/start-intent-targeted-node22.log)). The latest successor expires the Start/Next intent through the existing pause/suspend/controller-loss paths, checks it after both asynchronous boundaries and immediately before starting, and preserves completed picture readiness for a new gesture. This is source/model evidence, not a newly observed native bug. The earlier 114-case pair remains valid only for its recorded predecessor.

The final Start-intent successor passed **116/116 on Node 22.22.2 (159.888 seconds) and Node 20.19.5 (162.951 seconds)** across those nine whole files, without filtering. [The final receipt](automated/intent-complete-results.json) pins the commands, raw logs and unchanged direct inputs. [Final static checks](automated/intent-static-results.json) passed; [independent source review](automated/peer-start-intent.json) closed without a blocker against [the functional hold](automated/held-source-start-intent.json). The current Versus host SHA is `01adc74637025110ba2cff8867ccf1966c008023b0961070ba768dcffb0da56d`. The native successor retest below remains separate from the historical picture observations above.

The host tests use actual input/navigation/duel code with finite DOM, Image, URL and IndexedDB boundaries. They assert exact identity/bytes, both-board object/fit sharing, unchanged attempts and writes, failure/refusal, stale completion, cancellation and resource ownership. They do not establish browser decoding, storage quota, visual contrast or physical-device behavior.

## Native successor: keyboard preparation and focus

Root checked the final host `01adc746…` on 2026-09-15. [The source/HTTP and native receipt](native-successor/native-intent-verification.json) pins all four runtime overlays and [eleven retained observations](native-successor/events.json). The normally served preview reached readiness, accepted explicit Start and paused on Escape. Two earlier attempts to Cancel missed that window: one had no matching action after readiness; another browser bridge call timed out and the next actual state was ready. These attempted actions are retained and do not establish a runtime failure.

For a repeatable pending window, the [separate server fixture](native-successor/delayed-preview-preparation.json) delayed only the two allowlisted original PNG responses by eight seconds. The [binding](native-successor/preview-intent-delayed-binding.json) pins unchanged source and original SHA/byte counts. All other responses used the same normal routing. No application state was injected and no picture bytes were changed; this is a timing fixture, not a measurement of production loading speed.

With that fixture, real keyboard Cancel focused Retry and left Start disabled. Retry transferred focus to visible Cancel while downloading, then focused Start once ready without beginning the race. A second Retry followed by deliberate Tab to Race setup preserved that focus through completion. Explicit Start entered the race canvas, and Escape paused both boards. See [pending Cancel focus](native-successor/retry-pending.jpg) and [paused after explicit Start](native-successor/retry-start-pause.jpg).

The browser did not reproduce a held confirmation/Next promise across blur and return; those two lifecycle checks remain the actual-host automated evidence in the 116-case suite. Accessibility snapshots and DOM reads were separately awaited. This native supplement does not extend the earlier artwork comparison into all 51 visual combinations, manual assignment or saved-pin tests. Disabled primary-button styling remains a separate P05 observation. The copied server sources preserve their original cache layout for provenance and are not relocatable launchers.

## Remaining acceptance and evidence layout

No physical controller/touch hardware, narrow/short viewport matrix, storage quota/failure, explicit user assignment A→B, older saved/first-earned A, packaged/offline/public release, or full bright/dark/partially revealed contrast audit is claimed by the native observations here. The guide maps primary guidance on nearest sampling, owned URL disposal and meaningful non-text contrast to remaining acceptance checks.

[Copied-evidence mapping](copied-evidence.json) pins each original cache file and its retained copy. Native files and receipts are byte-identical. Three retained failing-test logs have trailing whitespace removed and one final newline; the mapping records that transformation and both original and retained hashes. Original cache logs are unchanged. Original relative layout strings inside raw receipts are preserved as provenance; the mapping supplies the committed relative locations. Screenshots are original root-owned JPEGs; no image resizing, editing or media-generation step was used for this evidence package.
