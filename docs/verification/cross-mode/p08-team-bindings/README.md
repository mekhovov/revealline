# Team picture bindings checkpoint

This internal source checkpoint starts from static composition `1817377c00049874eb8ab3f47f0b630894ba28f8`. It retains the exact reviewed painter, lease and two test files from `e5b2b91e480b235a6c24ba1fd44ba6e42415c743`, together with all 23 historical evidence files in [Team presentation](../p08-a/team-presentation/README.md). That earlier 107-case pair remains evidence for its earlier source and is not relabeled as current host adoption.

The new [binding guide](../../../team-picture-bindings.md) describes the closed two-row authority. The table binds the canonical starter pack and both authored levels to the approved Orchard and Foundry derivatives, `fpv@19`, collection `null`, and their exact asset IDs/revisions/file tuples/full frames. These are parity reuses without new artwork or mission counts. Changed same-ID imports, including a change in the other level, fail before any reader or decoder. The lease continues to enforce complete-frame contain/nearest output and independent resource release.

Root and an independent peer reviewed the exact new table and all 22 tests. The peer separately reconstructed canonical pack/level metadata from inspected Git source and matched the actual compiled manifest. Source hold `324a8b2dc53e245bf9f91c2b77577a2bac6ed180ab1a1df95dc7deb95905fa02` remains unchanged. Eight current Team/Versus host, markup, style, compiled metadata and page-adapter inputs remain byte-equal to the baseline.

Nine complete files passed on each supported runtime: the new binding tests, existing lease/painter tests, Team host/core/library/authoring/balanced-route tests, and presentation-page tests. Node22.22.2 passed 195/195 in 14.371 seconds; Node20.19.5 passed 195/195 in 21.472 seconds. Both report zero failures, cancellations, skips or todos, with all 350 finite inputs unchanged. Formatting and lint passed on the six source/test modules and formatting passed on the guide.

The initial Node22 command recorded 125/195 passes. All 70 failures were the same missing `display-preferences.mjs` dependency reached by the actual Team host's template-string dynamic import. The exact 7,319-byte baseline module was restored; no runtime or test was changed. The complete Node22 rerun had already launched when root narrowed the requested repeat to the affected host file. Its actual complete result is retained honestly, followed by the complete Node20 run. The initial log, source-input list, runner and exact dependency receipt remain in the archive; predecessor counts are not added to the final result. The final finite-input review also compared display/text/audio dependencies with the later P05-B/static composition manifest; its separate menu modules were not adopted here.

The [map](evidence.json) binds 21 unique [archive](evidence.zip) members to their originals. ZIP compression preserves every decoded byte, including diagnostic logs and source-diff whitespace. The historical `e5b2b91` evidence remains separate and unchanged. No artwork body is duplicated in the evidence archive.

This checkpoint has no Team picture host wiring, HTML/CSS changes or compiled-registry edits. The tests verify the actual approved PNG bytes and headers through a modeled decoder/release handle; they do not prove browser decoding or Team overlay readability. The current bindings are specifically `fpv@19`/null from `1817377`; any newer accepted presentation revision must be reread and explicitly qualified, never admitted by weakening the match. Later host adoption must preserve current departure/display/menu lifetimes and independently prepare each candidate core and image before replacing the accepted attempt. Native Team reveal/cues, host Cancel/Retry/focus/foreground behavior, offline/public/build gates and physical devices remain open. Neither P08 nor any public phase or release is accepted here.
