# Team picture, lobby and recovery composition

Source candidate for the queued P08-A/P03 feature. This packet does not certify a release, complete map parity or fresh native qualification.

- Base: `d2455ebfa444a8692d2694790afa6e4199af64e0`, v0.61.1.
- Isolated worktree: `../worktrees/p08a-team-composed-d245`.
- Branch: `codex/p08a-team-composed-v0612`.
- Proposed next feature version: **0.61.2**. Remote tag and release returned 404 and no open PR claimed this version at intake. Version files remain 0.61.1 pending coordinator review; recheck allocation before committing.
- Source patch: [composed-d245.patch](composed-d245.patch), 15 paths. [Source manifest](source-manifest.json) binds exact preimages and composed bytes.

## Behavior and composition

The Team lobby borrows the current prepared picture and shows a concealed border teaser. Winning reveals the accepted complete picture without changing captured cells, time, reserves, scoring or image ownership. The existing results dialog remains above the canvas; a separate unobstructed picture viewer is not introduced.

Returning from results or a confirmed Change setup keeps the accepted picture and shows the current primary action. Picture preparation failures show clear player-facing recovery copy. Direct Cancel/Retry focus and reveal remain bound to the current selection, attempt, Settings visit and foreground; newer intent vetoes them.

The four runtime files exactly preserve the already reviewed 628-based candidate bodies. Thirteen non-shared paths were applied against identical d245 preimages or absent new files. The two shared prompt/skill documents retain the complete d245 bodies as exact prefixes, then append Team guidance. Only current composition descriptions were updated from the predecessor to d245/FPV28; historical observations remain dated and unchanged.

The d245 simulation, content, media pins, artwork, compiled presentation and both strict FPV28 Team bindings remain unchanged. The [independent source review](source-review.md) and [production review](production-review.md) record their scopes and exact pins. The maintained producer does not fingerprint the four Team host files: unchanged recipe approval is not approval of the new Team interactions or native rendering. Exact-source release qualification must still run ordinary production reproduction/readiness and confirm an empty production delta.

## Verification records

The bounded runner executes **23 whole test files** on Node 20.19.5 and 22.22.2, with one file worker, a 1 GiB V8 old-space cap, a five-minute deadline, a 4 MiB output guard and a 512 MiB free-disk reserve. It pins every materialized source input before and after each run. There is no case-name filtering, full build or dependency installation.

- [Cohort](cohort.json) and [runner](run-cohort.py).
- Input stages are distinct: top-level `test-inputs.json` and the count fields retained in `cohort.json` describe the initial 275-input / 16,438,575-byte setup. The cohort's file list was then expanded to 23 whole files; the first attempted run pinned 284 inputs / 16,561,260 bytes. Restoring the six missing committed fixtures produced the identical 290-input / 16,572,322-byte maps in both passing runs. Their per-run `inputs.json` files are the executed-source authority. [Input-stage record](input-stages.json) preserves these boundaries without rewriting earlier records.
- [Initial Node20 record](runs/composed-node20/receipt.json): 371/373 passed. Two ENOENT failures exposed omitted sparse-workspace dependencies, not changed product expectations.
- [Fixture correction](fixture-correction.json): restores the five exact committed actor PNGs and Replay entry HTML. No runtime or assertion changes.
- Corrected complete-family records pass **373/373 on Node 20.19.5** in 114.920 seconds and **373/373 on Node 22.22.2** in 94.960 seconds. Both run all 23 files with zero failed, skipped or cancelled tests. Their identical 290 source inputs, 16,572,322 bytes, remained unchanged. See `runs/composed-node20-r2/` and `runs/composed-node22/`; the initial fixture failures above remain retained.
- [Scoped lint, formatting and diff checks](static/receipt.json) passed for the composed 15 paths.

## Original evidence

[Originals manifest](originals-manifest.json) maps 213 literal predecessor files, 4,126,491 bytes, to their retained copies. [Originals readme](originals-readme.md) separates source stages, failed attempts, native observations and relocation. Original absolute paths and source identities are not rewritten.

The Relay Yard predecessor recorded a legal stronghold win at 18% and Retry. First Connection recorded partial captures, losses and Retry before browser disconnection; no native First Connection win or post-victory Retry is established. CUA observations are scoped transcriptions with inline visual observations, not retained screenshot exports or physical-device certification. Prior native results do not qualify this new d245 composition.

## Remaining gates and adoption

Before commitment, review the complete source hunks, provenance, both corrected test receipts and unchanged production authority. Stage only the explicit related paths and adopted evidence. Synchronize package, lockfile and build version together after confirming the next version remains unused. The local `node_modules` symlink is test infrastructure, never a source change.

Fresh combined-source browser checks remain required, including both arenas, concealment/full-picture transitions, lobby return, loading/failure/Cancel/Retry, current Settings ownership and responsive control placement. First Connection native victory → full picture → retained-picture Retry remains open. Physical controllers/touch, broad accessibility, imports, offline and whole-phase coverage remain distinct gates.

Then follow the existing exact committed-source six gates, build/production/artifact checks, immutable freeze, coordinated PR/tag/Pages workflow and public identity/byte/ordinary-play acceptance. No version, staging, commit, push, release mutation or new browser recovery is performed by this packet.

Total local growth is bounded to 64 MiB across the worktree, packet and worktree administration, while preserving at least 512 MiB free. Original workspace and unrelated candidates are untouched.
