# Existing Team arenas: concealed lobby preview and earned full picture

This source candidate is composed in the isolated `codex/p08a-team-earned-picture` worktree on corrected v0.60.9 `628e95daf403082768cdcf900a8ea1d1ef4629a2` (tree `304acdf23c8ef9c465e61e8338b43a69aebfdca5`). It has twelve unstaged paths and an empty index. No version, commit, remote, browser, artwork or strict level format was changed. P03, P05 and P08-A remain incomplete.

The lobby borrows the selected arena's already prepared image. It exposes only its narrow border around an opaque black centre, says that winning reveals the picture, and adds no interactive focus stop. Selection changes clear old pixels; existing preparation owns status, Cancel, image Retry and Start. A preview-only drawing failure offers Retry preview without rereading or disposing the accepted original and without blocking valid Start. Procedural imports remain explicitly labelled.

After a legal win in First Connection or Relay Yard, the painter draws the exact accepted image in full and retires live concealment, walls, actors and hazard cues from that paint. It preserves the actual captured cells, recorded coverage and all simulation/reward state. Results still covers part of the canvas; an unobstructed picture viewer, reveal animation, stories and expanded Team encounters are separate work.

## Composition and ownership

The existing eleven-path proposal is retained at `../p08a-team-teaser-40fe4a31/`. Its patch is `a8eef16815ccff93eaae31b4466bf4d0a04316b76c0368b4cb41ee66cc945751`. Runtime/test hunks were applied selectively; additive skill and prompt sections were appended to the complete current parent bodies. No older whole host, stylesheet or guidance file replaced its newer counterpart.

`composition-review.json` binds all twelve before/after identities, unchanged contracts and approved images. It confirms the explicit fpv27 picture table, compiled snapshot, core, recipes, input policy and version files are unchanged. Both image bodies retain 1152×576 dimensions and their exact existing hashes (`53f1206a…` and `d76f309d…`). The helper retains current `modelTeamDialogs()` behavior. Preview and arena paint recorders stay separate, so a teaser cannot satisfy the no-arena-before-Start check. All 188 relative documentation targets resolve in the composed source or exact parent tree.

The new integration cases prove that a held original can finish behind the current Settings selector without closing Settings, moving focus or starting play. The actual-host terminal file now uses `coverageClear` for First Connection and `yardOpening` for Relay Yard under Standard / Full teamwork. Both win through public keyboard commands, then Retry the accepted recipe and image despite changed hidden setup values. They preserve three reserves; Yard reaches its authored stronghold goal at 30.7%, rather than inventing a 100% capture.

## Retained verification

`cohort.json`, `candidate-inputs.composed.json` and `run-composed.py` identify twelve complete test files and 270 exact inputs (16,339,920 bytes). Runs use Node 20.19.5 and 22.22.2 sequentially, 1024 MiB V8 old-space, test concurrency one and a 420-second bound. Final actual totals are recorded in `qualification.json` and the original `runs/composed-node20` / `runs/composed-node22` receipts. There is no broad test suite, build, image regeneration or install.

The initial complete Node 20 run is preserved at `runs/initial-node20`: 225/231 passed. Five failures came from two omitted tracked catalog JSON files in the sparse test closure; `settings-catalog-closure.json` records their exact Git identities. The Yard test initially sent movement before Start's mandatory neutral release tick. The corrected test lets the real host initialize its clock and consume that release before fresh input. `initial-won-terminal-host.test.mjs` and the failing output remain unchanged; the complete four-case correction run is retained at `runs/host-neutral-start-node20`. No runtime/core change resolved that setup issue.

Formatting, JavaScript syntax, Markdown parsing, the empty-index check, `git diff --check` and reverse patch applicability are recorded separately. An initial Markdown command used a misspelled local tool path; its failure and the corrected invocation are both retained. Historical renderer mask failures and the old 195-case teaser pair remain evidence for their original sources only; their counts are not added to this composition.

## Remaining acceptance

A browser must review the actual concealed preview at ordinary, portrait and short-landscape sizes, including Theme/Plain and Large text. Check selection, held/failed/cancelled image preparation, Settings focus, explicit Start, a legal earned win and Retry in each existing arena. Confirm real pixels use the complete approved image without arena cues after winning, and the new run is concealed again. Preserve old Results behavior and scope any remaining unobstructed viewer separately.

The finite DOM/Image/Canvas tests do not establish browser rasterization, layout, screen-reader speech, physical controller/touch operation, human difficulty or public/offline behavior. The rehearsed two-seat command routes are not records of human wins. Commit, integrated source qualification, immutable release and public deployment acceptance remain root-owned later gates.
