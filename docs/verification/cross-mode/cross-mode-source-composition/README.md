# Internal cross-mode source composition

This checkpoint combines the full P03, P05/static-Couch, resize and Team-host candidates, then fixes Collection Flight records return after imported content grows. It is not an eight-file-only commit. The [lossless archive](evidence.zip) and [original-byte map](evidence.json) retain 154 records, including failed runs and native observations. No version, release, publication selector or phase acceptance changes here.

The final commit must retain these four original parents directly:

| Role              | Commit                                     |
| ----------------- | ------------------------------------------ |
| P03 final         | `29a98c0e2984b8c3cca7079f40047465e9bf767b` |
| P05/static-Couch  | `90e8bc8102cc71a992a57aa1f41d7cdcd6659b3e` |
| Couch resize      | `657ac7b5528925a8c6bee0f77449a1428962f172` |
| Team picture host | `d3ff0bc1e9948f5752e79c5b21c8207dd74c69e1` |

Their common base is `3acce1941116d18f9e86dcfa26dea463433cd3b2`. The reviewed combined predecessor tree is `def1186e23ca8ce668b362c0b001f2b039eb76e5`; the tested/native successor is `ebcf47cc56bd7c620cb74f2fa2ea9eb3a6b38e9b`. Final packaging changes only documentation and evidence beyond that successor. Temporary tree objects are source bindings, not independently released checkpoints.

The original composition preserved inherited runtime bodies and combined three guidance files. Its first qualification exposed omitted finite inputs and an obsolete one-level departure fixture retaining unrelated briefs. Exact inputs and `campaign.briefs = []` corrected those test boundaries. Initial diagnostics and both source holds remain archived. The 39 complete files then passed **760/760 on each Node version**, with 1,001 physical inputs and index unchanged. That is the predecessor's scope; do not add it to later overlapping counts or describe it as a rerun after the return fix.

The [Collection successor](../../../collection-records-return.md) changes three production files: the host injects its existing progress presenter, Library owns refresh/reveal of the open Collection, and the surface adapter removes its synthetic Collection entry click. Only the actual restored Flight records control can own reveal after final population. Newer intent, visits, models and populations retire it. Pager/picture owners, schemas, original-image disposal and simulation remain intact.

| Successor qualification               | Node 22.22.2 | Node 20.19.5 |
| ------------------------------------- | ------------ | ------------ |
| Eight complete focus/Collection files | 177/177      | 177/177      |
| Separate complete Field Kit file      | 7/7          | 7/7          |

Each cohort has its own before/after input authority and command. The first new 17-case diagnostic passed 15 cases; missing fixture title, storage-envelope access and the retained disabled-card expectation were corrected without runtime changes. Its exact initial test body is reconstructed and SHA-verified. A later eight-file attempt lost disk space and ended without a final TAP summary; it is incomplete evidence, not a product failure or pass. The fresh full pair passed with all 987 inputs unchanged. The initial Field Kit file passed six behavior cases but missed its dynamically read 357-byte archive index; its full corrected pair passed with 542 inputs unchanged. No build ran. Exact owned fixtures were materialized/dematerialized between completed cohorts to remain below 100 MiB; receipts preserve those boundaries. Lint, formatting and diff checks passed.

Root inspected the scoped native evidence:

- Predecessor normal `64449`: 14 events, nine JPEGs. Actual Versus clear/View/return and Team capture/Help/explicit Resume/return passed. The first imported-growth return left the focused Records button below the viewport; a later already-populated revisit did not resolve that finding.
- Predecessor fresh `49355`: three events, two JPEGs. Empty → actual chooser import of 25 legal fixture completions → Escape + 600 ms confirmed the defect: Records top 1849.8004 and bottom 1893.7935, viewport height 1348, Collection scrollTop 0.
- Successor normal `54111`: four events, two JPEGs. At actual 1454×1348 and DPR 1.8, the same imported-growth return leaves Records top 1287.578 and bottom 1331.571, scrollTop 562.222; all five interior hit samples pass. Reopen/close keeps the same visible opener. No browser errors were reported. All 27 before/after HTTP and local bodies match the successor tree.

Chooser activation used a pointer. Fixture records come from legal traces, not human-earned victories. Native screenshots/geometry do not prove authoritative checkpoint equality; that is host-test evidence. The native binding explicitly admitted the already passed focus pair while Field Kit was pending. Its immutable receipt and root review retain that historical wording; the later full Field Kit pair closes only that separate test gate. The archive's `tested-inputs` guidance bodies likewise precede this documentation-only packaging.

Remaining work is concrete: compose the separately qualified Team HUD (`61fb43`) and actors (`d30f883`), and the separate P07 retained-Results/Next transaction; retain the P01/P02 integration/rebinding and exact final CI/build/public gates. Team full win/rescue, remaining theme/import-art policy and earned-original replay fidelity are not completed here. P05 still needs actual browser zoom/system-motion/storage-event and physical-device qualification. These desktop journeys do not certify every responsive state, input device, audible audio or a whole P03/P05/P08 phase.
