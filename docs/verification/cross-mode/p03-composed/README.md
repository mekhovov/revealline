# Composed P03 navigation: scoped source and browser verification

This internal qualification covers source `d0925c00518f0be6da0a3ea565112fee106dc8eb`, tree `02342b700c7d9fe7f2f1145161b66d7c8c04b62c`. It includes Title Start/Continue, three-mode entry, checked departures/replacements, shared Couch navigation and the completed-operation focus correction. It does not include the later recreated Library-row correction, P05/P08 presentation composition or the newest P01 release correction. No version or phase acceptance is established.

The [evidence index](evidence.json) maps 84 original files, 5,248,649 bytes, to an exact lossless [archive](evidence.zip). It retains commands, runtime/source/input hashes, failed predecessors, final logs, preview bindings and original screenshots. Every extracted member was checked against its original bytes and hash. Historical instructions and receipts inside the archive remain scoped to their stated source.

| Run                                           | Complete-file result                       | Interpretation                                                                                                       |
| --------------------------------------------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- |
| Earlier `3acce194`, Node 22                   | 737/740 in 34 files                        | Three obsolete host assertions were corrected in `d0925c0`; the original failure remains retained.                   |
| `d0925c0`, Node 22                            | 765/767 in 35 files                        | The two failures require a missing dynamically fetched Fieldcraft fixture; the other 34 files passed all 740 checks. |
| `d0925c0`, Node 22 after exact fixture intake | 27/27 in the complete operation-focus file | Restored the committed 15,204-byte Fieldcraft JSON. No product or test source changed.                               |
| `d0925c0`, Node 20 after intake               | 767/767 in all 35 files                    | One complete command; no failures, cancellations, skips or todos.                                                    |

Node 22 therefore has passing complete-file coverage for the same 767 checks, not a single passing 35-file invocation. The final Node 22 command took 10.18 seconds and Node 20 took 512.34 seconds. All 284 admitted inputs, 15,600,516 bytes, and both runtime binaries remained unchanged. Overlapping historical checks are not extra tests. The final release still requires the full hosted suite and all other source gates on the final integrated source.

The first final-source runner refused before testing because raw Git index bytes differed. The corrected harness checks canonical staged path/mode/blob/stage identities, clean tracked source, exact HEAD/tree, physical inputs and its own authority. Raw index hashes remain diagnostics; no writer was identified. The initial refusal, probe and both harness versions are preserved. This changes qualification bookkeeping, not game behavior or tests.

Root's earlier desktop keyboard journey on exact predecessor `3acce194` contains 47 events and eight PNGs at 1309×1348. Its ordinary origin serves exact Git source; the second delays only the campaign response by six seconds. It observes Title-to-Couch journeys, Stay/discard, exact Missions selection return, installed Fieldcraft Play, an actual unfinished cut retained through Team departure and explicitly continued, nested Library replacement and deliberate focus during loading. No application state or simulation time was injected.

That journey exposed chapter/Install focus loss. The subsequent [operation-focus correction](../p03-operation-focus/README.md) supplies the separate final nine-event/three-PNG native check. The earlier review remains an honest partial result. Events 1–3 include an accidental public Play online link during boot; local testing resumes at event 4. Event 42's premature replacement label is corrected by events 43–44. The paused screenshot alone is not trail proof; the live-cut and continued screenshots supply that observation. An unrelated 48th append was retained as `events-misbound-append.json`; `events.json` is the restored original 47-event record.

Remaining P03 work includes recreated installed-pack row focus, Score/Collection paging focus, final source composition, complete responsive/input journeys and public/offline acceptance. Gallery paging may await media metadata, so it must preserve newer focus and foreground decisions as well as a useful logical action. Physical controller and touch evidence, mobile/Safari/zoom and human playtesting are separate from these source and desktop checks. P05 and P08 retain their own acceptance gates.
