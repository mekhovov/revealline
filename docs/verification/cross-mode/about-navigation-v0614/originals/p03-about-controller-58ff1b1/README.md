# Home → About navigation correction

Source frozen on `58ff1b1da3c5487412e168ce34539866ac2f4970`, branch `codex/p03-about-controller-v0614`. This packet composes retained Home→About R2 with the current Enemy/Team source; versions remain 0.61.3 for parent-owned v0.61.4 adoption. No further source edits are planned.

`candidate.patch` contains all 14 related source paths, including all three new modules/tests. `candidate-pins.json` records exact base and candidate bodies; `final-source-inputs.json` pins 287 ordinary checked-out files (5,085,484 bytes). The full patch reverse check and whitespace check passed without staging. Existing app Next/Retry behavior, prior skill guidance, strict archived-play validator, shared resolver, compiled assets/producer inputs and version files were preserved.

The About owner starts before catalogue waits. Controller disclosure/select behavior uses the shared router/navigation, with explicit same-edition Return, native-editor Escape ownership, neutral restoration and terminal cleanup. Late catalogue completion cannot steal focus or revive a departed owner. Draft text has a local themed full-row live preview and is not repeated in the page status. The archive JSON override is restricted to this project's exact HTTPS numbered immutable About route; custom/source routes and same-edition Return remain intact.

Complete eight-file cohorts passed **199/199 on Node 20.19.5 and 199/199 on Node 22.22.2**, one worker each, below 120 seconds and 1 MiB output. Scoped lint and formatting passed. All 287 source inputs stayed unchanged across final cohorts and quality checks. The existing `site` build include covers the new module; only finite miniature builder/current-entry fixtures ran.

The earlier `initial-node20` sparse-fixture failure and `final-node20` finite-DOM compound-selector failure are retained, not accepted. The latter does not prove a native duplicate announcement; an equivalent explicit role check makes the intent independently testable, with the regression assertion unchanged. Full corrected reruns are `corrected-node20` and `corrected-node22`.

Original R2 patch remains 35,243 bytes, SHA-256 `adca136dd39ee6caf542bc9b7fa91da88287c210b91326ca9f872a78527fcf9b`; all twelve immutable proposal bodies still match their original manifest. Historical evidence is parent-owned under `docs/verification/cross-mode/about-navigation-v0614/originals`; its intake and unchanged producer fingerprints are referenced in `qualification.json`.

Parent next steps: review this exact candidate; reserve/bump v0.61.4 and requalify the actual versioned source; stage only related paths/evidence, commit, then run all release gates, ordinary build, native viewport/input checks, freeze/deploy and public/offline verification. Native SELECT dismissal, actual screen-reader announcements, physical controllers and compact Standard/Large layouts remain separate evidence gates. This packet is source qualification, not public-release acceptance.
