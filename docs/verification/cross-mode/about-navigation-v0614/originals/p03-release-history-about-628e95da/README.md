# Release history in About — R1 (native blocked)

Exact base: `628e95daf403082768cdcf900a8ea1d1ef4629a2`. Twelve finite source/test/guidance paths; no staged files, commits, version changes, builds, fetches or publishing.

Home keeps gameplay destinations and sound. Existing auxiliary links lead to same-edition About/build information. About initializes the shared release history URL before its catalog await, retains explicit current/archive actions, and preserves the strict archived-play validator and canonical HTTPS destinations. A scoped hidden rule prevents an incorrect pre-module link from being displayed.

Source qualification: 26 actual-host/URL tests and five current-entry/source-link tests pass. `checks-final.json` binds scoped format, lint, syntax and diff checks. The large maintainer skill is insertion-only; its unchanged pre-existing bytes were preserved. `source-pins.json` records 277 exact Git source bodies. Forty-two temporary source-link files were admitted, verified and removed from this owned sparse checkout; `temporary-dehydration.json` records unchanged candidate status and empty index. Peak allocated footprint during this step was 7,118,848 bytes, below the 7 MiB source allowance.

Native acceptance is **blocked**. Parent's ordinary keyboard flow focused Release history at 844×390; rotating to 390×844 moved that focus below the viewport because the long About material above it reflowed. R2 moves the existing disclosure before that content without a new focus/scroll owner. This packet and its proposal/preimage bodies remain immutable. Native evidence belongs to the parent and does not imply public catalog closure or physical-device qualification.

`runtime-pins-r2.json` names the final R1 runtime. The earlier `runtime-pins.json` is superseded: its game HTML pin preceded removal of a stray text character; other five runtime bodies were unchanged. Final source tests are bound to the corrected HTML. Earlier fixture-prerequisite failures and the older source-green receipt remain retained and are not promoted to final qualification.
