# About navigation — v0.61.4 candidate evidence

Home retains its game destinations and sound control; software history is reached through About's Build information. About now owns controller input before optional catalogue waits, offers an explicit Return to the same game version, and preserves native keyboard/touch selector behavior. Known numbered RevealLine archive pages use the main release catalogue without changing their Return destination. Choice previews use shared style tokens and one live announcement.

The feature is composed on `58ff1b1da3c5487412e168ce34539866ac2f4970`. Fourteen source paths implement the correction; package, lockfile and build versions are synchronized separately to 0.61.4. No simulation, compiled artwork, strict archived-play validator, shared game resolver or shared production recipe input changes.

`intake.json` binds every copied original body and mode. Historical R1/R2 source and browser packets are retained exactly, including failed placement/capacity observations and recovery. Their native observations concern predecessor source 628e95da and do not qualify this controller owner, archive routing, or v0.61.4. The implementation packet retains the initial sparse-bootstrap failure and later helper-selector boundary failure alongside corrected results; neither is silently discarded or called a native browser defect.

The final pre-version eight-file cohort passed 199/199 on Node 20.19.5 and Node 22.22.2, with scoped lint/format checks. Independent source review passes. `versioned-source/` records another complete 199/199 run on each Node version after the three version-file edits, their formatting check, exact 287-file input map and preserved producer identities. These are scoped, uncommitted-source results. Full exact-commit gates, production/build/freeze and public qualification follow the commit through the release coordinator.

Native qualification must exercise Home → Workshop → About, source/main-frozen/numbered-archive routes, controller disclosure/select/Back/Return, native selector Escape, changed options, held and failed catalogues, disconnect/reconnect, blur/visibility/cached return, and terminal departure. Inspect the controller preview in the header, preserved-build row and pack grid at desktop, handheld, portrait and short landscape sizes with Standard/Large text. Verify explicit links, focus visibility, one status announcement, public source/bytes and relevant offline behavior. Modeled DOM/hardware tests do not establish physical-device, native-popup, actual screen-reader or full P03/P16/P18 acceptance.

The current accessibility sources and their application are recorded in `originals/p03-about-controller-58ff1b1/navigation-guidance-review.md`. Maintenance prompts are included in the changed runtime skill and field-kit surfaces guide.
