# P03 integration supplement

Packaged original evidence; independent review and P03 acceptance remain separate.

P03 remains **In progress**. This supplement preserves original evidence from specific working-file cohorts. It is not a final-source, feature, public-release or phase acceptance record. No live worktree snapshot is created by the packager; original before/after records retain their own HEAD observations and working-byte identities.

The earlier integration archive remains separate: 917,952 bytes, SHA-256 `4658460bba55658c58a617b63fe1db75cea1f1562c8d50379b61ebbee61eefa3`. Its descriptor, README and manifest are included for provenance. Its ZIP is neither embedded nor repackaged.

## Recorded attempts

| Original attempt | Node | Complete files | Passing / executed |
|---|---|---:|---:|
| Production history — first reviewed draft | 20.19.5 | 1 | 8/11 |
| Production history — corrected ordering/scope | 20.19.5 | 1 | 11/11 |
| Production history — corrected ordering/scope | 22.22.2 | 1 | 11/11 |
| Team final fpv26 | 20.19.5 | 9 | 205/205 |
| Team final fpv26 | 22.22.2 | 9 | 205/205 |

These overlapping invocations are separate; do not sum them into a qualification total. Raw failed TAP, stderr (including empty logs), exits, receipts, commands, selected files, runner identities and input/index snapshots are unchanged. The nine-file Team group includes shared Couch reading/audio checks. The first history draft failed three assertions; later ordering/recipe-scope corrections do not rewrite that attempt or its oracle. Earlier fpv25/Versus/Gallery runs remain in the prior archive.

`p03-production-final/attempt-1` retains the actual write and check commands, their raw logs/receipts, source observation and all 674 physical input records before/after. Both commands exited successfully. They produced measured Field Kit revision 26, 131 compiled files and the unchanged 127 original payloads / 4,007,816 bytes. Only the ledger and three compiled metadata files changed. The unchanged check and logical index are recorded; sampled workspace allocation is not an unsampled peak or RAM guarantee. The producer still describes a candidate needing scoped review.

The independent `fpv26-scoped-review` records verify exact P02/fpv25 prefixes, fourteen review successors under fpv26, original payloads and compiled identities. Its initial and later reviews remain separate. Its “Team pending” text describes that review's time; the later final Team receipts above supply the completed run. The authored recipe-review draft is retained separately as documentation, not executed or independently certified evidence.

## Native observations and retirement

The native session used source-stage **fpv25**, not the later fpv26 metadata-review successor. `p03-native-integration/observations.md` is the coordinating reviewer's Markdown record of actual CUA actions. Screenshots were viewed inline; the tool could not export them, and this bundle contains no local screenshot originals. No acoustic, physical-controller/touch, browser offline or actual foreground-switch acceptance is claimed.

Native retirement retains all 31 original files named by its manifest plus that manifest itself, including preserved admission/server sources, full-index proof, HTTP log, audit and cleanup receipts. The log has 1,022 rows: 1,019 successful GET attempts across 343 paths and three recorded source-only missing build-info/favicon requests. Server response attempts and source hashes do not establish client receipt, complete coverage or a public deployment. The retirement removed only the admitted temporary source window; its original receipt and limits are retained without rerunning cleanup.

## Archive mapping and execution

`manifest.json` maps every retained original cache path, size and SHA-256 to its exact archive member. Identical bodies are stored once, including duplicate index/tree/input records, but every original keeps its mapping. The misleading `.z` suffix on the native full-index file is preserved exactly; its bytes are not re-encoded. Empty logs remain; empty `tmp` directories and unrelated loose cache copies are excluded. No source/distribution or game-asset rebuild is included.

After parent review, run:

```sh
python3 .cache/p03-final-composition-audit/integration-supplement-preparation/package-evidence.py --execute
```

Without `--execute`, the helper prints only the plan. Execution creates fresh cache `delivery/` files (`evidence.zip`, manifest, README, receipt), verifies every ZIP member against its pinned original and enforces a combined 5 MiB cap. It never modifies source/index/remote state, runs tests, generates production or rewrites the earlier archive. This preparing agent has not executed it; the parent owns subsequent review and Git delivery.
