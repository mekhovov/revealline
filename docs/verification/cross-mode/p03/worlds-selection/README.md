# P03 More worlds — correction evidence

This bundle preserves the More worlds correction over local source `10ebbb90127d43ba89aaacc7d3af3e6445427c84`. It grants no source qualification, public-release acceptance, physical-input certification or full P03 completion. P03 remains **In progress**.

Choosing another chapter from More worlds previously bypassed the unfinished-flight replacement decision. The correction routes installed, optional-download and verified original-pair choices through the existing checked-save/Stay/Replace decision. Selecting the same exact campaign preserves the current run. Panel generation and lifecycle checks prevent stale adoption callbacks or focus restoration after cancellation, replacement, closing or foreground loss. Downloaded/installed content may remain; preserved content-catalog work is distinct from changing the active flight.

The original targeted Node20 baseline had **2 failures and 7 skipped cases**. Its corrected counterpart had **2 passes and 7 skipped cases**. The separate quota/readback run had **2 passes and 9 skipped cases**. Those name-filtered commands were narrow probes, not whole-file acceptance. The baseline's modified assertion body was recovered from retained originals and actual edits; its recovery receipt and exact match to the originally recorded input hash are preserved, rather than calling it an original initial file snapshot.

The manifest retains actual terminal whole-file/check receipts only when supplied after completion. Their per-command results and current input hashes remain separate from earlier targeted probes and formatting operations. Skipped tests did not execute, and overlapping command totals must not be summed. The focused runner records its direct named source inputs; it is not a recursive imported-dependency or whole-source qualification manifest.

Original proposals, successive status/lifecycle corrections, peer reviews and their limits, sparse hydration records, formatter output, and the final reviewed source/diff are preserved. The peer review of the original-pair host scenario notes that setting a fixture details element open models expansion; it does not prove a physical keyboard/controller journey. Injected panel-adapter tests do not replace the actual original-pair/download/save host paths.

The preceding [navigation-recovery bundle](../navigation-recovery/README.md) is retained in commit `10ebbb90127d43ba89aaacc7d3af3e6445427c84`. This bundle includes its original small receipt and committed Git/byte descriptors, without copying or claiming to requalify its 1,489,541-byte ZIP. Its failed39e CI and unaccepted foreground observations remain unchanged.

`manifest.json` maps every original absolute path to its byte count, SHA-256 and `objects/<sha256>` ZIP member. Identical bodies are stored once; all original path identities and empty logs remain listed. The embedded manifest equals the adjacent one. `receipt.json` pins the ZIP, manifest and this explanation. The recorded source is a working correction snapshot; the later evidence-complete commit still needs complete hosted source gates, an original frozen release, deployment and public verification.
