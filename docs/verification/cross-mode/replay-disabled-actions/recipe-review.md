# Replay disabled-action screen recipe review

The scoped Replay correction at source `965f2049cb273b93d0258ee43cd806c62d479421` changes only `game/ui/field-kit-surfaces.css` among the ordered screen recipe inputs. Their new SHA-256 is `4f9a3429dd418dc2376c6d2b871280fd8866e4a23f1f2acc30d4775ecb013e0d`. Both shared and legacy Replay primary selectors now apply only to enabled controls; native-disabled and `aria-disabled="true"` controls retain muted, dashed component treatment. Enabled/focus semantics and JavaScript behavior remain unchanged. The legacy theater stylesheet is a reviewed companion change outside this recipe input list.

Retained [native evidence](originals/native/root-browser/) shows the legal Copper Crossing replay completing at tick 1305, disabled Play changing from amber to muted, focus remaining on Restart, Tab skipping Step, and keyboard Restart returning to tick zero. Plain/Large and Theme/Standard were exercised at 1280×720. The original navigation/display cohort passed 29 cases on each Node runtime. These records are scoped checks, not empty/loading/error, physical input, complete responsive/zoom or whole-phase acceptance.

The producer's exact screen approval and current-input assertion are updated together. Canonical generation must append compatible screen/theme records to the existing fpv26 ledger, preserve every historical record including fpv25 and every one of the 127 original payloads, and leave UI, motion, effects, audio, image/font bytes and theme CSS unchanged. The production-history cohort verifies historical prefixes and source invalidation. Generated results and final exact-source/public qualification remain separate evidence; this review does not claim that an unpublished correction has shipped.

Canonical cache-only generation produced **fpv27**, appending seven reviewed screen@15 records to the exact fpv26 prefix. All 127 payloads / 4,007,816 bytes and compiled theme CSS remained byte-identical. Existing `--write` followed by `--check` passed. The complete production-history and readiness files passed **16/16 on Node 20.19.5 and 16/16 on Node 22.22.2**, with no skipped cases. Working-ledger readiness reported 194 required reviewed declarations and 99 optional source slots; this remains a declaration check. Final committed-source and public gates are still required.
