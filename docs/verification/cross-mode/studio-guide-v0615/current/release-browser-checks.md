# Required native and public checks for the Studio guide feature

This scoped source feature does not complete the asset framework or cross-mode Studio preview benchmark. The release coordinator owns full gates, build, immutable bytes and browser checks. Historical native R2 applies only to source628e95da plus its four overrides.

1. Reach Workshop → Asset Studio → Studio guide from the published game with keyboard only; separately exercise touch/pointer. Native Enter/Space toggles the summary. Close and Escape return visible owned focus without changing pending crop/provenance/pixel state.
2. During delayed startup, open and read the guide; completing inventory/inspector rendering below must not displace its focus. Repeat in 390×844 and844×390, then desktop/1280×800 with Standard/Large and Plain. Verify44px actions, meaningful200%zoom and long-reader close at current code/assets.
3. Hold Escape to close help during pending image reading or sprite encoding; repeated events must not cancel preparation, including after focus changes. Release and press again deliberately to verify normal cancellation. Distinguish modeled event assertions from native key-repeat and physical input.
4. Preserve upload originals/derivatives, crop, provenance, pixel and draft histories; complete stage→save→export→import through actual controls, compare downloaded bytes where applicable. Open/close help must not change playback intent. Audio listening remains a separate check.
5. Exercise storage failure/stale save recovery without losing staged work. Verify cached history return and terminal departure cleanup; do not infer a browser lifecycle event merely from selecting another tab.
6. Confirm the frozen build contains guide.mjs and that actual Studio, Design atlas and Return links resolve. The former Markdown guide was already included; no established404 repair is claimed.

Record exact source/publication identity, original-byte audit, inputs/viewports, findings/corrections and evidence limitations. Controller authoring, physical devices, native assistive technology, offline and complete-mode playthroughs remain distinct outstanding qualification categories.
