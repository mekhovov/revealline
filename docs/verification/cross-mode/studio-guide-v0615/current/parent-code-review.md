# Parent source review

Reviewed all eight Studio feature paths on61cf9c14 after the retained candidate and held-key correction.

The guide attaches a capture listener only to identify repeats of the press that closed it. Escape keyup or a fresh keydown releases that ownership; unrelated keys and fresh authoring Escape retain prior behavior. Disposal removes both capture listeners and the guide listener. No timers, operation/storage authorities or post-close focus callbacks were introduced.

The actual-host regression reaches the existing window operation-cancellation listener, preserves original image read/encode/save byte checks, and verifies guide-close repeats after focus changes. The DOM boundary supports capture/bubble propagation; native details toggling and the final document-to-window event edge remain explicitly modeled. Unit coverage adds fresh-press recovery after a missed keyup and terminal guard retirement.

Studio integration consists of import, mount before startup and terminal disposal. The header disclosure remains outside authoring busy/inert regions and before dynamic inventory. Shared44px host roles and readable typography apply. All five recipe groups and16producerinputs, compiledtree andproductionledger remain untouched.

The guide author reviewed his two files as author and independently reviewed the parent/host integration; this parent review covers his guard implementation. Source counts and historical native observations are separate evidence. The pending About parent correction must preserve these originals and requalify the final versionedStudio source.
