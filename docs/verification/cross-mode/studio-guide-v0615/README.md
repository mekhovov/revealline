# Studio in-page guide — v0.61.5 source evidence

The Studio guide stays inside the workspace, above the changing inventory and inspector. It explains preparation, provenance, staging, save/export, imports and recovery. It remains available while authoring controls are busy. Reading and closing it preserve pending work and preview intent.

A held Escape closes the guide without cancelling preparation, including repeats after focus moves. Release and a fresh Escape retain deliberate operation cancellation. The actual-host regression covers pending image reading and encoding, then verifies exact saved PNG bytes and existing sprite/edit history. The capture guard owns only that guide-close press and is removed on terminal departure.

Final source is based on the corrected About parent d6800fa53. All six affected test files pass 46/46 on Node 20.19.5 and 46/46 on Node 22.22.2. Runtime/test lint, formatting and source whitespace pass. The final 130-file input map contains 2,146,440 bytes. Independent integration checks preserve the seven unchanged candidate bodies, compose the Studio skill paragraph with About’s correction, and confirm the only version changes are 0.61.4 to 0.61.5. All 16 shared producer inputs, compiled assets and the production ledger remain unchanged.

The intake manifest pins every original. It retains the earlier footer-displacement failure, later header correction, modeled held-Escape failures and corrected runs. Earlier native observations apply to source 628e95da with four overrides; they do not qualify the final source. The previous Markdown guide was already shipped, so this feature does not claim to repair an established public 404.

These are scoped precommit checks. Full exact-commit source gates, the complete build/allowlist test, production and frozen artifact checks, native journeys and public byte/play verification remain with the release coordinator. Physical controllers, touch hardware, assistive technology, zoom and offline behavior remain separate evidence categories. This feature does not complete the Studio framework, cross-mode preview benchmark or the whole redesign.
