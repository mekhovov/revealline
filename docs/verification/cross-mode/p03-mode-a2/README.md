# P03 mode-entry A2 evidence

This internal candidate extends `c7f4faae318d58eacab140c1b2913e620e15efb7`, preserving its A1 and J/K behavior. Five production inputs add guarded Solo Missions → Versus, a separate bounded same-tab v2 return and exact receiving focus. The original v1 Team helper is byte-identical. No version, phase or public acceptance is declared.

The [source/test receipt](source-tests/verification.json) binds **355/355 tests on Node 22.22.2 and 355/355 on Node 20.19.5**, with no skips or cancellations. These are fourteen complete affected files: six return/Couch files contribute 194 and eight retained-save/G/H/J/K/shared-input files contribute 161 on each runtime. This does not stand in for the eventual full-source CI. Final syntax, ESLint, Prettier and diff whitespace checks passed. Lagrange independently read all nine held runtime/test inputs and found no blocker.

The prior v1 host scenarios are parameterized for both Team and Versus. The actual host tests exercise Solo → actual Versus ready/paused departure → Solo, exact installed selection and after-ready focus, one-use returns, real checkpoint/saved-byte preservation, readback and newer-slot failures, truthful fallback and pending lock/hint cancellation. Couch tests preserve both real duel checkpoints and the existing native-default test boundary; they prove that Confirm keeps its captured scalar return token and Team never receives a nested Solo hint. Pure v2 tests cover strict shapes, UTF-8 bounds, exact declared authority/base, expiry, mixed parameters, replay and newer-record preservation. These are finite DOM and source checks, not native input or physical-device evidence.

The initial 52-case host and 114-case host/unit passes precede the final actual-Couch-host coverage and are historical, not additive. The first 161-case retained-navigation execution had 155 passes and six failed G cases because two small original pack fixtures were absent from the sparse worktree. Restoring Night Shift (8,848 bytes) and Living Threads (9,156 bytes) from the exact base Git blobs changed no runtime or test assertion; the complete eight-file rerun passed. Its failed raw log remains retained. The authored register appendix also initially had one extra blank line; only that whitespace was corrected. The original register was already formatted, so no formatter exception is claimed.

The [native receipt](native/native-root-verification.json) records **18 desktop keyboard observations and four PNG screenshots**, totaling 433,126 bytes for events and images. Root inspected the screenshots. Its five HTTP-matching production overlays ran over exact `c7f4faa`, without P05/P08/R5/title/R3 overlays, timing fixtures or game-state injection. The [preview binding](source-tests/native-preview-binding.json) and [HTTP byte check](source-tests/preview-http-check.json) preserve that authority.

Root selected 1994 Forever and Grid + buffer, deployed, sent Down and paused at the displayed 0:06. The nested departure warning remained visible, reported a verified saved flight and initially focused Stay. Escape returned to the actual Versus opener. A fresh explicit Leave used the fixed Versus route with a v2 token. Keyboard Return to solo opened the same First Signal/retro Missions view with Grid + buffer still selected, focused Versus and left the receiving page in briefing state. Explicit Back opened Title Continue; the inherited Continue then restored the actual saved flight paused at 0:06. That later explicit restore is distinct from the token return, which adopted no flight.

The labels in the raw events record intended actions and are retained unchanged: event 09 labelled Versus actually focused Team before the next Tab. Cut geometry, exact saved bytes and queued-turn state were not inspected in the browser; their stronger checks belong to the host tests. Some AX snapshots and subsequent DOM reads straddle a loading transition. Versus initially retained body focus, the separately qualified R5 issue; this preview does not include R5. The inherited Title Continue deliberately loads paused, while TitleB separately implements named Continue-to-run. No physical controller, touch device, portrait/zoom, native storage-failure/background race, public/offline deployment, P08 artwork or final integrated P03 acceptance is inferred.

The [copy mapping](copied-evidence.json) binds all 34 finite source/test/native originals. JSON, PNG, Python and Markdown originals remain byte-exact; raw logs and diffs are gzip-compressed without text rewriting, then decompressed and compared to their originals. Original receipt paths keep their historical cache-relative layout, mapped here to retained copies. The source/test receipt's documentation pins describe the pre-native source snapshot, while all nine runtime/test pins remain exact. Later packaging/native prose is separately reviewable and does not rewrite that receipt.

See [the mode-departure contract](../../../mode-departure.md#a2--solo-missions-to-versus-and-same-tab-return). R3 Library, title Start/Continue, coherent Couch lobby, P08 picture preparation/Start, R5 initial focus and final combined-source/public/offline/device qualification remain separate obligations. No unobserved browser navigation failure, rendered-art fidelity, physical controller or complete P03 acceptance is inferred from this package.
