# P03 Library pager and picture-return composition

This internal candidate composes row removal, stable Score/Collection pagers and picture-view return over `ec288a4e96d13e5a8fda51dff759838af96b46fe`. It does not include the separate P05/static, Couch or Team branches and does not close P03 or a public release.

The source sequence is row prerequisite `87f0f49c84e2f92bceb24b0a317d5e6c79e64ac6`, pager checkpoint `0368f58f8b866d9ae9e716536e7b914d4a6af818`, then gallery return `7fedeef11991ed7be19bdb1d5841f6aed00dfb09`. The index and worktree were prepared finitely; no full build or common sparse configuration was changed. The two production modules are the combined Library panel and the exact pager operation-focus helper. Pager and picture-return focus leases keep separate ownership; only the pager opts into nearest scrolling. The stronger gallery test adapter remains byte-exact to `7fedeef` (`b9e36ad0`), and the `ec288` qualification prompt is preserved.

The root source review compared the complete Library delta against both component checkpoints, along with the merged guidance. The final executable boundary is `source-held-02.json` (`34c02767`), staged tree `cb951eeb2acb6d5dcf9d907cd48cb642fcded401`. Later changes to the current status snapshot, shared guide and this evidence description are documentation only.

## Complete-file verification

Both established runtimes passed the same ten complete files: **201/201 on Node 22** and **201/201 on Node 20**, with no failure, cancellation, skip or todo. The runner observed 126.428 and 141.720 seconds respectively. All 293 participating inputs (27,114,408 bytes), the canonical staged index, source hold and runner/runtime pins remained unchanged. `verification-tests-final.json` records the full identities and raw receipts.

The complete files are `library-pager-host`, `gallery-return-host`, `gallery-focus`, `collection-context-host`, `main-menu-collection-host`, `gallery-difficulty`, `difficulty-host`, `library-row-focus-host`, `operation-focus-host` and `modal-navigation`, each under `game/test/` with the `.test.mjs` suffix. This union exercises composition boundaries; it does not repeat or replace the earlier broad 767-test qualification. Component counts of 108, 188 and 60 overlap this union and are not added to it.

The first attempt is retained as an incomplete diagnostic. It stopped producing TAP output after the controller picture-return case; the owned modal test child was explicitly stopped with SIGTERM after several minutes. The resulting raw summary has 183 passes and one file-level failure, with Node 20 unrun. This is not a completed assertion result or a demonstrated production defect. Its exact original test, partial/final logs, process identity and unchanged input records are retained.

The old modal fixture emitted close before restoring the opener. The approved gallery owner requires native opener restoration before the queued close event. The only test successor models that order, queues a non-bubbling close, and reports three unchanged identity checks as primitive booleans to avoid large cyclic diagnostics. Checkpoint, storage, error and exact opener assertions remain. Its three picture-return characterizations passed before the final whole-file pair; those three are not added to the 201 total. Production source never changed during this correction. This fixture models event/focus order, not browser layout.

## Native scope

The immutable preview binds exact `ec288` Git plus only the two held production overlays. All other served files are original regular Git blobs. There is no application state or timing fixture. Six HTTP/local/authority pins precede the root browser run; the helper keeps a bounded Git blob cache, rejects overlay drift and uses a fresh origin.

The existing portable Library fixture (`a122d456`, 24,606 bytes) supplies 25 scores and 25 archived pictures through the actual Copy/paste Import UI. Its generator and provenance are retained unchanged, and its core/replay/library/class authority is identical between the original fixture source and `ec288`. The imported archived cards correctly remain disabled because their packs are unavailable; the later viewer checks use a separately earned First Signal picture. These are simulated legal completions used to exercise UI behavior, not human gameplay achievements or original-art fidelity.

The combined recorder contains nine native keyboard observations and six screenshot files. The imported Collection reaches its final page, transfers disabled Next focus to Previous, and keeps that same Previous visible when the full middle page is rendered. Its recorded y≈1296.43 plus height≈43.99 stays within the observed 1356-pixel viewport.

A separate actual First Signal play-through then finished at 52.2% coverage, 8,160 points and three lives. The root filtered Collection to that newly earned picture, opened it, and verified that Escape, keyboard Close and keyboard Back to collection each return to its actual recreated card. Collection Escape then returns to the title Collection opener. The imported paging records are distinct from this actual gameplay completion.

The recorded viewport was 1923 or 1924 CSS pixels wide and 1356 high, with DPR approximately 1.8 or 2. There was no viewport override in this run, and those readings do not establish browser zoom or physical-device coverage. The root inspected all six original JPEG screenshot files and found the named paging and picture-return journeys passed. Native fault, background, delayed metadata and competing-focus cases are not inferred from these positive keyboard journeys.

## Evidence and limits

The [evidence map](evidence.json) binds lossless copies of the source holds, conflict records, finite intake, runner authority, complete raw logs, stopped diagnostic, fixture source/provenance, preview helper/configuration and native originals. Existing component evidence remains byte-exact in its own directories. Current prose uses “screenshot files” without treating a filename extension as proof of encoding; historical records are unchanged.

P01/P02 acceptance and integration, separate Couch/static/Team composition, a final full build/source/public/offline gate and physical input/device qualification remain separate. This checkpoint does not allocate a version, publish content, certify a release or complete a phase.
