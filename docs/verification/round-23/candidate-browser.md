# Round 23 packaged candidate

Checked 12 September 2026 at isolated port 8825, serving `.cache/round-23/candidate-final`. This working-tree build is **v0.13.0-rc2**, with 129 manifest assets and distribution SHA256 `a3db51a023ecc92612b6a94684d101b535ce40ba9b9152018fc57dcbb85dc513`. Its source revision is explicitly null; the later Git-tagged artifact has separate evidence. The unused earlier rc1 build remains in `.cache/round-23/candidate`.

The generated landing page, solo game, Playground, couch race, Replay Theater and Controller Lab loaded under the CLI build server's production CSP. Error/warning logs were empty for all six after the checks. Theater verified its default Copper Crossing recording and enabled playback at tick 0 / 1,305; this is a verification/load check, not another complete playback.

In solo, selected Toggle in Settings and saw **Saved on this device**. Both Immediate and Grid + buffer completed a real First Signal run using visible Tap steering: **52.2%, three lives, 8,160 points, 0:03** each. The full-picture celebration appeared, the terminal Boost cue showed status without a flight-button instruction, and the result offered picture viewing and retry. Reload retained the Toggle preference, one gold First Signal picture, first-clear appearances and the next ready campaign mission. The Lab's fresh same-origin scenario adopted the persisted Toggle mode.

In Controller Lab at an actual reported **320 × 640**, joined with a consumed first face-button gesture and started with a separate second gesture. Held physical Boost / 5 for 900 ms and released: Boost remained on. Its mapped action label was visible without overlapping controls in the scrolled [compact screenshot](screenshots/candidate-compact-toggle.jpg). Menu pause cleared it and changed the cue to plain “Controller Boost off.” Practice awarded no campaign progress.

The [source browser report](source-browser.md) separately covers remapping, both real trail-failure recoveries, native Stop, readers/hangar, rejoin, mixed local/controller input, import/Undo and five measured iframe sizes. These browser checks do not certify physical controllers, actual phones, native binaries, a public host or player enjoyment/retention.
