# Frozen v0.13.0 browser and offline verification

Checked 12 September 2026. [v0.13.0](http://127.0.0.1:8767/releases/v0.13.0/site/game/) is preserved from annotated tag `v0.13.0`, commit `c7b0122edffcaabbdfa6a817c06f603625f79fe0`. [Independent integrity verification](integrity-notes.md) ties the final 1,406-test source snapshot to its exact source archive and byte-identical rebuilt distribution.

Served the frozen `site` at isolated port 8826 with the CLI extracted from its own source archive. The user's persistent workspace server on 8767 remained running throughout.

1. Saved Solo controller Boost as Toggle in the ordinary game. Settings → Prepare offline play verified **126 files / 20,515,050 bytes**, with no missing or corrupt entries. The [visible JSON report](frozen-offline.json) records build `4a161aff62c90a2a215e7f05b733ded56d79fb64af617d7ae1fcb987a65d4657`, matching the independent artifact audit.
2. Stopped the entire isolated server and navigated afresh to the frozen game. It reopened from the prepared cache. No browser-network interception, hidden game calls or storage writes were used.
3. Selected Grid + buffer and Tap steering. Started First Signal, moved Down into a live cut, then used **Save & pause**. Reloaded while the server remained stopped. **Load saved flight** verified and restored the same unfinished cut at 0:01, paused with Controller Boost off. A separate Resume and Down completed it at **52.2%, three lives, 8,160 points and 0:04**.
4. Reloaded again offline. Collection retained one gold First Signal picture and first-clear appearances; Local scores retained exactly one scout / grid-center / seed 1 result at **8,160 and 4.03 seconds**. The next campaign mission was ready. [Retained picture](screenshots/frozen-offline-collection.jpg).
5. Opened Controller Lab for the first time on this origin with the server still stopped. All 18 practice choices loaded. Selected the actual reported 320 × 640 viewport, joined the virtual controller and started with a separate gesture. The saved Toggle preference was active. A 900 ms held Boost press toggled on once and remained on after release. Native Stop cleared it. Toggled on again, made a Down cut and reversed Up: the actual self-contact reduced lives from three to two and cleared Boost. Practice awarded no campaign progress.
6. Frozen solo and Lab error/warning logs remained empty after these operations. The isolated server remains stopped. This verifies one browser's retained offline storage, not its future eviction behavior or physical-device support.

On the persistent workspace origin, opened a separate v0.13.0 player page, installed all six bundled expansions through Library & saves and selected Sentinel Relay. It is ready at 0%, three lives, zero score and 0:00. No QA scores, unlocked pictures or test saves were imported into it. All sixteen earlier releases, their player data and immutable artifacts remain available.

The [source browser record](source-browser.md) covers five reported game sizes, both real failure/recovery policies, remapping, mixed input, readers, hangar, rejoin and import/Undo. The [packaged candidate](candidate-browser.md) covers all six production-policy entry points and actual completions in both steering modes. No public-host deployment, physical controller/phone certification, native/store execution, online service or player-enjoyment measurement is established by these tests.
