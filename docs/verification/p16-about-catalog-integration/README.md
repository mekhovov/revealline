# About mission catalogue independence — integration

The optional remote software archive previously prevented the local mission catalogue request from starting. Load them independently, report truthful pending/fallback status and keep starter/static links available. The footer now describes its actual game-return destination. No gameplay state, navigation ownership or scoring changed.

The exact integrated runtime/HTML/test/docs match the sealed five-path packet. The maintenance skill combines its About paragraph with the existing Studio and compact-Team guidance. Initial patch check rejected only the changed EOF context; no old whole-file skill snapshot was substituted.

Three baseline regression failures are retained. The complete About host file passes **31/31 on Node20.19.5 and Node22.22.2** at this integration. Lint, formatting and whitespace checks pass. Tests cover independent delayed/failing catalogues, current navigation lifetime, focus, release-local routes and controller navigation.

The retained native keyboard run held software history while all eight mission choices appeared, selected Frontier Lines through the native picker, then released history and observed unchanged selection/focus/launch URL and a clean console. All **156 unique served file bytes** match this integration’s tracked/candidate inputs. The native log’s source-origin label is historical; the byte comparison, not that label, establishes identity. This was a local1280×720 desktop browser; it does not certify touch/controller hardware, offline operation or a deployed release.

PR211’s source remains frozen. This is a held follow-up requiring fresh source PR, exact-source gates, version, publication and public verification after the accepted release.
