# v0.98.0 ordinary build evidence

The immutable v0.98.0 source is commit
`1168c02f5fb62056936878fe7d3c4e2e0afb0732`, tree
`f0b9d3d3afd5daa113f03f86a53ac6f3b18a7b92`.

Its release qualification and freeze completed on GitHub Actions run
`36013396993`. The automated test suites were explicitly waived by the
temporary repository policy and are not reported as passed.

The original release pull request's ordinary build job was cancelled after
the source had merged. This documentation-only successor exists to retain a
successful ordinary pull-request build for the same runtime, package, and
build inputs. No game, publishing utility, policy, workflow, package, or build
input differs from the frozen source.

