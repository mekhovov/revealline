# Secured Team core specimen

In Asset Studio, choose Field context → Couch Team → Relay Yard → **Secured core · two-relay specimen**. Inspect `team.core.secured` to see the selected core body while another relay remains shielded. The ordinary First Connection and Relay Yard scenes keep their original levels, pictures and command traces.

This scene uses a distinct authored `studio-two-relays` revision 1 level, built and validated through the ordinary Team recipe API. It retains Relay Yard's layout and actors and adds a southern relay with two anchors. Both relays are required for victory. The unchanged Standard/seed 17 public command trace earns the northern core at tick 832. No captured flags, player statuses or completion outcomes are assigned. The second relay remains shielded, its anchors available and the run active. Play preview advances released controls for three simulated seconds, holds briefly and restores the command-earned snapshot. Paused and Reduced effects hold the current state.

The real Team painter shows one secured core, one shielded core, two captured anchors and two available anchors. Functional boundaries and SECURED/SHIELD labels remain game-owned. The exposed-core role is inactive here; choose the existing Exposed relay core scene for it.

The specimen has no canonical arena-picture binding and uses a neutral backdrop. Picture artwork is neither decoded nor attached; selecting an arena-picture slot requests a normal scene instead. The normal authored identity/hash validation is unchanged, and the specimen fails that canonical identity check. This is role-state inspection, not approval of real-arena picture readability, produced artwork, physical controls or public gameplay.
