# Collection Flight records return

This internal P03 successor replaces the Library-close bridge that clicked the Collection entry again. Closing Flight records now refreshes the already open Collection through its own owner. It updates progress metadata and cards without another entry, Pause, modal opening, selection, or save.

Native close must still own the exact connected `collection-records` button before refresh starts. The owner captures that button once and uses the existing optional nearest/auto reveal policy. It does not capture newer Search/Close focus or BODY, refocus an already focused opener, or select a fallback. Settlement requires the same Collection visit, Library launch, profile object, expected gallery population and return owner. New focus/input, a dialog, suspension, close/reopen or a newer population retires the old permission. Picture-view and pager owners remain separate.

The source change is limited to `game/app.mjs`, `game/ui/library-panel.mjs` and `game/ui/field-kit-surfaces.mjs`. The host injects its existing read-only progress presenter. Strict content/profile formats, shared focus policy, retained pictures, replay handoff, simulation and navigation routes are unchanged.

The predecessor combined tree is `def1186e23ca8ce668b362c0b001f2b039eb76e5`. Its settled native failure began with empty Collection, imported 25 legal fixture completions through the actual chooser, then closed Flight records. After 600 ms the focused button remained below the 1348 px viewport (top 1849.8004, bottom 1893.7935, Collection scrollTop 0). The three events and two original JPEGs remain in the original worktree; all 25 post-native HTTP bodies match the held tree. This is a fixture-origin import journey, not human-earned progress or a whole-keyboard claim.

The new actual-host cases model native opener restoration before the queued close event, one Window identity and held IndexedDB metadata. They check final card publication before reveal, no second focus, delayed success/refusal, explicit newer intent and unchanged paused checkpoints/storage after the separate import or lifecycle action. Command assertions do not establish viewport geometry. Complete-file qualification and the corrected native growth journey are recorded separately; P03/P05/P08, browser zoom, physical devices and publication remain open.

The [full-composition checkpoint](verification/cross-mode/cross-mode-source-composition/README.md) records the completed successor checks: eight complete files passed 177/177 on both Nodes and the separate complete Field Kit file passed 7/7 on both. Root repeated the real fixture import at 1454×1348, DPR 1.8: after Escape and 600 ms, Records remained focused and fully visible (top 1287.578, bottom 1331.571, scrollTop 562.222), including a second Library return. All 27 before/after source responses matched. This closes the scoped growth defect, while the historical native failure, incomplete ENOSPC run and fixture diagnostics remain retained.
