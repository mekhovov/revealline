# Gameplay and presentation source composition

This internal candidate starts at `356401f936578db5d3643e13cb6287e59535e853`, the four-parent cross-mode checkpoint with the Collection Flight records growth correction. It composes three existing checkpoints without changing versions, content identities, strict formats or publication:

| Input                        | Commit                                     | Preserved scope                                                                                                                               |
| ---------------------------- | ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------- |
| Narrow Team HUD              | `61fb43b32974187fe664f130117465285223251b` | Two player columns plus full-width territory below at widths up to 600 CSS pixels.                                                            |
| Team actor presentation      | `d30f883df1e11d04fb8a5682e17171f98da67901` | Prepared sprite roles, CSS-sized cues and inward cosmetic offsets; actual actor state/contact stays authoritative.                            |
| Retained Results during Next | `99f33086c5c381cd91459e59e672c4adba1fe18d` | Staged picture/duel adoption preserves Results, original and View through failure or Cancel; explicit current action governs automatic Start. |

HUD CSS and actor modules/tests retain exact incoming bodies. P07's source and tests are also exact except the Couch shell: its default-true `focusTransition` flag controls the existing final transition reveal while the current-action resize handler remains intact. Both operate after synchronous layout publication; neither gives a stale or background action new focus authority. The Collection refresh owner and independent gallery/pager leases are unchanged.

Prompt and maintainer additions are combined, not replaced. Each checkpoint's original evidence stays unchanged. Historical prose keeps its source cutoff; the current register identifies which earlier exclusions are superseded. The final checkpoint must retain the base and all three input histories. Temporary merge objects are only preparation records.

The final union contains 19 complete files: the actor checkpoint's eight files and P07's twelve, deduplicated, plus the resize host file. Both Node 22.22.2 and Node 20.19.5 passed 414/414 on unchanged source tree `a20e424d31331221791ae429c2b8bcc39cfee5af`, at concurrency 1. All 637 inputs (87,830,945 bytes), their Git bodies and the private index stayed exact. Prior 167/246/46/760/177/7 counts remain separate source-specific evidence and are not added to this result.

The first complete Node 22 attempt passed 400/414 and did not start Node 20. The finite intake omitted the newer P03 `operation-focus.mjs` import, causing Solo boot and dependent preference cases to fail. A final-tree relative-import/literal-URL audit identified that single missing 5,039-byte module; its exact Git body was restored without a source, test or index-tree change. The corrected pair reran all 19 files. Retained raw TAP duration is separate from the first launcher's wall time, which includes a documented scheduling pause. A reused test inventory is only an intake seed; compose the actual host import graph before execution.

The [lossless evidence package](verification/cross-mode/gameplay-presentation-composition/README.md) keeps every initial failure, authority, test log, request schedule and native original. Root's five closed recorders contain 24 observations and nine JPEGs, with distinct scopes. Team's actual 390×844 portrait surface shows the full arena, player identifiers, paused keyboard Options and explicit Resume. The actual 760×351 Team arena is clipped: that short-landscape failure remains open in a separate CSS candidate. Partial movement at 0% does not qualify capture, encounters, joint cuts or rescue.

Versus real play earned 52.2%, three lives, 8160 points and 1:0. An exact-image HTTP 503 kept Results, View and focused Next. The first six-second Cancel attempt missed its timing window; a second origin consumed its delay during premature setup. Neither is a Cancel pass. On the final twenty-second schedule, actual Cancel retained the result and Next after late response completion; another Cancel remained visible through actual 433×938 resize. A still-owned shipped Next completed after actual 844×390 resize, legally started a fresh round with 1:0 and an in-viewport canvas, then Escape returned to visible Resume. That is not the installed-pack Ready/fresh-Start path. The resized final observations are DOM geometry, not resized screenshot acceptance. Forty source-response paths match before and after each of four origins; fault-target HEAD checks preserve request ordinals and do not assert an HTTP image-body comparison.

This does not complete P03, P05, P07 or P08. Full progression, full Team completion/rescue, all edition/import bindings, complete bright-art readability, actual browser zoom, broader system/storage preference behavior and physical devices retain their existing gates. Borrowed actor slots do not establish the planned Team semantic registry or new artwork. Current-P02 integration must review and rebind the historical exact Team fpv revision 19/null snapshot to the accepted current registry/assets, then rerun authority and native checks; never weaken revision/hash matching. P01/P02 source integration and final exact-source CI/build/public acceptance follow separately. Four tested guidance bodies are retained before these documentation-only closure updates; runtime and test bodies remain unchanged.
