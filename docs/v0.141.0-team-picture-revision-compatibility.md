# v0.141.0 Team picture revision compatibility

The v0.141.0 managed-media continuation advances the selected Field Kit theme from revision 90 to
revision 91. The change requalifies shared storage admission for creator and Team media; it does not
change either reviewed Team picture PNG.

The exact Team association table therefore names revision 91 while retaining revisions 58 through
90 for already accepted attempts. This is a finite compatibility policy. It does not permit another
theme, collection, revision, asset, crop, pack, level, or imported namespace to borrow the pictures.

The retained picture identities remain:

- `scene.reveal.wide.field-kit@2`, 52,720 bytes,
  SHA-256 `53f1206a11a8791892f5c844c0641529acbc2c09c8d558676d0d801d72113850`;
- `picture.fpv.adf5c9eea274ba7f.field-kit@2`, 38,090 bytes,
  SHA-256 `d76f309d8385cd5d20fc2fff72b7f3abc19299cccdde767d76dd9f4a4960929d`.

The implementation:

- binds First Connection and Relay Yard to exact current FPV revision 91;
- retains those two identities for exact revisions 58 through 90;
- expands the bounded historical policy from 33 to 34 distinct revisions;
- rejects revision 92 and every unlisted identity before reading or decoding a picture;
- authenticates the original PNG bytes and complete-frame geometry for current and retained
  revisions.

This record preserves immutable attempt compatibility. It does not claim new artwork, new picture
quality review, physical-device acceptance, or a general FPV fallback.
