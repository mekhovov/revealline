# Missions navigation — v0.62.1 candidate

This is a proposed follow-up to v0.62.0, based on source `72b6845e101850a4ee34df66656dae3c7ad87f5f`. The correction is allocated v0.62.1 locally and is not a public delivery. P03 and P05 remain open.

In Missions, keyboard and controller focus can reveal a chapter or Older card without choosing it. Activate the card to select the chapter; inspect the mission before using its existing Deploy action. Moving focus or resizing the window does not start or resume a flight. Deliberate reading scroll remains available.

The candidate keeps the focused chapter clear of its inner scrolling rail and the dialog's sticky heading and actions. In short landscape, mission names sit beside a smaller artwork preview. At widths of 360 CSS pixels and below, the complete Missions heading sits above Back while keeping the selected text size and existing action targets. Direct Brief keeps its separate reading layout.

Scoped keyboard browser observations on isolated previews retained the First Signal card during portrait/landscape rotation, exposed first/last Older cards across the 799/800px rail boundary, and selected an Older chapter without launching. A separate correction exposed the full Plain/Large heading at 280×800 and 360×800; 361×800 retained readable adjacent columns. At 280×800, First Signal's name and focus outline cleared the footer, and its brief expanded without launching. These are observed desktop-browser layouts, not physical-device or complete Missions acceptance.

The original focused source checks and retained native failures remain part of the record. The short-landscape and narrow-heading checks used distinct preview revisions; do not combine them into a claim of an exhaustive final-layout matrix. Complete direct-Brief reading and Back journeys, all affected fonts/localizations and zoom, physical controls, ordinary source/build gates and public acceptance remain separate work. Full source qualification must also run the unchanged briefing tests with their normal complete pack fixtures; the cache adapter's earlier missing-fixture setup error is not a passing test.

Earlier narrow Missions evidence covers 280×800. The separate 280×568 Plain/Large observation in `native-missions-portrait-r8.json` failed: the full 48px title wrapped into three lines (151.195px), Back began at y240.289 with 47px height, and the 217px sticky footer began at y351, obscuring the focused 136.195px chapter card beginning at y305.086. Preserve both records. The semantic-size phone heading and compact chrome subsequently passed the combined R4 browser check at 280×568 with Plain/Large text: the full title fits a 29.695px line, Back remains 44px high, and the focused chapter clears the footer. Preserve the failed R8 observation and the corrected R9/R10 observations separately.

The integrated correction passed 284 focused cases on each local runtime, Node 20.19.5 and 22.22.2, using the ordinary complete pack fixtures. In the combined browser preview, inline and direct paused Brief both retained reading across native Shift+Tab; Enter and Space on Done returned focus to Read. Forward Tab returned to the reading region, and another Tab ended reading at Flight setup without skipping it. Back from direct Brief restored the paused Mission brief opener with the clock unchanged. Plain/Large layout checks covered 280/360/361/480/481×568 and 600/799/800×400. These are scoped native-keyboard and desktop-viewport observations; physical controllers, touch hardware, foreground lifecycle and full-source/public qualification remain separate.

The [retained candidate evidence index](research/evidence/missions-v0621/index.json) pins the original focused outputs, the unsuccessful native observations and their corrected retests. This evidence does not replace the final intended source qualification or deployed-release check.
