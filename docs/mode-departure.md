# In-panel mode departures

This is the A1 work package inside P03, based on `f61a249277944a5fdd50cf7d545cbb18dbd0eaf8` with H and I already integrated. It closes the fixed Couch destination paths before the larger title and lobby redesign. It adds no version, save format, run format, ruleset, content binding or automatic Start.

Versus now uses its existing leave screen for both Return to Solo and Relay Rescue. During an unfinished attempt the host pauses and releases physical controls through its existing path. Stay is the default, both board continuations remain paused, and only a separate Resume resumes them. The decision captures the actual match reference and setup generation. Confirmation rereads both and permits only the fixed relative Solo or Team destination; it cannot use a URL supplied by a control or return parameter. A suspended, stale or cancelled decision cannot leave. Ready and finished matches keep direct native mode links, and modified clicks retain native behavior.

The leave copy states only what this host knows: the current Versus attempt exists on this page and is not saved. It does not promise that another Solo page has a saved flight. A permitted anchor retains native browser navigation; if navigation does not happen, this code has not reset or replaced either board.

Team adds Return to Solo and Go to Versus inside its active paused/results panel. They use I's existing fixed-action Stay/discard owner, including its stopped-arena recovery path. Stay returns to the actual in-panel link, while a separate Resume still requires fresh input. In a stopped arena Resume remains unavailable. The original header links remain available, but these new panel controls provide the keyboard/controller route without broadening the navigation root.

Solo entry retains its existing bounded v1 same-tab return hint. The in-panel Solo link uses that route when Team was entered from Solo; a direct/Versus entry uses the fixed Solo title route. Team's receiving page does not read or write Solo flight state. Go to Versus is always the fixed Couch route. Neither link adds a saved Team attempt, a previous Versus match restoration promise or an automatic start. Terminal/no-attempt behavior remains direct. New Team links have a local 50px minimum block size and wrap within the existing scrolling overlay; browser geometry is a separate gate.

## Source and test scope

The finite host tests exercise actual markup, shared keyboard/controller navigation and both real game cores. Versus tests compare both authoritative checkpoints and run references across Stay, background cancellation and permitted departure; test fixed native anchors, controller-held Confirm, current match/generation changes and real terminal draws. The finite DOM does not implement browser navigation or native anchor activation itself: the local test helper models focused-anchor Enter only after the actual key handler permits the native default. Team's host tests similarly keep native default modeling separate from guard behavior and use I's existing paint/HUD and fault boundaries.

Initial failures are retained under `.cache/p03a1-mode-departure`: the first new Versus tests omitted the DOM's native Enter default, and the existing navigation file lacked two exact baseline fixtures in the sparse checkout. The Team test lane retains its initial 64/72 result under `.cache/p03a1-team-tests-dewey`: the new cases initially omitted native interior dialog Tab and tried to join a controller with D-pad rather than its supported South adoption gesture. The corrected tests preserve their original checkpoint and route assertions. These diagnostics are not application failures or passing executions.

Seven complete affected files pass **211/211 on Node 22.22.2 and 211/211 on Node 20.19.5**, with no skips or cancellations. The nonoverlapping totals are 48 Couch shell/navigation/departure tests, 91 shared navigation/Couch input/Team input-policy tests, and 72 Team host tests. Final syntax/lint/format/whitespace checks pass. The [retained evidence](verification/cross-mode/p03-mode-a1/README.md) binds exact source, whole-file commands and raw logs; it does not add earlier failed executions or count a runtime twice.

Root's scoped desktop native check used the six exact HTTP-matching runtime inputs over `f61a249`, without P05/P08 overlays or game-state injection. It observed Versus → Team Stay/Escape/explicit discard, both Team in-panel destinations by keyboard, exact Stay openers at visible 0:04 with both players Line exposed, and explicit returns to Versus and ordinary Solo. Team links measured about 52.1 CSS pixels high. The 26 events and two inspected screenshots are retained. Native fault/terminal/from-Solo hint/Versus-to-Solo, exact checkpoints, background races, physical controller, phone/touch, responsive/zoom and public qualification remain separate. Fresh Versus initial focus remains the R5 gap. P03 remains implementing.

## Integration obligations

This branch deliberately does not replace the separate P08 static-picture host at `1da77ee586d0fdd3ac73883235a822b959e85b37`. Its `couch.mjs` preparation, one-shot Start intent, picture confirmation, cancellation and retry hunks must be preserved when composing the branches. A1 contributes only the host's match/generation reader, ordinary pause callback and decision invalidation at suspension/controller-loss boundaries. Its shell transition continues using the existing content cancellation callback. A combined-source test must verify that a pending required picture cannot start through a destination action, cancellation still releases the correct lease, and returning leaves Start as a fresh action. No unimplemented P08 state is invented here.

The initial injected Team presentation module is also separate: it must preserve I's captured authored Retry recipe and stopped-arena handling, as well as A1's in-panel destinations. It has no shipped binding or Team host adoption in this patch.

The A2 candidate below adds Solo outgoing Versus retention and an explicit origin-hint format after the separate Workshop Restart correction. Title Single player/Continue and the coherent two-document Couch lobby remain later approved work. A future title Continue may explicitly resume; existing restore and Back paths stay paused. This patch does not change P01 Library acceptance, P03 mission/setup retention, P07 progression or Team persistence.

## A2 — Solo Missions to Versus and same-tab return

The A2 candidate is isolated from `c7f4faae318d58eacab140c1b2913e620e15efb7`, which already includes A1 and the J/K corrections. The visible Missions Couch Versus link now uses the same checked departure owner as Team. A ready selection leaves directly when its return hint can be retained. An unfinished flight pauses first and waits for the existing writer, backup lock, recovery/journal checks and exact saved-flight readback. Stay returns to the actual Team or Versus opener and leaves the flight paused; it does not resume or rewrite a newer save. Failed verification uses session-only copy. A save changed after verification or unavailable return storage requires another deliberate Leave with the precise warning before the fixed fallback route can be used.

The original v1 Team helper and stored bytes remain unchanged. Versus uses `revealline.mode-return.v2:<Solo pathname>` in same-tab session storage: one exact record, at most 2,048 UTF-8 bytes before parsing, a 32-character lower-hex token, thirty-minute lifetime, exact declared channel/version/source/base, and the fixed `solo-missions` → `versus` origin pair. It carries only the existing validated campaign/level/theme bookmark and the fixed Missions/Versus focus intent. It has no saved flight, history URL, caller-provided destination or automatic install. The outgoing URL has `return=solo&return-token-v2=…`; the receiving Solo query has only `mode-return-v2=…`. Mixed old/new receiving parameters refuse without consuming either record. A matching incompatible own ticket is consumed once; a different or newer raw record is kept.

Versus reads a validated token without consuming the record. Its existing leave owner constructs the fixed relative Solo URL and captures that scalar token with the actual match/generation decision. Confirmation does not select a newer return. Ready and finished matches retain direct native links; unfinished matches retain A1's paused Stay/discard behavior. Going onward to Team does not forward the Solo hint or create a nested return stack. Missing, stale or incompatible return context reaches Solo's title. A valid return selects the exact installed Missions view and focuses its Versus link only after boot readiness, with existing foreground and focus-ownership guards. It never restores or resumes a saved flight.

Both Solo destinations now retire their pending decision on blur, hidden state, native inactivity or page exit. The cancellation signal and a final foreground/currentness check fence delayed save and hint callbacks. Page exit preserves a successfully prepared return hint while keeping the unfinished-flight save hold; a rejected navigation assignment/currentness check or explicit Stay clears only the owned hint. Modified link gestures remain native and create no departure record.

The A2 tests parameterize the prior complete v1 host scenarios for both destinations and add real Solo → Versus host → Solo journeys, exact checkpoint/save preservation, current-slot failures, storage fallback, cancellation during pending locks and hint writes, actual-opener focus, mixed-query refusal, bounded records and captured Couch-token behavior. The initial retained-navigation run lacked the original Night Shift and Living Threads JSON fixtures (18,004 bytes total); its six failed G cases are retained. Restoring those exact baseline Git leaves changed no runtime or test assertion. Fourteen complete affected files pass 355/355 on Node 22.22.2 and 355/355 on Node 20.19.5, with no skips or cancellations: six return/Couch files contribute 194 and eight retained-save/G/H/J/K/shared-input files contribute 161. Final syntax, lint, formatting and whitespace checks pass, and independent source review found no blocker. The scoped desktop native check is also complete: 18 keyboard observations and four inspected PNGs confirm nondefault retro/Grid + buffer selection, the visible verified-save warning and Stay/Escape opener, explicit fixed Versus departure, receiving Missions focus without flight adoption, and a separate inherited Continue restoring paused at 0:06. The [retained A2 evidence](verification/cross-mode/p03-mode-a2/README.md) distinguishes that visible state from host-tested exact checkpoints and queued turns. Raw event 09 actually focused Team before the next Tab despite its intended Versus label; cut geometry was not inspected. R5 body focus and TitleB’s separate Continue-to-run semantics remain separate. No phase, version, public, offline or physical-device acceptance follows from this source change.

On composition, retain the separate R3 Library launch resolver and the shared `missionReplacement` mutual exclusion. Title Start/Continue owns different shell callbacks and guarded restore adoption; A2 does not change those. Preserve P08's asynchronous picture preparation/Start/cancellation and R5's first-ready focus when merging Couch host hunks. These branches require combined-source qualification; neither a token nor a mode destination can bypass pending picture preparation or become an implicit Start.

## Current P03 Ready focus adaptation

The [minimal R5 adaptation](verification/cross-mode/p03-r5-adaptation/README.md) brings the one-shot first-ready focus contract onto current P03 `c5bccfc`. It keeps the existing await-before-clearing-boot behavior, then focuses Start only for the untouched foreground initial match after boot inertness clears. It does not activate Start or take controller ownership, and it preserves A1/A2 departure and token handling. Fourteen focused cases plus the affected whole files pass 211/211 on each Node 20 and 22. Independent source review and the scoped desktop native check are closed: nine keyboard observations and two PNGs show fresh Start focus, explicit Start/Pause, and a deliberate loading Back choice retiring automatic focus. That Back link later disappears, so BODY focus is expected until another Tab; no invisible-control retention is claimed. Earlier A1/A2 BODY-focus observations and the P08-based R5 receipt remain historical evidence; this source candidate does not rewrite them or qualify the later P08 composition.
