# P5 and Field Kit source integration

This source merge starts from committed `3d399074` (version 0.50) and incorporates runtime checkpoint `8ab6107` plus its dated documentation. It does not read or adopt the separate worktree’s uncommitted version 0.51 changes, create a release, or replace build and native qualification.

Field Kit remains the default typography, categorized Settings, Workshop, mission gallery and single compiled presentation registry. Stored `pixel` is labeled **Theme font**; **Plain** is optional for the current solo shell and renderer. See [text presentation](text-presentation.md).

The one Steering hand control uses the current touch configuration. An existing configuration or explicit `null` retains Field Kit behavior. Only a serialized older profile with an explicit `screenSteeringHand` and no `touchControls` migrates to the same left/right D-pad side. Both fields remain strictly validated; parsing performs no storage write. New profiles retain the current touch default. Actual side changes synchronize the compatible hand field, and existing controls keep DOM focus order aligned with their visible placement.

Collection and its picture retain their actual parent/opener until explicit Replay or another destination is selected. Workshop stays beneath Field Guide; Guide Back returns there, then Workshop Back returns to its menu. Settings recovery stays within the native dialog hierarchy, joins cancellation and URL cleanup before closing, and never steals a writer lease. Original-image recovery is still limited to the exact registered historical channels; newer unknown channels do not gain original-export authority from this merge.

The latest full-viewport touch layout is retained. The older compact-chrome CSS appendices were for the replaced layout and are not applied to shrink the new fullscreen arena. Native warning clearance, both hands/modes, Large text and paused-cut return remain acceptance work.

The renderer keeps compiled geometry in its existing argument position and explicitly separates an optional original body record. Compiled defaults do not also acquire old original-image leases. Manual overrides, explicit skins, original provenance and collision rules remain separate. The Guide consumes the same selected snapshot while preserving paused/reduced timing and its separate impact diagram. Fourteen existing standalone fallback records use the inspected 128px derivatives; their parent original identities and animation settings remain intact, and the thirty compiled defaults are unchanged. The original sources stay in Git. This does not establish small-screen readability or add fourteen new actors.

Arcade help follows the authored automatic-action policy. Manual Supply prompts and its button share the actual capacity/pad predicate. Generic role practice explains its existing stop-after-capture rule; the First Flight and impact lessons retain their authored behavior. These are UI copy changes, without physics or replay changes.

In an unbuilt source preview, recovery lazily reads the validated version from the source build configuration on explicit entry. The display label `dev` is not passed as a historical release identity. Packaged recovery retains its exact version and catalog checks; source previews offer raw diagnostics without original-export catalog authority.

The final 21-file integration run passed all 186 cases on Node 20.19.5, with no failures, skips or cancellations and unchanged test-source inputs. The same 186 named cases passed through the retained Node 22 runs and affected-file reruns. Initial failures from missing recovery status markup, incompatible source-version input, incomplete fixtures and outdated action waits remain recorded; no failed attempt is recast as a pass. Separate focused slices passed 33 renderer/Guide cases, 16 history cases and 15 derivative cases on both runtimes, with their own source scopes retained.

These focused checks do not replace the complete source gates. The wider original-art audits and complete Workshop build checks remain for full-source qualification. The current raw core projection is 65,235,889 bytes, below the unchanged 67,108,864-byte cap; generated packaged accounting is still pending. Native review is underway on the held source preview. Earlier immutable 362b receipts remain scoped to that source; no complete combined artifact, offline, public or physical-device pass is implied.
