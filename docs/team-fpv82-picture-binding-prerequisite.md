# Team FPV revision 82 picture binding prerequisite

## Scope

This unversioned checkpoint is a prerequisite for the planned v0.133 player release.
It does not publish, retag, select, archive, or otherwise change any immutable
release. It closes a production binding drift on the current main branch:

- the compiled FPV presentation is exact revision 82;
- the Team starter-picture authority still named revision 79;
- the approved Orchard and Foundry image bytes, asset revisions, pack identity and
  level identities are unchanged.

The current two-row authority now names revision 82. Revisions 79, 80 and 81 join
the existing retained revision 58–78 identities. Historical imported Team maps gain
the matching finite policies, increasing the closed policy capacity from 22 to 25.
There is still no `latest` alias, theme substitution, collection substitution, or
procedural fallback.

## Provenance boundary

Revision 79 introduced the reviewed shared-screen continuation. Revisions 80, 81
and 82 came from the production-provenance refresh, formatted soundtrack review and
offline-cache budget rebuild respectively. Comparing their compiled manifests proves
that both Team picture identities remain byte-identical:

- `scene.reveal.wide`: `53f1206a11a8791892f5c844c0641529acbc2c09c8d558676d0d801d72113850`
- `picture.fpv.adf5c9eea274ba7f`: `d76f309d8385cd5d20fc2fff72b7f3abc19299cccdde767d76dd9f4a4960929d`

The default Team Journey keeps its separately authenticated mission-original lease.
Its focused contract now reads the compiled production identity and proves that the
accepted binding pins the exact revision 82 snapshot. It does not borrow the starter
picture table or an implicit latest-picture fallback.

## Focused evidence

- Current First Connection and Relay Yard select and verify their real approved PNGs.
- Historical revision 62 still selects both retained pictures unchanged.
- Revisions 58–81 remain explicit immutable bindings and imported-map policies.
- Revision 83, another theme, another collection, substituted metadata and changed
  bytes fail before an image read or procedural fallback.
- The production bundle's current and retained picture/actor identities match, and
  the historical-policy table rejects a twenty-sixth entry.
- The Team Journey candidate-original route retains the exact compiled revision 82
  snapshot through confirmation and drawing.

Browser interaction, native decoding, physical controller/touch operation and full
production suites are outside this prerequisite. They must not be represented as
passing from these focused source tests.
