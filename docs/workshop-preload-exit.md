# Workshop recovery before modules load

A standalone tool needs an exit even when its module graph cannot load. Replay Theater now leaves its static `Game home` anchor available at `../`, the home of the same build. Its contextual `Return to Workshop` stays gated until the independent return module installs the validated route. When that module succeeds, it also updates the already available Game home link with the validated Journey hint. Nothing navigates automatically, changes progress, hides the focused exit or starts a recording.

This restores an actual pre-module recovery contract; changing the static-markup assertion to ignore inert links would have concealed the failure. All nine registered Workshop tools now retain their existing same-edition Game home / Return to game link before scripts run. Their contextual Workshop link remains gated until the independent module validates it. The static fallback cannot retain an unprocessed Journey hint; the ready navigation module adds that hint without taking focus.

A separate failing title-flow fixture omitted native dialog opening events and autofocus. Workshop now correctly retains Home below it, so the real modal owner needs those browser boundaries to establish opening order. Only this test's finite dialog boundary is corrected; production focus rules are unchanged. The journey keeps its actual opener assertion and now checks the complete unstarted simulation checkpoint and stored profile bytes as well.

## Evidence boundary

Exact source `4bb553d06d62f7a8cce301c77b245573838bfdcb` reproduced both hosted shard1 failures locally: 23/25 passed, two failed. The corrected complete `couch-markup` and `field-kit-flow` files pass25/25 on Node20.19.5 and22.22.2. The complete Workshop return, Replay navigation and Replay display-host files are separate supporting coverage.

Actual desktop browser verification used two isolated source-pinned origins. With all JavaScript requests deliberately refused, the loading status stayed visible and the first Tab selected Game home; Enter reached the same immutable-build game boot route. This does not claim the deliberately script-disabled game became playable. With normal requests, Replay settled, the Workshop link retained `journey=opening&workshop=replay-theater`, Game home retained `journey=opening`, and the latter remained keyboard reachable with a45-pixel-high target at1280×720.

These are local desktop keyboard and finite-host results. Final integrated six gates, source identity, frozen release, deployed bytes/public journeys, physical controls, mobile and offline checks remain separate. Preserve the original hosted failures and do not restart qualification until the complete corrective source is ready.

## Integrated correction

Motion Lab's five hosted focus failures had the same cause: its static game exit was inert before the independent return script ran. The corrected complete Motion display-host and still-workshop-build files pass55/55 on Node20.19.5. The miniature workshop build now explicitly includes the HTML-selected return entry and CSS, with existing transitive import copying; production build validation remains unchanged. The initial local Git-fixture adapter omitted a regular-file size and used a mutable global URL constructor; that setup failure is retained separately from hosted product failures.

The combined Replay markup, title flow, nine-tool return, Replay navigation and display cohort passes58/58 on Node20.19.5. The new nine-tool check requires an available static exit that resolves to the same edition both locally and under a versioned route. Native Motion failure-mode testing refused all five selected scripts, then Tab/Tab/Enter reached the same-origin game boot route. That origin intentionally also refused shared scripts; it verifies the exit, not a playable game after an induced script failure.
