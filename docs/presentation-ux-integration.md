# Combined presentation and navigation candidate

This source candidate combines [steering hand](device-controls.md), title Settings return, [Collection result labels](earned-picture-view.md), [seven FPV enemy presentations](fpv-enemy-presentations.md) and [seven Ukraine roles](ukraine-role-presentations.md). It starts from the current delivery documentation atop frozen v0.41.0 source; it does not modify that frozen edition or bump the version. The [touch and readability acceptance](research/2026-09-14-touch-and-role-readability.md) separates browser layout, surface accents, mechanical animation and physical-device work.

The selected runtime additions retain fourteen exact original PNGs, totaling 12,396,181 bytes. The two additive Ukraine source commits preserve all original and rejected variants in Git; only seven selected Ukraine bodies are included by the build configuration. Existing seven FPV player originals, all earlier owners and installed/download caps remain unchanged.

Three cherry-pick conflicts were resolved by preserving the existing 0.41 configuration and appending the seven Ukraine paths, composing the exact workshop allowlist expectation, and retaining both character-authoring instructions. Two real integration issues were then addressed:

- The production register still pins the earlier actor renderer. [Text history v2](../authoring/production/README.md#preserve-metadata-revisions) retains its exact 25,033 bytes, while v1 remains JSON-only. Historical modules are read as data and never executed; old register pins and approvals remain unchanged.
- Field Guide still painted its earlier vector body. It now uses flight's shared selected-role image lease and painter, retaining the original vector fallback and line-impact diagram. Separate artwork status preserves practice errors. Closing, changing topic/theme, hiding the page and entering practice release that lease; asynchronous completion does not advance the sampled clock.

Music Studio now says **Close studio**, accurately describing the same existing handler. Closing Settings or tools still requires explicit Resume before flight continues.

## Focused verification

The source/test runtime used by the final checks is `362fc5cb85e40ddf98067a1aa247f63895efee43`; subsequent integration documentation and the source-history authoring instruction do not change runtime or tests.

| Recorded scope                                         | Node 22                                                 | Node 20                                            |
| ------------------------------------------------------ | ------------------------------------------------------- | -------------------------------------------------- |
| Initial 16 complete files                              | 156/160; four historical-renderer pin failures retained | Not repeated before correction                     |
| Corrected history and complete five-case workshop file | 14/14                                                   | Included below                                     |
| Complete Field Guide lifecycle file                    | 15/15                                                   | 15/15                                              |
| Final 17-file union                                    | Passing unchanged cases retained from initial run       | 168/168                                            |
| Selected freshly pinned metadata-forgery controls      | 6/6                                                     | 6/6; twelve nonselected cases reported as filtered |

This composes 189 distinct focused checks on each supported runtime, not a fresh all-suite result. The initial run took 89.48 seconds; corrected history/workshop 2.00 seconds; final Node20 union 94.58 seconds. The tested source stayed unchanged within each execution. Scoped syntax, lint and format checks supplement these results.

Finite current-Git materialization supplies the host imports, named allowlist inputs and the workshop's small toy packages. The five workshop cases do not certify a full candidate namespace or build. No full source checkout, candidate build, original media producer or archived route proof was rerun. Local records are under `.cache/round47/presentation-ux-integration/`, including the initial failure, correction, final runtime output and exact source/fixture pins.

## Browser and release limits

The fixed preview uses the selected changed files over read-only frozen v0.41.0 dependencies. Its separate loopback origin uses the source `dev` profile, blocks service workers and supplies no offline preparation. The viewport lab is desktop iframe layout evidence. Root's native review records are separate under `.cache/round47/presentation-integration-native/`; source tests do not certify those interactions.

A metadata-only core projection is 65,262,842 bytes and 304 files, leaving 1,846,022 bytes beneath the unchanged 64 MiB cap. It preserves prior generated-byte overhead; an actual successor build must establish its own generated bytes and complete inventory. Full source gates, frozen/package/public delivery, offline behavior, physical controllers and real phones remain separate qualification steps. Core, replay, saved-flight, campaign and earned ownership identities are unchanged.
