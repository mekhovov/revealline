# Native launch and startup failures

A standalone generated distribution root opens that edition's game title directly. The v0.29.1 Pages correction routes public root entry into the complete immutable edition instead of running a changing module graph at `/game/`. The optional project overview, pack cards and release picker remain at `site/about.html`; they are no longer a stop before playing. Source checkout preview is `/site/`, while `/game/` remains the direct game address.

About's build picker prefers a validated `canonicalPlay` URL when present and otherwise uses the version-relative `play` route. Only bounded HTTPS archive-game URLs or version-relative game paths are accepted; script schemes, credentials, traversal and arbitrary destinations are rejected. Choosing a build never navigates until the player activates **Open build**. Old bridge routes remain usable when no canonical entry is supplied.

`site/launch.mjs` forwards HTTP(S) entry pages to the game on the same origin using its own script URL. This preserves a Pages project prefix and nested frozen-release paths. Query parameters and the fragment survive forwarding. A downloaded `file://` page never redirects externally; its **Play online** link is an explicit player action. The static page explains the local-server alternative.

`game/index.html` starts with a small inline dark styling guard. Until `data-boot-state` is `ready`, every direct body child except the launch screen and scripts is concealed. Existing game controls are also inert. This prevents an empty mission selector, unfinished HUD or legacy webpage layout from appearing while startup is pending or broken. The static launch screen and its links remain usable when JavaScript is disabled or the bootstrap itself cannot be downloaded.

`game/boot.mjs` is deliberately loaded as a **classic external script**, despite the file extension. Its dependency-free launch and failure UI therefore works before ES-module imports, including directly opened files. After the document and renderer arrive, it imports `app.mjs` and catches import rejection. A renderer or stylesheet load error also preserves the launch screen. If the app import resolves without calling `ready()`, startup fails visibly: status becomes visible again, game regions are made inert and partial dialogs close. Do not infer readiness from old DOM mutations. A genuinely pending import/top-level await keeps loading; the message changes after 15 seconds without inventing an error or retrying in a loop. This handshake diagnoses incomplete startup; it does not repair a mixed module graph.

The game host calls `RevealLineBoot.ready()` only on successful setup and `fail(error)` on initialization failure. Readiness removes the inert guard; failure never does. The host refreshes reading/focus after revealing the native menu, since controls concealed during initialization cannot receive normal browser focus. Scene updates remain blocked while a boot controller exists but its state is not ready. No simulation, replay format or score authority is changed.

The launch screen uses ordinary links and expandable local-server guidance, keyboard direction navigation and neutral-gated standard controller input. Native keyboard or pointer use prevents an old held controller direction from reclaiming focus. The boot controller stops its scheduler on readiness. Its static pixel bars do not need motion effects. Error help is allowed to scroll even after a partial game-shell setup.

The game runtime's public/native Content Security Policy permits the external scripts and inline **styles** used by its bootstrap. That bootstrap has no inline JavaScript, eval, automatic external redirect or new file-system permission. The separately generated Pages aliases below have their own dependency-free inline navigation and ordinary-link fallback. The build includes and hashes boot assets in the same offline inventory as the rest of the edition; historical release artifacts are not rewritten. Native platform runtime qualification remains separate.

## Immutable public entry — P7.7

v0.29 passed exact-source and public network-byte checks, yet a previously visited mutable `/game/` browser hung with new launch HTML and a hidden status, consistent with cached v0.28 app completion. The [immutable v0.29 permalink](https://mekhovov.github.io/revealline/releases/v0.29.0/site/game/) boots. Keep that failure and the frozen release unchanged. The v0.29.1 entry source is frozen and artifact-verified, but actual ordinary capture crashed online and with the server stopped. The [v0.29.1 record](verification/round-38/v0291-entry.md) preserves this failure; [v0.29.2](verification/round-39/v0292-capture.md) is the current entry/capture release gate.

The public main artifact replaces each latest-edition **root HTML entry** with a self-contained alias to the identical suffix under the same-origin versioned prefix, for example `/revealline/releases/v0.29.1/site/game/`. Navigation resolves the target relative to the actual entry URL, so the same artifact works on localhost and under a Pages project prefix. The scripted redirect preserves query and fragment. A keyboard-accessible **Play** link remains when JavaScript is disabled or a stricter local-server CSP blocks inline navigation; that static fallback opens the edition without promising query/fragment forwarding. No external script or stylesheet is needed by the alias. The full HTML/module/style/content graph then belongs to one immutable edition. Versioning only the first script or adding a cache-busting query does not provide that guarantee. Source checkout and standalone ZIP/native staging still enter their own local distribution normally.

`current-entry-routing.json` and canonical HTML metadata record the published GitHub identity; navigation does not hard-code that origin. The record lists every mutable-root body override with source/output hashes. Root non-HTML files remain for compatibility. Current and historical versioned trees retain their exact runtime files; archive bridges retain their separately recorded contract.

Only the mutable root `service-worker.js` becomes a finite HTML-route retirement worker. Its scope comes from its own script URL and must equal the registration scope; only known same-origin GET navigation routes are handled, including directory/index/slashless forms. It does not intercept `/releases/`, access or delete caches/profile/IndexedDB, call `skipWaiting` or `clients.claim`, or navigate a live tab. Normal activation unregisters that root registration. An already active root worker can still serve its old entry until the normal lifecycle permits retirement; users may need to close all mutable-root tabs and reopen. The explicit immutable link is available directly. Do not clear site data or force takeover to obtain a passing migration check. Re-prepare offline files explicitly at the selected immutable distribution scope; old saved/profile bytes are not migrated to another origin.

## Focused verification

```sh
node --test game/test/boot.test.mjs game/test/boot-host.test.mjs game/test/boot-build.test.mjs game/test/content-launch.test.mjs
```

The classic-script VM checks exercise the actual boot source with finite DOM/time/controller boundaries. Its loader cases include actual temporary ES modules that omit readiness, await readiness or reject; these are not browser network/cache evidence. Actual-app host checks cover failed content initialization retaining inert controls and saved bytes, plus a successful paused title. Build checks use actual entry/boot/site source with small unrelated dependency fixtures, native policy transformation, reproducible output and exact offline inventory hashes. They do not certify raster layout, disabled-JavaScript browser behavior or physical controllers.

Before a release, inspect fresh and previously cached browsers at the public root and direct mutable game entry, follow them to the immutable graph, and separately inspect the standalone generated root. Include a directly opened `game/index.html`, disabled JavaScript, and an actual blocked/missing game module. Confirm dark first paint, no usable game controls on failure, readable/scrollable launch help, and working keyboard focus after successful startup. Record actual document/module URLs, saved-flight continuation, normal worker migration and offline preparation at the new scope. Continue through an ordinary first capture and further flight with a clean console; title/restore success alone is not an offline gameplay pass. A successful network inventory alone does not prove these journeys. Repeat at narrow portrait and short landscape sizes. Do not equate these checks with physical-device or native-store qualification.

Round 38's actual HTTP browser check reached the native title with ready state and working focus. The in-app browser's security policy denied direct `file://` navigation, so the actual-file browser check remains unavailable; the passing file-path checks are finite modeled evidence only. Do not bypass that restriction through another browser surface or protocol. A separately owned HTTP fixture with a refused module can qualify a network-failure path, but does not prove direct-file behavior or disabled-JavaScript rendering.

## Authoring and device contract

Every theme, content pack and interface change must preserve the immutable public entry contract above and the same launch/error surface and the separation between the native player menus and authoring/marketing tools. A missing image or failed module is not permission to expose empty selectors, a legacy flight deck or placeholder controls. New visuals must preserve readable focus, working links, contrast, safe-area padding and scrollable failure help, including a partially initialized shell.

Derive available gameplay actions and their hints from the **effective authored action policy**. Direction-only Arcade editions must not advertise manual abilities, supply collection or Boost merely because a selected class has those recipes; classic contact pickups remain separate. Tactical and archived editions retain their authored actions and identities. Do not rewrite old replays, saved runs or historical proof inputs when changing a current edition's teaching text or menu presentation.

Keep keyboard, pointer, touch and controller access explicit. Confirm/Back and focused menu controls must not issue flight commands; old held inputs cannot reclaim control after a newer device input. Refresh focus and reading semantics after hidden launch UI becomes visible. Check both turning policies and the actual active control layout. Mouse coordinates, a resized desktop viewport and simulated Gamepad samples are different evidence from real phone/controller operation.

For each advertised device class, record the browser/OS, CSS viewport, input used, readable target/text dimensions and the journey exercised. Include narrow portrait, short landscape, desktop and handheld layouts; test safe areas and Large text. Record failures and unavailable hardware honestly. A canvas fixture, generated picture, prepared soundtrack file, passing source test or native wrapper build cannot stand in for browser delivery, listening, physical input or player enjoyment.
