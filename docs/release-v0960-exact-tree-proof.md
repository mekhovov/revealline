# v0.96.0 exact-tree build proof

This evidence-only file triggers pull-request CI. It is not part of the frozen release.
The proof parent is the immutable v0.96.0 source commit, and every non-document tracked byte is identical to that source.
