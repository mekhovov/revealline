// Selected editions accept the player's recordings through the ordinary theater.
// Unselected official example recordings are not a dependency of that tool.
export const REPLAY_EXAMPLES = Object.freeze({});
